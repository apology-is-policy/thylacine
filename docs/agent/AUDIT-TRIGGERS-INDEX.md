# Audit-trigger index

> Moved verbatim from `CLAUDE.md` on 2026-09-23 (the CLAUDE.md trim). One entry per row of `docs/AUDIT-TRIGGERS.md`; grep this or that file by path, then read ONLY the matching row window.
> Section headings keep their original levels.

- **Exception entry + EL0-entry trampolines** -- `arch/arm64/start.S`, `arch/arm64/exception.c`, `arch/arm64/vectors.S`, `arch/arm64/userla ...
- **Halls of Extinction crash dump** -- `arch/arm64/halls.c`, `arch/arm64/halls.h`, `arch/arm64/exception.c` (the four entry wrapp ...
- **Page fault + COW + W^X** -- `arch/arm64/fault.c` (`userland_demand_page` holds `p->vma_lock`), `kernel/syscall.c` (`SY ...
- **REVENANT file-backed demand-paged exec (the Image cache + the `BURROW_TYPE_FILE` fault arm)** -- `kernel/exec.c` (`exec_setup_from_spoor` + `exec_read_header` [OOB header-buffer guard] + ...
- **Boot-time LSE alternatives-patcher (Lazarus W1.5)** -- `arch/arm64/alternatives.{c,h}` (the `ALTERNATIVE()` macro emitting `.altinstructions` + ` ...
- **GICv2 driver + EL1 virtual timer (Lazarus W2)** -- `arch/arm64/gic.c` (the new GICv2 path: `gic_init_v2` + `dist_init_v2` + `gic_cpu_config_v ...
- **Kernel CSPRNG (software-RNG, Lazarus W3)** -- `kernel/chacha20.{c,h}` (the pure ChaCha20 keystream primitive), `kernel/random.c` (the fo ...
- **Hardware allowance / I-34 (Menagerie build-arc 2)** -- NEW `kernel/include/thylacine/allowance.h` (`struct Allowance` { `mmio[]` PA windows + `ir ...
- **devpci: mediated PCI topology + the devhw synth child (Menagerie 6b-1)** -- NEW `kernel/devpci.c` (`dc='P'`, name="pci"; the devdev/devctl read-only dir-Dev: reuse-`n ...
- **dev9p.poll readiness bridge + the 9P-client async reply-dispatch (net-6b-2b)** -- NEW `kernel/dev9p_poll.c` (the `dev9p_poll` Dev `.poll` slot + the global poll-pump kthrea ...
- **netd: the network daemon -- smoltcp embedding + NIC ownership (NET-DESIGN.md the #68 charter)** -- NEW `usr/netd` (the warden-bound `virtio-pci:1` driver: `impl libdriver::Driver`; the smol ...
- **Syscall byte-I/O staging + bulk uaccess + the per-service bulk ring (CF-3 A+B)** -- `kernel/include/thylacine/syscall.h` (`SYS_RW_MAX` 4096 -> 128 KiB + the new `SYS_RW_STACK ...
- **Allocator** -- `mm/buddy.c`, `mm/slub.c`, `mm/magazines.c`
- **Userspace virtio-PCI transport (`KObj_PCI` + the 3 PCI syscalls; pci sub-arc)** -- `kernel/pci_handle.{c,h}` (the `KObj_PCI` kobj: exclusive per-(bus,dev,fn) `g_pci_claims` ...
- **ASID generation-rollover (HOLOTYPE RW-1 B-F1)** -- `arch/arm64/asid.{c,h}` (the rolling allocator: `asid_generation` + the ASID bitmap + per- ...
- **Scheduler (SMP redesign, deep-smp-review 2026-06-05; #360 preemption discipline 2026-07-04; #33 SYS_YIELD 2026-07-05)** -- `kernel/sched.c` (the `on_cpu` protocol + the multi-step switch + `try_steal`/`pick_next` ...
- **Tickless idle (NO_HZ_IDLE; the TI arc)** -- `arch/arm64/timer.c` (`timer_arm_oneshot_cnt` + the pure `timer_oneshot_tval` -- the one-s ...
- **Territory** -- `kernel/territory.c`
- **Handle table** -- `kernel/handle.c`
- **VMO / BURROW** -- `kernel/burrow.c`, `kernel/include/thylacine/burrow.h`
- **Weft cross-Proc Burrow-share (the I-37 dataplane substrate)** -- `kernel/burrow.c` (NEW `burrow_share_into(dst, v, vaddr, prot)` -- map an existing Burrow ...
- **tapestryd: the compositor + the orphaned-weave reaper (Tapestry G-3; I-40 present half)** -- `usr/tapestryd/` (the warden gather-bound persistent compositor: `gpu.rs` the synchronous ...
- **tapestryd V-3a coherent ring (I-9 kick + I-45 ring scope + the drain-cap DoS bound; Warp-6 V-3a)** -- `usr/tapestryd/src/server.rs` (the `ctx/<id>/ring/<ridx>/{info,map,kick,fence}` subtree: `WARP_RING`=1<<43 + the disjointness assert; `wring_mint`/`wring_kick` [I-9 re-scan + the `WARP_RING_MAX_DRAIN_PER_KICK` cap]; the fence park + `poll_ring_fences`; `wring_teardown`; the ring test levers), `usr/tapestryd/src/gpu.rs` (`create_ring_blob`), `usr/warp-prove/src/main.rs` (`ring_prove`) ...
- **tapestryd V-3b-1a HOST3D + MAP_BLOB ring substrate (Model B; venus-ctx scope; I-45/I-32; Warp-6 V-3b-1a)** -- `usr/tapestryd/src/gpu.rs` (`create_host3d_blob` [HOST3D nr_entries=0 -> HDR+32] + `map_blob` [MAP_BLOB -> hostmem_base+offset, `RESP_OK_MAP_INFO` residue-guarded] + `unmap_blob` + the `host3d_probe` 2-arm venus-ctx/device-global self-test + the `HOST3D_PROBE_*` static-assert), `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (the venus-verdict host3d gate) ...
- **tapestryd V-3b-1b hostmem guest-map + the SYS_BURROW_FROM_HOSTMEM client binding (Model B; I-45/I-32/I-37; Warp-6 V-3b-1b)** -- `usr/lib/libthyla-rs/src/lib.rs` (`t_burrow_from_hostmem` FFI + `T_CACHE_*` consts, the V-3 client wrapper V-2 left unbuilt) + `usr/lib/libthyla-rs/src/hardware.rs` (`PciDev::burrow_from_hostmem`) + `usr/tapestryd/src/gpu.rs` (`HostmemAllocator` + `hostmem_map_probe` [host-dictated cache via `map_info_to_cache`; sentinel round-trip; `t_burrow_detach`] + the `HOSTMEM_PROBE_*` static-assert), `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (the venus-verdict hostmem gate) ...
- **tapestryd V-3b-1c-1 persistent hostmem ring engine (Model B; I-45/I-32/I-7; Warp-6 V-3b-1c-1)** -- `usr/tapestryd/src/gpu.rs` (`HostmemAllocator` first-fit free-list + oob/overlap double-free guard, hoisted into a persistent `Gpu.hostmem`; the reusable `mint_host3d_ring`/`drop_host3d_ring` lifecycle [`HostRing` non-Copy single-use token, by-value drop, full error-unwind, host-dictated cache, `u32::try_from` size guard] + the 2-ring/physical-reread/re-mint-reuse `hostmem_ring_probe` + `HOSTMEM_PROBE_RES_2`), `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (the venus-verdict hostmem-RING gate, 24/24) ...
- **tapestryd V-3b-1c-2a server host3d-ring path (Model B; I-45/I-32/I-7; Warp-6 V-3b-1c-2a)** -- `usr/tapestryd/src/server.rs` (`WarpCtx.venus_ctx` lazy per-client capset-4 ctx [`wctx_venus_ensure`, id=`WARP_VENUS_CTX_BASE`+slot, disjoint-band static assert -- the conv-probe alias catch]; the `host3d` ring flavor [`wring_mint`/`wring_install_host3d` via the 1c-1 engine, shared I-32 budget]; `wring_teardown` host3d arm [non-Copy move + early-return, no double-unref]; `wctx_finish` venus destroy [both arms, F1]; `wring_kick` host3d fail-closed guard; `warp_host3d_selftest`), `usr/tapestryd/src/gpu.rs` (`ctx_create_venus`), `usr/tapestryd/src/main.rs` (serve() self-test call), `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (28/28) + `tools/warp/boot-probe.sh` (capture filter for the `warp host3d-ring` line) ...
- **tapestryd V-3b-1c-2b client-claimable host3d ring: F1 weft arm + SYS_HOSTMEM_REFCOUNT + F2 observe-and-reap (Model B; I-7/I-37/I-45/I-32; Warp-6 V-3b-1c-2b)** -- NEW `SYS_HOSTMEM_REFCOUNT`=108 (`kernel/syscall.c` `hostmem_refcount_query` [va->hostmem VMA->handle+mapping sum, leak-closed] + `syscall.h` + `libthyla-rs` `t_hostmem_refcount`); F1 `kernel/weft.c` `weft_binding_alloc_maponly` HOSTMEM arm; F2 `usr/tapestryd/src/gpu.rs` (`retire_host3d_ring` reap-at-refcount==1-else-PARK + `reap_hostmem_parked` mint-reclaim + cap) + `server.rs` (`wring_weft_ensure` host3d share + `wring_teardown` disarm-then-retire); tests `weft.hostmem_share` + `weft.hostmem_refcount` (the F1 in-flight-claim window). Fable audit round-1 0/1P1/1P2/1P3 all-fixed (F1 = mapping_count missed the transferred claim pin -> the SUM); DIRTY -> round-2 on the fix ...
- **tapestryd multi-queue submit-fence (the F3 seam): per-timeline venus fences + the timelines file + the non-parking submit (I-45/I-9/I-7; Warp-6 GPU-submit chunk)** -- `usr/tapestryd/src/gpu.rs` (`FenceTag.ring_idx` + INFO_RING_IDX + the vindication lane retention), `server.rs` (`timeline_signaled[4]` + `ctx/<id>/timelines` + `submit1..3` + `poll_fences` DELIVER-TO-ALL), mesa patch 0015 (per-timeline ledgers + the transport mutex + the one-parker protocol + `max_timeline_count`=4), `tools/warp/boot-probe.sh` ...
- **tapestryd W-3c-1: the PRESENTABLE object -- the `img/` subtree + the display-safe teardown (I-40 fourth class / I-45 / I-32 / I-7; Warp-WSI W-3c-1)** -- `usr/tapestryd/src/server.rs` (`WarpImg` + the `ctx/<id>/img/{new,<n>/{info,ctl}}` ABI + `wimg_teardown` unbind-BEFORE-unref) + `gpu.rs` (`create_presentable` USE_MAPPABLE never-mapped + `set_scanout_blob`), the capture/verdict/driver tools ...
- **tapestryd W-3c-2: the generalized `present-to` (bo | img) + the presentable DIRECT arm (I-40 pbound / I-45 / I-7; Warp-WSI W-3c-2)** -- `usr/tapestryd/src/server.rs` (`PresentSrc`/`AdoptSrc` + `gl_adoption` img arm + `direct_bind_adopted` [PPresentBind] + composed machinery HARD-GATED to Bo [the va-0 DMA class] + `wimg_destroy` consent-clear), `usr/warp-prove/src/main.rs` (img-xproc + `img_direct_prove`), `tools/warp/warp-img.exp` + `tools/warp-host.sh` ...
- **tapestryd W-3d slice 1a: the one-shot REAL-class compose probe (SET_TYPE; I-45/I-7; Warp-WSI W-3d-1a)** -- `usr/tapestryd/src/server.rs` (`set_type_request` + the `settype` probe param + `real_class_compose_probe_maybe` at the first mem mint), `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (the two-token grammar gates). MEASURED: `settype=latched` on thyla-pi (opaque-fd blobs; composed arm host-unavailable) ...
- **mesa W-3d: the WSI DIRECT path (patch 0018; I-40 stage-0 / I-45 / I-7; Warp-WSI W-3d)** -- mesa `vn_wsi_thylacine.c` (NEW: headless-slot wsi_interface; marked no-bo dedicated allocs; img registration; the throttle-fence stage-0 bracket) + `vn_device_memory.{c,h}` (the THLW marker + routing) + `vn_renderer_thylacine.c` (img family + poke + the bo-create counter) + `warp_client.{c,h}` + `meson.build` (VN_USE_WSI_PLATFORM now ON) + `thylacine_prove.c` step 10, `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (the `wsi swapchain OK` witness, 83 checks) ...
- **W-3e: the SDL2 Vulkan glue + the img poke-completion + the first-Vulkan-frame witness (I-40 pbound / I-45 / I-7; Warp-WSI W-3e)** -- `usr/ports/sdl2/thylacine/SDL_thylacinevulkan.{c,h}` (the 5 hooks; WEAK venus externs; the arming CreateSurface: glsrc surface-half THEN set_surface ctx-half; the `-u vk_icdGetInstanceProcAddr` linking model) + `SDL_thylacinevideo.c` (slots wired), `usr/tapestryd/src/server.rs` (`img_poke_complete` -- the img family's present-COMPLETE rides the poke; `flip_in_place`), mesa patch 0020 (`vn_renderer_thylacine_warp_ctx_pub` + `thylacine-vk-sdl-prove`), `usr/joey/joey.c` + `tools/build.sh` (the boot probe + staging), `tools/warp/boot-probe.sh` + `tools/warp-host.sh` + `tools/test-venus-verdict.sh` (both witness halves: app PASS + `scanout direct N img res R`; 89 checks) ...
- **WEAVE-SKEIN: the scatter-gathered weave (`SYS_DMA_SEGMENTS`=110 + the fail-closed `SYS_DMA_MAP`; I-45/I-40/I-7/I-32/I-34)** -- `kernel/include/thylacine/dma_handle.h` (`struct dma_block` + `SKEIN_BLOCK` + the inline `blk[]`/` ...
- **MMU user-PTE clear + TLBI** -- `arch/arm64/mmu.c::mmu_uninstall_user_pte / mmu_uninstall_user_range`, `kernel/burrow.c::b ...
- **Errno ABI surface + `snare:*` fault-note family** -- `kernel/include/thylacine/errno.h` (T_E_* registry; ABI-pinned by `_Static_assert`s to POS ...
- **Memory-model defense-in-depth (F3 + F4 + F5)** -- `mm/phys.c::phys_init` (RAM cap), `mm/phys.c::alloc_pages` (KP_ZERO barrier), `arch/arm64/ ...
- **9P client (pipeline restoration, #841)** -- `kernel/9p_client.c`, `kernel/9p_session.c`, `kernel/9p_transport.c`, `kernel/9p_attach.c` ...
- **Pipe wait/wake** -- `kernel/pipe.c`
- **poll** -- `kernel/poll.c`, `poll.h` (register-then-observe; since 2026-09-21 the RE-ARM: a wake is a hint, every pass RE-REGISTERS, 0 only at the deadline; the loop owns death + stop; list locks irqsave; the preemption point (`sched_preempt_point`, each re-loop, 2026-09-22); `specs/poll.tla`) ...
- **SrvConn endpoint poll: BOTH endpoints + the four-edge wake (ARCH 23.3)** -- `kernel/srvconn.c` (`srvconn_poll(cn, client, ...)` + a list walk after EVERY ring mutation), `kernel/devsrv.c` (`devsrv_poll`'s `CSRVCLIENT` dispatch), pouch `poll()`'s stream-socket shaping ...
- **Notes / signals** -- `kernel/notes.c`, `kernel/devnotes.c`, `kernel/include/thylacine/notes.h`, `kernel/proc.c` ...
- **Capability checks** -- All syscall entry points
- **KASLR / ASLR** -- `arch/arm64/start.S`, `arch/arm64/kaslr.c`
- **ELF loader** -- `kernel/elf.c`
- **ELF loader: ET_DYN placement + AT_ENTRY (DISTRO D-2)** -- `kernel/elf.c` (the `e_type` gate + the one-place PIE bias), `kernel/inclu ...
- **`AT_HWCAP` exec-auxv CPU-feature word (the CF-4 A AEAD lever)** -- `arch/arm64/hwfeat.{c,h}` (`g_hw_features.linux_hwcap` — the Linux-uapi-numbered word deri ...
- **`burrow_attach` / `burrow_detach`** -- `kernel/syscall.c` handlers, `kernel/burrow.c`, `kernel/vma.c`
- **Overcommit memory: lazy-anon demand-zero + decommit + the I-32 VMA-count axis** -- `kernel/syscall.c` (a NEW `sys_burrow_attach_lazy_for_proc` + handler — eager `sys_burrow_ ...
- **`torpor_wait` / `torpor_wake`** -- `kernel/torpor.c`, `kernel/syscall.c` handlers, `arch/arm64/uaccess.S` (new `uaccess_load_ ...
- **`thread_spawn` / `thread_exit` / multi-thread exit** -- `kernel/thread.c::thread_create_user`, `arch/arm64/context.S::thread_user_trampoline`, `ke ...
- **pouch AF_INET socket-compat boundary-line (net-5)** -- `usr/lib/pouch/patches/0016-pouch-net-sockets.patch` (the 11-file musl stacking patch, on ...
- **pouch pthread boundary-line** -- `usr/lib/pouch/patches/0004-pouch-pthread.patch` (the 8-file boundary-line patch against v ...
- **argv pass-through to spawn** -- `kernel/syscall.c` (new SYS_SPAWN_* with argv buffer OR extended SYS_SPAWN_WITH_PERMS), `u ...
- **stratumd HW-cap spawn** -- `usr/joey/joey.c` (calls `t_spawn_with_perms` granting `CAP_HW_CREATE` to stratumd), `kern ...
- **stratumd virtio-blk driver arm** -- (Stratum branch `thylacine-pouch-arm`) `src/io/bdev_thylacine.c` (~500-800 LOC port of the ...
- **native fstat + lseek + pread/pwrite + wstat-posture surface** -- `kernel/include/thylacine/syscall.h` (SYS_FSTAT=50 + SYS_LSEEK=51 + **SYS_PREAD=85 + SYS_P ...
- **pouch abort -> _Exit override** -- `usr/lib/pouch/patches/0011-pouch-abort.patch` (overrides musl's `src/exit/abort.c` to `_E ...
- **Stratum bdev_thylacine rights mirror** -- (Stratum-side `thylacine-pouch-arm` branch) `src/block/bdev_thylacine.c` `T_RIGHT_SIGNAL` ...
- **pouch mallocng assert -> _Exit override** -- `usr/lib/pouch/patches/0012-pouch-mallocng-crash.patch` (overrides mallocng's `src/malloc/ ...
- **Thylacine mkfs RNG seed pinning** -- `tools/build.sh::build_stratum_pool_fixture` (THYLACINE_MKFS_SEED + THYLACINE_MKFS_PRESERV ...
- **9P-srvconn transport adapter** -- `kernel/9p_srvconn_transport.{c,h}` (new; parallel to `kernel/9p_spoor_transport.{c,h}`); ...
- **`SYS_ATTACH_9P_SRV` syscall** -- `kernel/syscall.c::sys_attach_9p_srv_handler` (new; parallel to `sys_attach_9p_handler`), ...
- **`SYS_PIVOT_ROOT` syscall + `territory_pivot_root`** -- `kernel/syscall.c::sys_pivot_root_handler` (new), `kernel/territory.c::territory_pivot_roo ...
- **Mount-table SHED at pivot / chroot (#80; ARCH 9.6.10)** -- `kernel/territory.c` (`territory_shed_unreachable_locked` + the pivot/chroot call sites), `dev.h` (`Dev.devno_per_walker`) + `kernel/devenv.c`, `usr/joey/joey.c` (the `/hw/pci` re-graft), `kernel/stalk.c` + `kernel/spoor.c` (every base-time consult of `union_snap->point` is a SEED here), `specs/territory_shed.tla` + `specs/check-territory-shed.sh` ...
- **`kernel_attached` SrvConn gate (16c-integration)** -- `kernel/srvconn.{c,h}::srvconn_set_kernel_attached` + `srvconn_is_kernel_attached`; `struc ...
- **Host-side pool populate via existing `stratumd + stratum-fs` (host build infra)** -- `tools/build.sh::build_stratum_pool_fixture` (orchestrates stratumd start, stratum-fs writ ...
- **FS-mutation syscalls (create / fsync / readdir)** -- `kernel/syscall.c` (`sys_walk_create_handler` / `sys_fsync_handler` / `sys_readdir_handler ...
- **FS-mutation syscalls (rename / unlink) -- FS-gamma** -- `kernel/syscall.c` (`sys_rename_handler` / `sys_unlink_handler`), `kernel/dev9p.c` (new `d ...
- **File metadata: owner/group + chmod/chown (A-2a)** -- `kernel/include/thylacine/syscall.h` (`struct t_stat` 72 -> 80 with `uid`@72 + `gid`@76 + ...
- **Kernel rwx enforcement layer (A-2d)** -- `kernel/syscall.c` (`perm_check` insertions: `sys_walk_open_handler` [X on src + R/W on ta ...
- **O_PATH byte-I/O block (`CWALKONLY`, #81)** -- `kernel/include/thylacine/spoor.h` (new `CWALKONLY` flag in the Spoor `flag` field), `kern ...
- **A-3: 9P identity presentation + dev9p enforcement activation** -- `usr/lib/pouch/patches/0006-pouch-sockets.patch` (SO_PEERCRED shim: `ucred.uid = info.prin ...
- **Group termination / cross-thread shootdown (`SYS_EXIT_GROUP`)** -- `kernel/proc.c` (`proc_group_terminate` + the single set-once `group_exit_msg` on `struct ...
- **Universal death-interruptible sleep (`*_INTR`)** -- `kernel/sched.c` (`sleep`/`tsleep` generalized register-then-observe of `group_exit_msg` + ...
- **A-4 capability model + legate elevation (`rfork` I-2 strip; `cap` device clearance grant/redeem)** -- `kernel/include/thylacine/caps.h` (`CAP_GRANT_CLEARANCE`=1<<6 fork-grantable; `CAP_DAC_OVE ...
- **A-4b cross-process kill (`/proc/<pid>/ctl` + `CAP_KILL`)** -- `kernel/devproc.c` (the `ctl` write parses `kill`/`killgrp` -> `proc_group_terminate` unif ...
- **A-4c trusted path: kernel console RX + SAK** -- `arch/arm64/uart.c` (RX IRQ + IMSC.RXIM unmask + RX-FIFO drain + `DR.BE` BREAK detect; PL0 ...
- **`/dev` namespace front-door + the I-27 gate-at-namespace-open (#57b)** -- `kernel/devdev.c` (NEW: the aggregating directory Dev, dc='d', name="dev"; `.attach` -> QT ...
- **Interactive Ctrl-C: `interrupt` as a real note (LS-5)** -- `kernel/include/thylacine/syscall.h` + `kernel/syscall.c` (new `SPAWN_PERM_CONSOLE_OWNER` ...
- **A-5 login + session lifecycle + per-user encrypted home** -- NEW `usr/login/` (native `/sbin/login`, libthyla-rs: SAK-gated `/dev/cons` prompt -> corvu ...
- **A-5c RECOVER recovery keyslot (corvus)** -- `usr/corvus/src/main.rs` (the new `VERB_RECOVER`=8 handler [`subject_kind` 0=system / 1=us ...
- **`SPAWN_PERM_MAY_POST_SERVICE` one-hop delegation (A-5b #827b)** -- `kernel/syscall.c` (the `SYS_SPAWN_*` perm-grant gate, now PER-BIT: `SPAWN_PERM_CONSOLE_TR ...
- **Pathname resolution (`stalk`) + namespace-resident `/srv`** -- the resolver (`stalk` + `cross_mounts`/`domount` + the in-call `trail`; folds in / superse ...
- **Symlink expansion in `stalk` (DISTRO D-1; the I-28 refinement)** -- `kernel/stalk.c` (`stalk_expand_link` + the `restart:` label + the `base` re-anchor), `k ...
- **Union mounts (walk/readdir/create/remove/fd-base; I-1/I-3/I-28)** -- `kernel/territory.c` (mount MBEFORE/MAFTER/MREPL ordering + `mount_members_snapshot`), `kernel/stalk.c` (`STALK_CREATE`/`STALK_REMOVE` amodes; `stalk_union_child`/`_create_member`/`_member_holding`; `stalk_build_union_snap`/`union_snap_point_only`; base + descent cross), `kernel/syscall.c` (`union_readdir_run` + `viv_mutation_parent`/`sys_union_dirfd_member` + the fd unlink/rename/create handlers), `kernel/spoor.c` (`union_snap {point,n,m[]}` w/ `union_member {opened,walkable}`); `specs/territory.tla` (`WalkFirstHit`/`ReaddirDedupFirstWins`/`CreateTargetCorrect`/`RemoveTargetCorrect`/`OrderCorrect`) ...
- **Exec from the namespace (spawn binary resolution)** -- `kernel/syscall.c` (`exec_load_from_namespace` -- the new helper: resolve the program name ...
- **Namespace layout: /proc + /ctl mounts (#57a)** -- `kernel/devramfs.c` (the synthetic `/ctl` mount-point dir added to `g_ramfs_synth_dirs[]` ...
- **Per-Proc cwd (`SYS_CHDIR` / `SYS_GETCWD` + the `SYS_OPEN` relative->cwd join)** -- `kernel/include/thylacine/territory.h` + `kernel/territory.c` (`Territory.dot_path` -- the ...
- **Loom -- the io_uring-inverted 9P ring transport (KObj_Loom + SQ/CQ rings + registered handles + the pluggable-completion 9P-engine seam)** -- `kernel/include/thylacine/loom.h` (the ABI: `loom_sqe`/`loom_cqe`/`loom_ring_hdr`/`loom_pa ...
- **Proc exit handle-close (#926)** -- `kernel/proc.c` (`proc_close_handles_at_exit` -- the static helper, since #68 setting `exi ...
- **`wait_pid` selection + non-blocking (`SYS_WAIT_PID` v2 / `wait_pid_for`, U-7-pre)** -- `kernel/proc.c` (`wait_pid_for(want_pid, flags, status_out)` the new core + the filtered ` ...
- **Resource/DoS floor -- per-Proc page/thread/child caps (#65)** -- `kernel/include/thylacine/proc.h` (`PROC_PAGE_MAX` / `PROC_THREAD_MAX` / `PROC_CHILD_MAX` ...
- **Namespace name retention -- Spoor.path (#66)** -- NEW `kernel/include/thylacine/path.h` + `kernel/path.c` (`struct Path { int ref; u32 len; ...
- **Wall clock + RTC (PL031) + time/identity syscalls (LS-K)** -- `arch/arm64/rtc.{c,h}` (the PL031 driver: `dtb_get_compat_reg("arm,pl031", ...)` discovery ...
- **Monotonic-clock vDSO page (#343)** -- NEW `kernel/vdso.c` (`vdso_init` -- one kernel-owned `burrow_create_anon(PAGE_SIZE)` held ...
- **Kaua console-TUI substrate: the cons/consctl backend + the ut raw-mode dance (LS-7)** -- `usr/lib/kaua/src/term.rs` (the backend: the VT/ANSI input parser fd 0 -> KeyEvent; the da ...
- **Pollable console + termios/`consctl` line discipline (LS-8)** -- `kernel/cons.c` (LS-8a: a `poll_waiter_list` embedded in the cons layer + the IRQ-set `pol ...
- **`/dev/cons` drain/feed renderer backend + `SPAWN_PERM_CONSOLE_RENDERER` (Tapestry G-4)** -- `kernel/cons.c` (the `cons_drain` mirror tap in `cons_emit` + the bounded drop-OLDEST ring ...
- **`/env` per-Proc environment device (Go Stage 4a, G15)** -- NEW `kernel/env.{c,h}` (`struct Env` { atomic `ref` + `lock` + `entries[ENV_MAX_ENTRIES]` ...
- **POUNCE: fused walk+getattr resolution (`Twalkgetattr` 140/141 + the stalk pounce + `SYS_STAT` = 88)** -- `kernel/9p_wire.{c,h}` + `kernel/9p_client.c` (`p9_client_walkgetattr`; NOFID query mode), ...
- **The Larder: guest-side 9P FS cache (L1c substrate + attr sub-cache; L1d dentry sub-cache; L1e page sub-cache + cacheability gate)** -- NEW `kernel/larder.c` + `kernel/include/thylacine/larder.h` (`struct larder` on `p9_client` -- a near-leaf ...
- **FID-LIFECYCLE: async-clunk + cached-open (the fidless close-to-open open)** -- **async-clunk**: `kernel/9p_client.c` (`p9_client_clunk_async` fire-and-forget + the owner ...
- **Kernel debug surface: `/proc/<pid>` debug-fs (stop/resume + regs/mem/kregs/kstack/wait; I-39; Go IDE Stage 8a-1)** -- `kernel/devproc.c` (the debug files + `devproc_debug_authorized` [the I-39 two-axis gate] ...
- **PTY / job control: sessions + process groups + the pts registry + the tty seam + the job-control stop (I-20 stop leg; PTY-1 kernel arc)** -- `kernel/proc.c` (`proc_setsid`/`setpgid`/`getpgid`/`getsid`; `notes_post_pgrp`/`notes_post ...
- **ptyfs: the pseudoterminal server + /dev/pts (I-20 data path; the PTY-2 arc)** -- `usr/ptyfs/` (the native device-less /srv 9P server: the Conn/fid table + dispatch; the `P ...
- **pouch pty boundary-line (PTY-3)** -- `usr/lib/pouch/patches/0021-pouch-pty.patch` (sixteen files: the seam numbers 89-92 + 95-9 ...
- **JIT code Burrow + `SYS_ICACHE_SYNC` + `CAP_JIT` (the Clade arc, CL-7k; I-42)** -- **AS-BUILT at CL-7k** (`1f0e66c0` + `5633d056`) (`docs/LLVM-DESIGN.md` §8 + `docs/JIT-ON-W ...
- **Spawn-time page-budget (the Clade arc F4; I-32 composition, CL-5)** -- `kernel/include/thylacine/proc.h` (`Proc.page_budget` + `Proc.page_peak` + `PROC_PAGE_HARD ...
- **Process creation: `execve` + shared address spaces + COW `fork` (the LINEAGE arc, L-1..L-7; I-44)** -- **L-1 through L-5 LANDED (stock `fork()` works); L-6 NEXT (the VIVARIUM clone/execve/wait4 ...
- **VIVARIUM: the syscall-entry phenotype branch + the spawn-time declaration (V-1b; I-43)** -- `kernel/syscall.c` (`syscall_dispatch`'s phenotype branch -> `viv_linux_dispatch`; the TIE ...
- **Warp: the GPU seam -- the GPU-BO subtype + the `/dev/warp` tree + the fenced controlq (Warp-2; I-45)** -- `kernel/dma_handle.{c,h}` (the subtype enum + the 64 MiB envelope), `kernel/syscall.c` (`SYS_DMA_CRE ...
- **File-backed EL0 mmap: the phenotype FILE arm (DISTRO D-3; I-36 GENERALIZES, I-12 + I-32 on the line)** -- **D-3a + D-3b + D-3c AS-BUILT.** `kernel/include/thylacine/viv ...
- **DISTRO D-4: the PT_INTERP rewrite to the interpreter (exec dispatch)** -- `kernel/exec.c` (NEW `exec_interp_argv` + the rewrite block ...
- **Per-mount `MNOEXEC`: the executable-mapping vouching gate (#217; I-12 PROVENANCE half)** -- `kernel/include/thylacine/territory.h` (`MNOEXEC` 0x0010 in the existing ` ...
- **Initial bringup** -- `kernel/main.c`, `usr/joey/joey.c`
- **Warp-6 V-2: host-visible BAR mapping (`SYS_BURROW_FROM_HOSTMEM` + `BURROW_TYPE_HOSTMEM` + the mmu attr-index widening + the F1 death-quiesce; I-45/I-32/I-37)** -- `kernel/syscall.c` (`sys_burrow_from_hostmem_handler` + `hostmem_resolve_subrange` + the `sys_weft_share_for_proc` gate), `kernel/burrow.{c,h}` (`burrow_create_hostmem` + the 3 type arms + share admission), `arch/arm64/mmu.{c,h}` + `arch/arm64/fault.c` (`mmu_install_user_pte_attr`), `kernel/pci_handle.{c,h}` + `kernel/proc.c` (the `hostmem_burrows` counter + `kobj_pci_quiesce_dma_only` + the death-path branch), `kernel/weft.{c,h}` (`WEFT_BIND_HOSTMEM`) ...
- **`SYS_FD_DEVCLASS` fd-class introspection + the Beacon emission layer (H-1)** -- `kernel/syscall.c` (`sys_fd_devclass_handler` = 80; rights=0, ref-transfer discipline), `kernel/devdev.c` (`devdev_fd_de ...
- **halcyond: the Halcyon transcript renderer + the shared VT core + the renderer-choice spawn (H-2)** -- `usr/halcyond/` (transcript.rs THE PARSER of untrusted OSC 1936 frames [format-fuzz class] + la ...
- **tapestryd + halcyond: the Daylight chrome path -- the renderer-gated chrome surface + the gated tile-status verb + the status feed (H-3b)** -- `usr/tapestryd/src/server.rs` (the `create ... role=chrome bind=<id>` gate; `surface_target`; the CONFIGURE fans; `paint_borders` the live hairline; `tag <id> status` + `PFK_STATUS`), `usr/tapestryd/src/pane.rs` (`Status` + the resets), `usr/halcyond/src/{chrome,chromeset,main,transcript}.rs` (the rules / the surfaces / the status feed / the exit latch) ...
- **tapestryd + halcyond: the obj verb menu -- the renderer-gated menu surface, the input GRAB, the compositor-owned dismiss, click-to-focus (H-3c, THE GATE)** -- `usr/tapestryd/src/server.rs` (`role=menu` create; `menu place`/`menu dismiss`; the grab in `key_event`/`ptr_route`/`ptr_btn`/`ptr_rel_emit`/`chord_key`; `retire`'s menu arm + `menu_heal` + `menu_reassert`), `usr/lib/libtapestry/src/lib.rs` (`menu_on_shared`; `Drop` writes `destroy` on a shared session), `usr/lib/beacon/src/verbs.rs` (the rules engine), `usr/halcyond/src/{menu,menuset,main}.rs` ...
- **libtapestry: THE EVENT SET -- one 9P session + one Loom ring per client, shared by every surface; the ring-slot lifecycle; the synchronous present (H-3c-2)** -- `usr/lib/libtapestry/src/lib.rs` (`EventRing`: `connect`/`adopt`/`wait`/`poll`; `RingCore::join`/`leave`/`reregister`/`arm_all`/`pump`/`route`; `Surface::{fullscreen_on,open_on,chrome_on,menu_on}`; `Drop` = destroy -> leave -> fds), `usr/halcyond/src/{main,chromeset,menuset}.rs` ...
- **tapestryd + halcyond + ut: the status bar -- the display-level chrome carve + `role=status` + OSC 7 / the cmd mark (H-3d)** -- `usr/tapestryd/src/server.rs` (`Comp.status`, the carve in `reconcile`, `status_rect`, the `statusbar` file), `usr/halcyond/src/{status,statusset,transcript}.rs`, `usr/utopia/libutopia/src/repl.rs` (`cwd_report`, `mark_cmd`) ...
- **tapestryd + halcyon: the Session(principal) pane-authority actor + the one-shot placement claim + the restore tool (H-4b; layout save/restore, the D decision)** -- `usr/tapestryd/src/server.rs` (`Actor::Session`; the 3-way `actor()`; `owner_principal`; the claim mint owner-gate + `create claim=`; `PFK_OWNER`; `reap_session_empties`), `usr/tapestryd/src/{pane,main}.rs` (the both-empty split stamp; the last-conn reap sites), `usr/lib/libtapestry/src/lib.rs` (`take_env_claim` + the spent-token unlink), `usr/lib/libhalcyon/src/{layout,skeleton}.rs` (the `env` marker + `prune_env`; the pure restore planner), `usr/halcyon/src/{main,lib}.rs` (the restore tool; `resolve_prog`), the H-4b-1 csprng cap chain (warden/libdriver/joey), `tools/interactive/ls-gfx-restore.exp` ...
- **#50 path-mutation family (`SYS_OPEN_CREATE`=109 + the phenotype openat-O_CREAT/mkdirat/unlinkat/renameat rows)** -- `kernel/syscall.c` (`sys_open_create_kpath_for_proc` + the extracted `spoor_create_install`/`spoor_unl ...
- **VIVARIUM getdents64(61) + fsync(82)/fdatasync(83) + the O_DIRECTORY admission (6.25)** -- `kernel/syscall.c` (`spoor_readdir_run` [no-offset-advance] + `viv_dirent64_encode_run` + the 61/82/83 arms + the `dir_r ...
- **VIVARIUM the git chunk: faccessat(48)/chdir(49)/fchmodat(53)/readlinkat(78) + geteuid(175)/getegid(177) + getrandom(278) + the PHENOTYPE-FORK-INHERITS-CAPS change (6.26)** -- `kernel/proc.c` (`rfork_forked_with_caps`) + `kernel/syscall.c` (`sys_rfork_core` PHENO_LINUX cap branch [`child->caps = (parent_caps & CAP_ALL) & ~CAP_ELEVATION_ONLY`, I-2/I-43] + 7 T2 arms + `sys_readlink_for_proc` [copy-out validated]) + `kernel/vivarium.c`/`vivarium.h` (`vivarium_faccessat_de ...
- **VIVARIUM O_APPEND (FS pass-through) + pread64/pwrite64 (67/68): git commit + clone (6.27)** -- `kernel/include/thylacine/syscall.h` (`SYS_WALK_OPEN_OAPPEND` 0x40 + OMODE_VALID 0xB3->0xF3 + the delegated-append stance note) + `kernel/dev9p.c` (OAPPEND->O_APPEND in dev9p_open + dev9p_create; c->offset advisory, Stratum positions writes at EOF) + `kernel/vivarium.c`/`vivarium.h` (VIV_OMODE_APPEND + both openat decides admit O_APPEND + the PREAD64/PWRITE64 T1 renumbers) -- the kernel DELEGATES append to the FS (no append mode); pread64/pwrite64 = pure renumbers to SYS_PREAD/PWRITE ...
- **Boot banner** -- `kernel/main.c`
- **VIVARIUM 13: the `/viv/bin` phenotype mount (`MPHENO_LINUX` resolver subtree-scope; I-43 second declaration channel; I-28)** -- `kernel/include/thylacine/territory.h` (`MPHENO_LINUX` 0x0020), `kernel/territory.c` (`mount_lookup` `flags_out` + ns-render), `kernel/stalk.{c,h}` (`stalk_cross_mounts` `crossed_pheno` + `stalk_core`/`stalk_exec` threading), `kernel/syscall.c` + `kernel/include/thylacine/exec.h` (`exec_resolve_from_namespace_ex` + the SYS_SPAWN_FULL_ARGV `sa->pheno_linux` OR) ...
- **VIVARIUM Design D: the phenotype re-decided at EVERY image load (execve re-decides; `Territory.root_pheno`; the commit-region store; I-43 amended, I-19/I-28/I-12 on the line)** -- `kernel/syscall.c` (`sys_execve_core` decide-local -> threaded; the three spawn thunks' `phenotype_decide` stamp + `territory_declare_linux`; the four resolve sites on `_ex`), `kernel/proc.c` (`proc_exec_replace(p, nas, new_pheno)`: the ONE RELEASE store + the reset on the PARAMETER), `kernel/exec.{c,h}` (`exec_load_into`/`_body` take `pheno`; the PT_INTERP dispatch on it), `kernel/stalk.c` (the `restart:` SEED), `kernel/territory.{c,h}` (`flags` in the old `_pad`, `territory_root_pheno`/`_declare_linux`, the clone copy), `proc.h` (`phenotype_decide`) ...
- **DOSBox-X dynrec: the CAP_JIT dual-map code-cache consumer (Cryptid DX-4; I-42/I-12)** -- `usr/ports/dosbox-x/patches/0006-thylacine-dynrec-capjit.patch` (the `__thylacine__` alloc arm: acquire CAP_JIT -> `SYS_JIT_CREATE` dual-map + the fail-loud `SYS_ICACHE_SYNC` publishes replacing `__builtin___clear_cache` + the `DYNCOREALLOC_THYLACINE_JIT` enum), `usr/ports/dosbox-x/config.h` (`C_DYNREC`+`C_TARGETCPU=0x07`), `usr/lib/thylajit/thyla_jit.h` (the JIT syscall wrappers), `tools/build.sh` (`build_dosbox_x` `-I usr/lib/thylajit` + fail-loud patch-apply), `tools/interactive/ls-gfx-dosbox-dynarec.exp` ...
- **Nocturne N-1: the virtio-snd driver + the Plan 9 audio file (`nocturned`; I-46 RESERVED / I-34 / I-12 / I-14-posture)** -- `usr/nocturned/src/{snd,server,main}.rs` (the virtio-snd modern-PCI transport: bounded control round-trips, the device-controlled used id validated before it steers a slot; the `/srv/nocturne` tree with the bounded FIFO + the DEFERRED Rwrite), `usr/nocturne-probe/`, `kernel/devdev.c` (the `/dev/nocturne` mount stub), `usr/joey/joey.c`, `usr/warden/src/main.rs`, `tools/run-vm.sh` (`THYLACINE_AUDIODEV`), `tools/audio-verdict.py` + `tools/test-audio.sh` ...
- **Nocturne N-3a-3 + N-3c-1: the per-connection sink-authority surface -- `/srv/nocturne-ctl`, `sink_authorized`, the sink volume WRITE + the sink TAP (capture) READ + the console-owner kernel flag (`nocturned`; I-46 candidate / I-26/I-39 / I-27 / I-32; the eavesdropping surface)** -- `usr/nocturned/src/server.rs` (`sink_authorized`, the two-post split, the `tap` file + `tap_mirror`/`tap_open` single-reader + `PendingTapRead` parked read, the mount `audio` read -> EPERM), `kernel/{proc.c,syscall.c,include/thylacine/syscall.h}` (`SRV_PEER_FLAG_CONSOLE_OWNER`), `usr/{nocturne-vol,nocturne-vol-probe,nocturne-tap-probe}`, `usr/joey/joey.c`, `tools/run-vm.sh` + `tools/test-nocturne-{volume,tap}.sh` ...
- **Nocturne N-3c-2: device capture -- the virtio-snd D_INPUT RX stream + the gated `source` (`nocturned`; I-46 candidate / I-14-posture on the RX path / I-34 / I-32; the eavesdropping surface, device side)** -- `usr/nocturned/src/snd.rs` (the `VQ_RX` capture stream + rxq + `negotiate_capture` [optional, non-fatal] + `pump_rx` [device-controlled `len` clamped to PERIOD_BYTES] + `start_capture`/`stop_capture`), `usr/nocturned/src/server.rs` (`P_SOURCE` on `-ctl` + `source_*` reusing the N-3c-1 gate/single-reader/parked-read; on-demand via `source_open`; `capture_available` ENODEV gate), `usr/nocturned/src/main.rs` (the cycle's on-demand start/stop + `pump_rx`), `usr/lib/libthyla-rs/src/ninep.rs` (`E_NODEV`), `usr/nocturne-capture-probe/`, `usr/joey/joey.c`, `tools/run-vm.sh` (`THYLACINE_CAPTUREPROBE` -> streams=2) + `tools/test-nocturne-capture.sh` ...

- **IM-1: the trusted EPISODE -- the console FREEZE + `SYS_CONSOLE_EPISODE` + the `sak` note (I-27 ENFORCED on serial; I-19 / I-9 / I-32; `IMPERIUM-DESIGN.md` 11.3 + its 8 as-built refinements)** -- `kernel/cons.c` (the episode block: `cons_episode_begin`/`_close`/`_park`, `cons_reader_slot_take` [`CONS_SLOT_BUSY` positive: never aliases `TSLEEP_INTR`], `cons_caller_frozen`, the freeze in `cons_input_read` [the under-lock vacate re-check] / `cons_output_write` [park before the role + the per-chunk cut] / `cons_feed_write` / `cons_set_mode_cmd` / `cons_poll` [`episode_poll_list`: a frozen poller is never woken per keystroke]), `kernel/proc.c` (`proc_console_sak` -> bool [BEGIN + the `sak` post + the caught-note wake under ONE table-lock hold; `g_console_owner_pre_sak`; the `cons: SAK (<decision>)` witness], `proc_console_episode` [trusted-identity gate; ARM / END], the three fail-safe closes: death / relinquish / replacement), `proc.h` (the caught-note sub-field 6 -> 7 bits; the pipe latch relocated to bit 18), `notes.{c,h}` (`NOTE_BIT_SAK` 6 / mask 0x7f / the exact-name synthetic-only POST gate), `syscall.{c,h}` (110 + the two ops), `kernel/test/test_cons.c` (14 `cons.episode_*` incl. two REAL-chokepoint deaths + two real `sys_poll` pollers across an episode), the libthyla-rs / libt / ut mirrors, `tools/interactive/im1-sak-lever.exp` (Ctrl-A b IS a BREAK on the muxed serial) ...
- **IM-2: the fork-PROPAGATING legate scope -- `SYS_CAP_GRANT_IMPERIUM` + the rfork carve + the straggler close + one-scope-per-Proc + `/proc/<pid>/imperium` (I-25 STRENGTHENED / I-2; I-39 / I-42 / I-46 heritability by construction; `IMPERIUM-DESIGN.md` 11.4 + its 11 as-built refinements; `specs/imperium.tla`)** -- `kernel/proc.c` (`rfork_internal`: the carve `(parent & mask) & ~(CAP_ELEVATION_ONLY & ~flow)` [`pscope` ACQUIRE first], the tag inherit keyed on it + `legate_caps = flow & child->caps` + `legate_flags`, the `group_exit_msg` re-check under the SAME `g_proc_table_lock` hold as `proc_link_child` + `rfork_rollback_unpublished`; `proc_become_legate` -> int, FRESH / FURTHER arms [the further arm keeps tag/root/traits, earlier nonzero deadline, refuses PROPAGATING]; the teardown comment; the EL0-tail deadline load), `proc.h` (`legate_caps` @392 + `legate_flags` @400, 392 -> 408; `LEGATE_FLAG_PROPAGATING`), `kernel/devcap.c` + `devcap.h` (`cap_register_imperium_grant_for_writer` [the 40-byte form; flags validated; PROPAGATING bounded to `CAP_GRANTABLE_IMPERIUM`]; the clearance core = flags 0; the redeem's nest refusal BEFORE the consume + the stamp UNDER the cap lock; `cap_set_entry_locked` resets `flags`), `kernel/syscall.c` + `syscall.h` (`SYS_CAP_GRANT_IMPERIUM`=111; the PHENO_LINUX fork mask `CAP_ALL | CAP_ELEVATION_ONLY`), `kernel/devproc.c` (`PQS_IMPERIUM` 0400 + `devproc_imperium_read_gated` [owner-or-hostowner] + `format_imperium`), the 10 tests (`proc.rfork_refused_while_terminating` through the REAL rfork + the clean twin; `caps.rfork_flow*` x3; `devcap.imperium_*` x3 + `further_redeem_keeps_scope`; `devproc.*imperium*` x2), the libthyla-rs / libt mirrors ...
- **IM-3: the lex curiata -- corvus's `imperium` level, the per-(user, level) key VERIFIER, `IMPERIUM_REQUEST` (19) with the PARKED-`Tread` deferred reply, and the SAK-episode consumer (crypto + privilege: I-27 / I-25 / I-2 / I-22 / C-22 / C-16; `IMPERIUM-DESIGN.md` 11.5 + its 16 as-built refinements)** -- `usr/lib/corvus-crypto/src/lib.rs` (`CapKeyWrap` CRVS-KV v1 [136 B] + `build_capkey_ad` + `make_capkey_wrap` + `verify_capkey`; 4 host tests), `usr/corvus/src/main.rs` (the `imperium` level; `clearance.db` v2 + `CAPKEYS`; the grant key tail + REVOKE; verb 19 + statuses 7/8; `ImperiumPending` ONE slot + the 60-s expiry + `IMPERIUM_FAILS`; `handle_imperium_request`; `episode_consume` / `episode_confer` / `console_read_key`; `Conn.awaiting_deferred` + `parked_read`, `dispatch_tflush`, `conn_reply_parked`; the notes fd in the poll set; the startup ARM), `usr/corvus/src/provincia.rs` (the cell-grid composer + serial rasterizer), `usr/lib/libthyla-rs/src/lib.rs` (`t_console_open`), `usr/imperium-probe/`, `usr/joey/joey.c` (the imperium grant + 4 deny-path probes), `tools/interactive/im3-lex-curiata.exp` + `im1-sak-lever.exp` ...
- **KT-1.5-kernel: the pollable Loom (`loom_poll` + the KOBJ_LOOM poll arm + halcyond's unified poll; I-9/I-29/I-30/I-5/I-7/I-32)** -- `kernel/loom.c` (`loom_poll`), `kernel/poll.c` (the KOBJ_LOOM arm + the keep_out loom-ref), `usr/lib/libtapestry/src/lib.rs` (`connect_sqpoll`/`poll_fd`), `usr/halcyond/src/{main,session}.rs` ...
- **KT-1: the kaua-term seam + the per-user session compositor (the pts host producer, the wire codec, halcyond's untrusted ingest, `login -> halcyond --session`, the DECLARED display handoff; format-fuzz class; I-2/I-22/I-19/I-20/I-32)** -- `usr/kaua-term/src/{lib,main,wire}.rs`, `usr/lib/ptyhold`, `usr/ptyhost`, `usr/lib/vt`, `usr/halcyond/src/{chrome,tiles,session,tile,transcript}.rs`, `usr/login/src/main.rs`, `usr/caps-probe` + `tools/interactive/ls-gfx-session.exp` ...
- **arm-6: the logout session teardown -- territory-at-exit (Part D) + the kernel-driven session hangup (A1) (I-24 / I-1 / I-28 / I-26-untouched; refines A-5 decision (3); IDENTITY-DESIGN §9.9.1)** -- `kernel/proc.c` (`proc_release_territory_at_exit` [Part D: the locked-detach + unlocked-free split at the `exits`/`thread_exit_self` last-live-thread windows] + `proc_session_hangup_if_leader`/`session_hangup_cb`/`proc_arm_session_hangup` [A1: the legate-teardown sibling in `proc_become_zombie_locked`]), `kernel/include/thylacine/proc.h` (`PROC_FLAG_SESSION_HANGUP`), `kernel/include/thylacine/syscall.h` (`SPAWN_PERM_SESSION_HANGUP`), `kernel/syscall.c` (`spawn_perm_grant_check` + `apply_spawn_perms`), `usr/login/src/main.rs` (the ut + halcyond session-leader spawns), the libthyla-rs / libt perm mirrors, `tools/interactive/ls-bghome-stall.exp` ...
- **HALCYON-SCALE: the display scale (the EDID-derived percent the compositor owns; `Metrics::at`; the gated `scale` verb + chords; halcyond follows)** -- `usr/lib/libhalcyon/src/{scale,theme}.rs`, `usr/tapestryd/src/{gpu,server,pane,chords}.rs`, `usr/lib/libtapestry/src/lib.rs`, `usr/tapestry-battery/src/main.rs` (SC-2); halcyond's `Sheet.scale`/`Sheet::px`/`ipx` sweep + `GlyphSource::set_scale`/`set_display` + the relayout re-read + `/env/HALCYON_SCALE` (SC-3); `gfx_compose.py --scale` + the compose gate's Super+= leg (SC-4) ...
- **HALCYON-THEME: one owner for the whole visual -- the terminal palette folded into `Theme`, the palette DECLARED to the tile it themes, and the whole visual loadable from ONE TOML file (TH-1..TH-5b + TH-4c; the row carries three addenda: the visibility split, the FILE, and the lint)** -- `usr/lib/vt/src/lib.rs` (`vt::DAYLIGHT` DELETED; `palette_to_spec`/`_from_spec`, the argv form), `usr/lib/libhalcyon/src/theme.rs` (`Theme.terminal` + `light_terminal`), NEW `usr/kaua-term/src/cmdline.rs` (the argv contract, pure), `usr/kaua-term/src/main.rs`, `usr/halcyond/src/{session_init,session,tile}.rs` ...
- **HALCYON-TYPE: the outline path -- the rasterizer swap (skrifa + zeno replace fontdue) + the smoothing stroke as a theme token (TY-1 / TY-2)** -- NEW `usr/halcyond/src/outline.rs` (`Face`: the y-negating pen + TopLeft fill, the stroke unioned as f + s - f*s on the explicit union box, the bearing `-placement.top`), `usr/halcyond/src/raster.rs` (`GlyphSource` on it; `smooth_mem` + `set_smooth` regen), `usr/lib/libhalcyon/src/theme.rs` (`Theme.smooth_mem`: thousandths of an em, DAYLIGHT 12), `usr/halcyond/src/layout.rs` (`Sheet.smooth_mem`), the four `set_smooth` sites in `{main,session}.rs`, the vendored `skrifa`/`read-fonts`/`zeno` closure ...
- **I-47 inline media: the `/srv/halcyon` place channel + the `view` decoder (the console spike; format-fuzz; docs/HALCYON.md 14.7) + the per-user SESSION-path channel (14.7.2)** -- NEW `usr/halcyond/src/placesrv.rs` (the minimal 9P server the console renderer posts) + NEW `usr/halcyond/src/inlineaccum.rs` (the PURE host-tested validate-before-alloc accumulator, `PLACE_MAX_PIXELS`) + `usr/halcyond/src/main.rs` (post + the unified-poll fds + the service/`inject_image` step) + NEW `usr/halcyond/src/paneplace.rs` (`PanePlaceServer`: the per-user `/srv/halcyon-<user>` server, token->leaf routing, the `t_srv_peer` accept gate, leaf-tagged completions) + NEW `usr/halcyond/src/paneroute.rs` (the PURE token codec + fail-closed walk) + `usr/halcyond/src/session.rs` (the compositor wiring: per-pane token in `/env/HALCYON_PLACE`, the poll/service/drain, the reap unregister) + NEW `usr/view/{src/lib.rs,src/main.rs}` (sniff + zune `decode_png`/`decode_jpeg` -> ARGB + `open_place_write` [session address preferred] + `place_on_halcyon`) + NEW `usr/lib/inlinewire/src/lib.rs` (the shared place-request wire) + `usr/joey/joey.c` (renderer spawn `| MAY_POST_SERVICE`) + `tools/build.sh` (stage `view`; bake `/test.png`/`/test.jpg`) ...
- **HAUL: the npxf secure channel -- a second implementation of a hand-rolled AEAD protocol, on a network-facing surface (`docs/HAUL-DESIGN.md`)** -- NEW `usr/haul/src/npxf.rs` (PURE, syscall-free: the psk / transcript / X25519+contributory check / HKDF sc ...
- **HALCYON-INSTRUMENT I-1: the second theme schema + the dispatching loader + the two projections + the 127-field bundle wire (format-fuzz class; the ctl gate's class)** -- NEW `usr/lib/libhalcyon/src/instrument.rs` (`InstrumentTheme`, `from_entries` strict, `project_legacy`/`project_instrument`, `Profile`, `Bundle`/`Visual`, `is_gallery_id`, `resolve_bundle`), `usr/lib/libhalcyon/src/theme.rs` (`load` dispatch; `to_wire`/`from_wire` over a `Bundle`, `WIRE_FIELDS` 127 + `end`), the 13 generated gallery files, `usr/halcyond/src/{session,main}.rs`, `usr/tapestryd/src/{main,server}.rs` (`Comp.bundle`), `usr/halcyon/src/{lib,main}.rs` ...
- **HALCYON-INSTRUMENT I-2: the profile's geometry -- the Instrument carve (rails, pad, tracks + 7x7 joints, frames, headers incl. collapsed), the weighted N-ary split with ONE snap, the minima judged before the mutation, `halcyon-layout v2` (compositor geometry; I-32)** -- NEW `usr/lib/libhalcyon/src/carve.rs` (`split_spans`/`stack_alloc`/`stack_min_h`), `usr/lib/libhalcyon/src/{theme,instrument,layout,skeleton}.rs` (`Metrics` 21 fields + the zero-base rule, `INSTRUMENT_BASE` + `metrics_base`, v2 weights, `Op::Weight`), `usr/tapestryd/src/pane.rs` (`recompute_instrument`/`carve`/`place_frame`, `split_fits`, the weight rules, `host_for`'s stack fallback), `usr/tapestryd/src/server.rs` (`workspace_area`, `paint_instrument`, `frame`/`dividers`, the `weight` verb, the `split` refusal), `usr/halcyond/src/{layout,session,main}.rs`, `tools/build.sh` (`THYLACINE_HALCYON_PROFILE`), `tools/interactive/ls-halcyon-instrument.exp` ...
- **HALCYON-INSTRUMENT I-3: the stack and the headers -- pointer routing to chrome (`chrome_at`, `TEV_PTR_LEAVE`), the header actions under the OWNER's pane authority (focus / close / the final tile / the tile menu), the placard carve, the separator row, the successor-rule fix, the retained tiles (compositor input routing; HALCYON.md 13.6; I-32)** -- `usr/tapestryd/src/server.rs` (`chrome_at`/`ptr_target`/`ptr_crossing`, `Comp.ptr_over`, `TEV_PTR_LEAVE`, the placard fill + the separator row, `calc_geom_sig`), `usr/tapestryd/src/pane.rs` (`Pane.separator`, the placard carve, `close_inner`'s index shift), `usr/lib/libtapestry/src/lib.rs`, `usr/lib/libhalcyon/src/instrument.rs` (`Derived`/`over`), `usr/halcyond/src/{chrome,chromeset,session,main,menu,status,statusset,tile,layout}.rs`, `tools/interactive/ls-halcyon-instrument.exp` ...
- **HALCYON-INSTRUMENT I-4: the rails -- `role=rail` gated like the bar and following the display, the rail as a pointer target, the `chords` file, the top rail's zones and the footer's four conditions from the tile's facts, Reset as a verb plan under the owner's authority (display-level chrome; HALCYON.md 13.6; I-32)** -- `usr/tapestryd/src/server.rs` (`Comp.rail`, the `role=rail` arm + `pane::admit_rail`, `rail_rect`, the Direct conjuncts, the retires, `retire_stale_rail`, `chrome_at` / `ptr_crossing`, the `rail` + `chords` files), `usr/tapestryd/src/{pane,chords}.rs`, `usr/lib/libtapestry/src/lib.rs`, `usr/lib/libhalcyon/src/instrument.rs`, NEW `usr/halcyond/src/{rail,railset}.rs`, `usr/halcyond/src/{raster,status,menu,chromeset,statusset,main,session}.rs`, `tools/interactive/ls-halcyon-instrument.exp`, NEW `tools/interactive/ls-halcyon-session-instrument.exp` ...
- **HALCYON-INSTRUMENT I-5: type and the rich document -- I-5a the substrate: three Plex cuts and two Cornucopia subsets embedded, the free-running mono face, the Italic cell face, the box-glyph precedence in the cell, the Sheet's role table, the chrome on the type map; I-5b the document: the profile-keyed fractional flow, the Sheet's document column, the terminal view and the code block, the position indicator, the raw grid's ground; I-5c the producers: the profile-keyed palette export with its reader beside the writer, ut's lambda prompt, nora's class roles; I-5d kerning: GPOS pair adjustments with HarfBuzz's semantics, the H1's tracking, the hanging line-end space (halcyond's in-process face, I-32; the /env contract's reader side; the font-table reader)** -- `usr/halcyond/src/raster.rs` (`FACE_SANS` / `FACE_SANS_MEDIUM` / `FACE_SANS_SEMIBOLD` / `FACE_MONO_ITALIC` / `FACE_MONO_TEXT`, `prop_slot`, `VENDORED_FACES`, `italic_shares_the_cell`, the box glyphs before the face, `is_instrument_sans`), `usr/halcyond/src/layout.rs` (`Flow` / `Rhythm`, the document column, `flow_box`), NEW `usr/halcyond/src/indicator.rs`, `usr/halcyond/src/lib.rs`, `usr/lib/cornucopia/src/{lib.rs,cornucopia-subset.ttf,cornucopia-subset-italic.ttf}`, `tools/subset-cornucopia.py` (`--extra`, `--match`), `usr/halcyond/src/{tile,rail,chrome,menu,main,session}.rs`, `tools/interactive/ls-halcyon-instrument.exp`; I-5c: `usr/lib/libhalcyon/src/theme.rs` (`env_palette` over the `Bundle`, `PromptRoles`, `prompt_roles`), `usr/utopia/libutopia/src/{palette,ansi,repl}.rs`, `usr/utopia/shell/src/main.rs`, `usr/nora/src/theme.rs`, `tools/interactive/ls-halcyon-session-instrument.exp`; I-5d: `usr/halcyond/src/outline.rs` (`kern_lookups`, `Face::kern_units`), `usr/halcyond/src/raster.rs` (`set_kerning`, `kern` in 1/256 px), `usr/halcyond/src/layout.rs` (`Sheet.{kerning, hdr_track, hang_spaces}`, `run_width_fx`) ...
- **HALCYON-INSTRUMENT I-6: dividers and minima -- the compositor's own pointer capture on the published tracks, the weights re-expressed as the children's pixel extents, the mockup's ratio on the pair's frame under the band and the minima, one relayout per frame, the capture's end conditions, the double-click; the `mode` / `move` fits-checks on a copy; the chrome bind re-judged at every reconcile; the two-per-pane allowance (compositor input routing; I-32 on the pool)** -- `usr/lib/libhalcyon/src/carve.rs` (`drag_pair` / `equalise_pair`), `usr/tapestryd/src/pane.rs` (`track_at` / `drag_track` / `equalise_track` / `fits_after` / `chrome_bind_admitted`), `usr/tapestryd/src/server.rs` (`DragState`, `Comp.drag` / `track_hover`, `OWNER_DRAG`, `drag_*`, `hover_update`, `paint_track`, `reap_orphan_chrome`, the caps), `tools/qmp-sendtext.sh` (`dblclick`), both Instrument gates ...
- **HALCYON-INSTRUMENT I-7: the theme picker, live switching, and the dialog family -- the gallery-directory registry, the picker/dialog as two more models on the ONE Role::Menu surface, the Super+T chord delivered to the rail owner, the live theme transaction and the cooperative re-theme, the durable write of the picker's word (format-fuzz on the gallery read; the gated `theme` verb's class; a cross-thread palette handoff; compositor input routing)** -- `usr/lib/libhalcyon/src/instrument.rs` (`Group` + the optional meta keys), `usr/lib/vt/src/lib.rs` (`remap_color` / `set_palette`), `usr/kaua-term/src/{wire,main,lib}.rs` (`Input::Palette` + the cross-thread apply + `Producer::reemit`), `usr/halcyond/src/{picker,dialog,menuset,railset,session,main,transcript,grid,tile,downq,indicator}.rs`, `usr/tapestryd/src/{chords,server}.rs` (`TEV_CHORD` + `deliver_chord`), `usr/lib/libtapestry/src/lib.rs` (`TEV_CHORD`), `tools/halcyon/{picker.json,instrument-gallery.py}`, both Instrument gates ...
- **HALCYON-INSTRUMENT I-7b: the keyboard reference and the asking `Super+Q` -- the help card as the FOURTH model on the ONE Role::Menu surface with its rows DERIVED from the compositor's `chords` file (never a literal), and `ChordAction::Close` delivered to the rail owner instead of acted on (TEV_CHORD code 3, the focused pane's id in `value`) so the owner can ask before discarding a running job** -- NEW `usr/halcyond/src/help.rs`, `usr/halcyond/src/{menuset,railset,session,main}.rs`, `usr/tapestryd/src/server.rs` (`deliver_chord` re ...

- **HALCYON-WORKSPACES W-1a: the live roots -- one live root per workspace with per-workspace focus, the d-1b dormancy predicate widened one workspace wider, `MAX_WORKSPACES` = 9 PARTITIONING the existing pane pool (I-32 unchanged), the `layout` header's `workspaces N active K` as the ratified channel, and `close_inner`'s root arm no longer freeing the WHOLE pane pool** -- `usr/tapestryd/src/pane.rs` (`Workspace`, `workspaces`/`active`, `root()` as an accessor no ...
- **HALCYON-INSTRUMENT I-8a: the two effect ops -- `Op::RectAlpha` + `Op::Glow`, the glow's blur radius clamped IN THE EXECUTOR (`GLOW_RADIUS_MAX` = 32 = section 10's 16-at-100 % doubled for the 200 % scale ceiling) and the coverage arithmetic in u64 so the bound carries no second obligation** -- `usr/lib/cartoon/src/lib.rs` (the variants, `Exec::fill_alpha`/`glow`, the two `execute` arms, the `CARTOON_V0` corrective), `usr/halcyond/src/tile.rs` (the tree's ONLY exhaustive `Op` match) ...
- **HALCYON-INSTRUMENT I-8b: the compositor paints the effects it owns -- tapestryd takes a `cartoon` dependency (ONE bounded blur, never a second implementation), `paint_cartoon` hands the screen buffer to the same executor, and the divider drag glow is decided by a PURE rule in the lib because `server.rs` is bin-only and has no host witness** -- `usr/tapestryd/Cargo.toml`, `usr/tapestryd/src/pane.rs` (`track_glow`), `usr/tapestryd/src/server.rs` (`track_dragged`, `paint_cartoon`, the threaded `paint_track`), `usr/lib/libhalcyon/src/instrument.rs` (`effects` -- section 10's literals) ...
- **HALCYON-INSTRUMENT I-8b-3b: `Op::Blur` -- the ONE executor op that READS the surface it paints into (section 10's modal backdrop), allocation-free because only the `r+1` already-overwritten values need keeping, and a radius cap that becomes MEMORY SAFETY because it sizes the ring** -- `usr/lib/cartoon/src/lib.rs` (`Op::Blur`, `Exec::blur`, the free `blur_line`), `usr/halcyond/src/tile.rs` (the tree's ONLY exhaustive `Op` match) ...
- **HALCYON-INSTRUMENT I-8b-3c: the modal backdrop and the card shadow wired -- painted ONCE at placement with the effect ring PUSH-SUPPRESSED, because a blend is not idempotent the way the card's opaque compose is and there is no choke point among the seven screen-buffer writers** -- `usr/tapestryd/src/pane.rs` (`bars_around` moved from the bin + its first witnesses, `menu_push_allowed`, `menu_effects`), `usr/tapestryd/src/server.rs` (`MenuState.fx`/`heal_rect`, `menu_paint_effects`, `screen_push_raw`, the split push + flush) ...
- **HALCYON-INSTRUMENT I-8c-1: `libhalcyon::motion` -- section 10's durations and easing, section 9.5's opt-out, and ONE poll-timeout fold replacing two hand-written reducers; a dead monotonic clock turns motion OFF rather than freezing a tile mid-tween** -- NEW `usr/lib/libhalcyon/src/motion.rs`, `usr/lib/libhalcyon/src/lib.rs`, `usr/halcyond/src/{main,session}.rs` (both poll sites) ...
- **HALCYON-INSTRUMENT I-8c-2: the caret blinks -- `/env/HALCYON_MOTION` read at session start, ONE predicate shared by the painter and the dirty rule, and a poll deadline that is the STEP rather than the frame; a tick that marks nothing paints nothing, because both render loops are dirty-gated** -- `usr/lib/libhalcyon/src/motion.rs` (`caret_next_step_ms`), `usr/halcyond/src/tile.rs` (`Tile.caret_on` + `paints_caret` + `set_caret_on`), `usr/halcyond/src/session.rs` (the lever, the per-pass step, the third poll fold) ...
- **HALCYON-INSTRUMENT I-8c-3a: the `motion` verb -- a fifth member of the compositor's default-deny apply-authority gate, because tapestryd reads no file of any kind and so cannot read the preference it needs; the word travels, the clock conjunct does not** -- `usr/tapestryd/src/pane.rs` (`motion_word`), `usr/tapestryd/src/server.rs` (`Comp.motion` + the gate conjunct + `set_motion`), `usr/lib/libhalcyon/src/motion.rs` (`stated`), `usr/halcyond/src/session.rs` (`request_env_motion`) ...
- **HALCYON-INSTRUMENT I-8c-4a: the split flash's PURE half -- one rect, two inks, ONE curve, because `opacity` animates the element and not its background; the degenerate guard precedes an unsigned subtraction** -- `usr/lib/libhalcyon/src/motion.rs` (`ease_out`), `usr/tapestryd/src/pane.rs` (`SplitFlash` + `split_flash`) ...
- **HALCYON-INSTRUMENT I-8d: the operator's hands-on fixes -- the modal backdrop covers the DISPLAY and the dismiss REBUILDS the scene from shown slots; a stack's members are TILES (a split on a stacked tile splits the stack, a window joins it, stacking a group holding a split is refused, a restore lays members flat); Super+N; cartoon's blur runs its window** -- `usr/lib/cartoon/src/lib.rs` (`blur_line`), `usr/tapestryd/src/{pane,server,chords}.rs` (`menu_effect_region`, `split_target` / `stacking_refused` / `new_tile_mode`, `scene_restore` / `restore_surface` / `menu_restore`, the frozen `screen_flush_full`, `set_mode`'s dismiss, `NewTile`), `usr/lib/libhalcyon/src/{layout,instrument}.rs` (`flatten_stack_members`, `resolve_profile`), `usr/halcyon/src/main.rs`, `usr/halcyond/src/help.rs` ...
- **HALCYON-INSTRUMENT I-8e: nothing freezes -- a menu carries only its shadow, a dialog the backdrop, and the card and its effects are laid on AT UPLOAD over a buffer that is the clean scene at all times; the save is a lazy display-sized burrow, not the 4 MiB heap; the GPU composed path forces CPU composition under a card and carves GL blits out** -- `usr/tapestryd/src/{pane,server}.rs` (`MenuClass` / `menu_place_args` / `menu_effects` / `effect_cartoons` / `overlay_plan` / `subtract_rects`; `upload` / `fx_save_take` / `menu_unplaced` / `restore_under` / `bo_blit_arm`), `usr/lib/libhalcyon/src/instrument.rs` (`MENU_SHADOW_*` / `DIALOG_SHADOW_*`), `usr/halcyond/src/menuset.rs` (the `dialog` word) ...

- **Graphical Lex curiata: Lictor + the trusted seat (the kernel seat endpoint, the chord scan, the held grant, SYS_SEAT_IMPORT, the broker ledger; I-27 / I-5 exception / I-25 / I-34 / I-40 / I-45)** -- `kernel/proc.c` (`g_seat`, `proc_seat_op`, `proc_seat_fail_locked`, `seat_attention_held`), `kernel/include/thylacine/seat.h`, `kernel/syscall.c` (121/122/123 + the three `SPAWN_PERM_SEAT_*` bits), `kernel/devcap.c` (`seat_held`), `kernel/weft.c` (`weft_share_claim_from`), `kernel/srvconn.c` (`srvconn_io_nonblock`), `usr/lictor/`, `usr/corvus/src/main.rs` (`EpisodeIo`), `usr/warden/src/main.rs` (`start_compositor`), the three `ls-graphical-sak*` gates ...
- **A-6: the truthful identity calls + the NAMED Stratum admin (IDENTITY-DESIGN 9.10; the A-3 identity surface; I-22)** -- a NEW `usr/lib/pouch/patches/` patch retargeting musl's `{getuid,geteuid,getgid,getegid}` off the 0xFFFF sentinel onto `SYS_GETUID`/`SYS_GETGID`, plus (Stratum `thylacine-pouch-arm`) `src/cmd/stratumd/serve.c` + `src/fs/fs.c` -- the ctl admin moves from `geteuid()` INFERENCE to an explicit NAMED principal (configured, never inferred) ...
- **B-1a: the permission ceiling -- `SYS_BURROW_RESERVE` (124) + `SYS_BURROW_PROTECT` (125), the multi-mapping reprotect, the fork's per-Burrow clone dedupe, the phenotype `mprotect` row, the lazy-piece detach (ARCH 6.5; I-12 / I-32 / I-44)** -- `kernel/vma.{c,h}` (`VMA_FLAG_MAX_*`, `vma_reprotect_*`, `reprotect_merge_in`, `vma_find_gap_aligned`), `kernel/burrow.{c,h}` (`burrow_protect_in`, `clone_cursor`), `kernel/addrspace.c` (`clone_one_vma` dedupe + the ceiling-keyed share), `kernel/syscall.c` (124/125 + the `VIV_LINUX_MPROTECT` arm + the exact anon mints + the lazy-piece detach), `kernel/vivarium.{c,h}`, `specs/cow.tla` (+4 cfgs, `check-cow.sh`), `kernel/test/test_protect.c`, `usr/protect-probe`, `usr/protect-guard-child` ...
