# Phase 4 — status and pickup guide

Authoritative pickup guide for **Phase 4: Device model + userspace drivers** (per `ROADMAP.md §6`).

## TL;DR

Phase 4 lifts the kernel from "userspace runs" (Phase 3 close) to **"hardware is reachable from userspace via Plan 9 9P"**. Major deliverables: Dev vtable (Plan 9 idiom verbatim) + Spoor lifecycle infrastructure; kernel-internal synthetic Devs (`/dev/cons`, `/dev/null`, `/dev/zero`, `/dev/random`, `/proc`, `/ctl`, `/ramfs`); VirtIO core (in-kernel transport, MMIO + minimal PCIe); IRQ forwarding to userspace via `KObj_IRQ` blocker wakeups; userspace Rust drivers for virtio-blk / virtio-net / virtio-input / virtio-gpu; driver crash supervision.

The phase culminates with userspace drivers exposing their devices as 9P servers at `/dev/<name>/`. Phase 5 (= ROADMAP §7 by local numbering) then layers 9P client + Stratum integration on top.

Per CLAUDE.md spec-first policy: VirtIO core is impl-only (transport correctness checked by integration tests + adversarial audit; no new TLA+). BURROW refcount + mapping lifecycle (`specs/burrow.tla`) is finalized this phase. IRQ-handle wakeup is covered by `scheduler.tla`'s wait/wake protocol (already proved I-9 NoMissedWakeup).

**Local phase numbering note**: this is "Phase 4" by local convention; ROADMAP §6 is the canonical scripture text (titled "Phase 3" in the ROADMAP but representing the device-model phase that comes after our local Phase 3 close). The local renumbering stems from splitting ROADMAP Phase 2's deferred address-space deliverables across local Phase 2 + Phase 3. Working name: **P4-* sub-chunks**.

---

## Landed chunks

| Commit SHA | What | Tests |
|---|---|---|
| `48692c6` | **P4-Ia2: Rust nostd userspace + first Rust hello binary.** Adds the Rust path through the build-from-disk pipeline alongside the C path landed at P4-Ia1. New `usr/Cargo.toml` (workspace root; members: hello-rs; release profile sets opt-level=z, lto=fat, panic=abort, codegen-units=1). New `usr/.cargo/config.toml` (pins target = aarch64-unknown-none, target-dir = ../build/usr-rs, rustflags = -Tscripts/aarch64-userspace.ld + posture flags matching the C side: --no-dynamic-linker, --build-id=none, -z noexecstack, relocation-model=static, code-model=small, target-feature=+lse,+paca,+pacg,+bti). New `usr/hello-rs/` crate with `Cargo.toml` (workspace-inherited edition/license/publish) + `src/main.rs` (#![no_std] + #![no_main]; inline SVC wrappers via core::arch::asm! for `t_exits` / `t_puts` / `t_putstr`; global_asm!-defined `_start` mirroring the C-side libt _start with `bti c` → `bl rs_main` → `mov x8,#0; svc #0`; `#[panic_handler]` calls `t_exits(1)`). `tools/build.sh::build_userspace` extended to also run `cargo build --release` from usr/ — skipped gracefully (with notice) if `rustup target add aarch64-unknown-none` hasn't been run. `build_ramfs` curates a second list (`usr_rs_bins=( hello-rs )`) copied from `build/usr-rs/<target>/release/`. Test `userspace.ramfs_hello_rs` added (refactored test_userspace_ramfs.c to share `run_ramfs_binary(name)` helper; thunk diagnostic message generalized from hardcoded "/hello:" prefix). Linker script reuse — both C and Rust binaries produce ELFs with the same PT_LOAD layout (W^X). Inline SVC wrappers per binary at v1.0 P4-Ia2; the `libthyla-rs` runtime crate is factored when the second Rust binary (P4-Ic virtio-blk driver) needs the same wrappers. Non-audit-bearing — Rust toolchain scaffolding doesn't touch a §28 invariant. Reference doc `docs/reference/38-userspace.md` extended with Rust-side sections (toolchain, _start, status). | 180/180 PASS × default (~411 ms) + UBSan (~440 ms); 4/4 fault; 5/5 KASLR distinct under `BOOT_TIMEOUT=20`. |
| `5467725` | **P4-Ia1: C userspace runtime (libt) + first hello binary.** Lifts userspace from "hand-encoded blob in kernel/joey.c" to "real ELF built out-of-tree and loaded from ramfs." New top-level `usr/` tree as a sibling CMake project: `usr/lib/libt/` (libt — Thylacine userspace runtime; `_start` + stack-canary stubs + header-only SVC wrappers `t_exits` / `t_puts` / `t_putstr`), `usr/hello/` (first binary), `usr/scripts/aarch64-userspace.ld` (linker script: two PT_LOAD segments — text+rodata RX, data+bss RW — enforcing W^X), `usr/CMakeLists.txt`. New `cmake/Toolchain-aarch64-userspace.cmake` (sibling of kernel toolchain; same clang+ld.lld+triple but with FP allowed, standard code-model, no `-mgeneral-regs-only` / `-mcmodel=tiny` / `-fpie`). `tools/build.sh userspace` implemented (was stub); `build_kernel` now chains `build_userspace` before `build_ramfs`; `build_ramfs` copies curated binaries (currently just `hello`) from `build/usr/<name>/<name>` into the cpio. `tools/build.sh sysroot` clarified to be the Linux-compat musl tree (Phase 6+); usr/ is Thylacine native. New public `kernel/include/thylacine/devramfs.h` exposing `devramfs_lookup(name, **data, *size)` for blob-by-name access (kernel-internal lifetime). New test `userspace.ramfs_hello` in `kernel/test/test_userspace_ramfs.c` — looks up /hello, memcpy's to 8-aligned static buffer (cpio is only 4-aligned; Ehdr cast requires 8), `rfork` + `exec_setup` + `userland_enter` + `wait_pid`. Graceful skip (returns PASS) if /hello absent. **Predecessor `kernel/joey.c` retained** as the boot-time embedded blob — disk-loaded /joey refactor lands when stratum mounts. **`tools/build.sh sysroot` deferral clarified**: now errors with explicit "Phase 6 deliverable (Linux-compat musl); native Thylacine userspace lives in usr/ — use build.sh userspace." Reference doc `docs/reference/38-userspace.md`. Sub-chunk decomposition for the rest of P4-I refined: P4-Ia2 (Rust nostd runtime + first Rust binary), P4-Ib (KObj_MMIO + KObj_IRQ handle-table integration, spec-first via handles.tla extension), P4-Ic (Rust no_std virtio-blk crate), P4-Id (driver-as-9P-server). Non-audit-bearing — toolchain scaffolding doesn't touch a §28 invariant. | 179/179 PASS × default (~415 ms) + UBSan (~436 ms); 4/4 fault; 10/10 KASLR distinct under `BOOT_TIMEOUT=20`. |
| `9e73223` | **P4-H: VirtIO PCIe enumeration (ECAM-based).** New `<thylacine/virtio_pci.h>` + `kernel/virtio_pci.c` (~210 LOC). DTB-driven probe of the `pci-host-ecam-generic` root complex; maps bus 0's 1 MiB of config space via `mmu_map_mmio` (256 of 512 vmalloc pages — half the available L3 capacity); walks (dev, fn) on bus 0 with multi-function detection via PCI_CFG_HEADER_TYPE bit 7; identifies VirtIO devices by vendor 0x1AF4 + device-ID range (legacy 0x1000..0x103F, modern 0x1040..0x107F). Per-device handle carries BDF + IDs + subsys + class/subclass/prog_if + virtio_device_id (derived from modern offset or legacy subsystem lookup). `virtio_pci_cfg_read{8,16,32}` accessors with bounds + alignment checks return the bus-floating sentinel (0xFF / 0xFFFF / 0xFFFFFFFF) on out-of-range. Boot wire-up: `virtio_pci_init()` after `virtio_init()` in `boot_main`. Banner adds `virtio_pci: ECAM at PA 0x4010000000 size 0x10000000 (3 VirtIO devices on bus 0, 32 functions probed)`. `tools/run-vm.sh` adds `-device virtio-rng-pci,id=rng_pci0` so the integration boot always has at least one PCI VirtIO device. BAR mapping + VirtIO PCI capability walking (per VIRTIO 1.2 §4.1.4) + INTx/MSI delivery deferred to first PCI-using driver chunk. PCIe bridges (Type 1 headers, secondary bus walking) deferred — QEMU virt's default config has no bridges. 7 new tests; reference doc `docs/reference/37-virtio_pci.md`. | 178/178 PASS × default + UBSan; 4/4 fault; 5/5 KASLR. |
| `8172dfe` | **P4-Fix157: trip-hazard #157 root-cause fix — `userland_enter` SPSel discipline.** Closes the forward-looking P0 deferred from R7. Root cause: `arch/arm64/userland.S`'s `mov sp, x17` writes SP_EL0 only when PSTATE.SPSel=0; under the IRQ-driven dispatch path (timer IRQ → preempt_check_irq → sched → cpu_switch_context, all at SPSel=1), the kthread runs at SPSel=1 and the `mov sp` clobbers SP_EL1 to user_sp = 0x80000000 (low VA, unmapped via TTBR0). After eret to EL0, the user code's first SVC enters EL1 with sp=SP_EL1=0x80000000; KERNEL_ENTRY's `sub sp + stp` then loops infinitely with FAR decreasing by EXCEPTION_CTX_SIZE per cycle. Fix: `msr SPSel, #0; isb` immediately before `mov sp, x17`, forcing the kernel-steady-state (sp aliases SP_EL0) regardless of entry context. Captured via `qemu-system-aarch64 -d int` showing the EL1→EL1 abort loop with ESR=0x96000044 and FAR stride 0x120 = EXCEPTION_CTX_SIZE. Reproducer at `kernel/test/test_userspace2.c` (was orphaned in tree) wired into build registry as 2 tests: `userspace.first_iteration` + `userspace.second_iteration`. F136 one-call guard in `joey_run` retained for its BSS-race-protection role; comment updated to drop the now-stale #157 mitigation rationale. Reference doc updates: `08-exception.md` § "userland_enter SPSel discipline" + `29-joey.md` regression-test pointer. P4-I (virtio-blk userspace driver) + P4-M (driver supervision) now unblocked. | 171/171 PASS × default (~412 ms) + UBSan; 4/4 fault; 5/5 KASLR distinct; specs unchanged. |
| `9e2b348` | **P4-A: Dev vtable + Spoor lifecycle + devnone stub.** New `kernel/include/thylacine/dev.h` + `spoor.h`. New `kernel/dev.c` (~120 LOC; bestiary[] registry, dev_register/dev_lookup_by_dc/by_name, dev_init walks bestiary calling each ->init()). New `kernel/spoor.c` (~150 LOC; SPOOR_MAGIC + counter discipline mirrors burrow.c; spoor_alloc/ref/unref/clone/clunk). New `kernel/devnone.c` (~110 LOC; dc='-', all ops safe no-ops or graceful failures). Boot wire-up: `dev_init()` after `sched_init` in main.c. Tests: 9 (`dev.boot_registration_smoke`, `dev.lookup_unknown`, `dev.devnone_ops_smoke`, `spoor.alloc_unref_round_trip`, `spoor.ref_lifecycle`, `spoor.clone_lifecycle`, `spoor.clone_copies_state`, `spoor.clunk_dispatches_close`, `spoor.alloc_10k_no_leak`). Reference doc: `docs/reference/30-dev-spoor.md`. | 105/105 PASS × default + UBSan; 4/4 fault; 5/5 KASLR. |
| `ce380f9` | **P4-G: irqfwd — IRQ forwarding to KObj_IRQ blocker.** New `kernel/include/thylacine/irqfwd.h` + `kernel/irqfwd.c` (~140 LOC). KObj_IRQ struct with magic + intid + refcount + Rendez + edge-triggered pending_count. `kobj_irq_create(intid)` registers the GIC handler + enables the IRQ; `kobj_irq_wait(k)` blocks until pending_count > 0, returns the collapsed count, zeroes the counter; `kobj_irq_dispatch` (GIC handler) increments under r->lock + wakes the rendez. Wait/wake atomicity follows scheduler.tla's NoMissedWakeup (I-9): cond under r->lock + producer update + wakeup straddle the same lock. Reserves SGI 1 (`IPI_IRQFWD_TEST`) for tests; SGI 0 stays IPI_RESCHED. Tests: 4 (`irqfwd.create_destroy`, `refcount_lifecycle`, `wait_wakes_on_sgi`, `collapses_concurrent_fires`). Reference doc: `docs/reference/36-irqfwd.md`. Handle-table integration (KOBJ_IRQ release path) held to P4-I+ alongside first userspace driver. | 169/169 PASS × default (~393 ms) + UBSan (~436 ms); 4/4 fault; 5/5 KASLR. |
| `e6e5507` | **P4-F: VirtIO core — MMIO transport + split virtqueue.** New `kernel/include/thylacine/virtio.h` (~190 LOC; VIRTIO 1.2 register offsets + device IDs + status bits + vring types with _Static_assert pins; struct virtio_mmio_dev + struct virtio_virtqueue + API). New `kernel/virtio.c` (~270 LOC; DTB probe via new `dtb_for_each_compat_reg`, MMIO register read/write, status state machine, feature negotiation per VIRTIO 1.2 §3.1.1 steps 1-5, virtqueue allocation with 3 separate page-aligned rings per modern-transport spec). Adds `dtb_for_each_compat_reg` (multi-match enumeration helper) to `lib/dtb.c` + `<thylacine/dtb.h>`. tools/run-vm.sh: `-device virtio-rng-device,id=rng0` for tests. **Page-alignment fix at probe**: QEMU virt's 32 virtio-mmio slots are 0x200 bytes apart (only every 8th slot starts a fresh page); probe page-aligns the PA + computes the slot's KVA as `page_kva + (slot_pa - page_pa)`. Tests: 7 (`virtio.mmio_probe`, `magic_value`, `version_modern` (range check, not exact), `rng_present`, `negotiate_features_smoke`, `virtqueue_alloc_destroy`, `find_by_device_id`). Reference doc: `docs/reference/35-virtio.md`. Boot banner adds `virtio: 32 MMIO slots probed (1 with attached devices, 0 skipped)`. | 165/165 PASS × default (~396 ms) + UBSan (~427 ms); 4/4 fault; 5/5 KASLR. |
| `5a90b77` | **P4-E: dev/ramfs — cpio-loaded in-memory FS.** Full scripture-faithful landing per ROADMAP §6.1 wording. New `tools/mkcpio.py` (~80 LOC; host script that emits cpio newc archives). New `tools/build.sh::build_ramfs` chained off `build_kernel` (generates `build/ramfs-src/{welcome,version}` + invokes mkcpio to produce `build/ramfs.cpio`). `tools/run-vm.sh` adds `-initrd build/ramfs.cpio`. New `kernel/cpio.{h,c}` (~200 LOC; newc iterator with 110-byte ASCII-hex header parser + 4-byte alignment). New `dtb_get_chosen_initrd` in `lib/dtb.c` + `<thylacine/dtb.h>` (probes `/chosen/linux,initrd-start` + `linux,initrd-end`; supports 4- or 8-byte big-endian cells). New `kernel/devramfs.c` (~270 LOC) with directory Dev (dc='m'); init reads DTB initrd + parses cpio + populates static file table (RAMFS_FILE_MAX=32). **Bug fix in `mm/phys.c::phys_init`**: added 5th reservation entry for the initrd PA range — pre-P4-E the buddy could reuse initrd memory + clobber the cpio bytes, exposed deterministically under UBSan. Bestiary 7 → 8 (`dev:  8 registered (none, cons, null, zero, random, proc, ctl, ramfs)`); boot banner adds `ramfs: 2 files loaded from initrd (412 bytes)`. Tests: 17 (7 cpio unit tests against synthetic byte-by-byte archives + 10 devramfs integration tests against the live initrd). Reference doc: `docs/reference/34-devramfs.md`. | 158/158 PASS × default (~392 ms) + UBSan (~417 ms); 4/4 fault; 5/5 KASLR. |
| `f38c0c2` | **P4-D: dev/ctl — kernel admin Dev.** New `kernel/devctl.c` (~280 LOC) with single-level directory Dev (dc='C'). Walks resolve `/ctl/{procs, memory, devices, kernel-base, sched}`; per-leaf format generators produce text-format kernel state. `/ctl/procs` uses `proc_for_each`; `/ctl/memory` calls phys accessors; `/ctl/devices` iterates `bestiary[]`; `/ctl/kernel-base` reads KASLR state; `/ctl/sched` reports runnable count. Writes rejected at v1.0 (admin commands held to Phase 5+ syscall surface). Reuses devproc walk pattern + tiny formatters (duplicated; future shared `<thylacine/fmt.h>` when third caller emerges). Tests: 11 (`devctl.bestiary_smoke`, `attach_returns_dir`, `walk_to_each_leaf`, `walk_unknown_misses`, `read_procs_format`, `read_memory_format`, `read_devices_format`, `read_kernel_base_format`, `read_sched_format`, `write_rejected`, `read_dir_returns_neg1`). Reference doc: `docs/reference/33-devctl.md`. Bestiary 6 → 7. | 141/141 PASS × default (~391 ms) + UBSan (~424 ms); 4/4 fault; 5/5 KASLR. |
| `c716f32` | **P4-C: dev/proc — synthetic /proc/<pid>/.** New `kernel/devproc.c` (~360 LOC) with directory-typed Dev (dc='p'). Walks resolve `/proc/<pid>/{status, cmdline, ctl, ns}`; multi-step walk supported via `walkqid_alloc` (added in `kernel/spoor.c`). status reports pid/state/threads/exit; cmdline reports argv[0] placeholder ("kproc" for kproc, "<unnamed>" for others — Phase 5+ ELF loader populates argv); ns reports `binds: N`; ctl reads return 0 (write-only) and writes consume the verb (the verb-set + dispatch hold to Phase 5+ when `specs/notes.tla` lands). Adds `proc_find_by_pid` + `proc_for_each` to `kernel/proc.c` (DFS under `g_proc_table_lock`). Tests: 13 (`devproc.bestiary_smoke`, `attach_returns_dir`, `walk_root_to_kproc_dir`, `walk_unknown_pid_misses`, `walk_to_status_file`, `walk_dotdot_to_root`, `read_status_format`, `read_cmdline_kproc`, `read_ns_format`, `read_ctl_returns_zero`, `write_ctl_consumes`, `read_dir_returns_neg1`, `read_partial_offset`). Reference doc: `docs/reference/32-devproc.md`. Bestiary count 5 → 6. | 130/130 PASS × default (~382 ms) + UBSan (~422 ms); 4/4 fault; 5/5 KASLR. |
| `6f417e9` | **P4-B: kernel-internal trivial Devs (cons / null / zero / random).** New `kernel/null.c`, `kernel/zero.c`, `kernel/random.c`, `kernel/cons.c` (~500 LOC total). Each is a single-file leaf Dev with the 16-op vtable; `dev_simple_{attach,open,close}` helpers in `kernel/dev.c` factor out the common lifecycle. devcons forwards writes to `uart_putc`; devnull bit-buckets writes + EOFs reads; devzero produces zeroes; devrandom reads via ARM64 RNDR (FEAT_RNG; ID_AA64ISAR0_EL1.RNDR detection at devrandom_init). Bestiary count 1 → 5; boot banner shows `dev:  5 registered (none, cons, null, zero, random)` + `random: RNDR available (FEAT_RNG)`. Tests: 12 (`trivial_devs.bestiary_smoke`, `null.attach_open_close/read_returns_eof/write_consumes`, `zero.read_fills_zeroes/write_consumes`, `random.rndr_available/read_produces_nonzero/read_varies`, `cons.write_advances/read_returns_eof`, `trivial_devs.devnull_10k_no_leak`). Reference doc: `docs/reference/31-trivial-devs.md`. **ROADMAP §6.2 exit criteria closed**: "Spoor lifecycle: 10K cycles on /dev/null without leak"; "cat /dev/random produces non-zero bytes". chacha20 stir per ROADMAP §6.1 held to a future hardening sub-chunk. | 117/117 PASS × default (376 ms) + UBSan (391 ms); 4/4 fault; 5/5 KASLR. |

---

## Remaining work

Sub-chunk plan (refined as Phase 4 progresses):

1. **P4-A: Dev vtable + Spoor infrastructure**. NEXT. New `kernel/include/thylacine/dev.h` + `kernel/include/thylacine/spoor.h` + `kernel/dev.c` + `kernel/spoor.c`. Plan 9 17-op vtable (verbatim per ARCH §9.2). Spoor refcount + per-Spoor lock + spoor_alloc/free/clone/walk/clunk. bestiary[] sentinel-terminated registry + dev_register + dev_lookup_by_dc/by_name. devnone stub Dev (all ops return `-ENOSYS`) anchors unconfigured Spoors + audit guard. Boot wire-up via `dev_init()` in main.c.
2. **P4-B: kernel-internal trivial Devs** (cons, null, zero, random). New `dev/` subdirectory. UART → /dev/cons; /dev/null absorbs writes; /dev/zero produces zeroes; /dev/random uses ARM64 `RNDR` + chacha20 stir.
3. **P4-C: dev/proc**. Synthetic `/proc/<pid>/` exposing process state (status, cmdline, fd list, mem map). Walk-driven; no caching.
4. **P4-D: dev/ctl**. `/ctl/` for kernel admin: scheduler stats, IRQ counters, territory dump, `/ctl/proc-events/exit` for driver supervision.
5. **P4-E: dev/ramfs**. cpio-loaded in-memory FS at boot. Holds /init's blob (currently embedded in kernel image; ramfs decouples). Freed once persistent FS mounts at Phase 5.
6. **P4-F: VirtIO core (kernel/virtio.c)**. Split virtqueue. MMIO transport (VIRTIO_MMIO_*). Feature negotiation + virtqueue setup. Audit-trigger surface.
7. **P4-G: kernel/irqfwd.c**. Hardware IRQ → KObj_IRQ blocker wakeup. p99 < 5µs latency budget per VISION §4.5. Audit-trigger surface.
8. ~~**P4-H: kernel/virtio_pci.c**~~. **LANDED.** Minimal PCIe enumeration for VirtIO-PCI devices (ECAM, bus 0, vendor 0x1AF4 + device-ID range identification). BAR mapping + capability walking + INTx/MSI deferred to first PCI-using driver chunk.
9. **P4-I: first userspace driver — virtio-blk** (Rust). Validates the Dev/Spoor/9P/handle plumbing end-to-end. Decomposed into sub-chunks:
    - ~~**P4-Ia1: C userspace runtime (libt) + first hello binary**~~. **LANDED.** Sibling CMake project at `usr/`; libt provides `_start` + SVC wrappers; `/hello` loads from ramfs via `devramfs_lookup` + `exec_setup`. Non-audit-bearing toolchain scaffolding.
    - ~~**P4-Ia2: Rust nostd userspace**~~. **LANDED.** Cargo workspace at `usr/`; aarch64-unknown-none target; `/hello-rs` binary uses inline `asm!` for SVCs + `global_asm!` for `_start` + `#[panic_handler]`. Same linker script + same kernel-side machinery as P4-Ia1. Runtime crate extraction (`libthyla-rs`) deferred to P4-Ic.
    - **P4-Ib: KObj_MMIO + KObj_IRQ handle-table integration**. Syscall surface: `kobj_mmio_create`, `kobj_mmio_map_into_user`, `kobj_irq_wait_handle`. I-4 / I-5 / I-6 audit-bearing; **spec-first** — extends `specs/handles.tla` for hardware-handle non-transferability.
    - **P4-Ic: minimal Rust no_std virtio-blk crate**. Split-virtqueue driver atop the syscall handles from Ib.
    - **P4-Id: driver-as-9P-server integration**. `#blk` synthetic Dev; `read`/`write` Stratum-style; closes ROADMAP §6.3 exit criterion ("a userspace driver reads from virtio-blk via 9P").
10. **P4-J: userspace virtio-net driver**.
11. **P4-K: userspace virtio-input driver**.
12. **P4-L: userspace virtio-gpu driver**.
13. **P4-M: driver supervision** (init/driver-supervisor). Watches driver process exit; restarts on crash. Hooks into `/ctl/proc-events/exit`.
14. **P4-N: BURROW finalize** (`specs/burrow.tla` reconciliation + impl audit).
15. **P4-Z: Phase 4 closing audit**. Cumulative review covering Dev/Spoor/VirtIO/IRQ-fwd/drivers/supervision; ROADMAP §6 exit-criteria checklist. (Trip-hazard #157 closed at P4-Fix157 — no longer a Phase 4 blocker.)

## Trip-hazard #157 — CLOSED at P4-Fix157

**Status (2026-05-07)**: **CLOSED.** Root-caused to `userland_enter` SPSel discipline, not a QEMU virt simulation artifact. Fix: `msr SPSel, #0; isb` immediately before `mov sp, x17` in `arch/arm64/userland.S`. Regression test in `kernel/test/test_userspace2.c` (`userspace.first_iteration` + `userspace.second_iteration`).

See `memory/audit_r7_closed_list.md` § "Trip-hazard #157 — CLOSED at P4-Fix157" for the full root-cause analysis, ruled-out hypotheses, and fix discussion. Quick summary:

- `userland_enter`'s `mov sp, x17` writes SP_EL0 only when SPSel=0. Under the IRQ-driven dispatch path the kthread runs at SPSel=1, so `mov sp` clobbers SP_EL1 instead. After eret to EL0, the user code's first SVC enters EL1 with the corrupt SP_EL1 (= user_sp = 0x80000000, low VA, unmapped); KERNEL_ENTRY's `sub sp + stp` loops infinitely with FAR decreasing by EXCEPTION_CTX_SIZE = 0x120 per cycle.
- The R7 hypothesis chain (MTE / TLB / walker cache / PA recycle) was misdirected — verified by ablation. State was "bit-identical" only because the audit didn't print PSTATE.SPSel or SP_EL1.
- The fix is one `msr SPSel, #0; isb` before `mov sp, x17`. Iter 1 + iter 2 both pass after the fix. P4-I (virtio-blk) and P4-M (driver supervision) are unblocked.

## Exit criteria status

Per `ROADMAP.md §6.2`, post-Phase-4-entry:

- [ ] `cat /dev/random` produces non-zero bytes.
- [ ] **Userspace virtio-blk**: read 1 GiB from VirtIO block device; write 1 GiB and read it back, verify bit-exact.
- [ ] **Userspace virtio-net**: send and receive raw Ethernet frames via `/dev/ether0`; checksum verified.
- [ ] **Userspace virtio-input**: keyboard input from VirtIO console reaches user processes via `/dev/cons`.
- [ ] **Userspace virtio-gpu**: write pixels to framebuffer via BURROW handle; visible on QEMU display.
- [ ] Spoor lifecycle: 10,000 open/read/close cycles on `/dev/null` without leak.
- [ ] Dev vtable: all 17 ops dispatch correctly for cons, null, zero, random, proc, ctl, ramfs.
- [ ] **Driver crash recovery**: kill the virtio-blk driver process mid-I/O; supervisor restarts; subsequent I/O resumes.
- [ ] **Hardware handle non-transferability**: attempt to transfer `KObj_MMIO` panics with explicit "non-transferable type" message.
- [ ] IRQ-to-userspace handler latency p99 < 5µs.
- [ ] `specs/burrow.tla` clean under TLC; `SPEC-TO-CODE.md` maintained.
- [ ] No P0/P1 audit findings on driver model.

## Build + verify commands

```bash
tools/build.sh kernel
tools/test.sh
tools/test.sh --sanitize=undefined
tools/test-fault.sh
tools/verify-kaslr.sh -n 5
```

## Trip hazards

(Cumulative from Phase 1+2+3 plus new Phase 4 entries.)

### NEW at Phase 4 entry

180. **Phase numbering vs ROADMAP scripture**. Local Phase 4 == ROADMAP §6 (titled "Phase 3"). The numbering shift stems from splitting ROADMAP Phase 2's deferred address-space deliverables across local Phase 2 + Phase 3. Either renumber ROADMAP scripture to match (user signoff required per CLAUDE.md scripture-binding) OR keep the offset and document. Status docs use local numbering; ROADMAP references use scripture numbering with explicit cross-reference.

(Future P4-* entries will append here.)

## References

- `docs/ROADMAP.md` §6 — Phase 4 binding scripture.
- `docs/ARCHITECTURE.md` §9 (territory + Dev vtable + driver model) + §13 (MMIO + VirtIO).
- `docs/phase3-status.md` — Phase 3 close.
- `memory/audit_r7_closed_list.md` — Phase 3 closing audit + trip-hazard #157 deferral.
