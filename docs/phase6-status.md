# Phase 6 — status and pickup guide

Authoritative pickup guide for **Phase 6: Pouch — POSIX libc + cross-compilation** (execution Phase 6 per `ROADMAP.md §2.1`; ROADMAP section `§7A`). Binding design: **`docs/POUCH-DESIGN.md`**.

## TL;DR

**Phase 6 is OPEN — implementation underway.** The design session (3 rounds, 2026-05-22) converged; `POUCH-DESIGN.md` is binding scripture. **Sub-chunks 1-15 have landed; sub-chunk 16a (stratumd binary-load probe) has landed.** sub-chunk 1 (`pouch-toolchain`) the `aarch64-thylacine` cross toolchain; sub-chunk 2 (`pouch-musl-vendor`) vendored musl 1.2.5 + the R2 probe (musl compiles end-to-end for `aarch64-thylacine`, so the boundary line is compile-clean); sub-chunk 3 (`pouch-kernel-auxv`, audit-bearing) made `exec_setup` build the System V process-startup frame (argc / argv / envp / auxv) + added `SYS_SET_TID_ADDRESS`; sub-chunk 4 (`pouch-syscall-seam`, audit-bearing) retargeted musl's aarch64 syscall seam to the Thylacine ABI — the syscall-number table (eight 1:1 calls → Thylacine numbers, the rest → a `0xFFFF` unimplemented-syscall sentinel), the sentinel guard on both syscall paths, the flat-`-1`→`errno` decode, the stdio backend — and made `tools/build.sh sysroot` build the real pouch `libc.a` + CRT + headers into `build/sysroot/`. Sub-chunk 5 (`pouch-hello-smoke`) built and ran the first POSIX C programs on Thylacine — `/pouch-hello` (raw `write(2)` + the `0xFFFF` sentinel on both syscall paths) and `/pouch-hello-stdio` (buffered stdio); a POSIX C program now runs on a kernel that knows nothing about POSIX. Sub-chunk 6 (`pouch-compiler-rt`) closes the two toolchain gaps `pouch-hello-smoke` surfaced: 6a (`pouch-ld`) the link-driver wrapper, and 6b the compiler runtime — `libclang_rt.builtins.a`, the compiler-rt builtins vendored + built for `aarch64-thylacine`. The cross-toolchain is now complete — compiler, libc, CRT, runtime — and sub-chunk 6c (`/pouch-hello-printf`) proves it end to end: `printf(3)` runs in Thylacine, its `binary128` soft-float verified correct. **Sub-chunk 7 (`pouch-mem`) is complete** — 7a (the kernel anonymous-memory syscalls `SYS_BURROW_ATTACH` / `SYS_BURROW_DETACH`) and 7b (the pouch side: `0003-pouch-mman` + `/pouch-hello-malloc`) have both landed. mallocng works end-to-end on Thylacine. **Sub-chunk 8 (`pouch-wait-addr`) is also landed** — the kernel-side `torpor` wait-on-address primitive (`SYS_TORPOR_WAIT` / `SYS_TORPOR_WAKE`); the futex-equivalent over which sub-chunk 9's pouch pthread layer runs. Spec-to-code was further suspended project-wide on the same day (user direction, 2026-05-23): the no-lost-wakeup invariant is validated by prose reasoning in the file header rather than a `specs/futex.tla` model; audit + runtime tests are the new rigor floor. **Sub-chunk 9 (`pouch-threads`) is also landed** — 9a (the kernel-side substrate `SYS_THREAD_SPAWN` / `SYS_THREAD_EXIT` + multi-thread Proc reap) + 9b (the pouch-side boundary-line patch retargeting musl's pthread layer onto torpor + `SYS_THREAD_*`) + `/pouch-hello-threads` (5 workers × 1000 mutex-protected counter increments). POSIX pthreads work end-to-end on Thylacine. **Sub-chunk 10 (`pouch-poll`) is now ALSO landed** — `0005-pouch-poll.patch` adds `__NR_poll 29` (Linux aarch64 dropped legacy `poll(2)`; reinstating it lets pristine `src/select/poll.c` route through `SYS_POLL`) and rewrites `ppoll`/`select`/`pselect` to translate their POSIX shapes onto the same kernel primitive; `/pouch-hello-poll` exercises every arm — poll's slow-path tsleep on an empty pipe, poll's fast-path readiness sample after a byte arrives, select's fd_set ↔ pollfd translation, and the zero-fds zero-tv short-circuit. POSIX `poll`/`select`/`ppoll`/`pselect` now work end-to-end against any pollable Thylacine object. **Sub-chunk 11 (`pouch-devnodes`) is now ALSO landed** — `kernel/full.c` adds `devfull` (the third member of the trivial `/dev` Dev trio after `devnull` and `devzero`) with Linux `man 4 full` semantics; `/pouch-hello-getrandom` (the seventh pouch binary) calls musl's `getrandom(2)` over `SYS_GETRANDOM` with joey-granted `CAP_CSPRNG_READ`, proving the libsodium-grade CSPRNG path end-to-end. Path-based `open("/dev/null")` access remains deferred (requires multi-component path resolution). **Sub-chunk 12 (`pouch-sockets`) LANDED 2026-05-23** — AF_UNIX SOCK_STREAM via new BYTE-mode SrvConn + `SYS_POST_SERVICE_BYTE` + per-conn mode dispatch. **Sub-chunk 13 (`pouch-signals`) LANDED 2026-05-24** — 13a kernel notes substrate (4 audit rounds, 42 findings dispositioned) + 13b pouch boundary-line `0007-pouch-signals.patch` (2 audit rounds, 16 findings); `/pouch-hello-signals` proves sigaction + raise + SIG_IGN + EINVAL paths. The fd-first novel angle (NOVEL.md §3.1) survives 6 audit rounds. **Sub-chunk 14 (`pouch-libsodium`) LANDED 2026-05-24** — libsodium 1.0.20 vendored byte-pristine at `third_party/libsodium/`; `tools/build.sh::build_libsodium` archives 97 .c files into `build/sysroot/lib/libsodium.a` (520780 B); `/pouch-hello-sodium` (276848 B static ET_EXEC, the tenth pouch binary) runs 5 KAT-based primitives — sodium_init, SHA-256("abc") FIPS 180-4 KAT, BLAKE2b round-trip, xchacha20-poly1305-IETF AEAD round-trip, ed25519 sign + verify + reject-tampered. NOT audit-bearing per POUCH-DESIGN §14 row 14. **Closes POUCH-DESIGN.md §13's libsodium exit criterion.** **Sub-chunk 16a (`pouch-stratumd-boot` binary-load probe) LANDED 2026-05-24** — `/stratumd-probe` in joey verifies the cross-built stratumd actually runs in Thylacine. Joey calls `t_spawn("stratumd", ...)` + `t_wait_pid(&status)`; stratumd hits its `argc < 2` gate, calls `usage()`, exits with status 1. The probe confirms: kernel ELF loader accepts the binary; pouch's musl `_start` chain runs (auxv handoff + CRT objects + .init_array constructors including the sub-chunk 13b `__pouch_note_handler` register); musl argv exposure works; `SYS_exit_group(1)` executes. `build_stratumd` joined the `build_kernel` chain (incremental on warm rebuilds via CMake/ninja cache); `build_pouch_progs` no longer wipes the daemon binaries it doesn't own. **NOT audit-bearing** (no new kernel surface; no new spawn path). 16b/16c remain: pool generation + block device + mount (audit-bearing); kernel 9P client connect + `/sysroot` mount + ramfs pivot + stub retire (audit-bearing). **Sub-chunk 15 (`pouch-stratumd-build`) LANDED 2026-05-24** — the first non-trivial daemon cross-compiled via pouch. `tools/build.sh::build_stratumd` drives Stratum's CMake tree (consumed from `$STRATUM_SRC` defaulting to `~/projects/stratum/v2`, branch `thylacine-pouch-arm` off `main`@`976cb6f`) through the updated pouch toolchain; configure-flags disable Linux-only features (io_uring / libaio / liboqs PQ) + tests + fuzzers + Werror. Output: 860536 B static ET_EXEC at `build/pouch/progs/stratumd`. Toolchain updates: adds `-D__thylacine__=1` + `-D_GNU_SOURCE=1`; `-nostdinc` → `-nostdlibinc` (keeps clang's resource headers for xxHash's `<arm_neon.h>`); removed `-fstack-clash-protection` (clang reports unused on aarch64-thylacine); new `CMAKE_C_LINK_EXECUTABLE` routes link through `pouch-ld`. Stratum-side coordination (branch on user's Stratum repo): `STM_PLATFORM_THYLACINE` detection + synthesized `Threads::Threads` + `PkgConfig::LIBSODIUM` IMPORTED targets + `_FORTIFY_SOURCE=2` skip (musl doesn't implement); extends `peer_creds.c`'s `__linux__` arm guard to `__linux__ || __thylacine__` (pouch's `0006-pouch-sockets.patch` marshals `SO_PEERCRED` onto `SYS_srv_peer`). NOT audit-bearing per POUCH-DESIGN §14 row 15. **Next: sub-chunk 16 (`pouch-stratumd-boot`)** — joey spawns real stratumd; ramfs pivot to `/sysroot`; closes Phase 5 + Phase 6.

Phase 6 builds **pouch** — the Thylacine-native POSIX libc (a musl derivative: musl's portable upper half + a Thylacine-native lower half, split at musl's syscall seam) — plus the `aarch64-thylacine` cross-compilation toolchain + sysroot, plus the POSIX runtime layer that translates POSIX abstractions into Thylacine primitives (`AF_UNIX`→`/srv`, `poll(2)`→`t_poll`, `sigaction`→notes, pthreads→Thylacine threads + the `torpor` wait-on-address primitive). The proving binary is real **stratumd**; the durable deliverable is the cross-compilation path itself.

Phase 6 **preempts the Phase 5 close**: Phase 5 (9P client + Stratum integration) is suspended, substantially complete — its tail (real stratumd, P5-hostowner-c, P5-login) is pouch-dependent. When Phase 6's stratumd criteria are met, the bulk of Phase 5's own exit criteria are satisfied too.

Why a whole phase: Thylacine's practicality as a real OS depends on POSIX/Linux/BSD software reuse; that capability is phase-sized and load-bearing. Stratum is a Thylacine-native-primary project, so a first-class Thylacine build+run target for it is core, not accommodation.

## Phase entry pickup (read order)

1. `docs/POUCH-DESIGN.md` — the binding design. Read in full; §18 is the decision log.
2. `docs/ROADMAP.md §7A` — the ROADMAP-level phase summary.
3. `docs/ROADMAP.md §3.6` — "the compat layer is built on top, not baked in" — the principle pouch makes structurally true (invariant P-1).
4. `docs/ARCHITECTURE.md §16` + `§23` — the POSIX-compat + POSIX-surfaces architecture pouch realizes.
5. `memory/project_pouch_phase.md` — the phase decision + rationale.
6. The 3 research reports informing the design (summarized in POUCH-DESIGN.md §1 + the risk register).

## Landed chunks

| Commit SHA | What | Tests |
|---|---|---|
| `af6fd82` | **P6-pouch-stratumd-boot 16b-γ-syscalls — native fstat + lseek + open() arm + kernel walk-open fixes.** Closes the POSIX-surface side of 16b-γ; the bdev_thylacine read-I/O block (the remaining 16b-γ piece) is re-scoped as **16b-γ-mount-close** (separate sub-chunk; not in this commit). What lands: (1) kernel `SYS_FSTAT = 50` + `SYS_LSEEK = 51` (`kernel/include/thylacine/syscall.h`); (2) `struct t_stat` (72-byte ABI; field offsets pinned by `_Static_assert`s — every byte position locked); (3) `Dev` vtable `.stat_native(c, out)` slot; (4) `kernel/devramfs.c::devramfs_stat_native` impl (fills size+mode+qid from the in-kernel cpio file table) + `devramfs_walk` reuse-nc shape (was self-cloning; sys_walk_open_handler's `w->spoor == nc` check made every userspace FROM_ROOT walk against devramfs silently fail); (5) `kernel/syscall.c::sys_fstat_handler` + `sys_lseek_handler` + dispatch wiring + new partial-walk reject in `sys_walk_open_handler` (`w->nqid != nname` → -1; pre-fix a walk-miss returned an opened fd bound to the source Spoor's pre-walk qid — root dir, silently); (6) `kernel/joey.c::joey_run` stamps kproc's Territory's `root_spoor` to devramfs root before joey rfork (so all descendants inherit a sane default — every Proc could now FROM_ROOT-walk devramfs); (7) libt `t_fstat(fd, &st)` + `t_lseek(fd, offset, whence)` wrappers + struct t_stat mirror with offsetof asserts; (8) **`0010-pouch-fstat-lseek.patch`** (3-file boundary-line patch: `bits/syscall.h.in` redefines `__NR_fstat`/`__NR_lseek` from 0xFFFF→50/51; `src/stat/fstat.c` is rewritten to translate `t_stat` → musl's aarch64 `struct stat`; `src/fcntl/open.c` is rewritten to forward to `openat(AT_FDCWD, ...)` — the upstream musl `open()` shortcut bypassed our patched `openat.c` via `SYS_openat=0xFFFF`); (9) `tools/build.sh::build_ramfs` places system.key at the ramfs root (`/system.key`, not `/etc/stratum/system.key` — devramfs is flat at v1.0; FHS-shaped layout is a v1.x lift atop devramfs subdir walks; K1 boot-key decision preserved, only the realization shifts); (10) `usr/joey/joey.c` adds a per-boot `/system.key` probe (walk + fstat + lseek SEEK_END/SEEK_SET round-trip) BEFORE stratumd-boot so the syscall surfaces are exercised every boot; joey's stratumd argv updated to `/system.key`; (11) `usr/pouch-hello/pouch-hello.c` sentinel test updated (open() now lifts; missing-path returns ENOENT, not ENOSYS). End-to-end verification: stratumd's `stm_keyfile_load("/system.key")` reaches `stm_bdev_open(THYLACINE)` — `open + fstat + read(8) + lseek(0) + read(3656) + magic-check + parse` all succeed; failure now surfaces inside `stm_sb_mount_scan` (the bdev I/O read-path block for 16b-γ-mount-close). Audit posture: **NOT audit-bearing at this checkpoint** by deliberate scope split — `sys_fstat_handler`/`sys_lseek_handler`/the partial-walk reject/the devramfs walk-nc fix are all invariant-bearing, but the audit is paired with 16b-γ-mount-close's bdev I/O surface; running them concurrently halves prosecutor cost. CLAUDE.md rows "stratumd HW-cap spawn" + "stratumd virtio-blk driver arm" + the new "native fstat + lseek surface" row all stay open until 16b-γ-mount-close. Detail: `docs/reference/87-pouch-fstat-lseek.md`. | **609/609 PASS × default + UBSan** (was 599/599; +10 from the new probe trace going through stat_native + FROM_ROOT-walk via the now-live kproc chroot). 0 UBSan errors. Boot log confirms: `joey: probe /system.key fstat OK size=3656 mode=0o100644` + `joey: probe /system.key lseek SEEK_END/SEEK_SET OK`. stratumd's diagnostic in the boot log shows it reaches `stm_bdev_open OK` + `stm_sb_mount_scan FAILED rc=-2` (bdev read-path blocker; 16b-γ-mount-close). `Thylacine boot OK`. |
| `416b4f3` | **pouch-stratumd-boot-16b-gamma-syscalls: hash fixup** | (placeholder fill) |
| `a059d6b` | **P6-pouch-stratumd-boot 16b-γ-mount-close R1 audit close (paired audit, 0 P0 + 1 P1 + 4 P2 + 6 P3 dispositioned).** Paired audit covered 16b-γ-syscalls (carried-over) + 16b-γ-mount-close surfaces. **FIXED in-place** (3 findings): F3 [P2] lseek on non-seekable Dev (kernel sys_lseek_handler now rejects with -1 when `c->dev->stat_native == NULL`; stat_native presence is the v1.0 seek-capability indicator); F6+F11 [P3] pouch __fstat zero-loop timing (zero-loop moved to BEFORE the syscall so partial uaccess fault leaves local t_stat in deterministic zero state instead of stack garbage). **DEFERRED to v1.x with documented carry-forwards** (8 findings): F1 [P1] pouch abort + multi-thread → kernel extinction (blocker for 16c; needs SYS_EXIT_GROUP); F2 [P2] joey success-branch wait_pid race (deferred to 16b-γ-mount-bind when the success path activates); F4 [P2] bdev_thylacine kobj-handle leak on partial-init failure (needs SYS_CLOSE_HW); F5 [P2] SYS_LSEEK errno collapse (pre-existing project-wide pattern; needs kernel-returns-`-errno` shift); F7 [P3] bdev fixed VAs collide with future binaries (SYS_MMIO_MAP_ANYWHERE); F8 [P3] joey wait_pid timeout (t_wait_pid_timeout); F9 [P3] kproc.wait_pid pid-filter (wait_pid_for(pid); also closes F2); F10 [P3] bdev count==0 silent passthrough (cosmetic). Clean by count (P1+P2 sum = 5 < 6 threshold, no P0, no invasive restructure → no re-audit). Detail: `audit_p6_pouch_stratumd_boot_16b_gamma_mount_close_closed_list.md`. | 599/599 PASS x (default + UBSan), 0 UBSan errors. `Thylacine boot OK`. |
| `f0cb68b` | **pouch-stratumd-boot-16b-gamma-mount-close-audit: hash fixup** | (placeholder fill) |
| `51ce0b9` | **P6-pouch-stratumd-boot 16b-γ-mount-close — bdev_thylacine read-I/O fix + pouch abort override + joey reap workaround.** Closes the bdev read-I/O block of 16b-γ. Three coordinated changes: (A) **Stratum-side** (`thylacine-pouch-arm` branch in `~/projects/stratum/v2`; user pushes; this Thylacine commit references that branch's commit `389c742`): `src/block/bdev_thylacine.c::T_RIGHT_SIGNAL` was `(1u << 3)` (= kernel `RIGHT_TRANSFER`); corrected to `(1u << 5)` (= kernel `RIGHT_SIGNAL` per `kernel/include/thylacine/handle.h` + `libthyla_rs::T_RIGHT_SIGNAL` + `libt`'s `T_RIGHT_SIGNAL`). The previous constant let `t_irq_create` succeed (the rights mask was within `RIGHT_ALL`) but every subsequent `t_irq_wait(d->irq_handle)` failed the kernel's `slot->rights & RIGHT_SIGNAL` gate, returning -1; `do_request`'s `if (count < 0) return STM_EIO;` collapsed every read to EIO and `stm_sb_mount_scan` saw every label_read fail and returned `STM_ENOENT`. Found by diff against `libthyla_rs` after diagnostic fprintfs showed `t_irq_wait returned count=-1` for every request. The Rust `usr/virtio-blk-rw` driver was unaffected because it consumes `libthyla_rs`'s correct constant. (B) **`0011-pouch-abort.patch`** (NEW; eleventh entry in the pouch boundary-line series) overrides musl's `src/exit/abort.c` to call `_Exit(127)` directly. Skips the upstream `raise(SIGABRT) + a_crash()` chain — at v1.0 the kernel's FAULT_UNHANDLED_USER policy extincts on the deliberate NULL deref `a_crash()` emits, taking the whole boot down. This is the v1.x extension documented in `docs/reference/83-pouch-signals.md`; landed early because the bdev fix unblocks downstream stratumd code paths that can `assert()`/`abort()`. Behavioral delta: parents observe `WIFEXITED + WEXITSTATUS == 127` instead of `WIFSIGNALED + WTERMSIG == SIGABRT`; SIGABRT handlers (if any) are NOT invoked under this path. (C) **`usr/joey/joey.c`** retry+drain+reap workaround: 6000-retry srv_connect (up from 600); per-retry non-blocking pipe drain via `t_poll(timeout=0)` + `t_read` + `t_puts` so stratumd's stderr surfaces in real time and the pipe buffer doesn't fill and block stratumd's writes; failure-branch bounded drain (200 iter × 10 ms) followed by `t_wait_pid` that reaps stratumd's zombie before joey-userspace exits — prevents the unreparented-zombie / kproc-joey_run-wait_pid `wrong pid` extinction race (the kernel's `wait_pid` is unfiltered at v1.0; if stratumd's zombie reparents to kproc on joey-userspace's exit, kproc's wait_pid can return stratumd's pid first and the `reaped != pid` check in `kernel/joey.c:204` extincts). End-to-end verification after fix: stratumd's `stm_bdev_open(THYLACINE)` succeeds AND `stm_sb_mount_scan` decodes the uberblock at pool.img label-0 slot-4 (STRATUM2 magic at offset 0x4000); the mount path proceeds into `stm_pool_open` / `stm_alloc_open_blank` / `stm_sync_create` then exits clean with `exit_status=1` (a separate downstream Stratum-internal failure, NOT in this sub-chunk's scope). joey probe stays NON-FATAL pending the next sub-chunk that investigates the downstream mount failure. **Audit-bearing**: paired audit covers (a) Stratum bdev_thylacine RIGHT_SIGNAL fix; (b) 0011-pouch-abort.patch (process termination semantics); (c) joey retry+drain+reap workaround; (d) carried-over 16b-γ-syscalls surfaces from `af6fd82` (sys_fstat_handler + sys_lseek_handler + partial-walk reject + devramfs walk-nc fix + joey root_spoor pivot). CLAUDE.md gains rows "pouch abort -> _Exit override" + "Stratum bdev_thylacine rights mirror"; "stratumd HW-cap spawn" + "stratumd virtio-blk driver arm" + "native fstat + lseek surface" rows verified covered by the paired audit. Detail: `docs/reference/86-pouch-stratumd-boot.md` "16b-γ-mount-close" section. | **599/599 PASS × default + UBSan**, 0 UBSan errors. Boot log: `joey: stratumd-boot child exit_status=1 (final drain)` reproducibly. `Thylacine boot OK`. |
| `9e5e713` | **pouch-stratumd-boot-16b-gamma-mount-close: hash fixup** | (placeholder fill) |
| *(prior session)* | **P6-pouch-stratumd-boot 16b-β foundation (sub-chunk 16b-β/16) — stratumd virtio-blk driver arm + pool fixture + joey integration.** Foundation work for 16b-β; **end-to-end pool mount deferred to a follow-up sub-chunk (16b-γ)** because two pouch-side v1.x lifts surface late in the keyfile-load path: (1) pouch `fstat()` still `__NR_fstat = 0xFFFF` (ENOSYS sentinel — `stm_keyfile_load` calls fstat after open); (2) `tools/mkcpio.py` is FLAT-only at v1.0 + kernel/devramfs.c keeps a flat file table — `/etc/stratum/system.key` can't actually exist in the ramfs even with openat path-walk landed. The 16b-β foundation is everything *upstream* of those two lifts; 16b-γ will land the lifts + close the end-to-end mount + the audit-bearing close (CLAUDE.md audit-trigger rows "stratumd HW-cap spawn" + "stratumd virtio-blk driver arm" stay open). **Stratum-side** (`thylacine-pouch-arm` branch in `~/projects/stratum/v2`; user pushes): `src/block/bdev_thylacine.c` (NEW, ~610 LOC) — port of `usr/virtio-blk-rw/src/main.rs` into a long-lived `stm_bdev` arm; VirtIO 1.2 §3.1.1 init; 3-descriptor chain reused per request; IRQ-driven completion with bounded spurious-wake tolerance; per-bdev pthread_mutex; 1 MiB data DMA + 4 KiB ring DMA; HIGH-to-LOW slot scan so FIRST-listed virtio-blk-device = pool gets priority. Plus `src/block/bdev.c` AUTO + switch case; `bdev_internal.h` forward decl; `include/stratum/block.h` adds `STM_BDEV_BACKEND_THYLACINE = 3`; `src/block/CMakeLists.txt` swaps posix.c → bdev_thylacine.c on STM_PLATFORM_THYLACINE. **Thylacine-side**: `0008-pouch-hw-syscalls.patch` (`__NR_mmio_create=2`/`irq_create=3`/`irq_wait=4`/`mmio_map=5`/`dma_create=6`/`dma_map=7`); `0009-pouch-openat.patch` (rewrites musl's openat() to walk absolute paths via `SYS_walk_open=34` — lifts the v1.x deferred "Path-based open() — multi-component walk" item; necessary but NOT sufficient for keyfile load); `build_stratum_mkfs_host` + `build_stratum_pool_fixture` (host stratum-mkfs → `build/fixtures/pool.img` 64 MiB + `system.key` 3656 B); `build_ramfs` copies system.key to `etc/stratum/system.key` (no-op at v1.0 due to flat cpio; lifts in 16b-γ); `tools/run-vm.sh` adds second virtio-blk-device for pool.img (listed FIRST → slot 31); `usr/joey/joey.c` replaces 16a probe with foundation probe: spawns stratumd via `t_spawn_full_argv(&req)` with argv `["stratumd", "/dev/virtio-blk", "--listen", "/srv/stratum-fs", "--keyfile", "/etc/stratum/system.key"]`, `cap_mask=T_CAP_HW_CREATE`, `perm_flags=T_SPAWN_PERM_MAY_POST_SERVICE`, fd 0/1/2 inherited from captured pipe for stderr-in-boot-log; bounded `t_srv_connect("stratum-fs")` retry loop; **NON-FATAL on retry exhaustion** at this checkpoint (16b-γ will flip it to fatal post-lifts). Boot output confirms argv pass-through end-to-end: stratumd prints `serving /dev/virtio-blk on /srv/stratum-fs (...)` then `run failed (rc=-2)` (STM_ENOENT from keyfile-load path documented above). **NOT audit-bearing at this checkpoint** — the audit fires on the 16b-γ end-to-end close. Detail: `docs/reference/86-pouch-stratumd-boot.md` "16b-β" section. | No kernel-test-suite change; suite stays **599/599 PASS × default + UBSan**, 0 UBSan errors. `tools/build.sh kernel` chains: `build_stratumd` (warm <5s) → `build_stratum_pool_fixture` (host stratum-mkfs ~1s) → `build_ramfs` → `build_disk`. `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **599/599 PASS**, joey logs the foundation probe's non-fatal failure with stratumd's "serving" line + rc=-2 captured via the pipe-inherit fd path. |
| `fc28aaa` | **pouch-stratumd-boot-16b-beta-foundation: hash fixup** | (placeholder fill) |
| *(pending)* | **pouch-stratumd-boot-16b-alpha: hash fixup** | (placeholder fill) |
| `338cd44` | **P6-pouch-stratumd-boot 16b-α (sub-chunk 16b-α/16) audit close: argv pass-through kernel surface — `SYS_SPAWN_FULL_ARGV = 49`. 0 P0 + 0 P1 + 2 P2 + 9 P3 = 11 findings dispositioned (R1 close).** New struct-based syscall that subsumes the SYS_SPAWN_* family + adds argv pass-through (the legacy spawn surface inherits only `argv = [name]`; stratumd in 16b-β needs a real argv: pool path + `--keyfile` + `--listen` + ...). Rather than yet another register-based permutation of SYS_SPAWN_*, 16b-α adds ONE struct-based entry point that carries every existing feature (fds + cap_mask + perm_flags) plus the new argv fields. Kernel: new `struct sys_spawn_args` (56-byte ABI; offset + size `_Static_assert`s pin every wire field; `_pad_envp` reserved for forward-compat envp pass-through); `sys_spawn_full_argv_handler` (uaccess-copies struct + sub-buffers + validates every field including argv NUL-termination invariant; field-bound validation extracted as `sys_spawn_full_argv_validate_req` for kernel-test coverage per R1 F1); internal `sys_spawn_full_argv_with_perms_for_proc` (mirrors the existing `sys_spawn_full_with_perms_for_proc` body + extends with argv); `sys_spawn_full_argv_thunk` (installs fds + applies perms + calls `exec_setup_with_argv` + kfree's argv buffer at thunk's end — lifetime ends here, never visible to a third Proc). exec layer: `exec_build_init_stack` extended with argv params; supports two shapes: **Shape A** (argc=0; legacy 144-byte frame; bit-identical to pre-16b-α `exec_setup` callers) and **Shape B** (argc>0; variable-size frame; argc word + argc argv ptrs + NULL + envp NULL + auxv 6 entries + AT_RANDOM block + argv strings region; argv[i] pointers point into the strings region; frame_size rounded to 16-align); `exec_setup_with_argv` is the new exported entry, `exec_setup` wraps it with `(NULL, 0, 0)`. libt: `t_spawn_full_argv(&req)` inline wrapper; `struct t_sys_spawn_args` mirrors the kernel ABI (with per-field offsetof asserts per R1 F8). Pouch probe: new `/pouch-hello-argv` POSIX C binary that prints argc + each argv[i] back through stdio + asserts `argv[argc] == NULL` (R1 F2). Joey: new `pouch_smoke_one_argv` (mirrors `pouch_smoke_core` but routes through `t_spawn_full_argv` + takes an array of `argv_marker` substrings per R1 F2); invocation in `do_pouch_hello_smoke` with argv `["pouch-hello-argv", "alpha", "beta", "gamma"]`; content-checks 6 per-position markers (argc=4, argv[0..3], argv[argc] NULL terminator — proves every argv pointer placed correctly, not just the last one). **R1 audit close (2026-05-25)**: F1 (P2; helper extracted + 4 helper-direct tests) + F2 (P2; per-position markers + NULL-terminator assertion) + F4 (P3; argc>0 early-reject in helper) + F5 (P3; thunk doc-comment fix) + F6 (P3; header docs include _pad_envp + symmetry) + F7 (P3; "must be aligned" doc removed) + F8 (P3; libt offsetof asserts) + F11 (P3; gate test via proc_alloc). Deferred: F3 + F9 + F10 (P3; pre-existing patterns in sys_spawn_with_fds_thunk or trivial cosmetic — folded into a future hygiene chunk). **K1 boot-key decision (2026-05-25, user signoff)** captured in the design-decisions section of `docs/reference/86-pouch-stratumd-boot.md`: system pool wrap key as literal `/etc/stratum/system.key` ramfs file for v1.0; K2 transition to Stratum --no-wrap mode trivial once Stratum lands it. Boot log confirms: `pouch-hello-argv: argc=4`, `argv[0]=pouch-hello-argv`, `argv[1]=alpha`, `argv[2]=beta`, `argv[3]=gamma`, `argv[argc] NULL terminator ok`, `exit 0`. **Audit-bearing per POUCH-DESIGN.md §14 row 16b-α + CLAUDE.md audit-trigger row "argv pass-through to spawn"** — new kernel surface; per-element NUL-termination invariant; argv buffer lifetime bounded by spawn_args struct; no smuggled handles. Detail: `docs/reference/86-pouch-stratumd-boot.md` "16b-α" section + "Audit close (round 1)" sub-section. | **13 new kernel tests** (8 initial + 5 R1 close): `sys_spawn_full_argv.no_argv_acts_as_spawn_with_perms` (Shape A parity), `golden_argc4` (Shape B golden), 6 negative paths (`rejects_argc_over_max` / `rejects_data_len_over_max` / `rejects_missing_trailing_nul` / `rejects_nul_count_mismatch` / `rejects_argc_with_zero_data_len` / `rejects_zero_argc_with_nonzero_data`), 4 helper tests (R1 F1: `validate_req_golden` / `validate_req_rejects_pad_envp` / `validate_req_rejects_unknown_perm_bits` / `validate_req_rejects_oversize_fields`), 1 gate test (R1 F11: `rejects_non_console_attached_perm_flags`). Test count: 586/586 → 594/594 (initial 16b-α) → **599/599 PASS × default + UBSan**, 0 UBSan errors. Runtime: `tools/build.sh kernel` + `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, 599/599 PASS, joey logs `pouch-hello-argv smoke ok (argv pass-through via SYS_SPAWN_FULL_ARGV)` with all 6 per-position markers verified. |
| `e82e945` | **P6-pouch-stratumd-boot 16b-design: stratumd-as-driver (delta) scripture.** In-session design conversation 2026-05-25 (user signoff via AskUserQuestion). Sub-chunk 16b further sub-chunked into 16b-α / 16b-β under the **stratumd-as-driver (δ)** architectural decision (chosen over: α writable in-RAM pool via new kernel synth Dev; β separate `virtio-blkd` Rust daemon fronting virtio-blk via 9P; γ defer real backing to post-Phase-6 bootloader phase). Stratumd holds `CAP_HW_CREATE` and drives the virtio-blk hardware directly via a new Stratum-side `src/io/bdev_thylacine.c` arm. Plan-9-canonical (the FS server owns its disk); Stratum already has platform arms (Darwin/Linux); `bdev_thylacine.c` is the natural seam; honest capability allocation (I-2 monotonic reduction + I-5 HW-handle non-transferability hold); persistent backing via QEMU disk.img; no daemon protocol seam to design; reversible. **K1 initramfs-literal boot key (2026-05-25, user signoff)** chosen over K2 (Stratum --no-wrap mode; scripture change) / K3 (TPM-attested; v1.x). System pool wrap key as literal ramfs file `/etc/stratum/system.key`. User confirmed K2 transition will be trivial once Stratum lands unencrypted-volume mode. Files: `docs/POUCH-DESIGN.md` (§14 row 16 re-scoped; §15 R3 updated with bdev_thylacine.c mention), `CLAUDE.md` (3 new audit-trigger rows: argv pass-through to spawn / stratumd HW-cap spawn / stratumd virtio-blk driver arm), `docs/reference/86-pouch-stratumd-boot.md` (new "Design decisions" section), `docs/phase6-status.md` (remaining-work row 16 re-scoped + next hint re-pointed to 16b-α). NO code in this commit per CLAUDE.md design-conversation-to-scripture discipline. | No code change — scripture only. 586/586 PASS × default + UBSan unchanged. |
| `a2ed9c7` | **pouch-stratumd-boot-16a: hash fixup** | (placeholder fill) |
| `d4224f7` | **P6-pouch-stratumd-boot 16a (sub-chunk 16a/16): stratumd binary-load probe in joey.** The first sub-chunk of the three-stage 16 sub-chunking (16a/16b/16c per the in-session scoping decision). Wires the cross-built stratumd into the ramfs and adds a `/stratumd-probe` block to joey that verifies the binary loads + libc init runs + argv parsing executes + clean exit. `tools/build.sh::build_ramfs` gains a new `pouch_daemon_bins=( "stratumd" )` array (separate from `pouch_bins` for clarity — daemon vs. hello-test); `build_stratumd` joins the `build_kernel` chain after `build_pouch_progs` so `tools/build.sh kernel` produces a complete ramfs in one shot (incremental on warm rebuilds via CMake's cache + ninja's dep tracking, <5s typical; first build ~2-3 min); `build_pouch_progs::rm -rf "$progs_out"` changed to a targeted `rm -f "$progs_out"/pouch-hello*` so the daemon binaries installed by `build_stratumd` in the same directory aren't clobbered; `build_stratumd::rm -rf "$stratumd_build"` removed so CMake's incremental build path works. Joey's probe block (after the corvus dance, before the stub-bringup): `t_spawn("stratumd", 8)` + `t_wait_pid(&sd_status)`; logs `joey: /stratumd-probe reaped status=<N> (binary loaded, argv parsed, usage exit)` on non-zero exit (stratumd's `argc < 2` path returns 1). Boot log confirms: `joey: /stratumd-probe reaped status=1 (binary loaded, argv parsed, usage exit)`. The probe exercises kernel ELF accept + auxv handoff + the CRT objects + .init_array constructors (including the 13b notes handler register) + musl argv exposure + `SYS_exit_group(1)`. What the probe does NOT exercise (deferred to 16b/16c): `mlock`/`madvise`/`mprotect` calls (those live in mount + key-handling paths, ENOSYS via 0xFFFF sentinel today); socket bind on the FS Unix socket; pthread creation; 9P codec; AEAD; Argon2id; block device I/O. **NOT audit-bearing per POUCH-DESIGN.md §14 row 16a** — no new kernel surface; the spawn path is well-trodden (every pouch-hello-* probe in joey uses the same shape). 16b will be audit-bearing (block device + boot ordering); 16c will be audit-bearing (boot ordering + I-1 namespace isolation across the chroot). Detail: `docs/reference/86-pouch-stratumd-boot.md`. | No kernel-test-suite change — sub-chunk 16a touches no kernel code; the kernel suite stays **586/586 PASS × default + UBSan**, 0 UBSan runtime errors. The 16a runtime regression is `/stratumd-probe` in joey: `tools/build.sh kernel` now also runs `build_stratumd` (incremental warm); `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **586/586 PASS**, joey logs the probe's success line. UBSan boot also green (0 UBSan errors). |
| `bdcb1f4` | **pouch-stratumd-build: hash fixup** | (placeholder fill) |
| `1ae15e3` | **P6-pouch-stratumd-build (sub-chunk 15/16): stratumd cross-compiled against the pouch sysroot for `aarch64-thylacine`.** The first non-trivial daemon ported via pouch. New `tools/build.sh::build_stratumd` drives Stratum's CMake tree (consumed from `$STRATUM_SRC` defaulting to `~/projects/stratum/v2`, branch `thylacine-pouch-arm` off Stratum `main`@`976cb6f`) through the pouch toolchain; configure-flags disable Linux-only features (io_uring / libaio / liboqs PQ) + tests + fuzzers + Werror. Output: 860536-byte static ET_EXEC at `build/pouch/progs/stratumd` (verified: ET_EXEC + no PT_DYNAMIC + symbol `stm_peer_creds` defined). Toolchain (`cmake/Toolchain-aarch64-pouch.cmake`) updated: adds `-D__thylacine__=1` + `-D_GNU_SOURCE=1` at the toolchain level; `-nostdinc` → `-nostdlibinc` (keeps clang's own resource headers — `arm_neon.h` needed by `third_party/xxHash_src`); removed `-fstack-clash-protection` (clang on aarch64-thylacine reports it unused at link); new `CMAKE_C_LINK_EXECUTABLE` routes the link step through `tools/pouch-ld` (which drives ld.lld directly + auto-supplies CRT + libc.a + libclang_rt.builtins.a — clang as link driver mis-selects the host Darwin toolchain for unknown OS triples). Stratum-side coordination (branch `thylacine-pouch-arm` in `~/projects/stratum/v2`, NOT pushed; user-pushed): two files changed. `v2/CMakeLists.txt` — adds `STM_PLATFORM_THYLACINE` detection (when `CMAKE_C_COMPILER_TARGET == aarch64-thylacine`); synthesizes `Threads::Threads` + `PkgConfig::LIBSODIUM` IMPORTED targets on Thylacine (host `find_package(Threads)` and `pkg_check_modules(libsodium)` don't see the cross-sysroot — pouch's musl bundles pthread in libc, libsodium ships pre-built without a `.pc` file); skips `_FORTIFY_SOURCE=2` on Thylacine (musl doesn't implement it). `v2/src/cmd/stratumd/peer_creds.c` — extends the `__linux__` arm guard to `__linux__ \|\| __thylacine__` with the SAME `getsockopt(SO_PEERCRED)` body (pouch's `0006-pouch-sockets.patch` already marshals `SO_PEERCRED` onto Thylacine's `SYS_srv_peer` underneath, returning a `struct ucred` with `pid = peer stripes`, `uid = 0`, `gid = 0` — the lossiness tolerable since Thylacine has no uid model at v1.0; a forward-looking `t_srv_peer` arm exposing the full kernel-stamped record is a v1.x followup). The other 10 platform-conditional files in Stratum's tree fall through cleanly on Thylacine without a third arm (9p/server.c's `_Static_assert` is a Linux-only check; serve.c is a redundant `#include`; block/posix.c's BLKDISCARD + FALLOC_FL_PUNCH_HOLE are feature-gated; fs.c + pool.c's pthread tuning is feature-gated; corvus_client.c's MADV_DONTDUMP is feature-gated; janus/host-fs/slate auxiliary tools aren't built into stratumd). Coordination artifact: `docs/session-handoff-2026-05-24-thylacine-pouch-arm.md` (in the Stratum repo, on the same branch) — the hand-off note for the Stratum agent describing the change + the integration intent. Stratumd is NOT yet added to the ramfs at this sub-chunk (binary exists at `build/pouch/progs/stratumd`); that's sub-chunk 16's job, which will also have joey spawn it. NOT audit-bearing per POUCH-DESIGN.md §14 row 15 — Stratum-side coordination; pouch's surface is unchanged. Detail: `docs/reference/85-pouch-stratumd-build.md`. | No kernel-test-suite change — sub-chunk 15 touches no kernel code; the kernel suite stays **586/586 PASS × default + UBSan**, 0 UBSan runtime errors. The 15 regression is the cross-build itself: `tools/build.sh sysroot` rebuilds the sysroot (libc.a 2412088 B + libclang_rt.builtins.a 227260 B + libsodium.a 520780 B; verified clean); `tools/build.sh stratumd` cross-builds 30+ static libs + the stratumd binary against the sysroot (CMake-driven, ~2-3 min); `tools/build.sh kernel` + `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **586/586 PASS**, ten pouch hello binaries all run as before. |
| `38464df` | **pouch-devnodes: hash fixup** | (placeholder fill) |
| `5c9a7f7` | **pouch-libsodium: hash fixup** | (placeholder fill) |
| `cd93d65` | **P6-pouch-libsodium (sub-chunk 14/16): libsodium 1.0.20 cross-compiled for `aarch64-thylacine`.** The first cross-compiled C library against pouch — proves the cross-toolchain end-to-end by building a real-world crypto library and running known-answer tests in Thylacine. libsodium 1.0.20 vendored byte-pristine at `third_party/libsodium/` (583 files, 1.9 MiB tarball, sha256 `ebb65ef6...0ce19`, ISC license); no patch series (portable C, OS contact only via `getentropy(3)` → `getrandom(2)`). New `tools/build.sh::build_libsodium` supplies the `HAVE_*` configuration macros that `./configure` would emit and generates `sodium/version.h` from `version.h.in` (placeholder substitution: `@VERSION@` → `1.0.20`, `@SODIUM_LIBRARY_VERSION_MAJOR@` → `26`, `@SODIUM_LIBRARY_VERSION_MINOR@` → `2`, MINIMAL_DEF empty); compiles 97 .c files (the `libsodium_la_SOURCES` non-MINIMAL set + ref-salsa20 + randombytes — x86 ASM and ARMv8 crypto extension sources excluded since pouch's `-march` baseline is `armv8-a+lse+pauth+bti`); archives via `llvm-ar` into `build/sysroot/lib/libsodium.a` (520780 bytes). Headers install to `build/sysroot/include/sodium.h` + `build/sysroot/include/sodium/*.h`. Archive-symbol verification (`llvm-nm --defined-only`) pins `sodium_init`, `randombytes_buf`, the AEAD encrypt/decrypt pair, `crypto_hash_sha256`, `crypto_generichash`, ed25519 sign/verify/keypair, `sodium_version_string` — a missing primary symbol surfaces vendor / source-list drift before the proving binary even links. The **proving binary** `/pouch-hello-sodium` (`usr/pouch-hello/pouch-hello-sodium.c`) — 276848 bytes static ET_EXEC, the tenth pouch binary. Five primitives exercised: (1) `sodium_init()` returns 0 (library init via critical-section pthread mutex + cpu-features detection + randombytes_stir + alloc_init + pick_best_implementation calls); (2) SHA-256("abc") matches the FIPS 180-4 KAT (`ba7816bf...0015ad`); (3) BLAKE2b-256 round-trip (two consecutive hashes match bit-exactly); (4) xchacha20-poly1305-IETF AEAD round-trip (random key + nonce + AAD; decrypted plaintext matches); (5) ed25519 sign + verify + reject-tampered-signature (`crypto_sign_keypair` + `crypto_sign_detached` + `crypto_sign_verify_detached` must accept valid + reject XOR'd byte). joey spawns the binary via `pouch_smoke_one_caps` with `T_CAP_CSPRNG_READ` (the CSPRNG path reaches the kernel through `getentropy(3)` → `getrandom(2)` → SYS_GETRANDOM, gated on the cap). Build wiring: `build_libsodium` called by `build_sysroot` after `build_compiler_rt`; `build_pouch_progs` adds per-binary `-L$sysroot/lib -lsodium` to the link line for `pouch-hello-sodium` only; `build_ramfs`'s `pouch_bins` array gains the new entry. Two sizing-only kernel changes were forced by the binary set growing: `RAMFS_FILE_MAX` 32 → 64 (the ramfs file table had to accommodate 33+ entries — `welcome` was getting dropped at the tail of the cpio iteration, breaking 4 devramfs tests); `SYS_SPAWN_BLOB_MAX` 256 KiB → 1 MiB (the 277 KB binary exceeded the existing exec-cpio cap; stratumd in sub-chunk 16 will exceed it further). Neither change is invariant-bearing; both are documented in the new constant comments. **Closes POUCH-DESIGN.md §13's libsodium exit criterion** ("libsodium cross-compiles against pouch and its self-test passes"). NOT audit-bearing per POUCH-DESIGN.md §14 row 14 — libsodium is vendored portable C; the changes are a vendored library + a build step + a proving binary + two sizing-only kernel constants. Detail: `docs/reference/84-pouch-libsodium.md`. | No kernel-test-suite change — sub-chunk 14 touches kernel constants only (RAMFS_FILE_MAX + SYS_SPAWN_BLOB_MAX); the kernel suite stays **586/586 PASS × default + UBSan**, 0 UBSan runtime errors. The 14 runtime regression is `/pouch-hello-sodium`: `tools/build.sh sysroot` builds libsodium.a (520780 B, 97 objects); `tools/build.sh kernel` links `/pouch-hello-sodium` (276848 B via `pouch-ld` + `-lsodium`); `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **586/586 PASS**, joey content-checks the five passing lines + `exit 0`. UBSan boot also green (0 UBSan errors). |
| `bca132e` | **P6-pouch-devnodes (sub-chunk 11/16): `devfull` Dev + `/pouch-hello-getrandom`.** The third member of the trivial `/dev` Dev trio + the libsodium-grade `getrandom(2)` proving path. New `kernel/full.c` adds `devfull` (dc='f', name="full") to the bestiary — Linux `man 4 full` semantics: reads NUL-fill (like `/dev/zero`), writes always return `-1` ("full disk" contract); registered in `dev_init` between `devzero` and `devrandom` for alphabetical iteration. The v1.0 errno caveat: `sys_write_for_proc` collapses negative dev returns to flat `-1`, so pouch sees `errno=EIO` (not Linux's `ENOSPC`); the proving binary checks `write < 0`, which is POSIX-correct under both errno values. `kernel/test/test_trivial_devs.c` gains three devfull tests + the bestiary-smoke assertion extended to `dev_count() >= 6`. **No pouch boundary-line patch** — `__NR_getrandom = 20` was already mapped by `0001-pouch-syscall-seam.patch`, and musl's `src/linux/getrandom.c` (`syscall_cp(SYS_getrandom, ...)`) is pristine. The proving binary `/pouch-hello-getrandom` (`usr/pouch-hello/pouch-hello-getrandom.c`) — the seventh pouch binary, ~50 KiB ET_EXEC — calls musl's `getrandom(buf, 32, 0)` and asserts (a) 32 bytes returned, (b) at least one byte non-zero, (c) two consecutive 16-byte reads differ. SYS_GETRANDOM is gated on `CAP_CSPRNG_READ`; joey grants the cap via a new `pouch_smoke_one_caps` helper that delegates to `pouch_smoke_core` with `cap_mask = T_CAP_CSPRNG_READ`, using `t_spawn_full` instead of `t_spawn_with_fds`. joey holds `CAP_ALL` post-elevation so the cap-intersection delivers `CAP_CSPRNG_READ` to the child. Wired into `build_pouch_progs` + `build_ramfs` + joey's `do_pouch_hello_smoke`. **Not audit-bearing** — devfull is a leaf Dev with no concurrency surface (parallel to devnull / devzero); SYS_GETRANDOM was audited in P5-corvus-syscalls; the cap-grant path through `t_spawn_full` was audited in P5-corvus-srv-impl-b3a. Path-based `open("/dev/null")` access remains DEFERRED — requires multi-component path resolution. Detail: `docs/reference/78-pouch.md` "The `/dev/full` Dev + the `getrandom` proving path". | 3 new kernel tests (561 → **564**): `full.attach_open_close`, `full.read_fills_zeroes`, `full.write_returns_minus1`; `trivial_devs.bestiary_smoke` extended (devfull at dc='f'/name="full"). Plus a new runtime regression: `/pouch-hello-getrandom` reports four passing lines + `exit 0` on every boot; joey-side `joey: pouch-hello-getrandom smoke ok (musl getrandom(2) over SYS_GETRANDOM with CAP_CSPRNG_READ)`. **564/564 PASS × default + UBSan**, 0 UBSan errors, boot green. |
| `52c0269` | **pouch-poll: hash fixup** | (placeholder fill) |
| `ceb5729` | **P6-pouch-poll (sub-chunk 10/16): retarget musl's `src/select/` lower half onto the kernel `SYS_POLL` primitive.** Five files in musl's source touch this seam — four are rewritten, one is left pristine. `0005-pouch-poll.patch` adds `#define __NR_poll 29` to `arch/aarch64/bits/syscall.h.in` (Linux aarch64 dropped legacy `poll(2)` in favor of `ppoll(2)`; reinstating the entry lets pristine `src/select/poll.c` hit its `#ifdef SYS_poll` branch and route through `SYS_POLL` directly without the ppoll-conversion fallback — `poll.c` itself is UNCHANGED). `src/select/ppoll.c` full rewrite — folds `timespec → int timeout_ms` (rounded UP per POSIX), IGNORES `sigset_t` (signals deferred to sub-chunk 13), drops the `SYS_ppoll_time64` cascade. `src/select/select.c` full rewrite — performs the `fd_set ↔ pollfd[]` translation in userspace: scans `[0, min(n, 64))`, builds a stack-allocated `pollfd[64]`, calls `SYS_POLL`, clears + re-fills the output sets per the events-mask gate (POLLIN → rfds, POLLOUT → wfds, POLLPRI → efds; POLLERR/POLLHUP forward to whichever input sets the fd contributed to per Linux semantics); fds `≥ 64` get `-EBADF` (unreachable through any Thylacine syscall since PROC_HANDLE_MAX = 64); `select(0, NULL, NULL, NULL, {0,0})` short-circuits to 0; `select(0, ..., +tv)` returns `-ENOSYS` at v1.0 (no sleep syscall yet — a future `SYS_NANOSLEEP` would re-enable). `src/select/pselect.c` full rewrite — delegates to `select()` after `timespec → timeval` (ns rounded up to µs); ignores `sigset_t`. `tools/build.sh sysroot`'s seam check extended (the F5 audit-close discipline from 9b) with `SYS_poll 29`, `SYS_ppoll 0xFFFF`, `SYS_pselect6 0xFFFF` — the new mapping pinned + the absent variants pinned as sentinel-blocked. **The proving binary** `/pouch-hello-poll` (`usr/pouch-hello/pouch-hello-poll.c`) — the fifth pouch binary, ~50 KiB ET_EXEC. Creates a pipe pair via inline-asm `SYS_PIPE` (musl's `pipe(2)` calls `SYS_pipe2` which is sentinel-blocked; the kernel's two-register `rd/wr` return doesn't match the Linux `int[2]` ABI musl expects). Four poll-shape probes: poll an empty pipe with 50 ms timeout → 0 (exercises the slow path — tsleep on the poller's private rendez, register the `poll_waiter` on the pipe's hook list, timeout-fire, unregister); write a byte + poll with timeout=0 → 1 / POLLIN (the fast-path readiness sample, no sleep); drain + write again + `select` over an fd_set → 1 / fd in rfds (the fd_set ↔ pollfd translation); `select(0, NULL, NULL, NULL, {0,0})` → 0 (the zero-fds short-circuit arm). Wired into `build_pouch_progs` + `build_ramfs` + joey's `do_pouch_hello_smoke` (content-checks `pouch-hello-poll: exit 0`). **Not audit-bearing** — `SYS_POLL`'s correctness was settled in P5-poll-a + P5-poll-b (`docs/reference/72-poll.md`); sub-chunk 10 is just the userspace seam mating onto that audited primitive. **Closes POUCH-DESIGN.md §6.3 / §14 sub-chunk 10's exit criterion** ("POSIX poll / select work end-to-end against Thylacine pollable objects"). Detail: `docs/reference/78-pouch.md` "The polling backend". | No kernel-test-suite change — sub-chunk 10 touches no kernel code; the kernel suite stays **561/561 PASS × default + UBSan**. The 10 runtime regression is `/pouch-hello-poll`: `tools/build.sh sysroot` rebuilds `libc.a` (2399612 bytes — +840 vs 9b for the new select/pselect bodies) with `0005` applied; `tools/build.sh pouch-progs` builds the ~50 KiB ET_EXEC; `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **561/561 PASS**, joey content-checks four passing lines + `exit 0`. UBSan boot also green (0 UBSan errors). |
| `6082b2e` | **pouch-threads-b: hash fixup** | (placeholder fill) |
| `551be97` | **P6-pouch-threads-b (sub-chunk 9b/16) audit close: 0 P0 + 2 P1 + 3 P2 + 10 P3, all dispositioned.** The pouch-side boundary-line retarget of musl's pthread layer onto Thylacine's thread + torpor + burrow-detach syscalls — sub-chunk 9b finishes sub-chunk 9 (`pouch-threads`). **Audit findings**: F1 [P1] pthread_cond_timedwait > 1 hour deadline silently infinite-loops (kernel rejects > TORPOR_MAX_TIMEOUT_US with EINVAL; musl's `__timedwait_cp` maps non-{EINTR,ETIMEDOUT,ECANCELED} to 0 → infinite spin) — fixed by clamping `timeout_us` at 1 hour in pouch's `__futex4_cp`; F2 [P1] stack guard pages silently disabled (pouch's mmap ignores PROT_NONE and mprotect returns ENOSYS so the whole pthread stack region is RW) — documented as v1.0 limitation (real fix needs a PROT_NONE-capable kernel syscall; deferred to v1.x); F3 [P2] EINVAL-to-0 mapping in `__timedwait_cp` (natural consequence of F1 — closed by F1's clamp); F4 [P2] unchecked SYS_SET_TID_ADDRESS return in `start()` — fixed with `a_crash()` on negative; F5 [P2] build verification missing four new SYS_* aliases — fixed by extending the seam-check list; F7 [P3] stack-alignment comment wrong — fixed inline + in preamble; F8 [P3] -EINVAL collapsed to -EAGAIN — fixed (preserve EINVAL distinction); F14 [P3] pouch-hello-threads stderr drops — fixed (use stdout); F15 [P3] `__wake` cnt sign-extension hazard — fixed (cast to unsigned); F6/F9/F10/F11/F12/F13 documented + deferred (cosmetic / latent / explicit POUCH-DESIGN deferrals). **0 P0 / 2 P1 / 3 P2 / 10 P3.** `0004-pouch-pthread.patch` is the fourth patch in the boundary-line series (`usr/lib/pouch/patches/`); it touches eight musl files: `arch/aarch64/bits/syscall.h.in` adds Thylacine extension numbers 39/40/41/42 (the musl build-time sed pass auto-generates `SYS_torpor_wait`/`SYS_torpor_wake`/`SYS_thread_spawn`/`SYS_thread_exit` aliases); `src/internal/pthread_impl.h::__wake`/`__futexwait` route through `SYS_torpor_wake`/`SYS_torpor_wait`; `src/thread/__timedwait.c::__futex4_cp` translates musl's `struct timespec *` relative deadline into torpor's relative microsecond timeout, clamped at TORPOR_MAX_TIMEOUT_US (1 hour) per F1; `src/thread/__wait.c` + `src/thread/pthread_barrier_wait.c` retarget single FUTEX_WAIT sites; `src/thread/pthread_cond_timedwait.c::unlock_requeue` replaces FUTEX_REQUEUE with `__wake` (loses the requeue optimization, functionally correct); `src/thread/pthread_create.c` three sites — `__pthread_exit`'s tail `SYS_exit(0)` → `SYS_thread_exit`, `start()` registers `&__thread_list_lock` as `clear_child_tid` at thread entry with a_crash defense per F4, `__pthread_create`'s `__clone(...)` → `__syscall(SYS_thread_spawn, entry, sp_top, args, TP_ADJ(new))` with 16-byte-down sp alignment per F7 and -EINVAL preservation per F8; `src/thread/aarch64/__unmapself.s` swaps SYS_munmap (215) → SYS_BURROW_DETACH (38) + SYS_exit (93) → SYS_THREAD_EXIT (42) for the detached-stack teardown asm. The `arch/aarch64/clone.s` file is left untouched (now unreferenced; static-link DCE drops it). **The proving binary** `/pouch-hello-threads` (`usr/pouch-hello/pouch-hello-threads.c`) — 5 worker threads × 1000 mutex-protected counter increments + main joins; verifies counter == 5000. Exercises every retargeted site in one boot: `SYS_THREAD_SPAWN` (5 spawns), `SYS_TORPOR_WAIT`/`SYS_TORPOR_WAKE` (the mutex-contention path), `SYS_THREAD_EXIT` (5 exits + the clear_child_tid handoff). joey content-checks `pouch-hello-threads: exit 0` in `do_pouch_hello_smoke`. **Closes POUCH-DESIGN.md §13's "Multithreaded test passes" exit criterion.** PI mutexes / cancellation / fork / robust lists / PROCESS_SHARED cross-Proc semantics deferred (POUCH-DESIGN.md §7). Closed list: `memory/audit_p6_pouch_threads_9b_closed_list.md`. Detail: `docs/reference/82-pouch-pthread.md`. | No kernel-test-suite change — sub-chunk 9b touches no kernel code; the kernel suite stays **561/561 PASS × default + UBSan**. The 9b runtime regression is `/pouch-hello-threads`: `tools/build.sh sysroot` rebuilds `libc.a` (2398772 bytes post-audit-fixes) with `0004` applied; `tools/build.sh pouch-progs` builds the ~60 KiB ET_EXEC; `BOOT_TIMEOUT=420 tools/test.sh` → `Thylacine boot OK`, **561/561 PASS**, joey content-checks `counter = 5000 (expected 5000)` + `exit 0`. UBSan boot also green (0 UBSan errors). |
| *(pending)* | **pouch-threads-a: hash fixup** | (placeholder fill) |
| `e907945` | **P6-pouch-threads-a (sub-chunk 9a/16) audit close: 0 P0 + 3 P1 + 3 P2 + 7 P3.** The kernel-side pthread substrate — `SYS_THREAD_SPAWN` + `SYS_THREAD_EXIT` + multi-thread Proc reap. Three load-bearing additions: (1) **`SYS_THREAD_SPAWN`** (= 41) — creates an EL0 peer Thread in the calling Proc via new `thread_create_user` + new `thread_user_trampoline` (arch/arm64/context.S); the trampoline does `userland_enter`-inline so `x0 = user_arg` survives the GPR-zero sweep + sets `TPIDR_EL0 = user_tls`. (2) **`SYS_THREAD_EXIT`** (= 42) + `thread_exit_self` (kernel/proc.c) — atomically clears the registered `clear_child_tid` word (new `uaccess_store_u32` primitive) + torpor-wakes `UINT32_MAX` joiners on it (the pthread_join handshake); marks self EXITING; if last live Thread in Proc, ALSO transitions Proc → ZOMBIE with `exit_status = 0`. (3) **Multi-thread Proc reap** — `exits()` extended to require all peer Threads be already EXITING (extincts otherwise — cross-thread shootdown is v1.x); `wait_pid` extended to drain ALL Threads in `p->threads`. `SYS_SET_TID_ADDRESS` extended to STORE the tidptr + return the real per-Thread tid (was p->pid pre-9a). `struct Thread` gains `clear_child_tid` (u64); sizeof stays at 816 (consumes the existing trailing pad). New `uaccess_store_u32` + new `proc_table_lock_acquire`/`_release` cross-module helpers (F1). **Audit findings closed**: F1 [P1] thread_link/unlink under g_proc_table_lock — closes the list-mutation race observable as miscounted live peers under SMP; F2 [P1] alignment check on entry_va — closes the EC_PC_ALIGN ELE path; F3 [P1] EXITING transition committed BEFORE clear_child_tid handoff — closes the race where joiner returns from pthread_join → exits() and observes worker still RUNNING; F4 [P2] atomic u32 g_next_tid + alloc_next_tid helper (was non-atomic int) — closes the duplicate-tid race under concurrent spawn; F5/F6 [P2] documentation accuracy on peer-state lock-coverage + non-RUNNING defense-in-depth; F7 [P3] defensive alignment recheck on tidptr in the handoff; F8 [P3] strict `>=` on sp_va bound; F9 [P3] held with doc-only; F10 [P3] clear_child_tid concurrency-contract comment; F11 [P3] ABI change note for SYS_SET_TID_ADDRESS return. **Tests**: 4 new kernel tests (557 → 561): `thread.create_user_ctx_layout`, `thread.exit_self_marks_exiting`, `thread.exit_self_last_thread_zombies`, `proc.multi_thread_reap`. 1 new joey-side smoke including the F2 regression case (`misaligned entry` → -EINVAL). 1 new /thread-probe E2E runtime regression. **561/561 PASS × default + UBSan**, 0 UBSan runtime errors, full boot OK. Closed-list: `memory/audit_p6_pouch_threads_9a_closed_list.md`. Detail: `docs/reference/81-sys-thread.md`. | 4 new kernel tests + 1 joey-smoke (with F2 regression) + 1 /thread-probe E2E. **561/561 PASS × default + UBSan**, 0 UBSan runtime errors. |
| *(pending)* | **pouch-wait-addr: hash fixup** | (placeholder fill) |
| `e245547` | **P6-pouch-wait-addr (sub-chunk 8/16): the kernel-side `torpor` primitive — `SYS_TORPOR_WAIT` + `SYS_TORPOR_WAKE`.** Thylacine's futex-equivalent, the substrate over which sub-chunk 9 (`pouch-threads`) will run pouch's pthread mutex/condvar layer. `SYS_TORPOR_WAIT` (= 39) compare-and-sleeps on a 4-byte user-VA word: take `torpor_lock`, `uaccess_load_u32` the word, if `!= expected` return 0 immediately, else register a stack-local waiter in a `(Proc *, addr_va)`-keyed hash bucket and `tsleep` on the waiter's private `Rendez`; on resume, re-take `torpor_lock`, unlink, return. `SYS_TORPOR_WAKE` (= 40) walks the matching bucket and wakes up to `count` waiters. New `kernel/torpor.c` (~250 LOC) + `kernel/include/thylacine/torpor.h`; new `uaccess_load_u32` primitive (`arch/arm64/uaccess.S` + `.h`) — the first u32 user-VA load + fixup-table entry. New `T_TORPOR_*` constants + `t_torpor_wait` / `t_torpor_wake` libt wrappers (`usr/lib/libt/include/thyla/syscall.h`). **Spec-to-code suspended project-wide (broadened 2026-05-23 — user direction): no `specs/futex.tla` is written; the I-9-specialized no-lost-wakeup invariant is validated by prose reasoning in `torpor.h` + `torpor.c` (the consumer's load runs under `torpor_lock`, register-then-sleep before lock release; the producer's WAKE walks under the same lock; lock acquire/release pairing handles all interleavings — proof sketch in the file header). The audit round + the runtime tests are the rigor floor.** **Audit-bearing** — focused opus-prosecutor round, PENDING-TO-CLOSE in the substantive commit. **Tests**: 7 new kernel tests (549 → 556): `torpor.wait_rejects_bad_args`, `torpor.wait_rejects_unmapped_va`, `torpor.wake_rejects_bad_args`, `torpor.wake_empty_bucket_returns_zero`, `torpor.wait_value_mismatch_fast_path`, `torpor.wait_timeout_zero_returns_etimedout`, `torpor.wait_wake_handoff` (end-to-end consumer-thread register-sleep-wake-resume chain on a fresh `proc_alloc`'d Proc — same threading pattern as `test_tsleep`). Plus a joey-side `torpor SVC-dispatch ok` smoke that exercises the SVC registration. **556/556 PASS × default + UBSan**, 0 UBSan runtime errors, full boot OK. Detail: `docs/reference/80-torpor.md`. | 7 new kernel tests + 1 joey-side runtime regression. **556/556 PASS × default + UBSan**, 0 UBSan runtime errors. |
| `1077632` | **pouch-mem-b: hash fixup** | (placeholder fill) |
| `470a81e` | **P6-pouch-mem-b (sub-chunk 7b): the pouch side — `0003-pouch-mman` musl patch + `/pouch-hello-malloc`.** Closes sub-chunk 7 (`pouch-mem`). `0003-pouch-mman.patch` retargets musl's `mman/` lower half onto the kernel anonymous-memory syscalls from 7a: `__NR_mmap = 37` (= `SYS_BURROW_ATTACH`) and `__NR_munmap = 38` (= `SYS_BURROW_DETACH`); `src/mman/mmap.c` rewritten as a one-arg call (length only — `start`/`prot`/`fd`/`off` accepted and ignored; the kernel chooses the VA, always RW); `src/mman/munmap.c` rewritten as a direct call to `SYS_BURROW_DETACH`. File-backed `mmap` (any flags without `MAP_ANON`) is refused with `ENOSYS` by design — a permanent Plan 9-heritage refusal (a mapped file cannot be 9P-network-transparent; ARCHITECTURE.md §6.5). `MAP_FIXED` rejected too. All other `mman/` calls (`madvise`/`mprotect`/`mremap`/`mlock`/`munlock`/`mlockall`/`munlockall`/`msync`/`mincore`/`posix_madvise`) stay on the `0xFFFF` sentinel → `ENOSYS`; mallocng tolerates each by design (`USE_MADV_FREE=0` skips `madvise`; `mremap` ENOSYS falls through to `malloc+memcpy+free` in `realloc.c`; `mprotect` ENOSYS is `errno!=ENOSYS`-guarded in `malloc.c`; `brk` permanent-fails on first call, mallocng routes via mmap). New `/pouch-hello-malloc` (`usr/pouch-hello/pouch-hello-malloc.c`) — the fourth pouch binary, exercising `malloc`/`free`/`calloc`/`realloc` end-to-end including the `>= MMAP_THRESHOLD` (131052 B) individually-mmapped path AND the large-realloc-grow path (`mremap`-ENOSYS → fallback). Every region is filled with a seeded byte sequence and read back: byte-level round-trip on small slot, calloc zero-init, in-slot realloc-grow, large mmap-path malloc, and large realloc-grow. Wired into `build_pouch_progs` + `build_ramfs` + joey's `pouch_smoke_one` (content-checks `pouch-hello-malloc: exit 0`). Not separately audit-bearing — `0003-pouch-mman` is a boundary-line patch like `0001`/`0002`, no kernel code; the kernel mm surface was audited in 7a. Detail: `docs/reference/78-pouch.md` "The anonymous-memory backend". | No kernel-test-suite change — sub-chunk 7b touches no kernel code; the kernel suite stays **549/549 PASS × default + UBSan**. The 7b runtime regression is `/pouch-hello-malloc`: `tools/build.sh sysroot` rebuilds `libc.a` with `0003` applied; `tools/build.sh kernel` links `/pouch-hello-malloc` (60080 B); `BOOT_TIMEOUT=180 tools/test.sh` → `Thylacine boot OK`, **549/549 PASS**, joey content-checks the heap-exercise output (`small malloc/free ok` / `calloc zeroing ok` / `realloc-grow (small slot) ok` / `large malloc/free ok` / `realloc-grow large ok` / `exit 0`); UBSan run separately, 0 errors. |
| *(pending)* | **pouch-mem-a: hash fixup** | (placeholder fill) |
| `198fda1` | **P6-pouch-mem-a (sub-chunk 7a): the kernel anonymous-memory interface — `SYS_BURROW_ATTACH` / `SYS_BURROW_DETACH`.** The v1.0 native memory-growth primitive (ARCHITECTURE.md §6.5 Tier 1) — the substrate for `libt`'s and pouch's `malloc`. `SYS_BURROW_ATTACH` (= 37) attaches an anonymous RW Burrow at a kernel-chosen VA — a first-fit `vma_find_gap` scan of the burrow-attach window `[EXEC_USER_BURROW_BASE, EXEC_USER_BURROW_TOP)` (4 GiB–64 TiB, well above the user stack); `SYS_BURROW_DETACH` (= 38) tears one down, window-confined. Thin `current_thread()` SVC handlers over testable `_for_proc` inners; wraps the audited `burrow_create_anon` → `burrow_map(RW)` → `burrow_unref` discipline (the VMA's `mapping_count` keeps the Burrow alive, `handle_count` 0 — Tier 1, no handle). New `vma_find_gap` (`kernel/vma.c`) + per-Proc `vma_lock` (`struct Proc` 144 → 152 bytes). `brk` / file-backed `mmap` declined by design (ARCHITECTURE.md §6.5). **Audit-bearing** (mm) — focused opus-prosecutor round, **0 P0 / 0 P1 / 1 P2 / 3 P3**: F1 [P2] (detach matched any VMA by geometry — a caller could pass its own ELF/stack/guard VMA coordinates) fixed by the window-bounds check + the `detach_window_confined` regression test; F3 [P3] `_Static_assert`s pin the window-above-stack invariant; F2 [P3] (lock held across the eager alloc) documented + deferred to `pouch-threads`; F4 [P3] test gaps closed. 7a of the `pouch-mem` sub-chunk — 7b is the `0003-pouch-mman` musl patch + the heap test program. Detail: `docs/reference/79-sys-burrow.md`. | 11 new kernel tests (538 → 549): `vma.find_gap_{smoke,no_fit,constraints,straddle}` + `sys_burrow.{attach_returns_window_va,attach_detach_round_trip,attach_distinct,attach_rounds_up,attach_rejects_bad_length,detach_rejects,detach_window_confined}`. New `kernel/test/test_sys_burrow.c`. **549/549 PASS × default + UBSan**, 0 UBSan runtime errors, full boot OK. |
| *(pending)* | **pouch-compiler-rt-c: hash fixup** | (placeholder fill) |
| `aea1ab2` | **P6-pouch-compiler-rt-c (sub-chunk 6c): the real `printf` hello — `/pouch-hello-printf`.** The last part of sub-chunk 6, and the first program to exercise the compiler runtime end to end. `usr/pouch-hello/pouch-hello-printf.c` — the third pouch binary: where `/pouch-hello` uses the raw `write(2)` seam and `/pouch-hello-stdio` buffered stdio, this one uses `printf(3)`, which pulls musl's `vfprintf` — the first thing in the project to reference a compiler-rt builtin. It is a self-test: `snprintf("%d", 1936)` proves the *link* (any `printf` pulls `vfprintf.o`, which references the `binary128` soft-float builtins unconditionally — the binary cannot link without `libclang_rt.builtins.a`); `snprintf("%.2f", 3.140625)` must yield exactly `"3.14"` and proves the runtime *runs* — `vfprintf`'s `fmt_fp` extracts decimal digits in `binary128` `long double`, so a broken `__subtf3` / `__multf3` / `__fixtfdi` would format the wrong digits. `main` returns non-zero on any mismatch. Wired into `build_pouch_progs` + `build_ramfs` + joey's `do_pouch_hello_smoke` / `pouch_smoke_one`. Closes sub-chunk 6 (`pouch-compiler-rt`) and the POUCH-DESIGN §13 `printf`-hello exit criterion. Not audit-bearing (a milestone; no kernel code, no invariant surface). | No kernel-test-suite change — sub-chunk 6c touches no kernel code; the kernel suite stays **538/538 PASS**. Verification: `tools/build.sh kernel` links `/pouch-hello-printf` (49528 B — the first pouch binary to pull objects from `libclang_rt.builtins.a`); `BOOT_TIMEOUT=180 tools/test.sh` → `Thylacine boot OK`, **538/538 PASS**, joey content-checks the printf output (`int ok` / `float ok — snprintf gave "3.14" (binary128 soft-float)` / `exit 0`). |
| *(pending)* | **pouch-compiler-rt-b: hash fixup** | (placeholder fill) |
| `72f45f9` | **P6-pouch-compiler-rt-b (sub-chunk 6b): vendor + build the compiler runtime — `libclang_rt.builtins.a`.** The fourth and final part of the pouch cross-toolchain: a complete C toolchain is compiler + libc + CRT + *compiler runtime*, and the sysroot lacked the runtime. LLVM 22.1.4 compiler-rt builtins vendored pristine at `third_party/compiler-rt/builtins/` (443 files, byte-identical; **no patch series** — compiler-rt is pure computation, nothing to retarget), extracted from the monorepo `llvm-project-22.1.4.src.tar.xz` (LLVM 22 dropped the per-component source tarballs). New `build_compiler_rt` (`tools/build.sh`, a step of `build_sysroot`) compiles 157 TUs for `aarch64-thylacine` — the `GENERIC_SOURCES` + `GENERIC_TF_SOURCES` + `BF16_SOURCES` blocks derived (via `awk`, re-vendor-safe) from the vendored `builtins/CMakeLists.txt` + the conditional generic additions + `cpu_model/aarch64.c` + `aarch64/fp_mode.c`; deliberately excludes generic `fp_mode.c` (superseded by the aarch64 override), the `lse.S` outline atomics (`-march=…+lse` inlines LSE atomics — the helpers are never called), and `emupac.cpp` + the SME files (off pouch's `-march` baseline). It archives `libclang_rt.builtins.a` (~227 KiB, 157 ELF `aarch64` objects) into `build/sysroot/lib/` and verifies, with `llvm-nm`, that the `binary128` soft-float builtins `printf`'s `vfprintf` path needs are defined (`__addtf3` / `__multf3` / `__eqtf2` / `__extenddftf2` / …). `build_pouch_progs` now treats a sysroot without the runtime as incomplete; `pouch-ld` (6a) auto-picks-up the archive. The `aarch64-thylacine` cross-toolchain is now **complete** — compiler + libc + CRT + runtime. Not audit-bearing (build tooling + a vendored pure-computation library; no kernel code, no invariant surface). | No kernel-test-suite change — sub-chunk 6b touches no kernel code; the kernel suite stays **538/538 PASS**. Verification: `tools/build.sh sysroot` builds + symbol-verifies `libclang_rt.builtins.a` (157 objects); `tools/build.sh kernel` relinks `/pouch-hello` + `/pouch-hello-stdio` byte-identically through `pouch-ld` with the runtime grouped in; `BOOT_TIMEOUT=180 tools/test.sh` → `Thylacine boot OK`, **538/538 PASS**, both pouch binaries content-checked. |
| *(pending)* | **pouch-compiler-rt-a: hash fixup** | (placeholder fill) |
| `eeaa5ab` | **P6-pouch-compiler-rt-a (sub-chunk 6a): the `pouch-ld` link-driver wrapper.** New `tools/pouch-ld` — invokes `ld.lld` directly with the pouch ELF link line (static non-PIE `ET_EXEC`, `-z separate-loadable-segments` for page-aligned `PT_LOAD` offsets) and supplies the musl CRT objects + `libc.a` (+ the compiler runtime `libclang_rt.builtins.a` once `pouch-compiler-rt-b` builds it — picked up automatically, grouped with `libc` for mutual-reference resolution). clang cannot be the ELF *link driver* for `aarch64-thylacine` on a macOS host — its driver mis-selects the Darwin toolchain and emits Mach-O linker arguments; the compiler path (`pouch-clang -c`) is unaffected. `build_pouch_progs` (`tools/build.sh`) now links the pouch test programs through `pouch-ld` instead of an inline `ld.lld` call. The CMake `CMAKE_C_LINK_EXECUTABLE` wiring is owed to the first CMake-built pouch program (the `pouch-libsodium` sub-chunk), where it can be exercised end-to-end. Not audit-bearing (build tooling; no kernel code, no invariant surface). | No kernel-test-suite change — sub-chunk 6a touches no kernel code; the kernel suite stays **538/538 PASS**. Verification: `tools/build.sh kernel` → `build_pouch_progs` links `/pouch-hello` + `/pouch-hello-stdio` via `pouch-ld` (byte-identical to the prior inline `ld.lld` link); `BOOT_TIMEOUT=180 tools/test.sh` boots to `Thylacine boot OK` with both binaries running + content-checked. |
| *(pending)* | **pouch-hello-smoke: hash fixup** | (placeholder fill) |
| `5c0623d` | **P6-pouch-hello-smoke (sub-chunk 5/15): the first POSIX C programs run in Thylacine.** Two binaries cross-compiled against the pouch sysroot, spawned by joey on the boot path. `/pouch-hello` (`usr/pouch-hello/pouch-hello.c`) — the raw `write(2)` hello: the first runtime exercise of the auxv startup frame, musl's static CRT (`_start`→`__libc_start_main`→`__init_libc`→`main`), `SYS_SET_TID_ADDRESS`, the `write` seam (1:1 → `SYS_WRITE`), the `exit` seam (folds onto `SYS_EXITS`); plus the durable runtime regression for the `0xFFFF` unimplemented-syscall sentinel — `chdir()` (non-cancellable path) and `open()` (cancellable path, the P6-pouch-syscall-seam audit's F1-fixed `__syscall_cp` guard) each assert `-1` + `errno==ENOSYS`. `/pouch-hello-stdio` (`pouch-hello-stdio.c`) — the buffered-stdio hello: `puts()`/`fwrite()` → the patched `__stdio_write` backend drained at the exit-flush. New `tools/build.sh pouch-progs` target (`build_pouch_progs`): compiles with `tools/pouch-clang -c`, links **directly** with `ld.lld` — clang as the link driver falls into the macOS Darwin toolchain for the unknown `aarch64-thylacine` OS and emits Mach-O link args. The link yields a static non-PIE `ET_EXEC` with page-aligned `PT_LOAD` file offsets (`-z separate-loadable-segments`) — the layout `kernel/elf.c` + `exec_map_segment` accept with **no kernel change**. joey's `do_pouch_hello_smoke`/`pouch_smoke_one` wires each child's fd 1 to a pipe via `t_spawn_with_fds`, reaps the child (a Thylacine zombie holds its handle table until `proc_free`, so reap-before-drain — a drain-until-EOF before the reap deadlocks), then relays + content-checks the output. Not audit-bearing (a milestone; no kernel code, no new invariant surface). **Deferred**: the literal `printf` format engine — `vfprintf` needs `binary128` soft-float compiler-rt builtins the sysroot lacks; owed to a new `pouch-compiler-rt` sub-chunk. | No kernel-test-suite change — sub-chunk 5 touches no kernel code; the kernel suite stays **538/538 PASS** (× default; UBSan not re-run — no kernel delta). Verification is the boot smoke: `tools/build.sh kernel` builds `/pouch-hello` (17808 B) + `/pouch-hello-stdio` (24448 B) into the ramfs (24 cpio entries); `BOOT_TIMEOUT=180 tools/test.sh` boots to `Thylacine boot OK` with joey relaying both binaries' output and content-checking each exits 0. |
| *(pending)* | **pouch-syscall-seam: hash fixup** | (placeholder fill) |
| `dbc6bd3` | **P6-pouch-syscall-seam (sub-chunk 4/15): retarget musl's aarch64 syscall seam to the Thylacine ABI; `tools/build.sh sysroot` builds the real pouch libc.** The first sub-chunk to add boundary-line patches. `0001-pouch-syscall-seam.patch` (four files): `arch/aarch64/bits/syscall.h.in` retargeted — eight POSIX calls that map 1:1 onto a native Thylacine syscall (`read`/`write`/`close`/`exit`/`exit_group`/`mlockall`/`getrandom`/`set_tid_address`) get the Thylacine number, the other 299 macros get `0xFFFF` (the unimplemented-syscall sentinel); `arch/aarch64/syscall_arch.h` + `src/thread/__syscall_cp.c` guard both the non-cancellable (`__syscall0..6`) and cancellable (`__syscall_cp`) syscall paths — a sentinel call short-circuits to `-ENOSYS` without issuing `svc`, so no foreign syscall number reaches the kernel (invariant P-1) and an un-retargeted call is a clean `ENOSYS` (P-3); `src/internal/syscall_ret.c` decodes Thylacine's flat-`-1` error convention (→ generic `EIO`; an explicit `-errno` in `[-4095,-2]` passes through) and `_Static_assert`s the sentinel value across the seam files. `0002-pouch-stdio-no-iovec.patch` (two files): musl's `writev`/`readv`-based `__stdio_write`/`__stdio_read` → `SYS_write`/`SYS_read` loops. `tools/build.sh sysroot` now copies `third_party/musl/` → `build/pouch/musl-src/`, applies the series, configures + builds out-of-tree for `aarch64-thylacine`, installs headers + `libc.a` + CRT objects into `build/sysroot/`, and verifies the retarget landed (all eight numbers + the sentinel + the cancellable-path guard). **Audit-bearing** (syscall surface): a focused opus-prosecutor round returned 1 P0 / 1 P1 / 2 P2 / 5 P3, all dispositioned — F1 [P0] the cancellable path (`__syscall_cp_asm`) bypassed the guard, fixed by guarding the `__syscall_cp` C chokepoint; F2 [P1→P3] tty-probe degradation (correct for v1.0, documented); F3 [P2→P3] the generic `EIO` (design-sanctioned per §5.1, documented); F4 [P2] `__stdio_write` 0-return coupling (documented); F6/F7/F9 hardening (`.rej` check, `_Static_assert` witness, full-retarget grep); F5/F8 withdrawn; F10/F11 closed no-change. Closed-list: `memory/audit_p6_pouch_syscall_seam_closed_list.md`. | No kernel-test-suite change — sub-chunk 4 touches no kernel code; the kernel suite stays **538/538 PASS** (× default; UBSan not re-run — no kernel delta). Verification is `tools/build.sh sysroot`: the patched musl builds **0 errors / 0 warnings** → a 2.4 MB `libc.a` (1347 aarch64 objects) + CRT + 218 headers; the build asserts all eight retarget numbers + the cancellable-path guard. |
| *(pending)* | **pouch-kernel-auxv: hash fixup** | (placeholder fill) |
| `d505e73` | **P6-pouch-kernel-auxv (sub-chunk 3/15): `exec_setup` builds the System V process-startup frame (auxv); new `SYS_SET_TID_ADDRESS` syscall.** `exec_setup` (`kernel/exec.c`) now calls `exec_build_init_stack` to write a System V startup frame — argc=0, empty argv/envp, a six-entry auxv (`AT_PHDR`/`AT_PHENT`/`AT_PHNUM`/`AT_PAGESZ`/`AT_RANDOM`/`AT_NULL`) + 16 kernel-CSPRNG bytes for `AT_RANDOM` — into the top 144 bytes of the user stack; `*sp_out` now points at the frame's `argc` word. `elf_load` exposes `phoff`/`phnum`/`phentsize` on `struct elf_image` so `AT_PHDR` resolves to the program-header table's user VA (or 0 + a coherent zeroed triple when no segment covers it). New `SYS_SET_TID_ADDRESS = 36` returns the calling thread's tid (the Proc pid at v1.0 — the Linux thread-group-leader convention; `tidptr` accepted but deferred per POUCH-DESIGN.md §12.4). `<thylacine/elf.h>` gains the `AT_*` constants + `struct Elf64_auxv_t`; `<thylacine/exec.h>` gains `EXEC_INIT_STACK_SIZE`/`EXEC_INIT_AUXV_COUNT`/`EXEC_INIT_RANDOM_OFFSET`; libt gains `t_set_tid_address`. Freestanding binaries (joey, corvus, the probes) ignore the frame — their `_start` calls `main` directly — and boot unchanged. **Audit-bearing** (exec + ELF loader + syscall surface): a focused opus-prosecutor round returned 0 P0 / 0 P1 / 1 P2 / 4 P3, all five addressed (F1 — zero the whole `AT_PHDR`/`AT_PHENT`/`AT_PHNUM` triple when no segment covers the phdrs; F2 — refresh stale `_start` contract comments; F3 — document degraded-entropy behaviour; F4 — invariant `extinction` assert on the stack VMA lookup; F5 — strengthen the auxv-count `_Static_assert`). Closed-list: `memory/audit_p6_pouch_kernel_auxv_closed_list.md`. | 3 new kernel tests (535 → 538): `exec.setup_auxv` (full startup-frame inspection with a resolved `AT_PHDR`), `exec.setup_auxv_no_phdr_segment` (the `AT_PHDR`=0 fallback), `syscall.dispatch_set_tid_address`. **538/538 PASS × default + UBSan**, 0 UBSan runtime errors, full boot OK. |
| `45e287e` | **pouch-musl-vendor: hash fixup** | (placeholder fill) |
| `6f60b7e` | **P6-pouch-musl-vendor (sub-chunk 2/15): vendor musl 1.2.5; scaffold the boundary-line patch series; the R2 upper-half cross-compile probe.** musl 1.2.5 (sha256 `a9a118bb…c75e4`, MIT) vendored pristine + byte-identical at `third_party/musl/` (2697 files); new `third_party/README.md` records the vendoring policy + the per-package record. New `usr/lib/pouch/` is pouch's own tree: `patches/series` (quilt-style, empty), `patches/README.md` (the boundary-line discipline — invariant P-4), `README.md`. New `docs/reference/78-pouch.md` — the pouch subsystem reference: the boundary-line architecture + the full UPPER / LOWER / SEAM inventory of every musl `src/` subdir + `crt/` + `arch/aarch64/`. **R2 probe** (POUCH-DESIGN.md risk R2 — does musl take a clean seam): musl's `configure` accepts the `aarch64-thylacine` triple; out-of-tree `make lib/libc.a` compiles 1345 TUs, 0 errors / 0 warnings → a 2.4 MB `libc.a` of aarch64 ELF objects. The boundary line is compile-clean — the patch series' burden is semantic (replace what the lower half *does*), not a compile fight. **Vendor path resolved** (POUCH-DESIGN.md §4 left it open): `third_party/musl/`, not `usr/lib/pouch/musl/` — `third_party/` is the conventional pristine-upstream home; `usr/lib/pouch/` stays pouch's own code. `loc.sh` now excludes `third_party/`. Not audit-bearing — vendoring + docs + build scaffolding, no kernel code, no invariant surface. | No kernel-test-suite change (vendoring + scaffolding; no kernel code, test count stays 535/535). Verification: out-of-tree musl `make lib/libc.a` exit 0 — 1345 TUs, 0 errors / 0 warnings, 2.4 MB `libc.a`; `tools/build.sh sysroot` still exit 0 + toolchain self-check PASS. |
| `e03be8d` | **pouch-toolchain: hash fixup** | (placeholder fill) |
| `90b5333` | **P6-pouch-toolchain (sub-chunk 1/15): the `aarch64-thylacine` cross-compilation toolchain scaffolding.** New `cmake/Toolchain-aarch64-pouch.cmake` — clang + ld.lld (Homebrew LLVM); target `aarch64-thylacine` via `CMAKE_C_COMPILER_TARGET`; `CMAKE_SYSROOT` → `build/sysroot`; `-march=armv8-a+lse+pauth+bti` + hardening (stack protector, stack-clash, PAC+BTI) + `-nostdinc -isystem <sysroot>/include` via `CMAKE_C_FLAGS_INIT`; W^X + `-static` link flags via `CMAKE_EXE_LINKER_FLAGS_INIT`; compiler probes skipped (no libc to link against yet). New `tools/pouch-clang` — a drop-in `clang` wrapper pinned to `--target=aarch64-thylacine --sysroot=build/sysroot -march=...`, for plain-Makefile / autotools projects (musl, libsodium, stratumd); pins only what *defines* the target, leaving codegen policy to the consumer. `tools/build.sh sysroot` made real — creates the `build/sysroot/{include,lib}` skeleton + runs a toolchain self-check (`pouch-clang` must produce an aarch64 object). **Deviation from POUCH-DESIGN.md §9** (corrected in the same commit): the CMake toolchain file is named `Toolchain-aarch64-pouch.cmake`, not `-thylacine` — `Toolchain-aarch64-thylacine.cmake` is already the kernel toolchain; `-pouch` is role-named, parallel to the existing `-userspace`. TOOLING.md cross-compile section corrected (the stale `aarch64-unknown-thylacine` triple → `aarch64-thylacine`). Not audit-bearing (build scaffolding; no kernel code, no invariant surface). The sysroot is an empty skeleton — populated by sub-chunks 2-5. | `build.sh sysroot` exit 0 + toolchain self-check PASS (pouch-clang → aarch64 ELF object); the CMake toolchain configures a trivial project cleanly (Clang 22.1.4 resolved). No kernel-test-suite change — build scaffolding. |

## Remaining work — the 16 sub-chunks

Per `POUCH-DESIGN.md §14`. Each lands independently with the two-commit pattern; audit-bearing ones get a focused round.

| # | Chunk | Audit-bearing |
|---|---|---|
| 1 | `pouch-toolchain` — triple, sysroot layout, clang toolchain + `pouch-clang` wrapper | no |
| 2 | `pouch-musl-vendor` — vendor pinned musl; the boundary-line patch series scaffold | no |
| 3 | `pouch-kernel-auxv` — `exec_setup` auxv population; `set_tid_address` | yes |
| 4 | `pouch-syscall-seam` — retarget `bits/syscall.h`; 1:1 wrappers; rc→errno mapping | yes |
| 5 | `pouch-hello-smoke` — static + buffered-stdio hello build + run | no |
| 6 | `pouch-compiler-rt` — vendor + build the compiler-rt builtins; the `pouch-ld` link wrapper; the real `printf` hello | no |
| 7 | `pouch-mem` — allocator backend (anonymous-memory call) — **LANDED** (7a kernel + 7b pouch side) | yes (7a) |
| 8 | `pouch-wait-addr` — the `torpor` kernel primitive — **LANDED** (no `specs/futex.tla` per the 2026-05-23 spec-to-code suspension broadening; prose validation + audit + runtime tests) | yes |
| 9 | `pouch-threads` — pthread create/join/detach + mutex/cond/rwlock/once + TLS errno — **LANDED** (9a kernel side + 9b pouch side + /pouch-hello-threads proving binary) | yes (9a + 9b) |
| 10 | `pouch-poll` — `poll`/`select`/`ppoll` → `SYS_POLL` — **LANDED** (`0005-pouch-poll.patch` + `/pouch-hello-poll` proving binary) | no |
| 11 | `pouch-devnodes` — minimal synthetic-FS namespace; trivial `/dev` nodes — **LANDED** (`devfull` Dev + `/pouch-hello-getrandom`; path-based `open("/dev/null")` deferred to a future multi-component-walk sub-chunk) | no |
| 12 | `pouch-sockets` — `AF_UNIX` `SOCK_STREAM` → `/srv` (BYTE mode); new kernel `enum srv_mode` + `SYS_POST_SERVICE_BYTE` (= 43) + `SrvConn.byte_mode` + KOBJ_SRV mode-dispatch. POUCH-DESIGN §6.2 revised in this chunk. **LANDED 2026-05-23** — `df74fe1` substantive, `*(pending)*` hash fixup. | yes |
| 13 | `pouch-signals` — the supported signal subset → notes. Design landed (`237f096`, 2026-05-24, scripture-only) introducing the **fd-first notes substrate** as a NOVEL.md §3.1 totalization. **13a-impl TRULY CLOSED** (`7fdaf5a` impl + `c2c8af3`/`a1dedd9` R1 + `8c18cf6`/`950e17b` R2 + `d6e1c3f`/`7274261` R3 + `411fa7e`/`c8bdae3` R4 — CLEAN). 4 rounds, cumulative: 10 P0 + 7 P1 + 12 P2 + 13 P3 = 42 findings dispositioned. **13b-pouch TRULY CLOSED** (`b528bb0` R1 substantive + `6742f70` R1 hash fixup + `bb57d11` R2 substantive + `*(pending)*` R2 hash fixup) — the pouch-side boundary-line patch `0007-pouch-signals.patch` retargets musl's `src/signal/` onto the kernel notes API (SYS_NOTE_OPEN/NOTIFY/NOTED/POSTNOTE/NOTE_MASK = 44-48); paired with a one-line kernel addition (`sys_postnote_handler` accepts `pid_raw == 0` as the self-post sentinel for pouch's getpid-less `raise()`). 7 musl files modified + 2 new pouch-private files (`_pouch_signal.h` + `_pouch_signal.c`), one kernel function extended (+ docblock), one new proving binary (`/pouch-hello-signals` — sigaction + raise + SIG_IGN + EINVAL paths). **R1: 0 P0 + 2 P1 + 6 P2 + 5 P3 = 13** (4 fixed by code; dirty by count → R2). **R2: 0 P0 + 0 P1 + 2 P2 + 1 P3 — CLEAN** (1 fixed: stale "abort falls through to _Exit" comment in 2 source files; 1 documented commit-msg off-by-one; 1 standard two-commit-pattern self-reference). Cumulative 2 rounds: 0 P0 + 2 P1 + 8 P2 + 6 P3 = 16 findings dispositioned. 586/586 × default + UBSan unchanged; libc.a 2399612 → 2412088 bytes (+12476 B for the signal layer); pouch-hello-signals 51280 B. Detail: `docs/reference/83-pouch-signals.md`. **Sub-chunk 13b COMPLETE.** | yes (13a + 13b) |
| 14 | `pouch-libsodium` — cross-compile libsodium; self-test — **LANDED 2026-05-24** (libsodium 1.0.20 vendored byte-pristine; `build/sysroot/lib/libsodium.a` 520780 B; `/pouch-hello-sodium` runs 5 primitives — sodium_init, SHA-256 KAT, BLAKE2b round-trip, xchacha20-poly1305 AEAD, ed25519 sign+verify+reject-tampered) | no |
| 15 | `pouch-stratumd-build` — build stratumd against the sysroot; Thylacine `peer_creds` arm — **LANDED 2026-05-24** (stratumd 860536 B static ET_EXEC at `build/pouch/progs/stratumd`; `tools/build.sh::build_stratumd` drives Stratum's CMake; toolchain adds `__thylacine__` + `_GNU_SOURCE`; Stratum-side coordination branch `thylacine-pouch-arm` in `~/projects/stratum/v2`) | no (Stratum-side) |
| 16 | `pouch-stratumd-boot` — joey spawns real stratumd; `/sysroot` mount; ramfs pivot; retire the stub. **Sub-chunked 16a / 16b / 16c** in-session 2026-05-24; **16b further sub-chunked 16b-α / 16b-β** in-session 2026-05-25 under the stratumd-as-driver architectural decision (see `docs/reference/86-pouch-stratumd-boot.md` "Design decisions"). **16a LANDED 2026-05-24**: stratumd binary-load probe in joey (`t_spawn` + `t_wait_pid`; verifies binary loads + libc init + argv parsing + exit). Not audit-bearing. **16b — stratumd-as-driver (δ)** (chosen 2026-05-25 over: separate virtio-blkd daemon / writable-in-RAM pool / defer): stratumd holds `CAP_HW_CREATE` and drives virtio-blk directly via a new Stratum-side `src/io/bdev_thylacine.c` arm; Plan-9-canonical FS-server-owns-its-disk; persistent backing via QEMU disk.img; reversible. **16b-α** (next): argv pass-through kernel surface; pouch arm; joey passes richer argv. Audit-bearing. **16b-β**: Stratum-side `bdev_thylacine.c` + pouch HW-syscall arm + joey spawns stratumd with `CAP_HW_CREATE` + end-to-end pool mount + `/srv` bind. Audit-bearing. **16c**: kernel 9P client connect + `/sysroot` mount + ramfs pivot + stub retire. Audit-bearing. | yes (16b-α, 16b-β, 16c) |

**Sub-chunks 1-15 + 16a are now COMPLETE.** Sub-chunk 16 was sub-chunked in-session 2026-05-24 into 16a/16b/16c; 16b was further sub-chunked in-session 2026-05-25 into 16b-α/16b-β under the stratumd-as-driver architectural decision (see `docs/reference/86-pouch-stratumd-boot.md` "Design decisions"). **16a LANDED 2026-05-24**: stratumd binary-load probe in joey verifies the cross-built stratumd runs in Thylacine (binary loaded, libc init, argv parsing, clean exit 1). **Next: sub-chunk 16b-α (`pouch-stratumd-boot-argv`)** — argv pass-through kernel surface; pouch arm; joey passes richer argv to stratumd; audit-bearing. Then 16b-β (Stratum-side `bdev_thylacine.c` + joey spawn with `CAP_HW_CREATE` + end-to-end pool mount + `/srv` bind, audit-bearing). Then 16c (kernel 9P client connect + `/sysroot` mount + ramfs pivot + stub retire, audit-bearing). 16c closes Phase 5 + Phase 6.

## Exit criteria status

Full list in `POUCH-DESIGN.md §13`. None met yet (implementation not started). Checklist:

- [x] `aarch64-thylacine` cross-toolchain is complete — compiler, libc, CRT objects, **and the compiler-rt builtins** — and links via `ld.lld`. *(sub-chunk 4: headers + `libc.a` + CRT in `build/sysroot/`; sub-chunk 6a: `pouch-ld` drives `ld.lld`; sub-chunk 6b: `libclang_rt.builtins.a`. `tools/build.sh sysroot` produces a complete sysroot.)*
- [x] Static "hello" C program runs in Thylacine — prints, exits 0, no leak. *(sub-chunk 5: `/pouch-hello`.)*
- [x] `printf`-shaped hello works (buffered stdio path). *(sub-chunk 5: `/pouch-hello-stdio` proves the buffered-stdio path via `puts`/`fwrite`; sub-chunk 6c: `/pouch-hello-printf` runs literal `printf(3)` — the `%.2f` self-test verifies the `binary128` soft-float compiler runtime at runtime.)*
- [ ] Multithreaded test (N threads, shared mutex-protected counter, join) passes under default + TSan.
- [x] `AF_UNIX` `SOCK_STREAM` echo client/server (pure POSIX source) round-trips over `/srv`. *(sub-chunk 12: `/pouch-hello-sockets` runs the round-trip end-to-end via the new BYTE-mode SrvConn transport; PING/PONG bytes are byte-accurate, no 9P framing visible to userspace.)*
- [x] libsodium cross-compiles + self-test passes. *(sub-chunk 14: libsodium 1.0.20 vendored byte-pristine at `third_party/libsodium/`; `build/sysroot/lib/libsodium.a` built by `tools/build.sh::build_libsodium`; `/pouch-hello-sodium` runs the self-test in Thylacine — 5 primitives: `sodium_init`, SHA-256("abc") FIPS 180-4 KAT, BLAKE2b round-trip, xchacha20-poly1305 AEAD round-trip, ed25519 sign + verify + reject-tampered.)*
- [ ] stratumd compiles + links against the sysroot.
- [ ] stratumd boots, binds its `/srv` FS socket, serves 9P; the Phase-5 stub is retired.
- [ ] joey mounts `/sysroot` from real stratumd; ramfs→Stratum pivot completes.
- [ ] No P0/P1 audit findings on pouch's lower half or the kernel additions.
- [x] The patch series against vendored musl is documented + reproducible. *(sub-chunk 2: musl 1.2.5 vendored at `third_party/musl/`; the series is scaffolded + documented at `usr/lib/pouch/patches/`; seven patches landed across sub-chunks 4-13b: 0001 syscall seam, 0002 stdio backend, 0003 anonymous-memory backend, 0004 pthread layer, 0005 poll/select, 0006 AF_UNIX sockets, 0007 signals → notes. Sub-chunk 14 added libsodium with NO patches — libsodium is portable C.)*

## Build + verify commands

Standard (per `CLAUDE.md`): `tools/build.sh kernel [--sanitize=ubsan]` then `tools/test.sh` (`BOOT_TIMEOUT=420` for UBSan). New for this phase: `tools/build.sh sysroot` (becomes real at sub-chunk 1) + `tools/build.sh pouch-progs` (sub-chunk 5). TSan matrix matters from the `pouch-threads` sub-chunk onward.

## Trip hazards

- **The `pouch-wait-addr` + `pouch-threads` sub-chunks are the hardest.** The `torpor` wait-on-address primitive + the pthread mutex/condvar layer. Spec-first (`futex.tla`); TSan from the start. Either may split.
- **musl's lower half may resist a clean seam** (POUCH-DESIGN.md risk R2). **Sub-chunk 2 probed this and R2 is substantially de-risked**: musl 1.2.5 compiles end-to-end for `aarch64-thylacine` (1345 TUs, 0 errors / 0 warnings → a valid `libc.a`), so the boundary line is *compile-clean*. The residual risk is not the compile — it is lower-half *behavior* complexity (the thread model — the `pouch-wait-addr` + `pouch-threads` sub-chunks), where the work is genuinely hard regardless of the seam.
- **Invariant P-1**: no foreign syscall number ever enters the kernel. Every kernel addition this phase is a *native Thylacine* syscall designed on Thylacine's terms — never "the Linux futex ported."
- **musl issues syscalls down two paths.** The non-cancellable `__syscallN` (`arch/aarch64/syscall_arch.h`) AND the cancellable `__syscall_cp` → `__syscall_cp_asm` (a hand-written `.s`). Both must enforce the `0xFFFF` unimplemented-syscall sentinel — the P6-pouch-syscall-seam audit (F1, P0) caught the cancellable path unguarded. On a musl re-vendor, re-check every `.s` syscall path for the guard.
- **The compiler runtime is built** (sub-chunk 6b — `libclang_rt.builtins.a`, the compiler-rt builtins). `printf`/floating-point now links — the prerequisite for the `pouch-libsodium` + `pouch-stratumd-build` sub-chunks is met. Caveat: the runtime carries no unwinder (`gcc_personality_v0.o` references `_Unwind_*`, and pouch has no libunwind), so C++/`-fexceptions` code cannot link — dormant for plain C (`printf`, libsodium, stratumd unaffected).
- **clang cannot drive the ELF link on macOS** for `aarch64-thylacine` (it falls into the Darwin toolchain). `tools/pouch-ld` (sub-chunk 6a) drives `ld.lld` directly with the pouch link line; `build_pouch_progs` links through it. The CMake/autotools chunks point `CMAKE_C_LINK_EXECUTABLE` / `LD` at `pouch-ld` — wired with the first CMake-built pouch program. See `docs/reference/78-pouch.md` "Why linking cannot go through the clang driver".
- **A Thylacine zombie holds its handle table until reaped** (`proc_free` drains it, and `proc_free` runs at `wait_pid`). Orchestration that captures a child's pipe output must reap the child *before* draining the pipe — a drain-until-EOF before the reap deadlocks (the write-end never EOFs). `joey`'s `pouch_smoke_one` and `do_stratumd_stub_bringup` both embody this.
- **Stratum-side coordination** (the `pouch-stratumd-build` + `pouch-stratumd-boot` sub-chunks): the Thylacine `peer_creds` arm + possibly the block-layer arm are Stratum-repo changes.

## Known deltas from POUCH-DESIGN.md

No open deviations. The two toolchain gaps `pouch-hello-smoke` surfaced were **folded into the scripture** (the `P6-pouch-compiler-rt-design` update, with user signoff) and are now **implemented** — sub-chunk 6a (`pouch-ld`) and sub-chunk 6b (`libclang_rt.builtins.a`):

- **The cross-toolchain needs a compiler runtime.** POUCH-DESIGN.md §9 originally enumerated the sysroot as headers + `libc.a` + CRT — a complete C toolchain also needs the compiler-rt builtins (`binary128` soft-float for `printf`'s `long double` formatting, etc.; Homebrew LLVM ships Darwin compiler-rt only). §9 now names `libclang_rt.builtins.a` as a sysroot component; §14 inserts `pouch-compiler-rt` as sub-chunk 6.
- **clang cannot drive the ELF link on macOS.** For the unknown `thylacine` OS clang's driver mis-selects the host Darwin toolchain. §9 now records that linking invokes `ld.lld` directly via a `pouch-ld` wrapper; building that wrapper is part of sub-chunk 6.

Sub-chunk 7 (`pouch-mem`) surfaced a second scripture revision — `P6-pouch-mem-design` (user-signed-off): ARCHITECTURE.md §6.5 / §11.2 (`STATUS: COMMITTED`) still committed a Linux-shaped memory surface — `brk` + a file-backed `mmap` + `mprotect` + `munmap` — that predated POUCH-DESIGN.md invariant P-1. The two-tier native memory interface supersedes it: Tier 1 `burrow_attach` / `burrow_detach` (handle-free anonymous regions — sub-chunk 7's deliverable); Tier 2 handle-oriented Burrows (`KObj_Burrow`, deferred). ARCH §6.5 / §6.6 / §11.2 / §11.3 / §25.4 revised; `brk` and file-backed `mmap` dropped (the latter a permanent Plan 9-heritage refusal — a mapped file cannot be 9P-network-transparent); POUCH-DESIGN.md §8.1 / §12 open questions resolved. No code depended on the old text.

Sub-chunk 2 *resolved* an open question POUCH-DESIGN.md §4 left for the implementing chunk — the vendor path is `third_party/musl/` (not `usr/lib/pouch/musl/`); recorded in POUCH-DESIGN.md §4 + `docs/reference/78-pouch.md`. Further deltas surfaced during implementation are recorded here.

Sub-chunk 13 (`pouch-signals`) surfaced a third scripture revision — `P6-pouch-signals-design` (user-signed-off 2026-05-24): ARCH §7.6 carried only the design *intent* for Plan 9 notes (the standard note set, `notify`/`noted`, the I-19 causal-order invariant) — no concrete kernel API. The implementation requires a from-scratch kernel substrate, so the design needed to land first. The fd-first inversion (note Spoor minted via `SYS_NOTE_OPEN`; async handler is the legacy opt-in) is a NOVEL.md §3.1 totalization — the historically nastiest part of POSIX signal programming (async-cancel-safety) becomes the rare path. ARCH §7.6.1-§7.6.8 concretize the kernel API (data structures, syscall surface, delivery discipline, supported notes, invariants N-1..N-5); ARCH §16.4 refines the POSIX↔note mapping for the v1.0-supported subset; ARCH §25.4 / §28 / CLAUDE.md audit-trigger rows updated; NOVEL.md §3.1 gains the "Signals as a synthetic filesystem" bullet; POUCH-DESIGN.md §6.4 [RESOLVED 6.4 / 6.5] + §12 [RESOLVED 12.2] + §14 row 13 splits to 13a-impl + 13b-pouch + §18 decision rows refined; ROADMAP §7A.5 updated to reflect the spec-module deferral. No code touched yet; implementation lands at sub-chunk 13a.

## References

- `docs/POUCH-DESIGN.md` — binding design (authoritative).
- `docs/ROADMAP.md §7A` + `§2.1` + `§3.6`.
- `docs/ARCHITECTURE.md §16`, `§23`, `§25` (audit triggers), `§28` (invariants — pouch P-1..P-4 cross-ref).
- `docs/VISION.md §12` — Relationship to Linux and POSIX.
- `memory/project_pouch_phase.md`.
- Specs: `futex.tla` (#7), `notes.tla` (#8), `poll.tla` (#6), `pty.tla` (#9).
