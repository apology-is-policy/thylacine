# Phase 7 — status and pickup guide

Authoritative pickup guide for **Phase 7: Utopia (textual milestone)** — execution Phase 7 per `ROADMAP.md §2.1`; ROADMAP section `§8`. Binding designs: **`docs/UTOPIA.md`** + **`docs/UTOPIA-SHELL-DESIGN.md`** + **`docs/UTOPIA-VISUAL.md`**.

## TL;DR

**Phase 7 is OPEN — scripture landed; implementation underway.** The U-1 scripture commit (this commit's predecessor at the time this status doc lands) materializes the design conversation as durable scripture: the `ut` shell, the `libutopia` shared library, the native Rust coreutils, the `hx` (Helix) editor port, and the Pale Fire visual identity. The U-* chunk arc unfolds from here.

The Phase 7 entry decision (taken under the U-1 scripture conversation):

- **Shell**: `ut`, a new from-scratch design — rc-shaped with refinements per twelve resolved design axes. Native Rust on `libthyla-rs`. NOT a port of rc; NOT bash.
- **Coreutils**: native Rust on libthyla-rs, 9base-shaped. NOT uutils-coreutils.
- **Editor**: `hx` (Helix), ported via Pouch. THE only Pouch consumer in Utopia.
- **Visual identity**: Pale Fire — four colours (`bg`, `fg`, `path`, `glyph`), one glyph (`⊢`), one prompt format. Disciplined in Utopia's programs; user programs colour freely.
- **Runtime**: native libthyla-rs (the Plan 9 split — see `docs/ARCHITECTURE.md §3.5` + `CLAUDE.md` "Native vs ported userspace programs").
- **Workspace**: Cargo workspace at `usr/utopia/`; Helix vendored separately at `usr/helix/`.

## Landed chunks

| Sub-chunk | What | Commit | Tests |
|---|---|---|---|
| Warp-C **C-3** — composition by GPU blit, under MEASURED conventions, with an in-guest pixel oracle (GPU-DESIGN §4.5.11) | **The CPU fill of the composed screen is replaced where the GPU can compose.** On a GL host a Composed present of a software surface transfers its damage into the presented slot's own resource and composes by `VIRGL_CCMD_BLIT` slot → screen inside `COMPOSITOR_CTX` on the compositor's SYNC slot (`submit_blits`, chunked at the widened sync request region), then flushes — one dispatch, so `ComposeBlit`/`ComposeComplete` close together and the in-flight blit set is empty at every retire point by construction (detach-before-unref stays the whole ordering; the pipelined fenced form with a real drain is the C-4+ evolution the spec is cut for). A witnessed GL adoption composes by one blit BO → screen: zero guest↔host pixel traffic for a windowed GL client. Chrome stays CPU-painted, uploaded on damage on both paths (focus-only repaints upload only the frame/strip rects — the whole-buffer push would blank every pane on the GPU path). **The screen is minted `Y_0_TOP`** — the 2D screen's display convention; C-2b's flags-0 screen displayed its top-down CPU fill inverted on a GL display, invisible under #195. **Conventions are MEASURED at bring-up per (source shape × size class)** on throwaway contexts (`CONV_PROBE_CTX_BASE`+, one fresh per attempt), request variants tried until the landing has the order the shape needs, flips read off where it landed, each CONFIRMED at an asymmetric offset, fail-closed per class; the compose path picks the class by the op's box sizes and issues through the same builder. The compositor's own #240 health copy runs once per tick after a GPU-composed present; a failure latches GPU composition OFF (sticky, `composed-gpu-dead`, structural repaint at the next tick — never inline in the dispatch). `Held::Composed { cpu, gpu }` (upload+flush vs flush). `res_stale[slot] = !covers_full` on the GPU arm (§4.5.8c decided). **The pixel oracle**: `probe-screen X Y` (tapestry ctl, test-mode, ungated like the determinism verbs, rate-limited) reads texel (X,Y) of the SCREEN back — `via readback` on the 3D screen, `via backing` on the 2D — and says it with the `composed gpu G cpu C` census; the battery probes its sample points at every pixel stage and grew `multirect-v` (top/bottom split) and `tab-cycled ready` (hidden→visible on the composed path). **The measurement was wrong once and the oracle caught it on the first probe**: one convention measured unscaled and applied to the (scaled) panes composed them vertically swapped — virglrenderer routes unscaled same-format blits to copy-image and scaled ones to glBlitFramebuffer, opposite conventions for an inverting `Y_0_TOP` pair (`memory/bug_c3_convention_per_request_class.md`). Vault-owned `sub-tapestryd` prose over yip. | `7296bf07` | **thyla-pi (KVM, V3D) — clean (final binary, both legs)**: `composed pixels = GPU (blit conv: slot U plain sf1 df1, S plain sf0 df0; bo U plain sf0 df1, S src-neg sf0 df0)`, all four class confirmations CONFIRMED; GL leg `WARP-COMPOSED PIXELS: 9 probes via readback ok (composed gpu 34 cpu 0)` (A red, B blue, multirect green/yellow, multirect-v green/yellow, tab strips `#3a3a44`/`#7a9ecc`, tab-cycled A red), 2D leg `9 probes via backing ok (composed gpu 0 cpu 27)`, verb `C-2b/C-2c/C-3 COMPOSED-SCREEN GATE: VERIFIED` (nine terms). **Run 1** (one convention for both classes): `screen-probe (960,200) = #0000ff` for A's red — the natural convention sabotage, RED. **S1** (blit never submitted, GPU bookkeeping intact): `(960,200) = #101014` (pane background) with `composed gpu 10` — RED on the first probe. **S2** (every present routed to the CPU path): all 9 pixels exact `via readback` — the CPU upload into the 3D screen composes right too — but `composed gpu 0 cpu 31`, RED on the census term (the discriminator a correct-pixels sabotage needs). **quake** `WARP-4 GATE: VERIFIED` (44.9 fps; the BO composed arm's first live execution before the direct switch). **decomp gl**: composed **36.9 fps** vs 25.4 fps on 2026-08-10 (direct 44.4 both days) — the composed present's cost 16.8 → 4.6 ms/frame, the windowed-GL overhead 1.75× → 1.20×. Locally: `specs/check-tapestry.sh` 4 clean (5413/44696 distinct, unchanged) + 7 buggy as claimed; suite 1367/1367; LS-CI 36/36 (JOBS=3, 0 retries, gate wall 1596 s — incl. `ls-gfx-panes` on the changed battery); v8.0 floor OK; kernel byte-unchanged (SMP 40/40 carries). |
| Warp-C **C-2c** — the compositor-side IMPORT, witnessed (GPU-DESIGN §4.5.10 decided `909896bb`; §4.5.10a as built) | **The attach verb that never was.** P1b proved a `CTX_ATTACH_RESOURCE` into another context is what PERMITS a cross-context blit; the design (research-collapsed, no vote needed: Wayland/Scenic/nitpicker/rio all agree) makes it a compositor-side import bounded by hosting — no client verb. At `alloc_weave` tapestryd attaches every slot resource of a generation into `COMPOSITOR_CTX`; at `present-to` it imports the GL adoption's consented BO (the client handing its buffer to the compositor IS the grant); every import is revoked BEFORE the resource's unref on every death path (`release_gen`, `retire`, `wbo_retire`, `present-to off`/replace, the consented surface's retire via `comp_release_consents_for`). **Every import is WITNESSED**, because §4.5.4c showed the attach's OK attests nothing: the compositor context's own #240 mark/sentinel pair (`comp_probe`, minted with the ctx), a health copy first (mark → sentinel; the ILLEGAL_RESOURCE latch means one bad blit kills GPU composition for the process lifetime, so `comp_attached` fails closed and C-3 must never blit from a resource without it), then per slot: two distinct tokens seeded into the slot's host copy at guest rows 0 and h−1 through the present path's own `TRANSFER_TO_HOST_2D` (legal: no client mapping exists yet at `alloc_weave`), the sentinel poisoned, `RESOURCE_COPY_REGION` slot → sentinel inside the compositor ctx, read back — and WHICH row came back is reported. The BO import uses a two-poison CHANGE witness (the client's texel is unknown to us). Say lines per import + a ctl census (`comp-attach witnessed W refused R`). The witness deviates from the design paragraph in two ways stated in §4.5.10a (a 1×1 compositor-owned target instead of the screen; import time instead of composed entry — the reason for composed entry no longer applied). **Found on the way**: the console TX ring is byte-atomic (a kernel `proc: orphan` burst tore the posture anchor char by char — OPEN, `memory/bug_console_tx_ring_byte_atomic.md`, handed to aux who was in `cons.c`); vrend's copy from a `Y_0_TOP` source names texel row h−1−y here (measured, C-3 inherits it); the gate script needed three fixes of its own (regexp under a format change; `-re` arm order beating buffer position; an unanchored alternation matching partial serial lines); and the `quake` run surfaced a C-2d-b leftover — five `scanout direct N (WxH)` regexps in `glq-virgl`/`glq-decomp`/`glq-wedge-probe` silently broken since `f86177b6` renamed the line (`slot S` token; fixed as optional). Vault-owned `sub-tapestryd` prose over yip. | `9c842f25` | **thyla-pi (KVM, V3D) — clean**: 8/8 generation imports `witnessed 3/3 (copy read texel row h−1)`, `WARP-COMPOSED ATTACH: witnessed 2 surfaces (copy read texel rows: 799 797)`, both legs PASS, verb `C-2b/C-2c COMPOSED-SCREEN GATE: VERIFIED` (7 terms, rc 0); **sabotage** (slot attaches skipped): first import `REFUSED (slot 0 copy did not land)`, every later one `SKIPPED (compositor ctx unhealthy)` — the latch measured — and the screen's 3D mint fell back for real (`2D ... -- 3D refused: renderer round trip`), verb RED, 2D leg unaffected; **quake** (the BO import through the SDL shim's real `present-to`): `comp-attach ctx 1 bo 1 res 79 -> surface 1: witnessed`, direct GL switch to res 79, 44.7 fps, census `GLQ-VIRGL COMP-ATTACH: witnessed 5 refused 0`, `WARP-4 GATE: VERIFIED` (after the eviction-regexp fix; the first run failed closed on it). Locally: suite 1367/1367 (0 FAIL); LS-CI 36/36 (JOBS=3, 0 retries, gate wall 1673 s); v8.0 floor OK; kernel byte-unchanged (SMP 40/40 carries). Six Pi `composed` cycles in all — one instrument control + one anchor tear + three gate-script defects before the green one, each recorded in §4.5.10a. |
| Warp-C **C-2b's "3D" word re-earned** — a virtio-gpu `RESP_OK` is not the renderer's verdict (GPU-DESIGN §4.5.4c) | **Found while designing C-2c's gate, by reading QEMU's source before repeating a claim a third time.** QEMU v10.0.0 `hw/display/virtio-gpu-virgl.c` IGNORES the `virgl_renderer_*` return for `CTX_CREATE`, `RESOURCE_CREATE_2D/3D`, `CTX_ATTACH/DETACH_RESOURCE`, `TRANSFER_TO_HOST_3D`, `SUBMIT_3D`, `CTX_DESTROY`, and checks `ATTACH_BACKING`'s only to clean up the iov — `RESP_OK_NODATA` means "QEMU parsed it" (nonzero, non-duplicate id; QEMU inserts into its own `reslist` BEFORE calling the renderer). Only `SET_SCANOUT` (`resource_get_info_ext`) and `RESOURCE_UNREF` consult existence. So the C-2b gate's "four response-checked round trips the host answered OK — a claim about the host accepting the object" was FALSE in `composed-screen.exp`, `149-warp.md` and (by reference) the C-2a+C-2b row below — and not only prose: `alloc_screen`'s "a 3D failure falls back to 2D" was DEAD for a renderer-side refusal (`is3d` reduced to `comp_ctx`; the failure would land later, silently, as `INVALID_RESOURCE_ID` at the composed `SET_SCANOUT`, whose result the code dropped after printing "scanout composed" BEFORE the bind — display keeps the previous scanout, gate says VERIFIED). #240 had measured this shape for `SUBMIT_3D` four days earlier and it was never checked against the family — the C-2d gate-pattern lesson one level up. **Repair = #240's technique**: `alloc_screen` earns "3D" by a sentinel pixel round trip through `COMPOSITOR_CTX` (`TRANSFER_TO_HOST_3D` → clobber → `TRANSFER_FROM_HOST_3D` → compare → restore), which only a resource the renderer holds + has attached + moves pixels through can pass; a refusal falls back to 2D for real with the reason on the screen line; the composed line prints AFTER the bind with its verdict; `composed-screen.exp` + the `composed` verb grew a fifth term (the bound resource IS the minted screen, both legs). C-2c consequence: its attach can never be verified by responses — its gate is P1b's two arms in-guest (attach + one blit + readback). Vault-owned `sub-tapestryd` prose over yip. | `46e8468e` (+ `909896bb` the C-2c design, docs only) | **thyla-pi (KVM, real V3D) A/B**: sabotage (3D create with `VIRGL_FORMAT` 0x7FFF) → GL leg `screen res 71 2D (1280x800) -- 3D refused: renderer round trip` + `res 71 bound` — i.e. create/attach/backing all returned OK from the device under a format the renderer refused; scenario RED on the arm, verb GATE FAIL (three terms), non-GL leg unaffected; **clean build: GL leg `screen res 71 3D (compositor ctx) (1280x800)` + `res 71 bound` PASS, non-GL leg `2D` + `res 71 bound` PASS, verb rc 0 on all five terms** — one variable, two verdicts, same resource id. Locally: build clean; suite 1367/1367; LS-CI 36/36 (JOBS=3, 0 retries, gate wall 1588 s, run DETACHED after the harness stopped the tracked launch -- #128 SEQUEL 3's shape exactly); the four warp scenarios that grep `scanout composed \((\d+)x(\d+)\)` still match (prefix); kernel byte-unchanged (SMP 40/40 carries). |
| Warp-C **C-2d** (a: client buffer age; b: one host resource per slot) + **the gate that can see it fail** | **The 4.5.8 decision made real.** §4.5.8 (operator-voted 2026-08-16) put ONE host resource per weave slot so a compositor blit of slot *i* can never collide with a client fill of slot *j* — and §4.5.8a found, before a line was written, that this removes the single accumulating resource every damage-only client had silently relied on (three slots, rotating, nothing copies between them; only the host resource made a damage-only present look complete). §4.5.8b (voted: buffer age, client-side) is the repair: `libtapestry::age()` — the `EGL_EXT_buffer_age` contract, DERIVED in the library because it owns the rotation and the loom CQE is kernel ABI — and aurora repaints the union of the last `age-1` presents' damage. **C-2d-a** (`931bf15a`) landed inert into a tree with the accumulator still there; **C-2d-b** (`f86177b6`) then minted `WEAVE_SLOTS` resources per generation, made `res_stale` per-slot, rebound Direct scanout to the PRESENTED slot every frame (a KMS page flip), and turned `Held::Direct(Rect)` into per-slot rects — **and shipped explicitly UNVERIFIED (§4.5.8c)**: with the server half live, disabling aurora's age handling left `ls-gfx` GREEN, so no existing gate could see the property. **This row's third commit is that gate** (`tools/interactive/ls-gfx-age.exp` + `gfx_region.py`, §4.5.8d): aurora in DIRECT scanout (composed cannot show a stale client slot — the screen is the accumulator there); fill → a POSITIVE control on four slot-rotated dumps → `clear` (one all-rows present into ONE slot) → eight rounds of 1,1,2,1,1,2,1,1 keystrokes (row-0 presents into successive slots) each dumped and asserted EXACTLY background over a cell-region, every pixel read. The 1,1,2 pattern is not decoration: it visits all three slot residues for any constant blink rate, which is what makes the one-stale-slot class (an off-by-one in the union) catchable deterministically instead of at (2/3)^8. **Building the gate found a defect in C-2d-a**: it recorded the WIDENED repaint range as damage history, a fixed point at full rows — correct pixels, but every present after any scroll repainted the whole grid forever, so the damage path was dead. Fixed (record the dirty span; a full entry falls out after `nslots` presents), and the fix is what the gate now guards. Vault-owned surfaces (`sub-aurora`, `sub-libtapestry`, `sub-tapestryd`) notified over yip; the local reference carries the gate. | `931bf15a` + `9b63f036` + `b34c1c07` + `f86177b6` + `7727c135` + `6bfab36e` | **ls-gfx-age GREEN** (0/368280 non-bg px on 8/8 dumps; positive 63882/368280 on 4/4) — and **RED under both sabotages, 3/3 attempts each**: S1 `stale_slot=false, back=0` at rounds 2,1,2; S2 `back` off by one at rounds 2,5,2; restore green. Suite 1367/1367; LS-CI 36/36 (JOBS=3, 0 retries, gate wall 1550 s); kernel byte-unchanged (standing SMP 40/40 records carry). **Owed**: the focused audit on `usr/tapestryd` (I-40 surface); agent spawning is off this session. |
| Warp-C **C-2a + C-2b**: the compositor's own virgl context and the 3D screen — landed unexercised, then given the driver that exercises them | **C-2a** (`7b116fc7`) mints `COMPOSITOR_CTX = 0x100` — a context the compositor owns, distinct from every client's — and reports the composed posture at bringup. **C-2b** (`ffa9105d`) makes the screen a host-side 3D resource in that context (`resource_create_3d` -> `ctx_attach_resource` -> `attach_backing`, the audited `warp_probe_res` shape), falling back to the 2D resource wherever the mint fails, per **4.5.9** (`62c350c5` + `7639fbf2`): the CPU path is not a fallback to be retired but the UNIVERSAL one, since `virgl=0` on the default dev device and there is no virtio-gpu at all on bare metal. **Both landed with the 3D arm having NEVER EXECUTED, and the commit said so** rather than presenting a green boot as verification — a green boot proves only that the gate did not fire, which an `if (false)` proves equally well. **The reason was a tooling gap, not a code problem, and finding it took reading the mode machine rather than the diff**: `alloc_screen` runs only under `Scanout::Composed`, and every pre-existing warp verb that boots the GL device (`capset`, `prove`, `tri`) drives at most ONE display-sized surface, which `reconcile()` resolves to **Direct** — scanning out the client's own resource and bypassing the screen entirely. So the composed path had no driver on the only host that can run its GPU half. **The fix is a driver, not a change to the thing under test**: `tools/warp/composed-screen.exp` runs `/bin/tapestry-battery`, a ramfs-native client that brings up TWO surfaces — the cheapest thing `reconcile()` resolves to Composed, and one that needs neither GL nor the pool, so the ONLY GL object in play is the compositor's own screen. **My own resume note asserted no verb forces Composed; that was wrong** — `quake`/`decomp`/`wedge` all split the layout — and the battery was picked over `quake` anyway for isolating the variable, not for being available. | `7b116fc7` + `ffa9105d` + `639b5b18` | **`tools/warp-host.sh composed` GATE: VERIFIED on thyla-pi (KVM, real V3D).** The control is the DEVICE, and the two legs returned DIFFERENT answers on one host with one variable changed — `virtio-gpu-gl-pci` -> posture GPU -> `screen res 67 3D (compositor ctx) (1280x800)`; `virtio-gpu-pci` -> posture CPU -> `screen res 67 2D (1280x800)`. That difference is the whole point: a GL-only leg would pass just as happily against a build that ignored `comp_ctx` and always minted 3D, so the non-GL leg is not a bonus — it is what makes the GL leg mean anything, and a measured disagreement is stronger evidence than two greens. Two claims kept separate rather than collapsed: the POSTURE line must match the device (feature negotiation works) and the SCREEN arm must match the posture (C-2b follows the capability) — so a host that silently lost its GL cannot satisfy the second by making both sides equally wrong. The verb's four-term conjunction requires both screen lines AND both scenario completions, since a leg that died right after printing its screen line would otherwise still show the evidence the gate greps for. |
| Warp-C **C-1**: the spec extension — the composed present is modelled, and the model refuted a premise of its own first draft | **The C-0 pair passed, so C-1 extends `tapestry_present.tla` BEFORE any impl** (GPU-DESIGN 4.5.6 — model-first is binding on this surface). The composed path lands behind `ALLOW_COMPOSE`: `Attach`/`Detach` (P1b's authority-conferral point), `ComposeBlit`/`ComposeComplete` (the fenced blit list, then SET_SCANOUT), the `DrainedOfBlits` conjunct on `ServerRelease`+`Free`, and two invariants repeating T-1's own LIFETIME/CONTENT split — `NoTornCompose` (pages stay backed under an in-flight blit) and `NoStaleCompose` (a blit and a fill of one host resource never overlap). **The additive claim is MEASURED, not asserted**: baselines were taken BEFORE touching the module, and with the switch off the six pre-existing cfgs reproduce 5413 distinct states exactly. **TLC then refuted my model, and the tree refuted the premise under it.** I had carried the in-flight blit as the SLOT it reads, reasoning that a client filling a different slot during a composition is legitimate pipelining — and wrote that justification into the header. It is false: tapestryd allocates ONE 2D resource per surface with whole-weave ATTACH_BACKING and transfers at a per-present OFFSET (`gpu.rs`), so guest-side slots buy NO host-side concurrency. The guard also had the shape of a pinned bug — `intransfer = 0` is a gauge reading zero, equally true of "the fill landed" and "no fill was ever issued" — now closed by an explicit `filled`. **The design consequence is real and belongs to C-2/C-3: the D1 recycle gate does not survive the composed path unchanged.** In the direct path a present's terminal CQE genuinely means the host has finished reading; once the compositor is a SECOND reader of that one resource, the CQE stops meaning the resource is free, and nothing in the old rule notices. Cheap to find here, expensive at C-4. The exclusion is symmetric, so it gets a sabotage per direction rather than one flag opening both gates. | `ee581fbd` | **`specs/check-tapestry.sh`: ALL 11 CFGS AS CLAIMED.** 4 clean run to completion (5413 / 5413 / 44696 / 44696); 7 buggy each violate the invariant they NAME, not merely something. The harness measures the two classes differently on purpose: a buggy cfg halts at the first violation, so with parallel workers its state count is scheduler noise — measured varying 129/141/155 across three identical runs — and pinning it would be pinning the instrument. Verdicts key on EXIT STATUS, never on prose (TLC writes both "is violated" and "was violated"). Non-vacuity checked by coverage rather than assumed: the composed actions fire `0:0` with the switch off and `ComposeBlit` 2264 / `ComposeComplete` 7328 with it on, so the green is over a CONSTRUCTED state. Liveness confirmed genuinely checked (TLC: "Checking temporal properties for the complete state space with 44696 total distinct states") — the point being that a real drain's risk is TERMINATION, and stage-0 synchrony could not deadlock because it had nothing to wait on. |
| **Warp-C P2 MEASURED**: no cross-context reordering in 500 unsynced trials — and the probe was PROVEN able to see one | **The C-0 pair is now both answered, host-side, without touching the guest.** P2 asks whether a blit on the compositor's context observes the client context's FINISHED frame with nothing ordering them. The hazard is real in principle: virglrenderer maps each guest context to its own host GL context, and in-order controlq dequeue orders the COMMANDS but not GL execution across two host contexts sharing an object. It is masked today by an accident -- the present path ends in `transfer_from_3d_sync`, which must produce bytes and so forces the sync as a side effect -- and **a blit has no such side effect, so the hazard goes live exactly when C-4 removes the readback.** Measured on thyla-pi (real V3D) across depths 24/64/256 at 1024x1024: SYNCED 0 mismatches, UNSYNCED **0/500**. **The result is worth something only because of the INVERTED arm**, which blits BEFORE the clear so staleness is guaranteed by construction and every trial MUST mismatch (40/40, every run). Without it a clean measurement is equally consistent with "ordering holds" and "this probe cannot see a stale read" -- the same trap that made P1a's same-context control load-bearing, and the third time in one day that a negative needed a positive to mean anything. **Two corrections while building that arm, both worth keeping**: it first scored 39/40 because trial 0 has no previous frame to be stale from; and the first fix seeded the DESTINATION, which changed nothing, **because a blit overwrites the destination wholesale -- staleness here is always a property of the SOURCE**. Seeding the source restored a strict all-must-mismatch bar rather than relaxing it to n-1, which would have hidden a genuine one-trial blind spot behind an allowance. **Stated limits, because a negative is not a proof**: rule-of-three bounds any per-trial reorder rate at ~0.6% (95%) on THIS stack for THIS pattern; V3D is a single-queue tiled renderer, so submission-order execution may be a property of the hardware rather than of virglrenderer, which would say nothing about a multi-queue GPU; and every trial ends in a readback, which serializes it. C-1 still models the hazard — this bounds how hard it is to hit instead of guessing. | `fb2a5c24` | **`tools/warp-host.sh p2` GATE: PASS** (log `build/warp-p2.log`). The gate's load-bearing term is SENSITIVITY, not the verdict: it requires the INVERTED arm to have mismatched on every run before it will accept a clean measurement, so a blind probe's silence is `NO VERDICT` rather than a pass. All four sabotages produce distinct correct outcomes — probe blinded -> NO VERDICT, hazard injected -> HAZARD OBSERVED (exit 1), verdict removed -> NO VERDICT, instrument failure -> NO VERDICT. A defect in the verb was caught by testing it rather than reading it: `P2_DEPTHS`/`P2_TRIALS` were escaped and so expanded on the REMOTE, where they are unset — the knobs silently did nothing until they were made to expand locally. |
| **Warp-C P1b ANSWERED**: an explicit `ctx_attach_resource` permits the cross-context blit -- the design survives | **P1b's gate was CIRCULAR, and the way out was to leave the guest entirely.** P1b must pass before anything structural lands, but in-guest it needs a cross-attach verb that does not exist: `CTX_ATTACH_RESOURCE` lives only inside tapestryd and both call sites attach to the resource's OWN `dev_ctx`, while the client-facing ctl verbs are only `verify`/`present-to`/`submit`. Building that verb is C-2 -- so P1b gated C-2 and needed C-2. Cut by asking virglrenderer directly, host-side on thyla-pi against the real 1.9.0 runtime: no guest change, no I-45 authority decision, no scripture change. **Deterministic 3/3 per arm: WITH the attach the destination goes GREEN; WITHOUT it the destination stays RED and vrend says `context error reported 2 "ctxB" Illegal resource 1080`.** **The second arm is the result, not a formality** -- a success WITH the attach shows only that the attach did not PREVENT the blit; only its absence shows the attach is what PERMITS it, and the two readings are opposite for I-45 ("the attach is the authority gate" vs "virglrenderer isolates nothing between contexts"). **The instrument re-derives a known answer before reporting a new one**: arm 2 reproduces P1a's refusal in the same words, from a different program, through the host API rather than the guest seam. **`submit_cmd` returned rc=0 in BOTH arms**, confirming 4.5.4a one layer down -- the refusal reports success at the host API too, so pixel readback was the only oracle that could separate them; the *design* risk closes while the *guest readability* gap (C-0d) is untouched. **Two dead ends worth keeping.** (1) Four winsys-flag sweeps chased a bare `rc=-1` that was my own bug: `virgl_renderer_init` brings the winsys up FIRST and only then checks `if (!cookie || !cbs)`, so passing a NULL cookie yields a successful `get_drm_fd` callback, a successful winsys, and then a refusal that looks exactly like a host that cannot go headless. Reading the source found it in one step after four blind sweeps found nothing. (2) The first detached watcher used `setsid`, absent on macOS -- it died instantly and the negative control PASSED, because a process that never starts also never fires; only the "is it still alive?" leg caught it. Opcodes and offsets come from `#include`d project headers, never transcription, after C-0d lost a run to a LINE NUMBER (96) standing in for an ordinal (17). | `a601d978` | **`tools/warp-host.sh p1b` GATE: PASS** on thyla-pi (real V3D), log `build/warp-p1b.log`. Gate is a FOUR-term conjunction, each term sabotage-tested to fail independently (missing control; encoding unproven; attached arm absent; a tripped CHECK; and the `-ge 2` boundary -- encoding proven in exactly ONE arm -- which is the term that would let an unproven arm through). Instrument legs assert themselves in-band: the colour convention is CHECKED against a real readback rather than assumed, a same-context blit proves the encoding before any cross-context claim, and a `0xDEADBEEF` sentinel makes "readback never landed" its own value rather than a colour. Also fixed a pre-existing defect found by the verb-coverage control: `usage()` printed a FIXED line range and had silently stopped listing `quarry-bench`, `quarry-wedge`, `native-bench`, while `wedge-gate` and `tri` were never listed at all -- a third of the verbs invisible; now ranged to the block terminator so adding a verb cannot truncate it. |
| **main#243 audit close** (Opus fallback): 0 P0 + 0 P1 + 2 P2 + 2 P3, all fixed | **The round's value was that it MEASURED the one thing I had left to reasoning.** **F1 [P2] -- the store width is an ABI.** `viv_sigtab_reset`'s byte loop compiled to 2-byte `sturh` under the kernel's own flags: `-ffreestanding -fno-builtin` is exactly what stops LoopIdiomRecognize forming a `memset`, leaving an unroll-by-2. Every 8-byte `handler` was written as four independent halfword stores, so a cross-Proc reader could observe a handler value **no code ever wrote** (half old address, half zero) and pass the `> VIV_SIG_IGN` gate on it -- and `viv_sigtab_note_handler` re-reads the field after gating it. Fixed by per-field struct assignment (28 `stp` of X registers, zero sub-8-byte stores) plus an explicit `__ATOMIC_RELEASE` fence, so the zeroing is published deliberately rather than incidentally by whatever lock the caller happens to take next. **The reviewer was careful that this was not a live defect -- the live defect was the COMMENT**: `proc.h` claimed "safe BY DEFAULT" and `proc.c` "byte-identical", both true of the settled table and false of the observable transient, in a comment addressed to precisely the future author who adds the third cross-Proc reader. Restated: memory safety and per-FIELD integrity are unconditional; **a snapshot is NOT promised** (a reader may see a mix of pre- and post-reset entries -- the POSIX exec-vs-signal race, sound). **F2 [P2]** -- `docs/reference/145-vivarium.md` still carried the superseded argument "the only cross-thread reader is `notes_post`'s SIG_IGN hook, and it touches one naturally-aligned u64"; both halves wrong (two cross-Proc readers; the second copies the whole 32-byte entry). **This claim has now been wrong twice in the same place** -- `VIVARIUM.md` records the first (#97: byte-sized entries "could not tear", true at V-6b, false once entries widened) and that correction did not reach the sibling doc either. **F3 [P3]** -- the 21-line header explained at length why it does not FREE and never stated the precondition making the WRITE safe. **F4 [P3]** -- the test probed 2 of 14 entries and 1 of 4 fields, so a regression zeroing only each `handler` passed every assertion. **C2** -- my withdrawal of the "UAF writes" escalation SURVIVED independent prosecution: every writer is same-Proc, and `rfork(RFMEM)` refcounts the AddrSpace and never the sigtab. **Fable was unavailable (quota); family independence FORFEITED, stated in the prompt and in the report rather than treated as equivalent** -- a Fable round on this surface remains owed. | `30627043` | Not a dirty close (0 P0, 0 P1, P1+P2 = 2). F1 verified **non-vacuously on the SHIPPED ELF**, not a standalone reproduction: 32 instructions disassembled, 28 `stp` against an independently derived expectation of 448/16, 0 sub-8-byte stores, 1 `dmb`, 1 `cbz` NULL guard -- and the first non-vacuity guard was itself miscalibrated (`^\s+[0-9a-f]+:` reported 0 instructions while 28 `stp` were present, because objdump emits addresses with no leading whitespace). F4's fix **revert-probed**: a handler-only reset fails ONLY the new byte-loop assertion (1364, rc=1) while every pre-existing accessor assertion passes -- F4's claim demonstrated rather than argued. `viv_sigtab_reset(NULL)` added to the accessor NULL-safety battery. |
| **main#244**: `tools/agent-protocol.md` was a MANDATORY co-update target that never existed | **An unfollowable item inside a mandatory list teaches the reader that the whole list is advisory.** Surfaced by #242's sweep and held back from it deliberately: the fix was a decision, not an edit, and it touches a stated ABI contract (the boot-banner tooling ABI), so it wanted the user's vote rather than my judgement. Three binding docs -- CLAUDE.md's boot-banner contract, `TOOLING.md` section 10, `reference/01-boot.md` + `04-extinction.md` -- named the file as a same-commit co-update target for any change to `Thylacine boot OK` / the `EXTINCTION:` prefix. It was planned in Phase 1 and never written. **The damage is not the phantom, it is the four real entries beside it**: a reader who discovers one member of a mandatory list is un-actionable has been taught the list does not have to be followed, and the other four ARE real files. Fork put to the user: (1) write the doc, or (2) retire the citation and point at `TOOLING.md` section 10, which IS the agent-side protocol. **User voted (2)** -- the doc would have been a third copy of a protocol already stated twice, and a third copy is a third thing to drift. | `b5285434` | Docs only; kernel byte-unchanged. All four citation sites verified to resolve after the edit; `tools/agent-protocol.md` verified at ZERO git history (never existed, as opposed to deleted) before retiring rather than restoring it. |
| **#173**: I-45 had no registry row anywhere, and CLAUDE.md's mirror was four rows behind | **A rule saying "keep these in sync" is safe-if-remembered; only a check that FAILS is safe-by-default.** Vault-reported. I-45 (GPU authority bounded by the context) was **enforced in code and prosecuted BY NAME in `server.rs`** ("the I-45 breach F6", closed by an audit round) while its only written definition -- `GPU-DESIGN.md` section 8 -- was headed "(proposed)" and called the same bound ENFORCED 360 lines earlier. Two different claims, both in circulation, neither in a registry. **Main found the worse half**: CLAUDE.md's condensed invariant table ended at **I-39, four rows behind ARCH** -- under an explicit "keep the ROW SET in sync" instruction AND a note recording that RW-10 had already repaired that exact drift once. CLAUDE.md is loaded into EVERY session, so it is what an instance believes BY DEFAULT before opening anything. **RW-10 fixed the instance and left nothing that could fail.** Both tables now carry identical 45-row sets; I-45's row names its three axes separately (guest-exposure ENFORCED / host RESERVED-not-enforced with the host documented trusted / v3d unbuilt) per the I-20/I-40 staged precedent, so "I-45 holds" can never again be said without naming which half. I-41 gained a row **saying it is NOT ALLOCATED** (AG-2's software-breakpoint isolation, reserved in `ADVANCED-GO-DESIGN.md`, never promoted) rather than being left as a silent gap -- a registry silent about a number it skips makes every future reader repeat the same investigation. **Then vault caught the follow-on in my own fix**: I filed I-41 AFTER I-44 in ARCH, having anchored the insert on the I-45 row I had just appended, so a reader scanning numerically still hit I-40 -> I-42. **My check was blind to it** -- set equality has no opinion about order, so I had probed the property I implemented rather than the property I needed. | `5da054e4` + `3cd6ff52` | **The durable half is `tools/check-invariants.py`, FATAL in `build.sh`.** Three properties: (A) CLAUDE.md row set == ARCH section 28 row set; (B) every `I-NN` cited in `AUDIT-TRIGGERS.md` resolves to a real row; (C) rows ascend numerically (added at `3cd6ff52` for the defect above). Plus a **vacuity guard** (`len < 20` on either side is a parse failure, not a pass) -- without it, a regex that stopped matching would report agreement between two empty sets. Discrimination proven per property, not per tool: deleting I-45 from ARCH reproduces the pre-fix state and fires (A) and (B) together; moving I-41 back after I-44 fires (C) alone; breaking the row regex fires the guard. Live reading in the build: `invariants OK: 45 rows, ARCH == CLAUDE.md, 43 cited in AUDIT-TRIGGERS.md all resolve`. |
| **main#243** [P1 UAF]: exec freed `p->sigtab` unlocked under two cross-Proc lockless readers | **A free that was correct when written and became wrong when the reader set grew -- the failure mode no single-commit review catches.** Reported by aux (yip 0011) from a self-audit in THEIR tree; verified here against main's own files before acting. `proc_exec_replace` did `kfree(p->sigtab); p->sigtab = NULL;` with **no lock held** (grep-verified: the only lock op between the last `spin_unlock_irqrestore(&g_proc_table_lock)` and the free is that unlock). The comment justifying it claimed "its only reader is `notes_deliver_at_el0_return`, which runs on a thread of THIS Proc, and we are the only live one" -- the single-THREAD half true (`proc_exec_alone`, re-checked under the lock), the single-READER half **false**: `notes_post`'s SIG_IGN hook (above that function's `q->lock`, returning before taking it) and `notes_proc_has_live_handler` both load `->sigtab` with a bare acquire, and `notes_post` is reached with ANOTHER Proc as target on every caller (child_exit->parent, SYS_POSTNOTE, the pgrp fan, the console interrupt, TTY hup). **FIX: reset in place, never free** (`viv_sigtab_reset`). Sound by the tree's own statement -- `vivarium.c` already says "SIG_DFL is 0 and SIG_IGN is 1... a zeroed table therefore reads as all-default", and `viv_sigtab_of` uses `kzalloc`, so a reset table is BYTE-IDENTICAL to a fresh one. **`docs/VIVARIUM.md` already described the sigtab as "freed at `proc_free`"** -- so the exec free was code that had DRIFTED FROM SCRIPTURE, and the fix restores the documented lifetime rather than inventing one (found by self-audit AFTER the commit; reading scripture first would have arrived faster). **WHY NOT A LOCK, recorded at `8f377c2d` in the header where the next author reads it:** a lock holds only if every FUTURE reader remembers it, and aux's tree grew a THIRD cross-Proc reader two commits before filing the bug, written as a bare atomic load **by the author holding the finding**. main is one predicate away from the same: it lacks that predicate but already has the cross-Proc call site (`proc_tty_susp_would_stop_locked`, `proc.c:3690`, called at `:3827` with an arbitrary pgrp member). Resetting makes reader-set growth safe BY DEFAULT, not safe-if-remembered. **SELF-CORRECTION, withdrawn not softened:** I first escalated to "UAF WRITES / heap corruption" off two `viv_sigtab_set` call sites; checking each site's ENCLOSING FUNCTION showed both writers run on a thread of the target Proc, which `proc_exec_alone` excludes. Aux's original read (a UAF READ) was right. The grep proved a write EXISTED; only the enclosing function proved WHO COULD RUN IT. | `a41fc9eb` + `8f377c2d` | **Full bar GREEN.** New `proc.exec_reset_dispositions`, **revert-probed**: restoring `kfree`+NULL fails it on exactly its intended assertion (1364, 2 FAIL, rc=1); fixed 1365/1365 rc=0. The test asserts a POINTER, not behaviour, and that is forced: `viv_sigtab_note_ignored/_handler` are NULL-safe and answer "nothing ignored, no handler" for a NULL table -- **exactly** what they answer for an all-default one, so freed-and-NULLed and reset-in-place are behaviourally indistinguishable at every observable surface and **no E2E can see this fix**. Hence the split-out seam + a pre-state non-default check. **SMP gate RE-RUN from scratch on the fixed kernel** (the banked 40/40 tested a kernel that no longer existed): **40/40, 0 corruption / 0 external-kill / 0 timing**. LS-CI **35/35 PASS, 0 SKIP**. specs **33/34 via `make specs` (0 FAILED)** + `sched_oncpu`'s 7 cfgs by hand -- 4 CLEAN at **66/124/86/152 distinct** (reproducing the prior run exactly) and the 3 counterexamples all still fire (`prefix860`(+3cpu) Safety, `intree_guard` Availability); verdict matched by SHAPE `(is\|was) violated` with the clean cfgs as the not-matching control, 0 unclassified. `docs/reference/147-execve.md` corrected (its table said "lazily re-allocated" -- the removed behaviour). |
| **main#242**: 17 phantom + stale path citations across three binding scripture docs | **A mechanical rename PROPAGATES a phantom rather than exposing it -- and leaves it looking FRESHER than the real files beside it.** Vault's #170 flagged three pathless rows in `docs/AUDIT-TRIGGERS.md` (filled at `abb8494e`); an EXPANDING sweep (brace-expand `foo.{c,h}`, stat each member) showed the defect was confined neither to that file nor to *missing* paths. Root cause is one commit: the Phase 4 thematic rename `0d1917a5` re-spelled the NOUNS without verifying the PATHS -- `| VMO | kernel/vmo.c, mm/vmo_pages.c |` became `| BURROW | kernel/burrow.c, mm/burrow_pages.c |`, where token one is real->real and token two is **phantom->phantom** (neither file has ever existed). `init/init.c`->`init/joey.c` is the same move. Nothing in the rewrite could tell them apart because it matched on the noun, and the touch leaves the error scoring well-tended under any recency heuristic. **The AUTHORITATIVE copy was the worse one**: `mm/vmo_pages.c` was fixed in AUDIT-TRIGGERS.md while `ARCHITECTURE.md` section 25.4 -- which many trigger rows *declare authoritative* -- carried the renamed twin, unswept. Fixed across ARCH (`mm/burrow_pages.c`, `init/joey.c`, `kernel/aslr.c`, `arch/arm64/context.c` x2, `fs/ramfs.c`, `specs/namespace.tla`, `compat/linux-syscall.c`), AUDIT-TRIGGERS (`docs/REVENANT.md` x4 -> the alias the row itself declared, `88-pouch-stratumd-boot-16c.md` x3, the larder brace) and CLAUDE.md's always-loaded index (`kernel/ctl.c`). Sub-class worth its own name: **`NN-` placeholders that outlived their docs** -- `NN-overcommit.md`/`NN-devenv.md` LANDED as `127-`/`128-`, so the row promised prose validation at a filename nobody can open. Two sweep methods proved complementary, neither a superset: a READING pass judges claims (documented negatives, declared aliases) and is blind inside a brace; vault's EXPANDING pass stats members and caught `larder.c`, but cannot judge a claim. **Enqueued not fixed (main#242):** `tools/agent-protocol.md` has never existed yet CLAUDE.md + TOOLING.md (x2) + `reference/01-boot.md` all name it a MANDATORY same-commit co-update target for the boot-banner ABI -- an unfollowable binding instruction erodes the three targets that DO exist; its fix is a decision (write it vs repoint at TOOLING.md section 10) and edits a stated ABI contract, so it wants the user's vote. | `abb8494e` + `d66a5322` | Docs only, kernel byte-unchanged. Every target verified PRESENT in the tree and every phantom verified at ZERO git history before substituting (vault's own suggested fill named `kernel/{addrspace,cow}.h`, which live under `kernel/include/thylacine/` -- the argument for checking over pasting). Structure verified intact: 20 insertions / 20 deletions (pure substitutions), line counts unchanged, the 99-row trigger table unchanged. Sweep tool kept at `scratchpad/pathsweep.py`. Left standing and correctly labelled: the documented negatives (`mm/vm.c`, `mm/wxe.c`, the dropped `specs/{futex,notes,pthread}.tla`), the RESERVED `specs/mandate.tla`, and `thylacine/vdso.h` (an include-form, not a path). |
| Warp-C **C-0d**: the #240 detector -- a refused stream is now observable in-guest | **`stream-rejected` on the ctx ctl, and the gate caught a vacuous detector on its first run.** Each warp ctx gains a `CtxProbe` (two 1x1 resources the client can never name: `mark`, uploaded once with `PROBE_MARK`, and `sentinel`); `verify` on the ctx ctl seeds a token into `sentinel`, submits ONE stateless `VIRGL_CCMD_RESOURCE_COPY_REGION` copying `mark`->`sentinel`, and reads back. Mark means the copy ran; the token means it did not, which latches a **sticky** `stream-rejected` + `rejected-at` + `verify-seq`. The `glGetGraphicsResetStatus` / `VK_ERROR_DEVICE_LOST` contract: recreate, never retry. **The submit path is untouched** -- zero per-submit cost, because the vrend latch is sticky so one probe after a window covers it. Cadence is the client's. **TWO SELF-CORRECTIONS BEFORE IT WORKED, both caught by controls rather than by argument.** (1) Scripture (`242e0d8d`) had specified a per-submit `CLEAR` stamp; implementation analysis showed a `CLEAR` needs `SET_FRAMEBUFFER_STATE`, and virgl context state PERSISTS across command buffers while Mesa dirty-tracks its own binds -- so the stamp would have silently repointed a client's framebuffer at our sentinel. Amended as scripture-before-code (`37e8acac`) to the stateless copy, and moved to verify-time only. (2) The first run came back **`C0-DETECT FAIL(vacuous)`: BOTH ctxs reported `stream-rejected 1`**, the healthy one included -- a detector that flags every context as dead, which without the class-matched healthy control would have shipped as a PASS. Root cause: `VIRGL_CCMD_RESOURCE_COPY_REGION` was coded as **96**, a LINE NUMBER out of a grep of `virgl_protocol.h`. The real ordinal is **17**, one past `BLIT`(16) -- and `BLIT = 16` was PROVEN last session and sits in `warp-prove` carrying a comment about this exact trap, so 96 should have been rejected on sight for being nowhere near its neighbour. Derive ordinals by counting the enum, never by grepping for the name. Post-fix the arms separate cleanly (rejected `got 0x24443140` = its own token; healthy `got 0x57415250` = `PROBE_MARK`) and re-verify holds `(1, 0)` -- sticky, with the healthy arm not drifting. Diagnostics were then trimmed: the healthy path is silent and skips the mark re-read entirely, since reading back the mark IS proof the mark held it. | `f2892a7a` | **`C0-DETECT PASS` on thyla-pi (KVM, real V3D)** -- both directions required and both observed: the refused ctx reports `stream-rejected 1` while the healthy ctx running the SAME verb reports `0`, plus a re-verify proving sticky. `verify-seq` is asserted to ADVANCE first, so a `0` reading can never be satisfied by a probe that never ran (#184). Local suite **1364/1364**, 0 FAIL. Warp-2 gate VERIFIED unchanged. Ramfs staging content-verified both sides (positive markers + a known-old control + a nonsense negative). Kernel byte-unchanged, so SMP/spec records carry. **Self-audit @`5d64db1e` closed 3 findings, all mine from this chunk**: [P2] `verify` was an UNRATED DoS lever -- three synchronous device round trips per write on the compositor's own dispatch thread, client-triggered, bypassing the fenced lane's `E_AGAIN` admission entirely; bounded to one probe per ctx per compositor tick, which is the per-frame cadence the design targets so the intended use pays nothing. [P2] the token could COLLIDE with `PROBE_MARK` at `verify_seq` 0x10730562 (~53 days at 60 Hz), on which one verify a LATCHED ctx would seed the mark's own value, read it back, and be reported HEALTHY -- a false negative on the exact question the probe answers. [P1-for-the-gate] the DoS fix then SILENTLY VOIDED the stickiness leg: the prover re-verified microseconds later, inside one 16 ms tick, so it read the first probe's cache and would have agreed with it regardless -- the leg now sleeps past a tick, REQUIRES `verify-seq` to advance, and prints `STICKY NOT TESTED` rather than implying a pass (it now reports `verify-seq bad 2 ok 2`, a second REAL probe). Withdrawn after checking rather than asserted: `verify` racing a ctx retire -- both `wctx` accessors already filter `c.retiring`. **STILL OWED: the formal focused audit (task #228). The round was spawned on the holotype reviewer and TERMINATED EARLY on an out-of-credits API error, producing ZERO findings -- C-0d is UNAUDITED, and that is an absent close, not a clean one.** The question most wanting a second pair of eyes: the submit stream is opaque and carries raw device-global ids, and the probe resources are attached to the CLIENT'S OWN `dev_ctx`, so whether a client can name them in its own `RESOURCE_COPY_REGION` is not settled by P1a (which proved only that vrend refuses cross-CONTEXT access). |
| Warp-C **C-0d audit close** (round 1, Opus fallback): 0 P0 + 2 P1 + 7 P2 + 3 P3, all 12 fixed | **The round refuted the author's own self-audit, and the gap was in the SYSTEMIC step.** I had prosecuted the forgery question myself and closed it: corrupting `mark` lands on "neither mark nor token -> UNKNOWN", which is *fails closed* -- correct locally, and I stopped there. **F1+F2 together**: UNKNOWN had **no representation on the ctl**, so a blinded probe read exactly like a healthy one and 149-warp.md told readers to interpret it that way. Failing closed into a state your interface cannot express is failing OPEN one level up -- trace a fail-closed to what the CONSUMER sees, not to what the function returns. **F1 was then MEASURED before being fixed** (the reviewer flagged its own key step as proof-by-scripture, virglrenderer being unreadable in-tree): a client derived `mark = its_first_res - 2` by arithmetic and copied over it, and the server read the mark back as **`0xFF00FF00` -- the client's own green**. So the probe is unreachable to the BO RESOLVERS but nameable by the SUBMIT STREAM; containment now rests on a per-verify **repaint**, not on naming. **F8: the gate did not gate** -- it grepped `C0-REJECT` while the detector prints `C0-DETECT`, so `warp-host.sh reject` would have exited 0 on `FAIL(vacuous)`, the precise outcome scripture names it as existing to catch; it only ever failed visibly because I was reading the output by eye. **F3 [P1] was the one with real blast radius**: the probe's backings were leak-and-FORGET on the wedge path while the doc claimed they shared the BOs' posture -- they shared NEITHER half (`wbo_retire` releases DEVICE refs unconditionally and defers only the guest backing, which a vindication reclaims). Every wedge stranded two kernel handles + two mappings permanently, in the process that IS the console, at one wedge per `FENCE_ABANDON_MS` against `PROC_HANDLE_MAX` 1024. Fixed with an `Option<CtxProbe>` graveyard per slot -- deliberately not a `Vec`, so it borrows none of the `leaked_count`/`MAX_WARP_BOS_PER_CTX` coupling that two prior rounds established makes `warp_park_leaked` infallible. Also: **F10** half the sticky leg was a TAUTOLOGY (nothing ever clears `stream_rejected`, so "did not clear" was unfalsifiable) -- `verify-ok` is the falsifiable twin; **F9** the C0 legs read fields through a reader that calls `fail()`, so a pre-C-0d tapestryd aborted the run instead of reaching the INSTRUMENT arm written for it; **F5** both UNKNOWN `say!`s were unconditional, ~480 console lines/s after F1; **F4** the probe build's third failure arm unwound nothing and was unreachable to the teardown. | `7b43c43c` + `98784256` | **Both gates VERIFIED on thyla-pi (KVM, real V3D)**, plus a SABOTAGE proving the new leg discriminates. New `prove_probe_reclaim` drives the only path that parks a probe (mint -> hold -> submit -> abandon -> DESTROY while wedged): clean `parked 0->1 freed 0->1 after 1 polls`; with the reclaim removed and the park kept -- F3 reproduced exactly -- `parked 0->1 freed 0->0 after 400 polls`, gate UNVERIFIED. **Two instrument lessons earned in the landing.** (1) **The harness TRUNCATES everything after the hard-fail token**: the first red run printed exactly `warp-prove: FAIL -- probe-r`, because LS-CI aborts the scenario the instant it matches, destroying the diagnosis carried on the same line. Every assertion now emits evidence as an ORDINARY line first and fails SHORT -- which is the only reason the sabotage numbers above are readable. (2) The first cut of the leg wrote `warp-hold off` after destroying its ctx; the verb requires the caller to own a LIVE ctx (#178, deliberately loud rather than a silent no-op), and the destroy ALREADY releases the hold at the `wctx_retire` chokepoint (round-8 F1) -- the lever was both impossible and unnecessary. Local suite **1364/1364**, 0 FAIL. Floor OK. Kernel byte-unchanged. **The round forfeited family independence** -- Fable quota was exhausted, so author and prosecutor were both Opus; re-prosecute on Fable when quota returns. |
| #240 MEASURED (Warp-C C-0 prerequisite): a host-refused submit reads as SUCCESS, and it is sticky | **The seam reports success for work the GPU host refused, and one refusal kills the context permanently.** Measured on thyla-pi (KVM, real V3D) by a new `warp-prove reject` leg + `tools/warp-host.sh reject`. Two ctxs built identically on separate connections, differing in exactly ONE variable -- whether vrend accepts the stream -- with the fence counters read BEFORE either submit so the delta is attributable: `pre-submit fence-signaled: bad 0 ok 0` then `t=0ms bad(poison 0 sig 1 inflight 0) ok(poison 0 sig 1 inflight 0)`. **The refused stream retires its fence at 0 ms exactly like the valid control**; `poisoned` never sets, `fences-in-flight` returns to 0. So a refusal is indistinguishable from SUCCESS -- strictly worse than the filed "indistinguishable from a hang", because a hang is at least visible as a stall. **And it is STICKY**: a later VALID stream on that ctx moves no pixels (`SENTINEL`) while the identical stream on a fresh ctx works (`GREEN`) -- vrend latches the context error and drops every later command, while every fence keeps reporting success and the `transfer_from` still retires with stale data, so even the readback path lies. **Blast radius is confined to the submitting ctx** (the second connection was unaffected throughout), so this is a robustness/observability defect, not a privilege escalation -- an unprivileged client can only self-DoS. **It blocks Warp-C**: the compositor composes by submitting a per-frame blit stream on its own ctx on behalf of clients, so one rejection freezes the screen permanently while reporting composed frames. **The correction is the lesson.** #240 was filed as "the fence never signals; a refusal looks like a hang" -- that came from a 200-iteration poll (a few hundred ms of 9P round trips) against tapestryd's `FENCE_ABANDON_MS` of 30 s. It described the PROBE'S BUDGET and was recorded as a property of the SEAM; the old log's missing reaper line proved nothing either, since the probe exited two lines after the rejection and the guest never lived 30 s past it. A bound on iterations is not a bound on time, and an absence observed inside too small a window is not evidence. The first re-probe was itself mis-controlled (a `transfer_from` control could not distinguish a lost submit-fence from a retired one, #212) -- the class-matched valid *submit* is what made the reading decisive. `GPU-DESIGN.md` 4.5.4's old claim is superseded in place by a new 4.5.4a rather than silently edited. | `567630af` | Warp-2 gate **VERIFIED** on thyla-pi KVM after the change (`WARP-PROVE PASS` + the scenario pass; the observation lane is deliberately OUTSIDE the battery, since it spends 45 s waiting out a 30 s timeout). New lane is report-only by construction -- it prints data, never a verdict token. Ramfs staging content-verified with a positive control (5 new markers), a known-old control string, and a nonsense negative, on BOTH the local bake and the Pi copy. Rider datum from the same run: **#219b is REFUTED** -- the churn refusal still fires at `attempt 1005 (cap 1024)`, i.e. via the dma-create handle wall, 19 short of the leaked-count cap, so the count cap remains unwitnessed. |
| #215 ANSWERED: the composed-present readback is 43% of the HW-GL frame (+ #232 diagnosis, #233 passphrase masking) | **Where the hardware-GL frame actually goes, priced two independent ways that agree to ~2%.** At 1280x800 the composed present path's per-frame framebuffer readback (`transfer_from_3d_sync`, host -> guest, ~4 MB/frame) costs **16.8 ms of a 39.3 ms frame -- 43%**; removing it is a measured **1.75x (25.4 -> 44.4 fps)**. Two methods, derived separately, agree: a resolution regression's fixed term (22.24 ms) vs the direct-path frame (22.50 ms), and the regression's pixel term (17.18 ms) vs the composed-minus-direct delta (16.82 ms). **The "hw-gl is resolution-invariant" lead is REFUTED** -- hw-gl scales 1.67x against sw's 1.51x over the same 13.33x pixel step, so the apparent invariance was the readback's own pixel-proportional cost, not fill or shading. **The pacer is exonerated** by a 13-leg within-boot paired A/B (every paced/unpaced pair within +-1.8%, drift -1.7%, cross-boot reproducibility <=1.2%) -- so `THYLACINE_PaceFrame` was never the missing 43%. **Direct-scanout eligibility is narrow** (`server.rs::reconcile` ~1536: one visible surface, one leaf, full display), which reframes the 1.75x as *the cost of windowing a GL client* -- the lever is a GPU-side composite, now chartered as #237. Two self-corrections en route, both closed by measurement rather than argument: "~22 ms fixed cost, therefore not the GL submit path" was stated while an unmeasured frame pacer still sat in the path; and "the composed path is a stall, not work" came from raw %CPU, which **reversed** once normalized per frame (+13.7 ms CPU/frame => 81% real burn). Instrument work: quarry gains per-leg resolution (`hw-gl@1280x800`) with a `+vid_describecurrentmode` **mode witness** that reports MISMATCH rather than trusting the flag, and `:paced` opt-in (bench legs now default UNPACED). **#232 is NOT a wedge** -- leg-lifecycle instrumentation showed tyrquake was discarding its `-condebug` open/write results and reopening per line; the fd is now held open and the path pinned. **#233**: login masked the passphrase AFTER emitting the prompt, so a byte racing the flip was both echoed and then discarded -- masking now precedes the prompt. The `kernel/cons.c` half is **comment-only** (12+/3- inside one `//` block, code byte-identical): the comment had asserted the *opposite* of the truth ("no current consumer flips mid-line") while login did exactly that, and the claim held only because the window was narrow, not because it was shut. | `437f50e8` + `424f3070` + `c90d7b0c` + `eeabb50c` + `0135922c` + `23599bbe` + `1063da4a` + `abd47935` + `8765875b` + `68ad19be` | **Full bar GREEN**: suite **1364/1364** (0 extinction) + `check-v80-floor.py --all` OK (209 aarch64 ELFs, 14753 LSE instrs, all runtime-gated; the guard's own selftest still discriminates) + **SMP gate 40/40** across all four configs (default/UBSan x smp4/smp8, N=10) with 0 corruption / 0 external-kill / 0 inject-miss / 0 timing + LS-CI **35/35 PASS, 0 SKIP** (wall 1596 s at JOBS=3; `pty-4` held at 502 s despite #162) + specs **33/34 clean via `make specs`, plus `sched_oncpu`'s 7 cfgs run by hand** (4 clean pass at 66/124/86/152 distinct; the 3 counterexamples -- `intree_guard` Availability, `prefix860`(+3cpu) Safety -- all still fire). Kernel delta is comment-only, so the SMP/spec records carry by proof as well as by run. Two riders filed from the bar itself: **#238** (native Go `os.Executable()` fails -- devproc has per-pid `exe` but no `self` alias, while the diorama defines one; fails closed, `go build` unaffected) and **#239** (`make specs` has NEVER run `sched_oncpu.tla` -- it selects cfgs by the `${module}.cfg` convention and that module's seven cfgs are all suffixed, so it prints `(no default cfg; skipped)` and still exits 0; the only such module in the tree, and it covers the SMP `on_cpu` protocol, a re-enabled spec-first surface). |
| #198 CLOSED: the four-table fid ceiling chain + the fid-lift audit + quarry | **A 75x end-to-end win (0.6 -> 44.7 fps) from four nested static tables, and the method that found the invisible one.** A GL client holds ONE 9P fid per live BO mapping (the map fid's clunk is what drops the mapping, I-7), so a Quake-class texture set walks into four ceilings in series: tapestryd's per-conn `MAX_FIDS` (32 -> 512, `afa90839`), the kernel's `P9_SESSION_MAX_FIDS` (256 -> 1024) and `PROC_HANDLE_MAX` (256 -> 1024) on BOTH glq and tapestryd (`61566af1`, user-approved "remove all toy limits"). The A/B ladder is the evidence: stock 2323 OOMs @0.6 fps -> handles-only **byte-identical** (exoneration) -> tapestryd-512 alone 1059 OOMs (the binder MOVED, not fixed) -> all four **0 OOMs / 0 storm / 44.7 fps** gate lane. **The durable lesson: the kernel's session-fid refusal was invisible to BOTH instrumented endpoints** -- the client saw a generic failure, the server never saw a T-message, and three rounds of theorizing at either end all died because the refusal sat in the layer BETWEEN them. Enumerate and instrument every layer a request crosses before theorizing at either end. **Fid-lift focused audit (holotype Fable 5, MODEL start==end): 0 P0 / 0 P1 / 1 P2 / 5 P3** -- F1 [P2] `LOOM_SETUP_SQPOLL` spawned a 32-KiB-kstack kthread under the EXEMPT kproc, uncounted and bounded only by the handle table the lift just quadrupled (32 MiB of unswappable kstack from an unprivileged Proc); FIXED (`d38db92f`) by charging the creator via a separate `Proc.loom_sqpoll_count` (separate so exit logic never waits on a phantom thread), charge-before-spawn, settled exactly once under `Loom.sqpoll_charged`. **The generalizable half: a constant that appears as an argument in someone else's safety proof cannot be lifted without re-running that proof** -- the original F4 disposition explicitly justified leaving SQPOLL uncounted BY the 64-slot table. The stale-constant sweep found the same class twice more: `docs/reference/72-poll.md` named `PROC_HANDLE_MAX` as poll's bound when the real one is `POLL_MAX_NFDS` (harmless while both were 64; had poll actually been sized off the table the frame would now be ~56 KiB against a 32-KiB kstack), and `LOOM_MAX_REG_HANDLES` was justified as "Matches PROC_HANDLE_MAX" -- a rationale the lift silently voided. Bench post-fix: paced 29.9 / unpaced 30.3 fps @1280x800 swap-clean, pacer exonerated, ~25% of native-V3D -> #215 residual re-scoped to ~4x per-submit serialization. **quarry** (`aad3feb5`+`62db8a79`, user-requested): the native Kaua TUI + CLI renderer launcher/bench (sw / llvmpipe / hw-gl live; lavapipe + hw-vk seamed for Warp-6), `/env`-driven driver selection with restore, hang-killed bench legs. En route: `tools/warp-host.sh`'s pool transfer rebuilt as **independently-retryable 64-MiB chunks with content verification** after three consecutive tunnel drops at 600-870 MB proved the single 5-G stream deterministically unsurvivable (a retry budget buys nothing when nothing varies between attempts); the old path also *destroyed* the previous good copy on failure, since `dd of=` truncates at open. | `afa90839` + `61566af1` + `aad3feb5` + `e552241e` + `d38db92f` + `62db8a79` + `5207982c` + `bc8562d8` | **Full bar GREEN**: suite 1364/1364 (0 extinction) + `check-v80-floor.py` OK + **SMP gate 40/40 across all four configs (default/UBSan x smp4/smp8, N=10) with 0 corruption / 0 external-kill** + LS-CI **35/35** (wall 1571 s at JOBS=3; the quarry scenario is new and standing) + specs byte-unchanged vs the pushed base (by-proof). Regression `loom.sqpoll_charges_thread_budget` revert-probed (pre-fix fails at exactly "kthread charged to the creator", 1363/1364, nothing else moves). **On thyla-pi (KVM, real V3D), quarry's in-guest matrix measured demo1 at 640x480: sw 42.5 fps / llvmpipe 21.0 / hw-gl 35.7** -- hardware GL BELOW the software rasterizer and only 1.70x software GL, corroborating the 1.86x at 1280x800 and so the ~4x per-submit residual (#215/#208); hw-gl runs LAST in table order, so #168's -23% within-boot degradation is an unbounded confound on that figure -- a reversed-order re-run is owed before 35.7 is treated as the datum. The run then WEDGED after the last leg (all console output ceased, guest alive, table never printed) -> **task #232**, undiagnosed between "quarry blocked in kill()" and "the console is wedged"; quarry's bench must not become a routine gate until that is settled. |
| #218 server half (the #198 fix-bundle step 1; audited CLEAN) | **A refused create3d CONSUMES the mint record.** A failed create3d left its minted-but-unbuilt corpse in `bos[]` for the ctx's life; ~1000 per-texture refusals exhausted the 1024-slot table, starved `wbo_mint`, and turned a recoverable per-texture failure into total BO death (the #198 cascade's second stage). Unmint hooked at the ctl arm (ONE chokepoint — the parse/OPNOTSUPP families that never reach `wbo_create` consume too); `wbo_unmint_refused` bounded owner-conn (I-45) + UNBUILT (`dma_fd<0`; an unbuilt record can never hold a share_id — `wbo_weft_ensure` refuses it) + non-retiring (the pump's); the unreachable `!built` arm now unwinds its device state. En route: warp-prove.exp's `set timeout 120` was DEAD (lc_expect stomps from `lc_cmd_timeout`; the battery ran on the 30 s default) → a real 180 s floor with caller passthrough; found+enqueued task #214 — the poisoned-churn R6-F1 discriminator fires via dma-create handle pressure (~237), NOT the count-cap it was built to prove. **Focused audit (holotype, MODEL(end) Fable 5; start-line lost to two transient API drops mid-round, resumed twice): 0 P0 / 0 P1 / 0 P2 / 2 P3 CLEAN, not dirty** — F1 (the wbo_create contract note doc-attached to wbo_diag_once) + F2 (a red battery waits out the full 180 s; new `lc_run_expect_hardfail` arm on the prover's own FAIL line) both fixed at the close. Reviewer verified the unbuilt-implies-nothing chain by full writer census, unmint-vs-pump/consent disjointness, extraction identity, and leg non-vacuity in both failure directions; weave-VA-burn adjacency folded into task #171. | `12ff3058` + `1f348e2e` | default suite PASS (0 extinction); **warp-prove 9/9 on thyla-pi KVM, A/B-proven** (pre-fix fails the new corpse-reclaim leg at exactly `size-align attempt 1025 (cap 1024)`; post-fix `2050 refused create3ds left the mint alive` + the built-bo repeat-refusal guard); ls-gfx + ls-gfx-panes + ls-gfx-mode PASS local HVF (ls-gfx re-run green on the close build); kernel byte-unchanged (standing SMP 40/40 records carry); closed list `memory/audit_218_closed_list.md` |
| #204 submit pipelining + BO-cap lift + census (audited CLEAN) + the #198 attribution arc | **The #215 fix's mechanism half, landed + audited; the measurement half root-caused to a guest-side cascade and fully tracked.** Depth: `FENCED_SLOTS` 4→16 (per-ctx share 2→**8**), controlq 16→**64** (its QEMU max; 2-page ring repacked, cursorq split at its own max 16), the Mesa winsys throttle now DISCOVERED (ctl `fence-lane`, clamp [1,64]) with the #210 conservation clamp (`signaled>issued` → depth 0 + one-shot warn, never a wrapped-u64 latch) — fork patch 0008, series re-verified onto tree `88ade8b2`. Cap: `MAX_WARP_BOS_PER_CTX` 128→**1024** (#213) on HEAP rows (mint-time `try_reserve` preserves round-6 F1's no-record-dropped). Census (permanent): `bo-live/bo-peak/bo-bytes/bo-bytes-peak` per-ctx + global, `warp-prove ctl` dump mode, the glq lane reports it even on fps-timeout. En route: the widened flane overshot the kernel's 1 MiB plain-DMA cap by ONE page (`FREQ_LEN` 64→36 KiB; caught ONLY by the GL-host capset gate — a 2D boot never allocates the lane; two compile asserts pin both bounds); build.sh's sdl2/tyrquake reuse gates now watch the fetched archive (the #139 class — it bit this round). **Focused audit (Fable-5 max, MODEL start==end): 0 P0 / 0 P1 / 0 P2 / 3 P3 CLEAN** — F2 fixed in-tree (assert derived from `SRV_MSIZE`), F3-doc fixed, F1+F3-patch → task #209. **The measurement: BLOCKED, root-caused, tracked.** The census exonerated BOTH server caps (bo-peak **26** / bo-bytes-peak **10.4 MiB**, deterministic ×3) and the #218 server-side one-shot diagnostic never fired — the ~1200 per-run create failures (mipmapped textures exactly) die GUEST-SIDE before the seam; each failure leaves a minted corpse (#218) that eventually exhausts mint slots → Mesa's "OOM in VBO allocation" → the vbo/BeginEnd dispatch WEDGES (`context.c` nop_handler — "(invalid call)" ×917k) → the storm gates fps through the synchronous console (~2 ms/line). #216 (S3TC advertisement, 1692 phantom OOMs) decoupled. The fix bundle = ONE builder round (client-side one-shot instrument + cache flush-and-retry + #218 corpse destroy + #216 + #209), then the §13 bar re-measure. Also: #217/#226/#227 (aux's cross-tree sweep) — both gate scripts now REFUSE over a live tree VM instead of pattern-killing it, and the idle gate's log name is no longer a BSD-mktemp LITERAL shared by both trees (trailing-X fix + CHECKED allocation, so a failed mktemp can never again masquerade as "qemu exited before boot OK"). | `6bf98c64`+`9b04d358`+`2feb4195`+`19c5029e`+`5d872c04`+`c5deccdd`+`04f20ca2`+`67b2f48d`+`7491cbf0`+`c1d0dfab`+`b09aa139`+`ee24cc9f` | suite PASS ×2 (fresh paired fixture, 0 extinction); thyla-gl capset/tri/prove/wedge-gate ALL VERIFIED; Pi capset/tri/bench/wedge-gate ALL VERIFIED (KVM, real V3D, depth 8); audit closed list `memory/audit_204_closed_list.md`; census witnesses in `build/warp-quake.log` history + the task queue; **full pre-push bar GREEN @`ee24cc9f`** (build + floor + suite + LS-CI 34/34 [wall 1555 s, JOBS=3] + SMP gate 4-config ×N=10 = 40/40 0-corruption/0-external-kill; specs byte-unchanged since the pushed base, so the last green sweep covers them) |
| Warp-5 close + HW-GL exit bar + #215 answered | **The Warp arc's focused audit (CLEAN) + a measured exit criterion + the #215 verdict.** Warp-5: the holotype prosecutor (Fable 5, full lineage independence) ran the whole GPU seam at `43d26b5d` -- **0 P0 / 0 P1 / 0 P2 / 3 P3**, the seam's ninth round and its most mature. F1 (an unprivileged ctx could soft-Off a stranger's scanout via `present-to` because a warp ctx and its tapestry surface live on different conns) FIXED in-place (gate the retarget on `surf.gl_src == self`); F2->#171 (BO-VA never reclaimed, sharpened), F3->#166 (an unplaceable optional BAR bricks the whole GPU claim on the Pi/KVM 64-bit-window substrate). The #210 dense-count fix + adoption/eviction spine + #213 mass-failure path all verified sound. **HW-GL exit bar** (GPU-DESIGN §13, ratified): a four-point grid (native/guest x SW/HW) on thyla-pi, guest HW/SW >= 0.5x native HW/SW, Direct arm, thermal-guarded. **#215 answered**: a surfaceless-EGL+FBO native microbench measured native V3D at **11.0x** llvmpipe (silicon EXONERATED) while the guest delivers 0.037x -- a ~300x per-draw collapse localizing the inversion to **per-submit serialization** in the virt path, not the GPU. En route: an EBADTAG fixture-pairing incident (reverse-synced pool + locally-rebuilt ramfs = key mismatch) diagnosed and the recovery guidance corrected. | `99eacb4d` + `4edea773` + `71d06805` + `44d0deac` | full suite PASS on a fresh paired fixture (boot OK, 0 extinction, stratumd clean pre+post-pivot); native-bench MEASURED (11.0x, reproduced 11.13x, thermal 0x0); audit closed list `memory/audit_warp5_closed_list.md` |
| Pi-GL ladder results (quake + decomp) | **The ladder's top rungs on thyla-pi/KVM, and the open finding they surfaced.** WARP-4 (quake) VERIFIED structurally -- virgl V3D renderer, both present arms, demo completion, eviction leg, LS-CI green -- BUT the timedemo ran 969 frames / 1757.6 s = **0.6 fps**, a ~50x INVERSION vs the same host's in-guest llvmpipe (bench: paced 29.9 / unpaced 30.2 + 23.3 fps; decomp 2d control: composed 16.2 / direct 16.1 fps at ~300% CPU, zero swap) and 4x slower than TCG-thyla-gl's virgl arm. The run carried ~975k GL_INVALID_OPERATION lines MID-demo (#198 ESCALATED -- was demo-end-only; llvmpipe idles clean) + 1,889 GL_OUT_OF_MEMORY (#213 REPRODUCED device-independently) + `unallocated buffer ... Draw skipped` (the #213 cascade reaching the draw path). Tracked as **#215 (task #208)**; candidates (none confirmed): serial spam as pacer / #213 incomplete-resource draws / V3D S3TC absence. decomp gl DELIBERATELY not re-run: its composed demo outran the 1800 s in-script fps-wait at storm speed (a budget miss on a progressing demo, not a wedge), and any attribution measured under the storm separates nothing -- the decisive re-measure rides task #204 (the #213 Mesa rebuild). Stale-text rider in #208: glq-virgl.exp's embedded "llvmpipe band 2.4-5.9" comparator is thyla-gl-era. | *(docs row; results of the 2cb88c19 tooling on the certified 34ab047e artifact pair)* | WARP-4 GATE VERIFIED + GLQ-VIRGL PASS + LS-CI PASS (glq-virgl); GLQ-DECOMP PASS 2d (both arms, swap-clean); decomp gl UNVERIFIED by in-script budget (disposition recorded in task #208); the full pre-push bar on the tools delta was RUNNING at row time |
| Pi-GL KVM enablement | **The Pi GL ladder's substrate rungs: thyla-pi (Pi 400, BCM2711, V3D 4.2, QEMU 10.0.11) rides KVM end-to-end.** First rungs surfaced that every warp leg pinned or fell back to TCG on a KVM-capable Linux host: `detect_accel` only knew HVF-on-Darwin (the `kvm` case arm was reachable only via an explicit `THYLACINE_ACCEL=kvm`) and the six warp `.exp` scripts hardcoded tcg. Now: `detect_accel` probes `Linux aarch64` + rw `/dev/kvm` + the qemu build listing kvm (arch match matters -- `-accel help` lists COMPILED accels, so an x86 nested-virt host would pass a laxer probe and fail at qemu init); the `.exp` guards honor a caller-set accel; `warp-host.sh` passes `WARP_ACCEL` / `WARP_BOOT_TIMEOUT` / `WARP_BOOT_POLLS` through. Effect: the boot gauntlet that never reached the banner in 1800 s under TCG completes in ~210 s under KVM (polls=42, twice, deterministic). En-route incident: a flagless `build.sh kernel` in a pre-commit chain clobbered the local pool + ramfs fixtures (the bake-trap M-PIN, chain form -- flags were on the SECOND command); recovered bit-exact by reverse-syncing the certified artifacts from the Pi, md5-verified both sides. | `2cb88c19` | gl-host-probe rung 6 PASS (+ Vulkan V3D 4.2.14 present for Warp-6); BOOT-smoke PASS ~210 s; CAPSET GATE VERIFIED (GET_CAPSET id=2 v2 -> 1384 bytes, virgl=1 capsets=2); WARP-3 GATE VERIFIED with `GL_RENDERER = virgl (V3D 4.2.14.0)` -- the first Thylacine GL frames on real GPU silicon; WARP-BENCH PASS (Pi llvmpipe baseline: paced 29.9 / unpaced 30.2 + 23.3 fps, zero swap; the 23.3 dip echoes the #168 shape); Mac path regression-checked (full suite on the certified set, accel=hvf auto-detected, pass path exit 0) |
| #210 close | **The second-GL-launch fence deadlock -- root-caused by a five-run instrument chain that first EXONERATED the kernel, then convicted a number-space mismatch two layers up.** The v6 verdict ("a 9P reply frame lost IN THE KERNEL") was refuted by measurement: run 2's /ctl/9p-sessions counters showed every kernel layer conserving exactly (rings drained both directions on every conn; sframes == rx+1, the +1 being the serial Rversion; demux own+orphc == rx, wake == own, orph=0) -- the two parked ops' replies NEVER ENTERED the kernel. Run 3 (T-kinds + the tapestryd custody mirror) proved both parked ops are Treads and tapestryd HOLDS the fence read (fparked=1, parsed clean, inbuf=0). Run 4's fenced-funnel ledger was the conviction: `wctx 2 rx 2 mint 2` with `signaled 911` -- the wedged ctx issued exactly TWO fenced ops, and 911 is the DEVICE-GLOBAL fence id the boot's FIRST (paced) leg had advanced. The winsys counts fenced ops it ISSUED and compares against `fence-signaled`; for any SECOND GL ctx of a boot the u64 throttle `issued - signaled` WRAPS to ~2^64, latches permanently, the next submit never sends, and the client parks on the fence file for a record only its own blocked submission could produce. THE CONFOUND: "unpaced wedges 5/5" was leg ORDER, not pacing -- the probe always ran paced first, so unpaced was always the second ctx (Boot A's original wedge was likewise its boot's second launch); the llvmpipe/2D controls never fence and were structurally blind. FIX (server.rs warp_service_fences): fence_signaled/fence_reported become DENSE PER-CTX completion counts (FIFO ring, 1:1 with the in-flight decrement; record content unused by the client -- the read is a doorbell). Run 5: BOTH legs progress (the wedge had reproduced 7/7 pre-fix). Also landed en route: the ownerless-frame taxonomy's THIRD legitimate twin (orphl -- abandoned ops' late original replies classified via outstanding[].awaiting_flush; the #214-F1 conflation lesson applied three times until `orph` reads zero across the full suite INCLUDING death flows), the T-kind/fid in-flight snapshot, the tapestryd custody mirror + fenced-funnel ledger, probe v7, and the 149-warp fence-space rewrite. Client-side hardening (signed throttle guard) rides the #213 Mesa rebuild. | `1b6122ca` | wedge run 5: PACED PROGRESSES + UNPACED PROGRESSES + WEDGE-PROBE PASS + LS-CI PASS (first both-legs-green boot ever); local suite 1453/0 + boot OK on the shipped pair; zero ownerless log lines; holotype audit CLOSED CLEAN `8b2278b3` (0/0/2/4, ALL fixed: F1 the vindication fence-count deficit -- counted before the sibling-poison gate, the drain would lose it; F2 the wedge-gate regression net -- WARP_WEDGE_EXPECT=progress + the wedge-gate arm, green side verified live, red side witnessed by 7 pre-fix transcripts; F3 :kind:fid documented + the live-session formatter test; F4 comment sweep; F5 s2c_frames atomics; F6 empty-label guard under the registry lock); 9p_client's five buggy cfgs re-verified detecting |
| #210 instrument | **`/ctl/9p-sessions` -- the three-way reply-loss discriminator, landed ahead of the fix it exists to identify.** The v6 ledger left exactly one conclusion (tapestryd delivered record 911, the client stayed parked: a reply frame lost IN THE KERNEL) and the focused read found every layer disciplined -- so the next move was instrumentation, not theory. Per-`srvconn_chan` lifetime `produced`/`consumed` byte counters (mutated ONLY in `chan_ring_write`/`chan_ring_read`, every caller lock-held; `produced == consumed + count` is the conservation law) + `SrvConn.s2c_frames` (whole server reply buffers, role-held increment) + a create-linked/unref-unlinked conn registry; per-`p9_client` `frames_rx`/`demux_owned`/`demux_wakes` + the ownerless taxonomy, where the healthy-boot baselines re-taught #214-F1 TWICE: "ownerless" folds ONE pathology in with THREE by-design flows, each split to its own counter across three suite runs (59 async-clunk Rclunks `orphc` -- fire-and-forget never registers `inflight[tag]`; 7 #845-abandon Rflushes `orphf`; 3 abandoned-op late original replies `orphl`, classified via the session table's `active+awaiting_flush` under `c->lock`) until the residual `demux_orphan` reads ZERO across the full suite INCLUDING death flows -- a nonzero value is now a frame NO living mechanism accounts for (first 4 per client also logged). Sessions register via the sole production funnel (`p9_attached`; loopback test clients invisible); `/srv` sessions relabel with the conn peer pid. Wedge read: (a) s2c produced>consumed = reply undrained (election wedge); (b) orph bumped = misdemux/tag; (c) own advanced + inflight tag `d`one with the game parked = lost wake (I-9). Probe v7 reads the file at baseline + healthy-mid-flight + wedge time. Also: the stale `spoor_write_common` comment (claimed the non-blocking devsrv sends; blocking since #348/CF-3 B) + the dead `chan_set_eof` deleted (teardown latches both directions inline). `DEVCTL_READ_BUF` 2048->4096. Audit posture: extends the 9P-client + srvconn surfaces; the focused round runs at the #210 close with the fix. | `f85d6caf` | suite 1453 PASS / 0 FAIL / boot OK; ZERO ownerless log lines on the healthy boot (the taxonomy's witness); 3 new tests (`srvconn.ctl_counters` conservation+frames+unlink; `p9_attached.ctl_registry` label/relabel/unlink + Rattach demux witness; `devctl.read_9p_sessions_format` end-to-end via the Dev vtable, negative arm discrimination-proven by the positive arm first); wedge run on thyla-gl IN FLIGHT |
| #214 SMP bring-up (Pi) | **The first real-silicon `-smp 4` boot -- root-caused from a frozen wedge to `Thylacine boot OK` at 4/4 CPUs on the Pi 400/KVM, through four stacked defects no emulated substrate could see.** ROOT: `secondary_entry` never zeroed TPIDR_EL1 (architecturally UNKNOWN at PSCI entry; `_real_start` step 4.5 zeroes it for the boot CPU -- P2-A R4 F44/F45 -- and the trampoline never inherited the fix). TCG/HVF reset it to 0, so `current_thread()`'s NULL guard held by accident; KVM poisons UNKNOWN-reset sysregs with `0x1de7ec7edbadc0de`, so the secondary's first `current_thread()` deref (`spin_preempt_inc`'s `t->magic` probe via `thread_init_per_cpu_idle`'s first allocation lock) read the poison and died -- and the fault path re-derefed it (cons locks), recursing. AMPLIFIER: the recursive EL1h-sync entry marched SP down through the directmap landing a 288-byte frame per iteration, scribbling RAM until the L1 page tables held 'THRD'-magic exception frames and every CPU's vector fetch died at a level-1 translation fault (autopsied live: QMP physical `xp` + gdbstub `-s`; the corpse read like a post-apocalypse until the guards preserved it). D1: the bring-up timeout rode `g_ticks` (IRQ-fed, dead in context, and NEVER once fired on any substrate) -- a dead secondary froze boot forever, silently. FIXES: the trampoline zeroes TPIDR_EL1/TPIDR_EL0/TPIDRRO_EL0 (TPIDRRO also at `_real_start`: never kernel-written, always EL0-readable); CNTVCT-based `SMP_BRINGUP_TIMEOUT_MS` + `smp_init` EXTINCTS on partial bring-up (refuse degraded boot); per-CPU EL1h-sync depth guard (depth 3 -> cons-ring flush + raw banner + park, corpse intact) + extinction re-entrancy guard; the `g_cpu_online`/`g_cpu_stage` line-isolated CIVAC mailbox protocol + the per-CPU stage trace (localized the death window in ONE boot); VBAR installed in the trampoline pre-C (faults were silent through a reset-UNKNOWN VBAR); `ic iallu` beside F120's tlbi in `mmu_program_this_cpu` + hoisted `asid_hw_bits` + the leaf-ness constraint (disasm-verified: the fn spans the MMU flip, a frame would push-Device/pop-cacheable-stale) + the false dc-cvau/PoC comment rewritten (the WBWA-IS walker is coherent; no table clean exists or is needed). 17-smp-bringup.md gains the #214 section; caveat 4's prophecy ("Pi bring-up revisits") closed. Pi timings at smp4: go4c cold 7.7s / bisect-compile 2.6s (faster than smp1) / warm 666ms. | `0847f2f7` | Pi smp4 KVM: 4/4 online + full suite in-guest + boot OK + login (`~/warp/smp4-fix4.log`); Pi smp1 unchanged-green; local test.sh PASS + check-v80-floor OK; SMP gate subset (default-smp4 + ubsan-smp4) green pre-commit; holotype audit CLOSED CLEAN `2dfe4606` (0 P0 / 1 P1 / 0 P2 / 2 P3, ALL fixed: F1 depth-guard false-park under sleeping uaccess handlers -> reset-at-context-switch; F2 leaf-ness now a fail-closed post-link build gate; F3 timeout 100->1000 ms with margin; full bar re-verified post-fix incl. Pi smp4 boot OK) |
| #196 decomp | **The virgl throughput decomposition -- the "20-25x under" premise was a MISQUOTE, the composed arm has NO stall, and the real defect is a new wedge (#210).** The premise correction first (the proximity-beats-provenance M-PIN, caught before measuring): the 192.8 fps anchor is macOS/HVF and DOES NOT TRANSFER -- the tree's own Warp-1 row said so -- and the same-host llvmpipe band is 2.4-5.9 fps @640x480; corrected across the task + 7 living-doc sites incl. the glq-virgl.exp instrument (the qualifier now sits at the source record, 143-tyrquake.md). Then the instrument (NEW `tools/warp/glq-decomp.exp` + the `decomp gl\|2d` verb): one boot per device, two unpaced 1280x800 launches per boot (Composed-only, then early-zoom Direct-only), each demo window CPU-attributed host-side as `/proc/<pid>/stat` utime+stime interval deltas (Linux `ps %cpu` is a lifetime average -- it cannot attribute a demo window), riding rp6 (NEW: the FORWARDING /env unpace wrapper -- rp5's forwardless ^C ORPHANS the game, #197 one level up, which retroactively gives #168's demo-3 collapse a candidate mechanism: demo 3 ran against demo 2's orphaned full-speed spinner). THE MATRIX: virgl-Composed 2.4 fps @170%/400% guest CPU swap-clean; llvmpipe-Direct @1280x800 2.4 fps @359% (the resolution-matched control; its Composed twin swapped, self-discarded); virgl-Direct WEDGED (0 progress in 1800s at the 26% resident-idle floor) = **#210**. VERDICT: the Composed-GL arm matches the 4-thread software control's fps at 2.1x less CPU with 2.3 vCPUs idle -- the bound is TCG-amplified SERIALIZED single-threaded guest work (game-thread encode + the sync per-present server transfer+blit), which shrinks by the TCG factor on native silicon; no v1.0 fix owed on that arm. #210 discriminated by two controls: the identical unpaced leg on the 2D device runs clean (the wedge NEEDS the GL arm) and the wedge probe (NEW `tools/warp/glq-wedge-probe.exp` + the `wedge` verb: bg-job legs + the I-39 owner-axis autopsy) proves PACED early-zoom PROGRESSES -- UNPACED is the trigger (2/2). Probe v1's autopsy taught two instrument lessons (phantom pids from partial-ANSI free-text parsing; no healthy-state control for the mid-wedge `ls /proc` "I/O error" anomaly) -- v2 adds the brute 1..48 cmdline sweep + false-positive AND detection controls + the healthy-kstack diff base. Also landed: the #200 reap census close (aux's fix was ALREADY in main's history via the gfx-4 merge; 3-way discrimination vs the live aux cmdline; the one residue -- ci-idle-gate's boot-fail pkill under a relative THYLACINE_DISK_IMG -- canonicalize+re-export fixed, `9d4ed024`); #209 accepted from aux (VIVARIUM openat O_DIRECTORY + getdents64); 149-warp gains Performance characteristics (the matrix + the per-frame RPC census: fenced Twrite chunks <=24KiB at CCMD bounds, 2-in-flight throttle parking via fence-read + ctl-re-read, present = 1 RPC, Composed adds glFinish + the 4MB sync transfer; raw sampler files archived build/cpu-*.txt). | `5c6fb7f7` | decomp gl + 2d MEASURED (4 legs, 3 clean figures + 1 self-discarded); wedge probe v1 PASS (paced/unpaced discriminated); #210 filed + owned, autopsy v2 in flight; kernel byte-unchanged (tools + docs + one gate-script fix only) |
| Warp-4c | **The GLQuake-on-virgl gate MET on the mechanism, and the number it measured opens the arc's next item** -- `tools/warp-host.sh quake` (`tools/warp/glq-virgl.exp`) runs the BARE ramfs name through the launcher's new production auto-detect (the two-step raw `/srv/warp` attach probe -> `/env/GALLIUM_DRIVER=virpipe`; a one-shot pouch open CANNOT cross the srv post -- probed locally before it cost a remote cycle) and exercises BOTH present arms in one run: composed entry (letterboxed 1280x800 surface in the split), the default Super+F zoom chord over QMP -> `scanout direct 1 GL res 69 (1280x800)` LIVE on thyla-gl (the SET_SCANOUT(bo) switch), the demo's remainder on the Direct flush-only arm, and the ^C teardown leg (launcher forward -> SDL_QUIT -> surface retire -> eviction + tick-driven console restore). Five gate runs, each catching something real: R1 -> **#195** (egl-headless + virtio-gpu-gl = NO host-side QMP screendump, "no surface" for every scanout; pixel legs demoted to an ANNOUNCED best-effort; the direct-scanout orientation keeps only the gallium top-down ground truth as witness). R2 -> the direct switch fired + **#196** (969 frames at 2.5-3.4 fps across runs vs the 192.8 llvmpipe anchor; ~20-25x under after the 640x480-vs-1280x800 correction -- the arc's next soundness item) + **#198** (demo-end Mesa `GL_INVALID_OPERATION (invalid call)` spam; llvmpipe idles clean). R3 ground truth -> **#197**: execv is dead (pouch has no execve until L-6) so the launcher interposes via posix_spawn, and the serial console posts `interrupt` to the OWNER PROC alone -- ^C killed the wrapper and ORPHANED a fullscreen game holding the zoomed Direct scanout (the make-orphans-cc1 class; kernel pgrp parity owed). R4 (forwarding launcher, still red) + local repro + instrument -> **#199**: a CAUGHT note never interrupts a blocking syscall (the kernel wakes only the terminate latch), so the launcher's handler could never run while blocked in waitpid -- the POSIX EINTR parity gap; GNU make has the same shape today. Launcher shape that closes the chain (ctrlc-probe PASS, `fwd sig=2 rc=0` -> `reaped st=0x0`): SIGINT/SIGTERM forward + WNOHANG + 50ms usleep reap (the syscall cadence IS the note-delivery point). Battery grows the glsrc 2D reject legs (off idempotent / unknown ctx E_NOENT / junk E_INVAL). Verdict = the `GLQ-VIRGL PASS` + `LS-CI PASS: glq-virgl:` conjunction, 3-way discrimination-proven incl. the poisoned half-match. | `e39456fb` | Warp-4 gate VERIFIED (run 8, swap-clean: 3.0 fps, pswpin +2) + suite green with the final ramfs; #195-#199 filed + owned; GPU-DESIGN ladder row 4 + 149-warp caveats/Tests + the AUDIT-TRIGGERS Warp-4 addendum LANDED |
| Warp-4a+4b | **Present integration, both protocol halves: the mutual adoption** -- a GL client's frame reaches the display with ZERO guest transfers (fullscreen: the F16 pending switch binds `SET_SCANOUT(bo.res_id)`, each present is only a `RESOURCE_FLUSH`; ordering structural on the one controlq) or ONE server-side readback (windowed: sync `TRANSFER_FROM_HOST_3D` into the BO backing + the shared letterbox/crop blit -- sync so the present stays one dispatch, the I-40 premise). The handshake is MUTUAL NAMING through two owner-gated ctls (surface `glsrc <ctx>`; ctx `present-to <surface> <bo>`, gen-pinned so slot reuse cannot re-arm a stale consent) -- rejected: a minted-token capability (needs RNG + secret storage; mutual naming revokes naturally on either side's death). Activity resolved fresh per present (geometry equality; nothing cached). `Comp.bound_res` records the DEVICE truth apart from the mode-machine intent; every BO-death path (wbo_destroy + wctx_retire, the same chokepoints as the fence-hold release) withdraws consent + retargets via the uniform pending rule + **evicts the binding BEFORE resource_unref** (the one order the display cannot survive); surface retire runs the same eviction. HOLD refused in all GL arms (no deferral exists). Client half (fork `b7f9ed2` = durable patch 0007, round-trips tree `cd00196c` VERIFIED LIVE on the builder): `OSMesaThylacineDirect/Off` + winsys `present_to` + the target bridge (driver-name-gated; weak-ref so sw-only targets build); the SDL shim negotiates per-bind, downgrades glFinish->glFlush when direct, and any half-failure explicitly restores the readback (a half-negotiated state = a frozen pane). En route **#194**: EVERY guest GL link against the Warp-3 archive was broken (virgl pulled util/xmlconfig; meson's expat auto-resolved through the subprojects WRAP -- builder provers green, guest links red, hidden by build caching until the shim edit forced the first relink); fixed by `-Dxmlconfig=disabled -Dexpat=disabled` (port finding 5) + `idep_nir_headers` on the osmesa target (finding 6); proven in the artifact (`nm -u`: 0 XML refs; gl-sdl-prove relinks green, both Direct symbols extracted). | `ec2bd8ad` | suite 1360/1360 with the full diff; builder cycle ~9 min (am tree-verified -> reconfigure -> ninja -> nm-verify -> fetch -> STOPPED); the on-device gate (GLQuake-on-virgl vs the 192.8 fps anchor) is Warp-4c |
| Warp-3d | **The tri gate MET: a triangle in-guest on the HOST GPU** -- `WARP-3 GATE: VERIFIED`, `GL_RENDERER = virgl (virgl (Apple M2 (Compat)))`, rendered twice (runs 2+3). First contact aborted in `virgl_disk_cache_create`: `assert(note)` with HAVE_BUILD_ID on but the static ld.lld ET_EXEC carrying no `.note.gnu.build-id` (meson's detection passed while the artifact lacks the note -- the FOURTH builds-clean-when-wrong; #191). Fix = the graceful bail (`if (!note) return;` -- run cacheless, the exact NULL-disk_cache configuration `MESA_SHADER_CACHE_DISABLE` ships and every consumer NULL-handles), folded into 0006 (round-trips to tree `d302f50e`); rejected: the meson port-gate (forks the flag for every consumer) and `-Wl,--build-id` (would ACTIVATE the cache onto the 9P home's untested flock/mmap surface). Builder round-trip ~8 min (reset -> am -> ninja 11.5s -> fetch -> STOPPED). En route the #120 trap RE-FIRED: the first `PRESERVE=0` re-mint baked the OLD prover (`build.sh all` never re-stages /clade; the staged copy predated the fetch by 31 min) -- caught by mtime, proven by content (staged == strip(fetched) byte-for-byte), re-staged + re-minted; whether the #139 stale-stage warning fired is UNKNOWABLE because the first bake's log went through `\| tail -40` (the recorded tail-destroys-the-verdict trap, self-inflicted; bake 2 kept the full log and is warning-free). And the tri verb's own scenario anchor was PATH-shaped (`PASS: /clade/bin/virgl-prove`) -- a string only `lc_run_expect`'s TIMEOUT text contains, so the first triangle rendered while the verdict stayed UNVERIFIED (fail-closed on a wrong anchor -- the right failure direction). Re-anchored on `lc_pass`'s success-only prefix (`LS-CI PASS: virgl-prove:`); discrimination proven 3 ways against the captured success + failure logs (anchor 1 ALONE matches the failure log's quoted pattern -- the #186 shape -- which is why the conjunction is load-bearing). Also: the 3b/3c row glue in this table repaired (3b's label cell had been consumed). | `c64ddbe4` + `2e93078a` | **`WARP-3 GATE: VERIFIED`** through the production verb; in-gate boot suite green both runs (full [test] roll + `Thylacine boot OK` + CL-4/CL-7b gates PASS on the same fixture); 149-warp winsys section + GPU-DESIGN ladder row 3 LANDED |
| Warp-3c | **The builder cycle: the virgl+warp closure LINKS** -- patch 0006 amended with three builds-clean-when-wrong port findings (osmesa target: `inc_virtio` + `libgalliumvl_stub`; libvirgl: `<libsync.h>` re-pointed at Mesa's vendored self-contained copy after losing libdrm's, a hole in GPU-DESIGN 2.3's pattern-bounded census; `inline_sw_helper.h`'s virpipe arm guarded out so the dead vtest reference cannot reach the link). Amended 0006 round-trips to fork tree `3b7b4888` from a clean 0005 tip. Pre-existing find: the builder's cross-LLVM tree had LOST its 8 JIT/ORC archives post-CL-7b (~45 GiB object tree cleaned to 6.5 G) -- `osmesa-prove` was unrebuildable as it stood; the llvm-config shim refused fail-closed as designed, meson said "LLVM found: YES" over the empty `--libs`, and the archives + a SECOND `meson setup --reconfigure` restored the link. `virgl-prove` + rebuilt `osmesa-prove` + `libOSMesa.a` (218 MB, now carrying virgl) fetched; builder STOPPED (~45 min). Repo: sysroot step 8 installs `thyla/` headers at the wipe chokepoint (belt: cross-file `-I`; braces: the sysroot exelist path meson cannot filter); `warp-host.sh tri` + `virgl-prove.exp` = the gate leg. Pool re-minted ONCE (`PRESERVE=0`) to carry `/clade/bin/virgl-prove`. | `eb62f97c` | both provers LINK (the meson link proof); the tri gate run is 3d |
| Warp-3b | **The Mesa virgl winsys + warp client library (fork patch 0006)** -- the unmodified virgl gallium driver over a new `src/gallium/winsys/virgl/thylacine/` winsys: vtest's 18 load-bearing slots minus the displaytarget arms (the OSMesa frontend reads back through pipe transfers, verified at the source, so `flush_frontbuffer` stays NULL like drm). Transport is `warp_client.{c,h}` -- blocking file ops on the /srv/warp tree via `<thyla/syscall.h>`, carried into the Mesa build by the generated cross file's c_args (single glued `-I` token; meson's check path drops two-token pairs). Fences are COUNTED, not parsed: client issue counter vs the promoted `fence-signaled`; the fence file is only the blocking primitive and is never read with nothing in flight. `transfer_get` is synchronous (queue + fence wait -- the driver maps and reads on return, vtest-v2's own choice); submits split at CCMD boundaries into <= 24 KiB writes under the 32 KiB msize, with an oversized single command failing LOUDLY and latching (a dropped cmdbuf must never be silent -- `virgl_context.c` ignores `submit_cmd`'s return). Meson gates on `cc.get_define('__thylacine__')` since the cross file deliberately claims system=linux. `virgl-prove` is the gate binary: structural double discrimination (never walks CAP_JIT, so an llvmpipe fallback CANNOT pass; GL_RENDERER must name virgl). Emitted 0006 re-applies onto a clean 0005 tip reproducing tree `e2c39274a8` exactly. All four C files pass cross-clang `-fsyntax-only -Wall` against real Mesa headers -- and the first run's rc=0 was a lie twice over (zsh unsplit `$COMMON` + `$?` after a pipe), caught and re-run honestly, whereupon it found a real missing c11-config pair. | `8b8ca40d` | cross-syntax gate x4; builder compile+link + thyla-gl triangle = Warp-3c/d |
| Warp-3a | **Seam capacity + fence-counter promotion for the winsys** -- `MAX_WARP_BOS_PER_CTX` 16 -> 128 (st/mesa mints ~8 hw_res before the first draw; the round-6 F1 graveyard-cap argument is width-independent by construction: the cap IS the array size, one park per entry; the 64 MiB byte bound is the real authority and does not move). Per-ctx ctl `fences-in-flight` + `fence-signaled` promoted out of test-mode (a production client cannot ride a test-mode field; the monotonic counter is the only raceless retire observable -- the same #184 reasoning that introduced it). New always-on global ctl `bo-cap <n>`; warp-prove's R6-F1 churn discriminator now READS it instead of hardcoding 16 (a mirrored constant is a claimed sync with no check, #187). | `ef6af62c` | suite 1360/1360 local HVF; pool PRESERVED; device exercise rides the Warp-3 gate run |
| #188 | **The abandon-scoping leg did not discriminate (L5a/L5b split)** -- L5's bystander was UNHELD, so by the time the abandon ran its fence had long since retired and it owned no in-flight slot. `abandon_matching` walks in-flight SLOTS, so with none of the bystander's to walk a GLOBAL abandon -- the pre-#178 shape that hands any unprivileged client a box-wide wedge -- does the identical thing to it. Sabotage S5a reverted the scoping to global and the harness printed "abandon stayed inside the abandoning client": a green tick over an untested property, which is worse than no leg. Only the HOLDER can pin a fence in flight, and round-8 F1 makes the hold exclusive (L1 asserts displacement is refused), so the two roles cannot both hold; L5b therefore INVERTS L5a -- the victim holds, the abandoner runs unheld. That makes the `abandoned` counter the sharpest discriminator, since an unheld abandoner owns nothing: a scoped abandon can only be a no-op, a global one must consume the victim's slot. Three readings a global abandon breaks (delta==0, victim unpoisoned, victim's fence still in flight) plus a premise assertion that the victim's queue really took a slot, so L5b cannot go blind the way L5a did. | `69a38bc1` | thyla-gl virgl: L5b line + S5a re-run must now be CAUGHT |
| #187 | **Every read of a cons deferred flag needs the mgr hold -- in BOTH directions** -- a byte-identical kernel extincted `1358/1359` on ~1-in-8 SMP boots. Root-caused by READING, not theorising: there is exactly one setter and one production clearer of the ISIG flag, and the clearer runs on the console-manager thread *the setter wakes*, while the test asserts one statement later on a 4-CPU guest. The flag reading false means the interrupt was DELIVERED, not lost -- a test defect, but one that extincts the boot and so can redden any arbitrary run (it voided a #180 sabotage verdict). Nine sites fixed, and the two directions fail oppositely and silently: a POSITIVE read fails spuriously; a NEGATIVE read PASSES spuriously, because a wrongly-set flag is consumed before the read and the assertion is satisfied by the exact bug it names. Idiom is sample-then-assert (readings inside the window, release, then assert -- `TEST_ASSERT` returns, so an assert inside the window strands the hold, which `cons_test_release_owned_state` then charges to the NEXT test). Also deleted `sak_via_console_mgr`'s `sched_runnable_count()==1` assert: it sampled a transient and is strictly weaker than the deadline-bounded `TEST_YIELD_UNTIL` on the SAK transition that follows. **The race was already found and fixed once, at #129-audit F6 -- and that fix's own comment ends "which every sibling deferred-flag test wraps against and this one did not". The claim is false and inverted:** the hold was armed at two sites in the whole file, so the site F6 fixed was nearly the only one that HAD it. A claim about sites you did not enumerate is a guess wearing a finding's clothes. | `2cf06f15` | suite 1359/1359; SMP multi-boot (see posture); automated census leaves exactly 3 unheld reads, all three asserting the mgr DID consume |
| #185 | **The ctx-destroy half of round-8 F1 (leg L6)** -- #180's departing-holder leg closes the CONNECTION, a case the ROUND-7 placement of the hold release (on `warp_retire_conn`) already handled. Round-8 F1 moved it into `wctx_retire` for the case its own comment names: hold, submit, then `destroy` the ctx with the conn STILL OPEN, where the swallowed retire kept `fences_in_flight` nonzero so the pump could never finish the ctx it had just been told to retire. Nothing covered it -- warp-prove released the hold before its only destroy, so reverting the chokepoint move left every leg green. L6 drives it: C arms the hold, submits a fenced chain, asserts the chain really occupies a slot (anti-vacuous, sound here because C holds and pins it), destroys WITHOUT releasing, requires the slot back. Paired sabotages keep the two legs distinct -- S2 (delete the release from BOTH paths) fails the disconnect leg; S6 (move it back to `warp_retire_conn`, the round-7 shape) fails L6 ALONE. | `e2a8543a` | thyla-gl virgl: all four two-client lines + `WARP-PROVE PASS`; `WARP-2 GATE: VERIFIED` |
| #184 | **The retire EVENT, not the in-flight gauge** -- #180's held-lane leg (L4) polled `fences-in-flight` until it read 0. A gauge at 0 is satisfied by "B's fence retired" and by "B never queued a fence" alike, so a no-op regression in the submit path would have kept the leg green while the 3D lane was dead (the #177 shape: the drain beat the abandon and seventeen rounds ran against a healthy ctx reporting PASS). Found by self-audit BEFORE the harness had ever run. Sibling leg L2 does not have the hole -- its `free_held >= free_before` guard is sound only BECAUSE the holder pins the slot, which L4 cannot copy since B is deliberately unheld and so races the retire. Now asserts an INCREASE in the monotonic `fence-signaled`, which only advances in `warp_service_fences` -- a function a SWALLOWED retire never reaches. | `73b9e288` | thyla-gl virgl control run: PASS with the new assertion |
| G-3 | **Per-scenario accel + enforced TCG anchors** -- the mechanism already existed (14 `.exp` files set `THYLACINE_ACCEL hvf`), and that WAS the unmeasured bimodal cost split; the work was a coverage judgement, not a scheme. 26 of 34 scenarios now boot HVF. Eight are pinned TCG by `LS_CI_TCG_ANCHORS` and the wrapper REFUSES to run if an anchor's `.exp` forces hvf. All 15 steerable TCG scenarios PASSED under forced HVF and three were kept on TCG anyway: `idle-probe` (timer-shaped), `ls-7` (#70 is a TCG-ONLY livelock), `pty-4` (**#162 reproduces only under TCG gate load** -- flipping it would have buried a live open defect behind a green tick). Accel is not just a speed knob: it moves GICv3->v2, `-cpu max`->`host`, and the #166 64-bit BAR arena from live to inert. | `b9b2a490` | 34/34 PASS in 1786 s across four batches = **29.8 min**, vs the 4908 s serial baseline |
| G-2 | **Parallel interactive gate** -- `LS_CI_JOBS` (default 3); per-scenario slot dirs carrying their own pool/disk/qmp socket via `THYLACINE_POOL_IMG`/`THYLACINE_DISK_IMG`/`THYLACINE_QMP_SOCK`, `LS_CI_VNC_BASE` spreading the VNC probe, budgets scaled by job count. Verdicts are FILES, not counters, so a runner that dies without writing one counts as a FAILURE -- an absent result is not a pass. Three traps found by TESTING rather than reasoning: a forked scenario INHERITS the EXIT trap (bash resets signal traps in a subshell but still runs an inherited EXIT trap), so the first scenario to finish would have reaped every peer's VM -- #59's cross-tree shootout reproduced inside one tree; the concurrency instrument itself was racy and invented a phantom 4th worker; and `disk_restore` MINTS the shared pristine twin, so three workers would have torn the twin they clone from. | `6fded6e5` | full parallel gate 34/34 in 2925 s |
| G-1 | **Gate timings** -- per-scenario and per-attempt rows to `build/ls-ci-timings.tsv`, an awk summary sorted by cost, and a reconciliation line. The measurement that had to precede any policy: the baseline is **4908 s = 81.8 min**, and BIMODAL -- 20 tcg scenarios at 185-270 s are 85% of the wall, 14 hvf ones at 31-77 s are the rest. Mid-run I extrapolated "110-120 min" from the first five scenarios; the roster is ALPHABETICAL and the cheap tier sorts late, so a prefix of a sorted list is not a sample -- the exact error G-1 exists to prevent, made while arguing for measurement. The accel column reads what actually BOOTED (out of the artifact), not the wrapper's value, which was wrong for 14 of 34 rows. | `7f79f8f7` | baseline 34/34 PASS, 4908 s |
| Warp-2e | **The gate prover + the Warp-2 close** — `/warp-prove` (native libthyla-rs; connects to `/srv/warp` DIRECTLY so its conn IS its identity [F3] and conn death retires everything): mint ctx + BO → `create3d` a 64×64 B8G8R8A8 render target → map + SENTINEL-fill the 5 sample points (the readback assert must discriminate) → submit a hand-built VIRGL stream (CREATE_SUB_CTX + SET_SUB_CTX + CREATE_OBJECT(SURFACE) + SET_FRAMEBUFFER_STATE + CLEAR red; `virgl_protocol.h` encodings pinned with provenance) → `transfer_from` → ride the fence file (records coalesce; the PIXEL is the loop condition) → assert 0xffff0000 ×5 → destroy → assert `ctxs 0`. Exit 0/2(SKIP on 2D)/1. Driven by `tools/warp-host.sh prove` → `tools/warp/warp-prove.exp` (fail-closed: needs the prover's own PASS line AND the scenario pass). Docs: NEW `docs/reference/149-warp.md` (the as-built seam reference) + the 139-tapestryd fenced-lane pointer + the cumulative AUDIT-TRIGGERS Warp row (+ CLAUDE.md index) + GPU-DESIGN §12 row 2 annotated LANDED. | `0a5a7f6c` | **GPU-DESIGN §12 row 2 gate MET on thyla-gl: `WARP-2 GATE: VERIFIED`** — `WARP-PROVE PASS (ctx create/destroy + CCMD round-trip)` on the live virgl device (the fenced lane's first real contact: submit + readback + parked fence read all live); SKIP path proven on the local 2D boot; suite **1358/1358**; weft buggy cfgs ×4 + readiness ×1 re-VIOLATE; full bar at the close commit |
| Warp-2d | **The attributed-completion Controlq + the fenced lane** — `SUBMIT_3D` 0x0207 + `TRANSFER_TO/FROM_HOST_3D` 0x0205/6 + fence CQEs on the fence file (GPU-DESIGN §4.3: "completion is the used-buffer notification we already process"). gpu.rs: the single-in-flight desc0/1+`seq` engine becomes **used-ENTRY attribution** — `publish(head)` + `drain()` consume entries by id (slot 0 = the sync chain; fenced slot i owns fixed desc pair 2+2i/3+2i in a SECOND `FLANE_DMA` region [4×64 KiB req + resp page, mapped at `GPU_FLANE_VA` **only when virgl negotiates** — a 2D boot allocates nothing and the audited two-page sync ring is byte-identical]). PRESENTS stay wait-for-mine (`submit_and_wait` drains fenced completions while waiting for ITS entry — the stage-0 I-40 empty-2D-in-flight argument is untouched); G-5 hygiene carries verbatim (used-ring authority, ISR read-to-clear per wake, spin-poll for the store-propagation window, wall-clock deadline **only for sync chains** — a fence legitimately takes as long as its GL work, and in-order controlq processing keeps the sync deadline honest under a fence backlog). Fence ids are **globally monotone** (QEMU's virgl fence walk retires everything <= the signaled value — per-ctx sequences would cross-release); a fenced ERROR response still retires its fence (logged — a fence that can never signal is the wedge this avoids). A full lane (4 in flight) refuses `Again`/E_AGAIN, never blocks (#31/#125: the compositor IS the console). server.rs: ctx fence bookkeeping (`fences_in_flight`/`fence_signaled`/`fence_reported`), `warp_submit` (one Twrite = one atomic opaque submission; reply = QUEUED) + bo ctl `transfer_to`/`transfer_from` verbs; the fence file read returns the newest signaled id once and **parks** otherwise (`PendingFence` + `poll_fences`, the FK_EVENT netd leg — all four cancel sites mirrored: clunk/Tversion/teardown/Tflush; ctx death EOFs the stream). **Teardown drains first**: `warp_drain_ctx_fences` (2s bound, t_yield-paced — no irq.wait outside submit) before every retire; a deadlined drain **LEAKS the BO backing** (handle+mapping pinned) rather than freeing pages an undrained fence may still DMA — leak-on-wedge, never UAF, and no dead-latch from the drain (the #31 false-latch lesson). main.rs: per-pass `warp_service_fences` + the poll timeout clamps to 1 ms while fences are in flight (the GPU IRQ is not pollable). Self-audit surfaced **#170** (fence-at-death: proc-exit frees DMA pages a wedged in-flight fence may still write — the sync world never had the window; kernel-side PCI quiesce-at-release queued, W2e audit prosecutes). | `16d425cb` | suite **1358/1358** PASS, 0 EXTINCTION; boot: `gpu up` + `serving /srv/tapestry + /srv/warp` + `Warp PROBE OK` through the REWRITTEN engine (every console frame is a present through it); interactive `ls-gfx` PASS + `ls-gfx-play` PASS (sustained present pressure); fenced path exercises live at the W2e thyla-gl gate run |
| Warp-2c | **The `/dev/warp` tree in tapestryd** — the seam becomes a mounted namespace (GPU-DESIGN §4.1/§5: tapestryd hosts the GPU service; a SECOND 9P tree from the same Proc). tapestryd posts `/srv/warp` beside `/srv/tapestry` (two posts kernel-supported, both tombstone at its death; no warden change); joey mounts it at `/dev/warp` (soft-missing / fatal-unmountable, the tapestry contract) over a new devdev `DEV_KIND_WARP` stub, with a boot probe asserting the `virgl ` ctl line. server.rs: `Conn.root` (set by the accepting listener — one Conn type, disjoint qid spaces via `WARP_FLAG`=1<<42); the tree `ctl` (virgl/capsets/capset-id-ver-len/ctxs) + `caps` (the RETAINED capset blob — `get_capset` now keeps it) + `ctx/new` open-mint (ONE ctx per conn, the I-45 exposure bound; `CTX_CREATE` on the device, virgl-gated E_OPNOTSUPP on 2D) + ctx `ctl` (capset/rings recorded at the seam — F_CONTEXT_INIT unnegotiated, context_init=0; destroy) + `bo/new` open-mint + bo `ctl` (`create3d <10-arg tuple> <size>` → GPU-BO mint + map + `RESOURCE_CREATE_3D` + `CTX_ATTACH_RESOURCE` + `ATTACH_BACKING`, reverse-unwound on failure; destroy) + bo `map` (the second Tweft anchor, weft_ensure-lazy) + bo `info` (`res <id> size … stride 0`). Ctx/BO tree names are MONOTONIC public ids (the pane discipline — no gen machinery; stale fids resolve to nothing); the DEVICE ctx id is slot+1 (virglrenderer's id space is bounded; reused only after the synchronous CTX_DESTROY). Teardown (conn death/Tversion) retires ctxs+BOs in the R2-F5 order: weft_unshare FIRST, then DETACH_BACKING+CTX_DETACH+RESOURCE_UNREF (in-flight set empty — all commands sync at 2c), then unmap+close (client mapping survives via #847 until its own end/reaper). walk_child AND h_readdir both carry the arms (the walkable-but-invisible trap). gpu.rs: `write_ctrl_hdr_ctx` + CTX_CREATE/DESTROY/ATTACH/DETACH + RESOURCE_CREATE_3D (sync lane). `submit`/`fence` exist but answer E_OPNOTSUPP until the fenced lane (W2d). | `2a3ab4f3` | suite **1358/1358** PASS, 0 EXTINCTION; boot log: `serving /srv/tapestry + /srv/warp` + `joey: /dev/warp mounted` + `Warp PROBE OK (/dev/warp/ctl: virgl 0)` — the 2D-boot degradation path proven; live ctx create/destroy = the thyla-gl gate run |
| Warp-2b | **The GPU-BO Burrow subtype (§6.1) + its kernel gates** — the second share-admissible kernel-minted DMA kind. `SYS_DMA_CREATE_GPU_BO = 106` (a SEPARATE number per the weave's #112 rationale; byte-for-byte the weave handler: `CAP_HW_CREATE` + the I-34 allowance `HW_RES_DMA` CreateBegin/CreateCommit pair; envelope `KOBJ_DMA_GPU_BO_MAX_SIZE` = 64 MiB) mints `KObj_DMA.gpu_bo` — create-immutable, mutually exclusive with `weave` by construction (`dma_create_body` takes a subtype enum, not bools). The safety argument is DISTINCT from the weave's and recorded on the field: a weave is device-READ (pixels out); a GPU BO is device-WRITTEN (render target / readback), bounded by GPU-side translation only the trusted owner programs — the client's RW cacheable mapping conveys zero hardware authority either way. Admission widened at **BOTH** gates — `burrow_share_into` (claim) **and** `sys_weft_share_for_proc` (register): the first build widened only the claim side and the new test FAILED at the register gate (the `[[audit-v8-closed-list]]` site-N/site-N+1 class, caught by the test's own red run; the code comment now says the two must widen together). New `WEFT_BIND_GPU_BO` kind (`weft_claimed_kind` demands entries==0 like the weave); `weft_binding_alloc_weave` → `weft_binding_alloc_maponly` (kind derived from the kobj bits); `weft_kind_maponly()` covers the clunk-unmap + orphan-reaper legs (`g_maponly_bindings`), so a dead GPU service's stale client BO mappings force-reclaim exactly like weaves. libthyla-rs: `t_dma_create_gpu_bo`. Tests: `weft.gpu_bo_share_and_claim` (mirror of the weave test: mint bits + envelope rejects, register+claim admit, kind decision both ways, budget charge/uncharge, binding kind names GPU_BO, Tweftio kind-gate + ring-alloc refusals, dual-domain free); went RED on the incomplete fix before green. Weft buggy cfgs re-run: all 4 still violate (detection net intact). | `e2accc2e` | suite **1358/1358** PASS (+1), 0 EXTINCTION, boot OK; weft_buggy_{premature_release,recheck_per_op,ring_toctou,share_outlives_flow} all still fire |
| Warp-2a | **#166: hostmem-BAR claim policy + cfg_type 8 shared-memory discovery** — the first Warp-2 sub-chunk (`GPU-DESIGN.md` §12 row 2; §3 fit-check defect + §6.2 prerequisite). Kernel: `pci_walk_caps` now parses `VIRTIO_PCI_CAP_SHARED_MEMORY_CFG` (cfg_type 8, a `virtio_pci_cap64` — 64-bit offset/length halves, `cap.id` = shmid) into a new `KObj_PCI.shm[2]` (discovery order, bounded; hostile layouts — bad/unassigned BAR, OOB extent incl. the non-wrapping 64-bit sum — reject the claim like the region caps). ABI: `t_pci_info` 208 → 256 (`t_pci_shm[2]` appended @208; prior offsets unchanged; asserts pinned in kernel + libthyla-rs + libt mirrors). Userspace: `PciDev::claim` no longer fails on a BAR > `PCI_BAR_VA_STRIDE` — the oversized BAR (the hostmem class) is claimed but left **unmapped** (`bar_va` None; `region()` fails closed on it), `PciError::BarTooLarge` retired, new `shm_region(shmid)` → `(device PA, length)`. Mapping a subrange stays the §6.2 Venus-chunk delta. Tests: `pci.walk_caps_shm` (6 cases — the 64-bit halves get DISCRIMINATING vectors: stored-offset asserts that fail if `offset_hi` is dropped, and a `length_hi` vector whose accept/reject verdict flips; plus the overflow-wrap vector a naive sum passes; the shared synthetic-config buffer grew 0x80 → 0x100 because chained 24-byte cap64s must not overlap). Live hostmem-device claim is verified when a hostmem device is first attached (the Warp-2 thyla-gl run or Warp-6 entry). | `ce70a3a9` | suite **1357/1357** PASS (+1), 0 EXTINCTION, boot OK; normal boot re-proves every existing virtio claim through the new loop |
| Warp-1 | **The GL-host substrate + the virgl capset probe** — the Warp arc's first impl chunk (`GPU-DESIGN.md` §12 row 1). (1) **Substrate AS-BUILT**: Thylacine boots on `thyla-gl` (Parallels Debian, TCG `-smp 4 -m 2048`, NOSTORM) in ~230 s; artifacts travel via NEW `tools/warp-host.sh sync` (git archive + scp + gzip→`dd conv=sparse` for the pool — macOS openrsync has no dependable `--sparse`); two probe-invisible substrate findings folded into `GPU-HOST-SETUP.md`: `ipxe-qemu` is load-bearing (rung 6 runs `-nodefaults` with no NIC, so the probe passes on a host where the real invocation dies on `efi-virtio.rom`) and Debian-13 `/tmp` is tmpfs (working copies live in `~/warp` on disk). (2) **The llvmpipe re-baseline ON THAT HOST** (§0.1 caveat discharged): NEW `tools/warp/glq-bench.exp`, one boot, paced + unpaced ×2, pswpin/out sampled around the run. THREE runs, an instrument lesson, and an enqueued anomaly: run 1 self-discarded by its own swap guard (pswpout 5→64079); run 2 (caches dropped) discarded again — and the guard's granularity was WRONG (per-RUN, quoting per-DEMO figures), so run 3 added **per-demo swap certification** after a VM reboot. Run 3: **paced 4.1 (clean) / unpaced 3.7 (pswpin +5, flagged) / unpaced-2 2.4 fps (pswpin +0 pswpout +0 — CLEAN and still collapsed)**, which REFUTES the swap/memory-wall theory for the 3/3-reproducing third-demo collapse → **#168 filed (UNEXPLAINED**; macOS/HVF multi-demo boots show no such degradation — thyla-gl-specific, guest-under-TCG not exonerated). **The recorded baseline is the honest band: llvmpipe on this host = 2.4–5.9 fps single-digit across 3 runs / 9 demos; the Warp-4 virgl figure must beat it decisively or resolve #168 first.** The macOS/HVF 192.8 anchor does not transfer. (3) **The capset probe** (tapestryd `gpu.rs`): `init_device` acks `VIRTIO_GPU_F_VIRGL` iff offered (2D boots byte-identical — the same suite boot logs `virgl=0 capsets=0`); `probe_capsets` reads `num_capsets` from the Device cfg region, enumerates `GET_CAPSET_INFO` (bounded 8), fetches the ranked-best blob (VIRGL2>VIRGL); ring 1→2 pages (RESP gets its own 4 KiB page — the 1384-byte VIRGL2 blob would have TRUNCATED in the old 0x300 slice). Plumbing: `run-vm.sh` `THYLACINE_GPU_DEV` + `egl-headless` display case (drops gpu-mmio0 like vnc); NEW `tools/warp/boot-probe.sh` (fail-closed boot probe). **GATE MET on thyla-gl**: `BOOT-capset: PASS` + `gpu GET_CAPSET id=2 ver=2 -> 1384 bytes; caps[0..16] = 0x00000002 …` (word 0 = `virgl_caps_v2.max_version` = 2 — the blob decodes) + `gpu up -- 1280x800, virgl=1 capsets=2` with the 2D compositor running over the GL device to `boot OK`. Kernel binary **byte-identical** (sha256 `dc0c4719…` pre/post) → userspace-only, no SMP gate (the U-7 precedent); standing SMP/spec records carry. Audit: focused Fable-5-max round on the diff — **0/0/1/3 NOT dirty, all four closed in code pre-commit** (`memory/audit_warp1_closed_list.md`; F1 [P2]: the capset failure disposition's "dead has latched" premise was true of the WRONG arm — a healthy-engine refusal now degrades to 2D instead of warden-restart-looping the console away; + size-0 capsets never ranked, info-words residue-guarded, a real bench floor, checked fixture copies). Refs: `docs/reference/139-tapestryd.md` "Warp-1" + `docs/GPU-HOST-SETUP.md` §3/§6 as-executed. | `db050b21` | suite 1356/1356 + boot OK, 0 EXTINCTION (2D arm `virgl=0`); GL host: smoke PASS ~230 s, capset PASS ~255 s (gate lines above), bench run 1 DISCARDED-by-guard / run 2 *(bench2-verdict)*; interactive gfx: **14/14 PASS, 0 SKIP** (4 batches; ran pre-F1/F2 binary — valid for the final one since a virgl=0 boot never reaches `probe_capsets`, where both fixes live entirely) |
| U-7-test hash fixup | Pinned the U-7-test impl hash (`70c1916`). | -- | -- |
| U-7-test | The U-7 job-control arc **cumulative composition smoke** -- **closes the U-7 arc**. PURE USERSPACE (a new `usr/u-7-test/` probe + the joey gate + the workspace/build wiring; the kernel binary is byte-identical, so no SMP gate; NOT an audit-trigger surface -- self-reviewed only). The U-7 analog of `/u-6-test`: where `/u-job-test` (19 scenarios) proves each U-7 surface in ISOLATION (the `&` launch + job table, jobs/fg/bg/wait/kill, `on note`/`mask note` delivery, the interruptible foreground wait), `/u-7-test` proves they COMPOSE the way a real session weaves them. Four composed flows, each weaving >=2 surfaces: (1) **bg-demux + builtins** -- two `echo &` jobs survive a foreground `seq`'s by-pid demux (foreground `$status` intact), then `wait` reaps every job + `jobs` consumes the Done entries exactly once; the foreground wait takes the no-note-queue DEGRADE path (plain blocking by-pid reap); (2) **forward-vs-handle by fg-presence** -- ONE `on note 'interrupt'` handler over one counter, fired at the idle prompt (`deliver_pending_notes`, counter increments) but NOT during a foreground wait, where the same note is forwarded to the child by `wait_pids_interruptible` and never dispatched (the counter is unchanged across the wait -- deterministic in both the immediate-reap and poll-and-forward timing arms); (3) **mask + reap** -- a `mask note 'interrupt'` body runs `wait %1`, reaping the bg job while the note is held by the kernel Thread mask, the deferred handler firing only at block exit AFTER the reap (scripture 10.8); (4) **the capstone** -- the whole stack through the REAL prompt cycle via `Repl::feed` (`run_line` -> `reap_jobs` -> `deliver_notes`): `open_notes` arms integrated delivery, a handler is registered + delivered through feeds, a `&` job is drained by the integrated reaper, `exit 0` ends the session. The harness cannot inject a `/dev/cons` Ctrl-C (A-4c), so a foreground interrupt is self-posted; every spawned child is reaped (no orphan -> joey wrong-pid). **Locked decision**: a dedicated probe (composition-focused, cleanly revertable, joey-gated, matches the per-sub-chunk pattern) over folding into `/u-job-test` (whose 19 scenarios test in isolation). Refs: `docs/reference/94-utopia-eval.md` 8.5 + the §11 status row. | `70c1916` | `make test-tcg` x2: `u-7-test: all OK` (4 flows) + `joey: /u-7-test reaped status=0; U-7 job-control arc integration verified` + `u-test`/`u-6-test`/`u-job-test`/all probes `all OK` + login E2E OK + `Thylacine boot OK`, **0 EXTINCTION** both boots. No SMP gate, no formal audit (pure userspace; kernel binary byte-identical). |
| U-7c-b hash fixup | Pinned the U-7c-b impl hash (`1218615`). | -- | -- |
| U-7c-b | **Interruptible foreground wait + Ctrl-C forward** -- the second half of U-7c; **the U-7c arc is COMPLETE**. PURE USERSPACE (libutopia + `/u-job-test`; built on U-2e `t::notes` + U-7-pre `wait_pid_for`; NOT audit-bearing -> `test-tcg` x2 floor, no SMP gate, no formal audit). New `eval::wait_pids_interruptible(env, &[pid])` replaces the blocking by-pid reap in ALL THREE foreground command waits -- `exec_external` (bare `cmd`), `exec_external_redirected` (`cmd > f`), `exec_pipeline` (`a | b`). The handoff named two; the bare-`cmd` path is the most common Ctrl-C case, so **completeness took all three**. Each iteration WNOHANG-reaps every still-live pid (the by-pid reap ground truth) then -- if any remain -- blocks on the note fd up to `FG_WAIT_BACKSTOP_MS` (100 ms) and drains the wake: `interrupt` -> forward `send(Pid(pid), "interrupt")` to each still-live fg pid (scripture 10.2: forward, do NOT run the shell's own handler); `child_exit` -> swallow (clears POLLIN so the next poll blocks, not spins -- the bg reaper WNOHANG-polls `live_pids`, not notes, so swallowing it breaks nothing); any other note -> `Env::defer_note` (new deferred-note queue) so its handler fires at the post-command `deliver_pending_notes` (now drains deferred FIFO before the live queue) rather than being lost (`try_read` consumes the queue front). DEGRADES to a plain blocking by-pid wait when `env.notes() == None` (host / boot-check / pre-open) -- identical to the prior reap. **Scope at v1.0**: command-substitution capture (`$(cmd)`, blocks on a capture pipe) + the `wait` builtin stay blocking (no forward); both gain forwarding with process groups (U-PTY). `/u-job-test` +4 scenarios (16 forward-to-live-child SEND; 17 `wait_pids_interruptible` drains a pre-queued interrupt + reaps without hanging; 18 the degrade path; 19 a non-interrupt note deferred-not-lost). Self-reviewed clean (the 100 ms backstop bounds every coalesced/lost/mask-deferred `child_exit` -> no hang + the kernel poll never spins; borrow-clean drain/defer; degrade path == prior blocking reap). The harness cannot inject a real `/dev/cons` Ctrl-C (A-4c), so the probe self-posts the `interrupt` the kernel console owner would post. Refs: `docs/reference/94-utopia-eval.md` 9.7 + `108-utopia-repl.md` 3.7 + `UTOPIA-SHELL-DESIGN.md` 10.2 (as-built) / 10.8. | `1218615` | `make test-tcg` x2: `u-job-test: all OK` (19 scenarios) + `u-test`/`u-6-test`/all probes `all OK` + `Thylacine boot OK` + login E2E OK, **0 EXTINCTION** both boots |
| U-7c-a hash fixup | Pinned the U-7c-a impl hash (`377b866`). | -- | -- |
| U-7c-a | **`on note` / `mask note` runtime delivery** -- the first half of U-7c (split: the note-delivery core here; the running-foreground Ctrl-C forward is U-7c-b, which reworks the foreground reap loops to poll the notes fd -- death-path-adjacent, isolated). PURE USERSPACE (libutopia + `ut` + `/u-job-test`; built on the U-2e `t::notes` substrate; NOT audit-bearing -> `test-tcg` x2 floor, no SMP gate, no formal audit). New `eval::deliver_pending_notes(env)` drains the shell's own note queue (`Notes::try_read`, non-blocking) at the REPL prompt-cycle sync point (`Repl::deliver_notes`, beside `reap_jobs`) and fires registered `on note` handlers (`eval_block`, best-effort, `$status`-transparent -- saved/restored around the drain like bash `$?`-around-a-trap). `eval_mask_note` rewritten from the U-6b body-only pass-through to engage the per-Thread kernel mask (`t::notes::set_mask` via new `Env::note_mask_add/remove`) for the body's duration -> a matching note is dequeue-deferred (`devnotes_poll` gates POLLIN on the mask, verified) and fires at the post-block drain (scripture 10.8). `note_class_for_name` maps `interrupt`/`pipe`/`child_exit` (kill/snare/user names don't map). New `Env` note-queue API (`set_notes`/`notes`/`note_mask*`); the note fd (`Option<Notes>`) is opened LAZILY by `Repl::open_notes` from the `ut` main loop after the FIRST read -- a bare-spawn `ut` (boot check) has an EMPTY handle table, so eager open would mint the note fd at fd 0 and the read loop would block on it (caught + fixed in this chunk: the boot stalled at the `ut` banner until the lazy-open fix). `/u-job-test` +3 scenarios (13 on-note-fires-on-delivery + exactly-once; 14 unhandled-note-benign; 15a parsed `mask note` defer+drain + 15b the mask mechanism directly). Self-reviewed clean (non-blocking drain, masked-queue-returns-None [kernel-verified], borrow-clean dispatch, no fd-0 clobber). Refs: `docs/reference/94-utopia-eval.md` 9.6 + `108-utopia-repl.md` 3.1/3.7 + `UTOPIA-SHELL-DESIGN.md` 10.7/10.8 as-built. | `377b866` | `make test-tcg` x2: `u-job-test: all OK` (15 scenarios) + `u-test`/`u-6-test`/all probes `all OK` + `Thylacine boot OK` + login E2E OK, **0 EXTINCTION** both boots |
| U-7b hash fixup | Pinned the U-7b impl hash (`fdaf673`). | -- | -- |
| U-7b | **Job-control builtins** `jobs` / `fg` / `bg` / `wait` / `kill` over the U-7a `JobTable` + the U-7-pre `wait_pid_for` primitive. PURE USERSPACE (libutopia + the probe; NOT audit-bearing -> `test-tcg` x2 floor, no SMP gate, no formal audit). `builtin.rs` gains the 5 builtins + the shared `reap_background(env)` (the WNOHANG poll-and-mark half, now called by both `Repl::reap_jobs` and the `jobs` refresh) + `resolve_jobspec`/`resolve_target_job`/`wait_pids_blocking`. `JobTable` gains `current_spec`/`previous_spec`/`spec_pids`/`cmd_of`/`remove`. **`jobs`** refreshes then lists `[N]{+\|-} Running\|Done cmd` + CONSUMES the reported-Done jobs (exactly-once, whether seen here or at the prompt -- bash); **`wait [id...]`** blocks by-pid on the named jobs/pids (or all); **`fg %N`** blocks to completion + removes the job silently (no Done line); **`bg %N`** reports already-in-bg (v1.0 has no stopped jobs -- U-PTY); **`kill {%N\|pid} [interrupt\|kill]`** posts a note, default `interrupt`. **Pulled-forward grammar fix**: `%` added to `lexer.rs::is_word_char_byte` (command context) -- it was a hard `UnexpectedChar`, blocking `%N` jobspecs (and literal `%` args like `echo 100%`); arith `%` modulo is unaffected (the expr parser re-lexes word TEXT via `retokenize_arith_word`). **Scripture clarification** (`UTOPIA-SHELL-DESIGN.md` §10.6): the jobspec convention is bash's `%N`=job / bare-N (job for fg/bg, pid for wait/kill), and `kill` defaults to `interrupt` (scripture's `snare:term` is unsendable from userspace -- `snare:*` is kernel-synthetic-only). `/u-job-test` extended with 6 new scenarios (wait %N + jobs-consume / wait-all / fg-removes / bg-reports / kill-error-paths / kill-to-a-live-`tr`-blocker) + 3 jobs.rs `#[cfg(test)]` units. Refs: `docs/reference/94-utopia-eval.md` §9.5 + `108-utopia-repl.md` §3.6 + `93-utopia-parser.md` (arith retokenization). | `fdaf673` | `make test-tcg` x2: `u-job-test: all OK` (12 scenarios) + `u-test`/`u-6-test`/all probes `all OK` (the `%` lexer change broke nothing) + `Thylacine boot OK`, 0 EXTINCTION both boots |
| U-7a hash fixup | Pinned the U-7a impl hash (`4808ea0`). | -- | -- |
| U-7a | **Background jobs** -- the U-7 job-control arc opener. PURE USERSPACE (libutopia + ut + a probe; NOT audit-bearing -> `test-tcg` x2 floor, no SMP gate, no formal audit). New pure `eval::jobs` (`JobTable` + `Job` in `Env`, host-testable); `eval_pipeline` routes `&` -> `eval_background_pipeline` (shares the factored `spawn_pipeline_elements` with `exec_pipeline`); both `exec_pipeline` + `capture_external_pipeline` reaps converted wait-any -> **by-pid** (the pid-precise foreground demux, on the U-7-pre `wait_pid_for` primitive); `render_pipeline_cmd`; `Repl::reap_jobs` (the prompt-cycle WNOHANG reaper, one poll per live pid). `cmd &` / `a | b &` register ONE job, print `[N] PID`, status 0; finished jobs emit `[N]+ Done  cmd` before the next prompt. bg builtin/fn (needs fork) stays NotImplemented. **Notes-fd / multi-fd-poll integration (async `[N]+ Done` while idle, `on`/`mask` delivery, Ctrl-C) is U-7c** -- the WNOHANG poll is the complete reap ground truth, so the deferral loses no correctness. New `/u-job-test` (registration / one-job-per-pipeline / reap-to-`Done` / spec-reset / by-pid demux / bg-builtin reject / integrated `Repl` reaper) + jobs.rs `#[cfg(test)]` units; `u-test` probes 4/7 updated (`&` now WORKS+reaps -- was the stale U-6 `NotImplemented` assertion). ut version 0.7-dev. Refs: `docs/reference/94-utopia-eval.md` §9 + `108-utopia-repl.md` §3.6. | `4808ea0` | `make test-tcg` x2: `u-job-test: all OK` + `joey: /u-job-test reaped status=0; U-7a background jobs verified` + `u-test: all OK` + `Thylacine boot OK`, 0 EXTINCTION both boots |
| U-7-pre hash fixup | Pinned the U-7-pre impl hash + the closed-list tip. | -- | -- |
| U-7-pre | `SYS_WAIT_PID` v2 (WNOHANG + wait-by-pid) -- the **kernel death-path** primitive the U-7 job table needs, built before U-7a (user-voted 2026-06-08; the documented `joey.c:2451` owed lift "a kernel `wait_pid_for(pid)`"). **AUDIT-BEARING death-path** (the #788/#809/#811/#926 lineage). A **syscall ABI change**: `SYS_WAIT_PID` goes `(status_out)` -> `(want_pid, flags, status_out)`; `pid=-1, flags=0` exactly reproduces the legacy reap-any-blocking behavior (the new thin `wait_pid` wrapper + all 25 joey reap sites + `Child::wait` unchanged in effect). Adds a **pid filter** (`want_pid>0` selects one child -- closes the wrong-pid extinction hazard the unfiltered reap-any caused) + **`WAIT_WNOHANG`** non-blocking (returns 0 = "not ready"; 0 is an unambiguous sentinel since pid 0 is never a reapable child -- `g_next_pid` starts at 1). The reap teardown (proc_unlink_child / on_cpu spin / thread_free walk / proc_free / #811 SLEEP_INTR) is **copied byte-for-byte** -- only the SELECTION + the blocking decision + the cond filter (`wait_pid_cond` now takes a stack-local `wait_cond_ctx`) change. Kernel core `wait_pid_for(want_pid, flags, status_out)` + `wait_pid(status_out) = wait_pid_for(-1,0,·)`; handler 3-arg + dispatch; `WAIT_WNOHANG` in proc.h. Userspace: `t_wait_pid_for` + `T_WAIT_WNOHANG` (libt + libthyla-rs); `t_wait_pid` now a `(-1,0,·)` wrapper; **`Child::wait` is by-pid** (precise) + new **`Child::try_wait`** (WNOHANG) -- **closes #856**. Invariants: I-8 (no starve), I-9 (no lost wake; the per-parent rendez wakes on every child exit, the filtered cond re-checks under r->lock), death-path lifetime (alloc-stable `c->pid` filter; `wait_cond_ctx` outlives the `sleep`). **No new spec / no new ARCH invariant** (refines an existing syscall's selection logic) per the 2026-05-23 broadening. Focused Opus audit (agent + self-audit) **0 P0 / 0 P1 / 1 P2 / 4 P3, all fixed**: F1[P2] WNOHANG poll-loop-must-yield contract note (doc -- proc.h + syscall.h + `Child::try_wait`); F2[P3] non-wrapper-callers-zero-x1/x2 ABI note; F3[P3] reject unknown flag bits (`flags & ~WAIT_WNOHANG -> -1`); F4[P3] `selects_target` made deterministic (poll spinner then exiter with no `sched()` between -> the "ignore a present zombie" case provably hits); F5[P3] pid-0-never-a-child filter comment. Closed list: `memory/audit_u7pre_wait_pid_v2_closed_list.md`. Owed consumers (separable): tighten joey's stratumd-coexist waits to by-pid; wire pouch `wait4` -> SYS_WAIT_PID. Refs: `docs/reference/14-process-model.md` (`wait_pid_for` section), CLAUDE.md + ARCH section 25.4 audit-trigger rows. | `f5a65a5` | kernel **787/787 PASS** (+3: `proc.wait_pid_for_{no_match,wnohang_alive_then_reap,selects_target}`); `make test-tcg` x2 + 0 EXTINCTION + boot OK + login/legate/thread-probe E2E; **SMP gate (`ci-smp-gate.sh`, N=10): 0 corruption across default+UBSan x smp4/smp8** (re-run on the post-audit-fix state). |
| U-6-test hash fixup | Pinned the U-6-test impl hash. | -- | -- |
| U-6-test | The U-6 evaluator arc **cumulative integration smoke** -- the last sub-chunk; **closes the U-6 arc**. **PURE USERSPACE: zero `kernel/`/`arch/`/`mm/`/`specs/` files -- the kernel binary is byte-identical (784/784 unchanged), so no SMP gate; not an audit-trigger surface (self-reviewed only).** Where the per-sub-chunk probes prove each U-6 surface in ISOLATION (`u-builtin`/`u-glob`/`u-subst`/`u-repl-test` + `u-test`'s `flow_eval_*` blocks), this probe proves they COMPOSE: realistic multi-feature scripts in one evaluation pass, landing assertions on the only pre-PTY observables (Env state, `$status`, and -- the key integration lever -- command substitution, which CAPTURES a real spawned/piped/globbed command's stdout into a variable). New dedicated `usr/u-6-test` (joey-gated on status==0; 7 composed flows, each weaving >=3 U-6 features): (1) subst-capture -> `for` -> `case` -> arith (classify+count `*.c` files); (2) glob -> spawn(`echo u-*`) -> subst-capture (enumerate the u-* binaries -- the full glob->argv->spawn->stdout->capture->split chain, with a self-reference since `u-6-test` is in the same ramfs); (3) var-interp -> pipeline(`echo|tr`) -> subst (transform + field-split into `(HELLO WORLD)`); (4) `cd` builtin -> `$status`/`$cwd` special-var bridge -> `if` -> Dq interp ("now at /srv"); (5) var -> heredoc-interp -> `<<` redirect -> spawn (var-fed heredoc to pipe-sink); (6) **the capstone** -- a realistic multi-statement session driven through the REAL read-parse-eval loop via `Repl::feed` (var + `$(echo $name|tr)` capture + `"[$up]"` Dq interp + `exit 0`), proving the whole U-6 stack runs through the U-6g entrypoint with state carried across feeds; (7) `Repl::feed` parse-error survival + post-error subst (interactive resilience, scripture 8.9). **Locked decision**: a dedicated probe (composition-focused, cleanly revertable, matches the per-sub-chunk pattern, keeps the 3389-line `u-test` from ballooning, gives the arc-close a legible joey gate + a `u-*` glob self-reference) over folding into `u-test` (whose `flow_eval_*` blocks test in isolation). Ref doc: `docs/reference/94-utopia-eval.md` (Tests section + Status table). | `e4b5100` | kernel **784/784 PASS** (unchanged -- no kernel delta); `make test-tcg` boot pass on two consecutive runs; all 7 flows + `u-6-test: all OK` + `joey: /u-6-test reaped status=0; U-6 evaluator arc integration verified` + login E2E + `Thylacine boot OK`; **0 EXTINCTION** both boots. No SMP gate, no formal audit (pure-userspace; kernel binary byte-identical). |
| U-6g hash fixup | Pinned the U-6g impl hash. | -- | -- |
| U-6g | poll/read-parse-eval REPL main loop + line-editor wire (#935). **PURE USERSPACE: zero `kernel/`/`arch/`/`mm/`/`specs/` files -- the kernel binary is byte-identical (784/784 unchanged), so no SMP gate is required.** `ut`'s `rs_main` becomes the read-parse-eval REPL: it prints the Pale Fire banner (bumped to `v0.6-dev`), draws the prompt, then loops reading fd 0 -> `libutopia::repl::Repl::feed` drives the U-4 line editor (`feed_bytes` -> `EditorAction`), on `Accept` parses + evaluates via `eval::eval_source`, renders the prompt/echo to fd 1, and loops; it exits on Ctrl-D / `exit` / input EOF. `Env::interactive = true` (scripture 8.9: a non-zero `$status` does NOT end the session). New **`libutopia::repl`** module -- the fd-agnostic `Repl` core (`new`/`env`/`env_mut`/`draw_prompt`/`feed`/`exit_code`), generic over an injected `io::Write` sink so it is testable without a real fd -- plus **`impl io::Write for Vec<u8>`** in libthyla-rs (the in-memory sink, std-conventional + reusable). **Input substrate**: fd 0 = the cons handle (login spawns `ut` with `Stdio::Inherit` over the A-5 `SYS_CONSOLE_OPEN` handle) or a pipe (CI); the blocking byte read is the existing login `read_line` precedent. **SCOPE BOUNDARY** (scripture 10.1/10.2 + ROADMAP rows U-6/U-7): the MULTI-fd `poll()` across the notes fds for job control (Ctrl-C/Ctrl-Z, `&`, jobs/fg/bg, `on note`/`mask note`) is **U-7**, predicated on the PTY line discipline; the pollable-cons + line-discipline + termios + per-Proc fd 0/1/2 substrate is **U-PTY**. The single-fd `PollSet` over fd 0 is the U-7 seam (cons has no `.poll` hook -> always-ready -> the blocking read waits; on a pipe poll is real). The cons `.poll` hook is deliberately NOT pulled forward -- it is an audit-bearing kernel surface with the IRQ-vs-poll-waiter-list hazard `cons.c` avoids by design (IRQ context only `wakeup()`s a Rendez), and scripture stages it to U-PTY. joey's bare `/ut` boot spawn now exits 0 cleanly: a bare `t_spawn` child has no fd 0 (rfork RFPROC gives a fresh empty handle table), so the first read errors -> banner + clean exit (a link + boot + banner smoke; the real loop is exercised by `/u-repl-test`). **Locked decisions**: (1) wire the REPL to the existing fd-0 cons/pipe substrate (the login precedent), NOT the cons `.poll` hook (= U-PTY) nor a U-PTY resequence -- scripture-aligned, research-collapsed (no user fork needed); (2) the multi-fd job-control `poll()` is U-7, not U-6g; (3) the `Repl` core is fd-agnostic + generic over `io::Write` so the deterministic probe drives it with in-memory bytes (the harness cannot inject cons keystrokes -- the A-4c constraint). **Verification**: 7 host `#[cfg(test)]` unit tests in `repl.rs` + the new pre-pivot `usr/u-repl-test` (joey-gated; 7 cases: Accept->eval->Env state, multi-read line accumulation, `exit N`, Ctrl-D on empty, Ctrl-C edit-discard + recovery, Redraw-renders-to-sink, parse-error survival). NOT an audit-trigger surface (pure userspace; no privilege/concurrency/lifetime invariant class) -- self-reviewed (no-spin loop: breaks on read 0/Err; `exit` short-circuits the action batch; bounds-safe `buf[..n]`; borrow-clean since `feed_bytes` returns an owned Vec). Ref doc `docs/reference/108-utopia-repl.md`. | `517e212` | libutopia + ut + u-repl-test `cargo build --release` clean (no warnings on the new files); kernel **784/784 PASS** (unchanged -- no kernel delta); `make test-tcg` boot pass on two consecutive runs; `u-repl-test: all OK` + `joey: /u-repl-test reaped status=0; U-6g REPL main loop verified` + `joey: /ut reaped status=0; Utopia shell REPL verified` (new `v0.6-dev` banner observed) + login E2E + `Thylacine boot OK`; **0 EXTINCTION** both boots. No SMP gate -- the kernel binary is byte-identical (pure-userspace chunk). |
| U-6f hash fixup | Pinned the U-6f impl hash + the audit closed-list. | -- | -- |
| U-6f | Command substitution `$(cmd)` / `` `{cmd}` `` (#933) **+ its kernel prerequisite #926** (user-voted "fix the kernel" 2026-06-08). **The chunk surfaced a real kernel dependency**: the kernel closed a Proc's fds only at REAP (`handle_table_free` in `proc_free`, ZOMBIE-gated), so a child's inherited pipe write end stayed open in the zombie until the parent reaped it -- a shell draining `$(cmd)`'s stdout to EOF hung forever. **#926 (kernel)**: a SINGLE-thread Proc closes its handle table at the TOP of `exits()` (`proc_close_handles_at_exit`, gated `thread_count==1`, while RUNNING + ALIVE -- the only sound spot: a 9P-clunk close hook may sleep [illegal once EXITING], `wait_pid` can't reap an ALIVE Proc mid-close, and no peer shares the table). Inherited fds thus close at process termination (correct Unix/Plan 9 semantics); the parent's drain-before-reap sees EOF immediately. Multi-thread Procs keep close-at-reap (their EXITING mark is atomic-under-lock with the last-thread determination -- a v1.x EXITING-protocol restructure lifts it + the killed-single-thread asymmetry). I-7 (the #847 Burrow dual-refcount) makes the handle-close-at-exit / vma-drain-at-reap ordering inversion safe. **U-6f (userspace)**: `stmt::run_command_substitution[_script]` + `capture_external_pipeline` (spawn an external redirect-free pipeline, drain the last element's stdout to EOF BEFORE reaping -- deadlock-free, wait-any + pid-match) + `split_fields` + `trim_trailing_newlines`; `$status` -> `Cell<i32>` (interior mutability so the `&Env` evaluator records the inner exit, §8.7); the `eval_value_token`/`eval_dq_in_word`/`render_heredoc_body` (token+DqPart layer) + `expr.rs` (pre-parsed `Box<Script>` layer) Subst/Backtick arms; `eval_command`/`eval_let`/`eval_assign` set `$status=0` before evaluating so a substitution-free command reports 0 while a bare/RHS `$(failing)` preserves the inner exit. **v1.0 scope**: a single redirect-free EXTERNAL pipeline; builtin/fn/control-flow/multi-statement bodies + `<(cmd)`/`>(cmd)` procsub -> `NotImplemented` (documented -- in-process capture needs fork; procsub needs `/proc/self/fd/N`). Bare context whitespace-splits (rc `$ifs`); `"$(cmd)"`/heredoc inline. **Locked decisions**: (1) fix the kernel (close fds at exit) over a 4 KiB-capped userspace workaround that silently deadlocks above the pipe buffer -- user-voted; (2) single-thread-only at-exit close (multi-thread = v1.x); (3) `$status` as a Cell (localized) over rippling `&mut Env` through the whole expression tree; (4) procsub deferred (no `/fd/N`). **#926 is the audit-bearing death-path surface** (ARCH §25.4): SMP gate (default+UBSan x smp4/smp8 N=10) **0 corruption** + a focused Opus prosecutor round CLEAN (0 P0 / 0 P1 / 0 P2 / 3 P3, all doc -- F1 handle.c lockless-justification updated, F2 kill-path asymmetry documented, F3 `$errstr`-stale cosmetic; `memory/audit_926_u6f_closed_list.md`). New `usr/u-subst-test` (9 probes) + the `u-test::flow_process_pipe` part-(c) #926 regression (read child stdout to EOF before reap -- the pattern formerly documented as "block forever") + the rewritten `test_sys_spawn_with_fds_child_rights_subset_of_parent` (spawns the BLOCKING pipe-sink to keep fd 0 live for inspection). | `e9e0aa9` | kernel **784/784 PASS** (the spawn-with-fds rights test rewritten; no count change); `make test-tcg` boot pass + `u-subst-test: all OK` + `joey: /u-subst-test reaped status=0; U-6f command substitution verified` + login E2E + `Thylacine boot OK`; **0 EXTINCTION**; **ci-smp-gate PASS** (default-smp4 + default-smp8 + ubsan-smp4 + ubsan-smp8, N=10, **0 CORRUPTION**) -- the #926 death-path change is SMP-sound. |
| U-6e-b-2 hash fixup | Pinned the U-6e-b-2 impl hash (`aba3b7d`). | -- | -- |
| U-6e-b-2 | Glob argv fs-expansion -- **the second half of U-6e-b (#928, #931), on the U-6e-b-1 `read_dir` foundation. PURE USERSPACE: zero kernel files, the kernel binary is byte-identical (784/784 unchanged), so no SMP gate is required.** **(1) `libutopia::eval::glob::expand(env, pattern) -> Vec<String>`** (the fs-walk, §6.3 of ref 94): splits the pattern on `/` (empty segments dropped -- `//`/leading/trailing `/` normalize away); the leading meta-free run is the literal start dir; walks one segment per level via `fs::read_dir` + `glob::matches`, descending ONLY matching **directories** for non-final segments (recursion bounded by segment count, NOT tree depth -- no unbounded descent, no cycle risk on a flat/symlink-free FS); POSIX dotfile rule (a leading-`.` name is skipped unless the segment itself starts with `.`); the display path stays pattern-shaped (a relative pattern yields relative matches; `$cwd` only LOCATES the start dir, it is never prefixed onto the output); collect-then-`sort()` once (bash whole-string order). **rc nullglob (scripture 6.10)**: a no-match -- or an unreadable directory along the way -- returns the EMPTY vec, NEVER the literal. `**` stays per-segment (== `*`); recursive `**`, trailing-slash dirs-only, and expr-context (for-list / let-RHS) globbing are v1.x. **(2) `stmt::evaluate_argv` wiring**: a new `glob_candidate(w)` gate expands ONLY a bare `Word::Single(TokenKind::Word)` carrying a meta char; quoted strings, `$var`, and `^`-concat words stay literal (rc-lineage -- `echo "*.c"` is the string); an empty match set drops the word entirely. **Verification**: new pre-pivot `usr/u-glob-test` (joey-gated on status==0) drives `expand` directly (prefix `u-*`, bare `*` [count >= 10 / sorted / no-`/` on the flat dir / no dotfile leak], `versio?`, `[vw]*`, absolute `/u-*`, nullglob `no-match-* -> []`), then observes the `evaluate_argv` wiring via `$status`: a bare glob matching nothing nullglobs to an empty command (status 0); the same pattern quoted is a literal arg spawned NotFound (status 127) -- the delta proves the bare branch globbed and the quoted branch did not. **Self-audit** (the rigor floor; NOT formal-audit-bearing -- a bounded read-only enumeration on the already-audited -b-1 read_dir surface, no capability/concurrency/lifetime invariant class): bounded recursion, owned strings across recursion (`into_file_name`), nullglob = empty-Vec, sort-once determinism (confirmed across 2 boots). **Locked decisions**: (1) collect-then-sort once (bash whole-string order, not a per-level sort that diverges around `/`); (2) cwd locates but does not prefix the result; (3) only a bare `Word::Single(TokenKind::Word)` expands -- quoted/`$var`/`^`-concat never; (4) `expand` is `pub` for the direct boot probe (argv echoes to a dropped pipe at v1.0, so the expansion is asserted on the returned Vec). | `aba3b7d` | libutopia + u-glob-test `cargo build --release` clean; kernel suite **784/784 PASS** (unchanged -- no kernel delta); `make test-tcg` boot pass on two consecutive runs; `u-glob-test: all OK` + `joey: /u-glob-test reaped status=0; U-6e-b-2 glob verified` + `Thylacine boot OK`; 0 EXTINCTION both boots. No SMP gate run -- the kernel binary is byte-identical (pure-userspace chunk). |
| U-6e-b-1 hash fixup | Pinned the U-6e-b-1 impl hash (`b5ffe7f`). | -- | -- |
| U-6e-b-1 | Directory-enumeration foundation -- **the first half of U-6e-b (#928), the read_dir surface U-6e-b-2's glob fs-walk + #925's `ls` both need; a KERNEL pull-forward** because devramfs had a NULL `.readdir` ("deferred nicety"), so `read_dir`/`ls`/glob could not enumerate the pre-pivot boot FS the user stands in. **(1) kernel `devramfs_readdir`** (`kernel/devramfs.c` + the dc='m' `.readdir` slot): enumerates the flat root -- every cpio file (QTFILE) + the synthetic `srv`/`proc` mount-point dirs (QTDIR) -- emitting the Thylacine 9P2000.L dirent wire format the `SYS_READDIR` handler parses (`qid(13)+offset(8 LE)+type(1)+name_len(2 LE)+name`), with the `offset` field a 1-based ordinal RESUME COOKIE the handler round-trips via `c->offset` (forward walk, no dup/skip); whole-entries-only, non-dir -> -1, synth dir -> 0/EOD; reads immutable-after-init data (no lock). It returns **-1, not 0,** when the first entry of a run cannot fit the caller's buffer (the getdents/EINVAL convention -- 0 would silently truncate as a spurious EOD). **(2) libthyla_rs `fs::dir`** (`ReadDir` `Iterator<Item=Result<DirEntry>>` over `t_readdir`, parsing complete-entries-only mirroring the kernel's cookie accounting; `DirEntry` name + qid_type + `is_dir`/`is_file`; 4 KiB staging buffer) + `fs::read_dir(path)` opening the dir through the **stalk resolver** (`t_open` -- handles `/`, multi-component, `..`). **(3) #929 closed**: `File::open`'s root case (`open_create_at_path` zero-component) now opens via `t_open` (stalk returns the Territory root Spoor for `/`), so `File::open("/")`/`metadata("/")`/`is_dir("/")` work; rights are derived identically to walk_open (`rights_for_omode\|RIGHT_TRANSFER`, verified). **Verification**: new pre-pivot `usr/u-readdir-test` (joey-gated) -- enumerates `/` + asserts welcome/version/srv/proc/self present, srv/proc type as dirs + welcome as a file, `is_dir("/")`/`metadata("/")` resolve (#929), read_dir on a file errors on the first item, a missing path fails to open. **Surfaced + tracked #932**: devsrv/devproc lack `.readdir`, so `ls /srv`/`ls /proc` (mounted Devs) need their own enumeration path (separable; audited stalk-3 surface). **Self-audit**: bounds (every write in `[pos, pos+entry) <= cap <= scratch`), nlen NUL-term, cookie monotonicity, non-dir/synth/EOD, immutable data -- clean; a bounded read-only enumeration, NOT an invariant-class (capability/concurrency/lifetime) surface, so 5 deterministic kernel tests + boot E2E + the SMP gate are the rigor floor (no formal prosecutor round). **Locked decisions**: (1) pull devramfs readdir forward (a read_dir that fails on the boot FS is a half-feature; boot-test verification needs a readable dir pre-pivot); (2) read_dir uses stalk, not the per-component walk (root + `..` + one syscall); (3) -1-on-too-small over silent EOD; (4) U-6e-b split into -b-1 (enumeration, kernel) / -b-2 (glob, userspace). | `b5ffe7f` | kernel suite **784/784 PASS** (5 new `devramfs.readdir_*` tests: enumerates_root, file_returns_neg1, buffer_too_small_errs, synth_dir_empty, paginates_no_dup_no_skip); `make test-tcg` boot pass on two consecutive runs; `u-readdir-test: all OK` + `joey: /u-readdir-test reaped status=0; U-6e-b-1 readdir verified` + `Thylacine boot OK`; 0 EXTINCTION both boots; **ci-smp-gate PASS** (default-smp4 + default-smp8 + ubsan-smp4 + ubsan-smp8, N=10, **0 CORRUPTION**; the timing entries are the known benign host-oversubscription flakes the gate classifies) -- the kernel `devramfs_readdir` is SMP-sound (read-only-after-init data, no new locks) |
| U-6e-a hash fixup | Pinned the U-6e-a impl hash (`fbba6d2`). | -- | -- |
| U-6e-a | Shell built-in dispatch + the must-be-internal builtins (scripture 9.1) -- **the first U-6e sub-chunk, on the native-argv runtime U-6e-pre delivered**. New `libutopia::eval::builtin` module: `try_builtin(env, &argv) -> Option<EvalResult<StatementFlow>>` (Some = handled in-process; None = fall through to external spawn) + `is_builtin(name)`. `eval_command`'s Simple arm now resolves argv[0] **fn -> builtin -> external** (a user `fn cd {...}` shadows the builtin; idiomatic). **Built-ins** (the §9.1 "must be internal" set needing no new substrate): **cd** (validates the target via `fs::is_dir`; lexically normalizes `..`/`.` against `$cwd` in `normalize_abs`; sets `$cwd`+`$oldpwd`; no-arg -> `$home`/`/`; `cd -` toggle + echo; the root `/` bypasses the fstat probe -- it is the territory root_spoor, a directory by construction, and `File::open` cannot open a zero-component path regardless [#929]), **pwd** (prints `$cwd` to the UART, the v1.0 no-fd1 convention), **exit [code]** (`Env::pending_exit` one-shot + `$status`; `eval_block` short-circuits the statement stack to `Return` + `invoke_function` does NOT normalize the exit-`Return` at the fn boundary; no-arg = current `$status`; non-numeric -> 2), **true/false**, **unset NAME...**, **eval ARGS...** (joins + `eval_source` in the CURRENT scope), **source FILE / .** (reads + `eval_source` in the current scope -- assignment + fn registration persist into the caller), **type/whence**. A built-in with a redirect or as a pipeline element -> `NotImplemented` (no spawned fd to retarget -- honest, not a confusing 127). **Folded-in pre-existing-gap fix**: the `$status`/`$errstr`/`$cwd` special-var read/write bridge (`Env::get`/`assign`/`let_set` route those three names to their dedicated fields) -- a silent gap since U-6b (scripture 8.5 requires them queryable in-script; the boot probes only ever read the Rust `status()` accessor), surfaced because `cd` is what first makes `$cwd` meaningful. **Deferred builtins** (own substrate): `set`/`export` (no envp), `alias`/`unalias`, `read`, `jobs`/`fg`/`bg`/`wait`, `history`, `kill`/`note`, `echo`/`printf`/`test`/`palette`/`bind`/`mount`. **Verification**: new pre-pivot `usr/u-builtin-test` (12 probes -- true/false, cd-to-synth-dir + `$cwd` bridge, normalize+root-clamp, `cd -` toggle, missing-dir fail + `$errstr`, unset, eval-in-scope, `$status` bridge, type, source `/builtin-test.rc` [assign + fn persist], `exit 7` flow + pending, exit-through-a-fn unwind) + a `/builtin-test.rc` cpio fixture; joey gates the boot on status==0. **Locked decisions**: (1) U-6e split into U-6e-a (builtins) / U-6e-b (glob fs-walk + `fs::read_dir`); (2) only the must-be-internal-now set; deferred builtins fall through to external (no half-impl); (3) the special-var bridge lands here (cd motivates it; uniform fix for all 3); (4) NOT audit-bearing -- userspace evaluator, no kernel surface (kernel binary unchanged). **Surfaced + tracked**: #929 (libthyla_rs `File::open`/`metadata`/`is_dir` cannot open the root `/`; worked around in cd). | `fbba6d2` | libutopia + u-builtin-test `cargo build --release` clean; kernel suite **779/779 PASS**; `make test-tcg` boot pass on two consecutive runs; `u-builtin-test: all OK` (12 probes) + `joey: /u-builtin-test reaped status=0; U-6e-a builtins verified` + `Thylacine boot OK`; 0 EXTINCTION both boots; **ci-smp-gate PASS** (ubsan-smp4 + default-smp8, N=10, **0 CORRUPTION**; ubsan-smp4 7 PASS/3 benign-timing, default-smp8 8 PASS/2 benign-timing) -- userspace-only chunk, kernel binary unchanged |
| U-6e-pre-b hash fixup | Pinned the U-6e-pre-b impl hash (`26d55c1`). | -- | -- |
| U-6e-pre-b | _(impl SHA `26d55c1`)_ Adopt the native coreutils onto the promoted U-6e-pre-a surface -- **second sub-chunk of the aux-coreutils adoption arc; the FIRST runtime execution of the coreutils (the aux track only compiled them)**. Brings 16 of the auxiliary track's `usr/apps/*` coreutils into the main build as a single `usr/coreutils` crate (one `src/bin/<name>.rs` per tool, cargo-auto-discovered -- far less wiring than 16 crates): **echo, cat, wc, head, tail, true, false, seq, sort, uniq, tr, cut, grep, basename, dirname, pwd**. Each is rewritten off the aux-rt workaround onto `libthyla-rs` proper (`env::args` [G03] + `io::{stdin,stdout,stderr,out,copy,slurp}` + the `print!`/`eprintln!` macros [G05] -- a pure symbol substitution, since the aux apps were already libthyla-rs except for those 3 aux-rt surfaces). libthyla-rs gains the last 3 convenience helpers the apps use: `io::out`/`io::err` (best-effort byte writes) + `io::slurp` (read-to-Vec). **Verification:** new native driver `usr/coreutil-smoke` -- joey spawns it; it spawns EACH coreutil via `libthyla_rs::process::Command` with a piped stdin (fed a known input) + piped stdout (captured), and asserts the output bytes + exit status (28 checks: argv tools, stdin filters, devramfs file reads via `File::open`, exit codes). `do_native_coreutil_smoke()` gates the boot; the first RUNTIME exercise of the coreutils + a second exercise of the native-argv path + the first parent-reads-child-piped-stdout path. **Two bugs surfaced + fixed during bring-up** (ground-truth, not theory): **(1) devramfs truncation** -- the 17 new cpio binaries pushed the live ramfs to 69 entries, past `kernel/devramfs.c`'s `RAMFS_FILE_MAX 64`, silently TRUNCATING the load (dropping `/welcome`) and failing the `perm.devramfs_enforced_real_metadata` kernel test (773/779). Fixed by bumping `RAMFS_FILE_MAX` to 128 (a static BSS array, +1.5 KiB, far below the synth-dir qid base) -- a legitimate capacity pull-forward for the coreutils. **(2) reap-before-read deadlock** -- reading a child's piped stdout to EOF then `wait()` (the std order) DEADLOCKS on Thylacine because a Proc's fds close at REAP (`proc_free`/`wait_pid`), NOT at exit (the #844 handle-lifetime design): the child's stdout write-end stays open until reaped, so the read blocks for an EOF only the reap produces. Fixed in the driver by REAPING FIRST (the child writes its output into the 4096-byte pipe buffer + exits; `wait()` closes the write-end; THEN the read drains the buffer + EOF) -- correct for output <= `PIPE_BUF_SIZE`; the streaming-large-output ergonomic gap is tracked (#926). **Deferred (tracked #925)**: ls (needs `fs::read_dir`), mkdir/touch/rm/rmdir (fs-mutation helpers), tee (post-pivot file write), stat/date/id/whoami/ns/cap/ln/readlink/env/which/uname/hexdump/cmp/realpath/yes (each its own gap). **Locked decisions**: (1) single `coreutils` crate w/ `src/bin/*` (vs 16 crates) -- idiomatic for a suite, minimal wiring; (2) batch = the pure filters + argv tools verifiable PRE-pivot in one driver (file-mutation tools need post-pivot, deferred); (3) reap-first in the driver (vs changing the kernel's reap-time fd close -- out of scope + the audited #844 design); (4) `RAMFS_FILE_MAX` bump is the right fix (the coreutils belong in the boot cpio). | `26d55c1` | coreutils + driver `cargo check` clean; kernel suite **779/779 PASS** (the RAMFS fix restored `/welcome`); `make test-tcg` boot pass on two consecutive runs; `coreutil-smoke: all OK (28 checks)` (every coreutil spawned + I/O-asserted) + `joey: coreutil-smoke ok` + `Thylacine boot OK`; 0 EXTINCTION; **ci-smp-gate PASS** (ubsan-smp4 + default-smp8, N=10, 0 CORRUPTION; 3 benign host-timing flakes classified) -- the kernel `RAMFS_FILE_MAX` bump + the coreutils + reap-first are SMP-sound |
| U-6e-pre-a hash fixup | Pinned the U-6e-pre-a impl hash (`6b0cf6b`). | -- | -- |
| U-6e-pre-a | _(impl SHA `6b0cf6b`)_ Native-argv runtime + std-stream handles in libthyla-rs -- **the U-6e prerequisite; first sub-chunk of the aux-coreutils adoption arc; the FIRST runtime execution of the native-argv path (a milestone)**. Promotes the two surfaces the aux track's `aux-rt` worked around (DOC-GAP-REPORT G03 + G05) INTO `libthyla-rs` proper, so native argv-taking programs (the coreutils) can drop aux-rt. **G03 (argv):** `usr/lib/libthyla-rs/src/lib.rs`'s `_start` now captures the kernel-built System-V startup frame (`exec.h` "Shape A / Shape B") -- `ldr x0,[sp]` (argc) + `add x1,sp,#8` (&argv[0]) BEFORE any prologue moves sp -- and tail-calls a Rust shim `__libthyla_rt_start(argc, argv)` that records both into process-global `AtomicUsize`/`AtomicPtr` statics (Release) then calls the app's `rs_main()` (unchanged signature -- backward-compatible: existing binaries that ignore argv are untouched; the captured argv is reached only via the accessor, never via rs_main params). Both startup shapes share the same capture (Shape A puts argc=0 at `[sp]`). New `pub mod env` with `env::args() -> Args` (Acquire-loads the statics; `Args` is `Copy`, byte-slice-based, `&'static` since the frame lives at the top of the user stack above the initial sp for the whole process lifetime), `Args::{len,is_empty,prog_name,get,get_str,operands}` + `Operands` (argv[1..]) + `impl Iterator`. **G05 (std streams):** `io::{stdin,stdout,stderr}` non-owning handles over `t_read`/`t_write` on fd 0/1/2 (do NOT close the fd on drop -- unlike `fs::File`, which would close inherited stdout); `io::copy(r,w)`; crate-root best-effort `print!`/`println!`/`eprint!`/`eprintln!` macros (swallow the unwired-fd-1 error per the G06 standalone caveat); `fs::File::from_raw_fd(fd, rights)` (unsafe; OWNS the fd, for wrapping a `t_walk_create`/`t_pipe` raw fd -- explicitly NOT for 0/1/2). **Milestone proof:** new native probe `usr/argv-smoke` (no aux-rt, no pouch) -- joey spawns it via the binary-agnostic `pouch_smoke_one_argv` harness with `argv=[argv-smoke,alpha,beta-2]` (argc=3) + a pipe as fd 0/1; argv-smoke reads its own argv through `env::args()`, echoes argc + every argv[i] to `io::stdout()` (fd 1, the pipe), and self-checks (exit 0 iff the argv matches). joey content-checks the per-position markers (`argv-smoke: argc=3` / `argv[0..2]=...` / `native argv OK`) AND the exit status; `do_native_argv_smoke()` gates the boot. This proves G03 (argv read) + G05 (stdout write) end-to-end through libthyla-rs -- before this the capture had only been disassembly-verified (the aux-rt note). **Locked decisions**: (1) `_start`-captures-into-globals + `rs_main()` unchanged (vs a naked-`rs_main` or an `rs_main(argc,argv)` signature change) -- backward-compatible across the whole native fleet; (2) Release/Acquire on the statics (single-write-at-startup; cheap insurance for a peer-thread `args()` read); (3) non-owning stdio handles (a `File` over fd 1 would close stdout on drop); (4) `from_raw_fd` is `unsafe` + owning, documented not-for-0/1/2; (5) NOT audit-bearing -- userspace EL0 startup path, no kernel privilege boundary or new concurrency; rigor = self-audit + the whole-native-fleet boot. Doc 38-userspace entry contract updated (G03/G04 fix) + the "Native argv + std streams" section. | `6b0cf6b` | libthyla-rs `cargo check` clean; usr workspace `cargo build --release` clean (argv-smoke + joey + the whole native fleet; only the pre-existing burrow-torture warning); kernel suite **779/779 PASS**; `make test-tcg` boot pass on two consecutive runs; `argv-smoke: argc=3` + `argv[0]=argv-smoke` + `argv[1]=alpha` + `argv[2]=beta-2` + `native argv OK` + `joey: argv-smoke ok (...first native-argv exercise)` + `Thylacine boot OK`; 0 EXTINCTION |
| U-6d-b hash fixup | Pinned the U-6d-b impl hash. | `50fe2c9` | — |
| U-6d-b | _(impl SHA `cc8b88d`)_ Shell I/O redirection (`<` `>` `>>` heredoc) -- **second sub-chunk of the U-6d arc; closes the redirect half of pipes+redirection**. Lifts the two redirect `NotImplemented` seams in `usr/utopia/libutopia/src/eval/stmt.rs`: the single-command `NotImplemented("redirects")` (now `exec_external_redirected`) and `exec_pipeline`'s per-element `NotImplemented("redirect on pipeline element (U-6d-b)")`. **The model**: the shell OPENS each redirect target and HANDS the fd to the child (`Stdio::File`); it does no bulk I/O itself (so Loom -- the async 9P DATA path -- is not the right layer here; that is the child's concern; the one shell-side write is the heredoc body into a kernel pipe, a plain SYS_WRITE). New `resolve_redirects(env, &[Redirect]) -> Result<ResolvedStdio, RedirError>` opens every target UP FRONT (a failure aborts before any spawn). Per kind: `< f` -> `File::open` at stdin; `> f` -> `OpenOptions::write+create+truncate` at stdout; `>> f` -> `...+append` at stdout; `<< TAG` -> render the body + a kernel pipe (rd at stdin, wr fed post-spawn then dropped for EOF). Last-wins per fd (a later same-fd redirect drops the earlier `Stdio::File`, closing it -- `> a > b` truncates a, writes b). `RedirError::Eval` propagates as an eval error (NotImplemented/Internal); `RedirError::Runtime` (ambiguous target / open failure) sets `$status`+`$errstr`, returns Normal. A redirect on a fn -> `NotImplemented("redirect on function (U-6f)")`; on a non-Simple in-process command -> `NotImplemented("redirect on in-process command (U-6f)")` (defensive -- the parser only attaches redirects to Simple commands); a bare redirect with no command (`> f`) opens/truncates the target then runs nothing (status 0). In `exec_pipeline` a per-element redirect OVERRIDES the pipe wiring for that fd (the displaced pipe end is dropped so the peer sees the right EOF); heredoc bodies are fed after the spawn-all barrier. **Dependency pulled forward (own surface)**: `libthyla_rs::fs` create-or-open + append. `OpenOptions::{create,create_new,append}` were `NotImplemented` stubs; now `File::open_create_at_path` drives the FS-alpha `SYS_WALK_CREATE` (`t_walk_create`). The create path is CREATE-FIRST (mirrors joey's `mkdir_or_open`): `t_walk_create` is exclusive, and the kernel's `walk_open` returns no distinguishable not-found code, so the robust create-or-open is "try create; on failure open the existing file (with truncate when requested)" -- never depends on a missing-file errno. `File::create` becomes std-shaped (create-or-open-truncate). `append` = open/create for write + seek-to-end-at-open (Plan 9 omode has no O_APPEND; exact for the single-writer shell case -- the Spoor offset is shared into the child via the handle bump, so the child's writes continue from end; concurrent atomic-append is a v1.x kernel surface). `OpenOptions::mode(m)` sets the create mode (default 0644). **Folded-in lexer fix**: `scan_heredoc_start` now skips horizontal whitespace between `<<`/`<<-` and the tag, so `cat << EOF` works as well as `cat <<EOF` (the universal heredoc form). **Locked design decisions**: (1) shell opens-and-hands-off, never does redirect I/O (Loom stays the child's data-path concern; #916 FS-open-via-Loom remains a tracked v1.x seam, user-confirmed); (2) up-front open (abort-before-spawn on any target failure -- a v1.0 simplification vs bash's per-element granularity, documented); (3) create-first create-or-open (robust to the kernel's generic walk_open miss code); (4) append = seek-to-end-at-open (single-writer-exact); (5) a redirect on a fn/in-process command is NotImplemented at v1.0 (no fd without fork; U-6f); (6) heredoc bodies are small at v1.0 (a body exceeding the pipe buffer fed to a non-reading command blocks -- documented). **Tests**: `alloc-smoke` create/append negative tests updated (the stub is gone -> they now assert create/append REACH the FS and fail appropriately on the read-only devramfs, not `NotImplemented`). New `flow_eval_redirect` in `/u-test` (7 pre-pivot probes, devramfs read-only: heredoc->pipe-sink exact; heredoc `$x` interpolation; `<<"EOF"` no-interp; redirect-on-fn NotImplemented; ambiguous multi-word target -> status+errstr; `>` create-failure on read-only devramfs -> status+errstr; bare-redirect status 0). New post-pivot binary `/u-redir-test` (joey-spawned on the writable dev9p root): the real `>`/`>>`/`<` round-trip -- `pipe-src > f` create+write -> read-back == payload; `pipe-src >> f` append -> read-back == payload x2; `pipe-src > f` truncate -> read-back == payload; `pipe-sink < f` -> exit 0. pipe-src/pipe-sink resolve from the devramfs cpio regardless of pivot (`devramfs_lookup` is unconditional in the spawn handlers). Wired into the usr workspace + `tools/build.sh` usr_rs_bins (cpio 50 -> 51) + joey post-pivot spawn+reap. `u-test` now reports **16 cross-module flows** (`u-test: eval redirect OK`). Reference doc 94 extended. | `cc8b88d` | usr workspace `cargo build --release` clean (both host + aarch64-unknown-none; only the pre-existing burrow-torture warning); kernel suite **779/779 PASS**; `tools/test.sh` (TCG) boot pass on two consecutive runs; `u-test: eval redirect OK` (7 probes) + `u-redir-test: all OK` + `joey: /u-redir-test reaped status=0; U-6d-b redirects verified` + `Thylacine boot OK`; 0 EXTINCTION |
| U-6d-a hash fixup | Update U-6d-a row with the impl's hash. | *(pending)* | — |
| U-6d-a | _(impl SHA `24eafac`)_ Multi-element pipeline -- **first sub-chunk of the U-6d arc (pipes + redirection)**. Lifts `eval_pipeline`'s `p.elements.len() > 1` NotImplemented branch via new `exec_pipeline(env, p)` in `usr/utopia/libutopia/src/eval/stmt.rs`. For `cmd1 | cmd2 | ... | cmdN`: (1) validate every element is a redirect-free EXTERNAL Simple command -- a fn / BraceBlock / Arith / Subshell as a pipeline stage returns `NotImplemented("non-simple command in pipeline")` or `NotImplemented("function as pipeline element")` (in-process eval has no fd to wire to a pipe without fork; U-6f+); a per-element redirect returns `NotImplemented("redirect on pipeline element (U-6d-b)")`. All argvs are evaluated UP FRONT so any rejection aborts before a single spawn. (2) Allocate N-1 pipes via `libthyla_rs::process::pipe()`; assign pipe j's write end to element j's stdout (`Stdio::File`) and read end to element j+1's stdin; the OUTER ends (element 0 stdin, element N-1 stdout, every stderr) get the U-6c v1.0 convention (`Stdio::Piped` + immediate parent-side drop, since the shell has no terminal-backed fd 0/1/2 yet). (3) Spawn ALL elements before the first wait (waiting between spawns deadlocks `producer | consumer` -- producer blocks on a full pipe while the parent blocks before spawning the drainer). (4) Reap via wait-any + pid-match: SYS_WAIT_PID reaps any zombie, so we collect the N spawned pids and match each reaped pid to its element index; at v1.0 there are no background children (U-7) so exactly N waits reap the pipeline. (5) Aggregate `$status` via new pure `pub fn aggregate_pipefail(&[(i32, bool)]) -> i32` per scripture 8.4: the RIGHTMOST non-zero exit among elements NOT marked `?|` (the `?|` pipe-modifier sets `tolerate_failure` on the LEFT element, whose status is then ignored), or 0 if every non-tolerated element succeeded. **v1.0 binary exit status (kernel limitation surfaced during impl)**: the kernel's `sys_exits_handler` normalizes ANY non-zero child exit to `exit_status = 1` (x0==0 -> exits("ok")/0; x0!=0 -> exits("fail")/1; the literal exit code is LOST -- a richer per-Proc u64 status is a Phase 5+ deferral per the kernel comment). So a real pipeline element's reaped status is 0 or 1, never its literal code. This is why the rightmost-VALUE distinction in pipefail is validated by the pure-function probe with SYNTHETIC statuses (7/3 -> 3), not by real binaries (which can only yield 0/1). It also means U-6c's exec_external `status_set(ExitStatus::raw())` yields 0/1 for any real command, not the literal code -- documented as a v1.0 caveat. **Stdio direction correctness**: `Stdio::File(end)` installs a specific pipe end at the child slot, then libthyla-rs drops the parent's copy after the spawn returns -- so once an upstream element exits, the downstream reader sees EOF (the parent holds no lingering write-end copy that would keep the pipe open). **Two new native test fixtures** (`usr/pipe-src/` + `usr/pipe-sink/`): pipe-src writes the fixed 13-byte payload `"PIPE-DATA-OK\n"` to fd 1; pipe-sink reads fd 0 to EOF and exits 0 iff it got exactly that payload, else 1. Needed because (a) hello-rs writes via SYS_PUTS (never fd 1) so it can't source pipe data, and (b) native binaries can't read argv at v1.0 (libthyla-rs `_start` calls `rs_main()` with no args) so a single multi-mode fixture isn't possible. Wired into `usr/Cargo.toml` workspace members + `tools/build.sh` usr_rs_bins; cpio 40 -> 42 entries. **Locked U-6d-a design decisions**: (1) pipeline elements restricted to external Simple commands at v1.0 -- in-process stages need fork; (2) all-spawn-before-wait (deadlock-freedom); (3) wait-any + pid-match reaping (SYS_WAIT_PID is wait-any; pid-match is order-independent + defensive against spurious reaps); (4) `aggregate_pipefail` is a pure exported fn -- the testable core of pipefail, boot-probed with synthetic statuses since real exits collapse to 0/1; (5) outer ends use U-6c's Piped+drop convention (flips to Inherit at U-PTY); (6) `Stdio::File` for inner pipe ends (kernel installs + parent auto-drops -> correct EOF propagation); (7) two fixtures (src + sink) rather than one, since no-argv + SYS_PUTS-only-hello-rs rule out a single multi-mode binary. Extended `/u-test` with `flow_eval_pipe` (8 boot probes: 2-element mechanics; 3-element mechanics; `pipe-src \| pipe-sink` REAL byte transfer -> 0; `hello-rs \| pipe-sink` EOF + real non-zero through pipefail -> 1; aggregate_pipefail rightmost-non-zero synthetic; aggregate_pipefail `?\|` tolerate synthetic; background pipeline -> NotImplemented; fn-in-pipeline -> NotImplemented). u-test now reports **15 cross-module flows**; new `u-test: eval pipe OK` line between `u-test: eval exec OK` and `u-test: all OK`. Reference doc 94 §10 + §9 extended. ~150 LOC stmt.rs + 2 fixtures (~95 LOC) + ~170 LOC u-test. | `24eafac` | usr workspace `cargo build --release` clean (19 binaries; no warnings; both host + aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs; new joey-reported `u-test: eval pipe OK` line; 8 pipeline-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path (incl. real byte transfer pipe-src->pipe-sink + EOF-through-pipefail); cpio 40 -> 42 entries; u-test now reports 15 flows; all PASS |
| U-6c hash fixup | Update U-6c row with the impl's hash. | *(pending)* | — |
| U-6c | _(impl SHA `c2661a8`)_ External command spawn -- **third sub-chunk of the U-6 evaluator arc**. Lifts `eval_command`'s SimpleCommand `NotImplemented("external command spawn or built-in")` branch via new `exec_external(env, &argv, span)` in `usr/utopia/libutopia/src/eval/stmt.rs`. When argv[0] does not resolve to a defined fn (and built-ins land at U-6e), exec_external builds a `libthyla_rs::process::Command`, sets all three Stdio slots to `Piped` (v1.0 convention), spawns, drops the parent-side pipe ends, waits, and reflects the result in `$status` + `$errstr`. **Status semantics**: clean reap -> `ExitStatus::raw()` (so a child exiting 0 is status=0); spawn failure -> 127 + `$errstr = "spawn failed: <Error>"` (bash command-not-found convention); wait failure -> 1 + `$errstr = "wait failed: <Error>"`. All three paths return `Ok(StatementFlow::Normal)` -- a runtime non-zero exit is not an eval-time error; the caller's implicit-fail discipline handles propagation upward. **Trace echo (closes U-6b deferral)**: when `env.trace_depth > 0`, `trace_echo(argv)` emits `+ argv[0] argv[1] ...\n` via `t_putstr` (kernel UART, matching the rest of the shell's v1.0 diagnostic output) BEFORE the spawn. When fd 2 (stderr) is wired via U-PTY + U-6g, the sink should switch to fd 2. Bash's `set -x`-style argv quoting is a v1.x refinement; v1.0 just space-separates. **Stdio inheritance (the U-6c design decision driving Stdio::Piped)**: `Stdio::Inherit` would tell the kernel to install the parent's fd 0/1/2 into the child's handle table, but the shell at v1.0 has no terminal-backed fd 0/1/2 (joey/u-test/ut all write via SYS_PUTS direct to the UART; joey spawns u-test via `t_spawn` which inherits no fds). The kernel's `SYS_SPAWN_FULL_ARGV` then rejects the spawn (-1 -> Error::NotPermitted in the libthyla-rs errno mapping) because the parent's handle table has no entry at those slots. Until U-PTY + U-6g land fd 0/1/2 wiring, exec_external uses the established v1.0 convention (matches `alloc-smoke` + `u-test::flow_process_pipe`): `Stdio::Piped` on all three slots followed by an immediate drop of every parent-side pipe end. The child gets kernel-installed pipes at 0/1/2 (so the kernel accepts the spawn); native binaries (SYS_PUTS-writers) don't touch them; ported binaries get EOF on read / EPIPE on write when they try -- which IS the correct v1.0 behavior for a shell without a terminal. When PTY lands, this whole block flips to `Stdio::Inherit` cleanly. **Subshell + background still deferred**: Subshell `( cmds )` returns `NotImplemented("subshell ( cmds )")` -- libthyla-rs has spawn-then-exec, not fork; Subshell requires a forked SHELL that runs the body in the child against an isolated Env; lands at U-6f or later. Background `&` returns `NotImplemented("background pipeline (&)")` -- needs job control wiring (U-7). **Built-ins still deferred**: at U-6e the dispatch chain becomes: fn lookup -> built-in lookup -> exec_external. **Argv glob fs expansion still deferred**: libthyla-rs has no `ReadDir` surface yet (per `usr/lib/libthyla-rs/src/fs/mod.rs` "deferred -- kernel directory-read mechanism not yet exposed"); U-6e lands both built-ins AND fs glob walks. The glob pattern matcher in libutopia is unchanged (case + `matches` operators continue to work; only fs-walk is the new add at U-6e). **Locked U-6c design decisions**: (1) `Stdio::Piped` + immediate drop (not `Stdio::Inherit`) for v1.0 -- the shell has no fd 0/1/2 to inherit yet; piped+drop is the established convention in alloc-smoke + flow_process_pipe; (2) status=127 on spawn failure (bash command-not-found convention) -- a single status for all spawn errors keeps the userspace contract simple; finer distinguishment between NotFound / PermissionDenied / InvalidArgument lands when libthyla-rs's Error -> rc errstr translation gets a proper boundary-line (v1.x); (3) status=1 on wait failure -- distinct from spawn failure (127) and from a clean non-zero exit (the child's own status); (4) `trace_echo` writes via `t_putstr` (UART) at v1.0 -- the shell's diagnostic sink. When fd 2 wires through line discipline, switch to fd 2; (5) Argv quoting in trace echo is space-only at v1.0 -- bash's `set -x` quoting (single-quoting args with embedded whitespace, escaping `$` and backslash) is a v1.x refinement; (6) Subshell stays NotImplemented at U-6c -- the fork primitive needed isn't available; (7) PATH search is NOT added at U-6c -- argv[0] is treated as a single-component name and passed to `Command::new`; the kernel resolves against devramfs OR the pivoted root; PATH search + path-style invocation (`/bin/ls`) lands at U-6e built-ins (`type` / `which`). Extended `/u-test` with `flow_eval_exec` (7 boot-time probes: hello-rs spawn-and-reap status=0; spawn-failure name "noenoe" -> status=127; trace { hello-rs } body executes + trace_depth restores + trace echo `+ hello-rs` reaches log; background `hello-rs &` -> NotImplemented(background); subshell `( hello-rs )` -> NotImplemented(subshell); fn-shadowing `fn mycmd ...; mycmd` -> fn runs, status=0; spawn-failure errstr begins "spawn failed"). u-test now reports **14 cross-module flows**; new `u-test: eval exec OK` line between `u-test: eval stmt OK` and `u-test: all OK`. Reference doc 94 status section extended. ~95 LOC stmt.rs additions + ~170 LOC u-test extension. | `c2661a8` | usr workspace `cargo build --release` clean (17 binaries; no warnings; both host + aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs; new joey-reported `u-test: eval exec OK` line; 7 spawn-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; trace echo `+ hello-rs` confirmed in boot log; hello-rs banner appears 5 times in log (joey + alloc-smoke + flow_process_pipe + probe 1 + probe 3); u-test now reports 14 flows; all PASS |
| U-6b hash fixup | Update U-6b row with the impl's hash. | *(pending)* | — |
| U-6b | _(impl SHA `0dc28d9`)_ Statement evaluation -- **second sub-chunk of the U-6 evaluator arc**. New `usr/utopia/libutopia/src/eval/stmt.rs` (~620 LOC) with `StatementFlow` (Normal/Return/Break/Continue), `eval_statement(env, stmt) -> EvalResult<StatementFlow>`, `eval_block(env, stmts)`, `eval_script(env, script)`, and the convenience `eval_source(env, src)` (parse + eval in one call). Implements every StatementKind except external SimpleCommand: Let / Assign / FnDecl / Return / Break / Continue / If (with elif chain + else) / While / For / Case (statement form -- glob-match patterns; first-match-wins; no-match returns Normal + status=0 unlike case-as-expression's NoCaseMatch) / Try / Trace / OnNote / MaskNote. **Pipeline** at U-6b supports single-element only: CommandKind::Simple where argv[0] resolves to a defined fn (invoke_function: push_scope, bind $0=name + $1..$N positional + $* full-args-list + FnDecl::args by name, eval_block, pop_scope; Return normalizes to Normal at call site; unconsumed Break/Continue across function boundary likewise) OR CommandKind::BraceBlock (executes in current scope per scripture 5.6 -- no fork) OR CommandKind::Arith (eval expr; status=0 if non-zero result else 1 per rc convention). Multi-element pipeline / external SimpleCommand / Subshell / background `&` / redirects -> NotImplemented (U-6c/d). **Implicit-fail** (scripture 8.1 + 8.3 + 8.9): `eval_block` checks after each Normal-returning statement -- if `$status != 0` AND `should_propagate_failure(env, stmt)` returns true (Pipeline first-element has `fail_propagate` set OR `!env.interactive && implicit_fail_suppressed == 0`), returns StatementFlow::Return. `Env::interactive` defaults to false (script mode); U-6g main loop sets true. **Try/Catch** (scripture 7.7 + 8.6): increments `implicit_fail_suppressed` around body; on body Err OR post-body $status != 0, sets $errstr from `EvalErrorKind::Display` + $status=1, runs catch. **Trace** (scripture 7.8): increments `trace_depth` around body; actual echo-to-stderr deferred to U-6c when argv-to-stderr surface lands alongside external commands. **OnNote / MaskNote** (scripture 9.5 + 10.7 + 10.8): OnNote registers handler in `Env::note_handlers` (BTreeMap by name; last-write-wins); MaskNote pass-through executes body (runtime masking + handler delivery wires at U-7 alongside note-fd polling). **Word -> argv expansion**: new `eval_word` + `eval_value_token` helpers mirror `eval_expr`'s atom dispatch on Token-shaped Word nodes (since `Word` holds raw Tokens, not Exprs); cross-product Concat for `Word::Concat`. **For-loop element binding**: each iteration sets `var_name -> Value::scalar(element)` (rc-style: loop var is a scalar even if the list has multi-word elements); var persists after loop. **Env extensions**: `interactive: bool`, `implicit_fail_suppressed: u32`, `trace_depth: u32`, `note_handlers: BTreeMap<String, Vec<Statement>>` + `note_handler_set` / `note_handler_get` / `note_handler_defined` accessors. New `eval_source` returns `EvalErrorKind::Internal("parse failed")` on parse-time error with the formatted `ParseErrorKind` placed in `$errstr` via Debug format. **Locked U-6b design decisions**: (1) StatementFlow as enum (not bool/i32) -- explicit Return/Break/Continue signals are clearer and self-document the flow contract; (2) Implicit-fail check in eval_block (not eval_statement) -- centralizes the post-statement bookkeeping at one point; (3) `Env::interactive` default false -- script mode is the strict default; the REPL opts in; (4) Function-call normalizes unconsumed Break/Continue to Normal -- prevents leakage; rc convention; (5) Case-statement no-match returns Normal+status=0 (NOT NoCaseMatch error) -- scripture is permissive on no-match in statement form; case-as-expression remains strict; (6) Arith command status=0 iff result != 0 -- rc/bash convention (zero is "false"); (7) MaskNote pass-through at U-6b -- runtime masking is a U-7 concern alongside note-fd polling; the body still executes so any side-effects fire; (8) `eval_word` mirrors `eval_expr` for Token-shape atoms rather than building synthetic Exprs -- cleaner code path with no Token-to-Expr lift for Subst/Backtick (which would need re-parsing); (9) `eval_source` uses `EvalErrorKind::Internal("parse failed")` for parse errors -- the static tag identifies the source; the formatted error goes to `$errstr` via Debug. **Known v1.x deferral**: a list literal `(a b c)` inline inside a `for` header (`for (x in (a b c)) { ... }`) currently triggers a parse-time `UnexpectedEof { expected: "`)`" }` error from `parse_list_literal`. The `for (x in $xs) { ... }` idiom with a pre-bound list var works cleanly (and is the common case). Investigation deferred -- low-priority since the working idiom is what users will reach for; the inline list pattern is uncommon in shell scripts (lists are typically named). Reference doc 94 status section extended. Extended `/u-test` with `flow_eval_stmt` (14 boot-time probes runtime-validated under ThylaAlloc: Let / bare-assign-to-global from fn body / fn with positional args (let-scope isolation verified) / if-elif-else / while+break (counts to 3) / for-loop (last-element after iter) / case-stmt match + no-match-Normal / try/catch on Pipeline-arith status=1 / try/catch on eval error (DivByZero) with errstr set / trace body executes + trace_depth scopes / on-note registers handler / brace-block shares scope / script-mode implicit-fail propagates Return up). u-test now reports 13 cross-module flows; all PASS. | `0dc28d9` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new stmt module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs (bimodal QEMU TCG variance confirmed transient); new joey-reported `u-test: eval stmt OK` line between `u-test: eval expr OK` and `u-test: all OK`; 14 statement-flow probes validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 13 flows; all PASS |
| U-6a hash fixup | Update U-6a row with the impl's hash. | *(pending)* | — |
| U-6a | _(impl SHA `3be8cba`)_ Evaluator scaffold + value model + expression evaluation for the no-side-effect subset -- **first sub-chunk of the U-6 arc; opens the evaluator sub-arc of Phase 7**. New `usr/utopia/libutopia/src/eval/` module with `{mod, value, env, error, glob, expr}.rs`. **Public surface**: `Value(pub Vec<String>)` (the unified-list value model -- a "scalar" is a one-element list per scripture sections 4.1 + 4.2 + 6.3; collapses bash/zsh's scalar-vs-array distinction into one Vec representation, matching rc; argv expansion + `$"var` join + `$#var` length emerge naturally); `Env` (variable scope stack via `Vec<BTreeMap<String, Value>>` with `let_set` writing the topmost frame, bare `assign` walking top-down to the first defining frame else global per scripture 6.2 dynamic scoping; function table `BTreeMap<String, FnDecl>`; `$status` / `$errstr` / `$cwd` accessors; `push_scope` / `pop_scope` for U-6b function call entry/exit); `EvalError { kind, span }` + `EvalErrorKind` (NonNumeric / DivByZero / Overflow / InvalidShift / NoCaseMatch / InvalidIndex (reserved) / NotImplemented / Internal); `EvalResult<T>` alias; `eval_expr(env, expr) -> EvalResult<Value>` recursive walker. **Implemented ExprKind variants**: atoms (Word + SingleQuoted + DoubleQuoted with $var + $#var interp + Integer + Var + VarLen + VarNoSplit + VarIndex 1-indexed + VarSlice inclusive); composites (List flattening + Concat with rc-style cross-product semantics); operators (full BinOp arith -- Add/Sub/Mul/Div/Mod/Pow/BitAnd/BitOr/BitXor/Shl/Shr -- via checked arithmetic; logical Or/And short-circuit; comparison Lt/Le/Gt/Ge/Eq/Ne with numeric-first-else-string rule collapsing scripture 6.13 + 6.14); UnOp (Neg + BitNot + Not); Match (Glob via pattern matcher + In via list membership; Regex returns NotImplemented); Case-as-expression (first-match-wins via glob; NoCaseMatch error if exhausted). **Deferred (return NotImplemented at v1.0)**: Subst/Backtick/ProcSubIn/ProcSubOut (need spawn -- U-6f); Match::Regex (need regex engine -- v1.x). **New sibling module `glob`**: rc/POSIX-shape pattern matcher (`*` no-`/` + `?` no-`/` + `[abc]` + `[!abc]` + `[a-z]` ranges; bytewise UTF-8-safe). NO filesystem walks -- pattern match only, used by case-as-expression + `matches` operator; argv-time glob expansion lands at U-6e. **Locked U-6a design decisions**: (1) Value as unified `Vec<String>` (not `enum { String, List }`) -- every consumer would match-then-collapse anyway; scripture preserves the conceptual distinction but rc's representation is uniform; (2) `BTreeMap` (not `HashMap`) for scope frames -- `alloc::collections::BTreeMap` is in no_std + alloc; HashMap would require hashbrown; per-frame counts are small so O(log n) is negligible; (3) Undefined variable -> `Value::empty()` (rc convention; scripture silent); (4) Truthiness convention pinned (empty / "" / "0" / "false" -> false; else true) -- scripture silent on expression-value truthiness; (5) Comparison: numeric if both parse as i64, else lexicographic string -- collapses scripture 6.13 + 6.14; (6) Arithmetic overflow -> error (not wrap/saturate) -- scripture silent; latent bugs should surface at evaluation; (7) Out-of-range / negative-index VarIndex -> empty (rc); `InvalidIndex` variant reserved for strict mode; (8) NotImplemented carries a static string for clear error formatting; (9) Boot probe `ev()` helper strips the trailing Eof token -- mirrors parse.rs's discipline at each placeholder site (parse_expr_tokens reports `TrailingTokensInExpr` if Eof remains). **Test posture**: cfg(test) host unit tests (9 in glob.rs + 26 in expr.rs; can't run on host directly because libutopia is no_std with no panic_handler -- they document expected behaviour); /u-test extended with 15-probe `flow_eval_expr` runtime-validated under ThylaAlloc on real arm64 (Integer in Arith + arith precedence + DivByZero + Var lookup + Undefined empty + VarLen + VarIndex 1-indexed + VarIndex OOR + VarSlice + VarNoSplit join + List flatten + Concat with var + DoubleQuoted $var+$#var interp + case-as-expression first-match + Subst+Regex NotImplemented). New reference doc `docs/reference/94-utopia-eval.md`. ~960 LOC eval (value 165 + env 220 + error 110 + glob 245 + expr 220) + ~480 LOC cfg(test) tests + ~310 LOC u-test extension. u-test now reports 12 cross-module flows; all PASS. | `3be8cba` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new eval module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs (bimodal QEMU TCG variance confirmed transient); new joey-reported `u-test: eval expr OK` line between `u-test: parser full OK` and `u-test: all OK`; 15 evaluator-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; 35 new `#[cfg(test)]` tests in glob + expr (host can't link but document expected behaviour); u-test now reports 12 flows; all PASS |
| U-5d hash fixup | Update U-5d row with the impl's hash. | *(pending)* | — |
| U-5d | _(impl SHA `255f80b`)_ Pattern matching + try/catch + trace + on/mask + case-as-expression -- **fourth and final sub-chunk of the U-5 arc; the U-5 parser arc is COMPLETE**. New statement-level constructs (per scripture sections 7.1 + 7.7 + 7.8 + 9.5 + 10.7 + 10.8): `case $x { pat ... => body ; ... }` (pattern matching; multi-pattern arms space-separated; `*` is the catch-all glob; first match wins; no fallthrough), `try { ... } catch { ... }` (error handling; catch is REQUIRED), `trace { ... }` (block-scoped tracing), `on note 'name' { ... }` (note handler registration), `mask note 'name' { ... }` (note delivery suppression). New expression-level construct (scripture 7.2): `case $x { pat => value ... }` as an expression -- works in every `ExprContext` (`parse_primary` for Cond/Arith Pratt path; `parse_value_top` / `parse_list_top` for non-Pratt Value/Return/List paths). **AST extensions**: `StatementKind` gains 5 variants (`Case(Box<CaseStmt>)` / `Try(Box<TryStmt>)` / `Trace(Box<TraceStmt>)` / `OnNote(Box<OnNoteStmt>)` / `MaskNote(Box<MaskNoteStmt>)`); `ExprKind` gains `Case(Box<CaseExpr>)`. New struct types: `CaseStmt` / `CaseArm` (statement form; arm body = single Statement; users wrap in `{ ... }` for multi-statement), `CaseExpr` / `CaseExprArm` (expression form; arm value = single Expr in Value context), `TryStmt` (body + catch + span), `TraceStmt` (body + span), `OnNoteStmt` / `MaskNoteStmt` (note_name as Expr + body + span). **Locked U-5d design decisions**: (1) Arm body is a single Statement -- cleaner than detecting next-arm-start ambiguity; (2) `catch` is REQUIRED after `try` -- scripture 7.7 always pairs them; `try { a }` emits `UnexpectedToken { expected: "`catch`" }`; (3) `on` / `mask` require the LITERAL `Word("note")` token after the keyword (`note` is NOT a reserved word per the U-5a taxonomy; the parser matches by text -- `on banana { ... }` errors); (4) Note names parse as any Value-context Expr (SingleQuoted / DoubleQuoted / Word / Concat all accepted; evaluator resolves to a string at registration time); (5) case-as-expression dispatched in 3 places (`parse_primary` Pratt path + `parse_value_top` + `parse_list_top` non-Pratt paths) so the AST has ONE canonical expression shape; (6) Empty pattern list (`case $x { => body }`) emits `EmptyCasePattern`; (7) Pattern list terminator is `=>` at depth 0 -- top-level Newline/Semicolon/RBrace inside pattern list is `UnexpectedToken { expected: "`=>`" }`; (8) Pattern collection reuses U-5c's span-gap whitespace-split helper, renamed from `split_index_body` to `split_value_tokens_on_whitespace` and exposed `pub(super)`. **Parser dispatch update**: `parse_statement` dispatches `Case` / `Try` / `Trace` / `On` / `Mask` to dedicated parsers (the U-5b `InvalidStatement` emissions for those keywords are removed); `Else` / `Catch` / `In` at statement-start still emit `InvalidStatement` (no surrounding context). **New `ParseErrorKind` variant**: `EmptyCasePattern`. Other shape errors continue to use `UnexpectedToken { expected }` / `UnexpectedEof { expected }` with appropriate labels. Modifications: ~500 LOC `parse.rs` additions (parse_case_stmt + parse_try + parse_trace + parse_on_note + parse_mask_note + 3 helper collectors + expect_note_keyword); ~250 LOC `expr.rs` additions (parse_case_expr + parse_case_expr_arm + 3 helper collectors); ~100 LOC `ast.rs` (5 new struct types + ExprKind::Case + StatementKind variants); ~10 LOC `error.rs` (EmptyCasePattern + Display). Reference doc 93 extended with section 8.7 covering AST extensions, public surface, pattern collection, locked design decisions, error kinds, impl file map. Extended `/u-test` with new `flow_parser_full` (12 boot-time probes runtime-validated under `ThylaAlloc`: case-stmt basic shape; multi-pattern arm; brace-block arm body; try/catch; try-without-catch error; trace; on note 'snare:int' with single-quoted name; mask note; on-without-note-keyword error; case-as-expression in let; newline-separated case arms; empty-pattern error). u-test now reports 11 cross-module flows; all PASS. | `255f80b` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new U-5d code compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser full OK` line between `u-test: parser expr OK` and `u-test: all OK`; 12 statement-shape + expression-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; 24 new `#[cfg(test)]` tests (20 in parse.rs + 4 in expr.rs); u-test now reports 11 flows; all PASS |
| U-5c hash fixup | Update U-5c row with the impl's hash. | *(pending)* | — |
| U-5c | _(impl SHA `5ecf108`)_ Expression parser for rc-shape expression contexts -- **third sub-chunk of the U-5 arc; closes the U-5b->U-5c lift**. New `usr/utopia/libutopia/src/parser/expr.rs` (~1100 LOC parser + ~500 LOC `#[cfg(test)]` tests; 35 tests). Public entry: `parse_expr_tokens(Vec<Token>, source_len: usize, ctx: ExprContext) -> ParseResult<Expr>`. Pratt-style precedence climbing over an owned token stream; eagerly parsed substitution bodies; full operator surface for arith + cond contexts; upfront arith retokenization that splits Word tokens containing operator chars (`+ - * /`) into proper Plus/Minus/Star/Slash tokens (the lexer emits these as word chars, not operator tokens -- the retokenize aligns with the Pratt climbing's token-kind expectations). **The AST now has ONE canonical shape**: every expression slot is an `Expr` (no `Vec<Token>` placeholders remain). `IfStmt.cond` / `IfStmt.elif_branches[].0` / `ForStmt.list_expr` / `WhileStmt.cond` / `LetStmt.value` / `AssignStmt.value` / `StatementKind::Return(Option<Expr>)` / `CommandKind::Arith(Expr)` all hold `Expr`. New `ExprKind` variants cover Atoms (`Word(String)` / `SingleQuoted(String)` / `DoubleQuoted(Vec<DqPart>)` / `Integer(i64)` (arith) / `Var(String)` / `VarLen(String)` / `VarNoSplit(String)` / `VarIndex(String, Box<Expr>)` / `VarSlice(String, Box<Expr>, Box<Expr>)` / `Subst(Box<Script>)` (eager) / `Backtick(Box<Script>)` (eager) / `ProcSubIn(Box<Script>)` / `ProcSubOut(Box<Script>)` / `Regex(String)`), Composites (`List(Vec<Expr>)` / `Concat(Vec<Expr>)`), Operators (`BinOp(BinOp, Box<Expr>, Box<Expr>)` / `UnOp(UnOp, Box<Expr>)` / `Match(MatchOp, Box<Expr>, Box<Expr>)`). New `BinOp` enum: Add/Sub/Mul/Div/Mod/Pow/BitAnd/BitOr/BitXor/Shl/Shr/Lt/Le/Gt/Ge/Eq/Ne/And/Or. New `UnOp`: Neg/BitNot/Not. New `MatchOp`: Glob (`matches`) / In (`in`) / Regex (`=~`). New `ExprContext`: Value / Cond / Arith / List / Return. Pratt precedence chain `parse_or → parse_and → parse_bit_or → parse_bit_xor → parse_bit_and → parse_eq → parse_rel → parse_shift → parse_add → parse_mul → parse_pow → parse_unary → parse_primary`; each level activates only in contexts where it's syntactically valid (e.g., bit ops only in Arith; logical+match in Cond+Arith; relational in Cond+Arith). **`**` (Pow) detection** uses SPAN-ADJACENT `Star Star` after retokenization (lexer doesn't emit a `**` token). **Var indexing** detects span-adjacent `LParen` after a `Var` token, splits the body on top-level whitespace via span-gap detection, parses each piece in Arith context (one piece -> `VarIndex`; two pieces -> `VarSlice`; else `InvalidVarIndex`). **Substitution body lifting** is EAGER (`$(cmd)`, `` `{cmd}` ``, `<(cmd)`, `>(cmd)` bodies all `parse(&body)` recursively at U-5c -- the AST is fully resolved; parse errors surface at parse time; the evaluator sees a uniform shape). New `ParseErrorKind` variants: `EmptyExpression` / `UnexpectedTokenInExpr` / `InvalidArithLiteral` / `InvalidVarIndex` / `TrailingTokensInExpr`. **Locked U-5c design decisions**: (1) in-place lifting (the AST has ONE canonical shape; no separate `lift_exprs(&mut Script)` pass -- closes the U-5b handoff open question); (2) eager substitution-body parse; (3) top-level operators in Value context are disallowed (`let x = 5 + 3` is `TrailingTokensInExpr`; user writes `let x = (( 5 + 3 ))`); (4) arith retokenization runs UPFRONT at parser construction; (5) implicit list allowed in List context (`for (f in a b c)` builds 3-element List); (6) empty list `()` parses to `List(vec![])`; (7) var-index range validated at evaluation time (parser only checks shape); (8) `**` requires span-adjacency on the two Star tokens. **Known v1.x deferrals**: `%` operator can't be lexed (lexer rejects `UnexpectedChar('%')`); `<<` / `>>` shift ops can't be lexed in arith (lexer tries heredoc-start); substitution body spans are body-relative (no outer-coordinate translation yet). Modifications to `parse.rs`: each placeholder construction site (`parse_if`, `parse_for`, `parse_while`, `parse_let`, `parse_assign`, `parse_return`, `parse_arith_command`) calls `parse_expr_tokens(...)` on the collected token stream. Modifications to `ast.rs`: `Vec<Token>` placeholder fields changed to `Expr`. Modifications to `error.rs`: 5 new `ParseErrorKind` variants. Reference doc 93 extended with §8.6 covering AST extensions, public API, Pratt precedence chain, arith retokenization, concat semantics, substitution body lifting, var indexing, locked design decisions, error kinds, impl file map. Extended `/u-test` with new `flow_parser_expr` (10 boot-time probes + 1 bonus, runtime-validated under `ThylaAlloc`: arith literal lift + Add; explicit list literal; if cond BinOp(Eq); for list_expr Var; VarIndex; Concat in value position; match operator `matches`; substitution body lifts to sub-Script; direct `parse_expr_tokens` API for paren grouping in arith; arith literal error for `hello`; bonus unary `!` in cond). | `5ecf108` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new parser/expr module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser expr OK` line between `u-test: parser core OK` and `u-test: all OK`; 10 expression-shape probes + 1 bonus validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 10 flows; all PASS |
| U-5b hash fixup | Update U-5b row with the impl's hash. | *(pending)* | — |
| U-5b | _(impl SHA `c94b7e9`)_ Parser core + AST for rc-shape statements -- **second sub-chunk of the U-5 arc**. New `usr/utopia/libutopia/src/parser/ast.rs` (~250 LOC: AST node types) + `parse.rs` (~900 LOC: recursive-descent parser; 37 `#[cfg(test)]` tests). Public surface: `parse(&str) -> ParseResult<Script>` (convenience: tokenize + parse) + `parse_tokens(Vec<Token>, source_len) -> ParseResult<Script>`. AST: `Script { statements, span }`, `Statement { kind, span }` with `StatementKind` variants `Pipeline / If(Box<IfStmt>) / For(Box<ForStmt>) / While(Box<WhileStmt>) / FnDecl(Box<FnDecl>) / Let(Box<LetStmt>) / Assign(Box<AssignStmt>) / Return(Option<Vec<Token>>) / Break / Continue`; `Pipeline { elements: Vec<PipelineElement>, background: bool, span }`; `PipelineElement { command, tolerate_failure: bool, span }` (`?|` sets `tolerate_failure = true` on the LEFT element per scripture section 8.4); `Command { kind, redirects, fail_propagate: bool, span }` with `CommandKind` variants `Simple(SimpleCommand) / BraceBlock(Vec<Statement>) / Subshell(Vec<Statement>) / Arith(Vec<Token>)` (Arith body stored as raw tokens for U-5c); `Word::Single(Token) | Word::Concat(Vec<Token>)` (Concat engaged only when value tokens are span-adjacent via Caret); `Redirect { kind, target: Option<Word>, span }` with `RedirectKind::Stdin / Stdout / Append / Heredoc { interp, strip_tabs, body: Vec<DqPart> }`. Heredoc body resolution: parser pre-extracts all `HeredocBody` parts at construction time into a FIFO `VecDeque<Vec<DqPart>>` (via `core::mem::take` from owned tokens; no clones); each `HeredocStart` pops the next body when parsing the redirect; `HeredocBody` tokens themselves are treated as separators during statement walking. Expression contexts (`if (cond)`, `for (var in expr)`, `while (cond)`, `let x = expr`, bare assignment value, `return expr`) all stored as raw `Vec<Token>` placeholders -- U-5c walks them into Expression trees. Parser dispatch: keyword tokens dispatch to dedicated parsers (`parse_if` / `parse_for` / `parse_while` / `parse_fn` / `parse_let` / `parse_return` / `parse_break_or_continue`); two-token lookahead detects assignment (Word(IDENT) + Equal); `Else / Catch / Case / Try / Trace / On / Mask / In` at statement-start emit `InvalidStatement`; otherwise -> pipeline. **Locked U-5b design decisions** (user-signed-off this session): (1) strict rc -- `--key=value` is `UnexpectedEqualInCommand` (users quote); (2) `^` concat requires span-adjacency on both sides -- `$a ^ $b` is three tokens; the orphan Caret causes the outer terminator check to error; (3) `case` deferred to U-5d per scripture section 19; (4) postfix `?` requires a statement terminator (`Newline` / `;` / `|` / `?|` / `&` / `}` / `)` / EOF) -- `cmd ? arg` is `UnexpectedTokenAfterFailPropagate`; (5) empty pipeline element `cmd1 \| \| cmd2` is `EmptyPipelineElement` per scripture section 17.10; (6) redirects may interleave with words; (7) `fn name args { ... }` with strict positional args at v1.0; (8) trailing `;` allowed; (9) list literal vs subshell distinguished by value-position vs command-position; (10) quoted reserved words stay strings. New `ParseErrorKind` variants: `UnexpectedToken / UnexpectedEof / ExpectedIdent / EmptyPipelineElement / UnexpectedTokenAfterFailPropagate / InvalidStatement / InvalidAssignmentName / UnexpectedEqualInCommand / UnexpectedCaret`. Every AST node carries `Span` from first-token-start through last-token-end. Reference doc 93 extended with section 8.5 "AST + parser core (U-5b)" covering the AST taxonomy + parser dispatch + locked design decisions + impl file map. Extended `/u-test` with new `flow_parser_core` (8 boot-time probes runtime-validated under `ThylaAlloc`: empty -> 0 stmts; semicolon multi-stmt; pipeline `?|` tolerate flag on left; if/elif/else; for loop; fn with args + body; heredoc body inlined into Redirect; postfix `?` error case; bonus Word::Concat). | `c94b7e9` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass; new joey-reported `u-test: parser core OK` line between `u-test: parser lexer OK` and `u-test: all OK`; 8 AST-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 9 flows; all PASS |
| U-5a hash fixup | Update U-5a row with the impl's hash. | *(pending)* | — |
| U-5a | _(impl SHA `94bff11`)_ Tokenizer for the rc-shape parser -- **first sub-chunk of the U-5 arc; opens the parser sub-arc of Phase 7**. New `usr/utopia/libutopia/src/parser/` module with `{mod, span, token, error, lexer}.rs`. Public surface: `tokenize(&str) -> ParseResult<Vec<Token>>` produces a token stream ending in a synthetic `Eof` point span. Every `Token` carries a `Span { start, end }` (byte offsets into source; UTF-8-safe slice). `TokenKind` taxonomy covers (per UTOPIA-SHELL-DESIGN.md sections 5-9): bare `Word` with glob meta chars preserved + `?` disambiguation (word char if surrounded by word chars; `Question` / `PipeTolerate` at separator pos); `SingleQuoted` with rc `''` escape; `DoubleQuoted(Vec<DqPart>)` pre-parsed into Literal/Var/VarLen/Subst parts with `\n \t \\ \" \$` escape resolution; top-level `Var($name)` / `VarLen($#name)` / `VarNoSplit($"name)`; substitutions `Subst($(cmd))` (raw body; balanced-paren scan respects quotes + escapes) + `Backtick(` `` `{cmd}` `` `)` (rc-shape, closing backtick required); process substitution `ProcSubIn(<(cmd))` / `ProcSubOut(>(cmd))`; heredocs `HeredocStart { tag, interp, strip_tabs }` (three flavors: `<<TAG`, `<<-TAG`, `<<"TAG"`) with body collection at next `Newline` (FIFO drain queue) -> `HeredocBody(parts)`; regex literal `Regex(/pat/)` (one-shot lex mode triggered by emitting `EqualTilde`, `\/` -> `/` escape resolved). Operators + punctuation: `Pipe` `PipeTolerate (?\|)` `AndAnd (&&)` `OrOr (\|\|)` `Ampersand` `Semicolon` `Newline` `LBrace` `RBrace` `LParen` `RParen` `DoubleLParen ((` `DoubleRParen ))` `Less` `Greater` `GreaterGreater (>>)` `Equal` `DoubleEqual (==)` `NotEqual (!=)` `LessEqual` `GreaterEqual` `EqualTilde (=~)` `FatArrow (=>)` `Plus` `Minus` `Star` `Slash` `Percent` `Caret (^)` `Tilde (~)` `Bang (!)` `Question (?)`. Reserved words (all 16 per scripture section 5.1): `Fn` `Let` `If` `Else` `Case` `For` `While` `In` `Try` `Catch` `Return` `Break` `Continue` `On` `Mask` `Trace` — matched against scanned word text after the scan completes; quoted forms (`'fn'`) stay strings. Errors: `ParseError { kind, span }` with kinds covering all unterminated forms (single quote, double quote, substitution, backtick, process sub, heredoc, heredoc tag, regex) + `EmptyVarName` / `EmptyVarLenName` / `EmptyVarNoSplitName` / `InvalidHeredocStart` / `UnexpectedChar`; single-error-stop semantics per scripture section 19 open question #2. UTF-8 aware throughout (lex advances by whole UTF-8 chars; non-ASCII bytes are word chars). Comments (`#` to EOL) and line continuation (`\<nl>`) skipped silently. Free helpers `find_balanced_paren_close` + `find_balanced_brace_close` track nesting + respect single + double quotes + `\` escapes; shared between the main lex path and the `parse_dq_or_heredoc_body` helper that parses both DQ string interiors and interp heredoc bodies. **Deferred to U-5b/c/d**: parser core (U-5b — commands, pipelines, control flow, AST node types); expression layer (U-5c — variable refs with index/slice, arithmetic re-interpretation inside `(( ))`, concatenation); pattern matching + match operators + final glue (U-5d). Lexer-mode-awareness for arithmetic (`<<` as shift-left, `^` as XOR, etc.) deferred to U-5c. ~900 LOC lexer (across 5 files) + ~560 LOC `#[cfg(test)]` host unit tests (49 tests covering every load-bearing path). New reference doc `docs/reference/93-utopia-parser.md`. Extended `/u-test` with new `flow_parser_lexer` (7 boot-time probes runtime-validated under `ThylaAlloc`: empty input, multi-word + `Pipe`, reserved `Let`, `DoubleQuoted` canonical example from scripture section 6.5, heredoc body collection, regex literal after `=~`, error case + span tracking). | `94bff11` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new parser module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser lexer OK` line between `u-test: line editor OK` and `u-test: all OK`; 7 cross-construct probes validated end-to-end on Thylacine target via ThylaAlloc heap path |
| U-4d hash fixup | Update U-4d row with the impl's hash. | *(pending)* | — |
| U-4d | _(impl SHA `9a3a4fb`)_ Tab completion via pluggable `CompletionSource` trait -- **fourth and final sub-chunk of the U-4 arc; the line editor sub-arc is COMPLETE**. New `CompletionSource` trait + `Completions { replace_range, candidates }` struct + shipped `StaticCompletionSource` (word-prefix matcher; preserves source order; used by tests + early-bringup until U-6 plugs the real shell-driven source). New `longest_common_prefix(strs: &[String])` UTF-8-safe helper (returned byte length rounded DOWN to nearest char boundary). New public `LineEditor::set_completion_source(source: Box<dyn CompletionSource>)` + `clear_completion_source()` setters. New `EditorAction::ShowCompletions(Vec<String>)` variant emitted when Tab dispatches with 2+ candidates AND no further common-prefix extension is possible -- the main loop displays the candidate list (typically newline-separated below the prompt) then redraws. Tab key (Ctrl-I, byte 0x09) now maps to `Action::Complete` (was `Action::Ignore` at U-4a; the existing `tab_is_ignored_at_u4a` test renamed to `tab_with_no_completion_source_is_noop`). Dispatch: 0 candidates -> NoChange; 1 candidate -> replace `buffer[comp.replace_range]` with the candidate + cursor to end of replacement + Redraw; 2+ candidates with `common_prefix.len() > current_word.len()` -> extend buffer to common prefix + Redraw; 2+ with no extension -> `ShowCompletions(comp.candidates)`. Tab in search mode falls through to do_search_cancel (the default catch-all in apply_in_search). New cfg(test) tests: longest_common_prefix basic/single/empty/utf8-safe; StaticCompletionSource word-prefix + mid-buffer; Tab single-candidate completes; Tab multi-candidate extends to common prefix; Tab multi-candidate no-extension emits ShowCompletions; Tab zero-candidates NoChange; clear_completion_source disables Tab. Extended `flow_line_editor` /u-test with 3 new U-4d probes (Probe 14: 3-candidate ShowCompletions + common-prefix extension; Probe 15: single-candidate Tab; Probe 16: Tab without source NoChange). Reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4d sections (trait + struct + StaticCompletionSource + Tab dispatch semantics + ShowCompletions main-loop contract). Title updated to "U-4 arc". | `9a3a4fb` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 13 to 16 probes (3 new U-4d probes all PASS); joey line "u-test: line editor OK" unchanged in shape; Tab completion machinery validated end-to-end through the typed engine surface with the shipped StaticCompletionSource |
| U-4c hash fixup | Update U-4c row with the impl's hash. | *(pending)* | — |
| U-4c | _(impl SHA `a80908a`)_ History nav refinements + Ctrl-R incremental search -- third sub-chunk of the U-4 arc. **Smart Up/Down** (zsh/fish-style): in multi-line buffer with cursor NOT on first/last line, Up/Down navigate cursor between buffer lines (column-preserving via new `desired_col: Option<usize>` sticky tracker); when cursor is on first/last line OR buffer is single-line, fall through to history-prev/next. New `do_cursor_up_line` + `do_cursor_down_line` + `line_start_byte` + `line_end_byte` + `col_in_line` + `byte_offset_at_col` helpers (UTF-8 aware via libutopia::ansi::visible_width). desired_col preserved across consecutive Up/Down; cleared on any horizontal motion / edit. **Ctrl-R incremental search**: new `Mode::Search { query, match_index, saved_buffer, saved_cursor }` variant. Ctrl-R (Action::SearchHistory) enters search; printable chars append to query + re-search from history.len()-1 backward; Backspace narrows; Ctrl-R again steps back to next-older match; Ctrl-N steps forward to newer; Enter Accepts the matched history line via the normal Accept path (balance check + reset); Ctrl-C / Ctrl-G / Ctrl-D / any other action cancels + restores saved buffer/cursor (v1.x can refine to readline's full action re-dispatch). Substring match (str::contains) at v1.0; fuzzy is v1.x. New search-mode render: `(reverse-i-search)\`<query>': <matched>` / `(failed reverse-i-search)\`<query>':` (no match), cursor positioned at end of query. **History cap**: HISTORY_CAP = 10000 (per UTOPIA-SHELL-DESIGN.md §12.4); push_history FIFO-evicts oldest entry when exceeded. **New public accessors**: `is_searching() -> bool`, `search_query() -> Option<&str>`, `search_match_index() -> Option<usize>`. New cfg(test) tests: smart Up/Down in multi-line; smart Up at first line falls back to history; desired_col preserved across consecutive Up/Down; push_history caps at HISTORY_CAP; Ctrl-R enters search; query appends find newest match; Ctrl-R cycles to older match; Backspace widens match; Enter accepts; Cancel restores saved state; no-match shows `failed` prefix; render shape contains expected prefix. Extended `flow_line_editor` /u-test with 3 new U-4c probes (Probe 11: smart Up cursor-up + fall-through; Probe 12: Ctrl-R + 2 chars + Ctrl-R-step + Enter Accept; Probe 13: search Cancel restores). Reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4c sections (smart nav semantics + search-mode state machine + history cap + new public accessors + probes 11-13). | `a80908a` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 10 to 13 probes (3 new U-4c probes all PASS); joey line "u-test: line editor OK" unchanged in shape; smart Up/Down + Ctrl-R search + history cap all validated end-to-end through the typed engine surface |
| U-4b hash fixup | Update U-4b row with the impl's hash. | *(pending)* | — |
| U-4b | _(impl SHA `28276a7`)_ Multi-line + bracket-balance continuation -- second sub-chunk of the U-4 arc. Adds `BalanceState` struct + `balance(s: &str)` lightweight tracker (brace + paren + bracket depth, single + double quotes, `\\` escape, `#` line-comments, trailing unescaped backslash). Negative bracket depth treated as balanced (malformed but submit + let parser report). `is_balanced()` + `awaits_continuation()` accessors. Enter behaviour: if `awaits_continuation()`, insert `\n` at cursor (yielding Redraw) instead of submitting; trailing `\\` stays in buffer verbatim (parser elides `\\\n` per POSIX/rc -- separating editor from parser per UTOPIA-SHELL-DESIGN.md). Multi-line `render(prompt)` reshaped: emits prompt + first buffer line, then `\r\n\x1b[K<continuation_prefix><line>` per subsequent buffer line; cursor positioned via `\x1b[<n>F` (cursor previous-line) + `\x1b[<col>C`. New `continuation_prefix(prompt_width)` private helper: `(prompt_width - 2)` leading spaces + `⋮` (U+22EE) in `palette::Role::Path` colour + 1 trailing space, total visible width = prompt_width (per UTOPIA-VISUAL.md section 3.2 -- user text on continuation lines aligns with user text on first line). Backspace + Delete auto-join lines (falls out of existing UTF-8 boundary-walking; `\n` is just another char). NO column-preserving Up/Down nav across multi-line buffer (Up/Down stay history-only as in bash; zsh/fish-style conditional cursor-nav lands at U-4c or v1.x). NO ghost-clear of trailing lines when buffer shrinks (the U-6 main loop will track `prev_render_lines` + emit `\x1b[J` at start of each render; documented in `docs/reference/92-utopia-line-editor.md` "Multi-line ghost-clear caveat"). New cfg(test) host unit tests covering BalanceState across brackets / quotes / escapes / comments / trailing-backslash / negative depth + Enter-yields-newline-when-unbalanced + Enter-yields-Accept-when-balanced + multi-line render shape + Backspace joining lines. Updated `flow_line_editor` in /u-test with 4 new U-4b probes: unbalanced-Enter -> Redraw + multi-line round-trip; single-quote isolates brackets; trailing-backslash continuation; multi-line render contains ⋮ + correct cursor positioning. Updated reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4b sections (balance tracker semantics + Enter behaviour + multi-line render strategy + ghost-clear caveat + status). | `28276a7` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 6 to 10 probes (4 new U-4b probes all PASS); joey line "u-test: line editor OK" unchanged in shape; new behaviour validated end-to-end through the typed engine surface |
| U-4a hash fixup | Update U-4a row with the impl's hash. | *(pending)* | — |
| U-4a | _(impl SHA `eccbf0f`)_ `libutopia::line_editor` engine core -- **first sub-chunk of the U-4 line editor arc**. Pure-logic engine consuming byte streams + producing editor actions; raw-mode I/O wiring deferred to U-6 (main loop) + U-PTY (kernel substrate; no PTY at v1.0). New `LineEditor` struct + `EditorAction` enum (NoChange / Redraw / Accept(String) / Cancel / Eof / ClearScreen). ANSI input escape parser state machine: Ground / Escape / Csi { params, param_count, current_has_digits } / Utf8 { buf, expected, have }. Emacs keybindings: Ctrl-A/B/C/D/E/F/H/K/L/N/P/U/W/Y + Backspace (0x7f or 0x08) + Enter (LF or CR) + printable 0x20-0x7e + UTF-8 multi-byte (0xc2..=0xf4 leading + 0x80..=0xbf continuation; accumulated in 4-byte buf, validated via `core::str::from_utf8` before insertion). CSI sequences: A/B/C/D arrows + H/F Home/End + 3~ Delete + 1~/7~ Home + 4~/8~ End. History up/down (Ctrl-P/N + CSI A/B) recognized but no-op when `history.is_empty()` (U-4c populates via `push_history`). Tab (Ctrl-I) no-op at U-4a (U-4d completion hook). Ctrl-R no-op at U-4a (U-4c incremental search). UTF-8 buffer invariant maintained by parser; `String::is_char_boundary` ensures Backspace + Delete walk char boundaries. Defensive `MAX_BUFFER_LEN = 64 KiB` cap; insert/yank refuse to grow past. Single-line `render(prompt)` emits `\r\x1b[K<prompt><buffer>\r\x1b[<n>C` where n = `visible_width(prompt) + visible_width(buffer[..cursor])`; cursor at column 0 omits the trailing `\x1b[<n>C`. New `libutopia::ansi::visible_width(s)` helper -- byte-walks `s`, treats `ESC [ ... final-byte` CSI sequences as zero-width, counts non-CSI chars as 1 column each (v1.0 approximation; grapheme + wcwidth is v1.x). New reference doc `docs/reference/92-utopia-line-editor.md`. ~640 LOC engine + ~290 LOC `#[cfg(test)]` host unit tests + ~150 LOC `flow_line_editor` probe in `/u-test` (6 end-to-end probes runtime-validated at boot: insert+Accept; ANSI arrow split across feed_byte boundaries; Backspace+Ctrl-K+Ctrl-Y round-trip; UTF-8 multi-byte across feed_byte boundaries; history Ctrl-P/Ctrl-N nav including saved-current restore; render() shape). | `*(pending)*` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass with new joey line: "u-test: line editor OK" between "u-test: hardware + cap OK" and "u-test: all OK"; six composed flow probes validated through the typed engine surface |
| U-3 hash fixup | Update U-3 row with the impl's hash. | *(pending)* | — |
| U-3 | _(impl SHA `6713635`)_ Utopia workspace skeleton + `ut` shell skeleton -- the **first chunk in the shell half of Phase 7**. New `libutopia` crate (palette + ansi + path modules) and `utopia-shell` crate (the `ut` binary, v0.0.1-skeleton: Pale Fire version banner + clean exit). Workspace placement: `utopia/libutopia` + `utopia/shell` added as members of the existing `usr/Cargo.toml` (small deviation from UTOPIA-SHELL-DESIGN.md section 14.1's "Cargo workspace root at usr/utopia/" sketch -- directory grouping under `usr/utopia/` preserved, single Cargo workspace; lift to a separate workspace is mechanical if a future chunk needs it). `tools/build.sh::build_userspace` builds the new crates via the existing cargo invocation (no new `build_utopia` shell function); cpio extended with `ut` (40 entries, up from 39 at U-2-test); joey spawns `/ut` after `/u-test` with 7-line spawn-and-verify block. Banner emits 24-bit-colour SGR escapes -- glyph in ember orange (`#e07840` = palette::GLYPH) + tagline in receded steel (`#8898b4` = palette::PATH), each segment closed with `ESC[0m` per UTOPIA-VISUAL.md. Skeleton scope: NO REPL, NO line editor, NO parser, NO evaluator at U-3 -- those land at U-4 (line editor), U-5 (parser), U-6 (evaluator), U-7 (job control), U-8 (Thylacine builtins), U-9..N (coreutils), U-PTY (PTY infrastructure), U-Z (bring-up integration test). ~150 LOC libutopia + ~50 LOC ut + ~25 LOC joey wiring + 2 lines workspace + 1 line build.sh + new reference doc `docs/reference/91-utopia.md`. | `6713635` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass with new joey line: "joey: /ut reaped status=0; Utopia shell skeleton verified"; Pale Fire banner verified byte-for-byte via `cat -v` on boot log -- all five emit-points (glyph FG escape + ⊢ + reset + path FG escape + tagline + reset) match the palette constants |
| U-2-test hash fixup | Update U-2-test row with the impl's hash. | *(pending)* | — |
| U-2-test | `/u-test` — cumulative integration smoke for the libthyla-rs uplift, closes the U-2 arc. _(impl SHA `e037b4c`)_ New binary at `usr/u-test/`; spawned by joey at boot immediately after `/alloc-smoke`. Where alloc-smoke isolates each U-2X module's surface in its own block, u-test runs COMPOSED cross-module flows — the patterns a Utopia builtin will reach for. Six flows: (1) alloc + fs + io (heap-backed Vec slurp of /system.key); (2) process + pipe (pipe() round-trip + spawn hello-rs Stdio::Piped + wait + verify exit); (3) notes + poll + time (open self-notes, poll timeout, self-send + poll-ready + drain); (4) thread + torpor + time (spawn child thread that wakes a shared AtomicU32 + clear_child_tid join); (5) ninep codec (Tversion hand-build + parse + Rversion build + peek); (6) hardware + cap (Mmio/Irq/Dma cap-missing rejects without CAP_HW_CREATE). Each flow prints "u-test: <flow> OK"; final "u-test: all OK" precedes a clean exits(0). On any FAIL, the binary prints "u-test: <flow>: <what> FAILED" + exits 1; joey treats that as a boot failure. ~340 LOC + ~25 LOC joey wiring + 1 line workspace member + 1 line build.sh cpio. New reference doc `docs/reference/90-u-test.md`. **Library half of Phase 7 (the libthyla-rs uplift) is now COMPLETE.** Next: U-3 (Utopia workspace skeleton + ut shell skeleton). | `*(pending)*` | usr workspace cargo build clean (16 binaries; no warnings); `tools/test.sh` boot pass with new joey block: "u-test: starting ... u-test: all OK" + "joey: /u-test reaped status=0; U-2 arc integration verified" |
| U-2h-hardware hash fixup | Update U-2h-hardware row with the impl's hash. | *(pending)* | — |
| U-2h-hardware | `t::hardware::{Mmio, Irq, Dma}` — typed RAII wrappers over the kernel `KObj_MMIO` / `KObj_IRQ` / `KObj_DMA` surfaces. _(impl SHA `98494de`)_ Combined create + map constructors (every existing consumer calls create immediately followed by map; the combined form covers v1.0 needs). Drop closes the handle via `SYS_CLOSE`; the kernel-side Burrow keeps the mapping alive until proc exit (independent refcount), so Drop is no-op in practice. Type-system-enforced non-transferability per I-5 (no `Transfer` trait impl). Volatile MMIO reads / writes (`read_u32` / `write_u32`) with runtime bounds + alignment asserts; the kernel-installed PTE carries MAIR_IDX_DEVICE so the hardware also doesn't reorder. DMA buffers are normal memory; `as_slice` / `as_slice_mut` for byte views, `read_u32` / `write_u32` with compiler fences for 32-bit access; HARDWARE barriers (`virtio_rmb`/`virtio_wmb`) remain the caller's responsibility. Irq impls `t::poll::AsFd` so a multi-IRQ event-loop driver can compose with `PollSet`. Bare SVC wrappers (`t_mmio_create` etc.) remain exported — drivers can continue using them; no forced-migration deadline. Error mapping: kernel returns -1 generic-rejection → `Err(Error::InvalidArgument)`; future kernel -errno values route through `from_syscall_return`. Migrations landed in this chunk: 3 simple probes (mmio-probe + irq-probe + irq-bench). 7 virtio-* binaries remain on bare wrappers (migration is mechanical; can land incrementally). alloc-smoke extended ~70 LOC: cap-missing rejection for all three constructors (positive paths exercised by the 3 probes which DO get CAP_HW_CREATE). New reference doc `docs/reference/89-hardware.md`. | `*(pending)*` | usr workspace `cargo build --release` clean (15 binaries; no warnings); `tools/test.sh` boot pass; mmio-probe + irq-probe + irq-bench all PASS via the typed API (validates Mmio::new + Mmio::read_u32 + Irq::new + Irq::wait against real PL031 RTC + GIC SPI 96); new joey line: "alloc-smoke: Mmio + Irq + Dma cap-missing reject OK" |
| U-2h-ninep hash fixup | Update U-2h-ninep row with the impl's hash. | *(pending)* | — |
| U-2h-ninep | `t::ninep` — 9P2000.L server-side codec lifted from corvus's private `p9` module into `libthyla_rs::ninep`. _(impl SHA `1ba1f00`)_ Server-side only at v1.0 (T-message parsers + R-message builders + the pack/unpack primitives + the Header peek). Client-side (T-builders + R-parsers) deferred until a real consumer surfaces — native programs reach 9P services via the kernel client (mounted trees + SYS_SRV_CONNECT), not by speaking the protocol from userspace. Corvus migrates in the same commit via `use libthyla_rs::ninep as p9;` (zero call-site change); corvus's `src/p9.rs` deleted. Module surface: 16 message-type constants (P9_T*/R* + P9_RLERROR), QID type bits (QTDIR/QTFILE), sentinels (NOFID/NOTAG), width constants (HDR_LEN=7/QID_LEN=13), walk bounds (MAX_WALK=16/NAME_MAX=255), 12 Linux errno values for Rlerror responses; `Qid` 13-byte struct; `Header` + `peek_header`; `pack_u8/u16/u32/u64/str/qid` primitives; `unpack_u8/u16/u32/u64/str` primitives (zero-copy for str); Tmsg parsers `parse_t{version,attach,walk,lopen,read,write,clunk}`; Rmsg builders `build_r{version,attach,walk,lopen,read,write,clunk,lerror}` with back-patched size. Error convention is `Result<_, ()>` for wire-format violations (caller short-circuits to Rlerror(EPROTO) on Err). alloc-smoke extended ~135 LOC: pack/unpack primitives round-trip; Rversion build + peek_header; Tversion hand-build + parse_tversion field check; Twalk hand-build with 3 names + parse_twalk field check; Rwalk build + peek + nwqid read-back; Rlerror build + ecode read-back; peek_header short-buffer reject; parse_twalk short-buffer reject. New reference doc `docs/reference/88-ninep.md`. | `*(pending)*` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: NineP codec OK" — validates the entire server-side codec end-to-end including back-patched header sizing and the Twalk variable-length name parser |
| U-2g hash fixup | Update U-2g row with the impl's hash. | *(pending)* | — |
| U-2g | `t::torpor::{wait, wake, wake_one, wake_all, WaitResult}` + `t::time::{sleep, Duration}` + `t::rand::{getrandom, fill_bytes, GETRANDOM_MAX}` + `t::thread::{spawn_raw, exit_self, set_tid_address, clear_tid_address, join_tid, Tid}`. Futex-style wait/wake over `&AtomicU32` (no-lost-wakeup guaranteed by kernel torpor_lock serialization); sleep built on torpor_wait with stack-local sentinel (no SYS_NANOSLEEP at v1.0); getrandom + fill_bytes with `CAP_CSPRNG_READ` gate; kernel-thread spawn + the canonical clear_child_tid join protocol. **t::tty deferred** -- the kernel has no PTY surface at v1.0; U-PTY chunk handles it later. **Multi-thread exit safety**: `set_tid_address` + `join_tid` is the only safe-exit pattern for a multi-thread Proc at v1.0 (SYS_EXITS with any RUNNING peer thread extincts the kernel; the join wakes only when the child is THREAD_EXITING). New syscall constants: T_SYS_TORPOR_WAIT=39 + WAKE=40 + THREAD_SPAWN=41 + THREAD_EXIT=42 + SET_TID_ADDRESS=36; 5 SVC wrappers; T_TORPOR_TIMEOUT_INDEFINITE + T_TORPOR_MAX_TIMEOUT_US sentinels. `Duration` re-exported from `core::time` (no `std::time` in no_std). alloc-smoke extended ~200 LOC: torpor value-mismatch + timeout + wake-with-no-waiters; time::sleep 5ms + Duration::ZERO; rand::getrandom Ok-or-NotPermitted tolerance (cap inheritance varies) + oversize InvalidArgument; thread::spawn_raw misaligned-sp negative + **end-to-end thread roundtrip via set_tid_address clear_child_tid wake** (the kernel-extinction-hazard path). | `0d1894e` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Torpor + Time + Rand + Thread OK" — validates SYS_TORPOR_WAIT + SYS_TORPOR_WAKE + SYS_THREAD_SPAWN + SYS_SET_TID_ADDRESS + SYS_THREAD_EXIT + SYS_GETRANDOM end-to-end through the typed Rust surface, including the multi-thread exit-safe clear_child_tid join handshake |
| U-2f hash fixup | Update U-2f row with the impl's hash. | *(pending)* | — |
| U-2f | `t::territory::{mount, bind_replace, bind_before, bind_after, unmount, chroot, pivot_root, MountFlags, PathId}` + `t::cap::{Caps, Stripes, grant, use_grant}`. Plan 9 namespace composition + the two-phase cap-elevation flow. mount/unmount key by an abstract u32 `path_id_t` at v1.0 -- string-path resolution is a v1.x kernel lift; libthyla-rs docs the future shape. Source handles via `&impl AsFd` (reuses U-2e's trait; File + future SrvConn compose for free; the kernel validates KOBJ kind + rights at the syscall boundary). chroot is the initial-bringup primitive; pivot_root is the long-running-Proc variant (audit-trackable, the joey-uses-this path from 16c). bind_before/bind_after/bind_replace are sugar that route through mount() with the matching MountFlags. New syscall constants: T_SYS_MOUNT=14 / UNMOUNT=15 / CHROOT=35 / PIVOT_ROOT=53; new SVC wrappers t_mount/t_unmount/t_chroot/t_pivot_root. T_MREPL/MBEFORE/MAFTER/MCREATE flag constants + TPathId alias. **No `current()` or `drop()` at v1.0**: the kernel exposes no cap-query or non-spawn cap-drop syscall. Programs that need to drop caps spawn a child via Command (U-2d) with reduced `cap_mask`; cap-querying is v1.x. CAP_HOSTOWNER ELEVATION-ONLY restriction documented (kernel-enforced; libthyla-rs comments the trap). alloc-smoke extended ~150 LOC: MountFlags + Caps bitops round-trip (compile-time + runtime against the kernel bit positions); positive mount/unmount round-trip on free path_id 0x7E5701 using /system.key as Spoor source; bind_before/bind_after/bind_replace variants; bogus-fd negative; cap::grant without CAP_GRANT_HOSTOWNER → PermissionDenied; cap::use_grant without console-attach + pending grant → PermissionDenied. | `0391383` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Territory + Cap OK" — validates SYS_MOUNT + SYS_UNMOUNT + SYS_CAP_GRANT + SYS_CAP_USE end-to-end through the typed Rust surface |
| U-2e hash fixup | Update U-2e row with the impl's hash. | *(pending)* | — |
| U-2e | `t::notes::{Notes, Note, NoteClass, NoteMask, NoteTarget, MaskGuard}` + `t::poll::{PollSet, PollEvents, PollTimeout, PollEvent, PollResults, AsFd}`. Notes is RAII over SYS_NOTE_OPEN (fd-shaped path -- the canonical Thylacine surface per NOVEL.md §3.1); read() blocks, try_read() probes via poll(0). NoteMask is a per-Thread bitflag set of NoteClass (Interrupt/Kill/Pipe/ChildExit/Snare); set_mask swaps and returns prior; with_mask returns a MaskGuard that restores on Drop. notes::send accepts NoteTarget::{SelfProc, Pid(Pid)}; validates empty/oversize names AND rejects `snare:`-prefixed names at the boundary (kernel-synthetic reservation). PollSet is a reusable Vec<TPollFd> with parallel events-vector for clean re-arm on each poll; supports add/add_raw/remove/remove_raw/clear/len; empty-set poll short-circuits to Ok(empty). PollEvent helpers (is_readable/is_writable/is_hup/is_err). AsFd trait bridges typed wrappers to PollSet; File and Notes both impl. New syscall constants: T_SYS_NOTE_OPEN=44 / NOTIFY=45 / NOTED=46 / POSTNOTE=47 / NOTE_MASK=48. SYS_NOTIFY/SYS_NOTED constants surfaced for v1.x async-handler callers; libthyla-rs surfaces only the fd path at v1.0. ABI mirror: TNoteRecord #[repr(C)] 32-byte (16 name + u32 arg + u32 sender_pid + u64 timestamp_ns); const-assert pins size. T_NOTE_BIT_* + T_NOTE_MASK_SUPPORTED + T_POSTNOTE_SELF_PID + T_NOTED_NCONT/NDFLT constants. alloc-smoke extended: drains any child_exit synthetic post from U-2d's wait(), validates mask round-trip + with_mask RAII restore, exercises send validation (empty/oversize/snare:), send + read("interrupt"), PollSet pipe round-trip + notes-fd integration. | `6b5811c` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Notes + Mask + PollSet OK" — validates SYS_NOTE_OPEN + SYS_POSTNOTE + SYS_NOTE_MASK + SYS_POLL + the kernel-synthetic child_exit poster end-to-end through the typed Rust surface |
| U-2d hash fixup | Update U-2d row with the impl's hash. | *(pending)* | — |
| U-2d | `t::process::{Command, Child, ExitStatus, Stdio}` + `t::process::pipe()` free fn. Command is a std::process-shaped builder (new/arg/args/stdin/stdout/stderr/caps/spawn). Stdio = Inherit \| Piped \| File(File); Null deferred (no kernel /dev/null analog). Child::wait reaps via t_wait_pid; ExitStatus::success/code/raw. Pipe cleanup discipline: PreparedStdio tracks (child_fd, keep_through_syscall, parent_keeps) so the parent's copy of the child-end pipe fd drops at end of spawn(). Adds T_SYS_WAIT_PID=22 + T_SYS_SPAWN_FULL_ARGV=49 syscall constants, t_wait_pid + t_spawn_full_argv SVC wrappers, #[repr(C)] TSpawnArgs (56-byte mirror with compile-time size assertion), T_SYS_SPAWN_ARGV_MAX/DATA_MAX + T_SPAWN_NAME_MAX/MAX_FDS bounds. File::from_raw_handle pub(crate) helper for wrapping kernel-returned fds (pipes, etc.). alloc-smoke extended: pipe() round-trip + EOF + Command::spawn(hello-rs).stdin/stdout/stderr(Piped).spawn().wait().success(). | `ecf1a1a` | usr workspace cargo build clean; tools/test.sh boot pass with new joey lines: "hello from /hello-rs" (alloc-smoke-spawned) + "alloc-smoke: pipe + Command + Child OK" |
| U-2c-fs hash fixup | Update U-2c-fs row with the impl's hash. | *(pending)* | — |
| U-2c-fs | `t::fs::{Metadata, OpenOptions}` + free functions `fs::{metadata, exists, is_file, is_dir}`. Metadata is `#[repr(C)]` 72-byte mirror of kernel `struct t_stat` with const fn accessors (len, is_file, is_dir, is_char_device, is_symlink, is_empty, mode, permissions, nlink, atime/mtime/ctime_sec, qid_path/vers/type, blksize, blocks). OpenOptions builder mirrors std::fs::OpenOptions: read/write/truncate compose to omode; append/create/create_new return Error::NotImplemented at v1 (no kernel SYS_TLCREATE/append surface). `File::metadata()` method backed by SYS_FSTAT. New syscall: T_SYS_FSTAT=50 + t_fstat wrapper. T_S_IF{MT,REG,DIR,CHR} mode constants added. **Deferred from U-2c-fs to a future sub-chunk**: ReadDir / DirEntry (no kernel directory-read mechanism exposed); free fns canonicalize/remove_file/copy (no kernel surfaces yet). alloc-smoke extended with Metadata + free fn + OpenOptions error-case validation. | `3c8a49b` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Metadata + OpenOptions + free fns OK" |
| U-2c-io hash fixup | Update U-2c-io row with the impl's hash. | *(pending)* | — |
| U-2c-io | `t::io::{Read, Write, Seek, BufRead, SeekFrom, BufReader, Cursor}` traits + concrete adapters (mirror std::io). `t::fs::File` RAII over per-component SYS_WALK_OPEN walk; impls Read/Write/Seek. New syscall wrappers: t_walk_open + t_lseek + constants (T_OREAD/OWRITE/ORDWR/OTRUNC, T_SEEK_SET/CUR/END, T_WALK_OPEN_FROM_ROOT, T_WALK_OPEN_NAME_MAX). Error gets two library-only variants (UnexpectedEof, WriteZero; non-exhaustive enum, backwards-compat). File::open absolute paths only at v1 (relative + .. parent-traversal return InvalidArgument). alloc-smoke extended with Cursor + File::open(/system.key) + Read + Seek validation. | `c19e0f9` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Cursor + File + Read + Seek OK" — validates SYS_WALK_OPEN, SYS_READ, SYS_LSEEK round-trip through the typed Rust trait surface |
| U-2c-path hash fixup | Update U-2c-path row with the impl's hash. | *(pending)* | — |
| U-2c-path | `t::fs::{Path, PathBuf, Component, Components, Display, SEPARATOR}`. Pure logic, no syscalls. Path is `#[repr(transparent)]` newtype over str; PathBuf wraps String. UTF-8 by convention. parent/file_name/file_stem/extension/join/starts_with/ends_with/components iterator. Full Ord/Eq/Hash/Display/Debug. Per-binary `#[global_allocator]` declaration added to the 11 libthyla-rs consumers (hello-rs, mmio-probe, irq-probe, irq-bench, virtio-blk-probe, virtio-blk-rw, virtio-net-probe, virtio-net-arp, virtio-net-loop, virtio-input, virtio-gpu) — sets the project-wide convention that every native Rust binary opts in to ThylaAlloc. U-2c split from one chunk into U-2c-path / U-2c-io / U-2c-fs (3 thin slices). alloc-smoke extended with Path/PathBuf/Components validation. | `d141398` | usr workspace cargo build clean; `tools/test.sh` boot pass with new joey line: "alloc-smoke: Path + PathBuf + Components OK" alongside the existing alloc lines |
| U-2b hash fixup | Update U-2b row with the allocator impl's hash. | *(pending)* | — |
| U-2b | `t::alloc` — `ThylaAlloc` `#[global_allocator]` backed by `linked_list_allocator::LockedHeap` over a single SYS_BURROW_ATTACH region (4 MiB initial). Lazy init via atomic state machine; multi-thread-safe. Adds `t_burrow_attach` + `t_burrow_detach` SVC wrappers + T_SYS_BURROW_ATTACH/DETACH (37/38) constants. New `usr/alloc-smoke/` workspace member: native Rust binary exercising Box/Vec/String/small-alloc loop. Wired into `tools/build.sh` + spawned by joey at boot. | `d8e95d3` | usr workspace cargo build clean; `tools/test.sh` boot pass with new joey lines: "alloc-smoke: Box + Vec + String + small-alloc loop OK" + "joey: /alloc-smoke reaped status=0; libthyla-rs::alloc verified" |
| U-2a hash fixup | Update U-2a row with the foundation impl's hash. | *(pending)* | — |
| U-2a | `t::err` (`Error` enum + `Result<T>` + `From<i32>` + `from_syscall_return` + `Display`) + `t::handle` (`Handle` RAII + `Rights` bitflags-newtype + RAII close via SYS_CLOSE on Drop). Foundational types for the libthyla-rs uplift; no_std + no_alloc; backwards-compatible (existing T_RIGHT_* constants + bare wrappers preserved). | `e99bb43` | usr workspace cargo build clean (no warnings); full boot test pass (`tools/test.sh` green; "Thylacine boot OK" reproducible) |
| U-2 hash fixup | Update U-2 row with the scripture amendment's hash. | *(pending)* | — |
| U-2 | Scripture amendment: §15 reframed as "the libthyla-rs uplift to the library Thylacine deserves" — lead-by-example framing, complete module structure, sub-chunk decomposition (U-2a..U-2-test, ~9-12 sessions). §19 + phase7-status.md + UTOPIA.md + ROADMAP §8.1/§8.7 updated. NO code. | `2fbfad3` | — |
| U-1 hash fixup | Update U-1 row with the scripture commit's hash. | *(pending)* | — |
| U-1 | Scripture: UTOPIA.md + UTOPIA-SHELL-DESIGN.md + UTOPIA-VISUAL.md + ARCH/ROADMAP/CLAUDE updates + this doc. NO code. | `c4e57f2` | — |

## Remaining work — the U-* arc

Per `docs/UTOPIA-SHELL-DESIGN.md §19`. Sequenced for dependencies.

The U-2 work was reframed in U-2 scripture amendment from a single "libthyla-rs extensions" chunk into a multi-chunk **libthyla-rs uplift** — the library Thylacine deserves. ~9-12 sessions of foundation work before U-3 begins; investment paying back across every subsequent native Rust program.

| Sub-chunk | Scope | Depends on |
|---|---|---|
| **U-2** | Scripture amendment: §15 (libthyla-rs uplift framing) + §19 update + phase7-status.md U-* arc refresh. NO code. | U-1 |
| **U-2a** | `t::err` (Error + Result + From<i64>) + `t::handle` (Handle RAII + Rights bitflags). Foundational. | U-2 |
| **U-2b** | `t::alloc` (`#[global_allocator]` via burrow_attach). Enables `alloc::*`. Smoke binary with Box/Vec/String. | U-2a |
| **U-2c-path** | `t::fs::{Path, PathBuf, Component, Components, SEPARATOR}`. Pure logic, no syscalls. Mirrors std::path. | U-2b |
| **U-2c-io** | `t::io::{Read, Write, Seek, BufRead}` + `t::fs::File`. | U-2c-path |
| **U-2c-fs** | `t::fs::{OpenOptions, Metadata, ReadDir, DirEntry}` + free functions. | U-2c-io |
| **U-2d** | `t::process::{Command, Child, ExitStatus, Stdio}` + `t::process::pipe()`. | U-2c-io |
| **U-2e** | `t::notes::{Notes, Note, NoteClass, NoteMask, NoteTarget, MaskGuard}` + `t::poll::{PollSet, PollEvents, PollTimeout, PollEvent, AsFd}`. **LANDED** at the U-2e row above. | U-2c-io |
| **U-2f** | `t::territory::{mount, bind_replace, bind_before, bind_after, unmount, chroot, pivot_root, MountFlags}` + `t::cap::{Caps, grant, use_grant}`. **LANDED** at the U-2f row above. (`current` / `drop` deferred -- no kernel syscalls at v1.0; `rfork` deferred -- not a standalone syscall at v1.0.) | U-2a |
| **U-2g** | `t::thread` + `t::torpor` + `t::time` + `t::rand` (+ t::tty deferred to U-PTY since the kernel has no PTY surface at v1.0). **LANDED** at the U-2g row above. | U-2c-io |
| **U-2h-ninep** | `t::ninep` (lift 9P2000.L server-side codec from corvus into libthyla-rs; aliases corvus to consume the lifted module). **LANDED** at the U-2h-ninep row above. | U-2c-io, U-2d |
| **U-2h-hardware** | `t::hardware::{Mmio, Irq, Dma}` typed wrappers (RAII over SYS_MMIO_CREATE/SYS_IRQ_CREATE/SYS_DMA_CREATE; impl AsFd on Irq for poll integration; type-system enforces I-5 non-transferability). **LANDED** at the U-2h-hardware row above; 3 simple probes migrated (mmio-probe, irq-probe, irq-bench); 7 virtio-* binaries on bare wrappers pending incremental migration. | U-2c-io, U-2d |
| **U-2-test** | Cross-module smoke binary on Thylacine. Validates the uplift. **LANDED** at the U-2-test row above. | U-2a..U-2h |
| **U-3** | Utopia workspace skeleton: `usr/utopia/{Cargo.toml,shell,libutopia,coreutils}`; libutopia palette + ansi + path modules; `ut` skeleton (version-print + exit); `tools/build.sh utopia` Rust cross-compile wiring; host-bake `/bin/ut`. | U-2-test |
| **U-4** | Line editor in libutopia: raw mode + emacs keybindings + line buffer + multi-line + tab hook + Ctrl-R history hook. Hand-rolled (~1500-2500 LOC); NOT reedline. | U-3 |
| **U-5** | Parser + AST for rc-shape syntax. Pure logic; unit-testable on host. **U-5a + U-5b + U-5c + U-5d LANDED** at the rows above; the U-5 parser arc is COMPLETE. | U-3 |
| **U-6** | Evaluator core + main loop: poll() main loop; built-ins (cd, exit, set, source, fn, alias, eval, type, etc.); external command spawn; pipes; redirection; `?`/try-catch; pipefail. **U-6a + U-6b + U-6c LANDED** at the rows above (U-6a: evaluator scaffold + Value + Env + eval_expr for no-side-effect subset; U-6b: statement eval -- Let/Assign/FnDecl/Return/Break/Continue/If/While/For/Case/Try/Trace/OnNote/MaskNote + Pipeline-as-fn-call + BraceBlock + Arith + implicit-fail bookkeeping; U-6c: external command spawn via libthyla_rs::process::Command + trace echo to UART; U-6d-a: multi-element pipeline + pipefail; U-6d-b: I/O redirection -- `<` `>` `>>` heredoc on a lone command + per-pipeline-element, via resolve_redirects + the libthyla_rs::fs create-or-open/append surface + the `<< EOF`-with-space lexer fix. Subst/ProcSub/Match::Regex + subshell + in-process-pipeline-stage + `&` + redirect-on-in-process-command still NotImplemented). **U-6e-pre-a LANDED** (the native-argv runtime + std-stream handles in libthyla-rs: `env::args` [G03] + `io::{stdin,stdout,stderr}` + `from_raw_fd` + print macros [G05]; `argv-smoke` proves the first native-argv execution -- the U-6e prerequisite). **U-6e-pre-b LANDED** (16 native coreutils -- echo/cat/wc/head/tail/true/false/seq/sort/uniq/tr/cut/grep/basename/dirname/pwd -- adopted into `usr/coreutils` + runtime-verified by `coreutil-smoke`, 28 checks; `RAMFS_FILE_MAX` 64->128; reap-first deadlock fix; deferred set tracked #925). **U-6e-a LANDED** (shell built-in dispatch fn->builtin->external + the must-be-internal builtins cd/pwd/exit/true/false/unset/eval/source/type + the `$status`/`$errstr`/`$cwd` special-var bridge). **U-6e-b-1 LANDED** (directory-enumeration foundation: kernel `devramfs_readdir` + libthyla_rs `fs::read_dir`/`ReadDir`/`DirEntry` over `SYS_READDIR` + #929 root-open via stalk; the read_dir surface glob + `ls` need). **U-6e-b-2 LANDED** (glob argv fs-walk: `glob::expand` + the `evaluate_argv` `glob_candidate` gate -- a bare unquoted `Word` carrying a meta char expands via a per-segment `read_dir` walk with rc nullglob; quoted/`$var`/`^`-concat stay literal; pure userspace). **U-6e-b + the whole U-6e arc COMPLETE.** **U-6f LANDED** (command substitution `$(cmd)` / `` `{cmd}` `` -- single redirect-free external-pipeline capture, drain-before-reap, whitespace field-split / quoted-inline, `$status` propagation; + its kernel prerequisite **#926**: a single-thread Proc closes its fds at EXIT not reap, so `$(cmd)`'s stdout drain sees EOF; builtin/in-process bodies + `<(cmd)`/`>(cmd)` procsub deferred [in-process capture needs fork; procsub needs `/proc/self/fd/N`]). **U-6g LANDED** (the read-parse-eval REPL: `ut`'s `rs_main` reads fd 0 -> `libutopia::repl::Repl::feed` drives the line editor + `eval_source` + prompt render; exits on Ctrl-D / `exit` / EOF; `Env::interactive`; the multi-fd job-control poll() is U-7, the pollable-cons/line-discipline substrate is U-PTY). **U-6-test LANDED** (the cumulative integration smoke -- 7 composed flows weaving subst-capture + for/case/arith, glob->spawn->capture, var-interp pipelines, builtin->special-var->control flow, var-fed heredoc redirect, and the whole stack through the real `Repl::feed` loop + error recovery, driven through `eval_source` + `Repl::feed`). **The U-6 evaluator arc is COMPLETE.** Next: U-7 (job control), whose Ctrl-C/Ctrl-Z + multi-fd `poll()` are predicated on the U-PTY line-discipline substrate. | U-4, U-5 |
| **U-7** | fd-notes job control. **U-7-pre LANDED** (`SYS_WAIT_PID` v2: kernel `wait_pid_for` WNOHANG + wait-by-pid -- the death-path primitive). **U-7a LANDED** (background jobs: `&` + the `eval::jobs` table + the by-pid foreground demux + `Repl::reap_jobs` prompt-cycle reaping; `cmd &`/`a | b &` -> `[N] PID` then `[N]+ Done`). **U-7b LANDED** (`jobs`/`fg`/`bg`/`wait`/`kill` builtins over the JobTable + the `%N` jobspec lexer fix; bg/fg-resume of a STOPPED job is U-PTY-gated). **U-7c-a LANDED** (`on note`/`mask note` runtime delivery: `deliver_pending_notes` at the prompt-cycle sync point + the kernel-mask-backed `mask note` + the lazy `open_notes`). **U-7c-b LANDED** (Ctrl-C foreground forwarding: `eval::wait_pids_interruptible` replaces the blocking by-pid reap in all three foreground command waits -- it polls the notes fd between WNOHANG sweeps and forwards `interrupt` to the running fg job's pids; non-interrupt notes deferred to the post-command drain; degrades to the blocking reap when no note fd is open). **The U-7c arc is COMPLETE.** **U-7-test LANDED** (the cumulative composition probe `/u-7-test` -- 4 composed flows weaving bg-demux + the builtins + `on note`/`mask note` delivery + the interruptible foreground wait, joey-gated; **the U-7 arc is COMPLETE**). Ctrl-Z (needs a kernel *stopped* thread-state) + termios + the command-substitution / `wait`-builtin forward (needs process groups) are seamed to **U-PTY**. | U-6, U-7-pre |
| **U-8** | Thylacine builtins: bind / mount / unmount / pivot_root / rfork / cap / note. | U-6 |
| **U-9..N** | Coreutils, one or two per chunk: cat, ls, echo, grep, sed, awk, cp, mv, rm, mkdir, find, wc. | U-3 (each independent thereafter) |
| **U-Helix** | Helix port via Pouch. Parallel arc; not in shell critical path. | U-3 (for host-bake) |
| **U-PTY** | PTY infrastructure if not landed by then: `/dev/ptmx`, `/dev/pts/<n>`, `termios` via `/dev/consctl`. | Independent of shell impl until U-Z |
| **U-Z** | The Utopia bring-up integration test (`docs/UTOPIA-SHELL-DESIGN.md §18`). Multiple full-suite passes; perf measurements; doc final pass. | All above |

Rough scale: 27-37 sessions across the arc. The libthyla-rs uplift (U-2..U-2-test) is the heaviest sub-arc — ~9-12 sessions — and is investment in the library every subsequent chunk builds on.

### The Life Support arc (LS) — interactive human usability

Scoped 2026-06-09 after the first hands-on interactive test surfaced that the
kernel UART console **never received keystrokes** (LS-1, root-caused + fixed
@`6e533d6`, closes #943). The full arc design — making Thylacine usable for
standard human-driven workflows (log in, navigate, list, inspect, create/edit/
delete files, search, pipe, interrupt, jobs) — is binding scripture at
**`docs/LIFE-SUPPORT.md`**. Chunks LS-1 [done @`6e533d6`] -> LS-2 [done @`8d3c13b`;
external stdout/stderr inherit the console via `env.stdio_inherit`] -> LS-CI [done
@6ea7452; `tools/test-interactive.sh` + `tools/interactive/{lib.exp,ls-ci.exp}`
-- the expect/PTY interactive regression net that drives a real keyboard, closes
#945] -> LS-3a [done @`4edf665`; adopted `ls`/`stat`/`clear` (native libthyla-rs,
from `usr/apps/{ls,stat}` + native `clear`) into `usr/coreutils/src/bin/`, wired
`tools/build.sh` + the cpio + LS-CI `ls-3a.exp`; `ls`/`stat`/`clear` verified
(`ls /var`->lib, `ls /`->thylacine-version, `stat /thylacine-version`, `clear`).
#955 FIXED in the same close: `ls` was the first consumer to readdir the pivot
ROOT in a paginating loop, surfacing a kernel readdir-cookie sign-truncation
(Stratum cookies > INT64_MAX wrapped negative in the s64 Spoor.offset; dev9p
clamped to 0 -> re-fetched batch 0 forever). Fixed (opaque-u64 cookie + a
non-advancing-cursor EOD guard) + a regression test + the `ls /` LS-CI
assertion] -> LS-3b [done @`89650da`; `mkdir`(+`-p`)/`rmdir`/`rm`(+`-r`/`-f`)/
`touch`/`cp`(+`-r`)/`mv`/`tee`(+`-a`) authored native on a NEW libthyla-rs `fs::`
mutation surface (`create_dir`/`remove_file`/`remove_dir`/`rename` + a shared
`with_parent_dir` parent-walk over the audited SYS_WALK_CREATE/UNLINK/RENAME).
The parent-walk opens intermediates `T_OPATH` (R|W since A-3b), which ALSO fixed
a latent `File::create` depth>=2 bug (old `T_OREAD` -> RIGHT_READ-only parent that
SYS_WALK_CREATE rejects -- pulled forward). `rm -r`/`cp -r` skip `.`/`..`. New
always-on `fs-mut-smoke` (joey post-pivot, 13 checks incl. depth-3 File::create)
+ `ls-3b.exp` LS-CI; 789/789, ci-smp-gate 0 corruption] -> **#957 [done @`8301e8d`]
(kernel, audit-bearing)**: LS-3b surfaced that a logged-in user could NOT write to their own
`/home/<user>` -- the single-hop `SYS_WALK_OPEN` (which libthyla-rs `fs::` mutations
navigate parent dirs with) did NOT cross mounts (only multi-component `stalk`/SYS_OPEN
did), so create/rename/unlink resolved the SYSTEM-owned placeholder under the mount, not
the user-owned mounted root. FIX: `sys_walk_open_handler` crosses at the source + result
via the now-public `stalk_cross_mounts` (Plan 9: all walking crosses); crossing into
`/srv` required making the devsrv registry root openable+stat-able as a directory
(`devsrv_open` root branch + `devsrv_stat_native`, the #932 readdir prerequisite).
Verified: 791/791 (+2 devsrv tests), boot + u-builtin cd /srv + ls-3b(a) /home-write
PASS, ci-smp-gate 0 corruption; focused Opus audit 0 P0/1 P1/0 P2/2 P3 (F1 pre-existing
open-return-bool, devsrv connect via single-hop walk leaked the endpoint -> adopt
open()'s return like stalk; F2 ls /srv -> #932; F3 comment). #958 = the ls-3b multi-step
LS-CI net is PTY-render-flaky (harness, not #957) -> LS-3c (misc
coreutils) -> LS-4 (relative paths) -> LS-5 (Ctrl-C) form the MVP; then LS-6/7/K (login
UX, a minimal editor, id/whoami/date) for breadth and LS-8 (U-PTY: pollable cons +
termios + async) for depth. Tasks #944-#953. Supersedes the loose U-9..N / U-PTY
rows above with a workflow-driven sequence.

**HOLOTYPE arc DFS-inserted here (2026-06-10, user-approved; see
`docs/HOLOTYPE.md`).** The whole-system deep review arc pre-empts the LS
emerge at the post-LS-5c boundary: six lenses (soundness / completeness /
SOTA / performance / consistency / gaps) over RW-0..RW-13, priority-ordered
for the review-model window. RW-0 = the owed LS-5 focused audit (#963),
folded in as the opener + methodology calibration. Fix policy: soundness
P0/P1/P2 in-arc with regression tests; non-soundness findings registered in
`docs/holotype/00-register.md` + enqueued, triaged at RW-13 (the
consolidation scripture commit that also decides the LS re-entry point).
The LS line (LS-6/7/K -> LS-8/LS-test -> Imperium #959) resumes at the
RW-13 emerge decision.

**RW-0 [done] (HOLOTYPE opener: the owed LS-5 focused review, #963).** One
reviewer pass over the three LS-5 commits (`2a6c9ed` + `9f407d0` + `9886704`)
-- the kernel notes/latch/wake surface, the spawn-perm gate, the shell forward
path -- plus the owed interactive regression net; doubles as the arc's
methodology calibration. **F1 [P1] (USERSPACE, ut): a Ctrl-C at a FRESH login
prompt logged the session out** -- the session shell is console owner (LS-5a)
but opened its note queue LAZILY (first keystroke), so an uncaught `interrupt`
before any input took the LS-5b default-terminate, and login read the shell's
exit as logout. FIXED `@f145ce8`: ut opens the queue eagerly for a real
session (gated on `io::stdout_is_live()`; the bare-spawn boot check stays lazy
-- an eager open there would mint the note fd as fd 0). Kernel surface
reviewed SOUND: 0 P0/P1/P2. Three P3s (#980), all dispositioned at this close:
**F2** (a console Ctrl-C is a synthetic `interrupt` post; a queue holding 16
non-interrupt notes gives coalesce nothing to overwrite -> -EAGAIN -> the
Ctrl-C drops and the latch never arms; unreachable for a typical fg coreutil)
-> ACCEPTED + notes.h known-caveat; **F3** (`thread_die_pending` read the
latch with no kproc guard -- correctness rested entirely on the single arm
site refusing kproc) -> FIXED: kproc skip on the latch leg + a forced-flag leg
in `notes.die_pending_predicate` (the wake already carried its own
`g_kproc` belt; the SYS_POSTNOTE self-post pre-check is caller-side, never
kproc -- all three latch readers now kproc-safe); **F4** (multi-thread
disposition race: thread B registering a handler / opening the notes fd
between an interrupt post and blocked thread A's resume costs A one spurious
`*_INTR`, e.g. -P9_E_IO; the tail re-validates so never a wrong termination;
v1.0-unreachable -- multi-thread Procs are stratumd-class, never
console-owner/fg) -> ACCEPTED + notes.h known-caveat. **The regression net
landed: `tools/interactive/ls-5.exp`** (+ `lib.exp::lc_ctrlc`) -- three cases
over a real PTY, each asserting session survival via an upper-cased output
probe: (A) fresh-prompt Ctrl-C (the F1 regression; the empty-\r drain between
Ctrl-C and probe is LOAD-BEARING -- the kernel cooked-consumes 0x03 so the
note sits queued while the editor stays blocked, and the next foreground wait
would FORWARD the stale note to the probe pipeline itself; the empty line
cycles deliver_notes first), (B) `sleep 3600` + Ctrl-C reaps promptly (LS-5c
blocked-in-kernel), (C) `yes` + Ctrl-C (LS-5b/c flooding). PASS first-attempt
TCG via `tools/test-interactive.sh ls-5`. Matrix: kernel 803/803 + 0
EXTINCTION + login E2E; SMP gate (default+UBSan x smp4/smp8, N=10) PASS --
0 CORRUPTION all four configs (ubsan-smp8 7/10 benign host-timing
classifications, the documented #894-family fragility; the gate's verdict
keys on corruption only). Register seeded: `docs/holotype/00-register.md`
(HT00.F1-F4);
closed list `memory/audit_ls5_closed_list.md` (feeds RW-2). Residual on the
surface: #964 (never-syscalling spinner, pre-existing). Files: notes.c
(thread_die_pending kproc guard), notes.h (F2/F4 caveats), test_notes.c
(forced-flag leg), tools/interactive/{ls-5.exp,lib.exp}. NEXT = RW-1 (memory,
FULL).

**LS-5c [done] (P3-terminate; KERNEL -- audit-bearing death-path + wait/wake
surface, the formal round rides LS-5-audit #963).** The #811/ARCH-8.8.1
death-wake predicate is widened to "group-exit death OR a pending
terminate-disposition `interrupt`" (ARCH 8.8.2), so a child BLOCKED in the
kernel (sleep / pipe read / cons read / poll / 9P RPC / torpor) wakes,
unwinds `*_INTR` to its EL0-return tail, and the LS-5b disposition terminates
it -- closing the last LS-5 property: `sleep 30` + Ctrl-C now dies promptly.
As-built: `PROC_FLAG_INTR_TERMINATE_PENDING` (proc_flags bit 7; a LATCH,
unlike the one-way bits) is the LOCK-FREE wake-hint -- the sleep path can
never take `q->lock` (the devnotes F3-close ABBA), so the disposition decided
at post time is cached on the Proc. ALL latch writes run under
`p->notes->lock`: SET by `notes_post`'s interrupt arm
(`notes_arm_intr_terminate_locked`: name==interrupt + no handler + not
self-managing + never kproc -- the kproc guard is load-bearing, in-kernel
tests post to the boot thread's queue); CLEARED by handler registration
(`notes_set_handler` -- the SYS_NOTIFY store moves under `q->lock`, closing
the store-vs-arm interleave that would strand a stale armed latch behind a
registered handler = spurious-`*_INTR`-forever), the self-managing mark
(`notes_mark_self_managing`, the SYS_NOTE_OPEN tail), and draining the last
queued interrupt (`notes_drain_intr_locked` in both dequeue helpers).
`thread_die_pending(t)` (notes.c, lock-free: group_exit_msg acquire-load OR
latch-armed AND interrupt-unmasked-for-t) replaces every direct
group_exit_msg read at the sleep sites: sleep/tsleep x4 register-then-observe
(sched.c), torpor's post-register check (torpor.c -- conservative-prompt for
the interrupt leg; the I-9 closure is tsleep's own check one layer down), and
the 9P client's `client_self_dying` (a Ctrl-C'd coreutil blocked in a 9P RPC
takes the audited #845 Tflush unwind, not a session kill). The WAKE
(`proc_interrupt_terminate_wake`, internally latch-gated; caller MUST hold
g_proc_table_lock) is the #811 death-walk verbatim (per-peer wait_lock ->
rendez_blocked_on -> wakeup, Option A) MINUS `torpor_wake_all_for_proc`
(torpor waiters' stack rendez is reachable via rendez_blocked_on) and MINUS
`smp_resched_others` (the IRQ-from-EL0 tail evaluates only group_exit_msg, so
the IPI buys nothing -- the never-syscalling pure-spinner gap, which also
afflicts single-thread `kill`, is pre-existing and now tracked as #964);
wired at all four interrupt-post sites (proc_console_post_interrupt,
proc_console_sak, postnote_walk_cb [the shell's Ctrl-C forward], and the
SYS_POSTNOTE self-post which now takes the table lock for the walk). The
EL0-return tail stays the TRUTH: a stale-positive latch costs one spurious
`*_INTR` unwind (confined to a notify-race in-flight window = POSIX
EINTR-with-handler semantics, or the inherited-notes-fd exotic), never a
wrong termination; a MASKED thread is woken by the walk but its own-mask
predicate re-sleeps it (masking defers; unmasking later fires the terminate
at the next tail; death still overrides the mask). All ten #811 blocking
sites' `*_INTR` arms re-validated mechanism-agnostic (cleanup -> return, no
loop, none re-reads group_exit_msg). Also fixed in passing: the
proc_group_terminate header-contract drift in proc.h (it still described the
pre-#811 "acquires NO g_proc_table_lock" envelope; proc.c's "caller MUST
hold" is the truth). Tests +6 (803/803, HVF + TCG):
`rendez.intr_terminate_interrupts_sleep` (drives the REAL waker under the
deterministic harness -- possible because it has no IPI broadcast, which the
death test must hand-roll around), `..._register_observe` (the I-9 leg: latch
armed BEFORE the first sleep -> immediate SLEEP_INTR with NO wake call),
`..._masked_sleeps_through` (wake absorbed, re-sleeps; death-overrides-mask
cleanup), `..._interrupts_tsleep` (the /sleep surface), +
`notes.intr_latch_lifecycle` (arm / notify-clear / no-rearm-with-handler /
re-arm-after-unregister / drain-clear-on-last / self-managing-clear /
kproc-guard) + `notes.die_pending_predicate`. Runtime regression: u-7-test
flow 5 (`sleep 3600 &` + `kill <pid>` + `wait` reaps promptly) -- proven
NON-VACUOUS by stash + rebuild on the base kernel where it HANGS the boot
check (Error 1). 0 EXTINCTION, login E2E OK; the full SMP gate + the
interactive `ls-5.exp` ride LS-5-audit (#963). Files: proc.h (flag + decls +
the contract-drift fix), proc.c (pending-read + wake + console-site wiring),
notes.h (3 decls + the latch section), notes.c (arm + drain + set_handler +
mark_self_managing + thread_die_pending), syscall.c (notify/note_open rewire
+ the 2 postnote wakes), sched.c (x4 widened checks), torpor.c, 9p_client.c,
test_rendez.c + test_notes.c + test.c (+6 tests), usr/u-7-test (flow 5),
usr/lib/libthyla-rs/src/time.rs (stale not-note-interruptible comment).

**LS-5b [done] (P2 default disposition; KERNEL -- audit-bearing death-path +
notes surface, the formal round rides LS-5-audit #963).** An uncaught `interrupt`
now default-terminates a NON-self-managing, handler-less Proc at the EL0-return
tail (SIGINT's "default = die, catchable"). New `PROC_FLAG_SELF_MANAGING_NOTES`
(proc_flags bit 6; atomic-OR set by `SYS_NOTE_OPEN`, atomic-load read --
fail-closed; mirrors `proc_mark_may_post_service`) -- a Proc that opened its
notes fd consumes its own notes, so it is exempt. `notes_deliver_at_el0_return`'s
`handler_va == 0` arm calls a pure, header-exposed decision
`notes_interrupt_should_terminate_locked` (no handler + not self-managing + a
deliverable [queued AND unmasked] `interrupt` -- a queue SCAN so an interrupt
behind another unconsumed note still fires) and, on true, drops `q->lock` and
`exits(NOTE_NAME_INTERRUPT)` -- the SAME primitive the `kill` branch uses
(single-thread -> ZOMBIE status 1; multi-thread -> the audited #811
`proc_group_terminate` cascade). Only `interrupt` newly terminates: child_exit /
pipe stay queued for the fd-read path; `kill`/N-4 unchanged; a handler-bearing
or masked Proc is exempt. I-19 extension; no struct-size change (reuses the
proc_flags word). Tests +2 (`notes.interrupt_terminate_gate` truth table +
`notes.self_managing_flag`): **797/797** (HVF + TCG), boot OK, 0 EXTINCTION,
login E2E OK. **Semantic ripple (intended, surfaced as a regression first):** the
shell's `wait_pids_interruptible` forwards a Ctrl-C to its foreground child
(U-7c-b `drain_fg_wait_notes`); pre-LS-5 that forwarded `interrupt` was a no-op
on a dumb coreutil (`echo` exited 0), so the U-7 probes pinned the forwarded
child's status to 0. LS-5b makes that forward TERMINATE the child (status 1) --
which is the whole point of Ctrl-C -- so `u-7-test` (flow 2) + `u-job-test`
(item 17) were relaxed from `status == 0` to "reaped (no hang)", their stated
intent; the load-bearing forward-not-handle assertion is untouched. Ground
truth: base @2a6c9ed green, the change is causal + correct (proven by
stash+rebuild). Files: `kernel/include/thylacine/proc.h` (flag + 2 helper
decls), `kernel/proc.c` (helper impls), `kernel/syscall.c::sys_note_open_handler`
(mark), `kernel/include/thylacine/notes.h` (`NOTE_NAME_INTERRUPT` + decl),
`kernel/notes.c` (scan + decision + dispatcher arm), `kernel/test/test_notes.c`
+ `test.c` (the 2 tests), `usr/u-7-test` + `usr/u-job-test` (assertion relax).
NEXT = LS-5c (#962): widen the #811 death-wake to also fire on a pending
terminate-disposition `interrupt`, so a BLOCKED child (sleep/read) wakes + dies.

**LS-5a [done] (P1 console owner; KERNEL -- audit-bearing surface, the formal
round rides LS-5-audit #963).** `SPAWN_PERM_CONSOLE_OWNER` (1<<2; mirrored in
libt + libthyla-rs `T_SPAWN_PERM_CONSOLE_OWNER`) -- a new spawn-perm bit that
records the spawned child as `g_console_owner`, the Proc that receives the
`interrupt` note on Ctrl-C. Console-*owner* is strictly distinct from
console-*attach* (I-27): the owner bit NEVER confers attach, so the SAK /
hostowner-elevation anchor is untouched. Gated in `spawn_perm_grant_check` the
SAME way as `MAY_POST_SERVICE` (console-attached OR a `MAY_POST_SERVICE`
holder), so trusted `/sbin/login` -- holding the bit from joey -- confers it on
the session shell `ut` (`Command::perm(T_SPAWN_PERM_CONSOLE_OWNER)`). The two
spawn thunks' identical perm-application blocks were DRY'd into one
`apply_spawn_perms` helper (the bit->`proc_mark_*`/`proc_set_*` mapping), which
also yields a deterministic owner-set test (a real spawn races the child's exit
clearing `g_console_owner`). Kernel tests
`sys_spawn_with_perms.console_owner_grant_gate` + `.console_owner_set_wiring`;
**795/795** (HVF + TCG), the login E2E shows "ut spawned stamped + reaped",
`make test-tcg` EXIT=0, **0 EXTINCTION**. (The lone HVF `test.sh` failure is the
pre-existing **#896** virtio-input QMP-injection probe -- unrelated to
spawn-perms; tracked.) Reference doc `73-sys-spawn-with-perms.md` refreshed
(also folded in the long-missing `CONSOLE_TRUSTED` + #827b one-hop-delegation
rows the doc had drifted past). LS-5a just wires the owner; **P2
default-terminate (LS-5b #961) + P3-terminate wake (LS-5c #962) + the
interactive `ls-5.exp` (LS-5-audit #963) follow.**

**LS-4 [done] (kernel per-Proc cwd; user-voted over shell-side argv-expansion
2026-06-09; scripture `43ce24e`).** Split LS-4a (kernel, audit-bearing) +
LS-4b (userspace). LS-4a `@c6b0ff2`: `Territory.dot_path` (a cleaned absolute
cwd string, name-based; NULL=="/") + a leaf `dot_lock`, `cwd_lexical_resolve` /
`territory_join_cwd` [named `territory_resolve_cwd` when LS-4 landed; renamed at #83] / `getdot` / `setdot`, `SYS_CHDIR`=69 / `SYS_GETCWD`=70,
and the `sys_open_handler` relative->cwd join (POSIX `openat(AT_FDCWD)`). I-28
PRESERVED with NO new mechanism (the join always hands stalk an
absolute-from-root path; stalk re-clamps `..`). Focused Opus audit **CLEAN 0 P0
/ 0 P1 / 0 P2 / 3 P3** (all doc/cosmetic; F1 combined-length-bound doc + F2 a
"cleaned is pre-resolved" comment FIXED, F3 pre-existing `source_is_valid` no-op
out of scope) -- arch SOUND. Kernel tests `territory.cwd_lexical` +
`territory.cwd_dot`; 793/793. LS-4b (userspace): `libthyla-rs` `t_chdir`/
`t_getcwd` + `env::current_dir`/`set_current_dir`, the `fs::` relative-mutation
absolutize (`cwd_join_clean` in `with_parent_dir` + `open_create_at_path`), and
the shell `cd` calling `SYS_CHDIR` so spawned children inherit the cwd. Proven
by `fs-mut-smoke` (joey post-pivot, boot-gated): 21/21 incl. relative
create+write+read, `..`-relative clean, getcwd round-trip. Name-based for v1.0;
handle-based dot is the v1.x upgrade (lands with symlinks). **Unblocks the
relative-path interactive MVP** (`cat foo.txt` after `cd`).

**LS-3c [done] (misc coreutils; pure userspace).** Adopted 8 tools from the
aux branch (`aux/userspace-apps`) onto the libthyla-rs surface (the LS-3a/3b
pattern -- `aux-rt` -> `env::args` + `io::` + the print macros, one
`coreutils/src/bin/<name>.rs` each, cpio-wired): `sleep` (rides
`libthyla-rs::time::sleep` -> `SYS_TORPOR_WAIT`; magnitude-guarded against a
`from_secs_f64` overflow), `hexdump`, `cmp`, `yes`, `realpath`, `which`, `env`
(degenerate, no envp -- G15), `uname` (static -- G16). Two LS-4-now-satisfied
deps folded in: **realpath** anchors a relative input against
`env::current_dir()` (was: rejected); **which**'s `fs::exists` probe resolves
relative-to-cwd. Verified `coreutil-smoke` 41/41 (+13 LS-3c checks) + `ls-3c`
LS-CI (`uname -a`, `realpath`, `yes | head`, `echo | hexdump` through the real
shell). **`yes` excluded from `coreutil-smoke`** (unbounded producer would
deadlock the capture harness -- no BrokenPipe; covered interactively by
`yes | head`). No new libthyla-rs surface, no kernel change -> NOT
audit-bearing; floor = `make test-tcg` x2 (both clean: 793/793 + boot OK + 0
EXTINCTION). NEXT on the MVP line: LS-5 (Ctrl-C); remaining breadth: LS-6
(login UX) / LS-7 (editor) / LS-K (id/whoami/date).

## Exit criteria status

Per `docs/UTOPIA-SHELL-DESIGN.md §18` / `docs/ROADMAP.md §8.2`. Twelve headline checks.

- [ ] Boot a fresh Thylacine VM; reach the Pale Fire `ut` prompt via UART.
- [ ] Multi-stage shell pipeline: `cat /etc/passwd | grep root | cut -d: -f1` produces correct output.
- [ ] Job control via fd-notes: `sleep 100 &` appears in `jobs`; Ctrl-Z + `fg` resume; Ctrl-C terminates.
- [ ] Error model: function with `cmd1?; cmd2?; cmd3?` short-circuits on `cmd2` failure.
- [ ] Namespace builtin: `bind /srv/stratum-ctl /n/stratum`; `ls /n/stratum` shows the Stratum admin surface.
- [ ] Notes builtin: `note send $$ snare:user1` triggers a registered `on note 'snare:user1' { ... }` handler.
- [ ] `hx /etc/hosts` opens Helix; edit + save observable.
- [ ] rc-shape script: `for (f in *.md) { wc -l $f }` runs.
- [ ] Pale Fire prompt renders with the canonical three-segment colour scheme (path `#8898b4`, glyph `⊢` `#e07840`, command `#d8e4f4`).
- [ ] No kernel extinctions, no driver crashes, no zombie processes.
- [ ] No P0/P1 audit findings on the Utopia surface.
- [ ] All planned U-* chunks landed.

## Build + verify commands

To be filled in as `tools/build.sh utopia` wiring lands under U-3. Anticipated shape:

```bash
# Build the Utopia workspace via aarch64-thylacine target (no_std on libthyla-rs)
tools/build.sh utopia

# Build the Helix port via Pouch
tools/build.sh helix

# Build everything (kernel + Utopia + Helix + sysroot + disk)
tools/build.sh all

# Boot + integration tests
tools/test.sh
```

## Trip hazards

- **Native libthyla-rs requires no_std + alloc.** Reaching for a std-dependent Rust crate is the most common mistake. The decision rule (`CLAUDE.md` "Native vs ported"): authored = native, ported = Pouch. Helix is the only Pouch consumer in Utopia.
- **The line editor is hand-rolled.** Reedline assumes std/Pouch; we have a libutopia line editor. Bug surface is real — see `docs/UTOPIA-SHELL-DESIGN.md §11.2`.
- **The fd-notes main loop is the core innovation.** Replaces classical signal handlers. Every shell operation routes through `poll()` (`docs/UTOPIA-SHELL-DESIGN.md §10`).
- **`ut` is NOT bash, NOT rc-as-shipped.** Rc-shaped with twelve refinements (`docs/UTOPIA-SHELL-DESIGN.md §4-§15`). Scripts written for POSIX `sh` do not run unchanged.
- **Pale Fire palette discipline.** Four colours; the `⊢` glyph; coloured by role not by personal preference. See `docs/UTOPIA-VISUAL.md`.
- **The integration test gates Phase 7 exit.** All twelve headline checks must pass.

## References

- `docs/UTOPIA.md` — the experience.
- `docs/UTOPIA-SHELL-DESIGN.md` — the binding design (12 axes + impl roadmap).
- `docs/UTOPIA-VISUAL.md` — Pale Fire palette + glyph + prompt format.
- `docs/ARCHITECTURE.md §3` — language and toolchain (extended in U-1 with native vs ported split).
- `docs/ARCHITECTURE.md §3.5` — the Plan 9 split scripture.
- `docs/ARCHITECTURE.md §23` — POSIX surfaces and the Utopia milestone (updated in U-1).
- `docs/ROADMAP.md §8` — phase definition (rewritten in U-1).
- `CLAUDE.md` "Native vs ported userspace programs" — operational decision rule.
- `docs/POUCH-DESIGN.md` — the ported-code substrate Helix consumes.
- `usr/lib/libthyla-rs/src/lib.rs` — the native runtime crate Utopia extends.

## Predecessors

- Phase 6 (Pouch; ROADMAP `§7A`) — `docs/phase6-status.md` — CLOSED at `218feb0`. Delivered the cross-compilation environment + the boundary-line for ported code.
- Phase 5 (9P + Stratum integration + corvus; ROADMAP `§7`) — `docs/phase5-status.md` — CLOSED. Delivered the 9P client + Spoor + Territory + the corvus precedent for native Rust on libthyla-rs.
