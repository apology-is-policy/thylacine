# Phase 7 — status and pickup guide

Authoritative pickup guide for **Phase 7: Utopia (textual milestone)** — execution Phase 7 per `ROADMAP.md §2.1`; ROADMAP section `§8`. Binding designs: **`docs/UTOPIA.md`** + **`docs/UTOPIA-SHELL-DESIGN.md`** + **`docs/UTOPIA-VISUAL.md`**.

## TL;DR

**Phase 7 is OPEN — scripture landed; implementation underway.** The U-1 scripture commit (this commit's predecessor at the time this status doc lands) materializes the design conversation as durable scripture: the `ut` shell, the `libutopia` shared library, the native Rust coreutils, the `hx` (Helix) editor port, and the Pale Fire visual identity. The U-* chunk arc unfolds from here.

The Phase 7 entry decision (taken under the U-1 scripture conversation):

- **Shell**: `ut`, a new from-scratch design — rc-shaped with refinements per twelve resolved design axes. Native Rust on `libthyla-rs`. NOT a port of rc; NOT bash.
- **Coreutils**: native Rust on libthyla-rs, 9base-shaped. NOT uutils-coreutils.
- **Editor**: `hx` (Helix), ported via Pouch. THE only Pouch consumer in Utopia.
- **Visual identity**: Pale Fire — four colours (`bg`, `fg`, `path`, `glyph`), one glyph (`⊢`), one prompt format. Disciplined in Utopia's programs; user programs colour freely.
- **Runtime**: native libthyla-rs (the Plan 9 split — see `docs/ARCHITECTURE.md §3.5` + `CLAUDE.md` "Native vs ported userspace programs").
- **Workspace**: Cargo workspace at `usr/utopia/`; Helix vendored separately at `usr/helix/`.

## Landed chunks

| Sub-chunk | What | Commit | Tests |
|---|---|---|---|
| U-6e-pre-b hash fixup | Pinned the U-6e-pre-b impl hash. | *(pending)* | -- |
| U-6e-pre-b | _(impl SHA `(pending)`)_ Adopt the native coreutils onto the promoted U-6e-pre-a surface -- **second sub-chunk of the aux-coreutils adoption arc; the FIRST runtime execution of the coreutils (the aux track only compiled them)**. Brings 16 of the auxiliary track's `usr/apps/*` coreutils into the main build as a single `usr/coreutils` crate (one `src/bin/<name>.rs` per tool, cargo-auto-discovered -- far less wiring than 16 crates): **echo, cat, wc, head, tail, true, false, seq, sort, uniq, tr, cut, grep, basename, dirname, pwd**. Each is rewritten off the aux-rt workaround onto `libthyla-rs` proper (`env::args` [G03] + `io::{stdin,stdout,stderr,out,copy,slurp}` + the `print!`/`eprintln!` macros [G05] -- a pure symbol substitution, since the aux apps were already libthyla-rs except for those 3 aux-rt surfaces). libthyla-rs gains the last 3 convenience helpers the apps use: `io::out`/`io::err` (best-effort byte writes) + `io::slurp` (read-to-Vec). **Verification:** new native driver `usr/coreutil-smoke` -- joey spawns it; it spawns EACH coreutil via `libthyla_rs::process::Command` with a piped stdin (fed a known input) + piped stdout (captured), and asserts the output bytes + exit status (28 checks: argv tools, stdin filters, devramfs file reads via `File::open`, exit codes). `do_native_coreutil_smoke()` gates the boot; the first RUNTIME exercise of the coreutils + a second exercise of the native-argv path + the first parent-reads-child-piped-stdout path. **Two bugs surfaced + fixed during bring-up** (ground-truth, not theory): **(1) devramfs truncation** -- the 17 new cpio binaries pushed the live ramfs to 69 entries, past `kernel/devramfs.c`'s `RAMFS_FILE_MAX 64`, silently TRUNCATING the load (dropping `/welcome`) and failing the `perm.devramfs_enforced_real_metadata` kernel test (773/779). Fixed by bumping `RAMFS_FILE_MAX` to 128 (a static BSS array, +1.5 KiB, far below the synth-dir qid base) -- a legitimate capacity pull-forward for the coreutils. **(2) reap-before-read deadlock** -- reading a child's piped stdout to EOF then `wait()` (the std order) DEADLOCKS on Thylacine because a Proc's fds close at REAP (`proc_free`/`wait_pid`), NOT at exit (the #844 handle-lifetime design): the child's stdout write-end stays open until reaped, so the read blocks for an EOF only the reap produces. Fixed in the driver by REAPING FIRST (the child writes its output into the 4096-byte pipe buffer + exits; `wait()` closes the write-end; THEN the read drains the buffer + EOF) -- correct for output <= `PIPE_BUF_SIZE`; the streaming-large-output ergonomic gap is tracked (#926). **Deferred (tracked #925)**: ls (needs `fs::read_dir`), mkdir/touch/rm/rmdir (fs-mutation helpers), tee (post-pivot file write), stat/date/id/whoami/ns/cap/ln/readlink/env/which/uname/hexdump/cmp/realpath/yes (each its own gap). **Locked decisions**: (1) single `coreutils` crate w/ `src/bin/*` (vs 16 crates) -- idiomatic for a suite, minimal wiring; (2) batch = the pure filters + argv tools verifiable PRE-pivot in one driver (file-mutation tools need post-pivot, deferred); (3) reap-first in the driver (vs changing the kernel's reap-time fd close -- out of scope + the audited #844 design); (4) `RAMFS_FILE_MAX` bump is the right fix (the coreutils belong in the boot cpio). | `(pending)` | coreutils + driver `cargo check` clean; kernel suite **779/779 PASS** (the RAMFS fix restored `/welcome`); `make test-tcg` boot pass on two consecutive runs; `coreutil-smoke: all OK (28 checks)` (every coreutil spawned + I/O-asserted) + `joey: coreutil-smoke ok` + `Thylacine boot OK`; 0 EXTINCTION |
| U-6e-pre-a hash fixup | Pinned the U-6e-pre-a impl hash (`6b0cf6b`). | -- | -- |
| U-6e-pre-a | _(impl SHA `6b0cf6b`)_ Native-argv runtime + std-stream handles in libthyla-rs -- **the U-6e prerequisite; first sub-chunk of the aux-coreutils adoption arc; the FIRST runtime execution of the native-argv path (a milestone)**. Promotes the two surfaces the aux track's `aux-rt` worked around (DOC-GAP-REPORT G03 + G05) INTO `libthyla-rs` proper, so native argv-taking programs (the coreutils) can drop aux-rt. **G03 (argv):** `usr/lib/libthyla-rs/src/lib.rs`'s `_start` now captures the kernel-built System-V startup frame (`exec.h` "Shape A / Shape B") -- `ldr x0,[sp]` (argc) + `add x1,sp,#8` (&argv[0]) BEFORE any prologue moves sp -- and tail-calls a Rust shim `__libthyla_rt_start(argc, argv)` that records both into process-global `AtomicUsize`/`AtomicPtr` statics (Release) then calls the app's `rs_main()` (unchanged signature -- backward-compatible: existing binaries that ignore argv are untouched; the captured argv is reached only via the accessor, never via rs_main params). Both startup shapes share the same capture (Shape A puts argc=0 at `[sp]`). New `pub mod env` with `env::args() -> Args` (Acquire-loads the statics; `Args` is `Copy`, byte-slice-based, `&'static` since the frame lives at the top of the user stack above the initial sp for the whole process lifetime), `Args::{len,is_empty,prog_name,get,get_str,operands}` + `Operands` (argv[1..]) + `impl Iterator`. **G05 (std streams):** `io::{stdin,stdout,stderr}` non-owning handles over `t_read`/`t_write` on fd 0/1/2 (do NOT close the fd on drop -- unlike `fs::File`, which would close inherited stdout); `io::copy(r,w)`; crate-root best-effort `print!`/`println!`/`eprint!`/`eprintln!` macros (swallow the unwired-fd-1 error per the G06 standalone caveat); `fs::File::from_raw_fd(fd, rights)` (unsafe; OWNS the fd, for wrapping a `t_walk_create`/`t_pipe` raw fd -- explicitly NOT for 0/1/2). **Milestone proof:** new native probe `usr/argv-smoke` (no aux-rt, no pouch) -- joey spawns it via the binary-agnostic `pouch_smoke_one_argv` harness with `argv=[argv-smoke,alpha,beta-2]` (argc=3) + a pipe as fd 0/1; argv-smoke reads its own argv through `env::args()`, echoes argc + every argv[i] to `io::stdout()` (fd 1, the pipe), and self-checks (exit 0 iff the argv matches). joey content-checks the per-position markers (`argv-smoke: argc=3` / `argv[0..2]=...` / `native argv OK`) AND the exit status; `do_native_argv_smoke()` gates the boot. This proves G03 (argv read) + G05 (stdout write) end-to-end through libthyla-rs -- before this the capture had only been disassembly-verified (the aux-rt note). **Locked decisions**: (1) `_start`-captures-into-globals + `rs_main()` unchanged (vs a naked-`rs_main` or an `rs_main(argc,argv)` signature change) -- backward-compatible across the whole native fleet; (2) Release/Acquire on the statics (single-write-at-startup; cheap insurance for a peer-thread `args()` read); (3) non-owning stdio handles (a `File` over fd 1 would close stdout on drop); (4) `from_raw_fd` is `unsafe` + owning, documented not-for-0/1/2; (5) NOT audit-bearing -- userspace EL0 startup path, no kernel privilege boundary or new concurrency; rigor = self-audit + the whole-native-fleet boot. Doc 38-userspace entry contract updated (G03/G04 fix) + the "Native argv + std streams" section. | `6b0cf6b` | libthyla-rs `cargo check` clean; usr workspace `cargo build --release` clean (argv-smoke + joey + the whole native fleet; only the pre-existing burrow-torture warning); kernel suite **779/779 PASS**; `make test-tcg` boot pass on two consecutive runs; `argv-smoke: argc=3` + `argv[0]=argv-smoke` + `argv[1]=alpha` + `argv[2]=beta-2` + `native argv OK` + `joey: argv-smoke ok (...first native-argv exercise)` + `Thylacine boot OK`; 0 EXTINCTION |
| U-6d-b hash fixup | Pinned the U-6d-b impl hash. | `50fe2c9` | — |
| U-6d-b | _(impl SHA `cc8b88d`)_ Shell I/O redirection (`<` `>` `>>` heredoc) -- **second sub-chunk of the U-6d arc; closes the redirect half of pipes+redirection**. Lifts the two redirect `NotImplemented` seams in `usr/utopia/libutopia/src/eval/stmt.rs`: the single-command `NotImplemented("redirects")` (now `exec_external_redirected`) and `exec_pipeline`'s per-element `NotImplemented("redirect on pipeline element (U-6d-b)")`. **The model**: the shell OPENS each redirect target and HANDS the fd to the child (`Stdio::File`); it does no bulk I/O itself (so Loom -- the async 9P DATA path -- is not the right layer here; that is the child's concern; the one shell-side write is the heredoc body into a kernel pipe, a plain SYS_WRITE). New `resolve_redirects(env, &[Redirect]) -> Result<ResolvedStdio, RedirError>` opens every target UP FRONT (a failure aborts before any spawn). Per kind: `< f` -> `File::open` at stdin; `> f` -> `OpenOptions::write+create+truncate` at stdout; `>> f` -> `...+append` at stdout; `<< TAG` -> render the body + a kernel pipe (rd at stdin, wr fed post-spawn then dropped for EOF). Last-wins per fd (a later same-fd redirect drops the earlier `Stdio::File`, closing it -- `> a > b` truncates a, writes b). `RedirError::Eval` propagates as an eval error (NotImplemented/Internal); `RedirError::Runtime` (ambiguous target / open failure) sets `$status`+`$errstr`, returns Normal. A redirect on a fn -> `NotImplemented("redirect on function (U-6f)")`; on a non-Simple in-process command -> `NotImplemented("redirect on in-process command (U-6f)")` (defensive -- the parser only attaches redirects to Simple commands); a bare redirect with no command (`> f`) opens/truncates the target then runs nothing (status 0). In `exec_pipeline` a per-element redirect OVERRIDES the pipe wiring for that fd (the displaced pipe end is dropped so the peer sees the right EOF); heredoc bodies are fed after the spawn-all barrier. **Dependency pulled forward (own surface)**: `libthyla_rs::fs` create-or-open + append. `OpenOptions::{create,create_new,append}` were `NotImplemented` stubs; now `File::open_create_at_path` drives the FS-alpha `SYS_WALK_CREATE` (`t_walk_create`). The create path is CREATE-FIRST (mirrors joey's `mkdir_or_open`): `t_walk_create` is exclusive, and the kernel's `walk_open` returns no distinguishable not-found code, so the robust create-or-open is "try create; on failure open the existing file (with truncate when requested)" -- never depends on a missing-file errno. `File::create` becomes std-shaped (create-or-open-truncate). `append` = open/create for write + seek-to-end-at-open (Plan 9 omode has no O_APPEND; exact for the single-writer shell case -- the Spoor offset is shared into the child via the handle bump, so the child's writes continue from end; concurrent atomic-append is a v1.x kernel surface). `OpenOptions::mode(m)` sets the create mode (default 0644). **Folded-in lexer fix**: `scan_heredoc_start` now skips horizontal whitespace between `<<`/`<<-` and the tag, so `cat << EOF` works as well as `cat <<EOF` (the universal heredoc form). **Locked design decisions**: (1) shell opens-and-hands-off, never does redirect I/O (Loom stays the child's data-path concern; #916 FS-open-via-Loom remains a tracked v1.x seam, user-confirmed); (2) up-front open (abort-before-spawn on any target failure -- a v1.0 simplification vs bash's per-element granularity, documented); (3) create-first create-or-open (robust to the kernel's generic walk_open miss code); (4) append = seek-to-end-at-open (single-writer-exact); (5) a redirect on a fn/in-process command is NotImplemented at v1.0 (no fd without fork; U-6f); (6) heredoc bodies are small at v1.0 (a body exceeding the pipe buffer fed to a non-reading command blocks -- documented). **Tests**: `alloc-smoke` create/append negative tests updated (the stub is gone -> they now assert create/append REACH the FS and fail appropriately on the read-only devramfs, not `NotImplemented`). New `flow_eval_redirect` in `/u-test` (7 pre-pivot probes, devramfs read-only: heredoc->pipe-sink exact; heredoc `$x` interpolation; `<<"EOF"` no-interp; redirect-on-fn NotImplemented; ambiguous multi-word target -> status+errstr; `>` create-failure on read-only devramfs -> status+errstr; bare-redirect status 0). New post-pivot binary `/u-redir-test` (joey-spawned on the writable dev9p root): the real `>`/`>>`/`<` round-trip -- `pipe-src > f` create+write -> read-back == payload; `pipe-src >> f` append -> read-back == payload x2; `pipe-src > f` truncate -> read-back == payload; `pipe-sink < f` -> exit 0. pipe-src/pipe-sink resolve from the devramfs cpio regardless of pivot (`devramfs_lookup` is unconditional in the spawn handlers). Wired into the usr workspace + `tools/build.sh` usr_rs_bins (cpio 50 -> 51) + joey post-pivot spawn+reap. `u-test` now reports **16 cross-module flows** (`u-test: eval redirect OK`). Reference doc 94 extended. | `cc8b88d` | usr workspace `cargo build --release` clean (both host + aarch64-unknown-none; only the pre-existing burrow-torture warning); kernel suite **779/779 PASS**; `tools/test.sh` (TCG) boot pass on two consecutive runs; `u-test: eval redirect OK` (7 probes) + `u-redir-test: all OK` + `joey: /u-redir-test reaped status=0; U-6d-b redirects verified` + `Thylacine boot OK`; 0 EXTINCTION |
| U-6d-a hash fixup | Update U-6d-a row with the impl's hash. | *(pending)* | — |
| U-6d-a | _(impl SHA `24eafac`)_ Multi-element pipeline -- **first sub-chunk of the U-6d arc (pipes + redirection)**. Lifts `eval_pipeline`'s `p.elements.len() > 1` NotImplemented branch via new `exec_pipeline(env, p)` in `usr/utopia/libutopia/src/eval/stmt.rs`. For `cmd1 | cmd2 | ... | cmdN`: (1) validate every element is a redirect-free EXTERNAL Simple command -- a fn / BraceBlock / Arith / Subshell as a pipeline stage returns `NotImplemented("non-simple command in pipeline")` or `NotImplemented("function as pipeline element")` (in-process eval has no fd to wire to a pipe without fork; U-6f+); a per-element redirect returns `NotImplemented("redirect on pipeline element (U-6d-b)")`. All argvs are evaluated UP FRONT so any rejection aborts before a single spawn. (2) Allocate N-1 pipes via `libthyla_rs::process::pipe()`; assign pipe j's write end to element j's stdout (`Stdio::File`) and read end to element j+1's stdin; the OUTER ends (element 0 stdin, element N-1 stdout, every stderr) get the U-6c v1.0 convention (`Stdio::Piped` + immediate parent-side drop, since the shell has no terminal-backed fd 0/1/2 yet). (3) Spawn ALL elements before the first wait (waiting between spawns deadlocks `producer | consumer` -- producer blocks on a full pipe while the parent blocks before spawning the drainer). (4) Reap via wait-any + pid-match: SYS_WAIT_PID reaps any zombie, so we collect the N spawned pids and match each reaped pid to its element index; at v1.0 there are no background children (U-7) so exactly N waits reap the pipeline. (5) Aggregate `$status` via new pure `pub fn aggregate_pipefail(&[(i32, bool)]) -> i32` per scripture 8.4: the RIGHTMOST non-zero exit among elements NOT marked `?|` (the `?|` pipe-modifier sets `tolerate_failure` on the LEFT element, whose status is then ignored), or 0 if every non-tolerated element succeeded. **v1.0 binary exit status (kernel limitation surfaced during impl)**: the kernel's `sys_exits_handler` normalizes ANY non-zero child exit to `exit_status = 1` (x0==0 -> exits("ok")/0; x0!=0 -> exits("fail")/1; the literal exit code is LOST -- a richer per-Proc u64 status is a Phase 5+ deferral per the kernel comment). So a real pipeline element's reaped status is 0 or 1, never its literal code. This is why the rightmost-VALUE distinction in pipefail is validated by the pure-function probe with SYNTHETIC statuses (7/3 -> 3), not by real binaries (which can only yield 0/1). It also means U-6c's exec_external `status_set(ExitStatus::raw())` yields 0/1 for any real command, not the literal code -- documented as a v1.0 caveat. **Stdio direction correctness**: `Stdio::File(end)` installs a specific pipe end at the child slot, then libthyla-rs drops the parent's copy after the spawn returns -- so once an upstream element exits, the downstream reader sees EOF (the parent holds no lingering write-end copy that would keep the pipe open). **Two new native test fixtures** (`usr/pipe-src/` + `usr/pipe-sink/`): pipe-src writes the fixed 13-byte payload `"PIPE-DATA-OK\n"` to fd 1; pipe-sink reads fd 0 to EOF and exits 0 iff it got exactly that payload, else 1. Needed because (a) hello-rs writes via SYS_PUTS (never fd 1) so it can't source pipe data, and (b) native binaries can't read argv at v1.0 (libthyla-rs `_start` calls `rs_main()` with no args) so a single multi-mode fixture isn't possible. Wired into `usr/Cargo.toml` workspace members + `tools/build.sh` usr_rs_bins; cpio 40 -> 42 entries. **Locked U-6d-a design decisions**: (1) pipeline elements restricted to external Simple commands at v1.0 -- in-process stages need fork; (2) all-spawn-before-wait (deadlock-freedom); (3) wait-any + pid-match reaping (SYS_WAIT_PID is wait-any; pid-match is order-independent + defensive against spurious reaps); (4) `aggregate_pipefail` is a pure exported fn -- the testable core of pipefail, boot-probed with synthetic statuses since real exits collapse to 0/1; (5) outer ends use U-6c's Piped+drop convention (flips to Inherit at U-PTY); (6) `Stdio::File` for inner pipe ends (kernel installs + parent auto-drops -> correct EOF propagation); (7) two fixtures (src + sink) rather than one, since no-argv + SYS_PUTS-only-hello-rs rule out a single multi-mode binary. Extended `/u-test` with `flow_eval_pipe` (8 boot probes: 2-element mechanics; 3-element mechanics; `pipe-src \| pipe-sink` REAL byte transfer -> 0; `hello-rs \| pipe-sink` EOF + real non-zero through pipefail -> 1; aggregate_pipefail rightmost-non-zero synthetic; aggregate_pipefail `?\|` tolerate synthetic; background pipeline -> NotImplemented; fn-in-pipeline -> NotImplemented). u-test now reports **15 cross-module flows**; new `u-test: eval pipe OK` line between `u-test: eval exec OK` and `u-test: all OK`. Reference doc 94 §10 + §9 extended. ~150 LOC stmt.rs + 2 fixtures (~95 LOC) + ~170 LOC u-test. | `24eafac` | usr workspace `cargo build --release` clean (19 binaries; no warnings; both host + aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs; new joey-reported `u-test: eval pipe OK` line; 8 pipeline-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path (incl. real byte transfer pipe-src->pipe-sink + EOF-through-pipefail); cpio 40 -> 42 entries; u-test now reports 15 flows; all PASS |
| U-6c hash fixup | Update U-6c row with the impl's hash. | *(pending)* | — |
| U-6c | _(impl SHA `c2661a8`)_ External command spawn -- **third sub-chunk of the U-6 evaluator arc**. Lifts `eval_command`'s SimpleCommand `NotImplemented("external command spawn or built-in")` branch via new `exec_external(env, &argv, span)` in `usr/utopia/libutopia/src/eval/stmt.rs`. When argv[0] does not resolve to a defined fn (and built-ins land at U-6e), exec_external builds a `libthyla_rs::process::Command`, sets all three Stdio slots to `Piped` (v1.0 convention), spawns, drops the parent-side pipe ends, waits, and reflects the result in `$status` + `$errstr`. **Status semantics**: clean reap -> `ExitStatus::raw()` (so a child exiting 0 is status=0); spawn failure -> 127 + `$errstr = "spawn failed: <Error>"` (bash command-not-found convention); wait failure -> 1 + `$errstr = "wait failed: <Error>"`. All three paths return `Ok(StatementFlow::Normal)` -- a runtime non-zero exit is not an eval-time error; the caller's implicit-fail discipline handles propagation upward. **Trace echo (closes U-6b deferral)**: when `env.trace_depth > 0`, `trace_echo(argv)` emits `+ argv[0] argv[1] ...\n` via `t_putstr` (kernel UART, matching the rest of the shell's v1.0 diagnostic output) BEFORE the spawn. When fd 2 (stderr) is wired via U-PTY + U-6g, the sink should switch to fd 2. Bash's `set -x`-style argv quoting is a v1.x refinement; v1.0 just space-separates. **Stdio inheritance (the U-6c design decision driving Stdio::Piped)**: `Stdio::Inherit` would tell the kernel to install the parent's fd 0/1/2 into the child's handle table, but the shell at v1.0 has no terminal-backed fd 0/1/2 (joey/u-test/ut all write via SYS_PUTS direct to the UART; joey spawns u-test via `t_spawn` which inherits no fds). The kernel's `SYS_SPAWN_FULL_ARGV` then rejects the spawn (-1 -> Error::NotPermitted in the libthyla-rs errno mapping) because the parent's handle table has no entry at those slots. Until U-PTY + U-6g land fd 0/1/2 wiring, exec_external uses the established v1.0 convention (matches `alloc-smoke` + `u-test::flow_process_pipe`): `Stdio::Piped` on all three slots followed by an immediate drop of every parent-side pipe end. The child gets kernel-installed pipes at 0/1/2 (so the kernel accepts the spawn); native binaries (SYS_PUTS-writers) don't touch them; ported binaries get EOF on read / EPIPE on write when they try -- which IS the correct v1.0 behavior for a shell without a terminal. When PTY lands, this whole block flips to `Stdio::Inherit` cleanly. **Subshell + background still deferred**: Subshell `( cmds )` returns `NotImplemented("subshell ( cmds )")` -- libthyla-rs has spawn-then-exec, not fork; Subshell requires a forked SHELL that runs the body in the child against an isolated Env; lands at U-6f or later. Background `&` returns `NotImplemented("background pipeline (&)")` -- needs job control wiring (U-7). **Built-ins still deferred**: at U-6e the dispatch chain becomes: fn lookup -> built-in lookup -> exec_external. **Argv glob fs expansion still deferred**: libthyla-rs has no `ReadDir` surface yet (per `usr/lib/libthyla-rs/src/fs/mod.rs` "deferred -- kernel directory-read mechanism not yet exposed"); U-6e lands both built-ins AND fs glob walks. The glob pattern matcher in libutopia is unchanged (case + `matches` operators continue to work; only fs-walk is the new add at U-6e). **Locked U-6c design decisions**: (1) `Stdio::Piped` + immediate drop (not `Stdio::Inherit`) for v1.0 -- the shell has no fd 0/1/2 to inherit yet; piped+drop is the established convention in alloc-smoke + flow_process_pipe; (2) status=127 on spawn failure (bash command-not-found convention) -- a single status for all spawn errors keeps the userspace contract simple; finer distinguishment between NotFound / PermissionDenied / InvalidArgument lands when libthyla-rs's Error -> rc errstr translation gets a proper boundary-line (v1.x); (3) status=1 on wait failure -- distinct from spawn failure (127) and from a clean non-zero exit (the child's own status); (4) `trace_echo` writes via `t_putstr` (UART) at v1.0 -- the shell's diagnostic sink. When fd 2 wires through line discipline, switch to fd 2; (5) Argv quoting in trace echo is space-only at v1.0 -- bash's `set -x` quoting (single-quoting args with embedded whitespace, escaping `$` and backslash) is a v1.x refinement; (6) Subshell stays NotImplemented at U-6c -- the fork primitive needed isn't available; (7) PATH search is NOT added at U-6c -- argv[0] is treated as a single-component name and passed to `Command::new`; the kernel resolves against devramfs OR the pivoted root; PATH search + path-style invocation (`/bin/ls`) lands at U-6e built-ins (`type` / `which`). Extended `/u-test` with `flow_eval_exec` (7 boot-time probes: hello-rs spawn-and-reap status=0; spawn-failure name "noenoe" -> status=127; trace { hello-rs } body executes + trace_depth restores + trace echo `+ hello-rs` reaches log; background `hello-rs &` -> NotImplemented(background); subshell `( hello-rs )` -> NotImplemented(subshell); fn-shadowing `fn mycmd ...; mycmd` -> fn runs, status=0; spawn-failure errstr begins "spawn failed"). u-test now reports **14 cross-module flows**; new `u-test: eval exec OK` line between `u-test: eval stmt OK` and `u-test: all OK`. Reference doc 94 status section extended. ~95 LOC stmt.rs additions + ~170 LOC u-test extension. | `c2661a8` | usr workspace `cargo build --release` clean (17 binaries; no warnings; both host + aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs; new joey-reported `u-test: eval exec OK` line; 7 spawn-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; trace echo `+ hello-rs` confirmed in boot log; hello-rs banner appears 5 times in log (joey + alloc-smoke + flow_process_pipe + probe 1 + probe 3); u-test now reports 14 flows; all PASS |
| U-6b hash fixup | Update U-6b row with the impl's hash. | *(pending)* | — |
| U-6b | _(impl SHA `0dc28d9`)_ Statement evaluation -- **second sub-chunk of the U-6 evaluator arc**. New `usr/utopia/libutopia/src/eval/stmt.rs` (~620 LOC) with `StatementFlow` (Normal/Return/Break/Continue), `eval_statement(env, stmt) -> EvalResult<StatementFlow>`, `eval_block(env, stmts)`, `eval_script(env, script)`, and the convenience `eval_source(env, src)` (parse + eval in one call). Implements every StatementKind except external SimpleCommand: Let / Assign / FnDecl / Return / Break / Continue / If (with elif chain + else) / While / For / Case (statement form -- glob-match patterns; first-match-wins; no-match returns Normal + status=0 unlike case-as-expression's NoCaseMatch) / Try / Trace / OnNote / MaskNote. **Pipeline** at U-6b supports single-element only: CommandKind::Simple where argv[0] resolves to a defined fn (invoke_function: push_scope, bind $0=name + $1..$N positional + $* full-args-list + FnDecl::args by name, eval_block, pop_scope; Return normalizes to Normal at call site; unconsumed Break/Continue across function boundary likewise) OR CommandKind::BraceBlock (executes in current scope per scripture 5.6 -- no fork) OR CommandKind::Arith (eval expr; status=0 if non-zero result else 1 per rc convention). Multi-element pipeline / external SimpleCommand / Subshell / background `&` / redirects -> NotImplemented (U-6c/d). **Implicit-fail** (scripture 8.1 + 8.3 + 8.9): `eval_block` checks after each Normal-returning statement -- if `$status != 0` AND `should_propagate_failure(env, stmt)` returns true (Pipeline first-element has `fail_propagate` set OR `!env.interactive && implicit_fail_suppressed == 0`), returns StatementFlow::Return. `Env::interactive` defaults to false (script mode); U-6g main loop sets true. **Try/Catch** (scripture 7.7 + 8.6): increments `implicit_fail_suppressed` around body; on body Err OR post-body $status != 0, sets $errstr from `EvalErrorKind::Display` + $status=1, runs catch. **Trace** (scripture 7.8): increments `trace_depth` around body; actual echo-to-stderr deferred to U-6c when argv-to-stderr surface lands alongside external commands. **OnNote / MaskNote** (scripture 9.5 + 10.7 + 10.8): OnNote registers handler in `Env::note_handlers` (BTreeMap by name; last-write-wins); MaskNote pass-through executes body (runtime masking + handler delivery wires at U-7 alongside note-fd polling). **Word -> argv expansion**: new `eval_word` + `eval_value_token` helpers mirror `eval_expr`'s atom dispatch on Token-shaped Word nodes (since `Word` holds raw Tokens, not Exprs); cross-product Concat for `Word::Concat`. **For-loop element binding**: each iteration sets `var_name -> Value::scalar(element)` (rc-style: loop var is a scalar even if the list has multi-word elements); var persists after loop. **Env extensions**: `interactive: bool`, `implicit_fail_suppressed: u32`, `trace_depth: u32`, `note_handlers: BTreeMap<String, Vec<Statement>>` + `note_handler_set` / `note_handler_get` / `note_handler_defined` accessors. New `eval_source` returns `EvalErrorKind::Internal("parse failed")` on parse-time error with the formatted `ParseErrorKind` placed in `$errstr` via Debug format. **Locked U-6b design decisions**: (1) StatementFlow as enum (not bool/i32) -- explicit Return/Break/Continue signals are clearer and self-document the flow contract; (2) Implicit-fail check in eval_block (not eval_statement) -- centralizes the post-statement bookkeeping at one point; (3) `Env::interactive` default false -- script mode is the strict default; the REPL opts in; (4) Function-call normalizes unconsumed Break/Continue to Normal -- prevents leakage; rc convention; (5) Case-statement no-match returns Normal+status=0 (NOT NoCaseMatch error) -- scripture is permissive on no-match in statement form; case-as-expression remains strict; (6) Arith command status=0 iff result != 0 -- rc/bash convention (zero is "false"); (7) MaskNote pass-through at U-6b -- runtime masking is a U-7 concern alongside note-fd polling; the body still executes so any side-effects fire; (8) `eval_word` mirrors `eval_expr` for Token-shape atoms rather than building synthetic Exprs -- cleaner code path with no Token-to-Expr lift for Subst/Backtick (which would need re-parsing); (9) `eval_source` uses `EvalErrorKind::Internal("parse failed")` for parse errors -- the static tag identifies the source; the formatted error goes to `$errstr` via Debug. **Known v1.x deferral**: a list literal `(a b c)` inline inside a `for` header (`for (x in (a b c)) { ... }`) currently triggers a parse-time `UnexpectedEof { expected: "`)`" }` error from `parse_list_literal`. The `for (x in $xs) { ... }` idiom with a pre-bound list var works cleanly (and is the common case). Investigation deferred -- low-priority since the working idiom is what users will reach for; the inline list pattern is uncommon in shell scripts (lists are typically named). Reference doc 94 status section extended. Extended `/u-test` with `flow_eval_stmt` (14 boot-time probes runtime-validated under ThylaAlloc: Let / bare-assign-to-global from fn body / fn with positional args (let-scope isolation verified) / if-elif-else / while+break (counts to 3) / for-loop (last-element after iter) / case-stmt match + no-match-Normal / try/catch on Pipeline-arith status=1 / try/catch on eval error (DivByZero) with errstr set / trace body executes + trace_depth scopes / on-note registers handler / brace-block shares scope / script-mode implicit-fail propagates Return up). u-test now reports 13 cross-module flows; all PASS. | `0dc28d9` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new stmt module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs (bimodal QEMU TCG variance confirmed transient); new joey-reported `u-test: eval stmt OK` line between `u-test: eval expr OK` and `u-test: all OK`; 14 statement-flow probes validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 13 flows; all PASS |
| U-6a hash fixup | Update U-6a row with the impl's hash. | *(pending)* | — |
| U-6a | _(impl SHA `3be8cba`)_ Evaluator scaffold + value model + expression evaluation for the no-side-effect subset -- **first sub-chunk of the U-6 arc; opens the evaluator sub-arc of Phase 7**. New `usr/utopia/libutopia/src/eval/` module with `{mod, value, env, error, glob, expr}.rs`. **Public surface**: `Value(pub Vec<String>)` (the unified-list value model -- a "scalar" is a one-element list per scripture sections 4.1 + 4.2 + 6.3; collapses bash/zsh's scalar-vs-array distinction into one Vec representation, matching rc; argv expansion + `$"var` join + `$#var` length emerge naturally); `Env` (variable scope stack via `Vec<BTreeMap<String, Value>>` with `let_set` writing the topmost frame, bare `assign` walking top-down to the first defining frame else global per scripture 6.2 dynamic scoping; function table `BTreeMap<String, FnDecl>`; `$status` / `$errstr` / `$cwd` accessors; `push_scope` / `pop_scope` for U-6b function call entry/exit); `EvalError { kind, span }` + `EvalErrorKind` (NonNumeric / DivByZero / Overflow / InvalidShift / NoCaseMatch / InvalidIndex (reserved) / NotImplemented / Internal); `EvalResult<T>` alias; `eval_expr(env, expr) -> EvalResult<Value>` recursive walker. **Implemented ExprKind variants**: atoms (Word + SingleQuoted + DoubleQuoted with $var + $#var interp + Integer + Var + VarLen + VarNoSplit + VarIndex 1-indexed + VarSlice inclusive); composites (List flattening + Concat with rc-style cross-product semantics); operators (full BinOp arith -- Add/Sub/Mul/Div/Mod/Pow/BitAnd/BitOr/BitXor/Shl/Shr -- via checked arithmetic; logical Or/And short-circuit; comparison Lt/Le/Gt/Ge/Eq/Ne with numeric-first-else-string rule collapsing scripture 6.13 + 6.14); UnOp (Neg + BitNot + Not); Match (Glob via pattern matcher + In via list membership; Regex returns NotImplemented); Case-as-expression (first-match-wins via glob; NoCaseMatch error if exhausted). **Deferred (return NotImplemented at v1.0)**: Subst/Backtick/ProcSubIn/ProcSubOut (need spawn -- U-6f); Match::Regex (need regex engine -- v1.x). **New sibling module `glob`**: rc/POSIX-shape pattern matcher (`*` no-`/` + `?` no-`/` + `[abc]` + `[!abc]` + `[a-z]` ranges; bytewise UTF-8-safe). NO filesystem walks -- pattern match only, used by case-as-expression + `matches` operator; argv-time glob expansion lands at U-6e. **Locked U-6a design decisions**: (1) Value as unified `Vec<String>` (not `enum { String, List }`) -- every consumer would match-then-collapse anyway; scripture preserves the conceptual distinction but rc's representation is uniform; (2) `BTreeMap` (not `HashMap`) for scope frames -- `alloc::collections::BTreeMap` is in no_std + alloc; HashMap would require hashbrown; per-frame counts are small so O(log n) is negligible; (3) Undefined variable -> `Value::empty()` (rc convention; scripture silent); (4) Truthiness convention pinned (empty / "" / "0" / "false" -> false; else true) -- scripture silent on expression-value truthiness; (5) Comparison: numeric if both parse as i64, else lexicographic string -- collapses scripture 6.13 + 6.14; (6) Arithmetic overflow -> error (not wrap/saturate) -- scripture silent; latent bugs should surface at evaluation; (7) Out-of-range / negative-index VarIndex -> empty (rc); `InvalidIndex` variant reserved for strict mode; (8) NotImplemented carries a static string for clear error formatting; (9) Boot probe `ev()` helper strips the trailing Eof token -- mirrors parse.rs's discipline at each placeholder site (parse_expr_tokens reports `TrailingTokensInExpr` if Eof remains). **Test posture**: cfg(test) host unit tests (9 in glob.rs + 26 in expr.rs; can't run on host directly because libutopia is no_std with no panic_handler -- they document expected behaviour); /u-test extended with 15-probe `flow_eval_expr` runtime-validated under ThylaAlloc on real arm64 (Integer in Arith + arith precedence + DivByZero + Var lookup + Undefined empty + VarLen + VarIndex 1-indexed + VarIndex OOR + VarSlice + VarNoSplit join + List flatten + Concat with var + DoubleQuoted $var+$#var interp + case-as-expression first-match + Subst+Regex NotImplemented). New reference doc `docs/reference/94-utopia-eval.md`. ~960 LOC eval (value 165 + env 220 + error 110 + glob 245 + expr 220) + ~480 LOC cfg(test) tests + ~310 LOC u-test extension. u-test now reports 12 cross-module flows; all PASS. | `3be8cba` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new eval module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass on two consecutive runs (bimodal QEMU TCG variance confirmed transient); new joey-reported `u-test: eval expr OK` line between `u-test: parser full OK` and `u-test: all OK`; 15 evaluator-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; 35 new `#[cfg(test)]` tests in glob + expr (host can't link but document expected behaviour); u-test now reports 12 flows; all PASS |
| U-5d hash fixup | Update U-5d row with the impl's hash. | *(pending)* | — |
| U-5d | _(impl SHA `255f80b`)_ Pattern matching + try/catch + trace + on/mask + case-as-expression -- **fourth and final sub-chunk of the U-5 arc; the U-5 parser arc is COMPLETE**. New statement-level constructs (per scripture sections 7.1 + 7.7 + 7.8 + 9.5 + 10.7 + 10.8): `case $x { pat ... => body ; ... }` (pattern matching; multi-pattern arms space-separated; `*` is the catch-all glob; first match wins; no fallthrough), `try { ... } catch { ... }` (error handling; catch is REQUIRED), `trace { ... }` (block-scoped tracing), `on note 'name' { ... }` (note handler registration), `mask note 'name' { ... }` (note delivery suppression). New expression-level construct (scripture 7.2): `case $x { pat => value ... }` as an expression -- works in every `ExprContext` (`parse_primary` for Cond/Arith Pratt path; `parse_value_top` / `parse_list_top` for non-Pratt Value/Return/List paths). **AST extensions**: `StatementKind` gains 5 variants (`Case(Box<CaseStmt>)` / `Try(Box<TryStmt>)` / `Trace(Box<TraceStmt>)` / `OnNote(Box<OnNoteStmt>)` / `MaskNote(Box<MaskNoteStmt>)`); `ExprKind` gains `Case(Box<CaseExpr>)`. New struct types: `CaseStmt` / `CaseArm` (statement form; arm body = single Statement; users wrap in `{ ... }` for multi-statement), `CaseExpr` / `CaseExprArm` (expression form; arm value = single Expr in Value context), `TryStmt` (body + catch + span), `TraceStmt` (body + span), `OnNoteStmt` / `MaskNoteStmt` (note_name as Expr + body + span). **Locked U-5d design decisions**: (1) Arm body is a single Statement -- cleaner than detecting next-arm-start ambiguity; (2) `catch` is REQUIRED after `try` -- scripture 7.7 always pairs them; `try { a }` emits `UnexpectedToken { expected: "`catch`" }`; (3) `on` / `mask` require the LITERAL `Word("note")` token after the keyword (`note` is NOT a reserved word per the U-5a taxonomy; the parser matches by text -- `on banana { ... }` errors); (4) Note names parse as any Value-context Expr (SingleQuoted / DoubleQuoted / Word / Concat all accepted; evaluator resolves to a string at registration time); (5) case-as-expression dispatched in 3 places (`parse_primary` Pratt path + `parse_value_top` + `parse_list_top` non-Pratt paths) so the AST has ONE canonical expression shape; (6) Empty pattern list (`case $x { => body }`) emits `EmptyCasePattern`; (7) Pattern list terminator is `=>` at depth 0 -- top-level Newline/Semicolon/RBrace inside pattern list is `UnexpectedToken { expected: "`=>`" }`; (8) Pattern collection reuses U-5c's span-gap whitespace-split helper, renamed from `split_index_body` to `split_value_tokens_on_whitespace` and exposed `pub(super)`. **Parser dispatch update**: `parse_statement` dispatches `Case` / `Try` / `Trace` / `On` / `Mask` to dedicated parsers (the U-5b `InvalidStatement` emissions for those keywords are removed); `Else` / `Catch` / `In` at statement-start still emit `InvalidStatement` (no surrounding context). **New `ParseErrorKind` variant**: `EmptyCasePattern`. Other shape errors continue to use `UnexpectedToken { expected }` / `UnexpectedEof { expected }` with appropriate labels. Modifications: ~500 LOC `parse.rs` additions (parse_case_stmt + parse_try + parse_trace + parse_on_note + parse_mask_note + 3 helper collectors + expect_note_keyword); ~250 LOC `expr.rs` additions (parse_case_expr + parse_case_expr_arm + 3 helper collectors); ~100 LOC `ast.rs` (5 new struct types + ExprKind::Case + StatementKind variants); ~10 LOC `error.rs` (EmptyCasePattern + Display). Reference doc 93 extended with section 8.7 covering AST extensions, public surface, pattern collection, locked design decisions, error kinds, impl file map. Extended `/u-test` with new `flow_parser_full` (12 boot-time probes runtime-validated under `ThylaAlloc`: case-stmt basic shape; multi-pattern arm; brace-block arm body; try/catch; try-without-catch error; trace; on note 'snare:int' with single-quoted name; mask note; on-without-note-keyword error; case-as-expression in let; newline-separated case arms; empty-pattern error). u-test now reports 11 cross-module flows; all PASS. | `255f80b` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new U-5d code compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser full OK` line between `u-test: parser expr OK` and `u-test: all OK`; 12 statement-shape + expression-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; 24 new `#[cfg(test)]` tests (20 in parse.rs + 4 in expr.rs); u-test now reports 11 flows; all PASS |
| U-5c hash fixup | Update U-5c row with the impl's hash. | *(pending)* | — |
| U-5c | _(impl SHA `5ecf108`)_ Expression parser for rc-shape expression contexts -- **third sub-chunk of the U-5 arc; closes the U-5b->U-5c lift**. New `usr/utopia/libutopia/src/parser/expr.rs` (~1100 LOC parser + ~500 LOC `#[cfg(test)]` tests; 35 tests). Public entry: `parse_expr_tokens(Vec<Token>, source_len: usize, ctx: ExprContext) -> ParseResult<Expr>`. Pratt-style precedence climbing over an owned token stream; eagerly parsed substitution bodies; full operator surface for arith + cond contexts; upfront arith retokenization that splits Word tokens containing operator chars (`+ - * /`) into proper Plus/Minus/Star/Slash tokens (the lexer emits these as word chars, not operator tokens -- the retokenize aligns with the Pratt climbing's token-kind expectations). **The AST now has ONE canonical shape**: every expression slot is an `Expr` (no `Vec<Token>` placeholders remain). `IfStmt.cond` / `IfStmt.elif_branches[].0` / `ForStmt.list_expr` / `WhileStmt.cond` / `LetStmt.value` / `AssignStmt.value` / `StatementKind::Return(Option<Expr>)` / `CommandKind::Arith(Expr)` all hold `Expr`. New `ExprKind` variants cover Atoms (`Word(String)` / `SingleQuoted(String)` / `DoubleQuoted(Vec<DqPart>)` / `Integer(i64)` (arith) / `Var(String)` / `VarLen(String)` / `VarNoSplit(String)` / `VarIndex(String, Box<Expr>)` / `VarSlice(String, Box<Expr>, Box<Expr>)` / `Subst(Box<Script>)` (eager) / `Backtick(Box<Script>)` (eager) / `ProcSubIn(Box<Script>)` / `ProcSubOut(Box<Script>)` / `Regex(String)`), Composites (`List(Vec<Expr>)` / `Concat(Vec<Expr>)`), Operators (`BinOp(BinOp, Box<Expr>, Box<Expr>)` / `UnOp(UnOp, Box<Expr>)` / `Match(MatchOp, Box<Expr>, Box<Expr>)`). New `BinOp` enum: Add/Sub/Mul/Div/Mod/Pow/BitAnd/BitOr/BitXor/Shl/Shr/Lt/Le/Gt/Ge/Eq/Ne/And/Or. New `UnOp`: Neg/BitNot/Not. New `MatchOp`: Glob (`matches`) / In (`in`) / Regex (`=~`). New `ExprContext`: Value / Cond / Arith / List / Return. Pratt precedence chain `parse_or → parse_and → parse_bit_or → parse_bit_xor → parse_bit_and → parse_eq → parse_rel → parse_shift → parse_add → parse_mul → parse_pow → parse_unary → parse_primary`; each level activates only in contexts where it's syntactically valid (e.g., bit ops only in Arith; logical+match in Cond+Arith; relational in Cond+Arith). **`**` (Pow) detection** uses SPAN-ADJACENT `Star Star` after retokenization (lexer doesn't emit a `**` token). **Var indexing** detects span-adjacent `LParen` after a `Var` token, splits the body on top-level whitespace via span-gap detection, parses each piece in Arith context (one piece -> `VarIndex`; two pieces -> `VarSlice`; else `InvalidVarIndex`). **Substitution body lifting** is EAGER (`$(cmd)`, `` `{cmd}` ``, `<(cmd)`, `>(cmd)` bodies all `parse(&body)` recursively at U-5c -- the AST is fully resolved; parse errors surface at parse time; the evaluator sees a uniform shape). New `ParseErrorKind` variants: `EmptyExpression` / `UnexpectedTokenInExpr` / `InvalidArithLiteral` / `InvalidVarIndex` / `TrailingTokensInExpr`. **Locked U-5c design decisions**: (1) in-place lifting (the AST has ONE canonical shape; no separate `lift_exprs(&mut Script)` pass -- closes the U-5b handoff open question); (2) eager substitution-body parse; (3) top-level operators in Value context are disallowed (`let x = 5 + 3` is `TrailingTokensInExpr`; user writes `let x = (( 5 + 3 ))`); (4) arith retokenization runs UPFRONT at parser construction; (5) implicit list allowed in List context (`for (f in a b c)` builds 3-element List); (6) empty list `()` parses to `List(vec![])`; (7) var-index range validated at evaluation time (parser only checks shape); (8) `**` requires span-adjacency on the two Star tokens. **Known v1.x deferrals**: `%` operator can't be lexed (lexer rejects `UnexpectedChar('%')`); `<<` / `>>` shift ops can't be lexed in arith (lexer tries heredoc-start); substitution body spans are body-relative (no outer-coordinate translation yet). Modifications to `parse.rs`: each placeholder construction site (`parse_if`, `parse_for`, `parse_while`, `parse_let`, `parse_assign`, `parse_return`, `parse_arith_command`) calls `parse_expr_tokens(...)` on the collected token stream. Modifications to `ast.rs`: `Vec<Token>` placeholder fields changed to `Expr`. Modifications to `error.rs`: 5 new `ParseErrorKind` variants. Reference doc 93 extended with §8.6 covering AST extensions, public API, Pratt precedence chain, arith retokenization, concat semantics, substitution body lifting, var indexing, locked design decisions, error kinds, impl file map. Extended `/u-test` with new `flow_parser_expr` (10 boot-time probes + 1 bonus, runtime-validated under `ThylaAlloc`: arith literal lift + Add; explicit list literal; if cond BinOp(Eq); for list_expr Var; VarIndex; Concat in value position; match operator `matches`; substitution body lifts to sub-Script; direct `parse_expr_tokens` API for paren grouping in arith; arith literal error for `hello`; bonus unary `!` in cond). | `5ecf108` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new parser/expr module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser expr OK` line between `u-test: parser core OK` and `u-test: all OK`; 10 expression-shape probes + 1 bonus validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 10 flows; all PASS |
| U-5b hash fixup | Update U-5b row with the impl's hash. | *(pending)* | — |
| U-5b | _(impl SHA `c94b7e9`)_ Parser core + AST for rc-shape statements -- **second sub-chunk of the U-5 arc**. New `usr/utopia/libutopia/src/parser/ast.rs` (~250 LOC: AST node types) + `parse.rs` (~900 LOC: recursive-descent parser; 37 `#[cfg(test)]` tests). Public surface: `parse(&str) -> ParseResult<Script>` (convenience: tokenize + parse) + `parse_tokens(Vec<Token>, source_len) -> ParseResult<Script>`. AST: `Script { statements, span }`, `Statement { kind, span }` with `StatementKind` variants `Pipeline / If(Box<IfStmt>) / For(Box<ForStmt>) / While(Box<WhileStmt>) / FnDecl(Box<FnDecl>) / Let(Box<LetStmt>) / Assign(Box<AssignStmt>) / Return(Option<Vec<Token>>) / Break / Continue`; `Pipeline { elements: Vec<PipelineElement>, background: bool, span }`; `PipelineElement { command, tolerate_failure: bool, span }` (`?|` sets `tolerate_failure = true` on the LEFT element per scripture section 8.4); `Command { kind, redirects, fail_propagate: bool, span }` with `CommandKind` variants `Simple(SimpleCommand) / BraceBlock(Vec<Statement>) / Subshell(Vec<Statement>) / Arith(Vec<Token>)` (Arith body stored as raw tokens for U-5c); `Word::Single(Token) | Word::Concat(Vec<Token>)` (Concat engaged only when value tokens are span-adjacent via Caret); `Redirect { kind, target: Option<Word>, span }` with `RedirectKind::Stdin / Stdout / Append / Heredoc { interp, strip_tabs, body: Vec<DqPart> }`. Heredoc body resolution: parser pre-extracts all `HeredocBody` parts at construction time into a FIFO `VecDeque<Vec<DqPart>>` (via `core::mem::take` from owned tokens; no clones); each `HeredocStart` pops the next body when parsing the redirect; `HeredocBody` tokens themselves are treated as separators during statement walking. Expression contexts (`if (cond)`, `for (var in expr)`, `while (cond)`, `let x = expr`, bare assignment value, `return expr`) all stored as raw `Vec<Token>` placeholders -- U-5c walks them into Expression trees. Parser dispatch: keyword tokens dispatch to dedicated parsers (`parse_if` / `parse_for` / `parse_while` / `parse_fn` / `parse_let` / `parse_return` / `parse_break_or_continue`); two-token lookahead detects assignment (Word(IDENT) + Equal); `Else / Catch / Case / Try / Trace / On / Mask / In` at statement-start emit `InvalidStatement`; otherwise -> pipeline. **Locked U-5b design decisions** (user-signed-off this session): (1) strict rc -- `--key=value` is `UnexpectedEqualInCommand` (users quote); (2) `^` concat requires span-adjacency on both sides -- `$a ^ $b` is three tokens; the orphan Caret causes the outer terminator check to error; (3) `case` deferred to U-5d per scripture section 19; (4) postfix `?` requires a statement terminator (`Newline` / `;` / `|` / `?|` / `&` / `}` / `)` / EOF) -- `cmd ? arg` is `UnexpectedTokenAfterFailPropagate`; (5) empty pipeline element `cmd1 \| \| cmd2` is `EmptyPipelineElement` per scripture section 17.10; (6) redirects may interleave with words; (7) `fn name args { ... }` with strict positional args at v1.0; (8) trailing `;` allowed; (9) list literal vs subshell distinguished by value-position vs command-position; (10) quoted reserved words stay strings. New `ParseErrorKind` variants: `UnexpectedToken / UnexpectedEof / ExpectedIdent / EmptyPipelineElement / UnexpectedTokenAfterFailPropagate / InvalidStatement / InvalidAssignmentName / UnexpectedEqualInCommand / UnexpectedCaret`. Every AST node carries `Span` from first-token-start through last-token-end. Reference doc 93 extended with section 8.5 "AST + parser core (U-5b)" covering the AST taxonomy + parser dispatch + locked design decisions + impl file map. Extended `/u-test` with new `flow_parser_core` (8 boot-time probes runtime-validated under `ThylaAlloc`: empty -> 0 stmts; semicolon multi-stmt; pipeline `?|` tolerate flag on left; if/elif/else; for loop; fn with args + body; heredoc body inlined into Redirect; postfix `?` error case; bonus Word::Concat). | `c94b7e9` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass; new joey-reported `u-test: parser core OK` line between `u-test: parser lexer OK` and `u-test: all OK`; 8 AST-shape probes validated end-to-end on Thylacine target via ThylaAlloc heap path; u-test now reports 9 flows; all PASS |
| U-5a hash fixup | Update U-5a row with the impl's hash. | *(pending)* | — |
| U-5a | _(impl SHA `94bff11`)_ Tokenizer for the rc-shape parser -- **first sub-chunk of the U-5 arc; opens the parser sub-arc of Phase 7**. New `usr/utopia/libutopia/src/parser/` module with `{mod, span, token, error, lexer}.rs`. Public surface: `tokenize(&str) -> ParseResult<Vec<Token>>` produces a token stream ending in a synthetic `Eof` point span. Every `Token` carries a `Span { start, end }` (byte offsets into source; UTF-8-safe slice). `TokenKind` taxonomy covers (per UTOPIA-SHELL-DESIGN.md sections 5-9): bare `Word` with glob meta chars preserved + `?` disambiguation (word char if surrounded by word chars; `Question` / `PipeTolerate` at separator pos); `SingleQuoted` with rc `''` escape; `DoubleQuoted(Vec<DqPart>)` pre-parsed into Literal/Var/VarLen/Subst parts with `\n \t \\ \" \$` escape resolution; top-level `Var($name)` / `VarLen($#name)` / `VarNoSplit($"name)`; substitutions `Subst($(cmd))` (raw body; balanced-paren scan respects quotes + escapes) + `Backtick(` `` `{cmd}` `` `)` (rc-shape, closing backtick required); process substitution `ProcSubIn(<(cmd))` / `ProcSubOut(>(cmd))`; heredocs `HeredocStart { tag, interp, strip_tabs }` (three flavors: `<<TAG`, `<<-TAG`, `<<"TAG"`) with body collection at next `Newline` (FIFO drain queue) -> `HeredocBody(parts)`; regex literal `Regex(/pat/)` (one-shot lex mode triggered by emitting `EqualTilde`, `\/` -> `/` escape resolved). Operators + punctuation: `Pipe` `PipeTolerate (?\|)` `AndAnd (&&)` `OrOr (\|\|)` `Ampersand` `Semicolon` `Newline` `LBrace` `RBrace` `LParen` `RParen` `DoubleLParen ((` `DoubleRParen ))` `Less` `Greater` `GreaterGreater (>>)` `Equal` `DoubleEqual (==)` `NotEqual (!=)` `LessEqual` `GreaterEqual` `EqualTilde (=~)` `FatArrow (=>)` `Plus` `Minus` `Star` `Slash` `Percent` `Caret (^)` `Tilde (~)` `Bang (!)` `Question (?)`. Reserved words (all 16 per scripture section 5.1): `Fn` `Let` `If` `Else` `Case` `For` `While` `In` `Try` `Catch` `Return` `Break` `Continue` `On` `Mask` `Trace` — matched against scanned word text after the scan completes; quoted forms (`'fn'`) stay strings. Errors: `ParseError { kind, span }` with kinds covering all unterminated forms (single quote, double quote, substitution, backtick, process sub, heredoc, heredoc tag, regex) + `EmptyVarName` / `EmptyVarLenName` / `EmptyVarNoSplitName` / `InvalidHeredocStart` / `UnexpectedChar`; single-error-stop semantics per scripture section 19 open question #2. UTF-8 aware throughout (lex advances by whole UTF-8 chars; non-ASCII bytes are word chars). Comments (`#` to EOL) and line continuation (`\<nl>`) skipped silently. Free helpers `find_balanced_paren_close` + `find_balanced_brace_close` track nesting + respect single + double quotes + `\` escapes; shared between the main lex path and the `parse_dq_or_heredoc_body` helper that parses both DQ string interiors and interp heredoc bodies. **Deferred to U-5b/c/d**: parser core (U-5b — commands, pipelines, control flow, AST node types); expression layer (U-5c — variable refs with index/slice, arithmetic re-interpretation inside `(( ))`, concatenation); pattern matching + match operators + final glue (U-5d). Lexer-mode-awareness for arithmetic (`<<` as shift-left, `^` as XOR, etc.) deferred to U-5c. ~900 LOC lexer (across 5 files) + ~560 LOC `#[cfg(test)]` host unit tests (49 tests covering every load-bearing path). New reference doc `docs/reference/93-utopia-parser.md`. Extended `/u-test` with new `flow_parser_lexer` (7 boot-time probes runtime-validated under `ThylaAlloc`: empty input, multi-word + `Pipe`, reserved `Let`, `DoubleQuoted` canonical example from scripture section 6.5, heredoc body collection, regex literal after `=~`, error case + span tracking). | `94bff11` | usr workspace `cargo build --release` clean (17 binaries; no warnings; new parser module compiles cleanly under aarch64-unknown-none); `tools/test.sh` boot pass; new joey-reported `u-test: parser lexer OK` line between `u-test: line editor OK` and `u-test: all OK`; 7 cross-construct probes validated end-to-end on Thylacine target via ThylaAlloc heap path |
| U-4d hash fixup | Update U-4d row with the impl's hash. | *(pending)* | — |
| U-4d | _(impl SHA `9a3a4fb`)_ Tab completion via pluggable `CompletionSource` trait -- **fourth and final sub-chunk of the U-4 arc; the line editor sub-arc is COMPLETE**. New `CompletionSource` trait + `Completions { replace_range, candidates }` struct + shipped `StaticCompletionSource` (word-prefix matcher; preserves source order; used by tests + early-bringup until U-6 plugs the real shell-driven source). New `longest_common_prefix(strs: &[String])` UTF-8-safe helper (returned byte length rounded DOWN to nearest char boundary). New public `LineEditor::set_completion_source(source: Box<dyn CompletionSource>)` + `clear_completion_source()` setters. New `EditorAction::ShowCompletions(Vec<String>)` variant emitted when Tab dispatches with 2+ candidates AND no further common-prefix extension is possible -- the main loop displays the candidate list (typically newline-separated below the prompt) then redraws. Tab key (Ctrl-I, byte 0x09) now maps to `Action::Complete` (was `Action::Ignore` at U-4a; the existing `tab_is_ignored_at_u4a` test renamed to `tab_with_no_completion_source_is_noop`). Dispatch: 0 candidates -> NoChange; 1 candidate -> replace `buffer[comp.replace_range]` with the candidate + cursor to end of replacement + Redraw; 2+ candidates with `common_prefix.len() > current_word.len()` -> extend buffer to common prefix + Redraw; 2+ with no extension -> `ShowCompletions(comp.candidates)`. Tab in search mode falls through to do_search_cancel (the default catch-all in apply_in_search). New cfg(test) tests: longest_common_prefix basic/single/empty/utf8-safe; StaticCompletionSource word-prefix + mid-buffer; Tab single-candidate completes; Tab multi-candidate extends to common prefix; Tab multi-candidate no-extension emits ShowCompletions; Tab zero-candidates NoChange; clear_completion_source disables Tab. Extended `flow_line_editor` /u-test with 3 new U-4d probes (Probe 14: 3-candidate ShowCompletions + common-prefix extension; Probe 15: single-candidate Tab; Probe 16: Tab without source NoChange). Reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4d sections (trait + struct + StaticCompletionSource + Tab dispatch semantics + ShowCompletions main-loop contract). Title updated to "U-4 arc". | `9a3a4fb` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 13 to 16 probes (3 new U-4d probes all PASS); joey line "u-test: line editor OK" unchanged in shape; Tab completion machinery validated end-to-end through the typed engine surface with the shipped StaticCompletionSource |
| U-4c hash fixup | Update U-4c row with the impl's hash. | *(pending)* | — |
| U-4c | _(impl SHA `a80908a`)_ History nav refinements + Ctrl-R incremental search -- third sub-chunk of the U-4 arc. **Smart Up/Down** (zsh/fish-style): in multi-line buffer with cursor NOT on first/last line, Up/Down navigate cursor between buffer lines (column-preserving via new `desired_col: Option<usize>` sticky tracker); when cursor is on first/last line OR buffer is single-line, fall through to history-prev/next. New `do_cursor_up_line` + `do_cursor_down_line` + `line_start_byte` + `line_end_byte` + `col_in_line` + `byte_offset_at_col` helpers (UTF-8 aware via libutopia::ansi::visible_width). desired_col preserved across consecutive Up/Down; cleared on any horizontal motion / edit. **Ctrl-R incremental search**: new `Mode::Search { query, match_index, saved_buffer, saved_cursor }` variant. Ctrl-R (Action::SearchHistory) enters search; printable chars append to query + re-search from history.len()-1 backward; Backspace narrows; Ctrl-R again steps back to next-older match; Ctrl-N steps forward to newer; Enter Accepts the matched history line via the normal Accept path (balance check + reset); Ctrl-C / Ctrl-G / Ctrl-D / any other action cancels + restores saved buffer/cursor (v1.x can refine to readline's full action re-dispatch). Substring match (str::contains) at v1.0; fuzzy is v1.x. New search-mode render: `(reverse-i-search)\`<query>': <matched>` / `(failed reverse-i-search)\`<query>':` (no match), cursor positioned at end of query. **History cap**: HISTORY_CAP = 10000 (per UTOPIA-SHELL-DESIGN.md §12.4); push_history FIFO-evicts oldest entry when exceeded. **New public accessors**: `is_searching() -> bool`, `search_query() -> Option<&str>`, `search_match_index() -> Option<usize>`. New cfg(test) tests: smart Up/Down in multi-line; smart Up at first line falls back to history; desired_col preserved across consecutive Up/Down; push_history caps at HISTORY_CAP; Ctrl-R enters search; query appends find newest match; Ctrl-R cycles to older match; Backspace widens match; Enter accepts; Cancel restores saved state; no-match shows `failed` prefix; render shape contains expected prefix. Extended `flow_line_editor` /u-test with 3 new U-4c probes (Probe 11: smart Up cursor-up + fall-through; Probe 12: Ctrl-R + 2 chars + Ctrl-R-step + Enter Accept; Probe 13: search Cancel restores). Reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4c sections (smart nav semantics + search-mode state machine + history cap + new public accessors + probes 11-13). | `a80908a` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 10 to 13 probes (3 new U-4c probes all PASS); joey line "u-test: line editor OK" unchanged in shape; smart Up/Down + Ctrl-R search + history cap all validated end-to-end through the typed engine surface |
| U-4b hash fixup | Update U-4b row with the impl's hash. | *(pending)* | — |
| U-4b | _(impl SHA `28276a7`)_ Multi-line + bracket-balance continuation -- second sub-chunk of the U-4 arc. Adds `BalanceState` struct + `balance(s: &str)` lightweight tracker (brace + paren + bracket depth, single + double quotes, `\\` escape, `#` line-comments, trailing unescaped backslash). Negative bracket depth treated as balanced (malformed but submit + let parser report). `is_balanced()` + `awaits_continuation()` accessors. Enter behaviour: if `awaits_continuation()`, insert `\n` at cursor (yielding Redraw) instead of submitting; trailing `\\` stays in buffer verbatim (parser elides `\\\n` per POSIX/rc -- separating editor from parser per UTOPIA-SHELL-DESIGN.md). Multi-line `render(prompt)` reshaped: emits prompt + first buffer line, then `\r\n\x1b[K<continuation_prefix><line>` per subsequent buffer line; cursor positioned via `\x1b[<n>F` (cursor previous-line) + `\x1b[<col>C`. New `continuation_prefix(prompt_width)` private helper: `(prompt_width - 2)` leading spaces + `⋮` (U+22EE) in `palette::Role::Path` colour + 1 trailing space, total visible width = prompt_width (per UTOPIA-VISUAL.md section 3.2 -- user text on continuation lines aligns with user text on first line). Backspace + Delete auto-join lines (falls out of existing UTF-8 boundary-walking; `\n` is just another char). NO column-preserving Up/Down nav across multi-line buffer (Up/Down stay history-only as in bash; zsh/fish-style conditional cursor-nav lands at U-4c or v1.x). NO ghost-clear of trailing lines when buffer shrinks (the U-6 main loop will track `prev_render_lines` + emit `\x1b[J` at start of each render; documented in `docs/reference/92-utopia-line-editor.md` "Multi-line ghost-clear caveat"). New cfg(test) host unit tests covering BalanceState across brackets / quotes / escapes / comments / trailing-backslash / negative depth + Enter-yields-newline-when-unbalanced + Enter-yields-Accept-when-balanced + multi-line render shape + Backspace joining lines. Updated `flow_line_editor` in /u-test with 4 new U-4b probes: unbalanced-Enter -> Redraw + multi-line round-trip; single-quote isolates brackets; trailing-backslash continuation; multi-line render contains ⋮ + correct cursor positioning. Updated reference doc `docs/reference/92-utopia-line-editor.md` extended with U-4b sections (balance tracker semantics + Enter behaviour + multi-line render strategy + ghost-clear caveat + status). | `28276a7` | usr workspace `cargo build --release` clean (no warnings); `tools/test.sh` boot pass; flow_line_editor extended from 6 to 10 probes (4 new U-4b probes all PASS); joey line "u-test: line editor OK" unchanged in shape; new behaviour validated end-to-end through the typed engine surface |
| U-4a hash fixup | Update U-4a row with the impl's hash. | *(pending)* | — |
| U-4a | _(impl SHA `eccbf0f`)_ `libutopia::line_editor` engine core -- **first sub-chunk of the U-4 line editor arc**. Pure-logic engine consuming byte streams + producing editor actions; raw-mode I/O wiring deferred to U-6 (main loop) + U-PTY (kernel substrate; no PTY at v1.0). New `LineEditor` struct + `EditorAction` enum (NoChange / Redraw / Accept(String) / Cancel / Eof / ClearScreen). ANSI input escape parser state machine: Ground / Escape / Csi { params, param_count, current_has_digits } / Utf8 { buf, expected, have }. Emacs keybindings: Ctrl-A/B/C/D/E/F/H/K/L/N/P/U/W/Y + Backspace (0x7f or 0x08) + Enter (LF or CR) + printable 0x20-0x7e + UTF-8 multi-byte (0xc2..=0xf4 leading + 0x80..=0xbf continuation; accumulated in 4-byte buf, validated via `core::str::from_utf8` before insertion). CSI sequences: A/B/C/D arrows + H/F Home/End + 3~ Delete + 1~/7~ Home + 4~/8~ End. History up/down (Ctrl-P/N + CSI A/B) recognized but no-op when `history.is_empty()` (U-4c populates via `push_history`). Tab (Ctrl-I) no-op at U-4a (U-4d completion hook). Ctrl-R no-op at U-4a (U-4c incremental search). UTF-8 buffer invariant maintained by parser; `String::is_char_boundary` ensures Backspace + Delete walk char boundaries. Defensive `MAX_BUFFER_LEN = 64 KiB` cap; insert/yank refuse to grow past. Single-line `render(prompt)` emits `\r\x1b[K<prompt><buffer>\r\x1b[<n>C` where n = `visible_width(prompt) + visible_width(buffer[..cursor])`; cursor at column 0 omits the trailing `\x1b[<n>C`. New `libutopia::ansi::visible_width(s)` helper -- byte-walks `s`, treats `ESC [ ... final-byte` CSI sequences as zero-width, counts non-CSI chars as 1 column each (v1.0 approximation; grapheme + wcwidth is v1.x). New reference doc `docs/reference/92-utopia-line-editor.md`. ~640 LOC engine + ~290 LOC `#[cfg(test)]` host unit tests + ~150 LOC `flow_line_editor` probe in `/u-test` (6 end-to-end probes runtime-validated at boot: insert+Accept; ANSI arrow split across feed_byte boundaries; Backspace+Ctrl-K+Ctrl-Y round-trip; UTF-8 multi-byte across feed_byte boundaries; history Ctrl-P/Ctrl-N nav including saved-current restore; render() shape). | `*(pending)*` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass with new joey line: "u-test: line editor OK" between "u-test: hardware + cap OK" and "u-test: all OK"; six composed flow probes validated through the typed engine surface |
| U-3 hash fixup | Update U-3 row with the impl's hash. | *(pending)* | — |
| U-3 | _(impl SHA `6713635`)_ Utopia workspace skeleton + `ut` shell skeleton -- the **first chunk in the shell half of Phase 7**. New `libutopia` crate (palette + ansi + path modules) and `utopia-shell` crate (the `ut` binary, v0.0.1-skeleton: Pale Fire version banner + clean exit). Workspace placement: `utopia/libutopia` + `utopia/shell` added as members of the existing `usr/Cargo.toml` (small deviation from UTOPIA-SHELL-DESIGN.md section 14.1's "Cargo workspace root at usr/utopia/" sketch -- directory grouping under `usr/utopia/` preserved, single Cargo workspace; lift to a separate workspace is mechanical if a future chunk needs it). `tools/build.sh::build_userspace` builds the new crates via the existing cargo invocation (no new `build_utopia` shell function); cpio extended with `ut` (40 entries, up from 39 at U-2-test); joey spawns `/ut` after `/u-test` with 7-line spawn-and-verify block. Banner emits 24-bit-colour SGR escapes -- glyph in ember orange (`#e07840` = palette::GLYPH) + tagline in receded steel (`#8898b4` = palette::PATH), each segment closed with `ESC[0m` per UTOPIA-VISUAL.md. Skeleton scope: NO REPL, NO line editor, NO parser, NO evaluator at U-3 -- those land at U-4 (line editor), U-5 (parser), U-6 (evaluator), U-7 (job control), U-8 (Thylacine builtins), U-9..N (coreutils), U-PTY (PTY infrastructure), U-Z (bring-up integration test). ~150 LOC libutopia + ~50 LOC ut + ~25 LOC joey wiring + 2 lines workspace + 1 line build.sh + new reference doc `docs/reference/91-utopia.md`. | `6713635` | usr workspace `cargo build --release` clean (17 binaries; no warnings); `tools/test.sh` boot pass with new joey line: "joey: /ut reaped status=0; Utopia shell skeleton verified"; Pale Fire banner verified byte-for-byte via `cat -v` on boot log -- all five emit-points (glyph FG escape + ⊢ + reset + path FG escape + tagline + reset) match the palette constants |
| U-2-test hash fixup | Update U-2-test row with the impl's hash. | *(pending)* | — |
| U-2-test | `/u-test` — cumulative integration smoke for the libthyla-rs uplift, closes the U-2 arc. _(impl SHA `e037b4c`)_ New binary at `usr/u-test/`; spawned by joey at boot immediately after `/alloc-smoke`. Where alloc-smoke isolates each U-2X module's surface in its own block, u-test runs COMPOSED cross-module flows — the patterns a Utopia builtin will reach for. Six flows: (1) alloc + fs + io (heap-backed Vec slurp of /system.key); (2) process + pipe (pipe() round-trip + spawn hello-rs Stdio::Piped + wait + verify exit); (3) notes + poll + time (open self-notes, poll timeout, self-send + poll-ready + drain); (4) thread + torpor + time (spawn child thread that wakes a shared AtomicU32 + clear_child_tid join); (5) ninep codec (Tversion hand-build + parse + Rversion build + peek); (6) hardware + cap (Mmio/Irq/Dma cap-missing rejects without CAP_HW_CREATE). Each flow prints "u-test: <flow> OK"; final "u-test: all OK" precedes a clean exits(0). On any FAIL, the binary prints "u-test: <flow>: <what> FAILED" + exits 1; joey treats that as a boot failure. ~340 LOC + ~25 LOC joey wiring + 1 line workspace member + 1 line build.sh cpio. New reference doc `docs/reference/90-u-test.md`. **Library half of Phase 7 (the libthyla-rs uplift) is now COMPLETE.** Next: U-3 (Utopia workspace skeleton + ut shell skeleton). | `*(pending)*` | usr workspace cargo build clean (16 binaries; no warnings); `tools/test.sh` boot pass with new joey block: "u-test: starting ... u-test: all OK" + "joey: /u-test reaped status=0; U-2 arc integration verified" |
| U-2h-hardware hash fixup | Update U-2h-hardware row with the impl's hash. | *(pending)* | — |
| U-2h-hardware | `t::hardware::{Mmio, Irq, Dma}` — typed RAII wrappers over the kernel `KObj_MMIO` / `KObj_IRQ` / `KObj_DMA` surfaces. _(impl SHA `98494de`)_ Combined create + map constructors (every existing consumer calls create immediately followed by map; the combined form covers v1.0 needs). Drop closes the handle via `SYS_CLOSE`; the kernel-side Burrow keeps the mapping alive until proc exit (independent refcount), so Drop is no-op in practice. Type-system-enforced non-transferability per I-5 (no `Transfer` trait impl). Volatile MMIO reads / writes (`read_u32` / `write_u32`) with runtime bounds + alignment asserts; the kernel-installed PTE carries MAIR_IDX_DEVICE so the hardware also doesn't reorder. DMA buffers are normal memory; `as_slice` / `as_slice_mut` for byte views, `read_u32` / `write_u32` with compiler fences for 32-bit access; HARDWARE barriers (`virtio_rmb`/`virtio_wmb`) remain the caller's responsibility. Irq impls `t::poll::AsFd` so a multi-IRQ event-loop driver can compose with `PollSet`. Bare SVC wrappers (`t_mmio_create` etc.) remain exported — drivers can continue using them; no forced-migration deadline. Error mapping: kernel returns -1 generic-rejection → `Err(Error::InvalidArgument)`; future kernel -errno values route through `from_syscall_return`. Migrations landed in this chunk: 3 simple probes (mmio-probe + irq-probe + irq-bench). 7 virtio-* binaries remain on bare wrappers (migration is mechanical; can land incrementally). alloc-smoke extended ~70 LOC: cap-missing rejection for all three constructors (positive paths exercised by the 3 probes which DO get CAP_HW_CREATE). New reference doc `docs/reference/89-hardware.md`. | `*(pending)*` | usr workspace `cargo build --release` clean (15 binaries; no warnings); `tools/test.sh` boot pass; mmio-probe + irq-probe + irq-bench all PASS via the typed API (validates Mmio::new + Mmio::read_u32 + Irq::new + Irq::wait against real PL031 RTC + GIC SPI 96); new joey line: "alloc-smoke: Mmio + Irq + Dma cap-missing reject OK" |
| U-2h-ninep hash fixup | Update U-2h-ninep row with the impl's hash. | *(pending)* | — |
| U-2h-ninep | `t::ninep` — 9P2000.L server-side codec lifted from corvus's private `p9` module into `libthyla_rs::ninep`. _(impl SHA `1ba1f00`)_ Server-side only at v1.0 (T-message parsers + R-message builders + the pack/unpack primitives + the Header peek). Client-side (T-builders + R-parsers) deferred until a real consumer surfaces — native programs reach 9P services via the kernel client (mounted trees + SYS_SRV_CONNECT), not by speaking the protocol from userspace. Corvus migrates in the same commit via `use libthyla_rs::ninep as p9;` (zero call-site change); corvus's `src/p9.rs` deleted. Module surface: 16 message-type constants (P9_T*/R* + P9_RLERROR), QID type bits (QTDIR/QTFILE), sentinels (NOFID/NOTAG), width constants (HDR_LEN=7/QID_LEN=13), walk bounds (MAX_WALK=16/NAME_MAX=255), 12 Linux errno values for Rlerror responses; `Qid` 13-byte struct; `Header` + `peek_header`; `pack_u8/u16/u32/u64/str/qid` primitives; `unpack_u8/u16/u32/u64/str` primitives (zero-copy for str); Tmsg parsers `parse_t{version,attach,walk,lopen,read,write,clunk}`; Rmsg builders `build_r{version,attach,walk,lopen,read,write,clunk,lerror}` with back-patched size. Error convention is `Result<_, ()>` for wire-format violations (caller short-circuits to Rlerror(EPROTO) on Err). alloc-smoke extended ~135 LOC: pack/unpack primitives round-trip; Rversion build + peek_header; Tversion hand-build + parse_tversion field check; Twalk hand-build with 3 names + parse_twalk field check; Rwalk build + peek + nwqid read-back; Rlerror build + ecode read-back; peek_header short-buffer reject; parse_twalk short-buffer reject. New reference doc `docs/reference/88-ninep.md`. | `*(pending)*` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: NineP codec OK" — validates the entire server-side codec end-to-end including back-patched header sizing and the Twalk variable-length name parser |
| U-2g hash fixup | Update U-2g row with the impl's hash. | *(pending)* | — |
| U-2g | `t::torpor::{wait, wake, wake_one, wake_all, WaitResult}` + `t::time::{sleep, Duration}` + `t::rand::{getrandom, fill_bytes, GETRANDOM_MAX}` + `t::thread::{spawn_raw, exit_self, set_tid_address, clear_tid_address, join_tid, Tid}`. Futex-style wait/wake over `&AtomicU32` (no-lost-wakeup guaranteed by kernel torpor_lock serialization); sleep built on torpor_wait with stack-local sentinel (no SYS_NANOSLEEP at v1.0); getrandom + fill_bytes with `CAP_CSPRNG_READ` gate; kernel-thread spawn + the canonical clear_child_tid join protocol. **t::tty deferred** -- the kernel has no PTY surface at v1.0; U-PTY chunk handles it later. **Multi-thread exit safety**: `set_tid_address` + `join_tid` is the only safe-exit pattern for a multi-thread Proc at v1.0 (SYS_EXITS with any RUNNING peer thread extincts the kernel; the join wakes only when the child is THREAD_EXITING). New syscall constants: T_SYS_TORPOR_WAIT=39 + WAKE=40 + THREAD_SPAWN=41 + THREAD_EXIT=42 + SET_TID_ADDRESS=36; 5 SVC wrappers; T_TORPOR_TIMEOUT_INDEFINITE + T_TORPOR_MAX_TIMEOUT_US sentinels. `Duration` re-exported from `core::time` (no `std::time` in no_std). alloc-smoke extended ~200 LOC: torpor value-mismatch + timeout + wake-with-no-waiters; time::sleep 5ms + Duration::ZERO; rand::getrandom Ok-or-NotPermitted tolerance (cap inheritance varies) + oversize InvalidArgument; thread::spawn_raw misaligned-sp negative + **end-to-end thread roundtrip via set_tid_address clear_child_tid wake** (the kernel-extinction-hazard path). | `0d1894e` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Torpor + Time + Rand + Thread OK" — validates SYS_TORPOR_WAIT + SYS_TORPOR_WAKE + SYS_THREAD_SPAWN + SYS_SET_TID_ADDRESS + SYS_THREAD_EXIT + SYS_GETRANDOM end-to-end through the typed Rust surface, including the multi-thread exit-safe clear_child_tid join handshake |
| U-2f hash fixup | Update U-2f row with the impl's hash. | *(pending)* | — |
| U-2f | `t::territory::{mount, bind_replace, bind_before, bind_after, unmount, chroot, pivot_root, MountFlags, PathId}` + `t::cap::{Caps, Stripes, grant, use_grant}`. Plan 9 namespace composition + the two-phase cap-elevation flow. mount/unmount key by an abstract u32 `path_id_t` at v1.0 -- string-path resolution is a v1.x kernel lift; libthyla-rs docs the future shape. Source handles via `&impl AsFd` (reuses U-2e's trait; File + future SrvConn compose for free; the kernel validates KOBJ kind + rights at the syscall boundary). chroot is the initial-bringup primitive; pivot_root is the long-running-Proc variant (audit-trackable, the joey-uses-this path from 16c). bind_before/bind_after/bind_replace are sugar that route through mount() with the matching MountFlags. New syscall constants: T_SYS_MOUNT=14 / UNMOUNT=15 / CHROOT=35 / PIVOT_ROOT=53; new SVC wrappers t_mount/t_unmount/t_chroot/t_pivot_root. T_MREPL/MBEFORE/MAFTER/MCREATE flag constants + TPathId alias. **No `current()` or `drop()` at v1.0**: the kernel exposes no cap-query or non-spawn cap-drop syscall. Programs that need to drop caps spawn a child via Command (U-2d) with reduced `cap_mask`; cap-querying is v1.x. CAP_HOSTOWNER ELEVATION-ONLY restriction documented (kernel-enforced; libthyla-rs comments the trap). alloc-smoke extended ~150 LOC: MountFlags + Caps bitops round-trip (compile-time + runtime against the kernel bit positions); positive mount/unmount round-trip on free path_id 0x7E5701 using /system.key as Spoor source; bind_before/bind_after/bind_replace variants; bogus-fd negative; cap::grant without CAP_GRANT_HOSTOWNER → PermissionDenied; cap::use_grant without console-attach + pending grant → PermissionDenied. | `0391383` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Territory + Cap OK" — validates SYS_MOUNT + SYS_UNMOUNT + SYS_CAP_GRANT + SYS_CAP_USE end-to-end through the typed Rust surface |
| U-2e hash fixup | Update U-2e row with the impl's hash. | *(pending)* | — |
| U-2e | `t::notes::{Notes, Note, NoteClass, NoteMask, NoteTarget, MaskGuard}` + `t::poll::{PollSet, PollEvents, PollTimeout, PollEvent, PollResults, AsFd}`. Notes is RAII over SYS_NOTE_OPEN (fd-shaped path -- the canonical Thylacine surface per NOVEL.md §3.1); read() blocks, try_read() probes via poll(0). NoteMask is a per-Thread bitflag set of NoteClass (Interrupt/Kill/Pipe/ChildExit/Snare); set_mask swaps and returns prior; with_mask returns a MaskGuard that restores on Drop. notes::send accepts NoteTarget::{SelfProc, Pid(Pid)}; validates empty/oversize names AND rejects `snare:`-prefixed names at the boundary (kernel-synthetic reservation). PollSet is a reusable Vec<TPollFd> with parallel events-vector for clean re-arm on each poll; supports add/add_raw/remove/remove_raw/clear/len; empty-set poll short-circuits to Ok(empty). PollEvent helpers (is_readable/is_writable/is_hup/is_err). AsFd trait bridges typed wrappers to PollSet; File and Notes both impl. New syscall constants: T_SYS_NOTE_OPEN=44 / NOTIFY=45 / NOTED=46 / POSTNOTE=47 / NOTE_MASK=48. SYS_NOTIFY/SYS_NOTED constants surfaced for v1.x async-handler callers; libthyla-rs surfaces only the fd path at v1.0. ABI mirror: TNoteRecord #[repr(C)] 32-byte (16 name + u32 arg + u32 sender_pid + u64 timestamp_ns); const-assert pins size. T_NOTE_BIT_* + T_NOTE_MASK_SUPPORTED + T_POSTNOTE_SELF_PID + T_NOTED_NCONT/NDFLT constants. alloc-smoke extended: drains any child_exit synthetic post from U-2d's wait(), validates mask round-trip + with_mask RAII restore, exercises send validation (empty/oversize/snare:), send + read("interrupt"), PollSet pipe round-trip + notes-fd integration. | `6b5811c` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Notes + Mask + PollSet OK" — validates SYS_NOTE_OPEN + SYS_POSTNOTE + SYS_NOTE_MASK + SYS_POLL + the kernel-synthetic child_exit poster end-to-end through the typed Rust surface |
| U-2d hash fixup | Update U-2d row with the impl's hash. | *(pending)* | — |
| U-2d | `t::process::{Command, Child, ExitStatus, Stdio}` + `t::process::pipe()` free fn. Command is a std::process-shaped builder (new/arg/args/stdin/stdout/stderr/caps/spawn). Stdio = Inherit \| Piped \| File(File); Null deferred (no kernel /dev/null analog). Child::wait reaps via t_wait_pid; ExitStatus::success/code/raw. Pipe cleanup discipline: PreparedStdio tracks (child_fd, keep_through_syscall, parent_keeps) so the parent's copy of the child-end pipe fd drops at end of spawn(). Adds T_SYS_WAIT_PID=22 + T_SYS_SPAWN_FULL_ARGV=49 syscall constants, t_wait_pid + t_spawn_full_argv SVC wrappers, #[repr(C)] TSpawnArgs (56-byte mirror with compile-time size assertion), T_SYS_SPAWN_ARGV_MAX/DATA_MAX + T_SPAWN_NAME_MAX/MAX_FDS bounds. File::from_raw_handle pub(crate) helper for wrapping kernel-returned fds (pipes, etc.). alloc-smoke extended: pipe() round-trip + EOF + Command::spawn(hello-rs).stdin/stdout/stderr(Piped).spawn().wait().success(). | `ecf1a1a` | usr workspace cargo build clean; tools/test.sh boot pass with new joey lines: "hello from /hello-rs" (alloc-smoke-spawned) + "alloc-smoke: pipe + Command + Child OK" |
| U-2c-fs hash fixup | Update U-2c-fs row with the impl's hash. | *(pending)* | — |
| U-2c-fs | `t::fs::{Metadata, OpenOptions}` + free functions `fs::{metadata, exists, is_file, is_dir}`. Metadata is `#[repr(C)]` 72-byte mirror of kernel `struct t_stat` with const fn accessors (len, is_file, is_dir, is_char_device, is_symlink, is_empty, mode, permissions, nlink, atime/mtime/ctime_sec, qid_path/vers/type, blksize, blocks). OpenOptions builder mirrors std::fs::OpenOptions: read/write/truncate compose to omode; append/create/create_new return Error::NotImplemented at v1 (no kernel SYS_TLCREATE/append surface). `File::metadata()` method backed by SYS_FSTAT. New syscall: T_SYS_FSTAT=50 + t_fstat wrapper. T_S_IF{MT,REG,DIR,CHR} mode constants added. **Deferred from U-2c-fs to a future sub-chunk**: ReadDir / DirEntry (no kernel directory-read mechanism exposed); free fns canonicalize/remove_file/copy (no kernel surfaces yet). alloc-smoke extended with Metadata + free fn + OpenOptions error-case validation. | `3c8a49b` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Metadata + OpenOptions + free fns OK" |
| U-2c-io hash fixup | Update U-2c-io row with the impl's hash. | *(pending)* | — |
| U-2c-io | `t::io::{Read, Write, Seek, BufRead, SeekFrom, BufReader, Cursor}` traits + concrete adapters (mirror std::io). `t::fs::File` RAII over per-component SYS_WALK_OPEN walk; impls Read/Write/Seek. New syscall wrappers: t_walk_open + t_lseek + constants (T_OREAD/OWRITE/ORDWR/OTRUNC, T_SEEK_SET/CUR/END, T_WALK_OPEN_FROM_ROOT, T_WALK_OPEN_NAME_MAX). Error gets two library-only variants (UnexpectedEof, WriteZero; non-exhaustive enum, backwards-compat). File::open absolute paths only at v1 (relative + .. parent-traversal return InvalidArgument). alloc-smoke extended with Cursor + File::open(/system.key) + Read + Seek validation. | `c19e0f9` | usr workspace cargo build clean (no warnings); `tools/test.sh` boot pass with new joey line: "alloc-smoke: Cursor + File + Read + Seek OK" — validates SYS_WALK_OPEN, SYS_READ, SYS_LSEEK round-trip through the typed Rust trait surface |
| U-2c-path hash fixup | Update U-2c-path row with the impl's hash. | *(pending)* | — |
| U-2c-path | `t::fs::{Path, PathBuf, Component, Components, Display, SEPARATOR}`. Pure logic, no syscalls. Path is `#[repr(transparent)]` newtype over str; PathBuf wraps String. UTF-8 by convention. parent/file_name/file_stem/extension/join/starts_with/ends_with/components iterator. Full Ord/Eq/Hash/Display/Debug. Per-binary `#[global_allocator]` declaration added to the 11 libthyla-rs consumers (hello-rs, mmio-probe, irq-probe, irq-bench, virtio-blk-probe, virtio-blk-rw, virtio-net-probe, virtio-net-arp, virtio-net-loop, virtio-input, virtio-gpu) — sets the project-wide convention that every native Rust binary opts in to ThylaAlloc. U-2c split from one chunk into U-2c-path / U-2c-io / U-2c-fs (3 thin slices). alloc-smoke extended with Path/PathBuf/Components validation. | `d141398` | usr workspace cargo build clean; `tools/test.sh` boot pass with new joey line: "alloc-smoke: Path + PathBuf + Components OK" alongside the existing alloc lines |
| U-2b hash fixup | Update U-2b row with the allocator impl's hash. | *(pending)* | — |
| U-2b | `t::alloc` — `ThylaAlloc` `#[global_allocator]` backed by `linked_list_allocator::LockedHeap` over a single SYS_BURROW_ATTACH region (4 MiB initial). Lazy init via atomic state machine; multi-thread-safe. Adds `t_burrow_attach` + `t_burrow_detach` SVC wrappers + T_SYS_BURROW_ATTACH/DETACH (37/38) constants. New `usr/alloc-smoke/` workspace member: native Rust binary exercising Box/Vec/String/small-alloc loop. Wired into `tools/build.sh` + spawned by joey at boot. | `d8e95d3` | usr workspace cargo build clean; `tools/test.sh` boot pass with new joey lines: "alloc-smoke: Box + Vec + String + small-alloc loop OK" + "joey: /alloc-smoke reaped status=0; libthyla-rs::alloc verified" |
| U-2a hash fixup | Update U-2a row with the foundation impl's hash. | *(pending)* | — |
| U-2a | `t::err` (`Error` enum + `Result<T>` + `From<i32>` + `from_syscall_return` + `Display`) + `t::handle` (`Handle` RAII + `Rights` bitflags-newtype + RAII close via SYS_CLOSE on Drop). Foundational types for the libthyla-rs uplift; no_std + no_alloc; backwards-compatible (existing T_RIGHT_* constants + bare wrappers preserved). | `e99bb43` | usr workspace cargo build clean (no warnings); full boot test pass (`tools/test.sh` green; "Thylacine boot OK" reproducible) |
| U-2 hash fixup | Update U-2 row with the scripture amendment's hash. | *(pending)* | — |
| U-2 | Scripture amendment: §15 reframed as "the libthyla-rs uplift to the library Thylacine deserves" — lead-by-example framing, complete module structure, sub-chunk decomposition (U-2a..U-2-test, ~9-12 sessions). §19 + phase7-status.md + UTOPIA.md + ROADMAP §8.1/§8.7 updated. NO code. | `2fbfad3` | — |
| U-1 hash fixup | Update U-1 row with the scripture commit's hash. | *(pending)* | — |
| U-1 | Scripture: UTOPIA.md + UTOPIA-SHELL-DESIGN.md + UTOPIA-VISUAL.md + ARCH/ROADMAP/CLAUDE updates + this doc. NO code. | `c4e57f2` | — |

## Remaining work — the U-* arc

Per `docs/UTOPIA-SHELL-DESIGN.md §19`. Sequenced for dependencies.

The U-2 work was reframed in U-2 scripture amendment from a single "libthyla-rs extensions" chunk into a multi-chunk **libthyla-rs uplift** — the library Thylacine deserves. ~9-12 sessions of foundation work before U-3 begins; investment paying back across every subsequent native Rust program.

| Sub-chunk | Scope | Depends on |
|---|---|---|
| **U-2** | Scripture amendment: §15 (libthyla-rs uplift framing) + §19 update + phase7-status.md U-* arc refresh. NO code. | U-1 |
| **U-2a** | `t::err` (Error + Result + From<i64>) + `t::handle` (Handle RAII + Rights bitflags). Foundational. | U-2 |
| **U-2b** | `t::alloc` (`#[global_allocator]` via burrow_attach). Enables `alloc::*`. Smoke binary with Box/Vec/String. | U-2a |
| **U-2c-path** | `t::fs::{Path, PathBuf, Component, Components, SEPARATOR}`. Pure logic, no syscalls. Mirrors std::path. | U-2b |
| **U-2c-io** | `t::io::{Read, Write, Seek, BufRead}` + `t::fs::File`. | U-2c-path |
| **U-2c-fs** | `t::fs::{OpenOptions, Metadata, ReadDir, DirEntry}` + free functions. | U-2c-io |
| **U-2d** | `t::process::{Command, Child, ExitStatus, Stdio}` + `t::process::pipe()`. | U-2c-io |
| **U-2e** | `t::notes::{Notes, Note, NoteClass, NoteMask, NoteTarget, MaskGuard}` + `t::poll::{PollSet, PollEvents, PollTimeout, PollEvent, AsFd}`. **LANDED** at the U-2e row above. | U-2c-io |
| **U-2f** | `t::territory::{mount, bind_replace, bind_before, bind_after, unmount, chroot, pivot_root, MountFlags}` + `t::cap::{Caps, grant, use_grant}`. **LANDED** at the U-2f row above. (`current` / `drop` deferred -- no kernel syscalls at v1.0; `rfork` deferred -- not a standalone syscall at v1.0.) | U-2a |
| **U-2g** | `t::thread` + `t::torpor` + `t::time` + `t::rand` (+ t::tty deferred to U-PTY since the kernel has no PTY surface at v1.0). **LANDED** at the U-2g row above. | U-2c-io |
| **U-2h-ninep** | `t::ninep` (lift 9P2000.L server-side codec from corvus into libthyla-rs; aliases corvus to consume the lifted module). **LANDED** at the U-2h-ninep row above. | U-2c-io, U-2d |
| **U-2h-hardware** | `t::hardware::{Mmio, Irq, Dma}` typed wrappers (RAII over SYS_MMIO_CREATE/SYS_IRQ_CREATE/SYS_DMA_CREATE; impl AsFd on Irq for poll integration; type-system enforces I-5 non-transferability). **LANDED** at the U-2h-hardware row above; 3 simple probes migrated (mmio-probe, irq-probe, irq-bench); 7 virtio-* binaries on bare wrappers pending incremental migration. | U-2c-io, U-2d |
| **U-2-test** | Cross-module smoke binary on Thylacine. Validates the uplift. **LANDED** at the U-2-test row above. | U-2a..U-2h |
| **U-3** | Utopia workspace skeleton: `usr/utopia/{Cargo.toml,shell,libutopia,coreutils}`; libutopia palette + ansi + path modules; `ut` skeleton (version-print + exit); `tools/build.sh utopia` Rust cross-compile wiring; host-bake `/bin/ut`. | U-2-test |
| **U-4** | Line editor in libutopia: raw mode + emacs keybindings + line buffer + multi-line + tab hook + Ctrl-R history hook. Hand-rolled (~1500-2500 LOC); NOT reedline. | U-3 |
| **U-5** | Parser + AST for rc-shape syntax. Pure logic; unit-testable on host. **U-5a + U-5b + U-5c + U-5d LANDED** at the rows above; the U-5 parser arc is COMPLETE. | U-3 |
| **U-6** | Evaluator core + main loop: poll() main loop; built-ins (cd, exit, set, source, fn, alias, eval, type, etc.); external command spawn; pipes; redirection; `?`/try-catch; pipefail. **U-6a + U-6b + U-6c LANDED** at the rows above (U-6a: evaluator scaffold + Value + Env + eval_expr for no-side-effect subset; U-6b: statement eval -- Let/Assign/FnDecl/Return/Break/Continue/If/While/For/Case/Try/Trace/OnNote/MaskNote + Pipeline-as-fn-call + BraceBlock + Arith + implicit-fail bookkeeping; U-6c: external command spawn via libthyla_rs::process::Command + trace echo to UART; U-6d-a: multi-element pipeline + pipefail; U-6d-b: I/O redirection -- `<` `>` `>>` heredoc on a lone command + per-pipeline-element, via resolve_redirects + the libthyla_rs::fs create-or-open/append surface + the `<< EOF`-with-space lexer fix. Subst/ProcSub/Match::Regex + subshell + in-process-pipeline-stage + `&` + redirect-on-in-process-command still NotImplemented). **U-6e-pre-a LANDED** (the native-argv runtime + std-stream handles in libthyla-rs: `env::args` [G03] + `io::{stdin,stdout,stderr}` + `from_raw_fd` + print macros [G05]; `argv-smoke` proves the first native-argv execution -- the U-6e prerequisite). **U-6e-pre-b LANDED** (16 native coreutils -- echo/cat/wc/head/tail/true/false/seq/sort/uniq/tr/cut/grep/basename/dirname/pwd -- adopted into `usr/coreutils` + runtime-verified by `coreutil-smoke`, 28 checks; `RAMFS_FILE_MAX` 64->128; reap-first deadlock fix; deferred set tracked #925). U-6e: built-ins (cd/exit/set/...) + glob argv fs-walk expansion. U-6f: substitution + process sub. U-6g: poll() main loop + line-editor wire. U-6-test: cumulative integration smoke. | U-4, U-5 |
| **U-7** | fd-notes job control: Ctrl-C / Ctrl-Z handling; `&`; jobs/fg/bg; on note / mask note. | U-6 |
| **U-8** | Thylacine builtins: bind / mount / unmount / pivot_root / rfork / cap / note. | U-6 |
| **U-9..N** | Coreutils, one or two per chunk: cat, ls, echo, grep, sed, awk, cp, mv, rm, mkdir, find, wc. | U-3 (each independent thereafter) |
| **U-Helix** | Helix port via Pouch. Parallel arc; not in shell critical path. | U-3 (for host-bake) |
| **U-PTY** | PTY infrastructure if not landed by then: `/dev/ptmx`, `/dev/pts/<n>`, `termios` via `/dev/consctl`. | Independent of shell impl until U-Z |
| **U-Z** | The Utopia bring-up integration test (`docs/UTOPIA-SHELL-DESIGN.md §18`). Multiple full-suite passes; perf measurements; doc final pass. | All above |

Rough scale: 27-37 sessions across the arc. The libthyla-rs uplift (U-2..U-2-test) is the heaviest sub-arc — ~9-12 sessions — and is investment in the library every subsequent chunk builds on.

## Exit criteria status

Per `docs/UTOPIA-SHELL-DESIGN.md §18` / `docs/ROADMAP.md §8.2`. Twelve headline checks.

- [ ] Boot a fresh Thylacine VM; reach the Pale Fire `ut` prompt via UART.
- [ ] Multi-stage shell pipeline: `cat /etc/passwd | grep root | cut -d: -f1` produces correct output.
- [ ] Job control via fd-notes: `sleep 100 &` appears in `jobs`; Ctrl-Z + `fg` resume; Ctrl-C terminates.
- [ ] Error model: function with `cmd1?; cmd2?; cmd3?` short-circuits on `cmd2` failure.
- [ ] Namespace builtin: `bind /srv/stratum-ctl /n/stratum`; `ls /n/stratum` shows the Stratum admin surface.
- [ ] Notes builtin: `note send $$ snare:user1` triggers a registered `on note 'snare:user1' { ... }` handler.
- [ ] `hx /etc/hosts` opens Helix; edit + save observable.
- [ ] rc-shape script: `for (f in *.md) { wc -l $f }` runs.
- [ ] Pale Fire prompt renders with the canonical three-segment colour scheme (path `#8898b4`, glyph `⊢` `#e07840`, command `#d8e4f4`).
- [ ] No kernel extinctions, no driver crashes, no zombie processes.
- [ ] No P0/P1 audit findings on the Utopia surface.
- [ ] All planned U-* chunks landed.

## Build + verify commands

To be filled in as `tools/build.sh utopia` wiring lands under U-3. Anticipated shape:

```bash
# Build the Utopia workspace via aarch64-thylacine target (no_std on libthyla-rs)
tools/build.sh utopia

# Build the Helix port via Pouch
tools/build.sh helix

# Build everything (kernel + Utopia + Helix + sysroot + disk)
tools/build.sh all

# Boot + integration tests
tools/test.sh
```

## Trip hazards

- **Native libthyla-rs requires no_std + alloc.** Reaching for a std-dependent Rust crate is the most common mistake. The decision rule (`CLAUDE.md` "Native vs ported"): authored = native, ported = Pouch. Helix is the only Pouch consumer in Utopia.
- **The line editor is hand-rolled.** Reedline assumes std/Pouch; we have a libutopia line editor. Bug surface is real — see `docs/UTOPIA-SHELL-DESIGN.md §11.2`.
- **The fd-notes main loop is the core innovation.** Replaces classical signal handlers. Every shell operation routes through `poll()` (`docs/UTOPIA-SHELL-DESIGN.md §10`).
- **`ut` is NOT bash, NOT rc-as-shipped.** Rc-shaped with twelve refinements (`docs/UTOPIA-SHELL-DESIGN.md §4-§15`). Scripts written for POSIX `sh` do not run unchanged.
- **Pale Fire palette discipline.** Four colours; the `⊢` glyph; coloured by role not by personal preference. See `docs/UTOPIA-VISUAL.md`.
- **The integration test gates Phase 7 exit.** All twelve headline checks must pass.

## References

- `docs/UTOPIA.md` — the experience.
- `docs/UTOPIA-SHELL-DESIGN.md` — the binding design (12 axes + impl roadmap).
- `docs/UTOPIA-VISUAL.md` — Pale Fire palette + glyph + prompt format.
- `docs/ARCHITECTURE.md §3` — language and toolchain (extended in U-1 with native vs ported split).
- `docs/ARCHITECTURE.md §3.5` — the Plan 9 split scripture.
- `docs/ARCHITECTURE.md §23` — POSIX surfaces and the Utopia milestone (updated in U-1).
- `docs/ROADMAP.md §8` — phase definition (rewritten in U-1).
- `CLAUDE.md` "Native vs ported userspace programs" — operational decision rule.
- `docs/POUCH-DESIGN.md` — the ported-code substrate Helix consumes.
- `usr/lib/libthyla-rs/src/lib.rs` — the native runtime crate Utopia extends.

## Predecessors

- Phase 6 (Pouch; ROADMAP `§7A`) — `docs/phase6-status.md` — CLOSED at `218feb0`. Delivered the cross-compilation environment + the boundary-line for ported code.
- Phase 5 (9P + Stratum integration + corvus; ROADMAP `§7`) — `docs/phase5-status.md` — CLOSED. Delivered the 9P client + Spoor + Territory + the corvus precedent for native Rust on libthyla-rs.
