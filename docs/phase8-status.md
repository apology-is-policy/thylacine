# Phase 8 status — Linux compat + network

Phase 8 (ROADMAP §9, §2.2): the network arc → container runner (#70) →
on-system toolchain (#67) → Linux binary shim. This doc tracks the **network
arc** (NET-DESIGN.md), the first Phase-8 work.

## TL;DR

The #68 network charter (net-0) is bound scripture; net-1 (the reusable
virtio-net frame-transport driver) is landed + proven. net-1 surfaced **#140**
(two persistent userspace drivers cannot soundly share a 4 KiB virtio-mmio
page), so net-2 is **preempted** by the **virtio-PCI transport sub-arc**
(pci-0..pci-3, `docs/VIRTIO-PCI-DESIGN.md`, the user-voted DFS fork 2026-06-15).
**pci-0..pci-3 are LANDED** — the scripture, the kernel `KObj_PCI` + the 3
syscalls, the userspace `PciDev` + `VirtioNetPci` driver (a PCI virtio-net ARP
round-trip passes on its own page-aligned BAR), and the focused soundness audit
(Opus 4.8 max + self-audit, CLEAN 0/0/0/4 — all P3, F2/F3/F4 fixed + F1 tracked).
**The virtio-PCI transport sub-arc is COMPLETE.** **net-2a is LANDED** — `netd`
embeds smoltcp on the PCI NIC and acquires a DHCP lease (`10.0.2.15/24` from
slirp), proving the whole lower stack (Ethernet/ARP/UDP/DHCP). **net-2b-1 is
LANDED** — netd is now a **persistent** service: a libdriver `Lifecycle` manifest
field + the warden's leave-running-on-`READY` policy keep netd resident (holding
the link) instead of reaping the one-shot, while the `netdev-driver` MMIO demo's
`DeviceRemoved` teardown is preserved. **net-2b-2 is LANDED** — the 9P `/net`
server: netd posts `/srv/net` and serves the §3.1 directory skeleton
(`tcp/udp/icmp` + a read-only `stats` file) over a combined accept/stack event
loop; joey mounts it at `/net`, and the boot probe walks+reads `/net/tcp/stats`
(43 bytes). Posting requires `MAY_POST_SERVICE`, conferred joey→warden→netd
(gated on the persistent lifecycle). **net-2c-1 is LANDED** — the `/net/tcp`
`clone` fid state machine (§3.4): a qid-encoded dynamic tree, the
clone-mints-`N` Plan 9 idiom (the kernel dev9p client accepts the rebound-fid
`Rlopen` qid), refcounted connection slots (the *last* clunk frees `N` — the
only free path), and the `libthyla_rs::ninep` `Treaddir` codec (`/net/tcp` lists
its live `N/` directories). **net-2c-2 is LANDED** — the live TCP data path: the
`socket-tcp` feature, the `Net` table owns the iface + socket set (post-DHCP), a
`clone` reserves a real `tcp::Socket` (freed at the last clunk), the `ctl` verb
parser drives `connect a.b.c.d!port`/`hangup` (`announce`/options →
`EOPNOTSUPP`), `status`/`local`/`remote` report the live socket, and `data` is
`recv_slice`/`send_slice` (non-blocking). Boot proof (deterministic,
peer-independent): `connect 10.0.2.2!9` → `remote 10.0.2.2!9` + `local
10.0.2.15!…` + the multi-fid clunk frees + reuses `N`. The NIC-IRQ poll fd is
deferred (a pollable IRQ fd is a kernel ABI surface). **net-2d is LANDED** — the
focused netd audit (Opus-4.8-max prosecutor + a concurrent self-audit) closed
**CLEAN 0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty** (F1 [P2] the readdir budget missed
the 11-byte Rreaddir frame overhead → a `rreaddir_budget` parity helper; F2/F3
[P3] the `P9_NOFID`-as-live-fid + the failed-connect port-burn / rolled-back-mint
stat → fail-closed + peek-then-commit; F4/F5 closed-justified; SMP gate 40/40
clean, 0 corruption). **net-2 is DONE.** **net-3a is LANDED** — the TCP server
side: `announce` + the blocking `listen`/accept via a **deferred 9P reply** (netd
holds the Rlopen until an inbound call lands, since a single-threaded server
cannot block in the handler; the socket-swap mints the accepted connection + re-
arms the listener) + the **Tflush/Rflush** cancellation path (also closing a
pre-existing net-2c-2 outstanding-tag leak). The blocking-open question is
**resolved pure-userspace, no ABI** (committed-blocking via the existing dev9p
client; §12's readiness multiplexing is the separate net-6 leg). Boot-proven
deterministically (`announce → Listen` + the gate); the inbound-accept E2E is owed
to net-3d. **net-3b is LANDED** — UDP: `/net/udp` clone/connect/data. The shared
`Slot` carries a `proto` (the `get::<tcp::Socket>` vs `get::<udp::Socket>`
discriminator; every socket touch dispatched on it); `/net/udp/clone` mints a
`udp::Socket`; `connect` binds a local port + records the remote; `data` is
datagram `send_slice`/`recv_slice`; no `listen` file. Proven by joey's
deterministic `/net/udp` machinery probe + netd's best-effort DNS round-trip demo
(logged — slirp forwards DNS to the host resolver; the UDP-via-9P data round-trip
E2E is owed to net-3d's loopback). The `socket-udp` Cargo feature is the only new
dependency; pure userspace. **net-3c is LANDED** — ICMP: `/net/icmp`
clone/connect/data (ping). The third `proto` along the slot discriminator;
`/net/icmp/clone` mints an `icmp::Socket` bound to a rotated Echo ident; `connect`
is portless (a bare IPv4) + records the target; `data` write wraps the payload
into an `EchoRequest`, `data` read returns the matching `EchoReply` payload; no
`listen` file; `hangup` is a no-op (connectionless). Proven by joey's deterministic
`/net/icmp` machinery probe + netd's best-effort gateway-ping demo (logged — slirp
did not answer the guest echo internally on the dev host, vindicating the
best-effort framing; the in-guest ICMP round-trip E2E is owed to net-3d's
loopback). The `socket-icmp` Cargo feature is the only new dependency; pure
userspace (the kernel `JOEY_BLOB_MAX` was bumped 256→384 KiB as the accumulating
net boot probes outgrew the init-blob bound). **net-3d is LANDED** — the focused
net-3 audit (Opus-4.8-max prosecutor + concurrent self-audit) + the deterministic
in-guest loopback E2E. The audit found **F1 [P1]**: a half-open deferred-`listen`
fid stranded a generation-less `PendingAccept`, so a clunk + cross-proto slot
re-mint drove a wrong-proto `get::<tcp::Socket>` panic (a whole-network DoS) —
fixed by a per-slot mint generation + the `poll_accepts` proto+gen guard + a
`cancel_accept_fid`-on-clunk + marking the listen fid `opened`. The **loopback
E2E** (an isolated `127.0.0.1` stack driving the real `Net` methods) delivers the
three owed deterministic in-guest round-trips (TCP inbound-accept — the F1 fix's
runtime regression — + UDP datagram + ICMP echo). **net-3 is DONE.** **net-4a is
LANDED** — `/net/cs` (the connection server: dial → clonefile line) + the
compiled-in ndb. cs resolves a numeric IPv4, an ndb static host (`localhost`), and
a numeric/named service (`http` → 80); a non-numeric non-ndb name yields an empty
response (DNS delegation is net-4b). Per the **net-4 ndb-source decision**
(user-voted 2026-06-18; NET-DESIGN §5/§18, scripture-first `dbc6523`): netd, a
confined leaf driver (I-34), cannot read `/lib`, so it compiles in `ndb/local`
(the config-at-construction idiom) and bakes the byte-identical user-readable
`/lib/ndb/local`; the resolver/router are read live from the DHCP lease. Pure
userspace; kernel byte-unchanged. **net-4b is LANDED** — `/net/dns` (the resolver) +
cs→dns delegation. One shared `dns::Socket` (seeded from the DHCP resolver)
multiplexes every query; the net-4a `CsSession` generalizes into the
deferred-capable per-fid `Query`. A name resolves **numeric → ndb → DNS**:
numeric/ndb fill synchronously; a DNS name starts a query and the read **defers**
(the net-3a held-Rread mechanism, completed by `poll_dns`). The smoltcp
query-lifetime hazard (`get_query_result` frees the slot on a result + panics on a
free slot) is closed by keeping the handle in one place nulled on every result
(never double-polled — a netd panic = a whole-network DoS). v1.0 is IPv4-A only;
pure userspace (only the `socket-dns` feature is new); kernel byte-unchanged.
**net-4c is LANDED** — `/net/ipifc/0` (the interface-config tree) + `/net/ndb` (the
live dynamic database) + the native `ipconfig` tool. The DHCP lease folds into an
`IfConfig` snapshot at bring-up (surfaced read-only through status/local/ndb); an
`ipconfig add IP MASK [GW]`/`remove` ctl write applies static config onto both the
live iface and the snapshot; the resolver socket is seeded from `ifc.dns` (one
source of truth for cs→dns + the ndb `dns=` line). Pure userspace; kernel
byte-unchanged. **net-4d is LANDED** — the focused net-4 audit + close (Opus-4.8-max
prosecutor + self-audit; **0 P0 / 0 P1 / 1 P2 / 3 P3, NOT dirty**, + a precautionary
round-2 CLEAN). **F1 [P2]** closed a cs/dns held-`Rread` loss (a 2nd concurrent read
/ a re-write while deferred could drop the held tag, I-9) with two minimal guards;
F2/F3/SA-3 [P3]. Three deterministic in-guest proofs landed (`proto_selftest` —
the OWED-since-net-2d parser coverage in-guest; `dns_defer_guard_selftest` — the F1
regression; `dns_loopback_e2e` — the OWED net-4b deferred-read E2E via a mock DNS
responder on an isolated `127.0.0.1` stack). Pure userspace; kernel byte-unchanged.
**The net-4 arc is COMPLETE.** **net-5 is LANDED** — the BSD-socket compatibility
boundary-line (NET-DESIGN §7): a new pouch patch
`usr/lib/pouch/patches/0016-pouch-net-sockets.patch` translates AF_INET
`socket()`/`connect()`/`bind()`/`listen()`/`accept()`/`send()`/`recv()`/`getsockname()`/
`getpeername()`/`setsockopt()`/`getsockopt()` into netd's `/net` 9P file ops,
**stacking on** `0006-pouch-sockets.patch` (AF_UNIX-over-`/srv`) via a per-slot
`family` dispatch. **NO new kernel surface** (only `SYS_open`/`read`/`write`/`close`,
already seamed — closing W4-F2). The **blocking** subset only (`SOCK_NONBLOCK` →
`EOPNOTSUPP`; non-blocking + `poll`/`select` are net-6). Proving binary
`/pouch-hello-net` (the first POSIX `socket()` program Thylacine runs): the
deterministic control-surface dance (socket / family+proto reject / setsockopt /
bind / listen→announce / getsockname / close) + a best-effort logged live
`connect`+round-trip. Boot-proven: `net-5 PROBE OK` + 930/930 + boot OK + 0 EXT +
the net-2c/3/4 probes unregressed. Pure userspace — kernel byte-unchanged. **net-6
is IN PROGRESS** — the §12.2 synchronous-readiness fork was design-bound (2026-06-18
user vote: the kernel `dev9p.poll` bridge, surfaced with prior-art — Plan 9's
blocking-read idiom; Linux v9fs has no real 9P `.poll`; Fuchsia/Genode's
server-driven readiness). **net-6a-1 is LANDED** — netd blocking `data` reads
(closes net-5 F2): `data_recv_outcome` Data/WouldBlock/Eof + `h_read` park +
`poll_data` deliver (the net-3a/4b deferred-reply pattern), so a pouch `recv()`
blocks automatically; proven by the `recv_blocking_e2e` in-guest selftest. **net-6a-2
is LANDED** — the pouch `shutdown`/`sendto`/`recvfrom` completion (closes net-5 F1):
`0017-pouch-net-datacalls.patch` makes the three tag-aware (shutdown(SHUT_WR)→netd
`hangup`; sendto's dest re-points an AF_INET UDP datagram; recvfrom blocks + fills
src); `sendmsg`/`recvmsg`/`socketpair` stay a fail-closed ENOSYS seam; pure userspace.
**net-6a-3 is LANDED** — the native `libthyla_rs::net` (`TcpStream`/`TcpListener`/
`SocketAddrV4`) + `usr/net-echo` + the Loom async composition (§16; pure userspace,
kernel byte-unchanged): the Plan 9-shaped `/net` client, a native echo-server
artifact, the netd `echo_e2e` (≥2-concurrent accept + bidirectional echo, in-guest),
and the Loom async witness (a Loom READ on a `/net` fid composes with zero new Loom
core). The live cross-Proc native round-trip is net-8's seam (NIC-only live stack →
no in-guest peer). **net-6b is COMPLETE** (the `dev9p.poll` bridge — the one
ABI surface; spec-first): **net-6b-1** landed `specs/net_poll.tla` (clean +
`BUGGY_LOST_READY` + liveness, TLC-green; the probe-then-observe I-9
generalization); **net-6b-2a** landed the netd `ready` file (the netd half;
pure userspace, kernel byte-unchanged); **net-6b-2b** landed the kernel
`dev9p.poll` slot + the global poll-pump kthread (the ONE new kernel surface;
the QTPOLL distinguisher + the elicited-readiness relay, boot-proven by the joey
`net-6b` UDP E2E); **net-6b-3** landed the pouch `poll()`/`select()` translation
(`0018-pouch-net-poll.patch`: a FAM_INET socket polls its `QTPOLL` `/net/.../ready`
sibling, not the always-ready data fd — pure userspace, kernel byte-unchanged;
boot-proven by the `/pouch-hello-net` POLLOUT-ready + POLLIN-times-out subtest;
the listener-poll gap is the #220 net-6b-4 seam). **net-6b-4 CLOSED the arc** (the
focused audit: TWO Opus-4.8-max prosecutor rounds + a concurrent self-audit each,
CONVERGED CLEAN 0 P0/P1/P2 over 2 rounds; the **F1** multi-client-pump P1 fixed
[`dev9p_poll_collect_clients` pumps every distinct QTPOLL client/cycle, not just the
head's — closing a v1.x-latent second-client starvation] + F2/F5 P3 fixed + F3/F4/
R2-F1/#220 dispositioned as v1.x seams [#221/#222/#223/#220]; spec gate re-verified;
SMP gate). **THE net-6b ARC IS COMPLETE.** Next: net-7 (TLS/NTP), net-8 (exit
criteria + arc audit).

## Landed chunks

| Commit | What | Tests |
|---|---|---|
| `54a6bf6` | **net-0**: the #68 network charter (`docs/NET-DESIGN.md`) — scripture, no code; the 3 votes (shared netd + view-narrowing; pouch socket-compat; ns-restriction firewall) | n/a (scripture) |
| `ff3a621` (+audit-close) | **net-1**: `usr/lib/netdev` — the reusable `VirtioNet` RX+TX frame driver (send/poll_rx/recycle/quiesce) + host-tested `ring` + the `netdev-test` boot probe | 7 host ring tests + `netdev-test: PASS 24/24` + boot OK + SMP gate 0 corruption + Opus-4.8 audit 0/0/1/4 (F1 P2 + F2/F3 P3 fixed; F4/F5 closed) |
| `378c667` | **pci-0**: the virtio-PCI transport scripture (`docs/VIRTIO-PCI-DESIGN.md` + ARCH §13.2/§25.4/I-5 + NET-DESIGN §17 + ROADMAP) — the #140 DFS fork; net-only, INTx | n/a (scripture) |
| `5a1b36c` | **pci-1a**: the DTB/config primitives — `dtb_pci_intx_route` (interrupt-map → GIC INTID), `dtb_pci_mem_window`, `dtb_get_compat_prop`, `virtio_pci_cfg_write{8,16,32}`; no ABI/kobj change | 883/883 (+3: `dtb.pci_intx_route` full swizzle + `dtb.pci_mem_window` + `virtio_pci.cfg_write_bounds`) + boot OK; SMP-inert (gate at pci-1b) |
| `89a6c69` | **pci-1b**: the `KObj_PCI` kernel object — `kernel/pci_handle.{c,h}` (exclusive `g_pci_claims` per-(bus,dev,fn) claim; bump-assign BARs from `dtb_pci_mem_window` with width-correct 32/64-bit sizing; `VIRTIO_PCI_CAP_*` cap-walk into `regions[]`, bounded + OOB-validated; INTx swizzle; quiesce-before-free) + `KOBJ_PCI=11` joining `KOBJ_KIND_HW_MASK` (I-5 non-transferable + NoHwDup for free) + `kobj_pci_init` at boot | 889/889 (+6: `pci.bar_decode_size` + `pci.claim_rng`/`claim_unknown`/`claim_exclusive`/`unref_releases_bars`/`live_count_balances`, claiming the live idle rng-pci) + boot OK + SMP gate; **self-found+fixed**: 32-bit BAR size mask must invert in 32-bit width (a 64-bit invert yields a bogus exabyte size) |
| `11eb559` | **pci-1c**: the 3 syscalls — `SYS_PCI_CLAIM=76` / `SYS_PCI_MAP_BAR=77` / `SYS_PCI_INFO=78` (`kernel/syscall.c` handlers + dispatch) + the 208-byte `struct t_pci_info` ABI (`_Static_assert`-pinned in the kernel header, the libt mirror, and the libthyla-rs `TPciInfo` with `offset_of!` asserts) + libt (`t_pci_claim`/`t_pci_map_bar`/`t_pci_info`) + libthyla-rs raw wrappers. CLAIM: `CAP_HW_CREATE` gate → `kobj_pci_claim` → mint fixed R\|W\|MAP (no TRANSFER). MAP_BAR: mirrors `sys_mmio_map` via the `kobj_pci_bar_mmio` seam (bar_index `>= 6` rejected before the u32 narrowing). INFO: zero-init + fill + byte copy-out (no implicit padding → no stack leak). | 891/891 (+2: `pci.syscall_reject` + `pci.syscall_claim_info`) + boot OK + SMP gate |
| `2908e0e` | **pci-2**: the userspace PCI transport. `libthyla-rs::hardware::PciDev` (claim + info + map-BARs over the pci-1c syscalls; per-BAR VA window `PCI_BAR_VA_STRIDE`; `region()` bounds each cap-region within its BAR; non-transferable I-5 like Mmio/Irq/Dma) + `netdev::virtio_pci::VirtioNetPci` (the PCI sibling of `VirtioNet`: reuses `ring` VERBATIM + the same RX/TX API + audit hardenings; the virtio-pci-modern `common`/`notify`/`isr`/`device` register transport; `CCFG_MIN_LEN`/`DEVICE_CFG_MIN_LEN` region-size guards [self-audit]; device-reset quiesce-on-drop) + `usr/netdev-pci-test` boot probe + `run-vm.sh` adds `virtio-net-pci,disable-legacy=on` (separate `net1` slirp; the MMIO net + `netdev-test` kept). `virtio.rs` left byte-identical (net-1 proof intact). | host ring 7/7 + 891/891 + boot OK (both `netdev-test` + `netdev-pci-test` PASS 24/24) + 0 EXTINCTION + login E2E + SMP gate |
| `44fc3fb` | **pci-3**: the focused soundness audit (Opus 4.8 max prosecutor + concurrent self-audit) over pci-1b/1c/2 — **CLEAN 0 P0 / 0 P1 / 0 P2 / 4 P3**. F1=R1 partial-map leak **tracked** (the suggested `t_burrow_detach` fix is a verified no-op: `SYS_BURROW_DETACH` is confined above 4 GiB, the driver-VA windows live below it by design → proc-exit-bounded like every virtio mapping; single-BAR NIC never triggers it). F2=R2 notify-doorbell bound **fixed** (`off*mul+2 <= notify_len` → `NotifyRegionTooSmall`). F3 `TPciInfo` `offset_of!` asserts completed. F4 `pci.walk_caps_hostile` test added (cap-loop / OOB-bar / unassigned-bar / oversized-region / valid-control over a synthetic config). + the `KObj_PCI` reference doc (`115-pci-claim.md`) + the `37` cross-reference. | 892/892 (+`pci.walk_caps_hostile`) + both NIC probes PASS 24/24 + 0 EXTINCTION + boot OK + login E2E + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `38cc1a7` | **#188**: the kernel virtio-rng boot-pull intermittent (surfaced by the 5e-4 SMP gate; **pre-existing**, proven causally disjoint from 5e). `random_virtio_pull` polled the **async** virtio-rng completion with a fixed *iteration* budget (`1<<22`); under HVF the boot vCPU spins natively (a normal success polls 0–82k iters, sub-ms) while QEMU delivers entropy via a bottom-half on a **separate host thread**, so under smp8 contention the fast native spin occasionally exhausted the budget before the completion landed → `g_rng_seeded` stayed false → whole-CSPRNG fail-closed → 6 random tests + `EXTINCTION`. **Confirmed by deterministic fault-injection** (shrinking the cap reproduced the exact evidence) + elimination. Fix: **wall-clock poll deadline** (`RNG_VIRTIO_POLL_MS=250ms` via CNTPCT/CNTFRQ; unconditional `1<<30` iteration backstop) + **bounded retry** (boot 3×, threshold 1×) + a precise per-site boot diagnostic (retires the lying "no RNG device" message). Focused Opus-4.8 audit + concurrent self-audit **CLEAN 0/0/1/3** (F1 frozen-counter non-termination = independent convergence, fixed; F2/F3/F4 = comment/doc, fixed). | 922/922 (+`kern_random.virtio_deadline_ticks`) + 40/40 smp8 HVF boots reseed OK (`any_188_signal=0`) + 0 EXTINCTION + boot OK + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `3dbf929` | **6a (#159)**: the **per-`(bus,dev,fn)` PCI allowance axis** — the I-34 fourth axis replacing the fail-closed `SYS_PCI_CLAIM` reject. `struct Allowance` gains `pci[ALLOWANCE_PCI_MAX]` + `pci_count` (each a `PCI_BDF_PACK(bus,dev,fn)`); `HW_RES_PCI` is the `allowance_permits` kind; `kobj_pci_resolve_bdf` (new, `pci_handle.{c,h}`) resolves `virtio_device_id → (bus,dev,fn)` read-only (the same first match the claim picks). `sys_pci_claim_handler` runs resolve → CreateBegin `allowance_permits(HW_RES_PCI)` → `kobj_pci_claim` → CreateCommit `allowance_handle_alloc` (re-check `revoked`); a broad Proc (allowance==NULL) passes (netdev-pci-test unaffected). ABI `t_allowance_desc` 176→216 (append `pci_count`+`pci[8]`+`_pad_pci`, prior offsets pinned; kernel/libt/libthyla-rs mirrors + `TAllowanceDesc::push_pci`). `proc_confer_allowance`/`allowance_confer_within_parent`/`allowance_clone_into` gain the PCI axis. NO spec change (abstract model covers a 4th opaque kind; the 4 buggy cfgs re-run green). Focused Opus-4.8 audit (MODEL start==end, no fallback) + concurrent self-audit **CLEAN 0/0/0/2, NOT dirty** — converged on the whole SOUND set (gate completeness, resolve-vs-claim consistency, packing injectivity, device-enable/rollback, ABI-append safety, monotonic reduction, broad-path); both P3 **fixed**: F1 (stale `allowance.c` doc-comment on `allowance_is_narrowed` still naming SYS_PCI_CLAIM — rewritten to the "drivers are leaves" gate) + F2 (no end-to-end handler witness — added `allowance.pci_claim_handler_gate`). | 923/923 (+`allowance.pci_membership` [the live rng-pci resolve leg] + `allowance.pci_claim_handler_gate` [the live narrowed-denied/allowed handler path] + the PCI legs in confer_within_parent/clone_inherit) + spec gate (clean + 4 buggy cfgs green) + boot OK + login E2E (broad netdev-pci path) + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `f0878ba` | **6b scripture (no code)**: the **devpci-mediated PCIe discovery source** design fork resolved (user-voted the devpci Dev over a `SYS_PCI_ENUM` syscall / extending devhw). ECAM is I-5-reserved + no userspace PCI-enum → a config-space-scanning source is impossible; Plan 9 (devpci = `/dev/pci/B.D.F/ctl`) + Fuchsia/Genode/seL4 converge on kernel-mediated topology; the kernel already enumerated (`g_virtio_pci_devs[]`) → the source is the `DtbSource` analog (in-process, no Proc, no reconcile). MENAGERIE §7 (the two PCIe-source realizations — mediated vs self-scanning brcmstb) + §16-6b; ARCH §9.4 (devpci as-built + the /hw mount drift reconciled). | scripture only |
| `cffe5ea` | **6b-1: the devpci kernel Dev** — NEW `kernel/devpci.c` (`dc='P'` at /hw/pci; the devdev/devctl read-only dir-Dev: root → `<bus.dev.fn>` dir → `ctl` file over `g_virtio_pci_devs[]`; **NO raw ECAM / no config-space write** — the pci-3 I-5 property; the ctl line `v1 bus= dev= fn= vendor= device= virtio= intid=` the PciSource will parse) + the devhw synthetic `pci` mount-point child (`HW_SYNTH_PCI` bit-62 qid) + the `/hw/pci` mount in joey (after /hw) + register/extern/build wiring. `PGRP_MAX_MOUNTS` 12→16: the new boot mount pushed the joey namespace past the cap (the documented #80 orphan-accumulation), surfaced as a `stalk-2 e2e t_mount` extinction — root-caused to ground (`territory.c:749` cap reject) + fixed, not waved off ([[feedback-no-host-load]]). Reference `docs/reference/120-devpci.md` + ARCH §9.4/§25.4 + CLAUDE.md audit row. The PciSource + netdev-pci-driver bind (the live I-34-on-PCI proof) = 6b-2/6b-3; the focused audit + SMP gate = 6b-4. | **930/930** (+ 6 `devpci.*` + `devhw.synth_pci_child`) + boot OK + /hw/pci mounted + stalk-2 e2e OK + full login E2E + 0 EXTINCTION |
| `b9f356c` | **6b-2: the libdriver PCI axis + the in-process PciSource** (pure userspace, no kernel change) — a NEW `DeviceId::VirtioPci(u16)` (string `"virtio-pci:<n>"`, deliberately DISTINCT from the MMIO `Virtio` so the two transports never collide), a `PciNeed::{None,Node}` manifest axis (`pci = "node"`), `NodeResources`/`BoundResources` gain `pci: Option<(u8,u8,u8)>` (the bdf) carried through `resolve` (the I-34 grant property: granted bdf == node's, never fabricated; no MMIO — BARs are handle-gated) + the descriptor codec (`;pci=<b>.<d>.<f>`) + `to_allowance` (`push_pci`, the 6a kernel gate), the pure `parse_pci_ctl` devpci-ctl-line parser (producer/consumer format ground-truthed vs `kernel/devpci.c::build_ctl`), and the `driver`-gated in-process `PciSource` over `/hw/pci` (the DtbSource sibling — TCB-mediated, no reconcile). Self-audit found+fixed the `to_record` silent-bdf-drop seam (fail-closed loud reject — the pipe codec cannot carry a bdf; the in-process source never pipes; a future out-of-process PCIe source extends the record first). Reference `docs/reference/118-libdriver.md` (status → 6b-2). The warden `PciSource` enumeration + the `netdev-pci-driver` bind = 6b-3; the focused audit + the single SMP gate (6b-1 is the arc's only kernel surface) = 6b-4. | **80/80 libdriver host tests** (+ manifest pci-need, + 8 resource pci, + 9 source pci/ctl-parser) + the device build (`tools/build.sh userspace`) compiles clean (libdriver/warden/virtio-mmio-source/netdev-driver) |
| `f759ff0` | **6b-3: the warden PciSource enumeration + `netdev-pci-driver` narrowed — the live I-34-on-PCI proof** (userspace only; 6b-1 remains the arc's sole kernel surface). The warden enumerates `PciSource::new().enumerate()` over `/hw/pci` **in-process** (the DtbSource analog) and — crucially — with **no `reconcile_reported_node`**: the kernel-mediated `/hw/pci` view is trusted by construction, unlike the non-TCB virtio-mmio bus-source Proc. The bind DB gains a `netdev-pci-driver` manifest binding `"virtio-pci:1"` `needs{ pci="node" irq="node:interrupts" dma="pool: 64 KiB" }` (**no `mmio`** — a PCI function's registers are in BARs, mapped off the claimed `KObj_PCI`, not an MMIO allowance window). NEW `usr/netdev-pci-driver` (`impl Driver`, the `netdev-driver` analog over `VirtioNetPci`): `probe` verifies the conferred identity is `virtio-pci:1` (fail-closed on a mis-bind), claims the net function (`SYS_PCI_CLAIM` by virtio-id; the kernel resolves it to the bdf + gates against the conferred PCI axis), maps BARs, inits; `serve` runs the 24-ARP round-trip, then holds long-lived (quiesce-before-`READY`) until `DeviceRemoved`. `VirtioNetPci::quiesce` made `pub` (the forced-teardown-skips-`Drop` discipline, the 5e-1 move for the PCI sibling). **Retires `usr/netdev-pci-test`** (the warden-bound narrowed driver replaces the standalone broad-cap probe; removed from joey's ladder + workspace + build.sh). Self-audit fixed a `doc_lazy_continuation` in the 6b-2 `parse_bdf` doc. The focused audit + the single SMP gate over the complete devpci + netdev-pci surface = 6b-4. | **930/930** + **the live I-34-on-PCI proof**: `warden: /hw/pci discovered 3 PCI function(s)` → `discovered virtio-pci:1 (0.1.0)` → `bind virtio-pci:1 -> netdev-pci-driver [mmio=0 irq=1 dma=0x10000 pci=Some((0,1,0))]` → `PASS -- 24/24 ARP replies via VirtioNetPci (grant-narrowed PCI claim)` → `up (READY) -> DeviceRemoved` → `torn down`; the tally `4 bound, 3 up, 1 gave up, 0 failed`; `Thylacine boot OK`; 0 EXTINCTION; test.sh exit=0; 80/80 libdriver host + device build + clippy clean |
| `5e12d0e` | **6b-4: the focused audit + SMP gate + close — the 6b arc COMPLETE**. One Opus-4.8-max prosecutor (the holotype-reviewer pins Fable, access-restricted → Opus per the reviewer-model record; `MODEL(start)==MODEL(end)`, no fallback) + a concurrent self-audit over the WHOLE arc (devpci + the libdriver PCI axis + the warden `PciSource` + `netdev-pci-driver`). **CLEAN 0 P0 / 0 P1 / 0 P2 / 4 P3 (3 agent + 1 self), NOT dirty** — converged on the SOUND set (I-5 read-only complete [no ECAM / no config-write], the **no-reconcile soundness** [in-process TCB read of a kernel-published write-less tree + the kernel re-resolves the bdf at claim — strictly stronger than the virtio-mmio reconcile path], qid bounds + the `virtio_pci_dev_get` negative-safe forged-qid backstop, the driver fail-closed + quiesce-before-teardown, no info leak, the dual-same-virtio-id fail-closed). P3s: **F1** (`/hw` readdir omits the synthetic `pci` mount point — cosmetic, no v1.0 consumer; document-and-track → task #191, the audited readdir's cookie scheme is not worth churning); **F2** (`parse_pci_ctl` pub+lenient — strengthened the TCB-only CONTRACT caveat); **F3** (`to_record` PCI-reject lacked a dedicated live-producer test — transitively covered; added a witness note); **F4** (self: `devpci_readdir` lacked the `off<0` guard its `devpci_read` sibling has — added the 1-line symmetry; unreachable [`.seekable=false`, cookie ≥1] + `emit_dirent` bounds-safe regardless). Plus a doc-currency sweep for the `netdev-pci-test` retirement (115/117/120). | **930/930** (all 6 `devpci.*` PASS incl. `devpci.readdir` after the off-guard) + boot OK + 0 EXTINCTION + the live I-34-on-PCI proof + test.sh exit=0 + 80/80 libdriver host + clippy clean + **SMP gate PASS (0 corruption, default+UBSan × smp4/smp8 N=10; the 2 "timing" boots ground-truthed guest-clean — 930/930 + the netdev-pci PASS + boot OK + login, a harness post-marker exit race, [[feedback-no-host-load]])**. Closed list `memory/audit_6b_closed_list.md`. |
| `a2a2614` | **net-2a: smoltcp on the NIC** — `usr/netd`, the network daemon. The warden binds `virtio-pci:1 -> netd` **narrowed** (retiring `netdev-pci-driver`, the ARP demo netd subsumes — the NIC handles are non-transferable per I-5, so the claimer runs the stack). netd embeds **smoltcp 0.12** (`no_std`+`alloc`; the libthyla-rs allocator + monotonic clock + CSPRNG) behind a `phy::Device` over `VirtioNetPci` (owned-frame `NicRxToken` / `&mut nic` `NicTxToken` — the no-alias token pattern), and proves the stack end-to-end by acquiring a **DHCP lease** from slirp (Ethernet TX/RX over the BAR-mapped virtqueues → ARP → UDP → the DHCP client). **ONE-SHOT** (exit 0 → warden `Up`), so NO warden-teardown change — net-2b adds persistence. Pure userspace — **kernel byte-unchanged**. Reference `docs/reference/121-netd.md` + the netd audit-trigger row (ARCH §25.4 / CLAUDE.md); the focused audit is net-2d. | **930/930** + `netd: PASS -- smoltcp brought the link up via the PCI NIC (DHCP)` (lease `10.0.2.15/24`, router `10.0.2.2`, dns 1) → `4 bound, 3 up, 1 gave up, 0 failed` + boot OK + 0 EXTINCTION + SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `3bf8978` | **net-2b-1: the persistent netd lifecycle** — netd goes from a one-shot to a resident service. A new libdriver `Lifecycle` manifest field (`persistent` vs default `transient`; on `Manifest` + parser + `to_text` + host tests) + the warden `run_once` READY arm branched on it: `persistent` → **leave the driver running** (drop the `Child` un-waited; `Child::drop` neither reaps nor kills, so netd reparents to the orphan-adopter when the warden exits, its I-34 allowance bound to its own Proc per #160); `transient` (the `netdev-driver` MMIO demo) → the **unchanged** `DeviceRemoved` revoke+terminate. netd's `serve` now signals `READY` after the DHCP lease and stays resident in a `poll_delay`-paced stack loop (clamped `[50 ms, 1 s]` — a floor against the #108 idle-spin class, a ceiling for responsiveness). **No 9P server yet** (`serves = "/net"` declared; net-2b-2 lands the post + mount). Pure userspace — **kernel byte-unchanged**. Reference `docs/reference/121-netd.md` (the net-2b-1 section) + `118-libdriver.md` + MENAGERIE.md §5/§6. | **82/82 libdriver host** (+ `lifecycle_persistent_parses_and_round_trips` + `lifecycle_rejects_bad_value` + the default-`Transient` assert) + **930/930** + the live proof `warden: netd … up (READY) -> serving (persistent; left running)` with the transient `netdev-driver … -> DeviceRemoved` PRESERVED + `4 bound, 3 up, 1 gave up` + boot OK + 0 EXTINCTION + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `bbed134` | **net-2b-2: the 9P `/net` server + the joey mount** — netd posts `/srv/net` (9P-mode) and serves the NET-DESIGN §3.1 directory skeleton over a combined event loop: NEW `usr/netd/src/server.rs` (the read-only `/net` 9P2000.L server — a static node table `tcp/udp/icmp` + a `stats` file each; `server::Conn` fid table; Tversion/attach/walk/lopen/read/getattr/clunk via `libthyla_rs::ninep`; the load-bearing `Tgetattr` security trio `0555`/`0444` SYSTEM, which the A-3 dev9p X-search reads); `main.rs` (post→READY→the `t_poll([listener]+conns, timeout=poll_delay)` loop folding the 9P accept into the stack poll). **The MAY_POST_SERVICE chain** (joey→warden→netd): joey grants the warden `T_SPAWN_PERM_MAY_POST_SERVICE` (`t_spawn_with_perms`; joey console-attached + holds it), the warden re-confers it gated on `lifecycle == Persistent` (`cmd.perm(...)`), so netd can `devsrv_post_listener` — the #827b one-hop delegation extended one hop, no new ABI. **joey** mounts `/net` post-pivot (open=connect `/srv/net` → dev9p root → `t_mount`, fail-soft if absent) + a gated walk+read probe. Pure userspace — **kernel byte-unchanged**. Reference `docs/reference/121-netd.md` (the net-2b-2 section) + `119-warden.md` + MENAGERIE.md §5 + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the 9P-server + perm surface (audit at net-2d). | **930/930** + `netd: serving /net (9P over /srv/net)` + `warden: netd … up (READY) -> serving (persistent; left running)` + `4 bound, 3 up, 1 gave up` (the transient `netdev-driver … -> DeviceRemoved` PRESERVED) + `joey: net-2b-2 /net mounted` + `PROBE /net/tcp/stats OK (43 bytes)` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `9368634` | **net-2c-1: the `/net/tcp` clone fid state machine + the ninep Treaddir codec** (pure userspace; **kernel byte-unchanged**) — grows the net-2b-2 static skeleton into the live §3.4 TCP fid machine. `libthyla_rs::ninep` gains the readdir codec (`P9_TREADDIR/RREADDIR` + `parse_treaddir` + `build_rreaddir` + `pack_dirent` + `DT_DIR/DT_REG`; the kernel dev9p client issues `Treaddir`, not a legacy `Tread` stream, so this is required to list at all). `server.rs` is reworked: the static `NODES` becomes a **qid-encoded dynamic tree** (static skeleton in `[0,8)`; a live connection `N` = `CONN_FLAG(1<<40)│proto<<32│N<<8│filekind`, resolvable only while the slot is live). Opening `/net/tcp/clone` (`Tlopen`) **mints** `N` and *rebinds the fid onto its `ctl`* (the Plan 9 clone idiom — the kernel dev9p client accepts the differing `Rlopen` qid, verified `9p_session.c`). A connection is **refcounted** by the fids naming its subtree (`fid_set` refs-new-before-unref-old; `fid_clunk`/`teardown`/Tversion unref); the **last** unref frees `N` — the only free path (I-10/I-11), bounded at `MAX_SLOTS=16` (#65 DoS floor). A slot carries **no smoltcp socket yet** ("N assigned"; the live data path is net-2c-2). netd is single-threaded → the global `Net` table needs no lock. The joey probe exercises the whole machine end-to-end through the dev9p session. Reference `docs/reference/121-netd.md` (the net-2c-1 section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the fid machine + readdir codec (audit at net-2d). | **930/930** (kernel byte-unchanged) + `joey: net-2c-1 /net mounted` + `net-2c-1 PROBE OK (clone->0, 0/ctl->0, readdir grew, clunk frees+reuses 0)` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + SMP gate (default+UBSan × smp4/smp8 N=10, 0 corruption) |
| `3547635` | **net-2c-2: the live TCP data path** (pure userspace; **kernel byte-unchanged**; only the `socket-tcp` smoltcp feature is new) — makes the net-2c-1 connection LIVE. **The `Net` table owns the stack**: after the DHCP bring-up `serve()` moves the smoltcp `Interface` + `SocketSet` into `Net` (which the 9P dispatch already holds `&mut`), so a `ctl`/`data` handler reaches both as disjoint fields; `device` stays a `serve()` local (`Net::poll(&mut device)`); the loop drives `net.poll` at the top AND after each dispatch batch (a just-enqueued SYN/data egresses that tick). **`clone` reserves a real `tcp::Socket`** (rx/tx 4 KiB; the §3.4 `ALLOCATED` state), removed at the last clunk (`slot_unref` → refs 0 — the sole free path). **The `ctl` verb parser** (§3.3): `connect a.b.c.d!port` → `socket.connect(iface.context(), remote, local)` with a rotating ephemeral local port (smoltcp requires a non-zero one; it auto-selects only the local ADDRESS), recording the resolved `local`/`remote` in the slot synchronously (peer-independent); `hangup` → `close()`; `announce`/`bind`/options → honest `EOPNOTSUPP` (net-3+). **The files**: `status` = live `socket.state()`, `local`/`remote` = recorded `ip!port`, `err` = recorded reason, `data` = `recv_slice`/`send_slice` (non-blocking). The joey probe writes `connect 10.0.2.2!9` on the ctl fid + reads back `remote`/`local` (deterministic, peer-independent) + the multi-fid clunk frees+reuses N. The NIC-IRQ poll fd is DEFERRED (a pollable IRQ fd is a kernel ABI surface; `SYS_IRQ_WAIT` blocks). Reference `docs/reference/121-netd.md` (the net-2c-2 section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the live data path + connect/ctl-verb/socket-reservation surface (audit at net-2d). | **930/930** (kernel byte-unchanged) + `joey: net-2c-2 PROBE OK (clone->0, connect 10.0.2.2!9, local 10.0.2.15!, frees+reuses 0)` + `netd: serving /net` + `4 bound, 3 up, 1 gave up` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + SMP gate (default+UBSan × smp4/smp8 N=10, **0 corruption**; all 31 "timing" boots ground-truthed guest-clean at login — the harness post-marker artifact) |
| `aa364f5` | **net-2d: the focused netd audit + close** (the network arc's central audit-bearing surface, NET-DESIGN §15.2; the only code touched is `usr/netd/src/server.rs` — **kernel byte-unchanged**) — an **Opus-4.8-max prosecutor + a concurrent self-audit**, **CLEAN 0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty**. The fid-machine refcount, the socket reservation/free balance, the disjoint borrow, the parser bounds, the fail-closed non-live ops, the single-threadedness (no `thread_spawn` → the lockless `Net` is sound), the I-5 probe gate, the MAY_POST_SERVICE persistent gate, malformed-frame safety (the ninep `parse_twalk`/`parse_twrite` bounds), and the Tgetattr trio all held. **F1 [P2]**: `h_readdir`'s budget omitted the 11-byte Rreaddir frame overhead (`P9_HDR_LEN+4`) that `h_read` reserves → a populated dir read by a small-msize client overran its negotiated msize → FIXED via a `rreaddir_budget` parity helper. **F2 [P3]**: `h_attach`/`h_walk` accepted the `P9_NOFID` sentinel as a live fid → fail-closed reject. **F3 [P3]**: a rejected re-`connect` burned an ephemeral port + a rolled-back clone over-counted `opened` → peek-then-commit port + `tcp_clone_rollback`. F4/F5 + the cross-session liveness closed-justified (single-threaded + the qid re-validates `slot_live`). The deterministic small-msize/NOFID/failed-connect regressions are architecturally unreachable in-VM (the only /net client is the trusted large-msize kernel dev9p mount; /net is 9P-mode; netd has no host-test harness) → owed to a netd pure-protocol host-test module (net-3+); the fixes rest on data-path parity (`h_read`) + the ninep `build_rreaddir` guard + fail-closed correctness. `memory/audit_net2d_closed_list.md`. | **930/930** (kernel byte-unchanged) + `joey: net-2c-2 PROBE OK` + `netd: serving /net` + `4 bound, 3 up, 1 gave up` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 40/40 clean, 0 corruption, 0 timing)** |
| `da7ffc5` | **net-3a: the TCP server side — `announce` + the blocking `listen`/accept via a deferred 9P reply** (pure userspace; **kernel byte-unchanged**; the only shared-crate change is the `ninep` Tflush/Rflush codec). `announce *!port`/`a.b.c.d!port` puts a connection's socket into LISTEN (`status` reads `Listen`). The blocking `open(/net/tcp/N/listen)` is a **deferred 9P reply** — a single-threaded server cannot block in the handler (it must keep polling the NIC to receive the very SYN that would unblock it = self-deadlock), so `h_lopen` registers a `PendingAccept` + returns a `Disp::Deferred` sentinel (no reply emitted; the client's `open()` stays blocked on the outstanding tag — the dev9p client matches by tag, with no per-op deadline, #811-death-interruptible). The serve loop's `poll_accepts` detects the established listener (`accept_ready` = `is_active() && state ≠ SynReceived`), **swaps** (mints `M` taking the established socket + re-arms `N` with a fresh listener — `N` stays ANNOUNCED), and `complete_accept` rebinds the listen fid onto `M/ctl` (refcount moves `N→M`) + sends the held `Rlopen`. **Tflush** cancels a dead deferred accept + replies `Rflush` — also closing a **pre-existing net-2c-2 outstanding-tag leak** (an ignored Tflush left the kernel tag `awaiting_flush`). This is the committed-blocking realization of §3.4; **no kernel surface** (§12's readiness multiplexing = the net-6 leg). Boot proof (deterministic): `announce *!7777 → Listen` + the `listen` file in the readdir + the not-announced gate. The full inbound-accept E2E is owed to **net-3d** (a deterministic in-guest inbound path — a netd loopback interface). Reference `docs/reference/121-netd.md` (the net-3a section) + NET-DESIGN §3.4/§20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the deferred-reply + announce/listen + Tflush surface (audit at net-3d). | **930/930** (kernel byte-unchanged) + `joey: net-3a PROBE OK (announce *!7777 -> Listen; listen file + readdir; listen gated on announce)` + `netd: serving /net` + boot OK + 0 EXTINCTION + netd clippy-clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption)** |
| `943b72c` | **net-3b: UDP — `/net/udp` clone/connect/data (datagrams)** (pure userspace; **kernel byte-unchanged**; the only new dependency is the `socket-udp` smoltcp feature). The shared `Slot` gains a `proto` field (`PROTO_TCP`/`PROTO_UDP`) — the discriminator for the type-recovering socket access (`sockets.get::<tcp::Socket>` vs `get::<udp::Socket>`; a mismatch *panics* in smoltcp, so every socket touch is dispatched on `slot_proto(n)`). `/net/udp/clone` mints a `udp::Socket` (a `PacketBuffer` of whole datagrams with per-packet sender metadata, unlike TCP's byte stream); `ctl_connect` dispatches — UDP `bind`s a local ephemeral port + records the remote (datagram setup, not a handshake); `data` is `send_slice(data, remote)`/`recv_slice → (n, meta)`; `status` reads `Open`/`Closed`; there is **no `listen` file** under a UDP conn dir (datagrams have no accept — `walk_child` rejects it). The slot pool is shared across protocols, so `walk_child`/`for_each_child` filter the numeric children of `/net/tcp` + `/net/udp` to slots of the matching protocol. The boot demo is a self-contained DNS round-trip through the live `/net/udp` data path (`udp_clone → connect → data_send → poll → data_recv`), bounded-polled inside netd like the DHCP bring-up — **best-effort, logged, never a boot gate** (slirp *forwards* DNS to the host resolver, so a response is environment-dependent). The deterministic asserted proof is joey's `/net/udp` machinery probe; the deterministic UDP-via-9P data round-trip E2E is owed to net-3d (the loopback interface, alongside the TCP accept). Reference `docs/reference/121-netd.md` (the net-3b section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the UDP surface (audit at net-3d). | **930/930** (kernel byte-unchanged) + `netd: net-3b UDP round-trip OK (DNS resp 61 bytes, ancount=2)` + `joey: net-3b PROBE OK (udp clone->0, connect 10.0.2.3!53, remote readback, no listen, frees+reuses 0)` + boot OK + 0 EXTINCTION + netd clippy-clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption)** |
| `e013cfc` | **net-3c: ICMP — `/net/icmp` clone/connect/data (ping)** (pure userspace; the only new dependency is the `socket-icmp` smoltcp feature; ONE benign kernel constant — `JOEY_BLOB_MAX` 256→384 KiB — since the accumulating net boot probes outgrew the init-blob bound). The third `proto` along the `Slot` discriminator. `/net/icmp/clone` mints an `icmp::Socket` bound to a rotated Echo identifier (smoltcp routes EchoReplies back by ident — `ICMP_IDENT_BASE` + per-clone rotation). `connect` is **portless** — `ctl_connect` dispatches to `icmp_connect`, which only records the ping target; the `ctl` verb parser accepts a bare IPv4 (`connect 10.0.2.2`) for an ICMP slot. `data` write wraps the payload into an `Icmpv4Repr::EchoRequest` (smoltcp's own encoder; the iface recomputes the checksum on egress); `data` read parses the queued packet smoltcp already filtered to the bound ident + returns the `EchoReply` payload. `remote`/`local` report a bare dotted-quad (`Content::push_ip`, no `!port`); `status` reads `Open`/`Closed`; there is **no `listen` file** under an ICMP conn dir, and `hangup` is a no-op (connectionless — the clunk frees the slot + removes the socket). Self-audit verified proto-dispatch completeness (every `get::<T>` reached only via a matching `slot_proto` dispatch or a TCP-only-by-construction accept — no unguarded type recovery → no smoltcp panic; the live boot confirms it). The boot demo is a self-contained gateway ping through the live `/net/icmp` data path, **best-effort, logged, never a boot gate** — whether slirp answers a guest echo internally (vs proxying it to a host ping socket) is host-dependent; on the dev host it did NOT answer it, *vindicating* the best-effort framing ([[feedback-no-host-load]] — an asserted round-trip would have flaked). The deterministic asserted proof is joey's `/net/icmp` machinery probe; the in-guest ICMP round-trip E2E is owed to net-3d (the loopback iface auto-replies to an echo to its own IP). Reference `docs/reference/121-netd.md` (the net-3c section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the ICMP surface (audit at net-3d). | **930/930** + `joey: net-3c PROBE OK (icmp clone->0, connect 10.0.2.2, remote readback, no listen, frees+reuses 0)` + `netd: net-3c ICMP round-trip best-effort: no echo reply (slirp host-ping?)` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption)** |
| `5763094` | **net-3d: the focused net-3 audit + the loopback E2E + the deferred-accept hardening** (pure userspace; **kernel byte-unchanged**) — closes net-3. An **Opus-4.8-max prosecutor + a concurrent self-audit** over the cumulative net-3a..3c surface, then a **dirty-close round-2** on the fix (a P1 on the deferred-reply / I-9-adjacent lifecycle). **F1 [P1]**: a deferred-`listen` fid was half-open (`opened == false`) with a committed `PendingAccept` — a native `/net` client could **clunk** it (clunk does not gate on `opened`) without removing the pending; the slot then frees and `clone` re-mints that index **cross-proto**, so the generation-less `PendingAccept{listening_n=N}` resolved into a wrong-proto `get::<tcp::Socket>` panic (a whole-network DoS — netd is the sole NIC owner, I-5). Latent (the trusted kernel dev9p client abandons only via Tflush, which `h_flush` handles) but reachable from the open=connect native client. **Fixed** by four complementary layers: a per-slot monotonic mint **`gen`** (`Slot.gen` + `Net.mint_seq`/`next_gen`, stamped at every mint; the listener keeps its gen on re-arm) + `PendingAccept.listening_gen` + the **`poll_accepts` proto+gen guard** (drop the pending unless the slot is still the same live TCP slot — turning the panic + any strand into a harmless drop, locally sound) + **`cancel_accept_fid`** in `fid_clunk` (clunk cancels the pending → no strand) + marking the listen fid **`opened`** (blocks walk-from + double-defer via the existing gates; `complete_accept`'s `fid_set` ignores `opened`, so the legit completion works). **F2 [P2]** (poll_accepts gated on liveness not proto) folds into the same guard; F3/F4 + SA-2/SA-3 are P3 doc caveats. The **loopback E2E** (`server::loopback_e2e` — an isolated `127.0.0.1/8` stack: a smoltcp `Loopback` device + an `Interface` + its **own** `SocketSet`, driving the **real** `Net` methods) delivers the three owed deterministic in-guest round-trips (TCP inbound-accept — the F1 fix's runtime regression, via the real `poll_accepts`/`accept_swap` — + UDP datagram + ICMP echo). The isolation is load-bearing: a loopback iface sharing the live NIC socket set mis-routes (the NIC default route steals the `127.0.0.1` egress, verified in the smoltcp 0.12 source). `Net::poll` was made generic over the device. Reference `docs/reference/121-netd.md` (the net-3d section) + NET-DESIGN §20 + the netd audit-trigger row (ARCH §25.4 / CLAUDE.md — the CLOSED record). `memory/audit_net3_closed_list.md`. | **930/930** + `netd: net-3d loopback E2E PASS (tcp-accept + udp + icmp, in-guest 127.0.0.1)` + joey net-3a/3b/3c PROBE OK (unregressed) + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption)** |

| `dbc6523` | **NET-DESIGN §5/§18: the net-4 ndb-source refinement** (scripture-first, no code) — net-4's cs/ndb surfaced a real tension: netd is a confined warden-bound leaf driver (I-34) and cannot read `/lib/ndb`, but §5 said cs/dns are netd-served AND ndb is read live from `/lib`. User-voted (2026-06-18) option A: netd serves cs from a **compiled-in ndb** (the static `localhost`/services half) + the DHCP-learned dynamic half read live, with the byte-identical `/lib/ndb/local` baked into the post-pivot FS (user-readable; the v1.x cs/dns-daemon-split's live source). The capability-microkernel config-at-construction idiom (Fuchsia/Genode), which preserves netd's confinement, rather than the Plan 9 full-namespace read a confined driver cannot adopt. Records HOW the ndb is sourced at v1.0; the binding §5 v1.0-fold/v1.x-split staging is unchanged. | scripture-only (no code) |
| `c49bb54` | **net-4a: `/net/cs` + the compiled-in ndb (the dial front door)** (pure userspace; **kernel byte-unchanged**) — the Plan 9 `cs(8)` connection server. NEW `usr/netd/src/ndb.rs` (a stateless ndb(6)-subset parser over `include_bytes!("../ndb/local")`: `lookup_host` [`sys=`/`dom=` → `ip=`] + `lookup_service` [`service=` → `port=`], no heap) + NEW `usr/netd/ndb/local` (the canonical ndb, baked byte-identical to `/lib/ndb/local`). `usr/netd/src/server.rs` adds the `P_CS` static node (walk/readdir/parent/getattr) + `cs_resolve` (`proto!host!service` → `<clone-file> <ip>!<port>`: a numeric IPv4 then the ndb host, a numeric port then the ndb service, `net`→tcp; unresolvable/malformed → an **empty** response, cs's "no path" signal, never an error frame) + the per-fid `CsSession` (the write IS the dial query → `cs_set` stashes the response; the read drains it via a per-fid cursor; dropped on clunk/teardown/Tversion → bounded by the fid table). A native `dial()` opens the clone-file, reads `N`, and writes `connect <ip>!<port>` to its `ctl` — exactly the arg netd's `connect` verb parses. Per the **net-4 ndb-source decision** (`dbc6523`): netd (a confined leaf driver, I-34) cannot read `/lib`, so it uses the compiled-in ndb + the baked user-readable `/lib/ndb/local`; the resolver/router are live from the DHCP lease. `tools/build.sh` bakes `/lib/ndb/local` (SYSTEM-owned, readback-verified). Reference `docs/reference/121-netd.md` (the net-4a section) + NET-DESIGN §5/§18/§20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the cs/ndb surface (the focused net-4 audit is net-4d). | **930/930** + `joey: net-4a PROBE OK (cs tcp!127.0.0.1!80 + net!localhost!http -> /net/tcp/clone 127.0.0.1!80; unresolved -> empty; /lib/ndb/local readable)` + `/lib/ndb/local baked + readback-verified` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10, 0 corruption across 60 boots; every "timing" boot ground-truthed guest-clean at login, count fluctuated 11→8 on a re-gate = the harness post-marker artifact)** |
| `e52d895` | **net-4d: the focused net-4 audit + close + the deferred-read guard + the loopback DNS E2E** (pure userspace; **kernel byte-unchanged**). One Opus-4.8-max prosecutor + a concurrent self-audit over the cumulative net-4 surface (net-4a/4b/4c) — **CLOSE: 0 P0 / 0 P1 / 1 P2 / 3 P3, NOT dirty** (+ a precautionary round-2 on the deferred-reply fix, CLEAN). **F1 [P2]** — a held deferred cs/dns read tag could be LOST (I-9): `Query.deferred` is a single `(tag, cap)` slot, so a *second concurrent read* on one fid (legal for a multi-thread Proc — the kernel dev9p client multiplexes by tag) overwrote it, and a *re-write while a read is deferred* (`query_begin`→`query_drop`) dropped it; either way the first held `Rread` was never delivered (no crash, self-inflicted, death-recoverable → P2). FIXED with two minimal guards (not a wait/wake restructure): `query_read` returns an empty `Rread` for a 2nd read when one is already deferred (the first keeps its real answer); `h_write` rejects (`E_PROTO`) a re-write while `fid_has_deferred(fid)`. The bare-clunk-while-deferred facet is mitigated on the trusted mount (Tflush→`cancel_dns_flush`) + a client protocol violation otherwise (dispositioned per the net-3 listen-clunk close). **F2 [P3]** `Content` buffer 128→256 (the net-4c commit msg wrongly claimed 256; the ~120 B status render was safe but tight — `push` min-clamps, no OOB). **F3 [P3]** documented the shared dns socket's `queries` Vec high-water (bounded ≤ `MAX_CONNS×MAX_FIDS`, reused — not a leak). **SA-3 [P3]** clarified the DHCP-renewal comment (iface + `ifc` both pinned at bring-up → coherent). Three NEW deterministic in-guest proofs (the `loopback_e2e`/`ipifc_e2e` pattern): `proto_selftest` (the cs/dns/ndb/mask parser battery — the OWED-since-net-2d host-test coverage delivered in-guest, since netd is a `no_std`+aarch64 **bin** crate that can't `cargo test` without a feature-gated refactor that would risk the green device build — carried as a named seam), `dns_defer_guard_selftest` (the F1 regression — fails on pre-fix code by construction), `dns_loopback_e2e` (the OWED net-4b deferred-read E2E — a mock DNS responder bound to `127.0.0.1:53` on an isolated stack answers a fixed A query, so the shared `dns::Socket` resolves a name in-guest via the real `dns_query_start`→poll→`dns_poll` methods; the net-3d loopback analog). Reference `docs/reference/121-netd.md` (the net-4d section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the net-4d CLOSED record. `memory/audit_net4_closed_list.md`. **The net-4 arc is COMPLETE.** | **930/930** + `netd: net-4d proto selftest PASS (cs/dns/ndb/mask parsers)` + `net-4d dns defer-guard PASS (no lost held-Rread on a 2nd read)` + `net-4d dns loopback E2E PASS (deferred resolve in-guest 127.0.0.1)` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + the net-2c/3a/3b/3c/3d/4a/4b/4c probes unregressed + **SMP gate PASS** (default+UBSan × smp4/smp8 N=10; every "timing" boot ground-truthed guest-clean per [[feedback-no-host-load]]) |
| `525348f` | **net-4c: `/net/ipifc` + `/net/ndb` + `ipconfig` (interface config + the dynamic database)** (pure userspace; **kernel byte-unchanged**; no new dependency). The IP-configuration half of NET-DESIGN §6. NEW `/net/ipifc/0` tree (`ctl`/`status`/`local`) + `/net/ndb` (the live dynamic network database, ndb(6) format) — the static nodes `P_IPIFC`=12 / `P_NDB`=13 / `P_IPIFC_0`=14 / `P_IPIFC_0_CTL`/`_STATUS`/`_LOCAL`=15/16/17 wired into walk/readdir/parent/getattr/file_content. NEW `server::IfConfig { mac, mtu, addr, prefix, gw, dns, up, dynamic }` (on `Net`): the DHCP lease folds into it at bring-up (`main.rs` captures `cfg.address`/`router`/`dns_servers` → `IfConfig`, surfaced read-only through status/local/ndb — the "DHCP-lease-into-ipifc" + "ndb dynamic half") and `Net::new` seeds the net-4b resolver socket from `ifc.dns` (one source of truth for cs→dns + the ndb `dns=` line). NEW `ipifc_ctl` (the `/net/ipifc/0/ctl` verbs): `add <ip> <mask> [gw]` applies static config (`ifc_set_static` — `parse_mask` accepts dotted-quad/`/N`/bare-prefix, contiguity-checked) onto **both** the live smoltcp iface AND the snapshot; `remove`/`unbind` clears it (`ifc_clear`); `bind ether` is a v1.x seam (rejected honestly). NEW `usr/coreutils/src/bin/ipconfig.rs` (the native `ip addr`/`ifconfig` equivalent: no-args prints status+ndb, `add`/`remove` write the ctl) + baked. NEW `server::ipifc_e2e` (a throwaway-`Net` selftest asserting the add/remove/status/ndb/local renders + malformed rejects, deterministically, off the live config — the loopback-E2E pattern). Reference `docs/reference/121-netd.md` (the net-4c section) + NET-DESIGN §6/§20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the ipifc/ndb surface (audit at net-4d). | **930/930** + `netd: net-4c ipifc E2E PASS (add/remove static + status + ndb, in-guest)` + `joey: net-4c PROBE OK (ipifc status addr=10.0.2.15 dhcp gw=10.0.2.2; ndb ip=10.0.2.15 dns; local addr; ctl rejects malformed)` + boot OK + 0 EXTINCTION + netd clippy/fmt clean + the net-2c/3a/3b/3c/3d/4a/4b probes unregressed + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 16 PASS / 0 CORRUPTION / 24 timing / 0 other across 40 boots; every "timing" boot ground-truthed to the FULL healthy guest end-state — boot OK + 930/930 + net-4c PROBE OK + 0 EXT + login prompt — so the guest is proven clean, the "timing" is the measured HVF post-marker exit artifact, #34/#105, per [[feedback-no-host-load]])** |
| `1f7a971` | **net-4b: `/net/dns` (the resolver) + cs→dns delegation** (pure userspace; **kernel byte-unchanged**; the only new dependency is the `socket-dns` smoltcp feature). One shared `dns::Socket` (seeded from the DHCP resolver — `Net::new(.., dns_servers)`; absent if the lease carried none → queries fail closed fast) multiplexes every `/net/dns` + cs→dns query. The net-4a `CsSession` generalizes into the deferred-capable per-fid `server::Query { fid, kind, query, resp, cursor, deferred }`: the write begins the resolution (`query_begin` — host resolves **numeric → ndb → DNS**; numeric/ndb fill `resp` synchronously, a DNS name starts a query with `resp=None`); the read (`query_read`) drains `resp` if ready, else **defers** (the net-3a held-`Rread` mechanism — `poll_dns` sends it when the query resolves; the dev9p client matches by tag, no per-op deadline, #811-death-interruptible). cs→dns is automatic (the cs unresolved-host branch becomes the DNS-delegate branch with `kind=Cs`). **The smoltcp query-lifetime hazard** (`get_query_result` frees the slot on a result + **panics on a free slot**) is closed by keeping the handle in **one place** (`Query.query`) nulled the instant `dns_poll` returns a result → never double-polled (a netd panic = a whole-network DoS); `dns_cancel` (clunk/teardown/Tversion/Tflush/rewrite) runs only on a still-occupied slot. v1.0 is IPv4-A only. The serve loop gains a `poll_dns` delivery pass + folds `has_pending_dns` into the poll-delay clamp. Reference `docs/reference/121-netd.md` (the net-4b section) + NET-DESIGN §20; the netd audit-trigger row (ARCH §25.4 / CLAUDE.md) gains the dns surface (audit at net-4d). The deterministic in-guest E2E of the 9P deferred-read plumbing is **owed to net-4d** (a loopback DNS responder, the net-3d analog). | **930/930** + `joey: net-4b PROBE OK (dns 10.0.2.2 -> 10.0.2.2; localhost ip -> 127.0.0.1; aaaa -> empty)` + `netd: net-4b DNS live query OK` (best-effort, logged) + boot OK + 0 EXTINCTION + netd clippy/fmt clean + the net-2c/3a/3b/3c/3d probes unregressed + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 40/40, 0 corruption, 0 timing)** |
| `a3b11a6` | **net-5: the AF_INET socket-compat pouch boundary-line** (NET-DESIGN §7; pure userspace; **kernel/arch/mm byte-unchanged**; **NO new syscall** — closes W4-F2). NEW `usr/lib/pouch/patches/0016-pouch-net-sockets.patch` translates the Linux/pouch AF_INET BSD calls (`socket`/`connect`/`bind`/`listen`/`accept`/`send`/`recv`/`getsockname`/`getpeername`/`setsockopt`/`getsockopt`) into operations on netd's `/net` 9P files — the Genode `socket_fs`-in-libc model. It **stacks on** `0006-pouch-sockets.patch` (the AF_UNIX-over-`/srv` layer): a slot gains a `family` field (FAM_UNIX = the 0006 `/srv` path, family 0; FAM_INET = the `/net` path) and each call gains an AF_INET arm + new `pouch_net_*` helpers (clone-open+read-N / conn-file open / `connect`+`announce` cmd build / `local`/`remote` endpoint parse). `socket(AF_INET)` opens `/net/{tcp,udp}/clone` (eager, the Plan 9 factory — reads `N`, holds `ctl`); `connect` writes `connect a.b.c.d!port` + opens `data`; `bind` stashes the local addr (netd has no `bind` verb); `listen` writes `announce *!port`; `accept` opens `listen` (blocks) → reads `M` → opens `M/data` → mints a slot. The **blocking** subset only — `SOCK_NONBLOCK` → `EOPNOTSUPP` (non-blocking + `poll`/`select` are net-6's `dev9p.poll`); `setsockopt` benign-no-ops only the options netd already satisfies (SO_REUSEADDR/REUSEPORT/TCP_NODELAY), the rest `ENOPROTOOPT` (P-3). NEW prover `usr/pouch-hello/pouch-hello-net.c` (the first POSIX `socket()` program Thylacine runs) + the joey net-5 PROBE; `pouch-hello-sockets.c` subtest A updated (AF_INET6 — AF_INET is now valid). The live `recv` returns 0 (netd's data read is non-blocking — net-2c-2; blocking-until-data is the net-6 leg). Reference `docs/reference/78-pouch.md` (the AF_INET backend) + NET-DESIGN §7/§20; the new pouch-socket audit-trigger row (ARCH §25.4 / CLAUDE.md). The full deterministic in-guest data round-trip + the server/soak E2E are net-8 (§16). | **930/930** + `joey: net-5 PROBE OK (pouch AF_INET socket-compat over /net)` + `pouch-hello-net: control surface OK` + `pouch-hello-net: exit 0` + boot OK + 0 EXTINCTION + the net-2c/3a/3b/3c/3d/4a/4b/4c/4d probes unregressed + **SMP gate PASS** (default+UBSan × smp4/smp8 N=10 = 40/40, 0 corruption, 0 timing, 0 other) + **focused audit CLOSE 0 P0 / 0 P1 / 1 P2 / 3 P3, NOT dirty** (Opus-4.8-max prosecutor + concurrent self-audit; F1 [P2] the absent shutdown/sendto/recvfrom overrides + F2 [P3] recv-returns-0-on-no-data → documented **net-6** readiness seams [#209]; F3/F4 [P3] unreachable-latent hardening [#210]; `memory/audit_net5_closed_list.md`) |
| `43a5dae` | **net-6a-1: netd blocking `data` reads** (NET-DESIGN §12; pure userspace; **kernel byte-unchanged**; no new dependency) — closes the net-5 audit **F2** seam (`recv()` returned 0 on transient no-data). A `read` on `/net/<proto>/N/data` now **blocks** until bytes arrive or the stream ends, so a pouch `recv()` → `read()` blocks **automatically** (no shim change). NEW `data_recv_outcome` returns `RecvOutcome::{Data, WouldBlock, Eof}` — the EOF-vs-no-data-yet distinction the older `data_recv` collapsed, grounded in smoltcp 0.12's `recv_error_check` (TCP `Ok(0)`=WouldBlock, `Err(Finished)`=Eof, `Err(InvalidState)`+`is_active`=connecting-WouldBlock; UDP/ICMP `Err(Exhausted)`=WouldBlock, never Eof). `h_read(FK_DATA)` parks a `PendingRead { fid, slot_n, tag, cap }` on `WouldBlock` (returns `Disp::Deferred` — the held tag, no reply); the serve loop's `poll_data` (the analog of `poll_dns`/`poll_accepts`, run after `net.poll`) delivers the held `Rread` on data/EOF — the net-3a/4b **deferred-reply pattern** applied to the data stream, with the same clunk/teardown/Tversion/Tflush cancel discipline + a `MAX_FIDS` bound (#65 floor) + FIFO over the slot. I-9 holds by netd's single-threadedness (`net.poll` runs before `poll_data`, so no edge is lost). A **self-audit** caught + fixed a `count==0`-read park (a 0-length dequeue is `Ok(0)` even on a readable socket → would have blocked forever; guarded to return 0 at once). Deterministic proof: `server::recv_blocking_e2e` — an isolated `127.0.0.1` loopback connection driven through the **real** `data_recv_outcome` across empty→`WouldBlock`, sent→`Data("hi")`, peer-close→`Eof`. The full 9P-level blocking round-trip is structurally identical to the net-3a/4b-audited deferred-reply path and is proven end-to-end at net-8. The synchronous `poll()`/`select()` readiness bridge is the separate **net-6b** (`dev9p.poll`). Reference `docs/reference/121-netd.md` (the net-6a section) + NET-DESIGN §12.2/§17/§20; joins the existing netd audit-trigger row (the formal net-6 audit is net-6b/net-8). | **930/930** + `netd: net-6a recv-blocking E2E PASS (WouldBlock/Data/Eof, in-guest 127.0.0.1)` + the net-3d/4c/4d E2Es + the net-2c/3/4 + net-5 probes unregressed + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 40/40, 0 CORRUPTION; net-6a PASS on all 40; the 29 "timing" boots all ground-truthed to the full healthy end-state — boot OK + 930/930 + net-6a PASS + 0 EXT + login — the #34/#105 HVF post-marker exit artifact, elevated because the gate ran concurrently with the build/boot cycles, per [[feedback-no-host-load]])** |
| `9874ce0` | **net-6a-2: pouch `shutdown`/`sendto`/`recvfrom` completion** (NET-DESIGN §7 + §12; pure userspace; **kernel byte-unchanged**; no new dependency) — closes the net-5 audit **F1** seam (the three calls were not tag-aware → `ENOSYS` at the `0xFFFF` sentinel). NEW `0017-pouch-net-datacalls.patch` (stacking on 0016) makes them tag-aware over netd's `/net`: **`shutdown`** — `SHUT_WR`/`SHUT_RDWR` write `hangup` to `ctl` (netd's TCP hangup is smoltcp `tcp::close` = a FIN on the send side; the socket keeps receiving until the peer FINs, exactly `SHUT_WR` — the read-the-response idiom survives), `SHUT_RD` a local no-op, not-connected → `ENOTCONN`, bad `how` → `EINVAL`, `AF_UNIX` → no-op `0`; **`sendto`** — no-dest = plain send, `AF_INET` TCP + dest = connection-mode (dest ignored, POSIX), `AF_INET` UDP + dest = `connect a.b.c.d!port` to `ctl` (netd `udp_connect` re-points the remote every call → a **per-datagram dest** works) + lazy data-open + write, `AF_UNIX` + dest → `EOPNOTSUPP`; **`recvfrom`** — blocking read (net-6a-1) + best-effort `src` fill from `/net/<proto>/N/remote`. `sendmsg`/`recvmsg`/`socketpair` stay a fail-closed **ENOSYS seam** (the scatter-gather + `cmsg` surface; no v1.0 consumer needs it; a single-iovec half-version now would be the half-feature net-5 warned against). The stale `_pouch_socket.h` socket-call-coverage comment (and 78-pouch's pre-net-5 deferred table) are corrected to the true surface. Self-audit clean (the UDP lazy-open shares net-5 connect()'s documented single-user-per-socket discipline; all flag/dest/state error paths POSIX-honest). Deterministic proof: the `/pouch-hello-net` control surface — `shutdown` bad-`how`→`EINVAL` / fresh→`ENOTCONN`, `sendto`/`recvfrom` bad-flag→`EOPNOTSUPP` / fresh→`ENOTCONN`, a UDP `sendto` to the on-link gateway 10.0.2.2:9 (egresses without a peer) → `getpeername` reads the dest back → `shutdown(SHUT_WR)` drives the real `hangup` ctl write. Reference `docs/reference/78-pouch.md` (the AF_INET backend) + NET-DESIGN §7/§20; joins the existing pouch-socket audit-trigger row. | **930/930** + `joey: net-5/6a-2 PROBE OK (...shutdown/sendto/recvfrom...)` + `pouch-hello-net: control surface OK` + `exit 0` + the net-2c/3/4/5 + net-6a-1 probes/E2Es unregressed + boot OK + 0 EXTINCTION + **SMP gate PASS (default+UBSan × smp4/smp8 N=10; kernel byte-unchanged so no kernel corruption is possible — the gate confirms the boot + pouch socket layer hold under SMP)** |
| `934865e` | **net-6a-3: the native `libthyla_rs::net` API + `usr/net-echo` + the Loom async composition** (NET-DESIGN §12.1/§16; **pure userspace; kernel byte-unchanged; no new dependency**). The native, Plan 9-shaped client of `/net` — the libthyla-rs analog of the pouch AF_INET boundary-line. NEW `usr/lib/libthyla-rs/src/net.rs`: `Ipv4Addr` + `SocketAddrV4` (parse/Display, bounded + reject-malformed); `TcpStream` (`connect`/`read`/`write`/`write_all`/`shutdown`/`peer_addr`/`local_addr`/`as_raw_fd`/`io::Read`+`Write`); `TcpListener` (`bind`/`accept`→`(TcpStream, SocketAddrV4)`/`local_addr`); `echo_serve` (the §4 single-threaded echo-server lifecycle). The protocol is netd's `/net` (clone→`ctl`→`connect`/`announce`/`hangup`, `data`, `listen`-accept), reusing net-6a-1's blocking read + net-6a-2's `hangup`. NEW `usr/net-echo` — the echo-server artifact + the deterministic boot probe (the parse round-trips, `bind`/`announce`/`local` over the live `/net`, and the **Loom async witness**: a Loom `READ` on a `/net` fid completes via the kernel dev9p async client, proving network I/O rides Loom with **zero new Loom core**, §12.1). NEW netd `server::echo_e2e` — the deterministic in-guest **≥2-concurrent** proof: announce one listener, accept TWO clients (the real `poll_accepts`/`accept_swap`), echo each, verify each reads back its OWN bytes (mapping-independent) — the server LOGIC the native `TcpListener` drives, over an isolated 127.0.0.1 loopback (the net-3d pattern; the sequential connect+accept respects netd's documented backlog-1). The Loom composition verified: a Loom READ on `data` composes with net-6a-1's `poll_data` (recv stream), a Loom LOPEN on `listen` composes with net-3a's `PendingAccept` (async accept). **Seams → net-8** (which owns the in-guest peer): the live cross-Proc native-API round-trip (netd's live stack is NIC-only — no loopback route), the literal §12.1 multishot-*read*-on-`listen` accept loop (wants a netd listen-read-defer), and a backlog > 1. Self-audit clean (the native module is a validated `/net` client — `File` Drop closes ctl+data, parse/read bounds checked, no new invariant; `echo_e2e`'s slot-free balance mirrors the audited `lo_tcp_accept`). NEW reference `docs/reference/122-net.md` + NET-DESIGN §20; joins the existing netd audit-trigger row (the formal net-6 audit is net-6b). | **930/930** + `netd: net-6a-3 echo E2E PASS (2 concurrent accept + bidirectional echo, in-guest)` + `net-echo: PROBE OK` + `joey: net-6a-3 PROBE OK` + the net-2c/3/4/5 + net-6a probes/E2Es unregressed + boot OK + 0 EXTINCTION + netd clippy/fmt clean + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 21 PASS / 0 CORRUPTION / 19 timing / 0 other across 40 boots; per [[feedback-no-host-load]] EVERY one of the 19 "timing" boots was ground-truthed to the FULL healthy guest end-state — boot OK + 930/930 + net-6a-3 echo E2E PASS + 0 EXT + login — so the guest is PROVEN clean on all 40; kernel byte-unchanged so kernel corruption is impossible by construction; the elevated timing count is the #34/#105 post-marker qemu-exit artifact, amplified because the gate ran concurrent with the build/boot/doc cycles)** |

| `fea625b` | **net-6b-1: `specs/net_poll.tla`** (the `dev9p.poll` readiness bridge; spec-first re-enabled for this kernel ABI surface per the 2026-06-18 user vote; **model-first** — written + TLC-green BEFORE the impl, the asid/allowance/loom_devgone precedent). Pins the **PROBE-then-observe** discipline neither `poll.tla` nor `cons_poll.tla` covers: a 9P socket's readiness lives in netd and must be **elicited** (a deferred readiness read; the offset carries the event mask), so `dev9p.poll` must ensure a readiness read is **outstanding** — atomically with installing the hook and sampling the cached bitmap — BEFORE the poller observes not-ready and parks; else the readiness edge fires with no request to answer and the poller sleeps forever (I-9 generalized to the elicited-readiness relay). The reply is demuxed by the #841 elected reader (driven by a per-client poll-pump kthread, the Loom-4 SQPOLL analog); `on_complete` records the bitmap under `c->lock`; the kthread walks `poll_waiter_list` in process context (the LS-8a `console_mgr` deferred-wake). `specs/SPEC-TO-CODE.md` gains the net_poll section (action↔impl map filled at net-6b-2). | TLC: `net_poll.cfg` clean GREEN (10 states) + `net_poll_buggy_lost_ready.cfg` → `NoMissedNetPoll` counterexample (depth 4) + `net_poll_liveness.cfg` `PollerEventuallyServed` GREEN |
| `8bbcb2a` | **net-6b-2a: the netd `ready` readiness file** (the `dev9p.poll` netd half; NET-DESIGN §12.2; **pure userspace; kernel byte-unchanged; no new dependency**). NEW non-consuming **`/net/<proto>/N/ready`**: a `read` carries the requested poll event-mask in its **Tread offset** (`POLLIN | POLLOUT`, mirroring the kernel `poll.h` bits — `dev9p.poll` is the only client) and returns the satisfied `revents` as a **u32 LE** without consuming socket data. `check_ready(n, mask)` = `POLLIN` iff `mask & POLLIN` and readable (`slot_poll_readable` — data buffered OR EOF, so a read won't block; non-consuming, from `can_recv` + the TCP **state machine**, since `peek_slice` cannot distinguish EOF from empty), `POLLOUT` iff `mask & POLLOUT` and writable (`slot_can_send`), `POLLERR`/`POLLHUP` always. Non-zero → reply the 4 bytes; zero → **DEFER** (park a `PendingReady { fid, slot_n, tag, mask }`; `poll_ready` delivers the held Rread when the socket is ready for that mask) — so a `poll(POLLIN)` on a writable-but-empty socket waits for **data**, never busy-loops (the net-6b motivation). Wired into walk/readdir/getattr (FILE_RO); cancelled on clunk/teardown/Tversion/Tflush (the net-6a `PendingRead` discipline); bounded by `MAX_FIDS` (#65). **Self-found + fixed**: the first `peek_slice`-based readability mis-reported EOF as not-ready (`leg3-eof-readable` FAIL) → switched to the state-machine check. Reference `docs/reference/121-netd.md` (the net-6b section). The kernel `dev9p.poll` slot + the poll-pump kthread (the ABI surface) are net-6b-2b. | **930/930** + `netd: net-6b ready E2E PASS (POLLOUT/POLLIN/EOF-readable, in-guest 127.0.0.1)` + the net-3d/4/6a E2Es + the net-2c/3/4/5/6a probes unregressed + boot OK + 0 EXTINCTION + netd clippy/fmt clean |
| `9ef2de6` | **net-6b-2b: the kernel `dev9p.poll` slot + the global poll-pump kthread** (the ONE new kernel surface in the net arc; NET-DESIGN §12.2; scripture `eeab08e` reserves the QTPOLL marker). `dev9p` gains a real `.poll` slot (NEW `kernel/dev9p_poll.c`): a netd readiness file (cached qid.type carries **`QTPOLL`** = 0x01, an unused 9P2000.L bit netd sets on the `ready` file) is PROBED; a regular dev9p file — Stratum/corvus — stays POSIX always-ready (the **fail-safe gate** — dev9p is generic, and a native `SYS_POLL` on a regular dev9p file is reachable). A `poll()` caller PARKS (it does not block-read), so a boot-spawned **global poll-pump kthread** (the cons_poll `console_mgr` + Loom-4 SQPOLL analog) drives the #841 elected reader for the outstanding readiness Tread (offset = the event mask), **borrowing** the netd client from a live op's `spoor_ref` pin (the Loom borrow-guard, never owning the client lifetime). `dev9p_poll_complete` records the bitmap into the Spoor's poll-state under `c->lock` (atomics only — no sleep, no poll-state lock, no re-entry) + wakes the kthread, which walks `poll_waiter_list` in **process context** + reaps the terminal op (`PollerRegister` / `NetdReplyDemux` / `KthreadWalk` = the `net_poll.tla` actions; the I-9 register-then-observe). The per-op mask = the caller's events — a broader concurrent poller widens it by abandon+resubmit (#845 Tflush) so a `poll(POLLOUT)` can't hang behind a `poll(POLLIN)`; a stranded op (a timed-out poll on a never-ready socket) is GC'd when its poll-list empties; a non-deadline-capable transport degrades to always-ready (fail-safe). Lock order `g_dev9p_poll_lock → c->lock`; the GC empty-check is nested under g_lock for atomicity with the unlink vs a concurrent reuse. Joins the ARCH §25.4 + CLAUDE.md audit-trigger table (the focused audit is net-6b-4). | **931/931** + `joey: net-6b PROBE OK (dev9p.poll: udp ready POLLOUT-ready + POLLIN-times-out, no hang)` + the `dev9p.poll_regular_file_always_ready` kernel test + net-3c/net-6a-3 + all net probes unregressed + boot OK + 0 EXTINCTION |
| `c49313f` | **net-6b-3: the pouch `poll()`/`select()` readiness translation** (NET-DESIGN §12.2; **pure userspace; kernel byte-unchanged**). NEW `usr/lib/pouch/patches/0018-pouch-net-poll.patch` (stacks on 0017/0016/0015) makes pouch/Linux `poll()`/`select()`/`pselect`/`ppoll` work over an AF_INET `/net` socket. The hazard: a tagged socket fd's `kernel_fd` is the `/net/<proto>/N/data` fd — a **regular** dev9p file, which the kernel `dev9p.poll` slot (net-6b-2b) treats as POSIX always-ready (it probes only a `QTPOLL`-marked Spoor), so the 0015 tag-dispatch (which polls `kernel_fd`) reports a `/net` socket ready unconditionally — `poll(POLLIN)` returns instantly, `select()` reports every `/net` fd ready always. FIX: a new slot helper `pouch_sock_poll_fd` resolves the **poll target** (distinct from `pouch_sock_kernel_fd`, the I/O target) — a FAM_INET socket polls its `/net/<proto>/N/ready` sibling (the `QTPOLL` readiness file, lazily opened on first poll + held for the socket's lifetime, closed at `close()` alongside `ctl_fd`); a FAM_UNIX socket keeps polling its `/srv`-stream `kernel_fd` (unchanged); the events bits pass straight through (the kernel threads them as the readiness `Tread` offset). `src/select/poll.c` routes its tagged-fd dispatch through `pouch_sock_poll_fd`; `select`/`pselect`/`ppoll` inherit (they route through `poll()` per 0005). NO new kernel surface (only a `SYS_open`). Composes I-1/I-5/I-23/I-28 + I-9; adds NO new invariant. The `/pouch-hello-net` probe gains a poll subtest: a connected UDP socket reads `POLLOUT`-ready but `poll(POLLIN)` **times out** (the load-bearing assertion — `poll()` *waited* on the `ready` file, not the always-ready data fd). **v1.0 seam — listener-poll (#220)**: netd's `check_ready` reports `can_recv()` (false for a TCP listener), so `poll(listener, POLLIN)` does not wake on a pending accept (the blocking accept via `open(listen)` works); weighed at net-6b-4. Reference `docs/reference/78-pouch.md` (the AF_INET backend, the poll subsection) + `docs/reference/121-netd.md` (the listener-poll seam) + NET-DESIGN §12.2. The focused audit + SMP gate are net-6b-4. | **931/931** + `pouch-hello-net: control surface OK (.../poll/close)` + `joey: net-5/6a-2 PROBE OK` + `joey: net-6b PROBE OK` + `netd: net-6b ready E2E PASS` + all net probes unregressed + boot OK + 0 EXTINCTION |
| `caa4db3` | **net-6b-4: the focused `dev9p.poll` audit + SMP gate + close** (the formal net-6 focused audit; the ONE kernel ABI surface of the net arc). TWO Opus-4.8-max prosecutor rounds (no Fable; `MODEL` start==end both — no fallback) + a concurrent self-audit each: R1 **0 P0 / 1 P1 / 0 P2 / 4 P3** — **DIRTY** (the P1 fix restructured the kthread pump loop) → R2 on the fixes **0/0/0/2** (both P3s sharpenings of R1 seams, addressed); **CONVERGED CLEAN** over 2 rounds. **F1 [P1]: the global poll-pump pumped only the HEAD op's client** (`dev9p_poll_borrow_client`) → a perpetually-parked op on one QTPOLL client starved a SECOND client's pending reply — v1.0-SAFE (one netd → one `dev9p` client demuxes all readiness replies by tag), **latent under the A-5b per-user-netd v1.x config** (the self-audit traced the single-client path; the prosecutor found the multi-client gap — the two-prosecutors-catch-different-things value). FIXED: `dev9p_poll_collect_clients` pumps EVERY distinct non-terminal-op client per cycle (bounded `DEV9P_POLL_MAX_PUMP=16`, dedup-by-client, the borrow-guard N-fold; k≤16 = every realistic config COMPLETE). **F2 [P3]** OOM-no-covering-op unwakeable-park (+ R2-F2 the narrower-live-op-under-sustained-OOM no-progress) → degrade to always-ready. **F5 [P3]** the lockless `p->poll` read → `__atomic` ACQUIRE/RELEASE. F3 (idle-pump 50Hz wake, #221) + F4 (pouch lazy ready-fd ABA, #222) + R2-F1 (>16-client STARVES via LIFO, #223) + #220 (netd listener-poll) = documented v1.x seams. The borrow-guard (N-fold pin/clunk), the `dev9p_close` extinction-invariant (a live `ps->op` always holds its own pin), the lock order, and the `npump==0`-vs-`count!=0` park race (sleep() re-checks the cond pre-register) ALL traced sound by both prosecutors. `memory/audit_net6_closed_list.md`. **THE net-6b ARC IS COMPLETE.** | **931/931** + spec gate RE-VERIFIED (`net_poll.cfg` clean GREEN + `BUGGY_LOST_READY` counterexample + liveness GREEN) + boot OK + 0 EXTINCTION + **SMP gate PASS (default+UBSan × smp4/smp8 N=10 = 0 CORRUPTION across all 40 boots; 13 clean-PASS + 27 timing, and per [[feedback-no-host-load]] EVERY one of the 27 timing boots was ground-truthed to the FULL healthy guest end-state — boot OK + 931/931 + `net-6b PROBE OK` + login + 0 EXT — so the guest is PROVEN clean on all 40; the elevated timing count is the #34/#105 harness post-marker qemu-exit artifact, amplified because the gate ran concurrent with the audit-doc-write cycles + the UBSan builds — the lesson is to keep the host quiet for the gate)** |
| `852fdac` | **net-7a-1: `SYS_CLOCK_SETTIME = 79` -- the runtime wall-clock setter** (the ABI surface for net-7's SNTP; closes the LS-K v1.x settability seam; NET-DESIGN §10; user-voted 2026-06-19 the syscall over the Plan-9 `/dev/time` file + the report-only fallback). `kernel/syscall.c::sys_clock_settime_handler` (REALTIME-only [MONOTONIC -> `-EINVAL`]; **`CAP_HOSTOWNER`-gated** [`-EACCES`] -- a clock step is system-global, the host owner's authority not an identity's, I-22; clk_id + cap validated BEFORE the buffer; `uaccess_load_u32` x4 reads the `t_timespec`; `tv_sec`/`tv_nsec` range + the `tv_sec*1e9` overflow guard -> `-EINVAL`; bad va -> `-EFAULT`) + dispatch; `arch/arm64/timer.c::timer_reset_wallclock_anchor_ns` (the **single-`u64`** `g_wallclock_offset_ns` re-anchored at ns granularity via ONE atomic store -- SMP-safe by construction, a concurrent `timer_realtime_ns` reads old-or-new, each coherent, no seqlock; MONOTONIC untouched; the boot anchor now routes through the same ns publish, byte-equivalent); `usr/lib/libthyla-rs` (`t_clock_settime` + `time::set_realtime`/`SystemTime::from_unix`) + `usr/lib/libt` (the clock surface: `t_timespec` + `t_clock_gettime`/`t_clock_settime` + `T_SYS_CLOCK_*TIME`, for joey's proof). NEW privilege surface -> JOINS the ARCH §25.4 + CLAUDE.md audit-trigger table (the focused audit is net-7d). The SNTP consumer is net-7a-2. | **default boot: 933/933** (the 2 new `clock.settime_reanchors` + `clock.settime_cap_gate` kernel tests) + `joey: net-7a SYS_CLOCK_SETTIME round-trip OK` (the elevated end-to-end proof: real dispatch + uaccess-from-a-valid-buffer + a live `CAP_HOSTOWNER` -- the path neither the kernel test [va=0] nor the SNTP tool [`-EACCES`] reaches) + boot OK + 0 EXTINCTION + all net probes unregressed |
| `0147e32` | **net-7a-2: the native SNTP client** (`usr/sntp`, RFC 4330; NET-DESIGN §10; **pure userspace; kernel byte-unchanged; no new dependency**). The net-7a-1 consumer + the first UDP client. NEW `libthyla_rs::net::UdpSocket` — the connected-UDP analog of `TcpStream` over `/net/udp` (`connect`→ binds a local port; `send`/`recv` = one datagram each; `ready_fd` = the QTPOLL `ready` sibling for a **bounded** receive, net-6b); the `/net/tcp`+`/net/udp` path helpers are now proto-parameterized (`clone_conn`/`open_conn_file`/`read_endpoint` take `"tcp"`/`"udp"`; TCP byte-unchanged — proven by the net-echo/net-2c/3 probes unregressed). NEW `usr/sntp`: builds an NTPv4 request, converts the reply's Transmit timestamp 1900→Unix (`−2_208_988_800`, saturating below the delta — the era-2036 seam), computes the offset against the LS-K **MONOTONIC** clock (`transmit + rtt/2`), and steps `CLOCK_REALTIME` via `time::set_realtime` (the net-7a-1 syscall). `sntp` (no args) = the **deterministic boot gate**: the build/parse/validate battery (mode-4 + stratum∈[1,15] + originate-nonce-echo anti-spoof + non-zero transmit + short-packet reject) + the offset arithmetic + the **EACCES clock-step gate proof** (spawned unelevated, `set_realtime` MUST be denied — a non-denial is a privilege regression that fails the boot). `sntp <a.b.c.d>` = the best-effort live query (bounded-poll the `ready` sibling for POLLIN, 3 s; host-dependent under slirp, NOT a gate). No new kernel surface (open/read/write/close over `/net` + the landed `SYS_CLOCK_SETTIME` + `SYS_POLL`); composes I-1/I-5/I-23/I-28; **no new §28 invariant**. Self-audit clean (the `UdpSocket` is a validated `/net` client — `File` Drop closes ctl+data, the codec bounds-checks every field, the proto-refactor is behavior-preserving for TCP; on-path NTP trust + NTS are documented v1.x seams). The `UdpSocket`/`sntp` surface joins the net-6a-3 `122-net.md` audit context (the formal net-7 audit is net-7d). Reference `docs/reference/122-net.md` (UdpSocket + the `sntp` section) + NET-DESIGN §10. The in-guest live round-trip over a loopback UDP responder is owed to net-8. | **933/933** + `sntp: SELFTEST OK (NTPv4 build/parse/validate + 1900->Unix + offset + EACCES gate)` + `joey: net-7a-2 PROBE OK (native sntp NTPv4 selftest + EACCES clock-step gate)` + the net-7a-1 round-trip + all net probes unregressed + boot OK + 0 EXTINCTION. **SMP gate deferred to net-7d** (the arc's close-time gate; pure userspace, kernel byte-unchanged) |
| `5c6febf` | **net-7b scripture: `/ctl/net` -> `/net/summary`** (NET-DESIGN §11; no code; user-voted 2026-06-19, Option A of three). The charter designed the one-shot network summary at the kernel-`/ctl` `/ctl/net`, but the data a summary aggregates — the live connection table — is netd-held, not kernel-held (§11 itself: it "reports only what the reader's /net view already exposes"). A literal kernel `/ctl/net` would reach up into a userspace service from a devctl read (a layering inversion) and could not honour the per-territory "reader's /net view" from a kernel mount. Resolve: the rollup lives where its data lives — netd serves `/net/summary`. Scripture-only: §11 rewrite + §17 net-7 row + §19 W4-F5 ledger row. | (no code) |
| `c6fa8f8` | **net-7b: observability — `/net/summary` rollup + the native `netstat`** (NET-DESIGN §11; **pure userspace; kernel byte-unchanged; no new dependency; no new §28 invariant**). netd already serves the observability tree (`/net/{tcp,udp,icmp}/stats` active/opened + per-connection `status`/`local`/`remote`/`err` + `/net/ipifc/0/status` + `/net/ndb`); net-7b adds the two designed *aggregate* surfaces. **netd `/net/summary` (`P_SUMMARY = 18`)**: a read-only (`0444` SYSTEM, `QTFILE`) `/net` node whose read renders `Net::render_summary()` — the interface view + the three per-protocol stats (`file_content` reused) + a `conn` table (one line/live `Slot`, reusing `slot_endpoint`/`state_str`). It renders into a **`Vec`** (a multi-connection table exceeds the 256-byte `Content` cap), so `h_read` offset-slices it like the `cs`/`dns` reads, and `h_getattr` reports `render_summary().len()`. Visibility-not-authority, per-territory by construction (it's *in* `/net`). **`netstat` (`usr/coreutils/src/bin/netstat.rs`, native libthyla-rs, sibling to `ipconfig`)**: the Plan 9 `netstat` as a thin walk of `/net` — `cat`s ipifc + the three `stats`, then `read_dir`s each `/net/<proto>` and reads each live numbered connection's `local`/`remote`/`status` into a table. The one-shot aggregate as a *client* walk, independent of `/net/summary`, reflecting exactly the reader's view. Composes I-1 + the existing read-only introspection. The focused audit + SMP gate are net-7d. Reference `docs/reference/121-netd.md` (the net-7b observability section) + NET-DESIGN §11. The populated-connection-table live round-trip is owed to net-8 (the in-guest peer). | **933/933** + `joey: net-7b PROBE OK (netstat walk + /net/summary rollup)` (spawns `/bin/netstat` — a clean exit proves the readdir walk over the live `/net` — then reads `/net/summary` and asserts the DHCP addr `10.0.2.15` + `opened` render) + all net probes unregressed + boot OK + 0 EXTINCTION. **SMP gate deferred to net-7d** (pure userspace, kernel byte-unchanged) |
| `de1a0aa` | **net-7c-1: native rustls TLS feasibility (PROVEN end-to-end)** (NET-DESIGN §9.1; **pure userspace; kernel byte-unchanged; no new dependency on the kernel; no new §28 invariant**). The voted-risk feasibility spike — and it did NOT balloon. NEW `usr/lib/tls` (the native TLS substrate): **rustls 0.23** (`default-features=false`, `custom-provider`) + **`rustls-rustcrypto`** (the pure-Rust `CryptoProvider`: aes-gcm/chacha20poly1305/p256+p384/rsa/x25519/sha2; `default-features=false` to drop its `std`) + **`getrandom` 0.2 `custom`** (`register_custom_getrandom!` → `SYS_GETRANDOM`, the kernel CSPRNG, for the client random + the ECDHE key share) + a **`TimeProvider`** reading LS-K `CLOCK_REALTIME` (no_std rustls has no clock; webpki needs the wall time for cert validity — the structural reason TLS follows net-7a). `client_config(roots)` + `client_hello(host)` (drives the no_std `UnbufferedClientConnection` to emit a real ClientHello). NEW `usr/tls-smoke` (`/bin/tls-smoke`): the in-guest **runtime** prover — builds the config + emits a ClientHello + asserts a valid handshake record (`0x16`, len>16). Spawned by joey **with `CAP_CSPRNG_READ`** (a TLS client legitimately needs the CSPRNG; the spawning authority confers it, as joey does for corvus — a real conferral requirement surfaced by the spike). Proven: **compile + link + size + runtime** all on `aarch64-unknown-none`, in-guest (the RustCrypto provider + the kernel CSPRNG + the TLS record layer all execute). Two as-built findings: **(1)** v1.0 native TLS is **TLS 1.3-only** — the 1.3 smoke is ~1,006,400 B (fits `SYS_SPAWN_BLOB_MAX`=1 MiB); +TLS 1.2/RSA-kx is ~1,067,432 B (60 KiB OVER) — a sound modern posture (pouch/curl still does 1.2). **(2)** the net-7c-2 https tool will exceed 1 MiB → the recorded size strategy is to **strip the TLS binaries** (no userspace tool consumes their symbols; keep an unstripped copy under `build/` — pure build config, no kernel change; a `SYS_SPAWN_BLOB_MAX` bump is the fallback). net-7c-2: the `TlsStream` transport over `/net` + the PEM root loader (the baked `/etc/ssl/certs/ca-certificates.crt`) + the `https` GET tool + the deterministic loopback handshake E2E. Reference `docs/reference/123-tls.md` + NET-DESIGN §9.1. | **933/933** + `tls-smoke: ClientHello OK` + `joey: net-7c PROBE OK (native rustls ClientHello: RustCrypto provider + CSPRNG + record layer on aarch64)` + all net probes unregressed + boot OK + 0 EXTINCTION (TCG). **SMP gate deferred to net-7d** (pure userspace, kernel byte-unchanged) |
| `92717fa` | **net-7c-2: the full native https client + the loopback E2E + the CA bundle (size strategy: STRIP)** (NET-DESIGN §9.2; **pure userspace; kernel byte-unchanged; no new dependency on the kernel; no new §28 invariant**). The net-7c-1 substrate gains the real transport. **`tls` crate**: `TlsStream<S: Read+Write>` drives the unbuffered rustls state machine over a `/net` `TcpStream` (a shared `drive_state<D>` handles every connection state; only the per-role `process_tls_records` differs — the `tlsconn_role!` macro; the incoming-buffer trim is `min(discard,len)`-clamped against a panic=abort self-DoS on a hostile-server path) + `load_roots_pem` (the baked bundle → `RootCertStore`) + `server_config_single_cert` + `client_config_untrusting` (the negative-test config) + `loopback_roundtrip` (a full client↔server handshake + app-data exchange in memory). **NEW `usr/https` (`/bin/https`)**: the native curl-over-pouch analog — `https`/`--selftest` (the deterministic gate: read+parse the baked bundle into a non-empty trust store + build a config) + `https <host> [path]` (the best-effort live GET: resolve via `/net/cs`, handshake over `TlsStream`, print the status line; shell-runnable, NOT a boot gate). **tls-smoke** now also runs the **loopback E2E** (full handshake + REAL WebPki cert verify vs a baked self-signed test root + the wall clock + the SAN + an app-data round-trip) AND the **negative test** (an untrusted-roots client must REJECT the handshake — proving the verifier is not permissive, the cert-validation regression a real client must never have). **Build wiring**: the CA bundle (a real 193-root Mozilla set, `usr/https/ca-certificates.crt`) baked into the pool at `/etc/ssl/certs/ca-certificates.crt` (the host-bake idiom); the TLS bins **stripped** (`llvm-strip`, ~1.1 MiB → ~520 KiB — they exceed `SYS_SPAWN_BLOB_MAX` unstripped) into the cpio root, unstripped kept under `build/` (the v1.0 expedient; REVENANT #231 retires the cap post-net). Composes I-1/I-5/I-23/I-28. Reference `docs/reference/123-tls.md` (the net-7c-2 sections) + NET-DESIGN §9.2. The live-socket `TlsStream` in-guest E2E (a netd loopback) is owed to net-8; the focused audit is net-7d. | **933/933** + `tls-smoke: ClientHello OK` + `tls-smoke: loopback E2E PASS` + `tls-smoke: untrusted-cert REJECTED (verifier validates the chain)` + `joey: net-7c PROBE OK (… + untrusted-cert rejected)` + `https: SELFTEST OK (baked CA bundle: 193 trust anchors …)` + `joey: net-7c-2 PROBE OK` + all net probes unregressed + boot OK + 0 EXTINCTION (TCG). **SMP gate deferred to net-7d** (pure userspace, kernel byte-unchanged) |
| `a8a7be1` | **net-8a: the resident loopback interface in netd** (NET-DESIGN §16/§17; **pure userspace; kernel byte-unchanged; no new dependency; no new §28 invariant** — composes I-1/I-5/I-23/I-28). netd now carries a real `lo` stack: a second isolated smoltcp `Interface` + `Loopback` device + `SocketSet` addressed `127.0.0.1/8`, polled alongside the NIC each `Net::poll` tick, so an in-guest client dialing `127.x` reaches an in-guest server **over the real `/net` 9P path** — the in-guest peer mechanism the owed E2Es need (the NIC-only live stack could not give a deterministic peer; slirp does not loop a guest-to-own-IP packet). A loopback socket cannot share the NIC iface+set (the NIC default route steals `127.x` egress — the net-3d "isolation is load-bearing" finding), so it is a SECOND stack. `Net.lo: Option<LoStack>` is **opt-in** (`enable_loopback`, at bring-up): the resident netd opts in; the isolated single-stack E2E selftests leave it `None` (they pass a loopback iface as the PRIMARY stack), so `set_ref`/`set_mut` route is **behavior-identical** for them and the audited E2E paths are untouched. A `Slot` gains `lo: bool`; a dial/announce to an explicit `127.x` calls `ensure_lo_stack` (drops the fresh NIC-set socket, mints an equivalent one on the lo set — the smoltcp `Socket` enum is not re-`add`able — flips `slot.lo`); EVERY socket access routes through `set_ref(n)`/`set_mut(n)` (a handle is set-specific). `accept_swap` mints M on the listener's stack; `tcp_connect` uses the lo iface `context()` for a migrated slot. `*`-announce does NOT span loopback at v1.0 (a v1.x seam). Boot-asserted `resident_lo_selftest` (a NON-loopback primary 10.0.0.1/24, so a `127.x` round-trip can ONLY succeed via migration → proves `ensure_lo_stack` + the dual-poll + clean teardown/no-leak). The in-guest cross-Proc over-`/net` round-trip is net-8b. Reference `docs/reference/121-netd.md` (the resident loopback section) + NET-DESIGN §16. | **933/933** + `netd: net-8a resident lo E2E PASS (127.x migrate + accept + data + no-leak)` + the net-3d/6a/6a-3/6b/4d loopback E2Es **unregressed** (PASS) + boot OK + 0 EXTINCTION (TCG) + netd fmt clean. **SMP gate deferred to net-8d** (the arc close; pure userspace, kernel byte-unchanged) |
| `e90e645` | **net-8b: the in-guest over-`/net` TCP echo round-trip + the #239 fix** (NET-DESIGN §16; **pure userspace; kernel byte-unchanged; no new dependency; no new §28 invariant** — composes I-1/I-5/I-23/I-28). `usr/net-echo`'s `loopback_e2e` (the joey boot probe) dials `127.0.0.1` and **accepts + echoes over the real `/net` mount** on the net-8a resident loopback — the FIRST live exercise of the full Plan 9 dial+accept+data path end-to-end (net-3a deferred accept + net-6a-1 blocking read + the net-8a lo stack), as opposed to the net-3* `echo_e2e` proofs that drive netd's methods DIRECTLY on an isolated stack. Peer-independent: one Proc is both ends (the blocking accept/recv DEFER inside netd — a separate Proc whose serve loop keeps polling both ends of the loopback — so this Proc blocks in the kernel while netd makes progress; no self-deadlock). **Surfaced + fixed #239**: netd's `h_getattr` served the `listen` file `FILE_RO` (0444) — but the accept opens it **ORDWR** (its fid is REBOUND onto the accepted connection's `ctl`, which `shutdown` writes), so the kernel's **A-3 dev9p `perm_check` denied the W and failed the open BEFORE the Tlopen ever reached `h_lopen`**. Latent because the deferred accept had only ever been driven by `echo_e2e`'s direct methods (which bypass `perm_check`) + net-3a's gate test opens a *non-announced* conn (rejected at `h_lopen` *after* the open succeeds) — only a live over-the-mount accept hit it. Fix: `FK_CTL | FK_DATA | FK_LISTEN => FILE_RW` (one line + WHY). Ground-truthed via a deterministic in-guest bind→connect→accept probe + netd slot/fid instrumentation (the prior session's "freed+reminted" theory was DISPROVEN — the listener slot stays alive; it's a perm mismatch). Reference `docs/reference/121-netd.md` (the net-3a `listen`-FILE_RW note) + NET-DESIGN §16. | **933/933** + `net-echo: net-8b loopback E2E PASS (bind+connect+accept+echo over /net)` + `net-echo: PROBE OK` (net-6a-3 unregressed) + all net probes unregressed + boot OK + 0 EXTINCTION (TCG). **SMP gate deferred to net-8d** (the arc close; pure userspace, kernel byte-unchanged) |
| *(pending)* | **net-8c-1: the soak / leak-baseline** (NET-DESIGN §16; **pure userspace; kernel byte-unchanged; no new dependency; no new §28 invariant**). `usr/net-echo`'s `soak_e2e` runs the net-8b live over-`/net` echo round-trip **8x** and requires netd's live TCP `active` count (`/net/tcp/stats`) to return to its pre-soak baseline -- a per-cycle leak of even one slot (each cycle opens + frees a listener + connector + accepted M = 3 conns; 24 mints total) would grow it. This is the §16 no-leak exit criterion realized in-guest over the real mount (the slot-lifetime / clunk-frees-the-slot discipline -- I-10/I-11 -- proven under repeated live use, not just the single net-8b round-trip). The soak gates the boot. (Build lesson re-confirmed: `tools/build.sh` swallows a userspace Rust compile error + ships the STALE binary, so a missing `net-8c-1` boot line meant a stale net-echo from a removed `use ...::io::Read` -- always verify the userspace compile is clean before trusting a boot.) | **933/933** + `net-echo: net-8c-1 soak PASS (8x echo over /net; tcp active returns to baseline)` + the net-8b E2E + net-echo PROBE OK + all net probes unregressed + boot OK + 0 EXTINCTION (TCG). **SMP gate deferred to net-8d** (the arc close; pure userspace, kernel byte-unchanged) |
| `fceb7b9` | **net-7d: the net-7 focused audit + SMP gate + CLOSE** (the focused round over the WHOLE net-7 arc; CLAUDE.md/ARCH §25.4 audit-trigger rows marked CLOSED). Opus-4.8-max prosecutor (MODEL start==end, no Fable, no fallback) + a CONCURRENT main-loop self-audit -> **CONVERGED**: **0 P0 / 0 P1 / 1 P2 / 4 P3, CLEAN close** (the F1 fix is a 2-line clamp — not structurally invasive). **F1 [P2]** the TLS incoming-buffer drain was unclamped at `write_app`/`queue_close` (vs `pump`'s `min(discard,len)` clamp) — the same hostile-server panic=abort self-DoS class the project already defends → FIXED uniformly at both sites. **F3 [P3]** `test_clock_settime_cap_gate` could leak a flipped `p->caps` on an early assert-abort (`TEST_ASSERT` returns) → FIXED (capture-then-restore-then-assert). **F2/F4 [P3]** doc'd (the `/net/summary` paginated re-render coherency in 121-netd.md; the SNTP-first bootstrap on RTC-less hardware in 123-tls.md). **F5 [P3]** the https `RESP_CAP` one-buffer overshoot closed-with-justification (cosmetic, shell-only). The prosecutor's verified-guarded set matched the self-audit's SOUND set exactly (the clock-set gating, the single-`u64` SMP re-anchor, the genuinely non-permissive WebPki verifier + the real negative test, the isolated test root, no boot-hang, the surgical strip + readback-verified bake). `memory/audit_net7_closed_list.md`. **THE net-7 ARC (TLS + SNTP/clock-set + observability) IS AUDITED + GATED.** | **933/933** (default TCG, post-fix) + both clock tests pass (incl. the F3-restructured `cap_gate`) + every net-7 probe OK (the untrusted-cert-REJECTED proof + the elevated `SYS_CLOCK_SETTIME round-trip`) + 0 EXTINCTION + full login E2E. **SMP gate PASS** — 0 corruption across 40 boots (default+UBSan × smp4/smp8 N=10); the 18 timing-classified boots ALL ground-truthed to the healthy end-state (the benign `stalk.lifetime` soft-warn under HVF host contention, NOT a guest fault) |

## Remaining work

### virtio-PCI transport sub-arc (pci-0..pci-3) — the #140 resolution

net-1 surfaced **#140**: QEMU packs 8 virtio-mmio slots per 4 KiB page, so net
(slot 30) + the Stratum-pool blk (slot 31) share page `0x0a003000`; the
page-exclusive `KObj_MMIO` claim over a hard 4 KiB MMU granule cannot give two
*persistent* userspace drivers (`netd` + `stratumd`) sound, isolated
co-residency (net-1 dodged it by running + exiting pre-stratumd). Per the
depth-first-dependencies principle (user-voted 2026-06-15), net-2 is preempted
to build the future-proof virtio-PCI transport (`docs/VIRTIO-PCI-DESIGN.md`):
net moves to its own page-aligned PCI BAR, dissolving the contention by
construction. net-only scope; INTx interrupts; blk→PCI + MSI-X are v1.x seams.

- **pci-0** the transport scripture (this doc + ARCH §13.2/§25.4/I-5 +
  NET-DESIGN §17 + ROADMAP). **LANDED.**
- **pci-1a** DTB/config primitives: `dtb_pci_intx_route` + `dtb_pci_mem_window`
  + `dtb_get_compat_prop` + `virtio_pci_cfg_write*`. **LANDED** (883/883;
  SMP-inert).
- **pci-1b** kernel `KObj_PCI` (exclusive per-function claim, non-transferable)
  + BAR assignment + `VIRTIO_PCI_CAP_*` resolve + the DTB `interrupt-map` INTx
  swizzle. **Audit-bearing** (the claim table is the first shared state).
  **LANDED** (kobj-only; the 3 syscalls split out to pci-1c). 889/889 + boot OK.
- **pci-1c** the 3 syscalls: `SYS_PCI_CLAIM=76` / `MAP_BAR=77` / `INFO=78` +
  `struct t_pci_info` ABI + libt/libthyla-rs wrappers + syscall tests.
  **LANDED** (891/891; the syscall envelope over the pci-1b kobj). The success
  copy-out (SYS_PCI_INFO) + the BAR burrow-map (SYS_PCI_MAP_BAR) need a user
  buffer + address space, so they are proven by the pci-2 userspace probe; the
  in-kernel tests cover the cap gate / mint / rights / reject paths.
- **pci-2** userspace: `libthyla-rs::hardware::PciDev` + virtio-pci-modern +
  port netdev's transport half (reuse `ring`); `netdev-pci-test` over PCI;
  `run-vm.sh` adds `virtio-net-pci`. **LANDED** (`VirtioNetPci` PASS 24/24
  alongside the kept MMIO `netdev-test`; host ring 7/7 + 891/891 + boot OK).
- **pci-3** focused audit + SMP gate + the kernel reference docs. **LANDED**
  (CLEAN 0/0/0/4; Opus 4.8 max + self-audit). The §7 surfaces all verified sound
  (KObj_PCI lifecycle / I-5 / exclusive-claim / BAR-assign / config-mediation /
  cap-walk / INTx); the 4 P3s: F1 partial-map leak tracked (no v1.0 detach path —
  proc-exit-bounded by design), F2 notify-doorbell bound fixed, F3 ABI asserts
  completed, F4 `pci.walk_caps_hostile` test added. New `115-pci-claim.md` +
  `37-virtio_pci.md` cross-reference. **The pci sub-arc is COMPLETE.**

### the Menagerie build arc (preempts net-2; authoritative status: `docs/MENAGERIE.md` §18)

After pci-3, the user voted (2026-06-15) to **preempt net-2 with the Menagerie
driver framework** (`docs/MENAGERIE.md`), so `netd` is born discovery-driven +
allowance-narrowed rather than holding coarse `CAP_HW_CREATE` + a hardcoded base
to be retrofitted later. The full per-chunk status (SHAs, tests, audits) lives in
**MENAGERIE.md §18**; the compact ledger:

- **devhw-1/2** (`4d3950d`/`85921fe`) — the DTB tree-walk publish Dev + `/hw`
  mounted in the boot namespace. **LANDED** (902/902; ref `116-devhw.md`).
- **the hardware allowance / I-34** (`1602e37` spec + `ad814b0` impl + `30f062c`
  audit) — per-Proc scoping of `CAP_HW_CREATE` at the 3 create gates; **model-first**
  (`specs/allowance.tla` clean + 4 buggy cfgs); audit CLEAN 0/1/1/2. **LANDED**
  (913/913; ref `117-allowance.md`).
- **the Loom device-gone terminal CQE** (`6db71fa` spec + `ad74e39` impl +
  `d5d52ca` audit) — §10 surprise-removal; `T_E_NODEV` async terminal; audit CLEAN
  0/0/0/4. **LANDED** (915/915).
- **5a** confer-at-spawn allowance ABI + #160 revoke-on-terminate fold-in
  (`fea1e7d`) — the I-34 grant path wired to userspace (`Command::allowance` over
  `SYS_SPAWN_FULL_ARGV`); audit CLEAN 0/1/1/2 (the #160 fold-in SMP-UAF F1 fixed).
  **LANDED** (917/917).
- **5b** the `libdriver` framework crate (`usr/lib/libdriver`) — the §6 manifest
  schema + parser, the `resolve` node-INTERSECT-needs grant (the auditable I-34
  property, host-tested), the single-argv descriptor codec, and the `Driver` trait
  + `run` scaffold + handle-mint helpers + `to_allowance`. Pure userspace, no kernel
  ABI; audit-light (the kernel validates every `SYS_*_CREATE`). **LANDED** (19 host
  tests + device build + whole-workspace build + clippy-clean; ref `118-libdriver.md`).
- **5c** the warden (`usr/warden`) + the first live `impl Driver`
  (`usr/menagerie-probe`) + `libdriver::dtb` (the `/hw` property decode, +8 host
  tests → 27). The full bind-loop — discover `/hw` (45 nodes) → match the pl061
  GPIO bind DB → `resolve` the grant → spawn `/menagerie-probe` with the
  descriptor + narrowed allowance + `CAP_HW_CREATE` → the driver maps its granted
  MMIO (allowance permits) + an out-of-grant create is rejected (I-34 enforced) →
  reap → `joey: warden ok`. Pure userspace, no kernel ABI; audit-light. **LANDED**
  (27 host tests + boot-probe E2E `1 bound, 1 up` + 0 EXTINCTION + the SMP gate;
  refs `119-warden.md` (new) + `118-libdriver.md`). Bring-up find: a boot-probe
  Proc has no stdio fds → the warden gives each driver `/dev/null` for the 3 slots
  (Command's default `Stdio::Inherit` bumps the parent's fd 0/1/2, which fails
  pre-resolution).
- **5d** the **discovery-source layer** — user-directed (2026-06-16) "pull it
  forward without hesitation": the MENAGERIE §3 proper architecture, where the
  warden binds by typed IDENTITY and **never reads a device register**; a sandboxed
  bus-enumerator Proc does the DeviceID-poke (the type-blind DTB cannot tell which
  of ~32 identical `virtio,mmio` slots holds the NIC). Kernel BYTE-UNCHANGED.
  **5d-0** (`7d9d6f5`) scripture resequence (no code). **5d-1** (`8468c24`)
  `libdriver::source` — typed `DeviceId`/`DeviceNode` + the source→warden
  node-record codec (strict + bounded = the trust boundary) +
  `DiscoverySource`/`best_match` + `DtbSource`. **5d-2** (`4b3bd07`+`8b7c686`)
  `usr/virtio-mmio-source` — a separate Proc granted ONLY the bank; reads each
  slot's DeviceID, pipes typed `virtio:<id>` records to the warden. **5d-3**
  (`1264673`) netdev → grant-driven `open_slot` (retires `virtio.rs:51` + the
  bank-probe) + `usr/netdev-driver` (`impl Driver`, the 24-ARP proof) bound by
  `virtio:1`; retired `netdev-test`. Self-found at 5d-3: a sub-page slot grant is
  unmappable (MMIO is page-granular) → `to_allowance` page-rounds each window.
  **5d-4** (`1535ad8`) the **composed focused audit close** (Opus-4.8-max prosecutor
  + concurrent self-audit, independently converged): **0 P0 / 1 P1 / 2 P2 / 2 P3,
  all fixed/dispositioned**. F1 [P1] the warden conferred a source's reported
  reg/INTID unchecked against the granted bank (a non-TCB source could inflate a
  peer driver's allowance) → FIXED by `reconcile_reported_node` (the source supplies
  IDENTITY, the warden supplies RESOURCES from its own trusted DTB view — enforced,
  not trusted); F2/F3 [P2] unbounded id-list + `read_to_end` DoS → bounded
  (`MAX_IDS` + `slurp_capped`); F5 [P3] source slot-read extent gate; F4 [P3] the
  page-round co-residency over-grant is the documented #140/net-2 I-34-deviation (no
  code). 41 libdriver host tests + boot `2 bound, 2 up` + `netdev-driver: PASS
  24/24` + 0 EXTINCTION + the SMP gate. **LANDED** (`memory/audit_5d_closed_list.md`;
  refs `118-libdriver.md` + `119-warden.md`).
- **5e** `DeviceRemoved` revoke+terminate + supervision — the driver-lifecycle
  half of the Menagerie spine. **5e-1** (`8874ec5`) **long-lived serve +
  DeviceRemoved teardown**: `netdev-driver` goes long-lived (24-ARP proof →
  `READY` → quiesce → block in `Irq::wait` until removed, never self-exiting);
  the warden's `bind_and_run` reads a readiness line via `await_readiness` (a
  `try_wait` + bounded poll — a libdriver driver's pipe does NOT EOF on exit
  because a single-thread Proc's `SYS_EXIT_GROUP` defers the handle-close to
  reap, the #926 asymmetry), then group-terminates a long-lived service via
  `Child::kill` → `/proc/<pid>/ctl` `killgrp` (revoke FIRST, atomic #160). New:
  `libthyla-rs Child::kill`; `VirtioNet::quiesce` made `pub` (explicit
  quiesce-before-block keeps the forced teardown DMA-safe). Boot: `netdev-driver`
  up → `PASS 24/24` → `serving` → warden `DeviceRemoved` → `torn down` → `2
  bound, 2 up` → boot OK + 0 EXTINCTION; stratumd claims the freed (page-shared)
  virtio-blk slot post-pivot. Device build + clippy clean + the SMP gate. The
  live-DMA-at-forced-teardown is the documented MENAGERIE section-10 hazard (an
  IOMMU / cooperative-quiesce fix owed net-2/real-hw). Ref `119-warden.md`.
  **LANDED**. **5e-2** (`ac400df`) **bounded restart-on-crash supervision**:
  the warden's `bind_and_run` is refactored into `run_once` (confer + one attempt
  → a `RunOutcome`) + `supervise` (the restart loop) driving the new PURE,
  host-tested `libdriver::supervise::next_step` (restart-vs-settle per the
  manifest `restart` policy, exponential back-off 50/100/200ms capped at 500,
  give-up after `RESTART_LIMIT`=3). Accounting splits three ways: **Up** /
  **GaveUp** (crashed + restarts exhausted — SOFT, does NOT fail the boot) /
  **Failed** (structural — HARD, exit 1). Proven by a new `crash-probe` driver
  (binds the undriven `virtio:16` GPU id; `probe` always `Err`s BEFORE any HW
  claim → its page-rounded allowance is never a live claim, benign vs netdev's
  shared-page bind). Boot: `crash-probe` restarts 3× (50/100/200ms) → gives up →
  `3 bound, 2 up, 1 gave up, 0 failed` → netdev still up → boot OK + 0 EXTINCTION.
  10 `supervise` host tests + device/clippy clean + the SMP gate. v1.0 SEAM: the
  kernel collapses non-"ok" exits to 1 (`sys_exits_handler`), so the supervisor
  reads clean-vs-crashed only, not specific exit codes (the finer policy lands
  with the structured 64-bit exit_status, docs/ERRORS.md). Refs `118`/`119`.
  **LANDED**. **5e-3** (`0d486c2`) **device-gone `-ENODEV` CQE proven over the
  production SrvConn transport**: a kernel test
  (`9p_srvconn_transport.devgone_posts_nodev_cqe`) stands up a real byte-mode
  `SrvConn` pair, handshakes a 9P client over the production
  `p9_srvconn_transport`, leaves a `Tfsync` in flight, then calls the actual
  `srvconn_teardown` (the path a `DeviceRemoved` group-terminate drives) and
  pumps the reader — proving the real `SrvConn` returns recv-0 on teardown (not
  recv-`-1`), so the device-gone reason reaches a real Loom CQ as `-ENODEV`. Its
  companion `transport_err_posts_eio_cqe` arms a past recv deadline on the same
  setup so the recv returns `-1` → `-EIO`, pinning the distinction over the wire
  a driver's Loom rides (neither passes unless the classifier discriminates).
  Step-4 (#162-165) landed the production path + the loopback proof (`force_eof`
  *synthesizes* the recv-0); 5e-3 closes the gap end-to-end over the production
  transport with no production code change (`canonical_responder` is shared —
  un-static'd — to pre-stage the handshake replies). Tests 918 → 920/920; boot
  OK + 0 EXTINCTION; the SMP gate. Ref `107-loom.md` (device-gone). **LANDED**.
  **5e-4** (`367c2c8`) **composed focused audit over the whole 5e arc + close**:
  one Opus-4.8-max prosecutor (MODEL start==end, no fallback) + a concurrent
  self-audit, **0 P0 / 2 P1 / 0 P2 / 1 P3, all fixed; NOT a dirty close**.
  **F1 [P1]** (both passes, independent) — `read_ready_line` read the driver
  readiness pipe ONE BYTE AT A TIME with *blocking* reads, so a non-TCB driver
  that wrote a partial line (`"READ"`, no newline) and held would stall the
  warden FOREVER, escaping the give-up budget — a boot-availability DoS on the
  TCB warden (the untrusted-driver model the framework exists to sandbox). Fixed:
  the line assembly moved to a pure host-tested `libdriver::readyline::feed_ready_line`
  (accumulator + scan, capped at `READY_LINE_MAX`); the warden does ONE bounded
  read per poll-readable event (returns the available bytes without blocking),
  feeding chunks across the poll loop. **F2 [P1]** (prosecutor) — DeviceRemoved
  did not fully revoke a driver that spawned an allowance-bearing **child Proc**:
  `proc_group_terminate` is thread-group-scoped, so a grandchild inheriting a
  clone of the narrowed allowance (`revoked==0`) survives reparented to init with
  live MMIO/IRQ/DMA the warden never tracks (I-34 "fully revoked" violated under
  an untrusted *spawning* driver). The fork was already **resolved in scripture**
  (MENAGERIE §13.2 "drivers are sources, not spawners; one auditable chokepoint"),
  so the fix *implements* it fail-closed: `rfork_internal` denies a child Proc to
  a hardware-allowance-narrowed parent (closes both the confer + clone-inherit
  paths; does not block step-6 recursive sources, which *report* to the warden).
  Regression `allowance.narrowed_proc_cannot_spawn`. **F3 [P3]** — documented the
  readiness contract (a driver's first stdout line MUST be exactly "READY";
  diagnostics → the console). libdriver host tests 51 → **61** (+10 readyline);
  kernel suite 920 → **921**; boot OK (`warden: 3 bound, 2 up, 1 gave up, 0
  failed`) + 0 EXTINCTION; SMP gate 0 corruption. Refs `117-allowance.md`
  ("Drivers are leaves"), `118-libdriver.md` (readyline), `119-warden.md` (F1+F3),
  `memory/audit_5e_closed_list.md`. **LANDED**. THE 5e ARC IS COMPLETE. → step 6
  (PCIe/USB sources + the per-(bus,dev,fn) PCI allowance axis #159).

### the net arc (resumes on the PCI NIC, after the Menagerie spine)

- **net-2 (LANDED, all sub-chunks)** netd: embed smoltcp, serve `/net`, the
  `/net/tcp` clone/connect/data client path + the fid state machine (NET-DESIGN
  §3.4). **net-2a** smoltcp + the `phy::Device` + DHCP-lease proof; **net-2b**
  the persistent 9P `/net` server + the joey mount; **net-2c** the `/net/tcp`
  clone/connect/data fid state machine (live TCP); **net-2d** the focused audit
  (Opus-4.8-max prosecutor + self-audit, CLEAN 0/0/1/4) + the ARCH §25.4 /
  CLAUDE.md enumeration + close. **net-2 is DONE.**
- **net-3** listen/accept + udp + icmp (ping). **DONE.**
- **net-4a** `/net/cs` + the compiled-in ndb. **DONE.** **net-4b** `/net/dns` +
  cs→dns. **DONE.** **net-4c** `/net/ipifc` + `/net/ndb` + ipconfig +
  DHCP-into-config. **DONE.** **net-4d** the focused net-4 audit + close (0/0/1/3,
  NOT dirty; F1 deferred-read guard + the loopback DNS E2E + the parser battery).
  **DONE — the net-4 arc is COMPLETE.** **net-5** socket-compat pouch patch.
- **net-6** dev9p.poll + reserved `net_poll.tla` + Loom-multishot accept.
- **net-7** TLS root bundle + SNTP + `SYS_CLOCK_SETTIME` + observability.
- **net-8** server + soak exit criteria + one focused audit over the arc.

## Ground truth (the DTB + the live device, read 2026-06-15)

The DTB dump (`qemu-system-aarch64 -machine virt,... ,dumpdtb` + `dtc`) of the
`pcie` node, reconciled with the **actual** in-boot enumeration (the kobj is
DTB-derived at runtime, so it is config-agnostic):

- **ECAM** PA varies by machine: `0x3f00_0000` (16 MiB) on the **default
  `gic-version=2`** machine that `tools/test.sh` boots; `0x40_1000_0000`
  (256 MiB, highmem) on the `gic-version=3` variant my pci-1a manual dump used.
  The kobj never hardcodes it — `kernel/virtio_pci.c` maps it from the DTB.
- **BAR window** PA `0x1000_0000`, ~768 MiB (32-bit, page-aligned, not reserved
  → a per-device BAR is page-aligned + userspace-claimable). Stable across both
  machines (`dtb.pci_mem_window` asserts the base).
- **INTx** swizzle `SPI = 3 + ((slot + pin − 1) mod 4)` → GIC INTID **35–38**.
  A single PCI device (net-only) lands on **one unshared INTID** → no irqfwd
  multi-waiter needed. The live rng-pci at bdf 0:1.0 routes to INTID 36.
- **MSI** present in HW but **undriven** → INTx is the v1.0 path; MSI-X v1.x.
- **BARs start unassigned** (no UEFI) → the kernel assigns them from the BAR
  window (pci-1b).
- **The live test device (rng-pci) is TRANSITIONAL**: it presents the *legacy*
  device_id `0x1005` (`is_modern=0`) but carries the *full* modern interface —
  a 32-bit legacy MMIO BAR1 (4 KiB) **and** a 64-bit modern BAR4 (16 KiB) with
  all four `VIRTIO_PCI_CAP_*` structures (common/notify/isr/device) packed into
  BAR4. So `pci.claim_rng` exercises BOTH BAR widths + the full cap-walk. **For
  pci-2/net-2**: prefer `virtio-net-pci,disable-legacy=on` for a clean
  modern-only NIC (device_id `0x1041`, no I/O BAR) — the kobj handles either.

## Exit criteria (refined per W4-F8; see NET-DESIGN §16)

- [ ] Client: `ping`, `curl`/`wget` (TLS), `ssh`-client, a native `TcpStream`.
- [ ] Server: native `TcpListener` echo (≥2 conns) + the Loom-multishot accept
      loop + a ported `listen`/`accept` server.
- [ ] Soak: N conns × M s, no fd/connection/Burrow leak, under the SMP gate.
- [ ] No Utopia regressions; no P0/P1 on the reserved net audit surfaces.

## Build + verify

```bash
tools/build.sh kernel              # kernel + userspace + ramfs
tools/test.sh                      # boot + netdev-test PASS + boot OK
cargo test -p netdev --no-default-features --target aarch64-apple-darwin   # host ring tests
tools/ci-smp-gate.sh               # SMP soundness gate
```

## Trip hazards

- A virtio driver Proc needs `CAP_HW_CREATE` (spawn via `t_spawn_with_caps` /
  `rfork_with_caps`); plain `t_spawn` → `SYS_MMIO_CREATE` rejected at the cap
  gate (net-1's root-cause).
- netdev-test runs PRE-stratumd in the joey ladder (the net page must be free).
- `THYLACINE_NO_QMP=1` makes the virtio-input probe fail (#34, unrelated).

## References

- `docs/NET-DESIGN.md` (the #68 charter), `docs/reference/114-netdev.md`.
- ARCH §10.1 (network is 9P), §28 (no new net invariant), ROADMAP §9.
