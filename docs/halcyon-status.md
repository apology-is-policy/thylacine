# Halcyon phase status (ROADMAP §11; the H-arc)

The authoritative pickup guide for the Halcyon phase (ROADMAP §11.1's H-0..H-7
table is the plan; this doc tracks what has LANDED). Named `halcyon-status.md`
rather than `phase8-status.md` because that filename is already the network-era
tracker (the ROADMAP §9 phase) — the H-arc is arc-named the way its chunks are.

## TL;DR

**H-1 through H-3 are CLOSED; H-4 (layouts) is open.** H-1 (Beacon: the
fd-class syscall + the beacon crate + the tier plumbing + the emitters,
audit-closed), H-2 (the transcript MVP on the CPU floor, audit-closed), and
the whole H-3 chrome arc -- H-3a (Daylight chrome), H-3b (the executable
per-leaf tag bar), H-3c (THE GATE: obj/verbs/menus), H-3c-2 (the event set:
one 9P session + one Loom ring per client), H-3d (the status bar) -- each
audit-closed. **H-4 layouts**: the D decision is ratified (a user-authority
session tool + a tapestryd `Session(principal)` actor keyed on the
kernel-stamped principal + a one-shot claim token; scripture `e233d390`); the
format core landed (`cdce7f3f`); H-4a-2 landed (`30551d8f`, the `halcyon layout
save` tool + `libhalcyon::layout::from_render_text`); **H-4b-1 landed** -- the
one-shot placement claim in tapestryd (`pane/<id>/claim` + `create claim=`),
plus the entropy it needed: `caps = ["csprng"]` on the warden manifest (a new
manifest field; joey -> warden -> driver, I-2 monotone). H-4b splits into three
(the fold recommended pre-compaction was refuted by ground truth). **H-4b-2
landed** -- the `Session(principal)` actor: a third tapestryd identity keyed on
the kernel PRINCIPAL (not the per-process `stripes`), so same-user peers gain
rio-style mutual pane authority (SYSTEM/unauth/unknown stay per-process
Client(stripes); the console + other users stay protected). Surfaces + empty
leaves carry `owner_principal` (stamped at split); the claim mint narrows from
H-4b-1's interim "any peer" to owner-only; a session may NAME an empty leaf it
owns (the tool tags at claim time); a last-conn reap closes a departed
session's empty scaffolding. REGRESSION-SAFE (single-process michael unchanged;
the panes E2E is 33/33 under `Session(michael)` -- the claim leg + the
michael->SYSTEM negative both hold). Also landed: four pure-`cargo fmt`
reformat commits over the tapestry crate family (493 pre-existing hunks;
operator-approved; proven pure). **H-4b-3b LANDED** -- the restore tool: `halcyon
layout restore` rebuilds a saved layout beside the console (skeleton build via
/dev/tapestry split/mode verbs with dump-diff id-binding + claim placement +
spawn-as-user), plus the `pane/<id>/owner` save-side classifier and
`prune_env`. The NEW `ls-gfx-restore` HVF E2E is green and carries H-4b-2's
POSITIVE cross-process mutual-authority witness (the tool focuses a peer
michael-process's tile). **The batched holotype over H-4b-1..3 is CLOSED** (0 P0 / 0 P1 / 0 P2 / 2 P3,
NOT dirty; Opus fallback -- Fable was out of credits): F2 [P3] FIXED (is_env_tile
owner-0 fail-open -> the pure `owner_is_env`), F1 [P3] DOCUMENTED as a v1.x
multi-seat seam (the empty restore-scaffold is grief-able cross-principal until
filled; harmless at v1.0 single-session). The AUDIT-TRIGGERS row + the CLAUDE
index landed. **Next: PUSH the whole H-4b arc** (reformat + H-4b-1..3 + the audit
close) to both mirrors, then H-4c (the gesture + halcyon.rc) / H-4d (the welcome).

## Landed chunks

| Commit | What | Tests |
|---|---|---|
| `9a4db65b` | **H-0a kickoff**: the design conversation → scripture. `docs/HALCYON.md` (the environment: 3 sources / 3 layers, vk rendering, paper-light theme, two pane classes, presentations + menus, layouts) + `docs/BEACON.md` (the markup: OSC 1936 wire, semantic v1 vocabulary, none\|cells\|rich tiers, the emission gate, the security clause) + reconciliation (ROADMAP §11 EVOLVED, VISION §3.3/§9/§14, NOVEL Angle #4, TAPESTRY §14/§17, COREUTILS-design pointer) | n/a (scripture) |
| `fecff135` | **H-0b concretization** (operator: "as much detail and rigor as you can"): BEACON.md §12 (the H-1 build card: wire grammar, op registry, tier mechanism, crate/relocation plan, ut hook sites, P1–P3 test plan) + HALCYON.md §13 (the BOUND architecture: native halcyond + display list + CPU-floor executor + pouch vk executor post-compose; VT-core extraction; transcript model; layout format) + ARCH §23.5.4 + ROADMAP §11.1 (the H-table) + the resequencing (H-2 = CPU floor; H-5 = compose; H-6 = vk executor; guest-lavapipe off the critical path) | n/a (scripture) |
| `7cd1ab94` | **H-1a**: `SYS_FD_DEVCLASS` = 80 (rights=0 fd introspection; the `/dev/cons` leaf normalizes to `'c'` via `devdev_fd_devclass` keyed on `DEV_KIND_CONS`) + the consctl `beacon none\|cells\|rich` verb (winsize discipline verbatim: staged parse, atomic reject, render append ` beacon <tier>`, resets at `cons_drain_close` + `cons_test_reset`; allowed on the renderer-minted CCONSWINSZONLY consctl) + libt/libthyla-rs wrappers + the joey `probe H1` five-arm E2E. Trap banked: the 67-byte render floor's reader set includes the kernel's OWN consctl staging buffer (`devdev.c` `tmp[96]`, was 64 — every consctl read EOF'd; caught by the suite) | 1434/1434 (+`cons.beacon_roundtrip`, +`devdev.fd_devclass`, exact-line renders updated) + boot OK + `joey: probe H1 … OK` |
| `5d638fec` | **H-1b**: the `usr/lib/beacon` crate — `wire` (OSC 1936 emit/parse/strip; caps FRAME_MAX 2048 / VALUE_MAX 1024 / ARGS_MAX 8 / depth 8; foreign-OSC passthrough; the P1 strip identity) + `sink` (Sink zones/em/obj/hdr/rule/mark + Table with padding-outside-frames) + `boxd`/`color`/`palette` relocated VERBATIM from the coreutils crate (git 100%-rename; the 15-test baseline reproduced). As-built deviations recorded in BEACON.md §12.5 | 27 host tests (`cargo test -p beacon`); coreutils lib baseline 4/4 preserved |
| `04186229` | **H-1c-1**: the tier plumbing — aurora writes `beacon cells` at consctl bring-up; ut reads the render line → exports `/env/BEACON` (children inherit via `env_clone_into`) → arms Repl zones iff `rich` AND stdout dc=='c'; the Repl transcript zones (prompt/output brackets + the exit mark; redraws stay in-zone); the u-repl-test beacon leg (rich vs plain discrimination pair; `strip(rich) == plain` asserted in-guest) | 1434/1434 + `u-repl-test: beacon zones OK` in-guest every boot |
| `8922ccd7` | **H-1c-2**: the four emitters — ls (rich short: `obj type=path` per name; rich `-l`: a beacon table, obj name cells; the `--color=auto` flip + `--beacon=WHEN`), grep (`obj path` prefix + `em strong` spans, byte-span wire emission), stat (`obj path` on the subject; the `table` op deferred — recorded), **ps built new** (one atomic `/ctl/procs` read; passthrough/boxed/rich table + `obj pid`); the unification sweep (18 stubs → the real probe; 17 defaults → Auto per COREUTILS-design's ordained end-state); `coreutils::path` + `coreutils::beacon_gate`; the coreutil-smoke Beacon E2E legs (the pipe-budget rule: file-operand subjects + the ps-rich loud-skip guard); BEACON.md §12.5 H-1c-2 deviations + the AUDIT-TRIGGERS.md `SYS_FD_DEVCLASS` row + LS-8 BEACON TIER addendum + the CLAUDE.md index line | suite 1434/1434 + smoke 55/55 (10 Beacon legs) + LS-CI 37/37 |
| `140db874` | **H-1 CLOSE — audit round 1** (Fable 5 start==end, 0 P0 / 1 P1 / 0 P2 / 10 P3, NOT dirty; `memory/audit_h1_closed_list.md`): the P1 = the tier transport dead on arrival (the inherited consctl fd's shared write-advanced offset vs the ≤67-byte line; devdev non-seekable; the attach gate never propagating) → the **ungated read-only `/dev/beacon` leaf** (`DEV_KIND_BEACON`, `cons_render_beacon`; §12.3's no-new-leaf bound revised by its own revisit clause) + ut fresh-opens/exports/prints the `ut: beacon <tier> exported` canary + the ls-3a transcript-grep regression. P3 fixes: drain-close reset reordered (F2), parser-side depth-8 enforcement + test (F3), encoded-length obj guard + test (F4), 12.4 Always-arm amended (F5), OSC 1936 pinned ADOPTED + registry wording aligned (F6), three stale comments (F8), ALL ps smoke legs gated on a direct `/ctl/procs` pre-measure (F9); F7/F10/F11 recorded → H-2. Riders: the winsize+beacon report-leaf fstat pair (0444; the pinned statless test deliberately updated), the exit-mark containment deviation (self-audit; 12.2 amended). | 1435/1435 (+`devdev.beacon_leaf`; `devdev.winsize_leaf` updated) + smoke 55/55 + beacon host 29/29 + canary ×2 in-guest + 0 EXTINCTION + boot OK |
| `05d837a7` | **H-2a**: the shared-VT-core extraction — `usr/aurora/src/vt.rs` → the `usr/lib/vt` crate (git 100%-rename + a 10-line crate header; the four aurora use-sites `crate::vt::` → `vt::`; the name `vt` resolved per the don't-force-it rule, §13.9 slot closed). Behavior-preserving by the §13.4 bar: the FULL 15-scenario `ls-gfx*` family ALL PASS on the extracted build (gl + glquake real legs, no skips; 380 s wall at JOBS=3), against the close-run 37/37 on byte-identical code as the before-leg. The module's 9 parser tests — dormant since birth in the no_std bin crate — are LIVE (`cargo test -p vt --target aarch64-apple-darwin`, 9/9; the beacon pattern). Aurora retains 9 dormant tests (config 3 / render 1 / osd 5) — the vault aurora dossier's subject | vt host 9/9 + ls-gfx* 15/15 |
| `1c053bb6` | **H-2b**: the display list + CPU executor — the `usr/lib/cartoon` crate (the thematic name: a tapestry *cartoon* is the full-size design the weaver executes; rationale in the crate header + HALCYON.md §13.2 as-built). The §13.2 v0 ops verbatim (Clear/Rect/Glyphs/Image/Embed) with runs FLAT (`start`/`count` — pre-shaped for the H-6 wire); the alpha atlas store + `GlyphEntry` table + shelf `AtlasPacker` (page growth never bumps gen; only `regen()` eviction does); the CPU executor (`execute` over `&mut [u32]` + stride + optional `ClipRect`; every write clamped; stale gen / malformed ids skip fail-safe; Image src-over at native size; Embed paints nothing). `blend` copied verbatim from aurora's render.rs WITH its lane-safety lesson comment. Zero deps, no_std + alloc, clippy clean | cartoon host 10/10 (incl. hand-derived blend literals + packer shelf/page/oversize + stale-gen + malformed-id + clip legs) |
| `9efe031c` | **H-2c**: the vendor step + the halcyond skeleton — `third_party/dejavu-fonts/` (the 4 Condensed faces + LICENSE/AUTHORS, PRUNED per its manifest; tarball sha256 `fa9ca4d1…`); **fontdue 0.9.4's no_std claim VERIFIED at vendor time** (§13.9 item 1: a scratch no_std lib built for `aarch64-unknown-none` with `default-features=false, features=["hashbrown"]` — the ttf-parser+hand-raster fallback is unneeded); `third_party/rust/` = the workspace's ENTIRE crates.io closure via `cargo vendor` (139 crates — source replacement is all-or-nothing, so the pre-existing registry deps went hermetic in the same motion; checksum-enforced, offline builds pinned in `usr/.cargo/config.toml`); `usr/halcyond` lib+bin from birth (the H-2a dormant-test lesson): the lib is the brain (pure, host-tested), the bin is the `guest`-gated body — H-2c lands `raster::GlyphSource` (fontdue faces → the cartoon packer, cached; kerning; regen eviction) + the bin's on-device raster floor probe | halcyond host 6/6 (incl. the full-stack word-through-executor leg + real DejaVu AV kerning) — all `--offline`; full workspace builds offline |
| `30dfa16c` | **H-2d-1**: the transcript core — `vt::SgrPen` extracted from the grid's `sgr()` (§13.4(b)'s sanctioned reuse: one SGR machinery, two consumers; the grid keeps a copy-in/apply/copy-out shim; bold-promotes-fg-at-application preserved exactly) + `halcyond::transcript`: zones→blocks (Prompt/Output/Foreign, lazily-opened anonymous blocks; exit marks incl. the tolerated pre-deviation-8 floating order), the column-addressed line discipline (\r overwrite, \t stops, BS, EL 0/1/2 — EL never moves the cursor), the streaming feeder (`safe_cut` holdback via a sequential open-escape scanner — **the last-ESC heuristic was a real bug the byte-by-byte determinism test caught on first run**: an OSC's ST terminator is itself a later ESC, so cutting there strands the opener; + cross-feed UTF-8), Beacon spans (em/obj/hdr die at block edges, the SGR pen persists), table capture (cells styled, inter-cell padding dropped), `raw_vt_intent` latch (alt-screen primary; CUP/CUU/ED/scroll = fullscreen intent, §13.4(b)), the block/cost/lines-per-block budgets with continuation blocks. Deviation recorded in-module: selection addressing is (block, line, col) over CELLS, not (block, run, byte) — the line discipline is column-based; runs derive at layout | halcyond host 17/17 (the streaming-determinism property + zones/table/SGR-utf8/discipline/intent/span-death/budget/continuation/floating-mark/split-frame legs); vt 9/9 post-extraction |
| `d02ed02f` | **H-2d-2**: layout + render — `raster` gains FACE_MONO (the baked Cornucopia atlas through the SAME packer/id space; missing-codepoint fallback = body-rasterized at cell height with the advance FORCED to the cell width — grid survives, glyph may clip, recorded); `layout.rs`: the paper-light `Sheet` (parchment-derived; §3), the per-cell face rule (em code → mono; annotated obj/em/hdr/table → body, bold under strong/hdr/ATTR_BOLD; un-annotated → mono — SGR color alone is not annotation), the §13.5 metrics rule VERBATIM (body box owns mixed lines, mono may not stretch it — a sneaked +2 clamp was removed when the test caught it; all-mono lines keep the exact cell box), greedy word-wrap with spill re-lay, proportional ruled tables (measured columns, l/r/c alignment, bold hdr row + rule), the failure-only exit badge, SGR-bg rects, per-glyph hit-test x's + obj hit targets on segs; `render_block` → cartoon ops | halcyond host 23/23 (+face rule, mixed/mono line boxes, wrap-reflow determinism + nothing-lost, table alignment + rules, exit badge on/off, end-to-end pixels) |
| `ef22fe38` | **H-2d-3**: the bin — halcyond becomes a real console renderer: aurora's proven loop shape (drain/feed/consctl + Surface + bounded connect + the #129/#135/#136 held-feed discipline — the policy now LIB-SIDE with aurora's selftest arms as real host tests in `input.rs`), advertising **`beacon rich`** (the first rich advertiser); the render pass (frozen blocks through a by-id `LayoutCache` keyed width+sheet.gen+atlas gen; the open block + pending line laid fresh; bottom-anchored scroll; the mode-visible cursor beam) through the cartoon CPU executor into `surf.pixels()`; the Helix-modal v0 (Esc→Normal: j/k/d-u/g-G/PgUp-Home navigation; i→Insert+bottom); TEV_CONFIGURE reflow (cache clear + winsize re-report — the mono-grid equivalent, the terminal-compatible answer); `layout_pending`+`cursor_pos` helpers; **joey's renderer choice** — the one-token `/lib/halcyon/renderer` file (post-pivot; `halcyond` selects, anything else including absent = aurora — fail-safe names only the proven fbcon); halcyond staged in `usr_rs_bins` | suite **1435/1435** + boot OK + 0 EXTINCTION on the default build (the joey default arm proven: `aurora spawned` with no config); halcyond host 31/31 (+8 input-policy legs) |
| `e1d82f44` | **H-2 CLOSE — audit round 1** (Fable 5 start==end, batched doubled-cadence over `05d837a7..7eb89320`; 0 P0 / 3 P1 / 2 P2 / 2 P3; DIRTY — 3 P1s + the F3 incremental-cap restructure; `memory/audit_h2_closed_list.md`). The threat model: every parsed byte is untrusted session output; OSC 1936 annotations are attacker-controlled (format-fuzz class); halcyond IS the console, so a panic/OOM/wedge loses the machine's face. **F1 [P1]** CSI numeric-param overflow → panic under overflow-checks+panic=abort (a plain `+ digit` after `saturating_mul`) → console DoS to one escape; fixed `saturating_add` in BOTH scanners (`transcript.rs` + `vt` — the vt copy also killed AURORA today, pre-existing+owned). **F2 [P1]** `mark k=exit code=i64::MIN` panicked `lay_exit_badge` (`n=-n`); fixed `code.unsigned_abs()` + saturating table col-x. **F3 [P1]** unbounded transcript growth → OOM (budget only ran at producer-chosen boundaries); incremental fail-safe ceilings: line soft-wrap (`MAX_LINE_CELLS`), objs cap+degrade (`MAX_OBJS_PER_BLOCK`, also fixes the P3 u16 wrap), styles cap (`MAX_STYLES_PER_BLOCK`, also kills style_idx O(n²)), table rows/cols/cell-chars + a whole-table byte budget. **F4 [P2]** the wire depth cap resets per feed() → the em/obj nesting stacks leak across chunks; fixed transcript-side with suppressed-open counters (LIFO-exact). **F5 [P2]** `stored_cost` obj-byte asymmetry drifted max_cost to zero; charged symmetrically. Self-audit converged on F3/F5 but MISSED F1/F2 (bias: exhaustion vs overflow-panic) — recorded. Riders: swept 2 pre-existing vt + 3 beacon clippy lints (inherited from aurora at extraction) so the whole userspace is clippy-clean. Docs: the AUDIT-TRIGGERS.md halcyond row + CLAUDE.md index line + HALCYON.md §13.3 (cells/Foreign/nesting as-built) + BEACON.md F10 as-built. Dirty-close re-prosecution of the fixes CARRIED FORWARD into the H-3 round (doubled-cadence). | host: vt 10/10 + cartoon 10/10 + halcyond 42/42 + beacon 29/29 (+7 regressions: 2 panic + 4 bound + 1 nesting); clippy-clean; default bake + boot OK + 0 EXTINCTION + arc 2/2 + clade 3/3; LS-CI full 38/38 (aurora-vt + skip arm + interactive family); ls-halcyon lever 12/12 [46s] (the audited render intact) |
| `7eb89320` | **H-2d-4**: the E2E + the fixes it forced — `tools/interactive/ls-halcyon.exp` (lever-gated on `THYLACINE_HALCYON=1` baking `/lib/halcyon/renderer`; SKIPs cleanly on default images — the cfg-4 precedent): 12 legs proving the render (parchment-dominant screendump + ~40k ink px — aurora can never produce it), the FULL rich chain (real advertisement → `ut: beacon rich exported` → ls table+obj frames ON THE WIRE), the split/zoom reflow round-trip (real QMP Super+H/Super+F; 128→63→128 cols via `/dev/winsize`), F10's silence, and a no-such-file negative control (Thylacine-worded). **Fixes the E2E forced**: (1) the SILENT-OOM death — libthyla-rs heap is a fixed 4 MiB and the no_std OOM panics into a bare `t_exits(1)`; `ThylaAllocN<BYTES>` added (const-generic; `ThylaAlloc` unchanged at 4 MiB) and halcyond declares 64 MiB LAZY (demand-zero; physical as touched); (2) the DARK-SCREEN wedge — the render pass moved to the loop TOP (first-present-wins scanout + frame ticks reach only visible surfaces: a renderer that waits before presenting stays dark and event-starved); console-up now witnesses a REAL presented frame; (3) F10 per-prompt tier re-read (`Repl::prompt_cycles` + the ut hook; re-export on change only); (4) Helix-modal selection v0 (`select.rs`: flat rows over lines+table-rows, Sel cursor/anchor/yank; Normal-mode v/y/p/Esc; selection bands + cursor-follow in the render; `LaidLine.src_item/src_row` stamping). **The hunt's findings**: post-pivot operands (boot-ramfs paths vanish at the pivot — `; pwd` sequencing passed hollowly while ls errored to stderr; ps framed because it takes no path — the gate was innocent all along); the chord-split focus rule (`pane.rs` split focuses the NEW leaf — zoom then hides the renderer); the ~32-char typed-line #60 cliff | ls-halcyon 12/12 on the lever image (45 s); halcyond host 35/35 |
| `6a812348` | **H-3a-1**: the `usr/lib/libhalcyon` crate (no_std; the `theme` module = the whole Daylight §1 palette as `Argb` constants + the NNW bevel + sage/cinnabar live-tile keys + syntax [`slate`=obj-ref] + status colours + `METRICS`; every value pinned to HALCYON-VISUAL by a test; `daylight_palette()` the transcript's Daylight-grounded vt palette — bg==surface, fg==fg so the pen default agrees with the Sheet's "default ink" check, `st.fg == sheet.ink`) + the halcyond transcript adopts it (`parchment_sheet()` → `daylight_sheet()` from `DAYLIGHT`; +`obj`=slate +`ok`=fen; `color_for` obj renders slate not ember; the transcript + tests grounded in `daylight_palette()`). The single token source the scripture names, shared by the transcript AND (H-3a-2) the chrome | host libhalcyon 3/3 + halcyond 42/42 + vt 10/10 + clippy-clean; lever ls-halcyon 12/12 [45 s] |
| `6680ca7f` | **H-3a-2**: the tapestryd Daylight pane chrome (geometric, NOT audit-bearing per §13.6). `paint_borders` extended from the 1px flat frame to the HALCYON-VISUAL §2 ring per visible leaf: floor(`gaps`) + the NNW 2px **four-value** mitred bevel (`bevel_top/left/right/bottom` from `libhalcyon::theme::DAYLIGHT`; nearest-edge mitre, horizontal wins ties → the top-weighted 45° corner) + the 1px inner hairline (== `header`, §2.4) + the 1px `border` cast shadow under the FOCUSED leaf's bottom edge only (§5.4, downward-only, owned by the tile). `pane.rs` `recompute`: content inset grew from `gaps` to `gaps + bevel(2) + hairline(1)` when >1 leaf (single leaf stays borderless — the stage-0 look; `BG_COLOR` unchanged, out of §2 scope). `paint_strips` segments recoloured to Daylight (active+focus=`ember` / active=`ember_deep` / inactive=`header`). tapestryd deps `libhalcyon`. The bevel is UNIFORM (§2.1/5.1 — no pane focus treatment; the old FOCUS/BORDER frame is gone). Consumer sweep (a colour/geometry change voids consumers): `tapestry-battery` strip-coord inset math 1→4; `ls-gfx-panes` tabA/tabB → Daylight; `ls-gfx-chords` $BG→floor (SKIPs sans cfg-4; verified by construction). **Also fixed a PRE-EXISTING ls-halcyon flake** owned in-passing: a boot-output race (login's "Thylacine login:" vs halcyond's "console up" on the shared serial) let the inline `lc_expect "console up"` swallow the login prompt and hang `lc_login` — moved both bring-up assertions to order-independent `tgrep`. Bevel witness added: after the split, sample the left pane's edges (bevel_left 226,214,192 != bevel_top 248,242,230 — four-value). Vault-owned (`sub-tapestryd`): dossier delta noted via yip 0031 | ls-halcyon lever PASS [35 s] (Daylight NNW bevel painted, four-value) + ls-gfx-panes default PASS [51 s] (tab strip Daylight + battery geometry) + host build/clippy-clean + libhalcyon 3/3 |
| `fd683761` | **H-3b-1**: the per-leaf Daylight tag-bar GEOMETRY + the resting fallback (sub-chunk 1 of 4; NOT audit-bearing — pure geometry + a read-only 9P file + a compositor paint; the gated verb is H-3b-4). `pane.rs` `recompute` carves `METRICS.header_h`=20 off each inset leaf's content TOP (inside the H-3a-2 ring; >1-leaf-gated like the ring — a single fullscreen leaf stays borderless AND bar-free, stage-0; a leaf too short to spare it stays bar-free) into a new `Pane.tagbar: Rect`; `content` is now the CLIENT rect (ring AND tag bar excluded). Token reconciliation as-built: `header_h` already IS the tag-bar height ("tag bar height (20)"), so no duplicate `TAG_BAR_H` was added; `TAB_STRIP_H` (5) moved into `libhalcyon::theme::METRICS.tab_strip_h` (the single-token-source precedent). A new per-pane `tagbar` 9P file ("x y w h"; ZERO when none) beside `geometry` — `PFK_TAGBAR` at the 4 mirror sites. `paint_borders` fills the strip `header`-bg (== the hairline colour, so hairline+strip read as one header band): the resting fallback so the region is never bare `BG_COLOR` (a full repaint clears to it and the strip is not a content rect); halcyond's OPAQUE `Role::Chrome` surface composites on top (H-3b-3). The painter needed only that one addition — it derives the ring `inset` from `content.x - r.x` (horizontal), so a top-only carve leaves the bands + cast shadow untouched. Consumer sweep by ground truth: the battery's tabbed strip-row derivation broke by exactly 20 (`sy` landed in the header-filled strip — the failing probe read `206 196 182`, incidentally proving the fill) → `sy` now subtracts the tag bar; content-centre probes were unaffected by construction. Witness added (both halves): the battery reads `pane/<A>/tagbar`, asserts it ABUTS A's content (same x/w, `ty+th == pa.y`), probes its centre device-side; ls-gfx-panes samples it host-side → `206 196 182` (`battery: tagbar A 960 14`). Vault-owned (`sub-tapestryd`): delta rung to vault via yip 0031; `theme.rs`/battery/exp unowned (sweep filed) | ls-gfx-panes default PASS [42 s] (+ the tag-bar witness) + ls-halcyon [27 s] and ls-gfx-chords [36 s] were env-gated SKIPS the runner scored PASS (their skip paths exited 0; the harness's SKIP code is 77) -- NOT witnesses of anything; corrected at H-3b-3, where both exps now exit 77 and the real lever runs are recorded + the 7 single-leaf CPU gfx (font/live/mode/mp/osd/osd-persist/osd-push) PASS inside a jobs=3 batch; ls-gfx-age PASS [40 s] after fixing a SECOND location of the pre-existing login-vs-`console up` serial race (the batch stall was NOT contention: solo at jobs=1 it failed identically, `no login prompt within 300s` against a healthy guest — its exp parsed the console grid from an inline pre-login expect that consumed the prompt when login raced ahead; now parsed from the `log_file -a` transcript after login, the H-3a-2 ls-halcyon cure; grid/region values unchanged; a comment-filtered sweep of every exp for the shape finds no third location) + host libhalcyon 3/3 + tapestryd/battery target builds clean |
| `88bdb6ab` | **H-3b-2**: the surface-create ABI + `Role::Chrome` activation (sub-chunk 2 of 4; capability-ADJACENT — a new renderer-gated create path — prosecuted in the H-3b-4 round). The surface ctl `create W H` takes optional `role=<content\|chrome>` + `bind=<pane-id>` (a ctl TEXT extension, not a wire break; §13.6): syntax is judged first (E_INVAL for every peer), then a well-formed `role=chrome` is RENDERER-GATED (`peer_is_renderer` → E_PERM — an ungated chrome role would let any client overlay fake chrome on another client's pane; the cfg-3 default-deny, the same class as the gated global verbs), then the bind must name a live LEAF (E_NOENT, checked BEFORE the weave allocation so a bad bind leaks nothing). `Surface.chrome_bind: Option<u32>` (the bound pane's PUBLIC id; ids are never reused, so a binding to a closed pane resolves to nothing). A chrome surface is NEVER hosted (`layout.host` skipped; no unzoom): placement is ONE authority, `surface_target` — a hosted surface → its visible pane's content rect, a chrome surface → its bound pane's `tagbar` strip (crop, never letterbox; None while the strip is ZERO or the pane hidden) — feeding `compose_geometry` / `compose_visible` / `note_present`; so non-focusable + excluded from the Direct count + from pointer routing by construction (none of those paths see an unhosted surface). Chrome rides the structural CONFIGURE fan with the STRIP size (the relayout hook its owner repaints/resizes on) and the frame fan (FRAME is a droppable coalesced class and CONFIGURE coalesces by replacement, so an idle chrome owner cannot wedge-retire its surface; only KEY/pointer events wedge, and chrome never receives them — not focused, never under `surface_at`). `paint_borders(fill_tagbars)`: the `header` resting fill is STRUCTURAL-only — a focus-only repaint refilling+pushing the strip would paint over a chrome surface's pixels. Client: `Surface::chrome_on(pane_id, w, h)` (its own conn; `open_on_bound`). Witness (the battery, a NON-renderer): four raw `create` probes in two errno classes — `role=bogus` / `role=chrome` (no bind) / `bind=1` (no role) → −22 E_INVAL; `role=chrome bind=<A>` → −1 E_PERM — the errno separates the parser's verdict from the gate's; the POSITIVE twin (the same line from a renderer, composited at A's strip) is halcyond's at H-3b-3 (one variable away: the renderer flag). Vault-owned (`sub-tapestryd` + `sub-libtapestry`): deltas rung on yip 0031; battery/exp unowned (sweep filed) | ls-gfx-panes PASS [42 s] (the four gate probes: `battery: chrome-create gate OK` + the exp's `discriminates (E_INVAL syntax / E_PERM non-renderer)`; the H-3b-1 tag-bar witness intact) at LS_CI_JOBS=1; ls-halcyon [27 s] and ls-gfx-chords [36 s] in that run were env-gated SKIPS scored PASS (exit 0; the harness's SKIP code is 77) -- NOT witnesses; corrected at H-3b-3 (both exps now exit 77; the real lever runs are recorded there); tapestryd/libtapestry/battery build clean (only the 3 pre-existing warnings); bake OK, keys paired |
| `9b77e2ba` | **H-3b-3**: halcyond paints the per-leaf Daylight tag bar (sub-chunk 3 of 4; DISPLAY-only; not audit-bearing on its own -- it CONSUMES the H-3b-2 gate as the renderer, and is the POSITIVE twin of the battery's E_PERM probe). NEW `halcyond::chrome` (`ChromeSet`): `reconcile` reads the pane 9P tree -- `layout` (leaf lines; `*` = focused; `hidden` skipped), `pane/<id>/tagbar` (ZERO = bar-free), `pane/<id>/tag` (the NAME; halcyond names its own pane "halcyon" once via that file -- §4.1 "the tile's program", §13.6 "the pane's tag file supplies a name") -- and diffs against the live tiles: gone → drop (Drop = conn close = server retire), new → `Surface::chrome_on(id, w, h)`, kept → repaint. `pump` drains every tile's events (CONFIGURE → `handle_configure` reweave; CLOSE/dead stream → drop) and returns whether a CONFIGURE was seen, which triggers a reconcile in the same pass (painting in the pump would flash the stale state). Runs after the first successful console present (first-present-wins scanout: chrome never precedes it) and on every main-surface CONFIGURE (any structural relayout fans one) or TEV_FOCUS. PAINT = one cartoon list per strip: `Clear{header}` + a 1px bottom-row separator (`ember_deep` on the focused leaf's tile = "resting, active tile", else `border`) + the name run (proportional face 10.5px, x=`tag_pad_x`, vertically centred via `line_metrics`; `fg` focused / `fg_muted` resting). No pills (a Resting bar has none; commands = H-3c), no trail; Live sage/cinnabar = H-3b-4. A gap my own design missed: a FOCUS-only epoch fans no CONFIGURE, so the "resting, active tile" separator would go stale on a focus move -- the server's focus-only branch now fans chrome a same-size CONFIGURE (the existing redraw semantics, coalesced by replacement). **The hollow greens**: this sub-chunk's first verify showed ls-halcyon PASS [27 s] with ZERO of its witness lines in the log -- the scenario is env-gated to the halcyond-renderer lever and had SKIPPED via `exit 0`, which the harness scores PASS (its SKIP code is 77); the sweep found ls-gfx-chords with the same idiom (a bare `==> SKIP`, no `LS-CI SKIP:` marker, exit 0) -- so the H-3b-1/H-3b-2 halcyon+chords "PASS" claims were skips (corrected above). Both exps now print `LS-CI SKIP:` + exit 77. Witness (ls-halcyon on the THYLACINE_HALCYON=1 lever): pre-split CONTROL (the strip-to-be region is transcript, not header); post-split the left strip is header WITH INK (the name) and its separator `border` while the focused new leaf's is `ember_deep`; after Super+Left the separators SWAP (the focus-epoch fan round-trips); after zoom the strip region reads transcript again (single leaf → strips ZERO → tiles dropped) | ls-halcyon on the THYLACINE_HALCYON=1 lever PASS [37 s] -- `pre-split control -- no tag-bar strip on a single leaf (dom 242,235,224)`, `left tag bar is Daylight header with name ink (186 4672 off/total)`, `tag-bar separators carry focus (left border, right ember_deep)`, `focus move re-keyed the tag-bar separators (left ember_deep, right border)`, `zoom dropped the tag bars (single leaf, strip ZERO)`, plus every H-2/H-3a-2 leg (parchment+ink, 63-col reflow, the four-value bevel, the rich chain); no `halcyond: chrome ... failed` line. ls-gfx-chords on the THYLACINE_AURORA_CFG4=1 lever PASS [34 s] with its real body (Super+H split, gaps 8 = the Daylight floor, Super+F unbound, Super+G zoom rebind + toggle) -- the first time today that scenario RAN. Default fixtures restored (0 lever lines in the restore bake) and ls-gfx-panes PASS [41 s] on them with both H-3b legs (`per-leaf tag bar renders Daylight header-bg`, `chrome-create gate discriminates`). All at LS_CI_JOBS=1; builds clean; keys paired |
| `2cbd4beb` | **H-3b-4**: the gated tile-status verb + the live-tile keys (sub-chunk 4 of 4; AUDIT-BEARING — the H-3b round runs on this commit and covers H-3b-2/-3/-4 + the H-2 dirty-close re-prosecution; the AUDIT-TRIGGERS row + CLAUDE.md index line land here). `tag <pane-id> status ok|err|resting` in `global_ctl` beside `gaps ` (gated BY CONSTRUCTION — `is_ungated_ctl` is a denylist; the cfg-3 gate judges authority BEFORE syntax; then E_INVAL syntax; then the id must name a live LEAF, E_NOENT). THE DESIGN CALL (ratified under the 2026-09-01 standing authorization): the verb carries the one fact only the renderer holds — the LAST EXIT (`Pane.status: Status {Resting, Ok, Err}`; `Resting` = nothing recorded, reset at alloc / host-into-empty-leaf / the root collapse) — and the compositor keeps FOCUS as its own fact, combining the two at paint time (§1.4: key = f(live, last exit)): the LIVE tile (the focused leaf) shows sage unless `err` (cinnabar); a tile that is not live shows NO key — so a stale or wedged renderer can never mark a tile that lost input, and a focus move re-keys the hairline inside the compositor's own focus-only repaint with no second verb (the per-tile display-key alternative, vote 1's literal wording, needs two verbs per focus move and lies in between). `set_tile_status` stores + repaints the rings (focus-only discipline: rect push, Composed only, only when the tile is the focused leaf). A new per-pane `status` read leaf (`PFK_STATUS`=8, four mirror sites) reports the RECORD; the pane-file write path returns E_PERM for it. `paint_borders` (§5.3/§5.4): the live tile's inner hairline is the key alongside the CONTENT (L/R + the bottom row = the shadow's dark half above H-3a-2's `border` row; the pane bevel between stays uniform, §2.1) and the bar's TINT alongside the TAG BAR (top row + flanks) so it still vanishes into the bar as §2.4 intends; bar-less live tile = four-side outline. halcyond: the §4.2 rows — LIVE sage/cinnabar (tint / key separator / key fg name), every other leaf "Resting, active tile" (header / `ember_deep` / `fg`; H-3b-3's focused=ember_deep stand-in corrected to the scripture; the plain Resting row is a stack's collapsed tiles, unbuilt); the key is READ from the pane's `status` file (one record for strip AND hairline); the feed = `Transcript::take_exit` (a latch off the `exit` mark) → `tag <own-pane> status ok|err` on the console conn → reconcile; refused once → off (display-only). THE LIB/BIN SPLIT ENFORCED: H-3b-3's `chrome.rs` syscalled inside the lib and had broken the documented host-test recipe (unresolved `libthyla_rs`/`tapestry`; unrun at H-3b-3) → `halcyond::chrome` is now the pure half (parsers, `key_for`, `key_colors`, `strip_list`; 5 host tests pin the §4.2 table + parse edges) and the bin's `chromeset` the surfaces. The zoom witness hardened: it now demands PARCHMENT at the top (a "not header" test would pass on the live tint before the zoom lands). Docs: 13.6 AS-BUILT (H-3b-4); 150-halcyond.md; AUDIT-TRIGGERS row + CLAUDE.md index. Vault-owned tapestryd delta rung on yip 0031 | ls-halcyon on the THYLACINE_HALCYON=1 lever PASS [43 s]: `tag-bar keys follow focus (left resting ember_deep, right live sage; left hairline header)`, `focus move re-keyed the tiles (left live sage: strip + content hairline; right resting ember_deep)` (+ the top hairline row = sage TINT alongside the live bar), `pwd; false` → `a non-zero exit keyed the live tile cinnabar (strip + separator + content hairline)`, `pwd` → `a zero exit re-keyed the live tile sage (the status is the last exit)`, `zoom dropped the tag bars (single leaf, strip ZERO, parchment at the top)`, every H-2/H-3a leg intact; no `tag status refused` / `chrome ... failed`. Default restored (0 lever lines); ls-gfx-panes PASS [42 s] with the battery as a NON-renderer: `tag-status gate refuses a non-renderer (E_PERM; state unchanged)` (write −1 AND `status` still `resting`), the H-3b-1/-2 legs intact. halcyond host 47/47; the three target crates clean; all at LS_CI_JOBS=1; keys paired |
| `6265702d` | **THE H-3b AUDIT CLOSE** (the executable tag bar: H-3b-2 `88bdb6ab` + H-3b-3 `9b77e2ba` + H-3b-4 `2cbd4beb`, plus the H-2 dirty-close re-prosecution). TWO prosecutors: R1 (the full brief) started on Fable 5.1 and FELL BACK to Opus 4.8 mid-run exactly at its server.rs deep read — the API transcript's `model` field, while its own `MODEL(end)` line still said Fable (the self-report is NOT a fallback detector; the JSONL field is); R2 (server-scoped, explicit `model: fable` override) stayed Fable. DISTINCT: **0 P0 / 3 P1-class / 4 P3, all fixed**; DIRTY by the invasive-fix rule → the fixes are the H-3c round's re-prosecution focus (`memory/audit_h3b_closed_list.md` "ROUND 2 FOCUS"). **F1 [P1]** (the H-2 re-prosecution): the transcript's OPEN block escaped the content budget (frozen-only eviction; a 10 000-line freeze → 320 MiB of soft-wrapped lines on a 64 MiB heap = a silent renderer death on `cat` of a big newline-free file) → `max_open_cost` (budget/8) freezes on BYTES; regression test (+1 → 48). **F2 [P1]** (R1+R2; pre-existing G-6, OWNED): the pane 9P subtree was ungated — any client could `close` the console leaf, steal focus (the graphical login prompt's keystrokes), wedge-retire a peer with 128 FOCUS flips in ONE service pass, forge a tile's `tag` (a rendered identity since H-3b-3) → **the pane tree's trust model** (§13.6, ratified under the standing authorization; rio's line): `Actor {Renderer, Client}` per write; subtree mutations need every hosted surface in the subtree to be yours; taking/naming a tile needs the leaf to host your surface; E_PERM; reads ungated; the chord layer is the environment; + FOCUS coalesces by replacement; + 4 layout mutations per conn per pass (E_AGAIN). The owner is the PROCESS (`Surface.owner_peer` = the peer's kernel `stripes`), not the conn — the first cut keyed on the conn and the battery's POSITIVE control (focus on its own pane through its driver session) caught it. The battery keeps every scenario (all on its own subtree); its end-of-run console refocus removed; a new leg witnesses the gate from both sides. **F3 [P1]** (R2; NEW at H-3b-3): a session per tag bar against global pools of 8 conns / 8 surfaces → a 3-window ceiling, then 5 s blocking connects inside the renderer per reconcile (listener un-armed when full; the kernel handshake deadline) and a console wedge under autorepeat → `Surface::chrome_on_shared` on the renderer's pane-tree session (`owns_root=false`), `MAX_SURFACES_PER_RENDERER` = 4 + MAX_PANES at mint, `MAX_SURFACES` = 8·4 + 32, listeners always armed + accept-then-refuse when full, failed mints said once. **P3s**: orphaned chrome told (TEV_CLOSE) + unbound at reconcile; the pane qid carries the full u32 (the 24-bit field DID alias the root past 2^24); `id_seq` `checked_add`; read-only pane leaves 0o444; chromeset `read_file` to EOF; the status feed retries instead of latching off. Docs: 13.6 AS-BUILT (trust model + chrome pool); 150-halcyond.md; AUDIT-TRIGGERS row addendum + round-2 list; the closed list. Vault deltas rung on yip 0031 | halcyond host 48/48; target crates clean; ls-halcyon lever PASS [41 s] (all H-3b-4 key lines; no `chrome ... failed` / `tag status refused`); ls-gfx-chords cfg-4 lever PASS [34 s]; default restored (0 lever lines): ls-gfx-panes PASS [42 s] incl. `pane-tree gate refuses close/focus/tag on a foreign pane (E_PERM; tree intact); own pane ok` + the H-3b-1/-2/-4 legs, ls-gfx-age PASS [39 s], the 7 single-leaf CPU gfx PASS; all at LS_CI_JOBS=1 |
| `81515e75` | **H-3c** (THE GATE: obj + verbs + menus -- input redirect + the compositor-owned dismiss; AUDIT-BEARING). **Compositor** (`server.rs`/`pane.rs`): `Role::Menu` surfaces (`create W H role=menu`, renderer-gated like chrome, no bind), ONE placed menu (`Comp.menu: MenuState{n,gen,rect}`) via the gated `menu place <surface-id> <x> <y>` (authority -> syntax -> a menu surface E_NOENT -> owned by the CALLER'S PROCESS E_PERM; clamp; a second placement replaces; forces Composed; the redraw CONFIGURE) + `menu dismiss`; THE GRAB (every key -> the menu, Esc = the compositor's dismiss + its release/repeats swallowed through the chord set; every pointer event menu-relative; a press outside = click-away: dismiss + press AND release swallowed; a chord dismisses then acts; FOCUS untouched); COMPOSITOR-OWNED DISMISS = force-retire (`retire`'s menu arm: unplace first, the `dismissed (<reason>)` line, the heal at the tail) reached by Esc / click-away / chord / the owner's verb / ctl `destroy` / conn death / WEDGE / replacement -- nothing needs the owner; the TARGETED heal (chrome intersections re-pushed, tag-bar/empty-leaf fills, the same-size CONFIGURE to intersecting surfaces; save-under REJECTED for 4.5.9); the menu composes LAST (`menu_reassert` in `screen_push`/`screen_flush_rect`/`screen_flush_full`); click-to-focus (§6) on the no-menu button press; the `ctl` `menu` line. **The rules engine** `beacon::verbs` (BEACON 7 as-built: `<type> <label> <template>`, `{}` = the resolved ref rc-single-quoted, control chars refused, bounded 16/32/256/256, `#` INTERNAL actions gated) + `/lib/beacon/verbs` baked from `verbs.default` (the lever prepends `path wedge-test #wedge 6000`); the session tier deferred to the settings push (halcyond has no `$home`). **halcyond**: `menu.rs` (pure: obj runs = one obj index per run; `w`/`b` step across rows; `run_rect`/`hit_run`; `build_menu`; the Daylight list: raised ground, border stroke, TYPE + RESOLVED ref first, `header` band on the selection) + `menuset.rs` (mint on the pane-tree session, `menu place`, the pump, `menu dismiss`) + main.rs (the frame hit map; Enter/click summon; a Command choice types `cmd\n` into the console + Insert; `#wedge` sleeps with the menu up under the `test-mode` feature). **Also fixed (found at the survey)**: the H-3b close's `chrome_on_shared` leaked a server-side surface per dropped tag bar (Drop closed fds without `destroy`; the compositor retires only on destroy/teardown/wedge; the 36-cap filled after ~17 zooms) -> libtapestry `Drop` writes `destroy` on a shared session. Docs: HALCYON.md 13.6 AS-BUILT (H-3c) + obj-interaction as-built; BEACON.md 7 as-built; 139-tapestryd.md "Menus"; 150-halcyond.md "The menu"; the AUDIT-TRIGGERS row + CLAUDE.md index line | beacon host 35/35 (+6); halcyond host 54/54 (+6); target crates clean; ls-halcyon on the THYLACINE_HALCYON=1 lever PASS [88 s]: `Enter on a path run opened its verb menu (raised ground ...; ref /lib/aurora/...)`, `Esc dismissed the menu compositor-side and the transcript healed under it`, the split keyboard menu, `a click on the run opened the same menu at the pointer`, `a click outside the menu dismissed it compositor-side; the press never reached the console`, `THE GATE -- the compositor dismissed the menu while its owner was frozen`, `THE GATE -- keys typed during the freeze reached the console`, `the transcript healed under the menu once its owner woke`, `the zoom's dropped tag bars + every menu retired server-side (surfaces 1)`, `no surface was wedge-retired`, beside every H-2/H-3a/H-3b line; default restored (0 lever lines): ls-gfx-panes PASS [42 s] incl. `menu gate refuses place/dismiss from a non-renderer (E_PERM; none placed)` + the H-3b legs; ls-gfx-age PASS [39 s]; all at LS_CI_JOBS=1 |
| `826095b6` | **THE H-3c AUDIT CLOSE** (81515e75 + the H-3b ROUND 2 FOCUS carried). ONE prosecutor (holotype-reviewer, explicit `model: fable`): every READ on Fable 5.1 (turns 1-72), then a fallback to Opus 4.8 at turn 73 for the host suites + the whole SYNTHESIS (the JSONL `model` field; `MODEL(end)` still said Fable -- the self-report cannot see its own fallback, second occurrence); the parallel self-audit was the second independent read. **0 P0 / 1 P1 / 0 P2 / 5 P3 + 5 self-found, ALL FIXED; DIRTY by the invasive-fix rule** (the grab's routing + the structural repaint changed) -> the fixes are the next round's focus (`memory/audit_h3c_closed_list.md` "ROUND 2 FOCUS"). **F1 [P1]** (both readers): the click-away RELEASE leaked to the pane under the pointer -- `ptr_btn` set the swallow record and THEN dismissed, and the retire arm cleared it (plus the press-inside variant); halcyond ignores releases, so no lever leg could see it; a release-activated widget in the pane would have fired. Fix = **a release or a repeat follows its press**: `key_owner`/`btn_owner` tables (packed slot+1 \| gen<<16, separate: BTN_LEFT & 0xff is KEY_Q) -- across a grab that began after the press, to a dismissed menu's retired slot (dropped), or to the compositor for a click-away (both edges swallowed); the lever's click-away leg now expects both swallow lines, printed inside the branches. **P3s fixed**: the Off arm ignored a placed menu (an invisible grab with nothing hosted); `menu_size` capped width but not height (a verb-rich type opened NO menu: `create` refused the taller surface) -> capped at the display + a scrolling `item_window` + `Menu::wheel` (+1 host test); SCROLL to a frozen owner's menu could wedge it -> summed at the back of its queue; `--` before `{}` in ls/stat/cat (hexdump/nora/wget/cd/kill do not take it -- queued); `chord_down` spanned 256 codes (`& 0xff` aliased KEY_FN + the media keys) -> 0x400. **Self-found fixed**: re-placing the same menu healed nothing under the old rect; `menu_reason` stale in the defensive branch; the STRUCTURAL REPAINT'S BLANK (a menu opened on a Direct console blinked the console black for a pass; every split too) -> `prefill_from_shown` composes each visible pane's last-presented slot before the flush (GL adoptions + HOLD excluded); the heal left the menu's pixels in the floor around a letterboxed/cropped surface (its client can never repaint there) -> `placement_rect` + `bars_around` fills; a chosen verb was APPENDED to a half-typed prompt line (`echo fo` + `ls` ran `echo fols -l ...`) -> halcyond feeds ^E ^U first (ut's editor: the draft moves to the kill buffer). **QUEUED, OWNED**: the chrome tiles' events (their same-size CONFIGURE on a focus-only epoch, a resize offer, the orphan CLOSE) land only at the next pane-tree RPC -- a focus move between two NON-console panes re-keys no tag bar; the H-3b-3 "pump reports a CONFIGURE" contract is hollow (the 2-pane lever never exercises it) -> the libtapestry EVENT SET (ONE Loom ring for all surfaces on ONE session), a chunk BEFORE H-3d (`memory/bug_chrome_tile_events_land_only_at_a_troot_rpc.md`). Docs: HALCYON.md 13.6 AS-BUILT (the close); BEACON.md 7; 139-tapestryd.md; 150-halcyond.md; the AUDIT-TRIGGERS row addendum + AUDIT-INDEX; the closed list | verified as the row says |
| `7b9a457d` | **H-3c-2: THE EVENT SET** (libtapestry; AUDIT-BEARING -- the new AUDIT-TRIGGERS row). ONE 9P session + ONE Loom ring per client, shared by every Surface opened on it: `tapestry::EventRing` (`connect`/`adopt`, `root`, `display_dims`, `wait` = arm every idle slot's read + one blocking `enter` + route every reaped CQE by its user_data into that surface's queue, `poll` = submit-only); a ring SLOT per surface (its queue + its place on the registered-handle table, rebuilt dense at every join/leave + a 128 B region of the ring's one staging buffer; 64 slots); `Surface::{fullscreen_on, open_on, chrome_on, menu_on}` (+ the private-ring `fullscreen`/`open` for the one-surface clients -- aurora, the battery, warp-prove unchanged). Presents are a synchronous `t_write` of the tpresent (the Rwrite is the recycle gate; the Loom WRITE bought nothing and cost a registered handle per surface). The slot lifecycle: `Drop` says `destroy` FIRST (the in-flight read EOFs promptly), leaves the ring (off the table; the slot freed now, or RETIRING until its read's completion frees it -- a stale completion can never land in a re-minted surface's region; the per-slot generation in the user_data is the belt). **Why one session is load-bearing**: a Loom wait pumps the session of its FIRST in-flight op only (kernel/loom.c `loom_wait_for_completions`), and a non-blocking enter demuxes nothing -- that is the mechanism behind the H-3c lever's session-reader finding, now built out instead of worked around. **halcyond**: the console + every tag-bar tile + the menu on one ring; step (1) blocks in `ring.wait()` when the console's queue is empty; `MenuSet::service` poll-only (the `block_first` dance gone); the pane-tree reads on the ring's root. FIXES the H-3b-3 latency (SA-10 of the H-3c close): a tile's CONFIGURE landed only at the next pane-tree RPC, so a focus move between two non-console panes re-keyed no tag bar. Docs: 139-tapestryd.md "THE EVENT SET"; 150-halcyond.md; HALCYON.md 13.6 AS-BUILT (H-3c-2); TAPESTRY.md F2 as-built; the AUDIT-TRIGGERS row + CLAUDE.md index line | halcyond host 55/55; every libtapestry client builds; ls-halcyon on the THYLACINE_HALCYON=1 lever PASS [113 s] incl. the new 3-leaf leg `THE EVENT SET -- a focus move between two non-console panes re-keyed both tag bars ... with no event to the console` + `and back` beside every H-3c line; default restored (0 lever lines): ls-gfx-panes PASS [42 s] (the battery's two surfaces on private rings; the menu-gate + pane-tree-gate legs), ls-gfx-age [40 s], font [66 s], live [74 s], mode [59 s], mp [44 s], osd [30 s], osd-persist [56 s], osd-push [35 s] all PASS (aurora on a private ring), 0 fails, all at LS_CI_JOBS=1 LS_CI_ATTEMPTS=1 |
| `dfdb6482` | **THE H-3c-2 AUDIT CLOSE** (7b9a457d + the H-3c ROUND 2 FOCUS carried). ONE prosecutor (holotype-reviewer, explicit `model: fable`): every one of its 103 turns on Fable 5.1 (the JSONL `model` field; `MODEL(start)` == `MODEL(end)`); the harness REFUSED its report-file Write ("subagents return findings as text") -- the report was recovered from that call's input and matched the completion notification verbatim; the parallel self-audit found 8 (4 coinciding). **0 P0 / 1 P1 / 1 P2 / 5 P3, NOT dirty by the count rule; all fixed but F7 (OWNED, deferred to a kernel seam).** **F1 [P1]** (pre-existing, universal since the shared ring): `route` latched `closed` on EOF only -- a NEGATIVE result (the session's death; a dead session completes every re-arm INLINE) left the slot open, the next pump re-armed it, the inline error CQE satisfied every blocking wait at once: a dead compositor livelocked every libtapestry client at 100 % CPU with the "compositor gone" exit arms unreachable -> `result <= 0` ends the stream (host test, sabotage-checked). **SA-4 [P2 latent]**: the DENSE table rebuild rebound a live slot's index while its SQE could still be QUEUED (`loom_drain_sq` stops early behind the CQ admission + chain gates) -> the read lands on ANOTHER surface's fid with the retiring slot's tag and that surface's event is dropped; unreachable at today's sizes (CQ 256 vs <= 48) -> INDEX-STABLE table (index == slot; a `ctl` placeholder fid; replaced whole) by construction (+ F3's indices-before-the-syscall subsumed; host test, sabotage-checked). **SA-3 [P1-class latent]**: MAX_RING_SURFACES 64 == the session's 64-tag table (a parked read holds a tag; `alloc_tag` refuses at 64 -> no present, no pane-tree read, no `destroy`) -> 48. **F2 [P2]**: a refused `create` closed the ctl without `destroy`; the mint already took a server-side slot the session's new lifetime never frees -> `fail_created` + the server retires a still-Minted surface at its last ctl fid's clunk. **F4**: an unpolled surface's client queue unbounded -> SLOT_QUEUE_CAP 256 stops arming (the server's 128 cap + retire applies again). **SA-6**: a blocking wait with nothing in flight spun -> `Err(Closed)`. **F5**: the Drop-order comment false -> `OwnedFd` by field order. **F6**: halcyond acted on Enter AUTOREPEATS (a held Enter re-summoned the menu 30x/s) -> one-shots on the press only. **F7 [P3, pre-existing] DEFERRED, OWNED**: the held-feed path never demuxes (keys queue server-side until a WEDGE-retire under a pathological pattern); the honest fix is a TIMED Loom enter (a syscall-arg change: its own kernel chunk; `memory/bug_held_feed_path_never_demuxes.md`). The slot bookkeeping is now `usr/lib/libtapestry/src/ring.rs` (syscall-free, behind the `guest` feature, 9 host tests). Docs: 139 + 150 + HALCYON.md 13.6 AS-BUILT (the close); the AUDIT-TRIGGERS row addendum + ROUND 2 FOCUS; AUDIT-INDEX; `memory/audit_h3c2_closed_list.md` | libtapestry ring host 9/9; halcyond host 55/55; beacon 35/35; every libtapestry client builds; ls-halcyon on the lever PASS [114 s] (38 lines, both EVENT SET lines, both click-away swallow edges, 0 WEDGED); default restored (0 lever lines): panes PASS [42 s] 31, age [40 s] 3, font [67 s] 5, live [73 s] 5, mode [60 s] 9, mp [44 s] 3, osd [31 s] 7, osd-persist [57 s] 6, osd-push [34 s] 5 witness lines, 0 fails; all at LS_CI_JOBS=1 LS_CI_ATTEMPTS=1 |
| `e3b5ba1e` | **H-3d: THE STATUS BAR** (design `a47b1a57`; AUDIT-BEARING -- the new AUDIT-TRIGGERS row; its round carries the H-3c-2 ROUND 2 FOCUS). **The compositor half**: `create W H role=status` (renderer-gated, no bind, one per display, W == the display width and H == the theme's `status_h` else E_INVAL) registers `Comp.status`; `reconcile` recomputes the layout on `disp_h - status_h` (the carve; a zoomed leaf too), `surface_target` places the bar at the bottom strip, `paint_borders` fills it `status_bg` at structural repaints, `retire` releases the carve, the global `statusbar` file reads the rect; Direct scanout is off while a bar exists (a leaf above the carve is smaller than the display: the console composes, as behind a menu). **The wire**: ut emits **OSC 7** (`file://localhost<cwd>`, RFC 3986-encoded) at both prompt sites inside the prompt zone, and `mark k=cmd;text=<line>` as the output zone's FIRST child (the 12.2 v1 amendment); the transcript's own scanner captures OSC 7 (a 256-byte bound; `pct_decode_path`: absolute, control-free, `localhost` or no host, else dropped whole) into `cwd()` and the cmd mark into `Block.cmd` / `last_command()` (the running command while the zone is open); `wire::strip` (the P1 tool) removes the report too -- **the first lever boot extincted at `u-repl-test`'s P1 check** (strip(rich) != plain) before that fix. **halcyond**: `status` (the lib: `status_list` -> the four slots + `Slots`; `condition_for`; `context_text`; the context truncates at its right with an ellipsis; host tests) + `statusset` (the bin: ONE `Surface::status_on` on the ring after the console is up; pump; `model_from(chrome.focused(), own_pane, t.cwd(), t.last_command())` + `clock_hm()` UTC; paint on change; the `painted` say line names the slot rects for the witness); `ChromeSet::focused()`. Votes (operator present): ONE filled workspace indicator until H-4; OSC 7 from ut now (an ABI addition, signed off). Docs: HALCYON.md 13.6 (RATIFIED + AS-BUILT (H-3d)); BEACON.md 12.2 + 12.11; 139; 150; the AUDIT-TRIGGERS row + CLAUDE.md index line | halcyond host 61/61 (4 status + 2 transcript tests new); beacon 36/36 (the strip test new); the ring 9/9; libutopia's 2 repl tests are guest-target only (libthyla-rs does not build on darwin); every client builds; ls-halcyon on the lever: PASS [116 s], 45 witness lines (the 5 status-bar legs + the 3 wire/context tgreps new; the census leg now expects the console + the bar), 0 WEDGED; the panes probes + the default set on restored fixtures: panes PASS [42 s] 32 (+1: the statusbar file), age [40 s] 3, font [67 s] 5, live [73 s] 5, mode [60 s] 9, mp [44 s] 3, osd [30 s] 7, osd-persist [57 s] 6, osd-push [34 s] 5 witness lines, 0 fails; all at LS_CI_JOBS=1 LS_CI_ATTEMPTS=1 |
| `3587d8f2` | **THE H-3d AUDIT CLOSE** (e3b5ba1e + the H-3c-2 ROUND 2 FOCUS carried). ONE prosecutor (holotype-reviewer, explicit `model: fable`): 60 turns on Fable 5.1 (every file read in scope + the re-derivation + the cataloguing of F1-F5), then a MID-RUN fallback to Opus 4.8 for the 29-turn synthesis/report/host-test tail -- the WHOLE ENVIRONMENT flipped at that moment (the commit-attribution reminder flipped with it, and back again later); the JSONL `model` field is the detector, `MODEL(end)` still claimed Fable (the self-report cannot see its own fallback -- the third occurrence in run 16). Per the standing rule a fallback round that FINISHES is closed; every key surface was read pre-fallback. The parallel self-audit was the second independent read. **0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty; all fixed.** The two reads caught DIFFERENT things: the prosecutor the one real bug, the self-audit 4 latent/cosmetic P3s (mode-set latent; say-line quote; dot-y; H-4 active-index -- noted/withdrawn/deferred). **F1 [P2]**: the transcript's `mark k=cmd` arm bumped the output block's `open.cost` but NOT `stored_cost` -- on eviction `stored_cost -= dead.cost` subtracts a charge never added, so the byte budget drifts toward zero (saturating) until `stored_cost > max_cost` never fires and the 32 MiB content budget stops enforcing (the exact hazard the sibling obj/cell/table/style comment names). THE SELF-AUDIT GOT THIS WRONG: it verified the `open.cost` increment and wrote "accounted", missing that the invariant is the PAIR. Fixed by the mirror; regression test `the_cmd_mark_charges_the_shared_byte_budget_symmetrically`, sabotage-checked (pre-fix `stored_cost` 96 vs the live 108 -- short by exactly the cmd length). **F2**: a menu overlapping the status strip left stale menu pixels there on dismiss (`menu_heal` healed leaves + tag bars at once, the strip only via the async CONFIGURE fan) -> the strip is filled `status_bg` + pushed before the fan. **F3** (witness): the sage condition-dot leg reused the Err line's `cx`; the dot moves ~2px with the label width, so it passed only by pixel overlap -> re-read per state. **F4** (witness; the row itself asked for it): the carve was witnessed by the `statusbar` rect alone -- a regression that registered the bar but skipped `layout.recompute(dw, layout_h, ..)` would pass -> the lever reads the console leaf's carved content height from the `layout` file and asserts it == disp_h - 20 (cell-height independent: 780 of 800). **F5**: a persistently-failing bar mint ran two sync RPCs EVERY pass (ChromeSet retries per relayout) -> `StatusBar.want_mint` + `rearm()` on a relayout. Docs: the AUDIT-TRIGGERS row addendum; AUDIT-INDEX; `audit_h3d_closed_list.md` (auto-memory); JOURNAL | halcyond host 62/62 (+1: the F1 regression); beacon 36/36; the ring 9/9; every client builds; ls-halcyon on the THYLACINE_HALCYON=1 lever PASS [118 s], 46 witness lines (+1: `the carve shrank the layout -- the console leaf is 780 of the 800px display`), 0 WEDGED, 0 fails; default fixtures restored (0 lever lines): panes PASS [43 s] 32, age [39 s] 3, font [67 s] 5, live [74 s] 5, mode [60 s] 9, mp [46 s] 3, osd [31 s] 7, osd-persist [56 s] 6, osd-push [35 s] 5 witness lines, 0 fails; all at LS_CI_JOBS=1 LS_CI_ATTEMPTS=1 on a quiet host (aux released mac for the run) |
| `e233d390` | **H-4 LAYOUTS DESIGN -- the D decision** (scripture-first, no code; ratified 2026-09-02, operator present, after a Fable 5.1 consult [200 turns, no fallback] + a parallel self-analysis CONVERGED). **Option D**: layout save/restore is a user-authority **session tool** (`halcyon layout save|restore`), run AS THE USER by the user's shell (Plan 9 riostart / acme Dump-Load); tapestryd gains a third actor **`Session(principal)`** derived from the kernel-stamped `srv_peer_info.principal_id` (already stamped; used nowhere today) -- ordinary pane authority keys on the USER principal, not per-process `stripes`, with NO new syscall/CAP/SPAWN_PERM; read side = the pane-file walk (no server verb); placement = a one-shot `claim` token (the Wayland xdg_activation / Fuchsia ViewCreationToken shape); halcyond supplies only the gesture + the device-tier geometry-only restore. The consult corrected 3 premises the finding + my initial Option A had wrong (the principal is already stamped -> no new grant; an all-empty subtree is anyone's -> no renderer-only skeleton; halcyond [pre-login SYSTEM, no $home] can't read the session-tier layout -> save is the user's tool too). **Ratified trust call**: same-principal peers gain rio-style mutual pane authority (weaker than the I-26 same-owner kill; the console [SYSTEM] + other users stay protected). **Also ratified**: the shipped device-tier DEFAULT layout IS the self-demonstrating first-launch welcome (two Utopia panes on Daylight; a live Genera tour whose runnable objs drive the right shell). Docs: HALCYON 13.1/13.6/13.7; TAPESTRY #42 realized; AURORA-CONFIG session-leader realized; NOVEL Angle #4 addendum; `design_h4_layouts.md`; scratchpad finding/analysis/intro-welcome | docs-only; no code |
| `cdce7f3f` | **H-4a-1: the layout FORMAT core** (`libhalcyon::layout`, pure + host-tested). The `halcyon-layout v1` serializer + a bounded, no-panic parser for the pane tree's SHAPE (container mode + active; per-leaf tag = the command line; surface ids never saved). Reads UNTRUSTED input (a `$home` layout file) -> every path fail-closed: MAX_DEPTH 32 / MAX_NODES 256 / MAX_TAG_LEN 1024, malformed -> Err (the caller degrades), never a panic. Shared by halcyond (device restore + gesture) + the session tool + tapestryd, so it lives in libhalcyon (the D decision's home). Backslash escape for the tag (`\` `"` `\n`); active clamped not fatal; the two-pane welcome's exact serialization pinned. NEXT: H-4a-2 (the save tool: the pane-file walk -> the format -> the two-tier durable write) | libhalcyon host 11/11 (8 layout: round-trip [leaf/welcome/deep-nested], escaping, active-clamp, 12 malformed-input Errs, bounds, trailing-blank); clippy 0 + rustfmt clean; device build (aarch64-unknown-none) 0 errors |
| `30551d8f` | **H-4a-2: the layout SAVE tool** (`usr/halcyon`, native libthyla-rs; NON-audit-bearing -- read + serialize + write, no new authority). `halcyon layout save <name>` walks /dev/tapestry (the `layout` render_text dump + each `pane/<id>/tag`), folds it via the new `libhalcyon::layout::from_render_text` (render_text and the v1 format share the pre-order, so both feed one `assemble` stack machine; an oversize tag drops to empty so the fold always round-trips), serializes, and writes the SESSION tier ($HOME/lib/halcyon/layouts/<name>) with aurora's config::save discipline verbatim (write-tmp + content fsync + atomic rename + STRICT post-rename fsync on the SAME OWRITE fd; gfx-status cfg-2a). The `halcyon` crate mirrors nora: a pure host-tested lib (dispatch + name validation [one path component, no leading dot -- traversal closed by construction] + session-path building) + a thin bin. Run AS THE USER (HALCYON.md 13.7 the D decision): no CAP, no SPAWN_PERM, no server verb. `layout restore` prints "not yet implemented" (H-4b). Wiring: usr/Cargo.toml members + build.sh usr_rs_bins. Runtime E2E rides H-4b (save+restore round-trip). | libhalcyon layout host 17/17 (+6 from_render_text) + halcyon lib host 6/6 + device build (halcyon 107632 B staged) + suite 1435/1435 + boot OK |
| `4bb6048c` | **H-4b-1: the one-shot placement CLAIM** (tapestryd; the HALCYON.md 13.7 placement primitive, ALONE -- H-4b was SPLIT: the pre-compaction fold was refuted by ground truth, a claiming child hosts its OWN surface and never observes cross-process authority; H-4b-2 = the Session actor, H-4b-3 = the tool, one batched holotype at the arc close). `pane/<id>/claim` (PFK_CLAIM) mints a 128-bit token on an EMPTY leaf (the offset-0 read, fid-pinned so a read-to-EOF spends one mint; last mint wins; container E_NOENT, occupied E_PERM, E_IO said); `create W H claim=<32hex>` hosts into that leaf after the weave alloc (`host_into`: no split, no focus move) and FALLS BACK to focus placement, said, when the token names no live empty leaf (13.7's opaque cookie -- a child never fails its create). `create`'s three loose role params -> a typed `Host` enum (a claim beside chrome/menu/status is E_INVAL at the syntax gate, before the renderer gate, unrepresentable past it). WHO MAY MINT: any peer at this stage (today's all-empty-is-anyone's rule; strictly weaker than the `close` every peer holds); 13.7's session-owned narrowing lands with `owner_principal` at split (H-4b-2) -- a Seams bullet. libtapestry `Surface::open_claim[_on]`. THE DEFECT THE FIRST E2E CAUGHT: `first mint unreadable` -- proven by code, tapestryd could not call SYS_GETRANDOM (CAP_CSPRNG_READ-gated; the warden confers only CAP_HW_CREATE). FIX (operator-chosen over the world-rw /dev/random or an AT_RANDOM DRBG): `caps = ["csprng"]` on tapestryd's warden manifest (a NEW manifest field, MENAGERIE.md section 6; libdriver's closed `Cap` vocabulary, fail-closed on unknown/repeated/duplicate) -> the warden maps + says the grant at spawn -> joey grants the warden the bit (I-2 monotone chain). Two scripture deviations caught by the pre-audit self-read (fail-loud vs 13.7's fall-back; "not an authority boundary" vs "PLACEMENT is a capability"). Found, enqueued, NOT fixed: /dev/random world-rw vs the cap-gated syscall. Vault: sub-tapestryd/sub-warden/sub-stratum-boot + seam-tapestry-battery-unowned + chg. NOT audit-closed (the batched H-4b holotype). | libdriver host 89/89 (+3 caps); clippy 0 new; rustfmt 0 new hunks in every touched range (the tapestry family's 493 PRE-EXISTING hunks surfaced as a decision); paired bake content-verified; suite: boot OK + arc gates 2/2 + exit 0; ls-gfx-panes (HVF) 33/33 incl. the new claim leg -- the first run was 28/29 with the leg red (the cap defect) |
| `636937b0` | **H-4b-2: the `Session(principal)` actor** (tapestryd; the authority-KEY half of HALCYON.md 13.7's D decision). A third actor keyed on the kernel PRINCIPAL: `actor()` is now 3-way -- Renderer / SYSTEM\|NONE\|INVALID -> Client(stripes) / else -> Session(principal), so system daemons + the unauthenticated + unknown peers stay per-process (the boot chain never becomes one mutually-authoritative session) and only a real user earns session-wide mutual pane authority (a program running as you may close/refocus/rename/claim your OTHER tiles -- strictly weaker than the I-26 same-owner kill; the console + other users stay protected). `Conn.peer_principal` cached once at `Conn::new` (immutable, unlike renderer-status which `peer_is_renderer` re-reads because the SAK can revoke it). `Surface.owner_principal` (set at mint) + `Pane.owner_principal` (empty leaves, stamped at SPLIT from the splitting actor via the free fn `actor_owner_principal`: Renderer/Client -> 0 = the environment, Session(p) -> p). The three authority checks gained `Session(p)` arms keyed on owner_principal; `actor_names` now lets a session NAME an EMPTY leaf it owns (the tool tags at claim time -- acme's tag-before-`win`). The H-4b-1 claim mint narrowed from its interim "any peer" to owner-only (Renderer anywhere / `Session(p)` iff `owner_principal==p` / a Client never; E_PERM otherwise -- this CLOSES the H-4b-1 Seams bullet). `reap_session_empties` closes a departed session's empty scaffolding on its principal's LAST conn death (main's two conn-removal sites do the last-conn check against the survivors; occupied leaves are already gone via teardown's retire; guards sentinels; test-mode say!). REGRESSION-SAFE by construction: single-process michael is unchanged (stripes and principal both uniquely ID it). The POSITIVE cross-process mutual-authority witness is OWED at H-4b-3 (the restore tool is the real second same-principal peer). NOT audit-closed (the batched H-4b holotype over H-4b-1..3 at the arc close); NOT pushed until it closes. | ls-gfx-panes (HVF) **33/33 under `Session(michael)`** -- REGRESSION: the claim leg (`battery: claim OK`) still mints (the battery owns the leaf it split) + the pane-tree negative still refuses `michael->SYSTEM` (E_PERM); full boot OK + the joey boot suite + login E2E michael+cora, 0 EXTINCTION; clippy 0 new (243 pre-existing, none in my ranges); no new rustfmt hunks. Preceded by 4 pure-`cargo fmt` reformat commits (`fb626450`/`0949963d`/`452ff1d2`/`d9334feb`; proven pure: idempotent + 0 string-literal content changed) |
| `f6e306ae` | **H-4b-2 refinement: split stamps BOTH empty leaves** -- HALCYON.md 13.7's PLURAL "empty leaves record an owner_principal at split". H-4b-2 stamped only the NEW child of a split, leaving the environment root's OTHER child at owner 0 (unclaimable) -- which breaks layout restore, whose precondition is that every leaf the tool builds is session-owned (so it can place a surface into each via the claim token). The fix stamps both empty children with the splitter. SOUND: bounded to EMPTY leaves (already anyone's to mutate per 13.6 -- confers nothing beyond the close every peer holds; never touches a leaf hosting a surface). Discovered while designing H-4b-3; pulled forward as its tapestryd precondition. An autonomous refinement to a just-landed audit-bearing surface -- the batched H-4b holotype is the check. | ls-gfx-panes (HVF) 33/33 -- the battery splits leaf A then claims the new empty leaf, so the run directly witnesses the both-stamp path; clean boot, 0 EXTINCTION; clippy 0 new; NOT pushed (batched audit) |
| `75365f95` | **H-4b-3a: libtapestry auto-claim** (the client half of restore) -- `Surface::open` auto-consumes an inherited `TAPESTRY_CLAIM` from `/env`, upgrading a Content mint to `Claim(token)` so a restored program lands in the tool's target leaf without knowing about placement (13.7's opaque cookie; i3 append_layout minus the swallow hack). One-shot per process via a `CLAIM_TAKEN` latch, but correctness does not depend on it (the server-side consume is already one-shot -- a spent/inherited-stale token falls back to focus placement). Absent/malformed var -> a normal un-placed open, so a normally-launched program is unaffected. Guest-gated. H-4b-3b (the restore TOOL: mint claims + tag leaves + spawn children with the token in /env + the skeleton-build) + the ACTIVE-path E2E are the fresh-context remainder. | ls-gfx-panes (HVF) 33/33 -- the auto-claim is a NO-OP for the battery (no TAPESTRY_CLAIM), every existing surface client opens as before; clean boot, 0 EXTINCTION; clippy 0 new; NOT pushed (batched H-4b holotype) |
| `3b12f7b4` | **H-4b-3b: the restore TOOL** (`halcyon layout restore <name>`; the syscalling half of HALCYON.md 13.7's D decision -- the H-4b arc's last sub-chunk). Reads the saved layout (SESSION tier then DEVICE tier), keeps the session's part (`libhalcyon::layout::prune_env` drops every `env`-marked tile -- the console/another user -- and dissolves the containers that lose a child), and rebuilds that subtree beside the console on the tool's OWN `/srv/tapestry` session (a `Session(michael)` peer, NOT the shared `/dev/tapestry` mount whose peer is joey). Mechanism: a throwaway PLACEHOLDER surface gives the tool a leaf it may split (the console's is not its tile); `libhalcyon::skeleton::plan` (NEW, pure + host-tested) turns the `LayoutNode` tree into `split`/`mode` verbs modelling the compositor's nest-vs-flatten rule, and the tool executes each verb DIFFING the live `layout` dump before/after to bind the new pane id and CHECK the nest/flatten expectation (a divergence aborts the restore, never mis-places a program); the placeholder is destroyed; then per tagged leaf it mints `pane/<id>/claim`, writes `pane/<id>/tag` (a session NAMES its own empty leaf -- H-4b-2), seeds the one-shot token into ITS OWN `/env` `TAPESTRY_CLAIM`, and spawns the tag as the user (`resolve_prog` mirrors the shell's `/bin` search since the kernel spawn-resolves relative to CWD, not `$path`) -- the child's libtapestry (H-4b-3a) auto-consumes the inherited token on its first `open` and lands in the leaf, knowing nothing about placement. The tool waits for every program to host, replays the saved focus over the tiles that arrived (the CROSS-PROCESS witness: focusing a peer michael-process's tile), and reports. SAVE side extended: `pane/<id>/owner` (NEW read-only tapestryd pane file, `PFK_OWNER` -- a hosted leaf's surface owner or an empty leaf's recorded owner, 0 = the environment) lets `layout save` mark each tile `env` iff a REAL other principal owns it (owner 0 = nobody = the session may rebuild an empty pane; owner == me = the session's own), fail-CLOSED on an unreadable owner. libtapestry now DROPS the spent `TAPESTRY_CLAIM` from the consuming child's own `/env` (a deep copy; the spawner's untouched) so a grandchild does not inherit a taken token. GROUND-TRUTH found + fixed in the E2E: `mkdir -p /home/michael/...` is refused (the coreutil creates every ancestor incl. the `/home/michael` MOUNT POINT, and a user cannot write the SYSTEM-owned `/home`); the restore/save tool is UNAFFECTED (its `session_dir_chain` starts at `<home>/lib`), the E2E uses single-level mkdirs, and the `mkdir -p`-under-home defect is enqueued. AUDIT-CLOSED at the arc close (the batched H-4b holotype over H-4b-1..3: 0 P0 / 0 P1 / 0 P2 / 2 P3, NOT dirty; F2 fixed [is_env_tile owner-0 fail-open -> `owner_is_env`], F1 documented [the empty-scaffold cross-principal grief, a v1.x multi-seat seam]). | libhalcyon host **31/31** (+13: the `env` marker + `prune_env` + the whole `skeleton` planner: single-leaf/flatten/nest/tabbed/nested-tabbed/alternating/same-mode-flatten/one-child/active-clamp/anchor-split/wide-tree, refs-are-a-permutation on each) + halcyon lib host **9/9** (+`prog_candidates` + `device_paths` + `argv_of`); clippy 0 new in halcyon's own source (144 warnings all in the libthyla-rs dep); NEW E2E `ls-gfx-restore` (HVF) PASS 5/5 -- the tool spawned 2 demos (pids 2371/2372) into built leaves 4/5, `focus -> pane` (the Session(principal) cross-process focus), `restored 2 of 2`; ls-gfx-panes (HVF) **33/33** REGRESSION (the new `owner` pane file rides the battery readdir); paired bake content-verified |
| `08f23d7f` | **Multi-console tile model + the kaua-term substrate** (design pass, operator-ratified 2026-09-03; co-designed with the aux track via yip call 0044). HALCYON.md §14 (authoritative) + §13.1/§13.3/§13.4 supersession notes + TAPESTRY.md §14 concretization note. Reconciles "the terminal is the desktop" (TAPESTRY §14) with the as-built one-`/dev/cons` world: **(b) uniform-pts-per-tile** substrate (the built `ptyfs`; `ut` already runs on a pts slave via ptyhost/PTY-4b); **UNIFORM-Y topology** — every tile a crash-isolated **`kaua-term`** process, native + Linux (the operator chose uniform over the co-design's hybrid lean, grounded in `no_std` `panic = abort` on hostile input crashing an in-process parser → the whole environment); the **(B) feed-cells seam** (the `kaua-term` is a headless parser → `TCell` + events; `halcyond` keeps transcript/render/inline-media/Helix and composites — a clean refactor of the already-cell-based transcript); the split (`halcyond` compositor+orchestrator / aux `kaua-term` / kernel-viv **C2-k1c** termios+winsize + **C2-k3** job-control follow-on); winsize+beacon per-tile relocation; native out-of-band media (`cat picture.png`); inline-live graphical apps unified as composited client surfaces; SAK/I-27 unchanged (serial per §18.7). Aux's half = `docs/KAUA-TERM.md` @`8cbc77e5`. **GATES H-4d.** Build order: **KT-1** (kaua-term native + seam + composite — ZERO kernel work) → H-4d; then C2-k1c (Linux tiles); then C2-k3 (Linux job control). Owed next: the seam **protocol** (a fresh co-design call). | n/a (scripture) |
| `a8dbf46c` | **KT-2 (the VT parser growth; committed under the label `KT-1a-*` -- a self-compact mis-numbering, corrected to the canonical build order per operator 2026-09-03: KT-1 = the kaua-term process + seam + halcyond composite [NEXT, MAIN], KT-2 = this parser growth, KT-3/KT-4 = the C2-k1c/k3 ioctl reach [AUX]. The pushed commits keep their `KT-1a` label; this row is the map). usr/lib/vt reaches full-xterm** (the shared parser grows the behaviors a real Linux TUI needs; a 4-piece batch + a 2-round audit). `c01bb942` KT-1a-1 (DECSTBM scroll regions + DECOM origin mode); `1c4d537d` KT-1a-2..4 (SU/SD `CSI S`/`T` band scroll with the 5-param-`T` highlight-tracking guard; wide chars -- a Kuhn wcwidth over decoded scalars, a double-width glyph = left half + `ATTR_WIDE` 1<<7 + a blank continuation, margin-wrap-whole, combining = zero-column, cols<2 degrades to single-width; SGR residue italic/dim/blink/strike `ATTR_*` 1<<3..1<<6, 22 clears bold+dim); `a8dbf46c` the R1 audit close. NEW `docs/reference/150-vt.md`. **Audit (format-fuzz class, HALCYON 14.10; Opus fallback x2 -- Fable credit-exhausted, MODEL start==end both rounds; ~325k+ fuzz iters + a verbatim-crate overflow-checks repro): R1 = 1 P0 + 1 P1 + 1 P2 + 2 P3, all fixed with regressions; R2 on the fixes = CLEAN 0/0/0/0. Clean via 2 rounds.** P0 = a wide glyph in a cols<2 grid wrote the continuation cell OOB (abort at the bottom row) + stranded cx>cols -- REACHABLE from aurora's live `.max(1)` resize floor (a 1px tile / oversized font -> cols=1) and the coming per-tile kaua-term; fixed by degrading a wide glyph to single width when cols<2. P1 = IL/DL ignored the DECSTBM region (corruption for exactly the region-using TUIs this arc serves); fixed by confining both to [scroll_top,scroll_bot]. P2 = Vt::new missing resize's zero-dim clamp. Backward-compatible with aurora (full-region path byte-identical; private fields, Vt::new-only ctor). | vt host **48/48** (34 in-tree +14 audit repros incl. an exhaustive F3 sweep + a 4000-seq fuzz over grids 1x1..4x4); clippy clean; vt + aurora + halcyond build aarch64-unknown-none; full build + boot gate GREEN (boot OK, 0 EXTINCTION, arc L-6c/D-5 PASS, aurora console up 128x36 + #129 feed selftest PASS) |
| `15796866` | **KT-1.5-kernel: KObj_Loom pollable** (the unified-poll enabler) -- `loom_poll` + the `poll_scan_one` KOBJ_LOOM arm: register on the ring's existing `cq_waiters`, POLLIN iff `loom_cq_ready>0` (register-then-observe, I-9; the wake was already wired at every CQE post). So halcyond can wait on the ring AND pipes in one `poll`. No new spec (process-ctx wake, a plain poll_waiter_list instance). SMP gate deferred to the KT-1 batch close (composes the already-gated cq_waiters + poll dispatch). | `test_loom.c::test_loom_poll` + `loom-smoke` KObj_Loom.poll leg + boot OK |
| `a85c94e4` | **KT-1.5b-i: the unified poll** -- libtapestry gains `connect_sqpoll`/`adopt_flags`/`poll_fd` (additive; `connect` stays non-SQPOLL so aurora is untouched); halcyond opens its event set SQPOLL and replaces the block-on-ring with ONE `poll { ring.poll_fd() | /dev/consdrain }`. Fixes the pre-existing frame-coupled console latency (output wakes the renderer at once, not at the next frame tick) + removes the F7 held-feed root cause (the SQPOLL kthread demuxes parked replies continuously). | suite 1463/1463 + aurora no-regression + `loom.poll` unit + `loom-smoke` SQPOLL leg + `ls-halcyon` E2E all-green |
| `a0324198` | **KT-1.5b-ii-a: the Tile model** (host-tested) -- `grid.rs` (a live `vt::Cell` grid, OOB-drop, cursor-clamp) + `tile.rs` (`Tile::apply(Record)` -- the 14.11.2 dispatch; `Osc1936Raw` -> the SAME beacon parser the console uses) + transcript `push_scrolled_rows` + extracted `intern_style`. The per-tile model = a live grid + a scrollback `Transcript`, separate because the grid spans zone boundaries. No boot. | 13 host tests (6 grid + 7 tile); halcyond lib green; reference `150-halcyond.md` updated |
| `739f6cb7` | **KT-1.5d-1a: the per-user halcyond bootstrap** (HALCYON.md §14.12) -- `login` reads `/lib/halcyon/session` and, when `on`, spawns `/bin/halcyond --session` AS the user (identity only -- no `CAP_SET_IDENTITY`, no `SPAWN_PERM_CONSOLE_RENDERER`; zero delegation). `session_main` skips the `/dev/cons` trio, connects to tapestryd as `Actor::Session`, presents. The `THYLACINE_HALCYON` system-renderer lever retired for the session. Wedge fixed (elusive-bug hunt): joey's seeded login passes `--no-session` -- a non-terminating child broke the boot waiter's `t_wait_pid_for`. | `ls-gfx-session` (HVF) PASS 27s -- login michael -> session up 1280x800 -> Direct scanout; reference `150-halcyond.md` + HALCYON §14.12 |
| `fe16a8b0`+`9a4b9c0b` | **KT-1.5d-1b: the display handoff** (scripture + impl; operator-ratified) -- tapestryd's reconcile BACKGROUNDS the console renderer (aurora, a SYSTEM leaf) when a SESSION leaf is visible: excluded from the scanout decision (`active_vis`/`active_nleaves`) + not composited + not FRAME-ticked, so the root collapses to `Direct(halcyond)`; on logout aurora un-backgrounds + its FRAME loop resumes. **aurora UNCHANGED.** Redesigned after the drain-resume plan FLAPPED 18x/session (#76: `say!`->`cons_emit`->the console drain carries ALL daemon output, never idle -> "drain has data" cannot mean "logout"; FLAPDBG decoded the waking bytes). BACKWARD-COMPAT: no session leaf -> byte-identical. Audit-bearing (scanout, I-40-adjacent); the vault ring (sub-tapestryd) + the focused audit ride the KT-1 batch close. | `ls-gfx-session` (HVF) PASS, ZERO flap (bg 1x / fg 1x); JOURNAL run-25 |
| `65db7b6f` | **KT-1.5d-2: one session tile** -- `session_main` spawns ONE kaua-term running `/bin/ut` AS itself (`Command` `Stdio::Piped`: child fd0=down [halcyond writes `Key`/`Resize` `Input` frames], fd1=up [child writes the `Record` stream]; zero delegation, I-22 clean). Unified `poll { ring.poll_fd() | up_pipe }`; ingest = one `t_read`/wake -> `FrameDecoder` -> `parse_record` -> `Tile::apply` (the trust boundary; a `WireError`/EOF/`Control::Exit` = logout); render = `Tile::render` (alt = grid only; normal = scrollback flow via `layout_block`/`render_block` + a mono grid tail, bottom-anchored, `scroll_up` wired@0 [wheel = follow-on]); input = `TEV_KEY` -> `input::map_key` -> `Input::Key` down; **real logout** when the tile's `ut` exits (retired the d-1b autoexit test lever). TWO findings: **PALETTE** -- the seam ships resolved RGB so halcyond can't re-theme downstream; the compositor palette is stamped at the PRODUCER (§14.6): `vt::DAYLIGHT` + `Vt::with_palette` (`new` delegates BONFIRE -> aurora untouched), the kaua-term builds its Vt with DAYLIGHT, `libhalcyon::daylight_palette()` = `vt::DAYLIGHT` single-source (pinned). **FOCUS** -- a backgrounded (invisible) leaf must not hold input focus; reconcile moves focus off it onto the session leaf (the input twin of the d-1b scanout priority; gated `has_session`) -- a d-1b gap d-2 surfaced (`host()` splits but leaves focus on aurora, so the tile would be uninputtable). Audit-bearing (reconcile focus + backgrounding); rides the batch close. | host: halcyond lib **82** (map_key x4 + render x3 new), vt 45, kaua-term 26, libhalcyon (+ the `daylight_is_vt_daylight` single-source pin); clippy no-new (the map_or hits are the file idiom), rustfmt clean. `ls-gfx-session` (lever-on, HVF) PASS 30s first attempt -- the full chain in order, zero flap: `background surface 0` -> `Direct(halcyond)` -> `tile spawned pid 128x36` -> `session up` -> `tile ingest live` -> input `exit` -> `tile exited (code 0) -- logout` -> `foreground surface 0` -> `Direct(console)` (the `exit` leg proves input routing end to end). REGRESSION (default pool, michael-session, backgrounding+focus EXERCISED 2x): `ls-gfx-panes` PASS 43s (all battery legs incl. focus/chord + the post-battery console+winsize restore) + `ls-gfx-restore` PASS 28s. Default suite (test.sh): **1464 [test] PASS, 0 FAIL, 0 EXTINCTION**, boot OK, aurora console up. reference `150-halcyond.md` (session tile + render + map_key) + `150-vt.md` (DAYLIGHT + with_palette). CAUGHT WRONG TURN: the session lever is POOL-WIDE (a lever-on pool makes michael's login in panes spawn the session halcyond, failing pre-battery) -- the gfx suite needs a DEFAULT bake, ls-gfx-session a lever-on bake. |
| `402fbf0a` | **KT-1.5d-3: multi-tile** (HALCYON.md §14.11.6/.7/.9/.10) -- the per-user compositor generalized from one tile to N. NEW `session.rs` (`session::run`, the I/O half; moved out of `main.rs`) hosts one `kaua-term` + content `Surface` + `Tile` per compositor leaf, keyed by leaf id in a `BTreeMap`, reconciled off the `layout` file each relayout. NEW host-tested `halcyond::tiles::plan_tiles` (the pure create/drop diff; 6 tests). **Create**: a new EMPTY leaf the session owns is CLAIMED (`pane/<id>/claim` -- the server-side owner+emptiness authority, §13.7; a failed read = "not ours", skip) + hosted via `Surface::open_claim_on` at the leaf's `pane/<id>/geometry` size + a kaua-term spawned in. **Multiplex**: ONE `poll { ring.poll_fd() | up_0..up_N }` over every live tile's up-pipe. **Focus-routed input FOR FREE**: tapestryd delivers `TEV_KEY` only to the focused surface (`key_event` -> `layout.focused_surface`), so per-surface drain is inherently focus-routed -- halcyond never reads `l.focused`; the Super chord layer (split/focus/zoom/close, §18.4) is the compositor's. **Containment** (§14.11.10): a clean `Control::Exit(0)` CLOSES the leaf (a `close <leaf>` layout verb, tmux-style) + reaps; a `WireError`/non-clean-exit/abnormal-EOF FREEZES the tile as an affordance (last frame held, pipe skipped, kaua-term killed), reaped on user-close; neither ends the environment; a `closed` leaf-id set is the permanent respawn guard. Logout at tile-count 0. **F1 (tapestryd fix the E2E surfaced)**: the compositor's Super-chord `Split` (`exec_chord`) never stamped the new leaf's `owner_principal` (the client `split` verb did), so a session could not claim its own chord-split leaf -> no tile B; fixed to stamp the split leaf's surface owner. So d-3 is NOT halcyond-only -- it touches `usr/tapestryd/src/server.rs` (the aux-3 merge surface; aux edits `main.rs`). **F2 (OPEN, surfaced)**: a BACKGROUNDED aurora leaf still consumes a tiling column (d-1b backgrounding is scanout-only; `recompute` runs before `bg_now`), so a multi-tile session tiles N+1 columns -- a d-1b refinement surfaced to the operator ([[bug_d3_aurora_backgrounded_leaf_tiling]]); does NOT block the mechanism (E2E greens; the d-2 focus-transfer self-corrects the close re-home off the backgrounded leaf). Unblocks H-4d (needs F2). | host: halcyond lib **88** (+6 `tiles::plan_tiles`), vt 45, kaua-term 26; clippy no-new, rustfmt clean; halcyond + tapestryd device builds 0 errors. `ls-gfx-session` REWRITTEN for the multi-tile arc (lever-on, HVF) **PASS 34s**: root tile (leaf 3) -> `session up`/`ingest live` -> Super+H split -> 2nd tile (leaf 4) -> `exit` closes the FOCUSED B (leaf 3 survives = focus routing + containment) -> focus re-homes to A -> `exit` closes A -> logout + `foreground` + `Direct(console)` (3 cross-process terminal markers matched order-tolerantly -- one alternation pattern, loop until all seen; two test-ordering bugs caught: exit-0-masks-FAIL, expect matches by arm-order not position). reference `150-halcyond.md`. |
| `53ee407f` | **KT-1.5d-3 F2: backgrounded-leaf tiling + STRUCTURAL TRANSPARENCY** (d-1b completion; two operator votes) -- (i) TILING: `reconcile` determines backgrounding from the TREE BEFORE `recompute` (owner-based, visibility-independent) into a new `Pane.backgrounded`; `layout_pane`'s Split arm zero-rects a backgrounded leaf (KEPT visible, so the post-recompute vis/bg/scanout accounting is untouched) and the foreground siblings divide the FULL rect; all-bg container guard. Session-less display -> empty set -> byte-identical (the console path). (ii) The ls-gfx-panes REGRESSION this exposed: excluding aurora makes the battery's pane A WIDE, so `pane::host()`'s geometry-based split (w>=h -> SplitH) FLATTENS [aurora, A, B] into the root -> `mode A tabbed` targets the root (contains aurora) -> E_PERM. Operator-ratified fix = **structural transparency**: a backgrounded leaf is transparent to a session's structural ops -- `actor_owns_subtree(Session)` skips it (GUARDED: >=1 owned non-bg surface, else a session could close an aurora-only subtree; empty stays vacuous), `tab_cycle` / `visible_strips` / the `layout_pane` Tab arm (a new `Next::Tab`, resolved outside the `&mut p` borrow) skip it -- never a segment, a cycle stop, or the shown child. (iii) Two residual bugs closed by ground truth: the battery's strip arithmetic UNDERFLOWED u32 on the now-borderless tabbed-root child (tb.x=0, tb.y=5) -> OOB probe crash (fix: saturating_sub, a test-correctness fix); and `tab next` E_PERM'd because the ownership skip keyed on `Surface.backgrounded` (VISIBILITY-derived, CLEARS once a tab hides the leaf) -- re-keyed on the LEAF tree flag `is_bg_leaf` via `subtree_hosted` (THE TWO-FLAGS TRAP). (iv) **F2b -- a PRE-EXISTING COMPOSITOR DEFECT the battery adaptation exposed (run 29):** to restore the nested splitV the mode legs assume independent of `host()`'s shape-keyed split, the battery now does an explicit `split <A> v` BEFORE hosting B -- and B, hosted into that pre-split EMPTY leaf, never received its CONFIGURE offer: `calc_geom_sig` folded only `(id, content rect)` per visible leaf, never WHAT the leaf shows, so a hosting that changes no rect ran NON-structural (the focus-only chrome branch) and the fan never fired (`offered == None`; the resize leg's stale-serial probe got E_INVAL, not E_AGAIN -- run 28 mis-read it as a bumped serial; the error's own discriminant said "no offer"). Reach: every host-into-an-existing-empty-leaf path -- an explicit `split` then a spawn, the H-4b `host_into` claim placement, `host()`'s first arm -- masked because every gate hosted via the SPLIT arm and production clients pre-size to the pane. Fix (state-based): the signature folds the hosted surface INCARNATION `(slot, gen)` (sentinel for empty), so hosting/unhosting/same-slot-rehost are structural on every placement path without a per-caller poison; plus a test-mode `say!` on every resize-ack rejection naming the discriminant + deciding state. `ls-gfx-panes` is the regression test (fails 3/3 without the fold). Audit-bearing (reconcile/recompute + the H-4b ownership model, I-40-adjacent); rides the KT-1 batch close. | `ls-gfx-panes` PASS (42 s, all 15 legs; the explicit pre-split + the resize leg = the F2b regression test, red 3/3 before the signature fold; the move leg re-keyed on layout-text child order) + `ls-gfx-restore` PASS (29 s; claim placements now structural hostings) + `test.sh` PASS, default bake. `ls-gfx-session` PASS (34 s; session bake) -- the F2 geometry leg: 2 session tiles SUM **1264/1280** (min 632; pre-F2 ~836; witness captured THEN spawn, sequentially -- expect matches by ARM ORDER, proven in isolation). `ls-gfx-chords` PASS (cfg-4 bake; the d-3 owner-stamp inert on the console chord path). tapestryd + battery build (3 pre-existing warnings, none new), rustfmt + clippy clean. SMP gate: at the batch push. |
| `062efe18` | **KT-1 batched audit close, round 1** (three Fable 5.1 prosecutors, MODEL start==end; static, on 53ee407f): A poll substrate 0/0/0/6 (the SQPOLL kthread yield-spin now live, the 10 ms re-arm jitter, spurious poll()==0, the pollable-arm test gap, doc/comment rot, the session down-pipe blocking write); B kaua-term seam 2/3/4/5; C compositor+session 1/2/4/6 -- DIRTY (P0 x3, P1+P2 = 13). **P0s**: the per-user halcyond (and every tile program) inherited login's CAP_SET_IDENTITY (`Command` defaults to inherit-all; login masked the ut path only) -> `.caps(SHELL_CAPS)` on the session spawn + `!T_CAP_SET_IDENTITY` on the tile spawn + `.caps(0)` on aurora-push + `/bin/caps-probe` typed into a session tile (plain spawn OK / identity spawn REFUSED); the session reconcile read a HIDDEN leaf (zoom, tabs) as vanished and killed its shell -> `parse_leaves_all` + `Leaf.hidden` (hidden never creates, never drops; 2 host tests) + a zoom/unzoom leg. **P1s**: `close` reached the transparent console leaf -> `actor_owns_subtree_all` for the destructive verb; Direct chosen against an INSET layout (the zero-rect leaf counted) -> `foreground_leaf_count` for the inset + a display-sized content conjunct; alt-screen enter/leave left the wrong content -> full CellDiffs at both boundaries; a capped ScrollOff x3 copies exceeded the kaua-term's 4 MiB heap -> a 256 KiB byte bound + a lazy 32 MiB span; halcyond's per-tile 32 MiB budgets x N vs 64 MiB + zero-cost empty lines -> ONE 32 MiB session budget shared by tile count + `ITEM_OVERHEAD` charged per line. **P2s**: the lone survivor kept a ring (F4, same fix); the 4-surface conn cap dead-keyboarded the 5th tile -> a DECLARED session conn gets the renderer's cap + a refused claim closes the leaf; the "byte-identical console path" claim was FALSE for every user program (any user pixel backgrounded the console) -> the display handoff is now DECLARED (`session on` on the compositor's own conn; `principal_is_session` alone no longer triggers it; the battery asserts an undeclared client leaves the console leaf tiled); aurora-push inherit-all; the resize applied after the post-SIGWINCH repaint -> re-checked after the read; the Title latch unbounded -> 256; a zero-count master write killed ptyhost's pump / tore a key -> bounded back-pressure retries; tile shells lacked `--home` -> login forwards it, halcyond appends it. **Also fixed** (P3): the sentinel principal stamp, the zero-content `surface_target`, the one-level bg exclusion (`is_bg_subtree`), TEV_FOCUS/close-driven reconcile, the down-channel desync teardown, the vt shrink scrolling rows off under capture, the poll.h/menuset/main.rs comments, ONE `Surface.backgrounded` flag (derived from the tree). **Deferred, tracked**: A-F1..F4 (kernel poll efficiency/latency/spurious-zero/test gap -- no kernel change this close), C-F11 (renderer chrome vs a session, aurora-only), C-F12 (lever-on image without a compositor), B-F11 (fd 2 inherit; `Stdio::Null` unimplemented), the encode-straight-into-out refactor. Round 2 on the fixes follows. | ls-gfx-session PASS (caps-probe OK/REFUSED, zoom survival, lone tile, 1264/1280) + ls-gfx-panes/restore/chords + test.sh on the tree just before the `session on` verb; the round-2 close re-ran all five on the final tree |
| `cf499fe1` | **KT-1 audit close, round 2** (two Fable 5.1 prosecutors on 062efe18's fixes, MODEL start==end; static): C2 compositor+session 0/0/1/3; B2 seam+ingest 0/1/3/4 -- not dirty by count (P1+P2 = 5), DIRTY by invasiveness (two restructures) -> a focused round 3. **P1**: halcyond's render laid out EVERY frozen block per paint -- a transient ~1.8x the retained bytes, charged to no budget, so one tile with ~20K rows of history (well under its 32 MiB share) OOM'd the whole session -> `Tile::render` windowed on a per-block height cache (only the blocks in view + the open block are laid out, one `LaidBlock` alive at a time; 3 host tests; `vault/hazards/haz-budget-stored-not-derived.md`). **P2s**: the round-1 declaration was first-come with no takeover and halcyond DIED on a refusal -> the seat is the principal's (takeover from a same-principal or idle holder; E_BUSY only for another principal's live tiles; halcyond retries then runs UNDECLARED, never into the login loop); `set_max_cost` was lazy (a quiet tile kept its old share; the sum grew as 32 MiB x H(N)) -> evicts at once (host test); the A-F6 drop-newest queue was record-blind (a dropped Resize was never re-sent) -> `DownQueue` with a never-dropped latest-wins geometry slot (4 host tests); `scroll_cap` bounded one ScrollOff, not the per-feed pile-up (`ESC [ 36 S` x 819 per 4 KiB read = 29K rows, ~60 MiB) -> `Producer::feed_into` ships each capped ScrollOff through a sink (host test). **P3s (all fixed)**: `retire_conn` un-declares after the retire loop; `TEV_LAYOUT` (kind 10) to the declared conn on every structural pass (the empty-leaf split had no channel); the shrink's rows precede the full diff; the POLLOUT entries stop at `POLL_MAX_NFDS` (1+32+32 = 65 read as 'compositor gone'); the battery's console control fails on parser drift and gains the one-variable declared control (`session on` -> ZERO; `close` on the console's container E_PERM = the C-F2 regression; `session off` -> restored) asserted by ls-gfx-panes; HALCYON.md step 4 rewritten. **Vault**: `adt-kt1-r{1,2}` + 24 `fnd-kt1-*` + 3 seams (`seam-loom-sqpoll-p3s`, `seam-console-chrome-on-handoff`, `seam-login-halcyond-fallback`) + `chg-2026-09-05-kt1-audit-close`; dossier sections on sub-tapestryd/sub-libtapestry/sub-stratum-session/sub-mechanism-drivers; `docs/reference/152-kaua-term.md` NEW + the 150-halcyond section. Kernel byte-identical since 53ee407f (SMP 40/40 stands). | ls-gfx-panes (+ the three declare-control arms) / ls-gfx-restore / test.sh / ls-gfx-session / ls-gfx-chords PASS on the final tree; host tests halcyond 97 + kaua-term 28 + vt 45 |
| `65bb05b3` | **KT-1 audit close, round 3** (one Fable 5.1 prosecutor on the round-2 restructures, MODEL start==end; static + two host experiments): 0/0/1/6 -- CLEAN; every finding fixed. **P2**: the feed sink fired only on the Scroll arm -- an alt-screen toggle is eight bytes and a full screen each way, so a 4 KiB read of toggles was 512 full diffs held at once (45 MiB at 128x36; 32 MiB from 320 bytes at 4K, measured) -> the sink triggers on the CELLS held in `out` reaching `SCROLL_ACC_BYTES`, checked after every boundary (host test: 256 pairs, peak <= bound + one screen). **P3s**: `TEV_LAYOUT` coalesces in `push_event` (a structural churn from any client vs a non-draining session could wedge-retire the root surface and log the session out); the resize drain ships rows only (`Producer::drain_pending`; an equal-cell-count resize shipped a stale-pitch diff before the full one, measured; host test); `OPEN_BLOCK_MAX_COST` = 512 KiB caps the open block whatever the share (it is laid whole every render -- up to 4 MiB of cells at 32 MiB -- and the comment claimed one block/O(view); now O(view + 2 x cap), `laid_lines_last` witnesses <= 12 lines for 200 blocks) and bounds the re-budget residue (4 MiB x H(N) before; host test); the seat is held only WHILE IT HOSTS (the same-principal takeover stole the user's own live compositor's seat and degraded it until logout -- gone; halcyond re-declares after its first surface so an idle usurper in the declare->mint window cannot leave it mislabelled); three doc drifts. **Self-found in parallel** (before the report): the height cache re-keyed on (id, exit, height) -- a floating exit mark is the one post-freeze mutation of a frozen block (host test); the poll-set ceiling is a bounded wait, not an unwatched pipe (unreachable at 30 tiles; kept as defence). Vault: `adt-kt1-r3` + `fnd-kt1-r3-{c2,c6}`; the hazard's class-shaped variant. Round 3 is the arc's last: the round-2 hash fixup rides this commit. | ls-gfx-panes / ls-gfx-restore / test.sh / ls-gfx-session / ls-gfx-chords re-run on the final tree; host tests halcyond 99 + kaua-term 30 |
| `f25781ad` | **The fullscreen-zoom fix** (the operator's Cmd+F report via aux 0048): Super+F on DOSBox-X showed its 640x417 frame native at the display's top-left on black. Root cause (aux's frames + the DOSBox-X source, not the placement rects): the #56 patchwork latch keyed on DAMAGE COVERAGE, but the property it exists for is SLOT ROTATION -- only a rotating client's slot is stale outside its damage. DOSBox-X is single-slot (thyla_tap presents slot 0, the app's framebuffer) yet presents partial rects (its menu bar, changed scanline bands), so the compositor latched it an accumulator and CROPPED at the content origin in the tile too (the tile frame shows it at the pane's top-left; the one-shot letterbox line was an earlier 640x400 incarnation's). Fix: `Surface.slots_presented` + the latch = partial AND >=2 distinct slots (one-way, said once); `ComposeOp.clip` = the damage's projection through the scale (`libhalcyon::place::scaled_clip`, brute-force host-tested against `nearest_src`, the mapping `compose_cpu` samples by) so a partial present of a letterboxed surface redraws its band, not the display; `letterbox` delegates to `libhalcyon::place` and the battery derives its sample points from the same function. `Surface::set_single_slot` (libtapestry) = thyla_tap's discipline for native clients + the battery. Witness (ls-gfx-panes `singleslot`): D (640x400) single-slot, zoomed -> the compositor's `letterbox 640x400 -> 1280x800 @(0,0)` line (the witness aux's log LACKED) with the latch line a FAIL arm; the partial present's green at (160,100), the frame's blue at (960,600) (black under the bug), a post-zoom partial present's yellow at (1120,700); then the control: rotation on, the second partial present latches (`slot 1 of slots 0b11`). FIT vs FILL: aspect-fit is the existing letterbox policy, unchanged (DOSBox 640x417 -> 1227x800, 26 px pillars); a per-surface FILL/integer preference is a future knob. UNAUDITED: batched into the next tapestryd round (the AUDIT-TRIGGERS row 42 note). The real-DOSBox re-run (aux's dx-fullscreen-repro.exp on the fixture aux-3 carries) is OWED to aux after the merge | ls-gfx-panes PASS [48 s] (the singleslot leg: the 2x letterbox line + 3 pixels + the rotating control latching), ls-gfx-restore PASS [29 s], test.sh PASS, ls-gfx-session PASS [40 s], ls-gfx-chords PASS [35 s]; libhalcyon host tests 39 (7 new); clippy clean; kernel untouched (SMP 40/40 on 53ee407f stands) |
| `26f903a0` | **H-4c: the layout gesture + named-layout management + the startup script** (HALCYON.md 13.7 AS-BUILT; BEACON.md 12.2 v1 + `type=layout`). The gesture is the transcript's own verb menu: `halcyon layout list` presents each saved layout as an `obj type=layout` (ref = the NAME), and `/lib/beacon/verbs` offers `restore` / `save` / `delete` on it (`halcyon layout <verb> {}`; a name never begins with `-`, so no `--`). `halcyon layout delete <name>` unlinks the session-tier file (device tier read-only; a save's `.tmp` residue is hidden from `list` and refused as a name; `SAVE_TMP_SUFFIX` shared). The startup script: `halcyond --session` spawns `ut --home $home $home/lib/halcyon.rc` once after the first present (else `halcyon layout restore default` when the image ships one -- the H-4d welcome's slot; else nothing; rio's `-i`), as the user under the tile cap mask, stdin /dev/null, reaped via try_wait with a bounded idle poll while it runs, killed at logout (`halcyond::session_init`, host-tested: 3). Gates: ls-gfx-restore list/delete legs; ls-gfx-session rc leg (save a 2-tile layout + write the rc in tile B, logout, re-login -> the init line, `restored 0 of 0`, tiling at THREE tiles, the exit line). OWED at H-4d: the tool/compositor claim race on TAGGED leaves (the compositor must skip a tagged empty leaf). UNAUDITED (batched) | ls-gfx-restore PASS [28 s] (list/delete legs), ls-gfx-panes PASS [45 s], test.sh PASS, ls-gfx-session PASS [51 s] (the rc leg: init line, `restored 0 of 0`, three tiles, clean exit); ls-halcyon SKIP (lever); host tests halcyon 13 / halcyond 102 / beacon 36; clippy clean |
| *(KT-1.1..1.4)* | The kaua-term **producer side** (local, batched): `a90eea53` KT-1.1 (extract `usr/lib/ptyhold` from ptyhost, behavior-identical) · `3cce9ff1` KT-1.2a (vt resumable `feed_until` + boundary events, opt-in) · `e2c115bc`+`5879804d` KT-1.2b (the kaua-term crate's record producer, host-tested; +bound the coalesced ScrollOff) · `3851df53` KT-1.3 (the KeyEvent->xterm re-encoder + DECCKM) · `e55543e3` KT-1.4a (the wire codec, bounds-safe decode) · `91333a9a`+`1f991394` KT-1.4b (the kaua-term process: pts host + 2 pump threads; +serialize the master's two writers). Detail: JOURNAL run-21. Full per-chunk rows land at the KT-1 batch close. | host: vt + kaua-term tests; the KT-1.5a boot-prove (`90d95d08`) PASS |

## Remaining work

**H-3 (Presentations + the Daylight chrome) — design ratified 2026-09-01**
(the design pass; HALCYON.md §13.6 concretized + operator votes recorded;
Daylight = `docs/HALCYON-VISUAL.md`, ratified binding scripture). Four
sub-chunks (vote 2), strip painted whole by halcyond (vote 1):
- **H-3a — LANDED** (both sub-chunks): H-3a-1 (`6a812348`) the
  `libhalcyon::theme` crate + the transcript-palette swap; H-3a-2 the tapestryd
  Daylight NNW 2px four-value bevel + inner hairline + live-tile cast shadow +
  the recoloured strips (see the Landed table). Geometric, not audit-bearing;
  bevel witnessed on the lever (four-value). **H-3b is in flight (H-3b-1 landed).**
- **H-3b -- LANDED + CLOSED** (four sub-chunks + the audit round; AUDIT-BEARING at H-3b-4): the executable tag bar. **H-3b-1 LANDED** (`fd683761` — the per-leaf geometry + the resting fallback + the `tagbar` file + `tab_strip_h` into METRICS; the tag-bar height is the EXISTING `header_h`, no second token). **H-3b-2 LANDED** (`88bdb6ab` — the surface-create ABI `create W H role=chrome bind=<id>`, RENDERER-GATED at create [as-built, §13.6], `Surface.chrome_bind` + the one placement authority `surface_target`; chrome never hosted → non-focusable / no Direct / no pointer by construction; the structural-only resting fill; client `Surface::chrome_on`; the battery's four-probe gate witness — see the Landed table). **H-3b-3 LANDED** (`9b77e2ba` — halcyond's per-leaf chrome surfaces: the `chrome` module, the focus-epoch CONFIGURE fan, the real lever witnesses; and the hollow-green correction — ls-halcyon/ls-gfx-chords skips now exit 77). **H-3b-4 LANDED** (`2cbd4beb` — the gated `tag <pane-id> status` verb + the live-tile keys on strip AND content hairline; the exit fact is the renderer's, focus is the compositor's; the halcyond lib/bin split enforced; the AUDIT-TRIGGERS row + CLAUDE.md index line). **THE H-3b AUDIT ROUND CLOSED** (`6265702d`: two prosecutors, 0 P0 / 3 P1-class / 4 P3 all fixed -- the transcript open-block byte cap, the pane tree's trust model, the chrome pool; DIRTY by invasive fix -> the H-3c round re-prosecutes the fixes). **H-3c LANDED** (`81515e75`: THE GATE -- Role::Menu surfaces + `menu place`/`menu dismiss` + the input GRAB + the compositor-owned dismiss (force-retire on every path) + the targeted heal + compose-last + click-to-focus; `beacon::verbs` + /lib/beacon/verbs; halcyond's obj runs / keyboard + click menus; THE GATE PROOF on the lever; + the shared-session surface leak fixed). **THE H-3c AUDIT ROUND CLOSED** (``826095b6``: 0 P0 / 1 P1 / 0 P2 / 5 P3 + 5 self-found, all fixed -- the click-away release leak, the press-owner routing, the scrolling menu, the structural pre-fill; DIRTY -> the next round re-prosecutes the fixes); the H-3 exit gate's audit half is done. **H-3c-2 THE EVENT SET LANDED** (``7b9a457d``: ONE session + ONE Loom ring per client, `tapestry::EventRing`; halcyond's console + tiles + menu on it; the tiles' latency fixed; the 3-leaf lever leg). **ITS AUDIT ROUND CLOSED** (``dfdb6482``: 0 P0 / 1 P1 / 1 P2 / 5 P3, not dirty; all fixed but F7 deferred OWNED to a kernel seam -- the timed Loom enter). **H-3d LANDED** (``e3b5ba1e``: the status bar -- `role=status` + the carve, OSC 7 + the cmd mark). **ITS AUDIT ROUND CLOSED** (``3587d8f2``: 0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty; all fixed -- F1 the transcript's `stored_cost` mirror for the cmd mark [the byte budget drifted to zero on eviction], F2 the menu heal over the strip, F3/F4 the witness gaps, F5 the mint-retry cadence). **The H-3 arc is CLOSED. NEXT: H-4** (layouts; HALCYON.md 13.7 -- the read-side fork to the operator first). **Ratified 2026-09-01
  (operator vote): PER-LEAF** — every visible leaf carves a 20px Daylight tag bar
  (§3.2/§4; acme/i3 prior art), NOT the per-container `visible_strips` (that stays
  the tab indicator). `recompute` carves `TAG_BAR_H`=20 off each leaf's inner
  rect top (inside the H-3a-2 ring; >1-leaf-gated; single fullscreen stays
  borderless) → another content-rect reshape + consumer sweep. `TAB_STRIP_H` +
  `TAG_BAR_H` move to `libhalcyon::theme::METRICS`; a new per-pane `tagbar` 9P
  file exposes the strip rect. surface-create extends the ctl `create` verb
  (`create W H role=chrome bind=<pane-id>` — a text change, not a wire break) →
  `Role::Chrome` activated (non-hosted, non-focusable, excluded from Direct).
  halcyond paints name/rule/pills/trail (DISPLAY-only; execution = H-3c) + the
  gated `tag <id> status` verb sets a per-pane status enum → `paint_borders`
  draws the §5.3 status hairline + the §5.4 cast-shadow's dark companion
  (completing H-3a-2's shadow). halcyond needs `T_SPAWN_PERM_CONSOLE_RENDERER`.
  Design in HALCYON.md §13.6 (the "RATIFIED ... PER-LEAF" block).
- **H-3c -- LANDED + CLOSED** (THE GATE; `81515e75` + the audit close
  `826095b6`): obj rendering + the verbs rules engine (BEACON §7's two-tier
  plumber rules file) + context menus (the gated `menu place/dismiss` verb;
  input-redirect-to-halcyond + compositor-owned dismiss — the wedge-proof
  security gate; audit-bearing). Keyboard-first obj interaction. **H-3c-2
  LANDED** (`7b9a457d`, THE EVENT SET: one session + one Loom ring per
  client -- the H-3c close's queued latency defect, fixed before H-3d;
  its audit round CLOSED -- see the H-3b bullet).
- **H-3d -- LANDED** (``e3b5ba1e``; design RATIFIED 2026-09-02 in HALCYON.md
  13.6 `a47b1a57`, the operator present; NEXT: its audit round with the
  H-3c-2 ROUND 2 FOCUS carried, then H-4): the status bar -- `create W H
  role=status` (renderer-gated, one per display, the display width by the
  theme's `status_h`), the display-level carve while it exists, the
  `status_bg` resting fill, the global `statusbar` file; the four slots (ONE
  filled workspace indicator until H-4 -- vote; focused context = tag name ·
  cwd · running-or-last command, the cwd from ut's OSC 7 at every prompt --
  vote, an ABI addition signed off, BEACON.md 12.11 -- and the command from
  ut's `mark k=cmd` (the 12.2 amendment); the condition off the focused
  pane's `status` file; the clock off CLOCK_REALTIME); halcyond paints it on
  its one ring.

Then H-4 (layouts) .. H-7. The parked display-wall work (B/A wall mechanisms +
the GL-parity ledger WSI-DESIGN §8.3 + the blit default flip) queues at H-6.

## Exit criteria status

ROADMAP §11.2 is the v1.0-final bar; per-chunk gates are in the §11.1 table.
H-1's gate: P1 strip property ✓ (host + in-guest) + the kernel fd-devclass
tests ✓ + `ls | cat` byte-clean ✓ (the coreutil-smoke piped legs) + aurora
zero-diff (its parser swallows OSC 1936; the LS-CI family is the standing
witness) + the focused audit round (in flight at the close).

## Build + verify

```bash
THYLACINE_BAKE_CLADE=1 tools/build.sh all && tools/test.sh   # the suite
cd usr && cargo test -p beacon --target aarch64-apple-darwin  # 29 host tests
cd usr && cargo test -p coreutils --lib --no-default-features --target aarch64-apple-darwin
```

## Trip hazards

- **SAVE writes the SESSION tier only** ($HOME/lib/halcyon/layouts/<name>);
  the device tier (/lib/halcyon/layouts/) is halcyond's / the image bake's
  (HALCYON.md 13.7). The two-tier RESTORE precedence (session first, then
  device) is H-4b; do not conflate "two tiers" with "save writes both."
- **`from_render_text` consumes tapestryd's `render_text` dump**, an
  introspection format that is NOT a pinned ABI (it is also read by
  ls-halcyon.exp). A change to `pane::render_text` must keep both consumers in
  sync (the leaf `<id> leaf surface=|empty`, the container `<id> <mode> n=
  active=`, the trailing rect). The parser ignores trailing tokens (rect,
  `hidden`) and the `*` focus marker by design.

- **The consctl render line floor is 67 bytes; every reader ≥ 96** — including
  the kernel's own staging buffers (the H-1a lesson; sweep readers by
  enclosing function).
- **The boot ramfs is FLAT**: pre-pivot, only root + the empty synth mounts
  are listable directories. A smoke leg wanting a bounded `ls` subject uses
  explicit file operands, not a directory.
- **coreutil-smoke's REAP-BEFORE-READ deadlocks the BOOT on child output >
  4096 bytes** (PIPE_BUF) — every new leg budgets its output; the ps-rich leg
  self-guards (measure raw, skip loudly).
- The tier resets at `cons_drain_close` + `cons_test_reset` (NOT "where
  winsize unsets" — winsize never reset on detach). **The consctl fd's
  offset is SHARED down the joey→login→ut chain and advanced by every mode
  WRITE, devdev is NON-SEEKABLE (pread/lseek both refuse), and a fresh
  consctl open fails the attach gate** — the render line is unreachable
  through that fd for a session consumer; the tier readback is the ungated
  `/dev/beacon` leaf (the round-1 P1's fix; fresh-open it, never the
  inherited fd).
- **H-2 obligations from the H-1 audit round**: (a) F10 — ut re-reads +
  re-exports the tier mid-session (a per-prompt `t_pread` is the natural
  shape) so a renderer swap cannot leave a stale `/env/BEACON` defeating
  the kernel's drain-close reset — build it WITH the first rich advertiser,
  where it is testable; (b) F11 — the transcript model decides deliberately
  where async job-reap / idle note output belongs (today it lands inside
  the open PROMPT zone); (c) F7's rich-forced-consctl E2E leg (zone frames
  on the wire through a real advertisement) also needs the rich advertiser
  — u-repl-test covers the zone emission until then, and the
  `ut: beacon <tier> exported` canary covers the transport every session.
- **Session operands must exist POST-PIVOT**: `/version`/`/welcome` are
  boot-ramfs paths and vanish at the pivot. A `; pwd`-sequenced leg passes
  hollowly while the command errors to stderr — pair it with content
  assertions AND the Thylacine-worded no-such-file negative control
  (generic "no such file" matches delve's boot-time history noise).
- **A chord split focuses the NEW (empty) leaf** (`pane.rs` split) — a
  follow-up zoom then targets the empty pane and HIDES the renderer
  (no frame ticks, no CONFIGURE). Focus back first (`Super+Left`).
- **The fixed 4 MiB libthyla-rs heap dies as a SILENT exit(1)** (no_std
  OOM → panic → bare `t_exits(1)`). A binary with a real working set
  declares `ThylaAllocN<{N}>` (lazy demand-zero — physical as touched).
- **A renderer must present BEFORE its first wait**: the scanout is
  first-present-wins and frame ticks reach only visible surfaces — a
  never-presented renderer is dark AND event-starved forever.
- Session-typed command lines stay well under ~32 chars (the #60
  quadratic-echo relay cliff bit at 32 in this scenario's shape).
- SGR never appears inside rich-structured output; the emitting bin forces
  `on = !rich && …`.
- An obj ref is cleaned-absolute-or-no-frame (`coreutils::path::abs`).

## References

`docs/HALCYON.md` (§13 = the bound architecture) · `docs/BEACON.md` (§12 = the
H-1 build card; §12.5 = as-built deviations) · `docs/SYS-FD-DEVCLASS-SPEC.md`
(AS-BUILT) · ARCH §23.5.4 · ROADMAP §11 · `docs/AUDIT-TRIGGERS.md` (the
`SYS_FD_DEVCLASS` row + the LS-8 BEACON TIER addendum) · `docs/JOURNAL.md`
runs 12–15.
