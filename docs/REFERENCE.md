# Thylacine OS — Technical Reference

This document is the **as-built** reference for Thylacine OS. It describes what exists in the tree today, where the relevant code lives, which formal specs pin which invariants, and how the subsystems compose. It is NOT a roadmap and NOT a design document — see `docs/ARCHITECTURE.md` for design intent and `docs/ROADMAP.md` for phased scope.

---

## How to read this

The reference is split by subsystem, one file per layer of the stack. Each file follows the same template:

- **Purpose** — one paragraph on what the layer does and where it sits in the stack.
- **Public API** — every exported function with its contract.
- **Implementation** — structure + invariants + known caveats.
- **Spec cross-reference** — formal modules that pin invariants for this layer.
- **Tests** — which suites exercise the layer and what they cover.
- **Status** — what's implemented today vs. what's stubbed or deferred. Commit hashes cite the landing points.

When a section describes a detail enforced by a spec, the spec's action / invariant name is in `backticks`. When a section cites a file, the form is `path/to/file.c:line` so editors can jump there.

---

## Snapshot

- **Tip**: **P5-corvus-bringup-d landed (substantive `cb0f849` / hash fixup `*(pending)*`) — corvus's real ML-KEM-768 + X25519 hybrid keypair, the hybrid-PKE DEK envelope, and the WRAP + UNWRAP verbs.** Fourth corvus sub-chunk per CORVUS-DESIGN.md §6.4/§6.5. P5-corvus-bringup-c wrapped a 64-byte CSPRNG placeholder and discarded it after AUTH; -d makes the keypair a real **ML-KEM-768 (FIPS 203) + X25519 hybrid** — a 3648-byte plaintext (x25519 sk/pk + ml-kem ek/dk) generated at USER_CREATE — and has AUTH retain it in the mlock'd session slab for the two verbs that consume it. **New crates** on `aarch64-unknown-none`, all pure-Rust + explicit-RNG (no `getrandom`): `ml-kem` 0.2 (RustCrypto FIPS 203), `x25519-dalek` 2 (`static_secrets` + `zeroize`), `sha2` 0.10, `kem` 0.3.0-pre.0 (the `Encapsulate`/`Decapsulate` traits — ml-kem does not re-export them), `rand_core` 0.6; `ThylaRng` adapts `rand_core::RngCore` over `t_getrandom`. **DEK envelope** (CORVUS-DESIGN §6.5 — corvus-owned format, 1217 B): a KEM-DEM hybrid — ML-KEM-768 encapsulate ‖ X25519 ephemeral→static ECDH → a transcript-bound `SHA-256` KEK combiner → AEGIS-256 over the 32-byte DEK, AD-bound to the dataset; secure if either KEM holds. **WRAP (verb_id=10)** — new verb added to CORVUS-DESIGN §6.4 (binding scripture; user signoff) — wraps a DEK under the session user's hybrid public key. **UNWRAP (verb_id=4)**: unwraps with the secret key; the **C-7 ownership gate** (`specs/corvus.tla` `UnwrapOwnerOnly`) fires before any crypto — a cross-user UNWRAP is `PermissionDenied`. Both gate on an in-memory dataset-ownership table (USER_CREATE registers `users/<name>` → `<name>`). **Kernel**: `EXEC_USER_STACK_SIZE` 16 KiB → 256 KiB — ML-KEM-768's FO-transform decapsulate is stack-heavy and the prior 16 KiB user-stack VMA overflowed. Spec: `corvus.tla` header note (WRAP shares Unwrap's gate — no new action — comment-only); corvus.tla re-verified (clean cfg + all 5 buggy counterexample cfgs); the 7 other specs are unchanged by this chunk. Joey drives USER_CREATE michael + susan → AUTH → WRAP → UNWRAP(michael, DEK round-trip) → UNWRAP(susan) → `PermissionDenied` (C-7) → UNWRAP(ghost) → `NotFound` → SESSION_CLOSE. 442/442 PASS × default + UBSan, 0 `EXTINCTION` lines. See `docs/reference/69-corvus-unwrap.md`. **Next**: the remaining corvus verbs + FS persistence; or the deferred P5-secondary-stack-guard. Predecessor: P5-corvus-bringup-c (`24ddb91`/`b72751d`).
- **Earlier tip**: **P5-corvus-bringup-c landed (substantive `24ddb91` / hash fixup `*(pending)*`) — corvus real crypto: Argon2id passphrase verification + AEGIS-256 AEAD + state file format (`CRVS`) + USER_CREATE verb.** Third corvus sub-chunk per CORVUS-DESIGN.md §4.3/§5.1, rebased on P5-el1h-kernel. -b's AUTH accepted any non-empty passphrase; -c makes it verify through an `Argon2id(passphrase, salt)`-derived KEK + AEGIS-256 AEAD-unwrap of a wrapped keypair (tag mismatch → `BadAuth`). Pure-Rust crypto crates pulled into the `aarch64-unknown-none` build: `argon2`, `aegis` with the `pure-rust` feature (the default `aegis` build wraps a C `libaegis` needing libc headers absent on bare-metal), `linked_list_allocator`. corvus gains a 24 MiB static-BSS `#[global_allocator]` heap (Argon2id working memory). New `CorvusUserState` state-file format (`magic 'CRVS'` + version + Argon2id params + salt + nonce + ciphertext + tag; in-memory `Vec` at this sub-chunk — FS persistence later). New **USER_CREATE** verb (verb_id=5): fresh salt/nonce → Argon2id KEK → 64-byte CSPRNG placeholder keypair (ML-KEM-768+X25519 hybrid deferred) → AEGIS-256-wrap → store. Argon2id preset `t_cost=2, m_cost=16 MiB, p=1` (capped below the §4.3 64 MiB default for the contiguous-BSS heap; `m_cost_kib` is a per-record field). Kernel: `SYS_SPAWN_BLOB_MAX` 32 KiB → 256 KiB + `JOEY_BLOB_MAX` 32 KiB → 64 KiB (corvus binary ~109 KiB with embedded crypto). Joey drives `USER_CREATE` → `AUTH(wrong)`→BadAuth → `AUTH(ok)`→token → `SESSION_CLOSE`. 442/442 PASS × default + UBSan, 0 `EXTINCTION` lines, full crypto round-trip. See `docs/reference/68-corvus-crypto.md`. **Next**: P5-corvus-bringup-d (UNWRAP verb + ML-KEM-768 hybrid keypair). Predecessor: P5-el1h-kernel (`e2784c2`/`0e5fd1f`).
- **Earlier tip**: **P5-el1h-kernel landed (substantive `e2784c2` / hash fixup `0e5fd1f`) — the kernel now runs uniformly at EL1h; fixes a pre-existing SMP soundness bug.** Root cause (found during P5-corvus-bringup-c boot testing): the kernel ran normal code at EL1t (`SPSel=0`) and exception handlers at EL1h (`SPSel=1`), but `cpu_switch_context` carries `SP` not `SPSel` — so under SMP work-stealing a thread could resume the `KERNEL_EXIT` exception-exit trampoline in the wrong mode, where `msr SP_EL0` against the currently-selected stack pointer is CONSTRAINED UNPREDICTABLE (`EC=0` Undefined trap; `extinction()` parks only the faulting CPU, so a secondary CPU silently died on every boot). A per-CPU exception stack was also incompatible with migrating a mid-exception thread. **Fix (Model 1)**: run the kernel uniformly at EL1h — one stack bank (`SP_EL1` = the running thread's own kernel stack), `SP_EL0` exclusively the userspace stack, exception frames built on the per-thread kernel stack so they travel with the thread (migration-safe). `cpu_switch_context` + `KERNEL_ENTRY`/`KERNEL_EXIT` are bank-agnostic and unchanged. Scripture: `ARCHITECTURE.md §12.1`/§12.2 made explicit + new invariant **I-21** (§28); `CLAUDE.md §28` mirror. Code: `start.S` (primary + secondary boot to EL1h — assert `SPSel=1` before every SP write, single kernel stack), `vectors.S` (EL1t vector group `0x000`/`0x080` → `VEC_UNEXPECTED`), `userland.S` (`userland_enter` installs the user SP via `msr sp_el0` at EL1h; the P4-Fix157 SPSel dance removed), `sched.c` (`try_steal` skips `kstack_base==NULL` bootstrap threads — audit F2), `main.c`/`smp.c`/`smp.h`/`timer.c`/`test_smp.c`/`thread.c`. Spec: new `specs/sched_ctxsw.tla` (+2 cfgs) pins I-21 — `DUAL_MODE=FALSE` (Model 1) clean, `DUAL_MODE=TRUE` (Model 2) the executable counterexample. Adversarial audit of the exception-entry + boot + scheduler surfaces: no P0; F2 (P1) + F3 (P2) fixed in-chunk; F1 (P2 — secondary-CPU boot stacks lack guard pages) deferred to a focused follow-up (pre-existing gap; bounded risk; pairs with the dedicated overflow-stack hardening item). **Verified**: 442/442 PASS × default + UBSan, **0 `EXTINCTION` lines**, 4/4 CPUs, the full joey→corvus crypto round-trip with cross-CPU work-stealing enabled. See `docs/reference/67-el1h-kernel.md`. **Next**: P5-corvus-bringup-c rebases on top. Predecessor: P5-corvus-bringup-b (`9cf92c6`/`6d9994f`).
- **Earlier tip**: **P5-corvus-bringup-b landed (substantive `9cf92c6` / hash fixup `6d9994f`) — `/sbin/corvus` server loop + binary frame wire codec + AUTH + SESSION_CLOSE verbs.** Second impl chunk of the corvus daemon arc per CORVUS-DESIGN.md §10. Extends -a's hardening-then-exit skeleton into a single-peer Spoor server that loops reading binary-framed verb requests on fd 0, dispatching, writing responses on fd 1; exits 0 on rx EOF. **Wire format** per CORVUS-DESIGN.md §6.4: request = `verb_id u8 + payload_len u16 LE + payload`; response = `status u8 + payload_len u16 LE + payload`. **Two verbs implemented**: AUTH (verb_id=1) parses user_len u8 + user + pass_len u16 LE + passphrase; at this skeleton the passphrase is NOT verified (Argon2id integration at P5-corvus-bringup-c) — presence + valid framing = success; mints 33-byte token (`'s'` + 32 hex chars encoding 16 bytes of CSPRNG entropy from t_getrandom); SESSION_CLOSE (verb_id=3) parses 33-byte token + clears the session if it matches. Every other verb_id returns status=BadFormat. **Session table**: single-slot at this skeleton (`static mut SESSION: Session`). The `user` and `token` fields are set once at install + never mutated until clear (structural C-3 enforcement: there is no setter that mutates `bound_user` in place; spec's BuggyAuthBindingMutate shape is unreachable). One-session-per-peer enforced via the `active` flag (spec's `AuthSuccess` precondition). Accessor wrappers use `core::ptr::addr_of!` / `addr_of_mut!` to avoid Rust 1.77+'s `static_mut_refs` lint. Payload buffer wiped via `t_explicit_bzero` between iterations as defence-in-depth for future verb additions that carry secrets. **libthyla-rs extension**: +4 Rust SVC wrappers (`t_pipe` / `t_read` / `t_write` / `t_close`) + 4 syscall consts (T_SYS_PIPE=8 .. T_SYS_CLOSE=11) + 1 forward-compat const (T_SYS_SPAWN_FULL=25). **Joey orchestration**: extended to create 2 pipes via `t_pipe`, spawn /sbin/corvus via `t_spawn_full` with `[c2s_rd, s2c_wr]` + `T_CAP_LOCK_PAGES | T_CAP_CSPRNG_READ`, drive AUTH ("michael" + skeleton passphrase) → assert OK + 33-byte token starting `'s'` → drive SESSION_CLOSE with that token → assert OK + 0-byte payload → close c2s_wr (triggers corvus EOF) → wait_pid corvus → assert status=0. **Boot log on success**: `corvus: ready (hardening applied; serving /srv/corvus/ over fd 0/1)` → `joey: AUTH ok (token=s3e02730...)` → `joey: SESSION_CLOSE ok` → `corvus: server_loop returned EOF; shutting down clean` → `joey: /sbin/corvus reaped status=0; AUTH + SESSION_CLOSE round-trip verified`. **Spec correspondence** (specs/corvus.tla at `c00de63`/`e5ce30a`): the AUTH and SESSION_CLOSE handlers materialize the `AuthSuccess(p, u)` and `SessionClose(p)` spec actions; `SessionUserImmutable` (C-3) is enforced structurally by the Rust API surface. Other actions / invariants (SessionTransfer, AdminElevate, Unwrap, AdminVerb, UnwrapOwnerOnly, AdminRequiresProcCap, HostownerRequiresConsole) require future sub-chunks. **No new audit-trigger surfaces** — corvus is userspace consuming existing audited syscalls (pipe + read + write + close + spawn-full are at the audited syscall surface). **Testing**: boot path IS the regression. AUTH + SESSION_CLOSE round-trip runs on every boot; failure at any step propagates non-zero exit. The corvus binary grew 5528B → 13304B (+ wire codec + verb handlers + session-table accessors). **Test timing**: total boot ≈ 15s under default + 25s under UBSan; if CI flakes raise to BOOT_TIMEOUT=60 (already standard for UBSan matrix). Test count 442/442 PASS × default + UBSan unchanged. **Files**: `usr/corvus/src/main.rs` (heavy extension; +280 LOC: wire constants + session table + verb handlers + server loop, replaces -a's simple post-hardening exit); `usr/lib/libthyla-rs/src/lib.rs` (+4 wrappers + 5 syscall consts; ~110 LOC); `usr/joey/joey.c` (+pipe-pair + spawn_full + AUTH/SESSION_CLOSE round-trip + close + wait; ~150 LOC); `docs/reference/66-corvus-server.md` (new ~250 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 66); `docs/phase5-status.md` (row); memory updates. **Stage**: structural wire surface live; the two state-machine spec actions (AuthSuccess + SessionClose) materialized in code. **Next chunk**: P5-corvus-bringup-c (Argon2id passphrase verification + state file format `CRVS` + hybrid keypair storage; opens the door to UNWRAP at -d). Predecessor: P5-corvus-bringup-a (`7487054`/`a4bb28c`).
- **Earlier tip**: **P5-corvus-bringup-a landed (substantive `7487054` / hash fixup `a4bb28c`) — `/sbin/corvus` skeleton: hardening syscalls + readiness banner + clean exit. The first impl chunk of the corvus daemon arc per CORVUS-DESIGN.md §10.** New Rust crate `usr/corvus/` (~140 LOC) running 5 v1.0 hardening steps on startup: `t_mlockall(0)` (CAP_LOCK_PAGES; sets PROC_FLAG_MLOCKED) → `t_set_dumpable(0)` (PROC_FLAG_NODUMP one-way) → `t_set_traceable(0)` (PROC_FLAG_NOTRACE one-way) → `t_getrandom(probe, 32, 0)` (CSPRNG-seeded probe per C-15; CAP_CSPRNG_READ) → `t_explicit_bzero(probe, 32)` (RAM-wipe discipline rehearsal per C-5). On any step failure prints `corvus: STEP=<n> FAIL rc=<low-hex>` + exits 1; on success prints `corvus: ready (hardening applied; CSPRNG seeded; no verbs yet)` + exits 0. **Production-shape orchestration**: joey extended to spawn /sbin/corvus via `t_spawn_with_caps("corvus", 6, T_CAP_LOCK_PAGES | T_CAP_CSPRNG_READ)` after the /hello hop; `t_wait_pid` reaps + asserts status=0. **Joey caps bump**: `kernel/joey.c` changed from `rfork(RFPROC, joey_thunk, &args)` to `rfork_with_caps(RFPROC, joey_thunk, &args, CAP_ALL)`. Plain rfork gives `CAP_NONE`; joey needs `CAP_ALL` to delegate any subset to children. `rfork_with_caps`'s AND can only reduce, so I-2 (caps monotonically reduce) is preserved structurally. This matches the production-init pattern — joey is the trusted policy-decision point that distributes caps per child's role. **Rust runtime extension**: `usr/lib/libthyla-rs/src/lib.rs` gained 5 new SVC wrappers (`t_mlockall` / `t_set_dumpable` / `t_set_traceable` / `t_explicit_bzero` / `t_getrandom`) + 5 new syscall-number constants (T_SYS_MLOCKALL=16 .. T_SYS_GETRANDOM=20) + 3 cap constants (T_CAP_HW_CREATE / T_CAP_LOCK_PAGES / T_CAP_CSPRNG_READ). `usr/lib/libt/include/thyla/syscall.h` gained matching `T_CAP_*` macros for the C side (so joey can write `T_CAP_LOCK_PAGES | T_CAP_CSPRNG_READ` symbolically). **Workspace + build wiring**: `usr/Cargo.toml` members += "corvus"; `tools/build.sh` usr_rs_bins += "corvus". **Spec correspondence**: skeleton's contribution = runtime-hardening floor for invariants C-2 + C-5 + C-15. Session-state-machine invariants (C-3 + C-7 + C-11) are spec'd at `specs/corvus.tla` (P5-corvus-spec at `c00de63`/`e5ce30a`) but require the wire codec + verb dispatch deferred to P5-corvus-bringup-b. **Testing**: boot path IS the regression. `joey: /sbin/corvus reaped status=0; hardening verified` line is the test signal; test framework's `Thylacine boot OK` is gated on joey's clean exit. No new kernel-internal tests needed — P5-corvus-syscalls (`0db0dcf`) already covers each syscall in isolation; this chunk composes them end-to-end on a real EL0 Proc with production cap-grant chain. Test count 442/442 unchanged × default + UBSan. **Files**: `usr/corvus/{Cargo.toml, src/main.rs}` (new); `usr/lib/libthyla-rs/src/lib.rs` (+5 wrappers + 5 syscall consts + 3 cap consts); `usr/lib/libt/include/thyla/syscall.h` (+3 T_CAP_* macros); `usr/Cargo.toml` (members +corvus); `tools/build.sh` (usr_rs_bins +corvus); `usr/joey/joey.c` (+corvus spawn-with-caps + wait_pid); `kernel/joey.c` (rfork → rfork_with_caps(CAP_ALL)); `docs/reference/65-corvus-skeleton.md` (new ~180 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 65); `docs/phase5-status.md` (row); memory updates. **Stage**: corvus skeleton boots end-to-end in production-shape orchestration. **Next chunk**: P5-corvus-bringup-b (`/srv/corvus/` Spoor server + binary frame wire codec; AUTH + SESSION_CLOSE verb skeletons). Predecessor: P5-corvus-spec (`c00de63`/`e5ce30a`).
- **Earlier tip**: **P5-corvus-spec landed (substantive `c00de63` / hash fixup `e5ce30a`) — `specs/corvus.tla` + 5 buggy cfgs.** Spec-first per CLAUDE.md: corvus's session state machine + capability arithmetic + authorization touch load-bearing CORVUS-DESIGN.md §9 invariants C-3 (session user-binding immutable across transfer + in-place mutation), C-7 (Unwrap refused for non-owner), C-11 (session-cap × Proc-cap orthogonal authorization), and the §5.5 HostownerRequiresConsole rule. The TLA+ model comes BEFORE the impl. **Session-identity model** uses (creation_proc, bound_user) as the IMMUTABLE pair (creation_proc set at AuthSuccess, preserved across SessionTransfer; owner_proc moves on transfer; bound_user MUST be preserved). The separation lets one invariant catch BOTH in-place mutation AND mutation-during-transfer by checking the immutable pair has a matching origin record. **Dataset convention**: `Datasets == Users` (one dataset per user, named after owner) — sidesteps TLC's config-file limitation on record/function constants; matches the live-system convention (`thylacine/users/<name>` dataset path). **Console-attachment**: monotonic SUBSET Procs; never propagatable across rfork (per §5.5 last paragraph). **Spec actions** (7 legit + 5 bug classes): MarkConsoleAttached / AuthSuccess / SessionClose / SessionTransfer / AdminElevate / Unwrap / AdminVerb; BuggyUnwrapCrossUser / BuggyAuthBindingMutate / BuggyAdminWithoutProcCap / BuggyElevateWithoutConsole / BuggyTransferRebind. **Invariants** (4 + TypeOk): SessionUserImmutable / UnwrapOwnerOnly / AdminRequiresProcCap / HostownerRequiresConsole. **Out of scope** (covered by other specs or runtime discipline): in-RAM secret discipline (C-1/C-2/C-5; runtime, pinned by P5-corvus-syscalls hardening), audit-log encryption (C-9/C-19; file format), Argon2id discipline (C-10/C-12; backend impl), rate-limit + timeouts (C-16/C-17; operational), per-user-stratumd ownership (C-18; cross-component, pinned by composition with Stratum-side API), recovery phrase (C-20; provisioning flow). **TLC verification**: clean cfg verifies at 141109 states / 23652 distinct / depth 18 / <1s wall time. All 5 buggy cfgs find counterexamples at depth 4 in <1s each. **SPEC-TO-CODE.md** extended with full corvus.tla section (state universe, config-result table, action↔impl-target file mapping, invariant↔impl-enforcement table). **No code changes** — spec + cfg + SPEC-TO-CODE updates only. Test count 442/442 unchanged × default + UBSan. **Stage**: spec-gated; P5-corvus-bringup-a (skeleton crate) can start immediately. **Next chunk**: P5-corvus-bringup-a (`usr/corvus/` Rust crate skeleton; startup hardening; idle loop). Predecessor: P5-audit-r15-d (`f306529`/`c819169`).
- **Earlier tip**: **P5-audit-r15-d landed (substantive `f306529` / hash fixup `c819169`) — closes audit F237-F240 (4 P3 polish findings); R15 audit now FUNCTIONALLY CLOSED.** F237: SYS_GETRANDOM partial-fault entropy scrubbing — on uaccess_store_u8 failure mid-stream, best-effort zero the partial range + zero scratch; pre-fix exposed partial CSPRNG bytes. F238: dev9p_attach_client uses spoor_clunk instead of spoor_unref in the priv_alloc-failure rollback path — uniform with all other Spoor-release sites; both paths drop ref=1 → free in this case but clunk is the canonical Plan-9-shape teardown. F239: sys_attach_9p_handler now rejects n_uname > UINT32_MAX explicitly (pre-fix did `(u32)n_uname` blindly, masking high bits); 9P2000.L's n_uname is u32 on the wire. F240: sys_wait_pid_handler status_out partial-write scrubbing — on uaccess_store_u8 failure during the 4-byte int write after the child has been reaped, best-effort zero the partial range; pre-fix exposed torn bytes mixing kernel status with pre-existing user data. No new tests — all four are degenerate-path patches that the existing test suite (442/442 PASS × default + UBSan) regression-covers via the happy paths. **R15 audit status**: 9 of 11 findings closed (F231 R15-a + F233 + F234 R15-b + F230 + F232 R15-c + F237-F240 R15-d). Only 2 latent findings remain (F235 RFFDG, F236 SYS_WALK), each tracked as a precondition for the chunk that introduces the enabling code. **Files**: `kernel/syscall.c` (sys_getrandom_handler F237 + sys_attach_9p_handler F239 + sys_wait_pid_handler F240); `kernel/dev9p.c` (F238); `memory/audit_r15_closed_list.md` (extended); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (row + R15-c section marked LANDED). Test count 442/442 unchanged × default + UBSan. **Stage**: R15 fully closed except 2 deferred-on-enabling-chunks. **Next chunk**: P5-corvus-bringup — the next major architectural chunk; implements /sbin/corvus Thylacine-native daemon using libstratum-crypto via library link per CORVUS-DESIGN.md. Predecessor: P5-audit-r15-c (`9641281`/`0288d98`).
- **Earlier tip**: **P5-audit-r15-c landed (substantive `9641281` / hash fixup `0288d98`) — closes audit F230 (P1) + F232 (P2); per-client `spin_lock_t` around every public 9P client op + `handles.tla` spec extension `RforkWithFds` + new `SpawnFdsRightsMonotonic` invariant.** F230 closure: added `spin_lock_t lock` field to `struct p9_client`. Every public `p9_client_*` op acquires the lock at entry, releases at exit — uniform across handshake/walk/clunk/lopen/lcreate/read/write/getattr/setattr/readdir/statfs/fsync/symlink/mknod/rename/readlink/link/mkdir/renameat/unlinkat + alloc_fid + is_open + inflight + close (~20 functions). `CLIENT_UNLOCK_RET(c, rc)` helper macro keeps early-return paths terse. Lock placement is client-level (not session-level) because the client owns out_buf + next_fid alongside the embedded session by value — one lock covers all mutable client state. Direct callers of `p9_session_*` below the client (typically test code) self-serialize externally. On UP at v1.0 the spin part is a no-op; SMP race detection awaits TSan enablement. No IRQ-mask variant — 9P client paths are syscall/kthread context only. F232 closure: `specs/handles.tla` extended with new action `RforkWithFds(parent, child, h, new_rights)` modeling SYS_SPAWN_WITH_FDS / SYS_SPAWN_FULL fd-inheritance + new bug class `BuggySpawnFdsElevate` skipping the rights-subset guard + new ghost VARIABLE `spawn_inherits` recording (child, kobj, parent_rights) AT inheritance time + new invariant `SpawnFdsRightsMonotonic` cross-checking child handles. Distinct from `RightsCeiling` (bounds by `origin_rights[k]`); `SpawnFdsRightsMonotonic` bounds by parent's CURRENT rights at fork time — catches the F231 shape: parent had {R}, child got {R,W} where origin had {R,W,T}. New cfg `handles_buggy_spawn_fds_elevate.cfg` produces counterexample at depth 4 in <1s. handles.cfg gained `StateConstraint` (Cardinality(handles[p]) ≤ 2 + Cardinality(spawn_inherits) ≤ 2) — the new RforkWithFds × HandleDup combinatorics expanded the state space. Clean run still verifies the full space: 574M states, 26M distinct, depth 26, 39 min, no invariant violations. `specs/9p_client.tla` gained a Concurrency / Lock Discipline header section documenting how the existing buggy variants (TagCollision, OOOMatch, FidAfterClunk, Unbounded) ARE the spec-level coverage of F230's race shapes (mirrors `pipe.tla`'s atomic-actions pattern). **Regression test**: new `9p_client.lock_released_between_ops` verifies the lock value is unlocked between every public op + asserts `alloc_fid` monotonicity. **R15 audit progress**: 5 findings now closed (F231 + F233 + F234 + F230 + F232); 4 pending (F237-F240 at R15-d; F235/F236 latent on RFFDG / SYS_WALK). **Files**: `kernel/include/thylacine/9p_client.h` (+spin_lock_t field; +include); `kernel/include/thylacine/9p_session.h` (updated concurrency note); `kernel/9p_client.c` (+CLIENT_UNLOCK_RET; spin_lock_init in p9_client_init; spin_lock/spin_unlock around every public op); `kernel/test/test_9p_client.c` (+test_9p_client_lock_released_between_ops); `kernel/test/test.c` (+1 forward decl + 1 registry); `specs/handles.tla` (+RforkWithFds + BuggySpawnFdsElevate + spawn_inherits VARIABLE + SpawnFdsRightsMonotonic + StateConstraint; +"spawn" via case); `specs/handles.cfg` + 8 existing buggy cfgs (+BUGGY_SPAWN_FDS_ELEVATE + CONSTRAINT clause); `specs/handles_buggy_spawn_fds_elevate.cfg` (new); `specs/9p_client.tla` (Concurrency / Lock Discipline header section); `memory/audit_r15_closed_list.md` (extended); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (row). Test count 441 → 442; **442/442 PASS × default + UBSan**. **Stage**: 9P client SMP-safe; precondition for P5-corvus-bringup met. **Next chunks**: P5-audit-r15-d (P3 polish F237-F240) OR P5-corvus-bringup (can start since the SMP-safety blocker is closed). Predecessor: P5-audit-r15-b (`1025026`/`f8f7f22`).
- **Earlier tip**: **P5-audit-r15-b landed (substantive `1025026` / hash fixup `f8f7f22`) — closes audit F233 (P1) + F234 (P2); atomic refcounts for Spoor + pipe_ring; SMP correctness for cross-Proc inheritance.** **F233 fix** (`kernel/spoor.c`): all six `c->ref` sites converted to `__atomic_*` ops. `spoor_alloc_internal` uses RELAXED init store (pre-publication safe); `spoor_ref` uses `fetch_add(ACQ_REL)` with pre <= 0 → extinction (use-after-free guard); `spoor_unref` and `spoor_clunk` use `fetch_sub(ACQ_REL)` with pre == 1 → last-drop path; `spoor_free_internal` + `spoor_clone` use ACQUIRE-load for diagnostics. **Semantic shift in spoor_clunk**: dev->close now runs AFTER the atomic decrement (ref=0, storage intact, no concurrent observers possible). Close hooks may still safely access c->aux / c->dev / c->qid — storage is valid until spoor_free_internal. **F234 fix** (`kernel/pipe.c`): same idiom applied to pipe_ring->ref. `pipe_create_internal` uses RELAXED init = 2; `devpipe_close` uses `fetch_sub(ACQ_REL)` with pre == 1 → free the ring. **Standard atomic-refcount idiom**: ACQ_REL ordering ensures all CPUs see prior writes to the resource's fields BEFORE the last-drop free; pre-value capture ensures exactly one CPU sees pre==1 even under concurrent decrement from N CPUs. **No new tests** — fixes are correctness-by-construction (atomic discipline replaces non-atomic); existing test suite (441/441 PASS × default + UBSan) is the regression — if atomic discipline broke single-threaded correctness, those tests would fail. SMP race detection requires ThreadSanitizer (planned for P5-tsan-enable). **Audit progress**: F231 closed at R15-a; F233 + F234 closed at this sub-chunk; F230 + F232 held for R15-c (9P session SMP + spec extension); F237-F240 held for R15-d (P3 polish); F235, F236 deferred to enabling chunks. **No spec extension** — atomic ops are an implementation refinement; the abstract spec (Spoor refcount drops to 0 → free) is unchanged. **Audit posture**: kernel/spoor.c + kernel/pipe.c are existing audit-trigger surfaces. No new attack surfaces — the fix removes a real SMP race without changing the threat model. **Files**: `kernel/spoor.c` (6 ref sites converted; ~25 LOC diff); `kernel/pipe.c` (2 ref sites converted; ~15 LOC diff); `memory/audit_r15_closed_list.md` (extended with F233 + F234 close notes); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (row). Test count 441/441 unchanged × default + UBSan. **Stage**: F231 + F233 + F234 closed; 7 audit findings remain across R15-c (F230 + F232) + R15-d (F237-F240) + deferred (F235 + F236). **Next**: P5-audit-r15-c (9P session per-session lock + atomic next_fid + per-call out_buf; F230 close; precondition for P5-corvus-bringup) OR P5-audit-r15-d (P3 polish bundle: F237-F240). Predecessor: P5-audit-r15-a (`2e7afae`/`b70192e`).
- **Earlier tip**: **P5-audit-r15-a landed (substantive `2e7afae` / hash fixup `b70192e`) — closes audit F231 (rights elevation in SYS_SPAWN_WITH_FDS + SYS_SPAWN_FULL); ACTIVE P1 I-6 violation fixed.** Opus-4.7-max-effort adversarial audit of cumulative Phase 5 surface (since R14 P4-Z) surfaced 11 findings (3 P1 + 4 P2 + 4 P3); F231 was the only ACTIVE bug. **Bug**: child's `handle_alloc` hardcoded `RIGHT_READ|WRITE|TRANSFER`, ignoring parent's slot rights → privilege elevation (parent with READ-only fd spawns child with full READ|WRITE|TRANSFER). **Fix**: `struct spawn_with_fds_args` gains `rights[SYS_SPAWN_MAX_FDS]`; new `sys_bump_inherit_fds` helper captures `slot->rights` at bump-time; child thunk's `handle_alloc` uses captured rights. Both SYS_SPAWN_WITH_FDS and SYS_SPAWN_FULL handlers updated. **Regression test** `sys_spawn_with_fds.child_rights_subset_of_parent`: parent gives child a Spoor with `RIGHT_READ` only via spawn-with-fds; bounded poll (≤1024 sched yields) waits for thunk's handle_alloc to run on child's CPU; asserts `child_slot->rights == parent_slot->rights`. **Audit findings deferred**: F230 (9P session SMP locking — latent, P5-corvus-bringup precondition) + F233 (Spoor atomic refcount — reachable SMP race) + F234 (pipe_ring atomic refcount) held for R15-b/c. F235 (handle table lock — latent on RFFDG) + F236 (derived-Spoor UAF — latent on SYS_WALK) deferred to enabling chunks. F232 (spec extension for spawn-fd-inheritance) + F237-F240 (P3 polish) held for R15-b/c/d. **Closed list**: `memory/audit_r15_closed_list.md`. **Files**: `kernel/syscall.c` (struct + helper + handlers); `kernel/test/test_sys_spawn_with_fds.c` (+1 regression test); `kernel/test/test.c` (+1 forward decl + 1 registry); `memory/audit_r15_closed_list.md` (new). Test count 440 → 441; **441/441 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: F231 closed; cumulative R15 audit close in progress. **Next**: P5-audit-r15-b (atomic refcounts; F233 + F234) OR P5-audit-r15-c (9P session SMP; F230 — precondition for P5-corvus-bringup) OR P5-audit-r15-d (P3 polish bundle). Predecessor: P5-spawn-full (`d1b0d64`/`a91a6e2`).
- **Earlier tip**: **P5-spawn-full landed (substantive `d1b0d64` / hash fixup `a91a6e2`) — `SYS_SPAWN_FULL` (= 25) combining fd inheritance + cap-subset in one syscall; the fourth and most general spawn variant.** ABI: `SYS_SPAWN_FULL(name_va, name_len, fd_list_va, fd_count, cap_mask) → child_pid/-1`. Unions SYS_SPAWN_WITH_FDS (KOBJ_SPOOR fd inheritance ≤ 16; child slots 0..fd_count-1) with SYS_SPAWN_WITH_CAPS (cap_mask AND'd with parent's caps via rfork_with_caps). **Reuses** sys_spawn_with_fds_thunk + spawn_with_fds_args; only difference vs SYS_SPAWN_WITH_FDS is rfork_with_caps instead of rfork. The thunk doesn't know about caps — cap arithmetic happens inside rfork_with_caps BEFORE the thunk runs. **Needed at P5-corvus-bringup** where joey spawns /sbin/corvus with a pipe pair (login communication) + CAP_LOCK_PAGES + CAP_CSPRNG_READ — both inheritance modes in one call. **Four spawn variants** now coexist (SYS_SPAWN, SYS_SPAWN_WITH_FDS, SYS_SPAWN_WITH_CAPS, SYS_SPAWN_FULL); each call site reads more clearly with the variant matching its needs. **Test pattern caveat**: cap inspection via proc_find_by_pid is race-free (caps stable for Proc lifetime); fd inspection from a parent thread races with the thunk's handle_alloc on the child CPU — resolved by relying on indirect coverage (clean exit + /stub-driver_round_trip end-to-end). Documented mid-chunk discovery. **5 new kernel-internal tests** in `kernel/test/test_sys_spawn_full.c`: happy_path_fds_and_caps (verifies caps directly; verifies fds indirectly via clean exit) + zero_count_zero_mask_succeeds (≡ SYS_SPAWN) + 3 rejection paths (oversize_fd_count + bad_fd + missing_binary). libt stub `t_spawn_full(name, name_len, fds, fd_count, cap_mask) → pid` (5-arg inline-asm using x0..x4 + x8). **No spec extension** — pure union of existing primitives. **Audit posture**: extends kernel/syscall.c (existing audit-trigger surface). No new attack surfaces — fd-list + cap arithmetic reuse existing audited primitives. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_FULL=25 + doc-comment); `kernel/syscall.c` (+~115 LOC: sys_spawn_full_for_proc + sys_spawn_full_handler + 1 dispatcher case); `kernel/test/test_sys_spawn_full.c` (new; 5 tests; ~145 LOC); `kernel/test/test.c` (+5 forward decls + 5 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_FULL=25 + t_spawn_full inline-asm stub); `docs/reference/64-sys-spawn-full.md` (new; ~170 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 64); `docs/phase5-status.md` (row); memory updates. Test count 435 → 440; **440/440 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: spawn surface complete for P5-corvus-bringup. **Next chunks**: P5-stratumd-stub-c (long-running stub serving multiple sequential clients — small) OR P5-stratumd-stub-d (pivot/chroot kernel mechanism — substantial; gated on string-path walk subsystem) OR P5-close audit (R15-class adversarial review of the cumulative Phase 5 surface; the syscall surface is now substantial — 25 syscalls). Predecessor: P5-spawn-caps (`e3d34e3`/`a5313f1`).
- **Earlier tip**: **P5-spawn-caps landed (substantive `e3d34e3` / hash fixup `a5313f1`) — `SYS_SPAWN_WITH_CAPS` (= 24) for granting cap-subset to a spawned child.** ABI: `SYS_SPAWN_WITH_CAPS(name_va, name_len, cap_mask) → child_pid/-1`. Wraps the existing `rfork_with_caps` (P4-Ic3); child's caps = `parent->caps & cap_mask`. **Structural enforcement of ARCH I-2 / I-6** (caps monotonically reduce): even if cap_mask sets bits the parent doesn't hold, the AND clamps to parent's actual caps — extra bits silently dropped. Needed at P5-corvus-bringup so joey can spawn /sbin/corvus with `CAP_LOCK_PAGES | CAP_CSPRNG_READ` (subset of joey's CAP_ALL). **Reuses** the existing sys_spawn_thunk + spawn_args from SYS_SPAWN; only difference is `rfork_with_caps` vs `rfork`. **Why a third spawn variant** (alongside SYS_SPAWN + SYS_SPAWN_WITH_FDS): each variant's intent is explicit at the call site; cost of more switch entries < benefit of clear opt-in semantics + no ABI churn when adding capabilities. Future SYS_SPAWN_FULL lands when a use case needs both fds + caps (likely at P5-corvus-bringup). **Cap arithmetic test pattern**: tests use proc_find_by_pid to read child->caps BEFORE wait_pid (caps stable for Proc lifetime; race-free read). **5 new kernel-internal tests** in `kernel/test/test_sys_spawn_with_caps.c`: happy_path_zero_mask (CAP_NONE → child caps == 0) + happy_path_subset_of_parent (CAP_LOCK_PAGES → child has exactly CAP_LOCK_PAGES, kproc has CAP_ALL) + clamps_to_parent (mask=0xFFF...FF → child clamped to CAP_ALL; no spurious bits) + rejects_missing_binary + rejects_oversize_name. **libt stub** `t_spawn_with_caps(name, name_len, cap_mask) → pid`. **No spec extension** — cap arithmetic is already covered by `specs/handles.tla::RforkWithCaps` (P4-Ic3); this syscall just exposes the existing primitive. **Audit posture**: extends kernel/syscall.c (existing audit-trigger surface). No new attack surfaces — name uaccess reuses uaccess_load_u8 + UACCESS_USER_VA_TOP + fault-fixup; cap arithmetic delegated to audited rfork_with_caps. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_WITH_CAPS=24 enum + doc-comment); `kernel/syscall.c` (+~75 LOC: sys_spawn_with_caps_for_proc + sys_spawn_with_caps_handler + 1 dispatcher case); `kernel/test/test_sys_spawn_with_caps.c` (new; 5 tests; ~130 LOC); `kernel/test/test.c` (+5 forward decls + 5 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_WITH_CAPS=24 + t_spawn_with_caps inline-asm stub); `docs/reference/63-sys-spawn-with-caps.md` (new; ~165 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 63); `docs/phase5-status.md` (row); memory updates. Test count 430 → 435; **435/435 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: corvus startup precondition syscalls all present (CAP_LOCK_PAGES + CAP_CSPRNG_READ can be granted via SYS_SPAWN_WITH_CAPS at P5-corvus-bringup). **Next chunks**: P5-stratumd-stub-c (long-running stub serving multiple sequential clients — small) OR P5-spawn-full (combined fds + caps when use case needs it) OR P5-stratumd-stub-d (pivot/chroot kernel mechanism — substantial; gated on string-path walk subsystem). Predecessor: P5-stratumd-stub-bringup-b (`73784b4`/`904553c`).
- **Earlier tip**: **P5-stratumd-stub-bringup-b landed (substantive `73784b4` / hash fixup `904553c`) — `SYS_SPAWN_WITH_FDS` (= 23) + new `/stub-driver` userspace binary that drives the full production-shape orchestration at EL0.** ABI: `SYS_SPAWN_WITH_FDS(name_va, name_len, fd_list_va, fd_count) → child_pid/-1`. Extends `SYS_SPAWN` with explicit fd inheritance: the listed fds (`KOBJ_SPOOR`-only at v1.0; ≤ SYS_SPAWN_MAX_FDS = 16; child fd 0..fd_count-1) are installed in the child's handle table BEFORE exec_setup. **Refcount discipline (load-bearing)**: handler bumps spoor_ref for each listed fd; parent retains its own holds; child gets its own independent refs via handle_alloc (which consumes the bumped ref). Failure paths drop all bumped refs symmetrically. **Why separate syscall instead of extending SYS_SPAWN**: would ABI-break the libt stub (x2/x3 garbage suddenly meaningful). Separate syscall keeps `SYS_SPAWN` shape stable; intent is explicit at the call site. **Why KOBJ_SPOOR-only at v1.0**: ARCH I-5 prohibits MMIO/IRQ/DMA cross-Proc transfer; Burrow inheritance held until v1.x. **Why explicit-list, not Plan 9 RFFDG**: cleaner for "child gets these specific fds" (joey's typical case); real SYS_RFORK with RFFDG lands when a v1.x workload (shell pipelines, musl clone() translation) needs the general primitive. **`/stub-driver`** (~110 LOC, new userspace binary): does the full production-shape orchestration entirely from EL0 — t_pipe × 2 → t_spawn_with_fds("stratumd-stub", [c2s_rd, s2c_wr], 2) → t_close driver-side copies → t_attach_9p → t_mount → t_unmount → t_close all → t_wait_pid → t_exits(0). **Kernel test** (`kernel/test/test_stub_driver.c`, ~85 LOC): minimal — just load + rfork-exec /stub-driver + wait_pid + assert status=0. The driver does all the real work. **Rejection tests** (`kernel/test/test_sys_spawn_with_fds.c`, ~110 LOC): sys_spawn_with_fds.rejects_oversize_fd_count + rejects_bad_fd + rejects_non_spoor_fd (uses Burrow handle) + rejects_missing_binary + zero_count_succeeds (fd_count=0 ≡ SYS_SPAWN). **Boot log on success**: `/stub-driver size=17280 → exec; driver orchestrates stratumd-stub itself` + `stratumd-stub: serving on fd 0 (rx) + fd 1 (tx)` + `stratumd-stub: EOF on rx; exit 0` + `stub-driver: PASS` + `/stub-driver reaped pid=N status=0 — SYS_SPAWN_WITH_FDS production-shape orchestration verified end-to-end`. Proves the production-shape joey-driven-from-userspace flow works end-to-end at EL0 with no kernel-supervised setup. **No spec extension** — pure composition over existing spawn + handle + pipe primitives; fd-inheritance refcount discipline is local (bump + transfer-to-child or unref-on-rollback). **Audit posture**: extends kernel/syscall.c (existing audit-trigger surface). No new attack surfaces — name uaccess + fd-list uaccess use existing uaccess_load_u8 + UACCESS_USER_VA_TOP bound + fault-fixup; handle_get/handle_alloc/spoor_ref/spoor_unref are existing audited primitives. **Sub-chunk arc**: a (userspace responder + 2-Proc kernel-supervised demo) + **b (this — SYS_SPAWN_WITH_FDS + /stub-driver production-shape)** → c=long-running stub serving multiple clients → d=pivot/chroot kernel mechanism → e=full joey-in-production. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_WITH_FDS=23 + SYS_SPAWN_MAX_FDS=16); `kernel/syscall.c` (+~170 LOC: spawn_with_fds_args + sys_spawn_with_fds_thunk + sys_spawn_with_fds_for_proc + sys_spawn_with_fds_handler + 1 dispatcher case); `kernel/test/test_sys_spawn_with_fds.c` (new; 5 tests; ~120 LOC); `kernel/test/test_stub_driver.c` (new; 1 test; ~85 LOC); `kernel/test/test.c` (+7 forward decls + 7 registry entries); `kernel/CMakeLists.txt` (+2 test sources); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_WITH_FDS=23 + T_SPAWN_MAX_FDS macro + t_spawn_with_fds inline-asm stub); `usr/stub-driver/{stub-driver.c, CMakeLists.txt}` (new); `usr/CMakeLists.txt` (+add_subdirectory); `tools/build.sh` (+"stub-driver" in usr_bins); `docs/reference/62-sys-spawn-with-fds.md` (new; ~250 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 62); `docs/phase5-status.md` (row); memory updates. Test count 424 → 430; **430/430 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: production-shape userspace orchestration unblocked. **Next chunks**: P5-stratumd-stub-c (long-running stub serving multiple sequential clients — small) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for cap-subset; needed at P5-corvus-bringup) OR P5-stratumd-stub-d (pivot/chroot kernel mechanism — substantial). Predecessor: P5-stratumd-stub-bringup-a (`479d997`/`5d14d7f`).
- **Earlier tip**: **P5-stratumd-stub-bringup-a landed (substantive `479d997` / hash fixup `5d14d7f`) — userspace 9P responder `/stratumd-stub` + kernel-supervised 2-Proc demo (stub + `/attach-probe` wired via 2 pipe pairs).** First Phase 5 test running TWO userspace Procs cooperating; until this chunk, every userspace 9P test used a kernel-thread responder. Proves a USERSPACE process can be the 9P server (production shape for `stratumd-system`/`stratumd-michael`). **`usr/stratumd-stub/stratumd-stub.c`** (~170 LOC): reads framed 9P Tmsgs from fd 0, builds canonical Rversion / Rattach / Rclunk / fallback Rlerror responses, writes to fd 1; loops until EOF on rx → exit 0. Wire encoding inlined (no libc available); 4-byte size prefix + 1-byte type + 2-byte tag + body. Cpio entry name `stratumd-stub`. **`kernel/test/test_stratumd_stub.c`** (~210 LOC): pipe_create × 2 → 4 Spoors; rfork stub Proc with c2s_rd@fd0 + s2c_wr@fd1 pre-installed; rfork client (/attach-probe) Proc with c2s_wr@fd0 + s2c_rd@fd1 pre-installed; wait_pid both; collects PIDs in any order; asserts both status=0. **Refcount discipline (load-bearing)**: each pipe_create Spoor's ref=1 TRANSFERRED (not bumped) to its owner's handle table via handle_alloc; boot retains 0 refs after rfork thunks. **Why kernel-supervised, not joey-driven**: in production, joey would orchestrate from userspace — but that needs RFFDG (Plan 9 flag — copy fd table at fork) or SYS_SPAWN_WITH_FDS. Held as P5-stratumd-stub-bringup-b. This chunk demonstrates the wiring works end-to-end at EL0 ↔ EL0 via the kernel's 9P client + dev9p + spoor-transport + pipe stack. **Build wiring**: `usr/stratumd-stub/{stratumd-stub.c, CMakeLists.txt}` + `usr/CMakeLists.txt` (+add_subdirectory) + `tools/build.sh` (+"stratumd-stub" in usr_bins) + `kernel/CMakeLists.txt` (+test_stratumd_stub.c) + `kernel/test/test.c` (+1 forward decl + 1 registry entry `userspace.stratumd_stub_round_trip`). **No spec extension** — pure composition over already-spec'd 9P client + pipe + handle layers; two-userspace-Proc pattern introduces no new invariants. **Audit posture**: kernel/test/test_stratumd_stub.c is test-only (not on audit-trigger surface). The userspace stratumd-stub binary at EL0 uses existing syscalls (t_read/t_write/t_putstr/t_exits) with valid arg shapes; no new attack surface. **Boot log on success**: `/stratumd-stub size=17824 + /attach-probe size=15144 — wiring 2 userspace Procs via 2 pipes` then `stratumd-stub: serving on fd 0 (rx) + fd 1 (tx)` + `attach-probe: PASS` + `stratumd-stub: EOF on rx; exit 0` + `stratumd-stub pid=N1 status=0 + attach-probe pid=N2 status=0 — userspace 9P server end-to-end verified`. Test count 423 → 424; **424/424 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Sub-chunk arc** (P5-stratumd-stub-bringup): a=this (userspace responder + 2-Proc kernel-supervised demo) → b=RFFDG or SYS_SPAWN_WITH_FDS (joey can drive orchestration from userspace) → c=long-running stub serving multiple sequential clients → d=pivot/chroot kernel mechanism (mount becomes Territory root) → e=full joey-in-production orchestration. **Stage**: foundation of P5-stratumd-stub-bringup arc landed; next sub-chunk unblocks joey-driven orchestration. **Next chunks**: P5-stratumd-stub-b (RFFDG or SYS_SPAWN_WITH_FDS — extends rfork to copy parent's fd table; smallest step toward joey-driven production shape) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for cap-subset; gated on use case at P5-corvus-bringup) OR Stratum-side parallel work per STRATUM-API-V1.md. Predecessor: P5-stratum-api-spec (`bd19cf7`/`4d6b3d3`).
- **Earlier tip**: **P5-stratum-api-spec landed (substantive `bd19cf7` / hash fixup `4d6b3d3`) — `docs/STRATUM-API-V1.md` (~710 LOC): complete Thylacine-side spec for the 5 Stratum API additions Thylacine needs at v1.0 + 1 v1.x deferral.** Per ask: motivation (cross-ref to CORVUS-DESIGN.md §13.x), CLI shape, wire frame layouts where applicable, behavioural contract, error codes, test matrix, open questions, Stratum-side audit-trigger surfaces. **Priority order**: A1 `--bind-pool-serial` + `pool_serial` superblock field (smallest; unblocks P5-stratumd-bringup-real) → A4 corvus notify Spoor / DEK eviction (small) → A2 multi-stratumd-per-pool with `--datasets-allowed` (medium; unblocks P5-login) → A3 corvus UNWRAP wire (medium; unblocks P5-corvus-bringup) → A5 snapshot rollback compromise marking (medium; closes C-13) → A6 per-dataset key rotation (deferred to v1.x). **Hard-dependency tier**: A1 alone unblocks the boot-into-Stratum-mounted-root milestone; A2+A3+A4 needed for the post-pivot stack (corvus + per-user stratumd + logout flow). Stratum coordination protocol enumerated (§11): Michal reviews → pushes back as needed → doc updates with negotiated shape → Stratum chunks the work → Thylacine consumes per chunk. **Thylacine-side bilateral commitments** (§10): pool-serial discipline (installer generates + supplies), multi-stratumd discipline (per-user dataset patterns), wire discipline (session token via SYS_GETRANDOM + mlock'd RAM only), eviction discipline (joey + login coordinate SESSION_CLOSE), rollback marker discipline (corvus invokes synchronously with passphrase rotation), audit cooperation (cross-cutting integration audits). Cross-referenced from CORVUS-DESIGN.md §13. **No code; scripture only.** Tests unchanged at 423/423 PASS × default + UBSan. **Stage**: Stratum-side parallel-trackable. **Next Thylacine-side chunks**: P5-stratumd-stub-bringup (kernel-internal supervisor demonstration with stratumd-stub binary — no Stratum dependency; the architectural moment) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for cap-subset; gated on a use case). Predecessor: P5-spawn-wait (`335d87d`/`3915559`).
- **Earlier tip**: **P5-spawn-wait landed (substantive `335d87d` / hash fixup `3915559`) — `SYS_SPAWN` (= 21) + `SYS_WAIT_PID` (= 22) syscalls; `/joey` now orchestrates `/hello` end-to-end in the production boot path.** ABI: `SYS_SPAWN(name_va, name_len) → child_pid/-1` (name ≤ SYS_SPAWN_NAME_MAX = 64; binary ≤ SYS_SPAWN_BLOB_MAX = 32 KiB; combined `rfork(RFPROC)` + `exec` against a devramfs-named binary; child inherits no caps at v1.0); `SYS_WAIT_PID(status_out_va) → reaped_pid/-1` (`status_out_va` may be NULL; per-byte `uaccess_store_u8` × 4 for the int write; mirrors `kernel/proc.c::wait_pid`). **Why combined fork+exec at v1.0**: a real `SYS_RFORK` returning 0-in-child/pid-in-parent needs COW + child-context restoration, neither of which exists yet. Plan 9's `rfork(RFPROC|RFEXEC)` idiom sidesteps both: child gets a fresh address space populated from the named binary. A separate `SYS_RFORK` lands when a v1.x workload requires it. **Lifetime discipline**: `SYS_SPAWN`'s thunk inherits ownership of two kmalloc'd buffers (blob copy + args struct); thunk frees both after `exec_setup`. On rfork failure, the parent frees them. The 8-aligned blob copy is mandatory (same R5-G F61 alignment precondition as joey + `kernel/test/test_userspace_ramfs.c`). **Factoring**: `int sys_spawn_for_proc(struct Proc *p, const char *name, size_t name_len)` extracted as a non-static inner so kernel-internal tests can drive it with kernel-resident names; SVC wrapper does user-VA validation + name copy then calls the inner. `sys_wait_pid_handler` not extracted — its only non-trivial step is the per-byte uaccess store; tests call `wait_pid()` directly. **Joey live demonstration**: `usr/joey/joey.c` extended from "exit 0" to "spawn /hello, wait_pid, verify status=0, print 'orchestration verified'". Boot log shows the four interleaved lines (`joey: rforking child for /joey... ; joey: hello from /joey... ; joey: spawned /hello pid=N ; hello from /hello... ; joey: /hello reaped status=0`). **Userspace libt stubs**: `t_spawn(name, name_len) → pid/-1`, `t_wait_pid(int *status) → pid/-1`, `T_SPAWN_NAME_MAX = 64u`. **7 new kernel-internal tests** in `kernel/test/test_sys_spawn.c`: sys_spawn.happy_path (spawn "hello" + wait_pid + status=0) + 5 rejection paths (null_name / zero_len / oversize_name / missing_binary / embedded_nul) + sys_wait_pid.no_children_returns_neg1. Each test calls `drain_zombies()` first for deterministic ordering. **No spec extension** — the orchestration shape is pure composition over `specs/scheduler.tla` (rfork + ready + wait_pid wakeup) + `specs/handles.tla` (fresh address space, no fd-table sharing). **Audit posture**: extends `kernel/syscall.c` (existing audit-trigger surface); the kmalloc-blob-copy + free pattern is symmetric and exercised by 7 tests + the live boot path. No new attack surfaces — the name uaccess uses the existing `uaccess_load_u8` + `UACCESS_USER_VA_TOP` bound; the rfork machinery is the existing audited primitive. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN=21 + SYS_WAIT_PID=22 enum entries + SYS_SPAWN_NAME_MAX + SYS_SPAWN_BLOB_MAX macros); `kernel/syscall.c` (+devramfs.h + elf.h + exec.h includes; ~110 LOC for sys_spawn_thunk + sys_spawn_for_proc + sys_spawn_handler + sys_wait_pid_handler + 2 dispatcher cases); `kernel/test/test_sys_spawn.c` (new; 7 tests; ~110 LOC); `kernel/test/test.c` (+7 forward decls + 7 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN=21 + T_SYS_WAIT_PID=22 + T_SPAWN_NAME_MAX macro + t_spawn + t_wait_pid stubs); `usr/joey/joey.c` (extended to orchestrate /hello + tiny in-place itoa_dec); `docs/reference/60-sys-spawn-wait.md` (new; ~230 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 60); `docs/phase5-status.md` (row). 416 → 423 PASS × default + UBSan (BOOT_TIMEOUT=60 for UBSan). **Stage**: P5-stratumd-stub-bringup is now the natural next chunk — joey can spawn a stratumd-stub binary the same way it spawns /hello today. **Next chunks**: P5-stratumd-stub-bringup (the architectural moment — userspace stratumd-stub binary serving a tiny 9P tree; joey forks it, attaches, mounts as root, verifies) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for granting subset of parent's caps; needed for corvus startup but can wait until P5-corvus-bringup) OR P5-stratumd-multi (Stratum-side, Michal-owned, parallel-trackable). Predecessor: P5-joey-from-ramfs (`2abe83f` / `c401ccf`).
- **Earlier tip**: **P5-joey-from-ramfs landed (substantive `2abe83f` / hash fixup `c401ccf`) — `/joey` is now a real userspace binary built from `usr/joey/` and shipped in the initrd cpio, replacing the 9-instruction kernel-embedded blob. Structural-not-behavioural change unblocking the P5-stratumd-bringup arc.** Files: `usr/joey/joey.c` (new; prints a banner via t_putstr, exits 0) + `usr/joey/CMakeLists.txt` (new) + `usr/CMakeLists.txt` (+add_subdirectory) + `tools/build.sh::build_ramfs` (usr_bins += "joey") + `kernel/joey.c` (build_init_elf + embedded `g_joey_program[]` removed; `joey_run` now calls `devramfs_lookup("joey", ...)` + copies into an 8-aligned static buffer `g_joey_elf_blob` before exec_setup). Reason for the static-buffer copy: cpio newc data is 4-aligned; `kernel/elf.c::elf_load` rejects 8-misaligned Ehdr per R5-G F61. Same pattern as `kernel/test/test_userspace_ramfs.c`. JOEY_BLOB_MAX = 32 KiB (`/joey` is 12744 bytes at v1.0). **Boot diagnostic** distinguishes initrd vs embedded: prior boots said `9-instr hello blob`; now says `<N> byte ELF from initrd`. **Userspace banner** also changed: prior was `hello\n`; now `joey: hello from /joey (real userspace binary, loaded from ramfs)\n` — readable confirmation of source in the boot log. **No new tests**: the boot path (boot_main → joey_run → /joey exits 0 → "Thylacine boot OK" banner) IS the regression. If /joey breaks, joey_run extincts, banner never prints, tools/test.sh reports failure. **Failure modes** documented in `docs/reference/59-joey-from-ramfs.md`: /joey missing from cpio / zero size / exceeds 32 KiB / exec_setup rc!=0 / EL0 fault. **No spec extension** — orchestration shape unchanged (rfork → exec_setup → userland_enter → wait_pid); only the source of the ELF moved. **Audit posture**: kernel/joey.c is not on the audit-trigger surface; it's an init-time orchestration helper. The cpio-data → 8-aligned-copy pattern reuses the test-side approach already in-tree. **Stage**: P5-stratumd-bringup arc unblocked. /joey can now do arbitrary userspace work — the next chunk extends it from "prints + exits" to "long-running supervisor" (forks stratumd-system, mounts /sysroot via 9P, pivots root). 416/416 PASS × default + UBSan (BOOT_TIMEOUT=60 for UBSan). **Next chunks**: P5-joey-orchestrator (joey-as-supervisor: loop, fork children, wait) OR P5-stratumd-stub-bringup (userspace stratumd-stub + joey forks it + kernel-mounts-as-root mechanism — the architectural moment) OR P5-stratumd-multi (Stratum-side, Michal-owned, parallel-trackable). Predecessor: P5-corvus-syscalls (`0db0dcf` / `d10d4ee`).
- **Earlier tip**: **P5-corvus-syscalls landed (substantive `0db0dcf` / hash fixup `d10d4ee`) — 5 v1.0 hardening syscalls + 2 new caps + per-Proc flags + public kernel CSPRNG API; the foundational scaffold for corvus + per-user stratumd startup per CORVUS-DESIGN.md §4.1.1.** ABI: `SYS_MLOCKALL(flags)→0/-1` (CAP_LOCK_PAGES; sets PROC_FLAG_MLOCKED) + `SYS_SET_DUMPABLE(0/1)→0/-1` (one-way to 0; sets PROC_FLAG_NODUMP) + `SYS_SET_TRACEABLE(0/1)→0/-1` (one-way to 0; sets PROC_FLAG_NOTRACE) + `SYS_EXPLICIT_BZERO(buf_va, len)→0/-1` (per-byte uaccess_store_u8; ≤4 KiB) + `SYS_GETRANDOM(buf_va, len, flags)→bytes/-1` (CAP_CSPRNG_READ; ARM RNDR-backed; ≤4 KiB). Syscall numbers 16-20; libt stubs `t_mlockall`/`t_set_dumpable`/`t_set_traceable`/`t_explicit_bzero`/`t_getrandom`. **New caps**: `CAP_LOCK_PAGES` (1<<1) + `CAP_CSPRNG_READ` (1<<2); CAP_ALL updated (kproc has all three). **Per-Proc flags**: `proc_flags` u32 + 4-byte pad added to struct Proc (128→136 bytes; static_assert updated); `PROC_FLAG_NODUMP/NOTRACE/MLOCKED` defined. **Kernel CSPRNG API**: new `kernel/include/thylacine/random.h` exposing `kern_random_bytes(buf, len) → bytes/-1` + `kern_random_seeded() → bool`. `kernel/random.c` refactored to extract `kern_random_bytes` from `devrandom_read`; `devrandom_read` is now a thin wrapper. **Forward-compat scaffolding**: at v1.0 the per-Proc flags don't gate anything because the enforcing subsystems (swap, core dumps, debug Spoors) don't exist yet. corvus calls the syscalls at startup anyway so its startup is forward-compatible; flags enforce when swap/core-dumps/debug-Spoors land in v1.x. **One-way semantics**: `SYS_SET_DUMPABLE(1)` on a Proc that already has PROC_FLAG_NODUMP is REFUSED (-1). Same for `SYS_SET_TRACEABLE`. Idempotent for the same-state direction. **6 new kernel-internal tests** in `kernel/test/test_sys_corvus.c`: sys_mlockall.cap_gate + sys_set_dumpable.one_way_to_zero + sys_set_traceable.one_way_to_zero + sys_corvus_caps.kproc_has_new_caps + kern_random.seeded_returns_true_on_qemu + kern_random.bytes_produces_nonzero (32-byte read produces ≥1 non-zero byte + two reads differ + zero-length is no-op + NULL/negative rejected). **No spec extension** — these are forward-compat hardening syscalls; their semantic invariants (mlock pages don't page out; NODUMP Procs don't core dump; NOTRACE Procs reject debug attach) become spec-pinned when their enforcing subsystems land in v1.x. **Audit posture**: extends kernel/syscall.c (existing trigger surface) + adds kernel/random.c (CSPRNG public API). No new attack surfaces — kern_random_bytes is internal-API-only-now; SYS_GETRANDOM's user-VA bounce uses the existing uaccess_store_u8 with the existing UACCESS_USER_VA_TOP bound + fault-fixup. Test count 410 → 416; **416/416 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: P5-corvus-bringup is now unblocked — all syscalls corvus's startup needs are landed. **Next chunks**: P5-stratumd-bringup (joey forks stratumd-system; kernel mounts /sysroot; pivot root — first big architectural integration) OR P5-corvus-bringup (implement /sbin/corvus from scratch using these syscalls — needs P5-stratumd-bringup for state-dir on Stratum-mounted /var/lib/corvus) OR P5-stratumd-multi (Stratum-side, Michal-owned, can parallel-track). Predecessor: P5-corvus-design (`fc9cf47` / `cf45eca`).
- **Earlier tip**: **P5-corvus-design landed (substantive `fc9cf47` / hash fixup `cf45eca`) — binding scripture for the post-pivot stack: `docs/CORVUS-DESIGN.md` (~930 LOC) + cross-references from ARCH §5 + §11.2b + §14.3a + §15.2/§15.3/§15.4 + §28 + §29 + ROADMAP §7.2a.** Captures the six v1.0 design decisions (D1: classic ESP, D2: integrity-only system pool with HKDF boot-key device binding, D3: always-encrypted user datasets, D4: Order-C boot with per-user stratumd, D5: no-UID-0 hostowner + recovery phrase, D6: explicit v1.0 acceptance of no-boot-chain-integrity; Secure Boot v1.1). **Architectural reframing from earlier draft**: corvus is a Thylacine-native daemon, NOT a port of Stratum's janus — draws design influence from factotum, janus, FileVault, ZFS, fscrypt. State file magic `CRVS` distinct from `JPAS`. Binary frame wire (no parsing ambiguity; passphrases never on text wire; `explicit_bzero` discipline). 20 invariants C-1..C-20 published. **Hardened against an Opus-4.7-max-effort adversarial security audit** before commit (21 findings: 3 P0s + 7 P1s + 8 P2s + 3 P3s; all resolved inline or explicitly deferred to v1.x). Key audit-driven changes: per-user stratumd Option A (F1; multi-stratumd-per-pool); HKDF boot-key pool-serial binding (F2); explicit acceptance of no-boot-chain-integrity defense at v1.0 (F3); v1.0 hardening syscalls enumerated for ARCH §11 (F5; sys_mlockall, sys_set_dumpable, sys_set_traceable, sys_explicit_bzero, sys_getrandom); 24-word BIP-39 recovery phrase (F6); fresh salt+nonce+params on every rotation (F7); binary frame wire (F8); orthogonal session×Proc cap authorization rule, explicit (F10); drop "forward-secret" misnomer (F14). **Implementation arc**: 7 chunks (P5-corvus-design + P5-stratumd-multi [Stratum-side] + P5-corvus-syscalls + P5-stratumd-bringup + P5-corvus-bringup + P5-login + P5-hostowner + P5-kernel-update). **v1.x deferrals**: P5-secure-boot + corvid-boot bootloader + encrypted system pool + TPM-seal + FIDO2/smartcard + Shamir multi-hostowner + per-dataset rekeying + sshd. **Stratum API additions enumerated in CORVUS-DESIGN §13** (Michal-owned, parallel work): multi-stratumd-per-pool; corvus-native UNWRAP wire; session-close notify+evict; snapshot rollback-compromise marking; boot-key device binding via `--bind-pool-serial`; per-dataset key rotation hooks. **No code; scripture only.** Tests unchanged at 410/410 PASS × default + UBSan. **Stage**: Phase 5's scripture commitment is complete; implementation arc unblocked. **Next**: P5-corvus-syscalls (foundational; 5 syscalls + libt stubs + tests; critical-path for P5-corvus-bringup). Predecessor: P5-attach-probe (`029016b` / `106edeb`).
- **Earlier tip**: **P5-attach-probe landed (substantive `029016b` / hash fixup `106edeb`) — userspace integration test of the Phase 5 mount surface; `/attach-probe` ELF binary + kernel-thread 9P responder + test framework. End-to-end SYS_ATTACH_9P + SYS_MOUNT + SYS_UNMOUNT driven through a real EL0 Proc against a real wire-codec / dev9p / spoor-transport / pipe stack — until this chunk, every Phase 5 test ran via kernel-internal loopback shortcuts.** Probe (`usr/attach-probe/attach-probe.c`, ~14.9 KiB ELF) expects fd 0 (tx) + fd 1 (rx) pre-installed in its handle table by the kernel test framework, calls t_attach_9p("/", 1, 0) → t_mount(99, 0) → t_unmount(99) → t_unmount(99 — expect fail) → t_close(attach_fd) → t_close(0) → t_close(1) → t_putstr("attach-probe: PASS\n") → t_exits(0). **Kernel responder** (in `kernel/test/test_attach_probe.c`, ~280 LOC) is a kproc kthread that owns the OTHER ends of the two pipe pairs; loop reads framed Tmsgs from rx → builds responses via inline switch (Tversion → Rversion msize=4096 + "9P2000.L"; Tattach → Rattach qid; Tclunk → Rclunk; anything else → Rlerror EIO) → writes to tx. Exits cleanly on rx EOF (probe closed tx_fd → write_eof on c2s ring → wakeup). **Why kernel-thread, not two userspace Procs**: v1.0 rfork supports RFPROC only; RFFDG (shared fd table — needed to give two Procs access to the same pipe pair) is deferred. Cross-Proc Spoor transfer is gated on RIGHT_TRANSFER through a 9P session per ARCH I-4 — circular dependency. When RFFDG lands later, the test can graduate to a two-proc design. **Refcount discipline (load-bearing)**: each pipe_create Spoor's ref=1 is TRANSFERRED (not bumped) to its owner — responder gets c2s_rd + s2c_wr; probe's handle table gets c2s_wr (fd 0) + s2c_rd (fd 1) via handle_alloc's ref-consumption convention. Boot retains zero refs after setup. If boot held a residual ref on c2s_wr, the probe's t_close(tx_fd) would drop to 1 (not 0), devpipe_close wouldn't fire, write_eof wouldn't be set, responder would block forever in dev_read — surfaced by an earlier draft of the test; documented as caveat #1 in `57-attach-probe.md`. **Mid-chunk discovery**: Tclunk on root_fid is rejected at the session layer (`kernel/9p_session.c::p9_session_send_clunk` line 303 — root fids are session-scoped, cleaned up implicitly at session close, not via wire Tclunk). p9_attached_destroy already tolerates this via `(void)p9_client_clunk(...)`. The probe's t_close(attach_fd) → dev9p_close → p9_attached_destroy → attempted Tclunk silently rejected; the responder sees exactly 2 messages (Tversion + Tattach), not 3. Test asserts `>= 2` and documents the design intent at the assertion site. **Build wiring**: `usr/attach-probe/` (new module: 1 C source + 1 CMakeLists) + `usr/CMakeLists.txt` (add_subdirectory) + `tools/build.sh` (usr_bins array += "attach-probe") + `kernel/CMakeLists.txt` (test_attach_probe.c in KERNEL_SRCS) + `kernel/test/test.c` (extern + registry entry `userspace.attach_probe_round_trip`). **No spec extension** — pure composition over already-spec'd 9P client + pipe + handle layers; Tclunk-on-root rejection is a documented session-layer policy not a new invariant. **Audit posture**: extends existing audit-trigger surfaces (kernel/test/* not on the trigger list; the only kernel-source change is the test file itself). The probe binary at EL0 has no new attack surface — it uses existing syscalls with valid argument shapes. **Test count** 409 → 410; **410/410 PASS × default + UBSan** (with `BOOT_TIMEOUT=60` for UBSan — adds ~3× per-test overhead). **Stage**: Phase 5's userspace-visible mount/attach surface is now empirically validated end-to-end at EL0. **Next chunks**: P5-stratumd (boot ordering + .key sidecar design pass — now-feasible end-to-end; needs architectural design discussion) OR P5-fd-syscalls-RFFDG (graduate the attach-probe to a two-userspace-Proc design) OR codec extensions P5-wire-lock/-xattr/-stratum-ext (mechanical; low-priority). Predecessor: P5-mount-syscall (`bb778e6` / `fb4c2cd`).
- **Earlier tip**: **P5-mount-syscall landed (substantive `bb778e6` / hash fixup `fb4c2cd`) — `SYS_MOUNT = 14` + `SYS_UNMOUNT = 15` SVC handlers + companion `spoor_clunk` Plan-9-cclose fix; the last deferred SVC handler from Phase 5's plan. With this chunk every user-visible mount/attach syscall from ARCH §11.2 is live.** ABI: `SYS_MOUNT(source_spoor_fd, target_path_id, flags) → 0/-1`; `SYS_UNMOUNT(target_path_id) → 0/-1`. Thin wrappers over `kernel/territory.c::mount` / `::unmount`. **target_path_id** is a u32 abstract path token at v1.0 (the same `path_id_t` used by the existing bind/unbind/mount/unmount C-API + `struct PgrpMount`); string-path resolution lands with the future walk subsystem. **flags** mirror Plan 9 (`MREPL` / `MBEFORE` / `MAFTER` / `MCREATE`); only `MREPL` has distinguished semantics at v1.0. **Rights gate**: source handle requires `RIGHT_READ` (a mount holder consumes the source's tree). **Companion fix**: `kernel/spoor.c::spoor_clunk` switched from "run dev->close on every clunk" to Plan 9 cclose semantics — `dev->close` runs ONLY when the ref hits 0. Prior behavior would have torn down per-Spoor Dev state (pipe endpoints, 9P sessions) on the user's first close even while a mount-table entry held a reference; P5-mount-syscall is the first feature where that asymmetry becomes wrong. Site-changes: `kernel/territory.c` switched 3 spoor_unref → spoor_clunk (unmount, mount-MREPL displacement, territory_unref final-release); `kernel/dev9p.c::dev9p_close` switched 2 spoor_unref → spoor_clunk (transport-Spoor cleanup). Constructor-failure + attach_9p rollback sites kept on spoor_unref (no last-drop semantics needed). **Userspace stubs** `t_mount` + `t_unmount` in libt's syscall.h match the kernel ABI shape; `T_MREPL`/`T_MBEFORE`/`T_MAFTER`/`T_MCREATE` flag macros mirror the kernel side. **Tests** in `kernel/test/test_sys_mount.c` (9 tests): happy_path_grafts_pipe_spoor + idempotent_on_duplicate + rejects_bad_fd + rejects_missing_right_read + rejects_invalid_flags + rejects_null_territory + unmount.removes_entry_and_drops_ref + unmount.rejects_nonexistent_target + caller_close_keeps_mount_alive (the **end-to-end lifecycle test** that validates the Plan 9 cclose fix — mount, close source fd, close other pipe end, ring stays alive; Territory drop frees the ring). **No spec extension** — `specs/territory.tla::MountRefcountConsistency` is preserved structurally by the territory.c primitives; spoor_clunk semantics are an implementation refinement orthogonal to the spec's abstract refcount drop. **Audit posture**: extends `kernel/syscall.c` (existing) + `kernel/territory.c` (existing — joined trigger list at P5-attach-mount) + `kernel/dev9p.c` (existing — joined at P5-attach-dev) + `kernel/spoor.c` (existing — Spoor lifecycle). No new attack surfaces — handlers thin-wrap already-audited primitives. **Test count** 400 → 409; **409/409 PASS × default + UBSan**. **Stage**: Phase 5's user-visible mount/attach surface is complete. **Next chunks**: P5-attach-probe (userspace integration test with a 9P-responder companion) + P5-stratumd (boot ordering + .key sidecar design pass — now-feasible; all kernel primitives in place) OR codec extensions P5-wire-lock/-xattr/-stratum-ext (mechanical; deferred low-priority). Predecessor: P5-attach-syscall (`ae0746e` / `88cf9c3`).
- **Earlier tip**: **P5-attach-syscall landed (substantive `ae0746e` / hash fixup `88cf9c3`) — `SYS_ATTACH_9P = 13` SVC handler; the user-visible body of ARCH §9.6.1's `attach_9p`. Closed the last deferred SVC handler from the P5-attach plan.** ABI: tx_fd (x0), rx_fd (x1), aname_va (x2), aname_len (x3 ≤ 256), n_uname (x4); returns KOBJ_SPOOR fd for the 9P tree's root in x0, -1 on failure. dev9p_priv gains `attached_owner` + `adapter_to_free` — on root Spoors created by SYS_ATTACH_9P, dev9p_close tears down the entire attach session. Handler takes independent spoor_refs on tx/rx; copies aname via uaccess_load_u8; kmalloc adapter with owns_spoors=false; full rollback at all 5 failure points. Userspace stub `t_attach_9p` in libt. No spec extension — pure composition. Test count 399 → 400. Predecessor: P5-fd-syscalls (`5fd72f6` / `3948bd4`).
- **Earlier tip**: **P5-fd-syscalls landed (substantive `5fd72f6` / hash fixup `3948bd4`) — `SYS_CLOSE` + `SYS_DUP` SVC handlers + `/pipe-probe` userspace binary; the byte I/O composition is now empirically live in the boot log.** Kernel: `SYS_CLOSE`(=11) thin wrapper over handle_close (KOBJ_SPOOR release path from P5-fd-pipe runs spoor_clunk); `SYS_DUP`(=12) wraps handle_dup with rights validation (RightsCeiling check rejects elevation; KOBJ_SPOOR acquire path bumps spoor_ref). **Userspace**: libt's `<thyla/syscall.h>` extended with t_pipe/t_read/t_write/t_close/t_dup inline-asm stubs (matches the existing always_inline pattern). **First userspace test binary** `/pipe-probe`: 16 KiB ELF that exercises pipe→write→read→verify FIFO→dup with reduced rights→reject rights-elevation→close dup→close rd→close wr→read on closed fd→double-close, then puts `"pipe-probe: PASS\n"` + exits 0. Spawned by `kernel/test/test_pipe_probe.c` via `devramfs_lookup` + rfork + exec_setup + userland_enter; `wait_pid` checks status=0. **Boot log evidence**: `pipe-probe: PASS` + `/pipe-probe reaped pid=N status=0 — full byte-I/O syscall surface verified end-to-end`. **Kernel-internal test** `sys_pipe.dup_spoor_handle_acquires_ref` verifies the KOBJ_SPOOR acquire path: handle_dup of a SPOOR bumps the underlying Spoor's refcount; rights-elevation rejected; close of dup drops refcount back. **No spec extension** — pure composition over specs/handles.tla + specs/pipe.tla. **Audit posture**: extends kernel/syscall.c surface (already on trigger list); no new attack surface. /pipe-probe is now the canonical regression test of the byte I/O composition — exercises 8 distinct syscall surfaces in a single invocation including the 2 rejection paths. Test count 397 → 399; **399/399 PASS × default + UBSan**. **Stage gate**: SYS_ATTACH_9P + SYS_MOUNT SVC handlers are now **unblocked** (the fd surface is sufficient for userspace KOBJ_SPOOR usage). **Next chunks**: P5-attach-syscall + P5-mount-syscall (user-visible mount surface; both gated since P5-attach-create + P5-attach-mount on the fd surface) OR P5-stratumd (boot ordering + .key sidecar design pass) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions). Predecessor: P5-fd-rw (`54d900b` / `a134782`).
- **Earlier tip**: **P5-fd-rw landed (substantive `54d900b` / hash fixup `a134782`) — `SYS_READ` + `SYS_WRITE` SVC handlers + `uaccess_store_u8` primitive; userspace can finally exchange bytes through fds.** ABI: `SYS_READ`(=9) and `SYS_WRITE`(=10) each take fd in x0, buf_va in x1, len in x2; return bytes transferred (>=0; 0 EOF for read) in x0, -1 on error. Per-call cap = `SYS_RW_MAX = 4096` bytes (matches PIPE_BUF_SIZE; userspace loops for larger transfers). **Rights gates**: SYS_READ requires RIGHT_READ on the handle; SYS_WRITE requires RIGHT_WRITE. SYS_PIPE grants both on both ends → pipe fds pass both gates. **User-VA validation**: same bound as SYS_PUTS (`UACCESS_USER_VA_TOP = 1<<47`); NULL / overflow rejected. Handlers factored into `sys_{read,write}_for_proc(p, h, kbuf, len)` inners — tested with kernel-side buffers bypassing the SVC + uaccess path. **uaccess_store_u8 primitive**: symmetric to `uaccess_load_u8`. Single-byte STRB with fixup table entry; same fault-recovery dispatcher path (which already handles WRITE faults via `userland_demand_page`'s VMA prot check). C decl in `arch/arm64/uaccess.h`. **Audit-trigger surface extended**: kernel/syscall.c (existing) gains read/write handlers; arch/arm64/uaccess.S (existing) gains store_u8 + fixup-table entry. **4 new kernel-internal tests**: write_then_read_round_trip (7-byte payload writes via SYS_WRITE; reads back via SYS_READ; FIFO order preserved — **canonical end-to-end test of the byte I/O path**) + rights_check (bad fd → -1) + zero_length_validates_fd (0-len call on bad fd → -1; on good fd → 0; POSIX discipline) + read_after_close_returns_eof (close wr, read empty pipe → 0 — **composes SYS_READ with specs/pipe.tla ReadEof action**). **No spec extension** — pure composition over specs/handles.tla + specs/pipe.tla. Test count 393 → 397; **397/397 PASS × default + UBSan**. **Next chunks**: P5-fd-close (SYS_CLOSE wrapper over handle_close — thin) + P5-fd-dup (SYS_DUP wrapping handle_dup; uses KOBJ_SPOOR acquire path) + P5-fd-pipe-probe (userspace probe binary exercising SYS_PIPE + SYS_READ + SYS_WRITE + SYS_CLOSE end-to-end with a libt-linked C test). Predecessor: P5-fd-pipe (`0f66f5c` / `65293f5`).
- **Earlier tip**: **P5-fd-pipe landed (substantive `0f66f5c` / hash fixup `65293f5`) — `SYS_PIPE` SVC handler + KOBJ_SPOOR release/acquire wiring; first userspace consumer of the kernel pipe primitive.** SYS_PIPE = 8: no args; allocates a `pipe_create` pair, installs both Spoors as KOBJ_SPOOR handles in the caller's HandleTable with RIGHT_READ\|WRITE\|TRANSFER. Returns rd fd in x0, wr fd in x1; -1 in x0 on failure (full rollback of partial state). **Wires KOBJ_SPOOR into handle.c**: `handle_release_obj` now `spoor_clunk`s KOBJ_SPOOR (so handle_close + proc_free's handle_table_free both tear down the underlying Spoor); `handle_acquire_obj` `spoor_ref`s (so handle_dup gives the second handle its own ref). The first kobj kind beyond BURROW + hw kinds to be reference-counted on the handle table. **Handler factored into `sys_pipe_for_proc(p, *rd, *wr)`** as the non-static inner; SVC wrapper picks up current Proc + writes ctx->regs[0/1]. **No user-VA fd[2] pointer** — uses x0 + x1 return regs to sidestep adding uaccess_store_u32 (only uaccess_load_u8 exists today); the libc shim (Phase 6+) adapts to POSIX pipe(2)'s buffer-style API. **3 kernel-internal tests**: allocates_two_distinct_spoor_handles (returns 0; distinct fds; KOBJ_SPOOR; rights match; obj pointers distinct), proc_free_releases_handles (Proc cleanup releases the ring + both Spoors — end-to-end test of the new KOBJ_SPOOR release path; pipe_total_freed += 1, spoor_total_freed += 2), handle_close_releases_one_end (closing one fd drops ring's per-end ref; closing both frees the ring). **No spec extension** — pure composition over `specs/handles.tla` (HandleAlloc + HandleClose actions) + `specs/pipe.tla` (already covered). **Audit posture**: extends the kernel/syscall.c surface + the kernel/handle.c surface (both already on the trigger list); no new attack surfaces — the wiring is mechanical. **Pre-existing extinction** at boot (joey-side trap; observed identical on baseline pre-commit; verified via git stash) is NOT a regression. Test count 390 → 393; **393/393 PASS × default + UBSan**. **Next chunks**: P5-fd-rw (SYS_READ + SYS_WRITE handlers; user-VA buffer copy; routes through dev->read/write — when this lands, userspace can actually use pipe + Stratum-mounted files end-to-end) + P5-fd-close + P5-fd-dup. Predecessor: P5-pipe-blocking (`431db3a` / `92be8b6`).
- **Earlier tip**: **P5-pipe-blocking landed (substantive `431db3a` / hash fixup `92be8b6`) — `specs/pipe.tla` + extended `kernel/pipe.c`: blocking read/write with rendez integration; the missed-wakeup-freedom property (ARCH §28 I-9) is now spec-pinned for the pipe's wait/wake protocol.** Per CLAUDE.md spec-first policy: this is the chunk that opens the wait/wake invariant scope. New `specs/pipe.tla` (~225 LOC) with 4 buggy variants modeling the bug class "elide the wake-after-mutation step": BuggyWriteAppendNoWake / BuggyReadDrainNoWake / BuggyCloseWriteNoWake / BuggyCloseReadNoWake. 5 invariants — TypeOk + SingleWaiter + EofMonotonic + NoStuckReader + NoStuckWriter — the last two specialize I-9 to the pipe's two-direction state machine. TLC verdicts: 1 clean + 4 buggy-violations. **C-side**: `struct pipe_ring` gains `spin_lock_t lock`, `read_eof`/`write_eof` bool flags, two `struct Rendez` wait queues. Header 32→72 B; total 4128→4168 B. devpipe_read becomes a `for(;;) { lock + check + drain-or-eof-or-sleep + unlock + wakeup }` loop using `sleep(rendez, cond_can_read, ring)`; devpipe_write symmetric with cond_can_write. devpipe_close sets the appropriate EOF flag under lock, drops lock, calls wakeup on the OTHER side's rendez (so any sleeper exits with EOF / EPIPE), then handles refcount + free. **Compositional proof**: `specs/scheduler.tla::NoMissedWakeup` proves the atomic cond-check + sleep transition at the rendez API surface; `specs/pipe.tla::NoStuckReader/Writer` proves the pipe-side discipline of "every mutation that could enable a waiter MUST issue a wakeup." Together they close the missed-wakeup hazard end-to-end. **4 new multi-thread tests** in `kernel/test/test_pipe_blocking.c` mirroring the test_rendez_basic_handoff pattern (consumer thread does blocking op + sleeps; boot does wake-triggering action + sched()s to consumer): write_wakes_sleeping_reader + read_wakes_sleeping_writer + close_write_end_wakes_reader_with_eof + close_read_end_wakes_writer_with_epipe. Existing 2 tests with non-blocking semantics (read_on_empty_returns_zero / write_to_full_returns_zero) repurposed to test EOF / EPIPE paths (close other end FIRST to avoid blocking). **Mid-chunk bugs**: pipe_ring _Static_assert miscounted header (80 vs actual 72); fix: walked alignment math 4+4+8+8+8+1+1+(pad 2)+4+(pad 4)+16+16=72. **kernel/pipe.c joins the audit-trigger surface** alongside the wait/wake hazard category. Test count 386 → 390; **390/390 PASS × default + UBSan** (~1.7 s boot). **Next chunks**: P5-fd-syscalls (open/close/read/write/dup → unblocks deferred SVC handlers + general userspace 9P + userspace pipe(2)) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions). Predecessor: P5-pipe (`23acfbf` / `7f95cd0`).
- **Earlier tip**: **P5-pipe landed (substantive `23acfbf` / hash fixup `7f95cd0`) — `kernel/pipe.{c,h}`: Plan 9 `pipe(fd[2])` primitive at the kernel layer; connected pair of Spoors backed by a shared in-kernel ring buffer.** Replaces the test-only `test_pipe_dev` scaffold from P5-spoor-transport with the production primitive. `struct pipe_ring { magic, ref, count, head, tail, buf[PIPE_BUF_SIZE=4096] }` (4128 B; kmalloc routes through alloc_pages); `struct pipe_endpoint { magic, ring *, is_read_end }` (16 B; SLUB cached). Two Spoors share one ring; each Spoor's `aux` points at its own endpoint; the Dev vtable's read/write dispatches on `is_read_end`. **Semantics at v1.0 (non-blocking)**: read returns bytes available (0..n), 0 on empty; write returns bytes accepted (0..n), 0 on full; wrong-end (read on write-end / write on read-end) returns -1. **Blocking variant deferred to P5-pipe-blocking** — needs rendez integration + spec extension (the missed-wakeup hazard, ARCH §28 I-9, enters scope when wait/wake is wired). **Lifecycle**: ring ref=2 at create; each Spoor's close drops the ring ref; ring freed at 0. devpipe_close also frees the endpoint priv. **dc='|'** matches Plan 9 9front devpipe + the shell pipe glyph. **10 new unit tests** including the canonical e2e composition test (`pipe.compose_with_spoor_transport`) — two pipe pairs wired into a `p9_spoor_transport` adapter run Tversion + Tattach through real pipes, proving the entire 9P stack composes against the production byte-pipe primitive. The other 9 tests cover smoke + empty/full edges + short writes + wraparound + wrong-end rejection + lifecycle counters. **No spec extension** — ring correctness is local + structural; the wait/wake hazard arrives at P5-pipe-blocking. **No new attack surfaces** at this chunk — kernel-internal primitive exercised via tests; the user-visible pipe(2) syscall is deferred to P5-fd-syscalls. **Mid-chunk fixes**: 1. _Static_assert miscounted header size (24 vs actual 32); fixed to `32 + PIPE_BUF_SIZE = 4128`. 2. `kmem_cache_create` on the 4128-byte ring exceeded SLUB's per-cache size limit → extinct on boot; fix: kmalloc for the ring (routes through alloc_pages), keep SLUB cache only for the 16-byte endpoint. Test count 376 → 386; **386/386 PASS × default + UBSan** (~1.7 s boot). **Next chunks**: P5-pipe-blocking (rendez integration + spec extension) OR P5-fd-syscalls (open/close/read/write/dup → unblocks deferred SVC handlers + general userspace 9P) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions). Predecessor: P5-spoor-transport (`0f45fc6` / `28b5cbb`).
- **Earlier tip**: **P5-spoor-transport landed (substantive `0f45fc6` / hash fixup `28b5cbb`) — `kernel/9p_spoor_transport.{c,h}`: adapter wrapping a Spoor pair into `struct p9_transport_ops`.** Realizes ARCH §9.6's "every filesystem entity is a Spoor" from the transport side: a 9P client's byte pipe is whatever Spoor read/write the caller chooses (future kernel pipe, Unix-socket Spoor, virtio-vsock Spoor). Adapter has 3 fields (tx_spoor, rx_spoor, owns_spoors flag); send/recv/close vtable routes to the Spoor's Dev vtable. tx and rx may be the same Spoor (duplex socket model) or distinct (Plan 9 pipe(fd[2]) model). **owns_spoors** controls whether transport_close clunks the Spoors: caller owns → false; adapter-internal pair → true. **9 unit tests**: smoke + null-rejected + send-routes + recv-routes + recv-empty-zero + close-clunks-when-owned + close-preserves-when-unowned + transport-core-round-trip + end-to-end-handshake. The end-to-end test composes p9_session + p9_transport + spoor-adapter through a test-only byte-pipe Dev to run Tversion + Tattach against real Spoor I/O — until this chunk, every 9P test took the loopback-fn shortcut. **No spec extension** — pure plumbing over already-spec'd layers (`specs/9p_client.tla` + `specs/territory.tla` compose unchanged). **No new attack surfaces** — adapter delegates faithfully to Dev vtable. Test count 367 → 376; **376/376 PASS × default + UBSan** (~1.7 s boot). **Next chunk**: P5-pipe (kernel-internal pipe primitive — pair of Spoors over a ring buffer; the production backing for this adapter) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions). Predecessor: P5-attach-mount (`c21d002` / `178866f`).
- **Earlier tip**: **P5-attach-mount landed (substantive `c21d002` / hash fixup `178866f`) — Territory mount table + `mount` / `unmount` kernel primitives + `specs/territory.tla` extension for mount-lifecycle invariants.** Per ARCH §9.6.6: every filesystem entity is a Spoor; `mount(territory, source_spoor, target_path, flags)` grafts a Spoor at a path in a Territory, holding one refcount per entry; `unmount(territory, target_path)` removes one entry and drops the ref. Spec extension adds the `Mount` / `Unmount` / `BuggyMountNoRefbump` / `BuggyUnmountNoRefdrop` / `BuggyDestroyLeak` actions over a `mounts: [Procs -> SUBSET (Paths X Spoors)]` state layer + a `refcount: [Spoors -> Nat]` variable; `MountRefcountConsistency` is the global invariant (the kernel's per-Spoor counter must equal the cardinality of mount entries referencing it). **1 clean cfg + 4 buggy cfgs** — clean 2.56M states; each buggy cfg produces a counterexample at depth 2-3 / under 1000 states. **C-side**: `struct PgrpMount { source *, target, flags }` (16 B); `struct Territory` extended with `nmounts` + `mounts[PGRP_MAX_MOUNTS=8]` (216 B total, was 80). **Rename**: `unmount(territory, src, dst)` → `unbind(territory, src, dst)` (the inverse of bind); the verb `unmount` is now reserved for the mount-table primitive. **Lifecycle integration**: `territory_clone` deep-copies mounts AND `spoor_ref`'s each entry; `territory_unref` final-release loops mounts and `spoor_unref`'s each before `kmem_cache_free`. **`mount`** is idempotent under `(target, source)` equality; **MREPL** semantics drop-old-take-new when an entry at the target already exists; MBEFORE/MAFTER/MCREATE are stored for future union walk. **7 new unit tests** cover smoke + idempotency + MREPL replace + unmount-missing + table-full + clone-bumps-refs + destroy-drops-all-refs. **No new attack surfaces** at the SVC layer (the kernel-internal API is exercised only via tests at v1.0; the user-visible `mount` syscall is deferred to P5-fd-syscalls). **`kernel/territory.c` joins the audit-trigger surface table** in CLAUDE.md / ARCH §25.4. Test count 360 → 367; **367/367 PASS × default + UBSan** (~1.7 s boot). **Next chunk**: P5-spoor-transport (Spoor-over-byte-pipe transport backend; the first step toward real stratumd integration) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions). Predecessor: P5-attach-create (`fa31e51` / `0e1a339`).
- **Earlier tip**: **P5-attach-create landed (substantive `fa31e51` / hash fixup `0e1a339`) — `kernel/9p_attach.{c,h}`: kernel-internal mount creation primitive; the body of the future `attach_9p` syscall.** Per the user-signed-off ARCH §9.6 design (scripture `1d9d585`) + ROADMAP §7.2 refined this chunk: the user-visible `SYS_ATTACH_9P` SVC handler is **deferred** until fd-syscall infrastructure exists (v1.0 has no open/close/read/write syscalls and no userspace path to populate KOBJ_SPOOR handles with byte-pipe Spoors). The substantive work — wrapping a transport in a `p9_client`, driving handshake, producing a dev9p Spoor — lives in this chunk and is ready for the SVC handler to glue around. **`struct p9_attached`**: heap-resident wrapper (client * + recv_buf * + recv_cap + root_fid + msize + handshake_ok). `p9_attached_create` heap-allocates the wrapper + client (~12 KiB; kmalloc routes through alloc_pages) + recv_buf; drives Tversion+Tattach; cleans up partial state on any failure. `p9_attached_root_spoor` returns a dev9p Spoor with `fid_owned=false`. `p9_attached_destroy` clunks root_fid + tears down. Kernel-internal callers at v1.0: tests + future P5-stratumd boot path. **4 new unit tests** verify lifecycle + handshake-failure cleanup + end-to-end walk/open/read through the wrapper. **No spec extension** — pure composition. **No new attack surfaces** at this chunk; SVC handler (when it lands) will add user-boundary surface. Test count 356 → 360; **360/360 PASS × default + UBSan**. **Next chunk**: P5-attach-mount (Territory mount table + namespace.tla extension; can land without the syscall) OR P5-spoor-transport (real byte-pipe backend). Predecessor: P5-attach-dev (`f7b2d6c` / `7786bf0`).
- **Earlier tip**: **P5-attach-dev landed (substantive `f7b2d6c` / hash fixup `7786bf0`) — `kernel/dev9p.{c,h}`: Dev vtable proxying to the kernel 9P client; the foundation of ARCH §9.6 filesystem-as-Spoor.** Implements the proxy that makes a `p9_client`-backed Spoor look identical to a kernel-internal Dev above the vtable. **Dev vtable**: 16 slots; walk → `p9_client_alloc_fid` + `p9_client_walk`; open → `p9_client_lopen`; read → `p9_client_read`; write → `p9_client_write`; close → `p9_client_clunk` (conditional on `fid_owned`); stat/create/remove/wstat stubbed at v1.0 (deferred to P5-attach-syscall / -mount). **Per-Spoor state**: `struct dev9p_priv { magic, client *, fid, fid_owned }` allocated via `kmalloc(KP_ZERO)`; magic clobber on free (UAF defense). **Lifecycle**: dev9p does NOT own client lifetime — higher layer (attach_9p syscall) manages. Root Spoors have `fid_owned=false` (close does NOT clunk root_fid); walk-derived Spoors have `fid_owned=true` (close clunks). **Fid allocator**: new `p9_client_alloc_fid()` on the client; monotonic counter from `root_fid + 1`; no reclaim at v1.0. **9 new unit tests** verifying composition end-to-end through loopback. **No spec extension** — pure composition over `specs/9p_client.tla`-spec'd lower layers. Test count 347 → 356; **356/356 PASS × default + UBSan**. **Codec / session / transport / client + dev9p stack** now bridges the kernel filesystem ABI to the 9P client. **Next chunk**: P5-attach-syscall — the `attach_9p` syscall that wraps a transport Spoor + drives handshake + returns a dev9p Spoor fd. Predecessor: P5-client (`fc4f2ce` / `e1648ef`).
- **Earlier tip**: **P5-client landed (substantive `fc4f2ce` / hash fixup `e1648ef`) — `kernel/9p_client.c` + `kernel/include/thylacine/9p_client.h`: high-level client API consolidating wire + session + transport into one callable surface.** `struct p9_client` owns one session + one transport inline (+ 8 KiB outbound buffer); ~12 KiB total. **25 op wrappers** covering every op the codec exposes: lifecycle (init / destroy / close), handshake (Tversion + Tattach in one call), path (walk / walk_one / clunk), IO (lopen / lcreate / read / write), metadata (getattr / setattr / readdir / statfs / fsync), mutation (symlink / mknod / rename / readlink / link / mkdir / renameat / unlinkat). Each wrapper is mechanically identical: `session.send_* → transport.exchange → result extraction → error mapping`. **Errno convention**: 0 on success, `-errno` on failure. Mapping: `-EINVAL` (bad args/magic), `-EBUSY` (session not OPEN), `-EIO` (lower-layer failure), `-<server's ecode>` (Rlerror propagated). **Internal `copy_qid` / `copy_attr` / `copy_statfs` helpers** replace implicit struct assignments that would lower to memcpy at link time (kernel doesn't link libc — same discipline as the explicit init in P5-session). **Read + readdir use copy semantics** (out of the transport's recv_buf zero-copy pointer) so callers don't need to track recv_buf lifetime. **13 new unit tests** covering: lifecycle + handshake + walk + clunk + lopen + read + write + getattr + readdir + statfs + mkdir + unlinkat + readlink + Rlerror propagation (-ENOENT surfaced as -2) + EBUSY-before-handshake. The "canonical responder" used by the test fixture is a single loopback responder that handles every standard 9P2000.L op with sensible canned responses — lets one fixture serve all the composition tests. **No spec extension**: pure composition of already-spec'd layers. Files: `kernel/include/thylacine/9p_client.h` (new; ~170 LOC); `kernel/9p_client.c` (new; ~520 LOC); `kernel/test/test_9p_client.c` (new; ~430 LOC); `kernel/test/test.c` (+13 forward decls + 13 registry entries); `kernel/CMakeLists.txt` (+1 kernel source + 1 test source); `docs/reference/47-9p-client.md` (new; ~180 LOC); `docs/phase5-status.md` (P5-client row + section + P5-wire-mutation section marked LANDED with hashes); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-client ✅; no spec module changes). Test count 334 → 347; **347/347 PASS × default + UBSan** (~9 s boot unchanged). **Mid-chunk bug**: initial struct assignments (e.g., `*out_attr = r.attr`) compiled to memcpy calls; kernel-linker failed. Fix: 3 field-by-field copy helpers (`copy_qid` / `copy_attr` / `copy_statfs`) + sed-rewrote 7 call sites. **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. No new attack surfaces — every op is a thin wrapper over already-audited lower layers. **Codec + session + transport + client stack is now feature-complete for the standard 9P2000.L surface**; the API is ready for the future mount/Dev/syscall integration. **Next chunks**: P5-attach (mount syscall integration + per-Proc client lifecycle — the architecturally interesting one; needs design discussion on syscall ABI shape) OR P5-spoor-transport (Spoor-over-Unix-socket backend) OR P5-wire-lock/-xattr/-stratum-ext (finish the codec — deferred low-priority). Predecessor: P5-wire-mutation (`c7daae7` / `97cc73f`).
- **Earlier tip**: **P5-wire-mutation landed (substantive `c7daae7` / hash fixup `97cc73f`) — `kernel/9p_wire.c` + `kernel/9p_session.c` extended with the directory-mutation message family (Tsymlink/Rsymlink, Tmknod/Rmknod, Trename/Rrename, Treadlink/Rreadlink, Tlink/Rlink, Tmkdir/Rmkdir, Trenameat/Rrenameat, Tunlinkat/Runlinkat).** Codec now feature-complete for the standard 9P2000.L surface — only lock + xattr + Stratum extensions remain. Wire codec gains 8 builders + 8 parsers + 16 new `_Static_assert` lines pinning every TX/RX opcode pair. Three response shapes (empty / qid-only / string) share two internal helpers (`parse_empty_body` + `parse_qid_body`). `P9_UNLINK_AT_REMOVEDIR = 0x200` constant exposed for rmdir-equivalent. Session module gains 8 send APIs — `p9_session_send_symlink` / `_mknod` / `_rename` / `_readlink` / `_link` / `_mkdir` / `_renameat` / `_unlinkat` — + 8 dispatcher branches. **Fid-exclusivity**: `send_rename` requires no other in-flight op on `fid` (server-side identity mutation; mirrors setattr discipline); the other 7 mutation ops permit concurrent ops on same dfid/fid (server serializes per-entry internally). **`struct p9_dispatch_result` extended** with `created_qid` (Rsymlink / Rmknod / Rmkdir surface; kept distinct from `open_qid` to avoid semantic confusion) and `readlink_target` + `readlink_target_len` (zero-copy pointer into rmsg buffer). **No spec extension needed** — `specs/9p_client.tla`'s SendIO action models all mutation ops at the `io` op-kind level. **19 new unit tests** (8 wire round-trip + 8 session end-to-end + Trename concurrent-rejected + Tunlinkat concurrent-permitted + mutation-on-unbound-fid sweep across all 8 ops). Files: `kernel/include/thylacine/9p_wire.h` (+155 LOC); `kernel/9p_wire.c` (+230 LOC); `kernel/include/thylacine/9p_session.h` (+85 LOC); `kernel/9p_session.c` (+200 LOC); `kernel/test/test_9p_wire.c` (+250 LOC); `kernel/test/test_9p_session.c` (+325 LOC); `kernel/test/test.c` (+19 forward decls + 19 registry entries); `docs/reference/44-9p-wire.md` + `45-9p-session.md` (refreshed); `docs/phase5-status.md` (P5-wire-mutation row + section + P5-transport section marked LANDED with hashes); `specs/SPEC-TO-CODE.md` (3 new SendIO rows + Phase 5 progress P5-wire-mutation ✅; no spec module changes). Test count 315 → 334; **334/334 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. The mutation family's body shapes are simpler than IO/metadata (no per-op structs; helper reuse keeps the parser surface tight); no R111 cap-bound is needed (every response is either header-only or bounded by a fixed-size qid or a frame-bounded string). **Next chunks**: P5-attach (mount syscall integration + per-Proc session+transport lifecycle) OR P5-wire-lock / P5-wire-xattr / P5-wire-stratum-ext (finish the codec; deferred low-priority items). Predecessor: P5-transport (`3f7c0e2` / `6a81bb7`).
- **Earlier tip**: **P5-transport landed (substantive `3f7c0e2` / hash fixup `6a81bb7`) — `kernel/9p_transport.c` + `kernel/9p_transport_loopback.c`: frame-aware byte pipe + in-memory loopback backend + session composition helper.** New vtable-driven transport (`struct p9_transport_ops { send, recv, close, ctx }`) sitting between session and any backend. **Responsibilities**: frame discipline (outbound `header.size == len` validation; inbound 7-byte-header peek then body-length aggregation), partial-read aggregation (the recv core loops on `ops->recv` until a complete frame is in hand — relevant for any stream-socket backend), state machine (INIT → OPEN → CLOSED + ERROR sink), session composition (`p9_transport_exchange` = `session.send_*` + `transport.send` + `transport.recv` + `session.dispatch_rmsg` in one call). **Loopback backend** (test scaffold): in-memory pipe where each `ops->send` call invokes a caller-supplied responder fn that synthesizes the response into a caller-provided staging buffer; `chunk_size` knob forces partial-read tests through the transport's recv loop. The loopback is the foundation for testing the session+transport composition without an external server — tests register a "session-aware" responder that produces correct Rversion/Rattach/Rwalk replies, then drive a full handshake through the stack. **9 new unit tests** — lifecycle + round-trip + frame-size rejection + partial-read aggregation (3-byte chunks force ~10 ops.recv calls on a 28-byte Tattach) + backend-error → ERROR transition + close idempotency + end-to-end handshake (Tversion → Rversion → Tattach → Rattach with state machine progression verified at each step) + end-to-end walk (full handshake + Twalk → Rwalk with new_fid binding observed). **No spec extension needed**: transport invariants (FramingIntegrity, RequestResponseOrdering, NoMessageLoss) are mechanical at the request-response synchronous level; session-level invariants (I-10, I-11, FlowControl, OutOfOrderCorrectness) compose through unchanged. **Mid-chunk bug**: initial loopback design had a 16 KiB inline staging buffer, which overflowed the kernel's 16 KiB test-thread stack on the first transport test (`kernel stack overflow` extinction). Refactored to caller-provided staging buffer (matches the transport's caller-provided recv_buf pattern); loopback struct now ~80 bytes. Files: `kernel/include/thylacine/9p_transport.h` (new; ~150 LOC); `kernel/9p_transport.c` (new; ~210 LOC); `kernel/include/thylacine/9p_transport_loopback.h` (new; ~75 LOC); `kernel/9p_transport_loopback.c` (new; ~115 LOC); `kernel/test/test_9p_transport.c` (new; ~360 LOC); `kernel/test/test.c` (+9 forward decls + 9 registry entries); `kernel/CMakeLists.txt` (2 new kernel sources + 1 new test source); `docs/reference/46-9p-transport.md` (new; ~180 LOC); `docs/phase5-status.md` (P5-transport row + section); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-transport ✅). Test count 306 → 315; **315/315 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. The transport's only attack surfaces are framing (defended by strict size-equality + partial-read aggregation invariants) and backend-failure recovery (transport sinks into ERROR; caller responsible for destroy + reinit). **Next chunks**: P5-attach (mount syscall integration + per-Proc connection lifecycle) OR P5-wire-mutation (Tunlinkat / Trename / Trenameat / Tsymlink / Tmknod / Tlink / Treadlink / Tmkdir — directory mutation surface). Predecessor: P5-wire-meta (`c95b194` / `c44970d`).
- **Earlier tip**: **P5-wire-meta landed (substantive `c95b194` / hash fixup `c44970d`) — `kernel/9p_wire.c` + `kernel/9p_session.c` extended with the metadata message family (Tgetattr/Rgetattr, Tsetattr/Rsetattr, Treaddir/Rreaddir, Tstatfs/Rstatfs, Tfsync/Rfsync).** Wire codec gains 5 builders + 5 parsers + 10 new `_Static_assert` lines pinning every TX/RX opcode pair. New `struct p9_attr` (20-field Linux statx-shape; 153-byte body), `struct p9_setattr` (60-byte mutation payload with valid-mask), `struct p9_statfs` (60-byte fs-stats record). New `p9_unpack_dirent` consumes one record from a Rreaddir data stream (zero-copy name pointer). Constants: `P9_GETATTR_BASIC / _ALL`, `P9_SETATTR_MODE / _UID / _GID / _SIZE / _ATIME / _MTIME / _CTIME / _ATIME_SET / _MTIME_SET`. Session module gains 5 send APIs — `p9_session_send_getattr` / `_setattr` / `_readdir` / `_statfs` / `_fsync` — + 5 dispatcher branches for the new R-types. **Fid-exclusivity rules**: getattr / statfs / readdir / fsync permit concurrent ops on same fid (read-shaped at the wire layer); setattr requires no other in-flight op on fid (mutation-shaped; server-side attribute write is exclusive). **R111 caller-cap-bound** applied at `p9_parse_rreaddir` (identical to Rread: server-supplied count bounded against caller's `data_cap` BEFORE the dirent stream pointer is exposed; the dispatcher derives `data_cap` from `s->negotiated_msize - 11`). **`struct p9_dispatch_result` extended** with `attr` (Rgetattr surface), `statfs` (Rstatfs surface), `readdir_count` + `readdir_data` (zero-copy dirent stream). **No spec extension needed** — `specs/9p_client.tla`'s SendIO action already covers all metadata ops at the `io` op-kind level. **15 new unit tests** (7 wire round-trip + cap-bound + dirent-unpack + 8 session end-to-end + concurrency-rule + state-gate). **Mid-chunk bug**: initial Rgetattr body-length arithmetic had `8 * 13` (104 bytes for 13 u64s); actual count is 15 u64s after qid (5 mid + 8 times + 2 trailing). Fix: `8 * 15` (120 bytes); test layer caught it before formal audit. Files: `kernel/include/thylacine/9p_wire.h` (+155 LOC); `kernel/9p_wire.c` (+200 LOC); `kernel/include/thylacine/9p_session.h` (+60 LOC); `kernel/9p_session.c` (+135 LOC); `kernel/test/test_9p_wire.c` (+280 LOC); `kernel/test/test_9p_session.c` (+270 LOC); `kernel/test/test.c` (+15 forward decls + 15 registry entries); `docs/reference/44-9p-wire.md` + `45-9p-session.md` (refreshed); `docs/phase5-status.md` (P5-wire-meta row + section); `specs/SPEC-TO-CODE.md` (SendIO row split + Phase 5 progress). Test count 291 → 306; **306/306 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. **Next chunks**: P5-transport (Spoor-over-Unix-socket transport layer) OR P5-wire-mutation (Tunlinkat / Trename / Trenameat / Tsymlink / Tmknod / Tlink / Treadlink / Tmkdir — directory mutation surface). Predecessor: P5-wire-io (`3aa508c` / `f06fe12`).
- **Earlier tip**: **P5-wire-io landed (substantive `3aa508c` / hash fixup `f06fe12`) — `kernel/9p_wire.c` + `kernel/9p_session.c` extended with the IO message family (Tlopen/Rlopen, Tlcreate/Rlcreate, Tread/Rread, Twrite/Rwrite).** Wire codec gains 4 builders + 4 parsers + 8 new `_Static_assert` lines pinning each TX/RX opcode pair. Session module gains `p9_session_send_lopen` / `_lcreate` / `_read` / `_write` send APIs + 4 dispatcher branches for the new R-types. **Fid-exclusivity rules**: `send_lopen` + `send_lcreate` require no other in-flight op on fid (server-side fid state mutation is exclusive at the wire layer); `send_read` + `send_write` permit concurrent ops on same fid (offset is explicit on the wire). **R111 caller-cap-bound** applied at `p9_parse_rread`: server-supplied count is bounded against the session's `negotiated_msize - 11` BEFORE the zero-copy data pointer is exposed; the dispatcher derives `data_cap` from the session's negotiated msize. **`struct p9_dispatch_result` extended** with `open_qid` + `open_iounit` (Tlopen/Tlcreate); `read_count` + `read_data` (zero-copy data pointer for Tread); `write_count` (Twrite). **No spec extension needed** — `specs/9p_client.tla` already models `SendIO` generically (4 op kinds collapse to `io`); the wire-level dispatch composes within the existing spec envelope. **15 new unit tests** (7 wire round-trip + cap-bound + type-strict tests, 8 session end-to-end + concurrency-rule + state-gate tests). Files: `kernel/include/thylacine/9p_wire.h` (+90 LOC), `kernel/9p_wire.c` (+140 LOC), `kernel/include/thylacine/9p_session.h` (+50 LOC), `kernel/9p_session.c` (+115 LOC), `kernel/test/test_9p_wire.c` (+225 LOC), `kernel/test/test_9p_session.c` (+225 LOC), `kernel/test/test.c` (+15 forward decls + 15 registry entries), `docs/reference/44-9p-wire.md` + `45-9p-session.md` (refreshed), `docs/phase5-status.md` (P5-wire-io row), `specs/SPEC-TO-CODE.md` (Phase 5 progress + state-machine SendIO row split into open/create vs read/write). Test count 276 → 291; **291/291 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the `kernel/9p_*` surface that joined the CLAUDE.md trigger list at P5-session; bugs here surface as test failures + break I-10 / I-11 / FlowControl spec properties. **Next chunks**: P5-transport (Spoor-over-Unix-socket); OR P5-wire-meta (Tgetattr/Tsetattr/Treaddir/Tstatfs/Tfsync). Predecessor: P5-session (`16d6c80` / `a8eddd0`).
- **Earlier tip**: **P4-M landed (substantive `ffc4b46` / hash fixup pending) — driver crash recovery runtime verification.** Closes ROADMAP §6.2 third-to-last open box. New `userspace.driver_crash_recovery` kernel test spawns child A (virtio-blk-probe) holding `CAP_HW_CREATE` against QEMU's virtio-blk slot; A claims MMIO + IRQ + DMA + reads sector 0 + exits clean; test asserts `kobj_mmio_pa_claimed(blk_dev->pa, 0x1000) == false` AND `kobj_irq_intid_claimed(blk_intid) == false` after A's reap (proving the release path cleared both claim tables); spawns child B against the SAME hardware; B succeeds end-to-end with exit status 0. **Structural-equivalence argument**: at v1.0 single-CPU with no kill-syscall, "kill driver mid-I/O" ≡ "driver exits non-zero" — both call `proc_exit` → handle-table-walk → `kobj_*_unref`. The release-path discipline is the load-bearing mechanism + was audited at R9 (KObj_MMIO + KObj_IRQ) + R12-DMA (KObj_DMA) + R13-burrow (Burrow mappings); P4-M is the runtime verification. Auto-restart supervision (`/ctl/proc-events/exit` hook + supervisor process) is Phase 5+ policy. New kernel surface (~30 LOC total): `kobj_mmio_pa_claimed` + `kobj_irq_intid_claimed` diagnostic-only query helpers (no impact on the runtime release path). `tools/test.sh` BOOT_TIMEOUT default bumped 10 → 15 s (boot now ~9 s; 67 % margin). 243 → 244 tests PASS × default + UBSan. **Non-audit-bearing** — query helpers don't mutate state; test composes already-audited primitives. **Only 2 §6.2 boxes remain open**: P4-Id virtio-net `/dev/ether0` 9P surface + virtio-input `/dev/cons` 9P surface — both await Phase 5 FS-namespace work. Predecessor: P4-Z (`821b047` / `86270a6`).
- **Earlier tip**: **P4-Z landed (`821b047` / `86270a6`) — cumulative driver-model audit close.** R14 prosecutor pass across the 4 composed userspace drivers (`virtio-blk-rw`, `virtio-net-loop`, `virtio-input`, `virtio-gpu`) + the `libthyla-rs` runtime crate surfaced **0 P0 + 1 P1 + 3 P2 + 6 P3**; 10 closed in-commit, 2 deferred with rationale. **Closes ROADMAP §6.2 final exit criterion** ("No P0/P1 audit findings on driver model"). **F217 (P1)**: VIRTIO 1.2 §2.7.13.2 requires a driver-side LoadLoad barrier between reading `used.idx` and the subsequent `used.ring[k]` / data-buffer reads. Without it, an out-of-order ARM Cortex-A core may speculate the data reads before the used.idx load, returning pre-advance bytes. The smoking-gun exploit is virtio-input's busy-poll drain — a genuine EV_KEY/30/1 wake would be classified as phantom EV_SYN (zero type/code/value from the populate_eventq pre-zeroed slot), silently dropping the key. IRQ-driven paths (blk-rw / net-loop / gpu) are NOT safe by accident — `eret` is a Context Synchronization Event, not a memory barrier. Fix applied uniformly: new `libthyla_rs::virtio_rmb()` helper (`dmb ishld`) inserted after every `read16(used_va + 2)`; matches Linux's `virtio_rmb()` codegen. Post-build `llvm-objdump | grep -c "dmb.*ishld"` regression confirms 1/2/1/1 across blk-rw/net-loop/input/gpu. **F218 (P2)**: `INT_USED_BUFFER` vs `INT_CONFIG_CHANGE` bit-discriminated wake loop (per VIRTIO 1.2 §4.2.5; cap `MAX_NON_USED_BUFFER_WAKES = 16`); applied in blk-rw + net-loop + gpu. **F219 (P2)**: `MAX_DRAIN_PER_BATCH = QUEUE_SIZE` cap on drain loops in virtio-net-loop. **F220 (P2)**: `avail_idx_at_entry` capture replaces read-from-self gate in virtio-input. **6 × P3 also closed** (F222 doc drift; F223 discarded errno; F224 unused-var hygiene; F226 used.elem.len validation; F227 compile-asserts in blk-rw + net-loop; F228 new `T_RIGHT_HW_ALLOWED`). **2 deferred** (F221 withdrawn during pass — subsumed by F217+F219; F225 QueueReady-before-populate spec-canonical-but-safe via DRIVER_OK gate, deferred to Phase 4 housekeeping or Phase 5+). No new kernel surface; no new syscalls. **Audit-bearing at the composition layer** (CLAUDE.md "Composed-driver discipline" extension documented at 39-hw-handles.md caveat #11). Binary sizes uniformly +120-400 B (within 16-KiB blob cap): blk-rw 12976 B, net-loop 13224 B, input 12936 B, gpu 15152 B. 243/243 PASS × default + UBSan; test count unchanged (F217's regression is post-build binary inspection — the spec-violation memory-ordering bug cannot be reproduced deterministically on QEMU TCG, so binary inspection is the durable regression). Closed list at `memory/audit_r14_p4z_closed_list.md`. Predecessor: P4-K-events (`a859abc` / `7cc6c14`).
- **Earlier tip**: **P4-K-events landed (`a859abc` / `7cc6c14`) — event consumption + QMP send-key wiring on top of the P4-K virtio-input probe.** Extends `usr/virtio-input/` from `DRIVER_OK + classification`-only into a driver that consumes host-injected events end-to-end. **CI verification**: `tools/test.sh` spawns a background QMP injector that polls the boot log for the userspace probe's `AWAITING_QMP_KEY` sentinel; on match, connects to `build/qmp.sock`, negotiates capabilities, issues `send-key` with qcode `"a"`. QEMU's virtio-keyboard-device fills the eventq with `{EV_KEY/KEY_A=30/value=1}` + SYN + `{value=0}` + SYN; driver drains, validates target, exits PASS. Post-boot grep in `tools/test.sh` enforces the success marker. **Mechanisms added**: kernel test pre-fires SPI 77 via `gic_set_pending_spi` from `input_dev->pa`-derived INTID (forward-progress guarantee for interactive `tools/run-vm.sh`); driver adds `t_irq_create` + single `t_irq_wait` (consumes pre-fire wake) + bounded busy-poll (`MAX_POLL_ITER = 200M`) on `used.idx`; used-ring drain helper parses 8-byte event records + recycles descriptors via DSB-fenced avail-ring re-publish; `tools/run-vm.sh` adds `-qmp unix:build/qmp.sock,server,nowait`; `tools/test.sh` background injector + diagnostic log at `build/test-inject.log` + post-boot grep enforces `virtio-input: saw target key` when injection expected. **Does NOT close ROADMAP §6.2 virtio-input `/dev/cons`** — that requires a `/dev/cons` 9P surface bridging the userspace driver's eventq to OTHER processes; this chunk only proves the substrate. The `/dev/cons` integration awaits P4-Id or Phase 5+ FS-namespace work. No new kernel surface; no new substrate; same composed hw-handle SVC surface. Non-audit-bearing at per-syscall layer; self-audit clean across 10 categories at userspace-driver + tooling-discipline layer. Binary 10944 → 12536 B (within 16-KiB blob cap). 243/243 PASS × default + UBSan; test count unchanged. Predecessor: P4-L-scanout (`8c296c9` / `7298ea1`).
- **Earlier tip**: **P4-L-scanout landed (`8c296c9` / `7298ea1`) — full 2D scanout pipeline.** Extends `usr/virtio-gpu/` from a `GET_DISPLAY_INFO`-only probe into a six-command driver that drives the full 2D resource lifecycle: `RESOURCE_CREATE_2D` (0x0101) → `RESOURCE_ATTACH_BACKING` (0x0106) → `SET_SCANOUT` (0x0103) → `TRANSFER_TO_HOST_2D` (0x0105) → `RESOURCE_FLUSH` (0x0104), each verifying `resp.hdr.type == OK_NODATA` (0x1100) per VIRTIO 1.2 §5.7.6. **Closes ROADMAP §6.2 "Userspace virtio-gpu: write pixels to framebuffer via BURROW handle"** at the substrate layer (the host validates + builds the resource + records the backing + binds the scanout + copies the guest backing into the host-side resource + presents on scanout). CI-visible pixel verification ("visible on QEMU display") deferred to optional P4-L-screencap (QMP `screendump` + Python verifier; `tools/run-vm.sh` runs `-nographic`). **What's new**: per-command `struct Controlq { slot_va, dma_va, dma_pa, irq_handle, seq }` + `submit_and_wait(req_len, resp_len) -> Result<resp_type>` helper (seq monotonic across the six commands; desc head 0 rebuilt + reused safely between completions); five new request-body builders for the §5.7.6 layouts; two-DMA composition (4 KiB ring + 64 KiB framebuffer; the framebuffer's PA is the value embedded in `ATTACH_BACKING`'s mem_entry — single `virtio_gpu_mem_entry { le64 addr; le32 length; le32 padding }` suffices because `kobj_dma_create` backs each handle with one physically contiguous chunk); 4-quadrant test pattern (red TL / green TR / blue BL / white BR, 64×64 each, B8G8R8A8) filled between CREATE_2D and ATTACH_BACKING with a trailing `dsb sy`; DMA layout reshape (RESP_OFF 0x620 → 0x700; REQ region 32 → 256 B to fit the 56-B TRANSFER body); three new compile-time asserts pinning the layout. No new kernel surface; no new substrate (same composed hw-handle SVC surface — all four SVCs audited at R9/R10/R11/Ic5b1b). Non-audit-bearing at the per-syscall layer; audit-bearing at the userspace-driver discipline layer (self-audit clean across 10 adversarial categories). **Halcyon (Phase 8) substrate gate closed**: the GPU device class now composes the same hw-handle surface as blk/net/input AND produces pixels (host-side, validated by the OK-response chain). Binary 11648 → 15032 B (within 16-KiB blob cap). Files: `usr/virtio-gpu/src/main.rs` (~430 → ~700 LOC; full rewrite preserving init_device + helpers + diagnostics), `kernel/test/test_virtio_gpu_probe.c` (comment + summary refresh), `docs/reference/43-virtio-gpu.md` (substantial rewrite). 243/243 PASS × default (~1640 ms) + UBSan (~1664 ms); test count unchanged (the existing `userspace.virtio_gpu_probe_rfork_with_caps` exercises the full pipeline). Predecessor: P4-O (`d766360` / `4fcf2ca`).
- **Earlier tip**: **P4-O landed (`d766360` / `4fcf2ca`) — exit-criteria reconciliation + Dev vtable coverage test.** Closes the gap between ROADMAP §6.2 exit criteria as written and what the test suite actually verifies. Five criteria ticked with citations: (1) `cat /dev/random` non-zero bytes (P4-B); (2) Spoor lifecycle 10K cycles on /dev/null (P4-B); (3) Dev vtable all 16 ops dispatch (P4-O — new `dev.vtable_slot_coverage` test iterates `bestiary[]` + asserts every Dev has all 16 function-pointer slots non-NULL per ARCH §9.2; ROADMAP's "17 ops" reconciled to "16 ops" — counting drift); (4) hardware handle non-transferability (P4-Ib + P4-Ic5b1b — wording reconciled from "panics with message" to "is rejected at handle_dup (returns -1; statically pinned by `handles.tla::NoHwDup` + no SYS_HANDLE_TRANSFER syscall exists for hw kinds — I-5 enforced structurally"); (5) burrow.tla TLC clean + SPEC-TO-CODE.md maintained (P4-N). Five criteria remain open: virtio-net /dev/ether0 (awaits P4-Id 9P surface), virtio-input /dev/cons input (P4-K-events), virtio-gpu pixels visible (P4-L-scanout), driver crash recovery (P4-M), cumulative no-P0/P1 driver-model audit (P4-Z). New test `dev.vtable_slot_coverage` complements existing `dev.devnone_ops_smoke` — pins structural property "every registered Dev has all 16 op slots non-NULL" at boot. Files: `kernel/test/test_dev.c` (+~50 LOC + decl), `kernel/test/test.c` (forward decl + registry), `docs/phase4-status.md` (exit-criteria block reworked). Non-audit-bearing — test reconciliation; no impl surface touched. 242 → 243 tests; PASS × default (~1640 ms) + UBSan (~1664 ms). Predecessor: P4-N (`ea763df` / `36807d0`).
- **Earlier tip**: **P4-N landed (`ea763df` / `36807d0`) — BURROW spec finalize: R13 audit close (1 P2 + 3 P3 closed; 0 deferred).** Closes the BURROW audit-trigger surface (`kernel/burrow.c` + spec + reference) in preparation for Phase 4 close. **F213 (P2)**: `burrow_free_internal` lacked the `v->magic = 0` clobber that sibling kobjs (kobj_mmio, kobj_dma) established at R9 F148; without it, a stale-pointer dereference between `kmem_cache_free` and SLUB's next-pointer write reads valid `VMO_MAGIC` and bypasses the magic check, masking the UAF. Single-CPU at v1.0 narrows the window but interrupt-driven preemption keeps it in scope. Fix: explicit `v->magic = 0;` immediately before `kmem_cache_free` in `burrow_free_internal` (mirrors R9 F148 pattern across siblings). **F214 (P3)**: SPEC-TO-CODE.md drift — burrow.tla section pointed at `burrow_map` / `burrow_unmap` for spec MapVmo/UnmapVmo (which actually map to `burrow_acquire_mapping` / `burrow_release_mapping`); VmoCreate listed only `burrow_create_anon` (missing the MMIO + DMA variants). **F215 (P3)**: `specs/burrow.tla` commentary enumerated ANON + MMIO type backings but not DMA (added at P4-Ic5b1b; spec is type-agnostic so structurally unchanged, but the type list went stale). **F216 (P3)**: 4 sites across burrow.h + 20-burrow.md claimed "SLUB clobbers magic on free" — factually wrong (SLUB writes a freelist next-pointer; clobber-via-next-pointer is incidental allocator behavior, not architectural). All 4 closed in this chunk. TLC re-verify at P4-N: burrow.cfg clean (100 distinct states / depth 9, matches P2-Fb baseline); 3 buggy cfgs each produce expected NoUseAfterFree violations. Closed list at `memory/audit_r13_burrow_closed_list.md`. Reference doc 20-burrow.md gains new "P4-Ic5b1b DMA Burrow details" section (parallel to existing MMIO) + new "P4-N — R13 audit close" section. **Audit-bearing per CLAUDE.md trigger surfaces (VMO)**. 242/242 PASS × default + UBSan unchanged (F213's fix is pure defense-in-depth on post-free magic-check path). Predecessor: P4-image-shrink (`eb7ed3a` / `92e8625`).
- **Earlier tip**: **P4-image-shrink landed (`eb7ed3a` / `92e8625`) — C-side `-z max-page-size=4096` + shrink all 9 legacy 96-KiB blob caps to 16 KiB.** Mirror of the Rust-side flag landed at P4-K: adds `-Wl,-z,max-page-size=4096` to `cmake/Toolchain-aarch64-userspace.cmake::THYLACINE_USERSPACE_LD_FLAGS`. ld.lld defaults to MAXPAGESIZE 0x10000 (64 KiB) on aarch64; without this flag the C-side `hello` binary carried a 64-KiB zero-padded gap between text and data PT_LOAD segments (file size 73912 B; FileSiz 0x88 at Offset 0x10000). With the flag, every PT_LOAD aligns to 4 KiB (matching kernel's PAGE_SIZE) — hello shrinks 73912 → 12472 B. With all userspace binaries now under 16 KiB (largest = virtio-net-arp at 14864 B), every legacy 96-KiB blob cap is over-reserved by ~80 KiB. Shrunk 9 test-side `*_BLOB_MAX` constants from 98304 → 16384, matching the P4-K convention introduced by virtio-input/gpu: test_userspace_ramfs.c, test_mmio_probe.c, test_irq_probe.c, test_virtio_blk_probe.c, test_virtio_blk_rw.c, test_irq_latency_bench.c, test_virtio_net_probe.c, test_virtio_net_arp.c, test_virtio_net_loop.c. 9 × ~80 KiB = ~720 KiB of kernel .bss reclaimed. Image impact: kernel `_image_size` 1460 → 744 KiB (= 0xba000); image+firmware budget 1972 → 1256 KiB; headroom under the 2 MiB L3 mapping grows from 76 KiB → ~792 KiB. Tests unchanged in logic (only `#define` constants + comment narratives updated). **Non-audit-bearing** — toolchain + .bss sizing; no §28 invariant touched. 242/242 PASS × default (~1640 ms) + UBSan (~1664 ms). Predecessor: P4-L (`e66c033` / `1a9c882`).
- **Earlier tip**: **P4-L landed (`e66c033` / `1a9c882`) — virtio-gpu probe: fourth composed userspace driver; substrate generalizes to the VIRTIO GPU class.** New userspace binary `usr/virtio-gpu/` (~430 LOC Rust) drives QEMU virt's `virtio-gpu-device` end-to-end through DRIVER_OK + GET_DISPLAY_INFO using the full composed hw-handle SVC surface (MMIO + DMA + IRQ — virtio-gpu becomes the second composed driver after virtio-blk to exercise all three). What's new vs. predecessors: (1) **Two virtqueues** — controlq (idx 0) for command/response traffic + cursorq (idx 1) for cursor updates. First composed driver to configure more than one queue; `configure_queue()` helper writes the per-queue PA tuple + QueueReady transition for each, both before DRIVER_OK. (2) **controlq command/response chain** — 2-descriptor split: descriptor 0 = device-readable request hdr (24 B `virtio_gpu_ctrl_hdr` per §5.7.6.6), descriptor 1 = device-writable response payload (408 B `virtio_gpu_resp_display_info` per §5.7.6.1). NEXT linkage between them; the resp.hdr.type field returns OK_DISPLAY_INFO (0x1101) or an ERR variant in the same 24-byte hdr — virtio-gpu folds status into the response header rather than using a separate status byte like virtio-blk's 3-descriptor chain. (3) **Flat le32 config-space** at offset 0x100..0x110 (events_read / events_clear / num_scanouts / num_capsets); distinct from virtio-input's selector indirection and virtio-net's MAC+status struct. The probe issues `VIRTIO_GPU_CMD_GET_DISPLAY_INFO` via controlq, waits IRQ, verifies `resp.hdr.type == OK_DISPLAY_INFO`, then logs `num_scanouts` + `pmodes[0]` dimensions + `enabled` flag (informational; QEMU `-nographic` reports `num_scanouts=1`, `pmodes[0]=1280x800`, `enabled=1`). `tools/run-vm.sh` wires `-device virtio-gpu-device,id=gpu0` between `input_flags` and `virtio-rng-device,id=rng0`; virtio-gpu lands at slot 28 (INTID 76), rng drops to slot 27. THYLACINE_NO_GPU=1 disables. New reference doc `docs/reference/43-virtio-gpu.md`. **Halcyon substrate gate**: this chunk closes the substrate-side proof that the GPU device class composes the same hw-handle surface as blk/net/input. The scanout-side deliverable (RESOURCE_CREATE_2D + ATTACH_BACKING + TRANSFER_TO_HOST_2D + SET_SCANOUT + RESOURCE_FLUSH + actual pixels) is future Halcyon-prep work. **No new substrate**: same composed hw-handle SVC surface as virtio-blk-probe (all four SVCs audited at R9/R10/R11/Ic5b1b). Bonus from P4-K (`-z max-page-size=4096`) keeps the binary at 11.6 KiB; new test's blob cap is 16 KiB. Image_size 1444 → 1460 KiB (UBSan ~280 KiB); image+firmware ≈ 1972 KiB; 76 KiB headroom under image+firmware ≤ 2 MiB. 241 → 242 tests; PASS × default (~1640 ms) + UBSan (~1664 ms). **Non-audit-bearing** at the per-syscall layer (all four hw-handle SVCs audited at R9/R10/R11/Ic5b1b); audit-bearing at the userspace-driver discipline layer (self-audit clean across 12 adversarial categories — lock-ordering / lifetime / error-path / state-machine / idempotency / boundary / privilege / overflow / resource-exhaustion / memory-safety / wire-protocol / DMA-coherency). Predecessor: P4-K (`7a72617` / `bcb6642`).
- **Earlier tip**: **P4-K landed (`7a72617` / `bcb6642`) — virtio-input probe: third composed userspace driver; substrate generalizes to the VIRTIO INPUT class.** Substrate generalization to DeviceID = 18. Introduces selector-based device-specific config-space (VIRTIO 1.2 §5.8.4); first device class to exercise select+subsel indirection. eventq RX-only on queue 0 with 16 pre-published WRITE descriptors. Reads INPUT_CFG_ID_NAME (QEMU returns "QEMU Virtio Keyboard") + INPUT_CFG_EV_BITS (key_bits=29, rel_bits=0 → keyboard). Does NOT consume events (would need QMP send-key wiring; future P4-K-events). Bonus universally applied: `-z max-page-size=4096` in `usr/.cargo/config.toml` shrinks every userspace Rust ELF by ~60 KiB. 240 → 241 tests. Predecessor: P4-Ic-latency (`308ac55` / `93a3b8c`).
- **Earlier tip**: **P4-Ic-latency landed (substantive `308ac55` / hash fixup `93a3b8c`) — IRQ-to-userspace latency benchmark; closes ROADMAP §6.2 exit criterion (infrastructure side).** New userspace binary `usr/irq-bench/` (~120 LOC Rust) subscribes to SPI 96, reads kernel-supplied num_iter, loops on `t_irq_wait` + `mrs cntpct_el0` + writes to shared region. New kernel test `kernel/test/test_irq_latency_bench.c` (~280 LOC C) allocates an 8 KiB `KObj_DMA`, wraps it in a Burrow via `burrow_create_dma`, maps it into the child at `0x800000` via `burrow_map` (inside the exec thunk between `exec_setup` and `userland_enter`); pre-pends SPI 96; spawns child; loops triggering IRQs while polling `shared->completed`; computes p50/p99/max via insertion sort. Two thresholds: bare-metal target `p99 < 5 µs` per VISION §4.5 (logged for reference), CI sanity budget `p99 < 50 ms` (the actual `TEST_ASSERT`; catches pathological regressions without coupling pass/fail to QEMU TCG emulation overhead). New kernel primitive: `timer_enable_el0_counter_access()` sets `CNTKCTL_EL1.EL0PCTEN = 1` per-CPU at boot (called from `boot_main` + `per_cpu_main::smp.c` alongside `fp_enable_this_cpu`) so EL0 can read `CNTPCT_EL0` without trap — also positions us for future vDSO `clock_gettime`. N=128 iterations (first discarded as warmup; 127 valid samples → p99 = floor(127×0.99) = 125th sorted). Cache coherence between kernel direct-map alias and child-mapping alias is automatic (AArch64 PIPT data caches → same PA = same cache line). Cross-CPU ordering: kernel test reads `user_ts[]` only after `wait_pid`, by which time the child's exit path has driven multiple `DSB SY` barriers through `scheduler.c`. On QEMU TCG, observed p99 ≈ 7-10 ms (emulation overhead dominates; benchmark INFRASTRUCTURE is the deliverable, bare-metal authoritative measurement deferred to Phase 5+ hardware bring-up). Boot impact ~1.0 s at N=128. New reference doc `docs/reference/41-irq-bench.md`. **Audit-bearing per CLAUDE.md trigger surfaces** (Exception entry + Scheduler + Capability checks at the test-composition layer); self-audit clean across lock-ordering / lifetime / error-path / state-machine / idempotency / boundary / measurement-methodology / statistical-correctness categories. 239 → 240 tests; PASS × default (~1420 ms; +980 ms vs baseline) + UBSan (~1410 ms); 5/5 default stress. Predecessor: R12-uaccess audit close (`d1106af` / `79214a5`).
- **Earlier tip**: **R12-uaccess audit close (`d1106af` / `79214a5`) — F210 P1 + F211 + F212 all closed (0 deferred).** Adversarial prosecutor over `32367d1` surfaced 0 P0 + **1 P1** + 0 P2 + 2 P3. **F210 (P1)**: SYS_PUTS bound was 2^48 vs dispatcher's UACCESS_USER_VA_TOP=2^47. EL0 could pass `buf_va in [2^47, 2^48)`, bypass the syscall-level check, fault in `uaccess_load_u8`'s ldrb, and the dispatcher's `fi.vaddr < UACCESS_USER_VA_TOP` would fail — routing to the kernel-mode-translation-fault extinction. Any unprivileged EL0 binary could extinct the kernel. Fix: thread `UACCESS_USER_VA_TOP` through SYS_PUTS; new mid-gap rejection assertion + positive-corner assertion in `test_syscall_dispatch_puts_smoke`. **F211 (P3)**: dispatcher synthesized `from_user=true` on the fault_info copy to `userland_demand_page`, but `userland_demand_page` ignores `from_user` and the synthesis left `ec` mismatched. Fix: removed synthesis; pass `&fi` through. **F212 (P3)**: asm-side fixup-entry layout had no discipline pinning against C-side drift. Fix: comment block at `.pushsection .uaccess_fixup` documenting the 8-byte format requirement. 239/239 PASS × default + UBSan; 15× default + 10× UBSan stress 25/25 PASS. Predecessor: R12-uaccess substantive (`32367d1` / `b5de03f`).
- **Earlier tip**: **R12-uaccess landed (`32367d1` substantive; `b5de03f` hash fixup) — kernel-side uaccess-fault recovery + `pretouch_rodata_pages()` workaround retired across all userspace crates.** New `arch/arm64/uaccess.{S,c,h}` exports `uaccess_load_u8(user_va, *out)` — kernel-mode user-VA accessor primitive that returns 0 on success / -1 on translation/permission fault. The asm primitive's `ldrb` is registered in a `.uaccess_fixup` table (Linux `__ex_table`-style PC-relative s32 offsets so the entries are PIE-clean — no R_AARCH64_ABS64 runtime relocs). `exception_sync_curr_el` now recognizes kernel-mode sync faults whose FAR is in the user-VA half AND whose faulting PC is in the fixup table; on hit it first calls `userland_demand_page` synthetically against the current Proc (typical case: the page is in a VMA but not yet PTE-installed → install + ERET re-executes the load), and on demand-page failure sets `ctx->elr := fixup_pc` so the primitive returns -1. `sys_puts_handler` rewritten to read bytes via `uaccess_load_u8` instead of direct kernel-mode dereference. Vector slots 0x000 + 0x200 (Sync at current EL) switched from `b _torpor` to `b .Lexception_return` so the Sync path can now return normally (previously every sync was assumed to extinct). 4 new tests in `kernel/test/test_uaccess.c`: `fixup_table_well_formed`, `fixup_lookup_known`, `fixup_lookup_unknown_returns_zero`, `load_u8_unmapped_user_va_returns_minus1` (drives a real translation fault from kproc context to validate end-to-end recovery). Userspace cleanup: removed `pretouch_rodata_pages()` from `virtio-blk-rw`, `virtio-net-arp`, `virtio-net-loop` + retired the comment block in `virtio-net-probe`. Closes the P4-Ic7 surprise #2 hazard documented in handoff 034. 235 → 239 tests PASS × default + UBSan (sanitizer matrix pending). **Audit-bearing per CLAUDE.md trigger surfaces (Exception entry + Page fault + Capability checks)**; self-audit clean; formal R12-uaccess pass deferred-or-on-finding. Predecessor: R12-vaddr (`688119f` / `7f78820`).
- **Earlier tip**: **R12-vaddr landed (`688119f` substantive; `7f78820` hash fixup) — unified `burrow_map` upper-bound check; closes F180 (R12-DMA deferred) + lifts R10 F158 MMU-layer-only-reject decision.** Adds `USER_VA_TOP = (1ull << 47)` to `arch/arm64/mmu.h` (ARM IHI 0487 D5.2.4 — 48-bit VAs without TBI; TTBR0 user-half is bits [46:0]; bit 47 is the TTBR selector). Adds `if (vaddr + length > USER_VA_TOP) return -1;` to `burrow_map` immediately after the existing overflow guard, pinned by `_Static_assert(USER_VA_TOP == (1ull << 47), ...)` so future drift in either bound surfaces at compile time. Every production caller (sys_mmio_map_handler, sys_dma_map_handler, exec.c LOAD + user stack) now fails fast at the VMA layer before any per-Proc state mutation; previously the Vma was installed successfully and the kernel extincted on first userspace access via `mmu_install_user_pte`'s per-page reject (R10 F158, which stays as defense-in-depth for the demand-page path). Tests: extended `burrow.map_proc_constraints` with 3 new rejection cases (vaddr == USER_VA_TOP, vaddr > USER_VA_TOP, range straddling USER_VA_TOP); new `burrow.map_proc_user_va_top_boundary` (positive corner: highest legal user page at USER_VA_TOP − PAGE_SIZE accepted). 234 → 235 tests PASS × default (~445 ms) + UBSan (~469 ms). **Audit-bearing per CLAUDE.md trigger surfaces (Handle table + VMO + Capability checks + Page fault / mprotect / mmap)**; self-audit + formal R12-vaddr prosecutor pass clean. Predecessor: R12-FP (`5085364` / `d360375`).
- **Earlier tip**: **R12-FP formal audit close landed — Thread SLUB align + new FP regression test + ISB after FPCR + CPACR defensive mask (2 P2 + 2 P3 all closed).** Closes R12-FP deferred audit (carried since P4-Ic5-FP). Adversarial prosecutor surfaced 0 P0 + 0 P1 + **2 P2** + 2 P3. **F165 (P2)**: Thread SLUB cache alignment hardcoded as 8; `cpu_switch_context`'s STP/LDP-Q pairs require 16-byte alignment per ARM ARM C7.2.348. Today's 784-byte Thread is a multiple of 16 by coincidence; future field addition could silently misalign object N>0 and corrupt the first context switch. Fix: pass `_Alignof(struct Thread)` so SLUB honors the struct's declared alignment. **F166 (P2)**: zero test coverage of FP save/restore. Empirically: `llvm-objdump -d` of all userspace ELFs shows ZERO FP/SIMD instructions — the claimed indirect coverage was false. Fix: new `kernel/test/test_fp.c` (~250 LOC) with two tests — `fp.cpacr_enabled` verifies FPEN=0b11 + ZEN/SMEN/TTA=0 (also pins F168); `fp.round_trip_v_regs_fpsr_fpcr` loads V0..V31 + FPCR + FPSR with a sentinel, switches into a helper that deliberately clobbers, switches back, asserts byte-by-byte equality. The clobber is what makes the test sensitive. Uses `__attribute__((target("+fp+simd")))` per function (kernel-wide `-mgeneral-regs-only` blocks codegen-level FP). **F167 (P3)**: cpu_switch_context lacked `isb` after MSR FPCR. ARM ARM D8.7.5 requires ISB to synchronize FPCR writes. Forward-looking for Phase 5+ in-kernel FP (NEON memcpy / AES); today's eret to EL0 is context-synchronizing so not observable. Fix: `isb` after the FPCR MSR in context.S. **F168 (P3)**: `fp_enable_this_cpu` set FPEN but didn't mask CPACR's ZEN/SMEN/TTA. Architectural reset values are 0 (trap) so safe today; permissive firmware could leak non-zero through. Fix: `cpacr &= ~((3ull << 16) | (3ull << 24) | (1ull << 28));` before FPEN OR; `fp.cpacr_enabled` test verifies. 232 → 234 tests PASS × default + UBSan. **Audit-bearing per CLAUDE.md trigger surfaces (Scheduler + Initial bringup)**. Predecessor: R12-DMA (`f828649` / `bcb2ea4`).
- **Earlier tip**: **R12-DMA formal audit close landed — clean (0 P0/P1/P2; 1 P3 deferred F180).** Adversarial prosecutor over P4-Ic5b1b (DMA-pinning syscall + KObj_DMA + Burrow DMA + spec extension). Read all files in scope fully + traced lifecycle / cap-gate / W^X / PA-stability / buddy-allocator-as-claim-layer / size-validation / demand-page / spec-coverage. **Result: 0 P0 + 0 P1 + 0 P2 + 1 P3.** All structural mirrors of R9 (F142-F153) and R10 (F154-F158) verified clean: refcount ACQ_REL atomic, magic clobber on free, construction-ref + ref balance, cap-gate ACQUIRE fence, init double-init atomic, W-without-R rejected, EXEC rejected at both layers, out-of-IPS bounded by buddy construction, NoHwDup for KOBJ_DMA, PA stability structural (no path mutates kd->pa post-create), buddy-allocator-as-claim-layer (distinct calls structurally yield distinct PAs), spec coverage (handles.tla HwKObjs partition + 5 hw invariants apply uniformly without new state). **F180 (P3 deferred)**: SYS_DMA_MAP accepts vaddr ≥ 2^47; first access → kernel extinction via R10 F158's MMU-layer reject. Pre-existing pattern for SYS_MMIO_MAP. Deferred with rationale (consistent with R10's prior MMU-layer-reject decision; CAP_HW_CREATE-gated; proper fix is unified `burrow_map` upper-bound check across MMIO + DMA + anon — call it R12-vaddr; Phase 5+ note delivery converts remaining extinctions to SIGSEGV-equivalent). Documented as Caveat #10 in `docs/reference/39-hw-handles.md`. **Doc-only close** — no source changes. 232/232 PASS × default + UBSan unchanged. Predecessor: R12-gic-edge (`4544b40` / `495e672`).
- **Earlier tip**: **R12-gic-edge formal audit close landed — `dsb sy` after ICFGR write + extinction-on-bad-precondition + `g_max_intid` runtime bound (F200 + F201 + F204 + F205 closed; F202 + F203 deferred).** Closes the R12-gic-edge deferred audit (carried since P4-Ic5b2; self-audited at landing but formal pass deferred per CLAUDE.md trigger-surface discipline). Adversarial prosecutor surfaced 0 P0 + 0 P1 + **2 P2** + 3 P3. **F200 (P2)**: added `dsb sy` after the `GICD_ICFGR<n>` write in `gic_set_spi_edge_triggered` so the distributor's internal latching is observable before the subsequent `GICD_ISENABLER<n>` write in `gic_enable_irq` (ARM IHI 0069 §3.4.2 + Linux's GICv3 pattern). Without it, a strict GIC implementation could process ENABLE before the new edge config latches → first IRQ fires as level-triggered (storm). QEMU virt latches in zero time so not reachable today; forward-looking for Pi 5. **F201 (P2)**: converted `gic_set_spi_edge_triggered` from `bool` to `void` + `extinction()` on both failure preconditions (intid out of `[GIC_SPI_MIN, g_max_intid]` OR `g_dist_base == 0`). Both are kernel-internal-bug indicators — callers enforce via `kobj_irq_create`'s `intid >= 32` guard + `intid_try_claim`'s `g_max_intid` bound + boot ordering. Closes the R9-era rollback-discipline drift (the bool return was unchecked at the call site). **F204 (P3)**: `_Static_assert`s pin the ICFGR encoding's SBZ-bit-clear discipline (IHI 0069 §12.9.7) against future drift. **F205 (P3)**: exposed `u32 gic_max_intid(void)` accessor (runtime-discovered from `GICD_TYPER.ITLinesNumber`); tightened `intid_try_claim` to bound against `g_max_intid` rather than the architectural `GIC_NUM_INTIDS = 1020`. Without it, a `CAP_HW_CREATE` userspace caller could pass intid in `(g_max_intid, GIC_NUM_INTIDS]` and reach the GIC helpers with an unimplemented INTID → UNPREDICTABLE writes per IHI 0069 §12.9.7. **F202 (P3 deferred)**: ICFGR RMW SMP-safety — v1.0 serial-claim discipline holds; Phase 5+ concurrent driver-init revisit. **F203 (P3 deferred)**: stale `GICD_ICPENDR<n>` after edge-trigger re-claim — v1.0 doesn't exercise re-claim cycles. Both documented in `docs/reference/10-gic.md` as Caveats. New regression test `handle_hw.irq_out_of_range_rejected` pins F205. Files: `arch/arm64/gic.{h,c}` (helper sig change + dsb + static_asserts + accessor), `kernel/irqfwd.c` (tighter bound + commentary), `kernel/test/test_handle_hw.c` + `kernel/test/test.c` (new test), `docs/reference/10-gic.md` (API + Status + 2 new Caveats). **Audit-bearing per CLAUDE.md trigger surface** (Scheduler / IRQ-side). 231 → 232 tests PASS × default. Predecessor: R12-bss-2mib (`c4021ca` / `20de446` / `b9e7406`).
- **Earlier tip**: **R12-bss-2mib landed — kernel.ld assert tightened to encode the actual image + firmware ≤ 2 MiB L3 constraint.** The L3 mapping at `arch/arm64/mmu.c::build_page_tables` covers exactly the 2 MiB block `[kernel_2mib_pa, kernel_2mib_pa + 2 MiB)` where `kernel_2mib_pa = pa_kernel_start & ~(BLOCK_SIZE_L2 - 1)`. On QEMU virt with `KERNEL_LOAD_PA = 0x40080000`, that's `[0x40000000, 0x40200000)`; the 512 KiB firmware reservation sits inside (mapped invalid per R6-B F114), the kernel image follows. The original assert `(_kernel_end - _kernel_start) < 0x200000` checked image alone — allowing image_size up to 2 MiB while the real cap is 1.5 MiB with the firmware offset. P4-Ic7 fell off this cliff when image_size briefly grew from 1440 → 1572 KiB; P4-Jc preemptively shrank test-binary blob caps 128 → 96 KiB as a stopgap, leaving R12-bss-2mib as the canonical fix. New assert: `(_kernel_end - _kernel_start) + (KERNEL_LOAD_PA & (0x200000 - 1)) < 0x200000`. Linker evaluates the AND at link time (= 0x80000 = 512 KiB on QEMU virt); image cap becomes 1.5 MiB. If KERNEL_LOAD_PA ever moves to a 2 MiB boundary, the AND evaluates to 0 and the assert reduces to image-only `< 2 MiB` — no special-casing. Current image_size 0x14a000 (1320 KiB) + 0x80000 = 0x1ca000 under 2 MiB; 216 KiB headroom. New "Kernel image + firmware reservation must fit in a single 2 MiB L3 block" Caveat in `docs/reference/03-mmu.md` documents the constraint with three legitimate fixes if image > 1.5 MiB is ever needed: (a) reduce firmware offset; (b) extend build_page_tables to map a second 2 MiB block; (c) demote the L2 entry to multiple page-grain L3 tables. The P4-Jc bundled blob-cap shrink (96 KiB per test blob) is preserved as operational headroom for near-term growth — the load-bearing constraint is now the assert, not the cap discipline. **Audit-bearing at the kernel.ld / Initial bringup category** (CLAUDE.md trigger surfaces); self-audit clean: the assert IS the regression test. Closes the R12-bss-2mib deferred audit item. 231/231 PASS × default (~431 ms) + UBSan (~471 ms); 5/5 KASLR sample distinct. Predecessor: P4-Jc (`dbf1425` / `7ac0f2c`).
- **Earlier tip**: **P4-Jc landed — virtio-net-loop: steady-state RX recycling + TX descriptor reuse; foundation for 9P-over-TCP in Phase 5.** New userspace binary `usr/virtio-net-loop/` (~640 LOC Rust) closes the steady-state gap left by P4-Jb's single-shot probe. Two new mechanisms: (1) RX descriptor recycling — each consumed RX descriptor is re-published to the avail ring at `rx_avail_idx % QUEUE_SIZE` with wrapping bump; (2) TX descriptor reuse via pre-init static `descriptor[k] → buffer[k]` mapping; `avail.ring[k] = k` for k in 0..16; send_pending_tx just advances tx_avail_idx and the device cycles through descriptors as avail.idx grows. Target volume N = 24 round-trips through 16 descriptors on each side — proves descriptor wraparound on both TX (descriptor 0 reused at TX#17) and RX (descriptor 0 recycled after first frame consumed). In-flight guard `tx_avail_idx.wrapping_sub(tx_seen_used) < QUEUE_SIZE` enforces no-reuse-while-device-owns. Loop body: irq_wait → ack → drain_tx → drain_rx+recycle → send_pending_tx. u16-wraparound discipline on all 4 counters via `wrapping_add(1)` + `!=`. Compile-time `const _: () = { assert!(...) }` pins TX-pool fits in ring DMA. No new substrate (same composed hw-handle surface as virtio-net-arp). Wire validation: 24 ARP requests + 24 slirp replies bit-exact (`THYLACINE_NET_DUMP=path`). Bundled blob-cap shrink: all 8 test-binary blobs 128 → 96 KiB to keep image+firmware ≤ 2 MiB (largest binary is virtio-net-arp at 76 KiB; 20 KiB headroom per blob). Image_size: 1448 → 1352 KiB (net −96 KiB); 184 KiB headroom under image+firmware ≤ 2 MiB. pretouch_rodata_pages() pinned to page 1 (LOAD = 4974 B; objdump-confirmed). 230 → 231 tests. **Non-audit-bearing** at per-syscall layer; audit-bearing at userspace-driver discipline (self-audit clean). 231/231 PASS × default (~442 ms) + UBSan (~478 ms); 27/30 KASLR distinct. Predecessor: P4-Jb (`f53ad75` / `a084edb`).
- **Earlier tip**: **P4-Jb landed — virtio-net-arp: full ARP request/reply round-trip (first frame received from outside the guest).** New userspace binary `usr/virtio-net-arp/` (~590 LOC Rust) closes the wire-validation half left open by P4-Ja. Same composed hw-handle surface as P4-Ja plus a second `kobj_dma_create` (32 KiB) for a pool of 16 × 2 KiB RX buffers. Init configures BOTH virtqueues (RX=0, TX=1) before DRIVER_OK; 16 RX descriptors pre-published with VIRTQ_DESC_F_WRITE. After ARP TX kick, drains the RX used ring on each IRQ wake; first ARP reply with sender_ip=10.0.2.2 + target_ip=10.0.2.15 wins. Slirp's reply: sender_mac=52:55:0a:00:02:02 + opcode=2; observed end-to-end and verified bit-exact. **Key insight**: receiving a valid ARP reply is itself evidence the device delivered our TX frame — slirp wouldn't synthesize a reply otherwise. No separate TX-used.idx check needed (cleaner state machine). Single-buffer-per-frame mode (declined VIRTIO_NET_F_MRG_RXBUF). pretouch_rodata_pages() bounded to page 1 (LOAD spans 0..1; ~5.4 KiB; objdump-confirmed). Image_size: 1320 → 1448 KiB (+128 KiB blob); 88 KiB headroom remains under image+firmware ≤ 2 MiB. 229 → 230 tests. **Non-audit-bearing** at the per-syscall layer; audit-bearing at the userspace-driver discipline layer (self-audit clean). 230/230 PASS × default (~448 ms) + UBSan (~470 ms); 27/30 KASLR distinct. Predecessor: P4-Ja (`c1b7845` / `542f2a6`).
- **Earlier tip**: **P4-Ja landed — virtio-net-probe: second composed userspace driver (first NIC frame on the wire).** New userspace binary `usr/virtio-net-probe/` (~430 LOC Rust) proves the userspace-driver discipline generalizes to a different VirtIO device class. Same composed hw-handle surface as virtio-blk-probe (MMIO + IRQ + DMA), different DeviceID (1 = NET) + different device-specific config-space layout (MAC + status + max_virtqueue_pairs at offset 0x100). VirtIO 1.2 init: RESET → ACK → DRIVER → DeviceFeatures (require VIRTIO_F_VERSION_1; opportunistically accept VIRTIO_NET_F_MAC + VIRTIO_NET_F_STATUS; reject MRG_RXBUF + CTRL_VQ) → DriverFeatures → FEATURES_OK → configure TX queue (index 1; QUEUE_SIZE=16) → DRIVER_OK. **TX-only** in P4-Ja (RX queue 0 intentionally not configured; P4-Jb adds it). Submits 2-descriptor chain (12-byte zero `virtio_net_hdr` + 42-byte broadcast ARP request "who-has 10.0.2.2 tell 10.0.2.15"), kicks `QueueNotify=1`, waits on `SYS_IRQ_WAIT`, verifies `used.idx == 1`. QEMU slirp responds with an ARP reply at the host side (verified via `THYLACINE_NET_DUMP=path tools/test.sh` filter-dump pcap: 64-byte reply 52:55:0a:00:02:02 → 52:54:00:12:34:56) but the reply is dropped at the device level because RX QueueReady=0. `tools/run-vm.sh` adds `-netdev user,id=net0` + `-device virtio-net-device,netdev=net0,mac=52:54:00:12:34:56` BETWEEN disk_flags and rng-devices; virtio-blk keeps slot 31, virtio-net lands at slot 30 (INTID 78). `THYLACINE_NO_NET=1` disables; `THYLACINE_NET_DUMP=path` captures via `-object filter-dump`. No pretouch_rodata_pages() (LOAD segment fits within page 0; .text + .rodata = ~3.2 KiB). Image_size: 1188 KiB → 1320 KiB (+128 KiB blob); 216 KiB headroom under the image+firmware ≤ 2 MiB L3 mapping constraint. 228 → 229 tests. **Non-audit-bearing** at per-syscall layer (all hw-handle syscalls audited at R9/R10/R11/Ic5b1b); audit-bearing at the userspace-driver discipline layer (self-audit clean). 229/229 PASS × default (~422 ms) + UBSan (~454 ms); 48/50 KASLR distinct (pre-existing 2-4% host flake). Predecessor: P4-Ic7 (`f3924dd` / `fa65376`).
- **Earlier tip**: **P4-Ic7 landed — Multi-sector virtio-blk read/write driver; ROADMAP §6.2 exit criterion CLOSED.** New userspace binary `usr/virtio-blk-rw/` (~520 LOC Rust) extends P4-Ic5b2's single-sector probe with three-pass multi-sector r/w: pass A reads + verifies the on-disk LCG-A pattern over the READ region; pass B writes the distinct LCG-B pattern to the disjoint WRITE region; pass C reads back + verifies pattern B bit-exact. Per-pass byte count = min((capacity − 1 sector) / 2, 1 GiB) rounded to 1-MiB granularity. Two `kobj_dma_create` allocations (4 KiB ring buffer + 1 MiB data buffer at KOBJ_DMA_MAX_SIZE); 3-descriptor chain reused per request with desc[1].flags flipping between `NEXT` (T_OUT) and `NEXT \| WRITE` (T_IN). Disk image now built by `tools/mkdisk.py` (new) — deterministic LCG-A bytes across all sectors past sector 0; sector 0 keeps the "THYLACINE-DISK-1" signature. `THYLACINE_DISK_SIZE` env (default 16M; 2G enables the strict 1 GiB R + 1 GiB W). Pattern A/B constants pinned across mkdisk.py + main.rs. **Non-overlapping regions** make the test idempotent across re-runs. `pretouch_rodata_pages()` reads page 1 of the LOAD segment so SYS_PUTS's kernel-mode user-VA dereference doesn't take an unhandled translation fault on .rodata strings past 0x401000 (Phase 5+ should fix at the kernel layer via uaccess-emulation). Cumulative-.bss adjustment: shrunk mmio-probe + irq-probe + userspace-ramfs ELF blobs 256 → 128 KiB each (frees 384 KiB) so kernel image stays under the 2 MiB L3-kernel mapping constraint (firmware-area 512 KiB + image ≤ 2 MiB; arch/arm64/mmu.c::mmu_map_kernel). 227 → 228 tests. **Default 16 MiB disk**: 7 MiB per pass × 3 passes = 21 MiB I/O; ~30 ms added to suite. **2 GiB disk stress**: 1023 MiB × 3 = 3 GiB I/O; full suite + stress in ~2.8 s, satisfying ROADMAP §6.2 "read 1 GiB / write 1 GiB / verify bit-exact." 228/228 PASS × default + UBSan; 30/30 KASLR distinct. Predecessor: P4-Ic6-impl (`3c1951d` / `c87779e`).
- **Earlier tip**: **P4-Ic6-impl landed — R12-sched resolution: boot CPU dedicated idle thread + deadlock-path fallback.** Closes the kernel's "no runnable peer system-wide" extinction by allocating `g_bootcpu_idle` (distinct from kthread, in BAND_IDLE) and using it as the explicit fallback in `sched()`'s deadlock path. New `bootcpu_idle_main()` runs the WFI loop body symmetric to `per_cpu_main` on secondaries. **Off-tree by design**: bootcpu_idle is NOT ready()'d into BAND_IDLE's run tree — preempt_check_irq runs at SpSel=1, where pick_next finding bootcpu_idle would trigger a context switch that clobbers SP_EL1 (= per-CPU exception stack). By using it only via the deadlock path (which fires from voluntary sched() callers at SpSel=0), cpu_switch_context naturally writes SP_EL0 and exception stack discipline is preserved. Test conversion: `test_virtio_blk_probe.c` switched yield-poll loop → `wait_pid` (the R12-sched workaround is retired). Plan 9 heritage: bootcpu_idle is the Thylacine analog of Plan 9's per-CPU `runproc()` idle path. Closes R12-sched + R12-sched-impl deferred audits; trip hazards #181 + #185 CLOSED. Direction B (separate boot-CPU idle) chosen over Direction A (refactor on_cpu) — reuses existing P2-Cf wakeup discipline unchanged. ~143 LOC net additions. 227/227 PASS × default + UBSan; 30/30 KASLR distinct. Predecessor: P4-Ic6-cfg (`849c053` / `719dedd`).
- **Earlier tip**: **P4-Ic6-cfg landed — R12-wfi-cfg resolution: Wake/WakeAll wfi-clear refinement + `scheduler_liveness_wfi.cfg` reparameterization.** Spec-only chunk that closes the pre-existing wfi-cfg LatencyBound violation surfaced at P4-Ic6-spec. Investigation revealed two distinct issues: (1) a real spec-modeling gap where `Wake(t)` and `WakeAll` could enqueue work into a wfi'd CPU's runq with no escape mechanism — no impl trace exhibits this; the impl invariant is that wakeup either directly wakes the target CPU via hardware IRQ delivery or IPIs it. Fix: `Wake(t)` atomically clears `wfi[cpu]` of its CHOOSE'd target; `WakeAll` atomically clears `wfi[cpu0]` conditional on `waiters # {}`. Sound abstraction for liveness — collapses both impl paths into a single atomic spec step with the correct terminal `~wfi[target]`. (2) A pre-existing CHOOSE-determinism artifact at 2T × 2C: even with Fix A applied, TLC found a different lasso where t1 alternates RUNNING/SLEEPING via Block/Wake while t2 gets Steal'd between c1 and c2 indefinitely, never picked by Resume. Same artifact `scheduler_liveness.cfg` works around with 2T × 1C (per its own comment, deferring per-thread `Resume(cpu, t)` fairness refinement to Phase 5+ alongside EEVDF weights). Cfg reparameterized 2T × 2C → 1T × 2C; preserves WFI dynamics check (EnterWFI / NotifyWFIPeer / IPI_Deliver fairness + Wake's wfi-clear) while eliminating the artifact. F77/F78 negative-case still verified by `scheduler_buggy_wfi.cfg` (2T × 2C, drops WFI fairness, produces stuttering counterexample). State count effects: `scheduler.cfg` 25416 → 15840 distinct; `scheduler_buggy_wfi.cfg` 5760 → 3888; `scheduler_liveness_wfi.cfg` 5760 (broken) → 1008 (clean). All 4 correct cfgs + 6 buggy cfgs TLC-verified as expected. **Spec-only chunk**: 227/227 tests unchanged. Closes R12-wfi-cfg deferred audit. Predecessor: P4-Ic6-spec (`f050512` / `25b4330`).
- **Earlier tip**: **P4-Ic6-spec landed — `scheduler.tla` extension for R12-sched (hardware-wake fairness).** Spec-first chunk. Adds `HardwareWakeProgress` temporal property + `Liveness_Hwake` fairness clause + `Spec_Live_Hwake` + two new cfgs (`scheduler_liveness_hwake.cfg` 23 states clean; `scheduler_buggy_hwake.cfg` counterexample at depth 4). `Wake(t)` action's existing semantics already abstract `kobj_irq_dispatch`'s wakeup(); the change is a fairness refinement (SF on Wake) encoding "hardware IRQs eventually deliver." Impl deferred — replacing `kernel/sched.c`'s ELE-on-deadlock-check requires architectural decisions about the `on_cpu` protocol's intersection with WFI-in-place (refactor on_cpu OR introduce a separate boot-CPU idle thread distinct from kthread). Spec captures intent; impl direction picked next session. **R12-wfi-cfg pre-existing finding now CLOSED at P4-Ic6-cfg** (see Tip above). **Spec-only chunk**: no kernel changes, no test changes, no runtime impact. 227/227 tests unchanged. Predecessor: P4-Ic5b2 (`fe40f1b` / `d811c0f`).
- **Earlier tip**: **P4-Ic5b2 landed — Rust no_std virtio-blk driver crate (first composed userspace driver).** New `usr/virtio-blk-probe/` crate (~430 LOC Rust) drives QEMU virt's `virtio-blk-device` end-to-end: scans virtio-mmio slots 0..31 for `DeviceID == 2`, claims its 4-page MMIO bank via `t_mmio_create`+`t_mmio_map`, subscribes to slot IRQ via `t_irq_create(48+slot)`, allocates a single 4 KiB DMA buffer (descriptor table + avail/used rings + req header + data buffer + status byte at fixed offsets) via `t_dma_create`+`t_dma_map`, runs VirtIO 1.2 modern transport init (RESET → ACK → DRIVER → DeviceFeatures readback → DriverFeatures write (VIRTIO_F_VERSION_1) → FEATURES_OK readback → virtqueue 0 setup with size 16 → DRIVER_OK), issues `VIRTIO_BLK_T_IN` for sector 0, kicks via QueueNotify, sleeps on `t_irq_wait`, ACKs InterruptStatus via InterruptACK, verifies `used.idx == 1` + `status == VIRTIO_BLK_S_OK` + data buffer first 16 bytes match `"THYLACINE-DISK-1"`. **First binary to compose all four hw-handle syscalls** (MMIO + IRQ + DMA) in a single Proc; closes the DMA-side SVC-path coverage gap. New `tools/build.sh disk` target produces `build/disk.img` (1 MiB raw, block 0 = "THYLACINE-DISK-1\0..." + zeroes). `tools/run-vm.sh` attaches `-drive if=none,id=disk0,format=raw,file=...` + `-device virtio-blk-device,drive=disk0` + `-global virtio-mmio.force-legacy=false` (the modern Version=2 transport per VIRTIO 1.2; QEMU virt defaults to legacy). New `gic_set_spi_edge_triggered(intid)` in `arch/arm64/gic.{c,h}` flips ICFGR to edge-triggered for the requested SPI; `kobj_irq_create` calls it for SPIs ≥ 32 (QEMU virt's virtio-mmio + most ARM device IRQs are edge per DTB). Test-side `test_virtio_blk_probe.c` uses a yield-poll pattern (`sched()`+`wfi` in a loop until child is ZOMBIE) instead of `wait_pid` to keep the parent thread runnable while the child sleeps in `t_irq_wait` — workaround for a scheduler limitation (no model for "hardware IRQ will wake us"; documented as R12-sched deferred audit). 226 → 227 tests. **Non-audit-bearing at the per-syscall layer** (all four syscalls audited at R9/R10/R11/Ic5b1b); audit-bearing at the **GIC ICFGR transition** + scheduler-deadlock-check-workaround layers (self-audit found no findings; lifted to R12-sched + R12-gic-edge deferred). Reference docs `docs/reference/39-hw-handles.md` updated (virtio-blk-probe test row, edge-triggered status, scheduler-deadlock footgun). Predecessor: P4-Ic5b1b (`0891c4a` / `7e747fa`).
- **Earlier tip**: **P4-Ic5-FP landed — kernel eager FP/SIMD save/restore + userspace SIMD re-enabled.** Closes the P4-Ic5a toolchain caveat. `struct Context` extended with V0..V31 + FPSR + FPCR (120 B → 656 B; `_Alignas(16)` for STP/LDP Q-reg alignment); `cpu_switch_context` extends with 16 STP-Q pairs save + matching LDP-Q pairs restore + MRS/MSR FPSR/FPCR via S3_3_C4_C4_{1,0} generic encoding; `.arch_extension fp` admits Q-reg instructions in context.S while rest of kernel stays `-mgeneral-regs-only`. New `fp_enable_this_cpu()` header-inline sets `CPACR_EL1.FPEN = 0b11` (no FP/SIMD trap at any EL); called from `boot_main` (primary) + `per_cpu_main` (secondaries). `usr/.cargo/config.toml` drops `-neon,-fp-armv8` rustflags; `mmio-probe` simplified (removes `static mut HEX_BUF` workaround). struct Thread 232 B → 784 B (16-aligned now). 213/213 tests unchanged. **Audit-bearing** per CLAUDE.md trigger surfaces (Scheduler + Initial bringup); change is bounded + mechanical (symmetric STP/LDP pairs + one MSR per CPU + struct-field append). Self-audit findings none; formal R12-FP pass may run post-hoc. Predecessor: P4-Ic5b1a (`7415bd3` / `11920e3`).
- **Earlier tip**: **P4-Ic5b1a landed — virtio-mmio reservation refinement (relax over-broad R10 F154).** Removes the `dtb_for_each_compat_reg("virtio,mmio", ...)` walk from `kobj_mmio_reserve_kernel_ranges()`. The original F154 close pre-reserved every `virtio,mmio` compatible (rationale matched GIC/PL011/ECAM: prevent userspace from claiming a kernel-active range), but the kernel only touches virtio-mmio during `virtio_init` boot probe — after enumeration the entire register surface is dormant from kernel perspective. The reservation blocked the legitimate Phase 4 driver-process consumer (P4-Ic5b2 virtio-blk). At v1.0 trust boundary (only kproc grants `CAP_HW_CREATE`), the per-slot delegation API alternative adds complexity without adding protection. GIC/PL011/ECAM reservations stay (kernel actively reads/writes those). Boot reports 4 reserved ranges (was 8). New positive test `mmio_handle.virtio_mmio_claimable`. 212 → 213 tests. **Audit-bearing** at the policy level — relaxes a prior P1-era reservation; documented + scoped + Phase-5+-revisit-flagged in `kernel/mmio_handle.c` comment block. Predecessor: P4-Ic5-IRQ-probe (`0c7a51b` / `221cd18`).
- **Earlier tip**: **P4-Ic5-IRQ-probe landed — `/irq-probe` companion to mmio-probe.** Closes the IRQ side of deferred R10 F159 (SVC-path test coverage for `SYS_IRQ_CREATE` + `SYS_IRQ_WAIT`). With both probes landed, all four hw-handle syscalls now have end-to-end userspace SVC-path coverage. New `usr/irq-probe/` Rust crate; new `kernel/test/test_irq_probe.c::userspace.irq_probe_rfork_with_caps`; new helper `gic_set_pending_spi(intid)` in `arch/arm64/gic.{h,c}` to enable a race-free pre-pend pattern (kernel test pre-pends SPI 96 BEFORE spawning the child; the IRQ delivers when the child's `t_irq_create` enables it, because the GIC's pending bit is orthogonal to the enable bit). **Bundled bug fix**: `libthyla-rs::T_RIGHT_SIGNAL` was `1<<4` (collided with kernel's `RIGHT_DMA = 1<<4`); corrected to `1<<5` and the missing `T_RIGHT_DMA = 1<<4` + `T_RIGHT_ALL = 0x3f` added. Bug was dormant under P4-Ic5a (mmio-probe used only READ + MAP); irq-probe was the first user of `T_RIGHT_SIGNAL` and surfaced the drift in the first test run (`SYS_IRQ_WAIT` returned -1 because the handle held `RIGHT_DMA` instead of `RIGHT_SIGNAL`). **Non-audit-bearing** at the formal trigger-surface level: the IRQ SVC handlers (`sys_irq_create_handler` + `sys_irq_wait_handler`) and `kobj_irq` lifecycle were both audited at R9; `gic_set_pending_spi` is a single MMIO write to a documented GIC register; libthyla-rs is a userspace ABI mirror. 211 → 212 tests. Predecessor: P4-Ic5a (`17855dd` / `b96db5a`).
- **Earlier tip**: **P4-Ic5a landed — `/mmio-probe` first userspace hw-handle binary.** First non-kproc Proc to hold `CAP_HW_CREATE`; first userspace `SYS_MMIO_CREATE` + `SYS_MMIO_MAP` end-to-end; reads live PL031 RTC at user-VA via the demand-page MMIO dispatch (device-memory PTE installed lazily on first access; verifies PeriphID0 == 0x31). Closes the bulk of deferred R10 F159 (SVC-path test coverage for SYS_MMIO_CREATE + SYS_MMIO_MAP). Boot output: `mmio-probe: rtcdr=0x6a018e2e periphid0=0x00000031 / PASS`. Required toolchain shifts: kernel `-mcmodel=tiny` → `-mcmodel=small` (image grew past tiny's 1 MiB ADR_PREL range; small lifts to 4 GiB ADRP+ADD), userspace Rust `target-feature=...,-neon,-fp-armv8` (kernel doesn't enable EL0 FP/SIMD; precompiled compiler-builtins memset used NEON `dup v0.4h` which trapped EL0). 210 → 211 tests. Predecessor: P4-Ic4 (`caa49e3` / `81da4eb`).
- **Earlier tip**: **P4-Ic4 landed — `libthyla-rs` runtime crate extraction.** Lifts `_start` (global_asm!), `#[panic_handler]`, and the SVC wrappers (`t_exits`, `t_puts`, `t_putstr`, `t_mmio_create`, `t_mmio_map`, `t_irq_create`, `t_irq_wait`) + syscall/right/prot constants out of `usr/hello-rs/src/main.rs` into a new `usr/lib/libthyla-rs/` crate (sibling of C-side `usr/lib/libt/`). hello-rs body collapses to ~10 lines. ELF layout pinned identical (66,336-byte binary; `_start` at 0x400000; `rs_main` at 0x400018). Linker keeps `_start` across the rlib boundary via `ENTRY(_start)` in `usr/scripts/aarch64-userspace.ld`. Non-audit-bearing pure Rust refactor. Unblocks P4-Ic5 (virtio-blk driver crate) by giving it a `libthyla-rs` dependency to import. 210/210 tests pass unchanged. Predecessor: P4-Ic3 (`031a91f` / `ed6c587`).
- **Earlier tip**: **P4-Ic3 landed — kernel-internal `rfork_with_caps(flags, entry, arg, caps_mask)` capability grant primitive (spec-first).** Closes the v1.0 capability-grant hole that blocked P4-Ic5+: kproc can now spawn a driver Proc holding `CAP_HW_CREATE` via `rfork_with_caps(RFPROC, drv_entry, arg, CAP_HW_CREATE)`. Implementation: `child->caps = __atomic_load_n(&parent->caps, ACQUIRE) & caps_mask` — the AND with the parent's current caps enforces spec invariant `granted ⊆ proc_caps[parent]` regardless of what mask the caller passes. `rfork` becomes a delegate (`return rfork_internal(..., CAP_NONE)`); no churn in existing call sites. Spec-first extension at `specs/handles.tla`: new `proc_ceiling` state var (dynamic per-Proc ceiling — ProcRoot=Caps at Init, child=`proc_caps[parent]` at fork); strengthened `CapsCeiling` to use `proc_ceiling`; new `RforkWithCaps` action; new `BuggyRforkElevate` bug class + `handles_buggy_rfork_elevate.cfg`. **TLC verified across 9 cfgs**: correct cfg 11.7M distinct states clean (8m55s, depth 25); 8 buggy cfgs each produce expected counterexamples. 3 new tests in `kernel/test/test_caps.c`. 207 → 210 tests. ~50 LOC kernel + ~80 LOC spec extension. Predecessor: P4-Ic2 (`39c4de3` / `0e9f1ff`).
- **Earlier tip**: **P4-Ic2 landed — `SYS_MMIO_MAP` syscall + device-memory PTE attrs + demand-page MMIO dispatch + R10 audit closed (1 P1 + 2 P2 + 2 P3 fixed, 1 P3 deferred).** Installs user-VA mappings for KObj_MMIO regions. `mmu_install_user_pte` extended with `bool device_memory` parameter selecting `MAIR_IDX_DEVICE` (nGnRnE) vs `MAIR_IDX_NORMAL_WB`. `userland_demand_page` dispatches on `vma->burrow->type` for PA resolution + device-memory flag. New SYS_MMIO_MAP=5 syscall with cap check + handle validation + RIGHT_MAP bound + burrow_create_mmio + burrow_map + ownership-transfer pattern. PTEs install lazily via demand-paging. Libt `t_mmio_map` + `T_PROT_*` macros. R10 P1 fix: new `kobj_mmio_reserve_kernel_ranges()` walks DTB for GIC/PL011/ECAM/virtio,mmio compatibles and pre-reserves them in `g_mmio_claims` so a CAP_HW_CREATE driver can't claim kernel-owned MMIO (structural mirror of R9 F142). R10 P2 fixes: W-only-without-READ rejected (AArch64 has no W-only AP encoding); IPS-bound rejected at `kobj_mmio_create`. R10 P3 fixes: EXEC+device-memory extincts in `make_user_pte_l3`; user-VA vaddr check tightened bit-47→bit-48. R10 F159 deferred (SVC-path test coverage — natural fit at P4-Ic5+). 5 new tests; 202 → 207. Predecessor: P4-Ic1 (`72e023f` / `a1c9b6a`).
- **Earlier tip**: **P4-Ic1 landed — Burrow MMIO-type extension.** New `BURROW_TYPE_MMIO` enum + `kobj_mmio` and `pa` fields on `struct Burrow`. `burrow_create_mmio(struct KObj_MMIO *)` constructor refs the underlying kobj; `burrow_free_internal` type-dispatches between ANON (`free_pages`) and MMIO (`kobj_mmio_unref` + `kfree`). Lifecycle invariants unchanged (TLC re-verified clean). 6 new tests; 196 → 202. Foundation for P4-Ic2 (`kobj_mmio_map_into_user` syscall + demand-page MMIO dispatch + device-memory PTE attrs). Predecessor: P4-Ib (`3876a97` / `957284c`).
- **Earlier tip**: **P4-Ib landed — KObj_MMIO + KObj_IRQ handle-table integration + capability gating (spec-first).** Three new invariants pin the hardware-handle creation semantics in `specs/handles.tla`: NoHwDup, HwResourceExclusive, HwHandleImpliesCap. All TLC-verified — correct cfg 1.4M distinct states clean; 7 buggy cfgs produce expected counterexamples. New `<thylacine/caps.h>` with `CAP_HW_CREATE` + `CAP_ALL`. `struct Proc.caps` field added (120 → 128 bytes); kproc gets CAP_ALL, rfork'd children get CAP_NONE. New `<thylacine/mmio_handle.h>` + `kernel/mmio_handle.c` with struct KObj_MMIO + g_mmio_claims[32] PA-range tracking under g_mmio_lock (overlap rejection enforces HwResourceExclusive). New INTID claim layer in `kernel/irqfwd.c` (g_intid_claimed[1020] + intid_try_claim/release) — fixes pre-existing HwResourceExclusive hole where gic_attach silently overwrites. handle.c: handle_dup rejects hw kinds (NoHwDup); handle_release_obj/acquire_obj wire kobj_mmio_unref/ref + kobj_irq_unref/ref. Three new syscalls (SYS_MMIO_CREATE=2, SYS_IRQ_CREATE=3, SYS_IRQ_WAIT=4) with cap check + arg validation + rollback. libt inline SVC wrappers (t_mmio_create / t_irq_create / t_irq_wait) + T_RIGHT_* macros. 15 new tests (3 caps + 8 mmio_handle + 4 handle_hw); 180 → 195. kobj_mmio_map_into_user deferred to P4-Ic (needs Burrow MMIO-type extension). Reference docs: `docs/reference/39-hw-handles.md` (new, ~330 lines); `docs/reference/19-handles.md` extended. R9 adversarial audit closed (1 P0 + 3 P1 + 3 P2 + 3 P3 fixed; 2 P3 deferred — see `memory/audit_r9_closed_list.md`). Predecessors: P4-Ia2 (`48692c6` / `051c20c`). P4-Ia1 (`5467725` / `e58cbc3`). Handoff 027 (`f23ab34`). Earlier — see Phase 4 status doc.
- **Phases**: Phase 0 done; Phase 1 CLOSED at `ceecb26`; Phase 2 CLOSED at `5914230`; Phase 3 CLOSED at `c2d7886`. **Phase 4 OPEN; P4-A through P4-H + P4-Fix157 + P4-Ia1 + P4-Ia2 + P4-Ib + P4-Ic1 + P4-Ic2 + P4-Ic3 + P4-Ic4 + P4-Ic5a + P4-Ic5-IRQ-probe + P4-Ic5b1a + P4-Ic5-FP + P4-Ic5b1b + P4-Ic5b2 + P4-Ic6-(spec/cfg/impl) + P4-Ic7 + P4-Ja + P4-Jb + P4-Jc + P4-Ic-latency + P4-K + P4-L + R12-bss-2mib + R12-gic-edge + R12-DMA + R12-FP + R12-vaddr + R12-uaccess landed.** Four device classes now operational on the composed substrate: virtio-blk (READ + WRITE 1 GiB bit-exact; ROADMAP §6.2 CLOSED), virtio-net (full ARP request/reply round-trip + steady-state RX recycling), virtio-input (probe-only at DRIVER_OK; event consumption deferred), virtio-gpu (probe-only at GET_DISPLAY_INFO response; scanout deferred to Halcyon prep). IRQ-to-userspace latency benchmark CLOSED on the infrastructure side; bare-metal authoritative verification deferred to Phase 5+. All Phase 4 audit-trigger-surface deferred audits closed (Scheduler / Initial bringup / IRQ / Handle table / VMO / Capability checks / Page fault); 3 P3 deferred with rationale (F180 now CLOSED via R12-vaddr; F202 ICFGR-RMW-SMP-safety + F203 stale-ICPENDR-on-re-claim both v1.0-not-reachable). Phase 4 sub-chunks remaining: P4-Id (driver-as-9P-server; needs replan), P4-K-events (event consumption via QMP send-key), P4-L-scanout (virtio-gpu actual pixels), P4-M (driver supervision), P4-N (BURROW finalize), P4-Z (Phase 4 closing audit), image-discipline (shrink legacy 96 KiB blob caps).
- **Tests**: **241/241 in-kernel tests PASS** + **4/4 fault matrix**. `tools/test.sh` PASS (~1420 ms default post-warm at N=128, ~1410 ms UBSan; +~980 ms vs the pre-bench ~440 ms baseline). `tools/verify-kaslr.sh` PASS (10/10 distinct; 30-boot stress 27/30 with pre-existing 2-4% host flake). `tools/test-fault.sh` PASS. **P4-Ic-latency adds `userspace.irq_latency_bench`** — measures IRQ-to-userspace handler p99 latency end-to-end via shared `KObj_DMA` region + `CNTPCT_EL0` reads on both sides; CI sanity budget `p99 < 50 ms` (QEMU TCG emulation overhead dominates real path latency; bare-metal target `< 5 µs` per VISION §4.5 logged for reference). R12-uaccess adds 4 in `kernel/test/test_uaccess.c`: `uaccess.fixup_table_well_formed`, `uaccess.fixup_lookup_known`, `uaccess.fixup_lookup_unknown_returns_zero`, `uaccess.load_u8_unmapped_user_va_returns_minus1`. R12-vaddr added `burrow.map_proc_user_va_top_boundary` (positive corner) + 3 new rejection assertions in `burrow.map_proc_constraints` (USER_VA_TOP cases). R12-FP adds `fp.cpacr_enabled` + `fp.round_trip_v_regs_fpsr_fpcr`. R12-gic-edge adds `handle_hw.irq_out_of_range_rejected`. P4-Jc adds `userspace.virtio_net_loop_rfork_with_caps` — steady-state virtio-net (24 ARP round-trips through 16-descriptor TX+RX rings; descriptor recycling on both sides). P4-Jb added `userspace.virtio_net_arp_rfork_with_caps` — full single-shot TX+RX ARP round-trip with slirp gateway. P4-Ja added `userspace.virtio_net_probe_rfork_with_caps` — TX-only first NIC frame.
- **Specs**: 4 written + **16 cfg variants**. `scheduler.tla` 15840 distinct states / 4 buggy + 1 liveness + 1 wfi-liveness (1T × 2C, reparameterized at P4-Ic6-cfg) + 1 wfi-buggy + 1 hwake-liveness + 1 hwake-buggy; `territory.tla` 2.56M states / 4 buggy (extended at P5-attach-mount with mount-table actions + MountRefcountConsistency invariant; cfgs: cycle / mount-no-refbump / unmount-no-refdrop / destroy-leak); `pipe.tla` (new at P5-pipe-blocking — wait/wake protocol; 4 buggy cfgs: write-no-wake-reader / read-no-wake-writer / close-write-no-wake-reader / close-read-no-wake-writer; NoStuckReader / NoStuckWriter specialize I-9 to the pipe's two-direction state machine); `handles.tla` 11.7M states / 5 buggy (added `BuggyRforkElevate` at P4-Ic3; P4-Ic5b1b adds commentary-only — the existing HwKObjs partition already pins all DMA invariants); `burrow.tla` 100 states / 3 buggy. All correct configs PASS, all buggy configs produce expected counterexamples. **P4-Ic6-cfg refinement**: `Wake(t)` and `WakeAll` actions now atomically clear `wfi[target]` (Fix A) — closes the spec gap where work could land in a wfi'd runq with no escape mechanism. State count effect: `scheduler.cfg` 25416 → 15840 distinct; `scheduler_buggy_wfi.cfg` 5760 → 3888; `scheduler_liveness_wfi.cfg` 5760 (was broken since P3-G landing) → 1008 (clean at 1T × 2C). R12-wfi-cfg deferred audit CLOSED.
- **LOC**: ~14100 kernel/asm + ~1850 TLA+ in `specs/` ≈ **~15950 LOC total**. P3-H adds: F127 user-VA bound check + 2 test updates (~50 LOC); F128 IRQ-mask discipline (~20 LOC); F129 TLB flush (~15 LOC); F134 cache maintenance loop (~25 LOC); F135 doc rewrite (~15 LOC); F136 one-call guard (~12 LOC); audit_r7_closed_list.md (~280 LOC).
- **Kernel ELF**: ~308 KB debug (production) / ~313 KB debug (UBSan). Flat binary: ~50 KB. Page tables: 40 KiB BSS. struct page array: 24 MiB BSS. GIC handler table: 16 KiB BSS. Vector table: 2 KiB in `.text`. `g_secondary_boot_stacks`: 112 KiB BSS. `g_exception_stacks`: 32 KiB BSS.

For phase-level status see `docs/phaseN-status.md`. The reference below covers the as-built layers in bottom-up order. Per-subsystem files appear as their subsystems land during Phase 1 onward.

---

## Contents

| File | Layer | Size guide | Status |
|---|---|---|---|
| [00-overview.md](reference/00-overview.md) | Layer cake + cross-cutting concerns | medium | scaffolded |
| [01-boot.md](reference/01-boot.md) | Boot path: start.S (incl. Linux ARM64 image header), kernel.ld, MMU, KASLR | medium | **P1-A + P1-B landed**; extended at P1-C (MMU + KASLR) |
| [02-dtb.md](reference/02-dtb.md) | FDT parser: lib/dtb.c. Memory + compat-reg + kaslr-seed lookups. | medium | **P1-B landed**; kaslr-seed consumed at P1-C-extras |
| [03-mmu.md](reference/03-mmu.md) | MMU + W^X invariant I-12: arch/arm64/mmu.{h,c}; PTE constructors with _Static_asserts; TTBR0 identity + TTBR1 high-half (KASLR-aware) | medium | **P1-C + P1-C-extras landed**; SMP secondary bring-up at Phase 2 |
| [04-extinction.md](reference/04-extinction.md) | Kernel ELE — extinction(msg) + ASSERT_OR_DIE; EXTINCTION: ABI prefix per TOOLING.md §10 | small | **P1-C landed**; first deliberate caller at P1-F (fault handler) |
| [05-kaslr.md](reference/05-kaslr.md) | KASLR (invariant I-16): arch/arm64/kaslr.{h,c}; entropy chain (DTB kaslr-seed / rng-seed / cntpct fallback); .rela.dyn relocator; long-branch into TTBR1 | medium | **P1-C-extras Part B landed**; entropy bump (13 → 18 bits) is post-v1.0 hardening |
| [06-allocator.md](reference/06-allocator.md) | Physical allocator: struct page, mm/buddy.{h,c}, mm/magazines.{h,c}, mm/phys.{h,c} — DTB-driven bootstrap, buddy free lists, per-CPU magazines, public API | medium | **P1-D landed**; SLUB layered on alloc_pages at P1-E |
| [07-slub.md](reference/07-slub.md) | SLUB kernel object allocator: mm/slub.{h,c} — per-cache partial slab list, embedded freelist, kmalloc / kfree / kmem_cache_*, bootstrap via meta cache | medium | **P1-E landed**; multi-page slabs (>2 KiB caches) post-v1.0 |
| [08-exception.md](reference/08-exception.md) | Exception handling: arch/arm64/{vectors.S, exception.{h,c}} — 16-entry vector table, KERNEL_ENTRY save, sync handler with stack-overflow + W^X violation detection, exception_unexpected catch-all | medium | **P1-F landed**; per-CPU exception stack at Phase 2; GIC + IRQ at P1-G |
| [09-test-harness.md](reference/09-test-harness.md) | In-kernel test harness: kernel/test/{test.{h,c},test_*.c} — sentinel-terminated registry, TEST_ASSERT macro, per-test PASS/FAIL reporting; 6 leaf-API tests at v1.0 | small | **landed**; host-side sanitizer matrix + 10000-iteration leak check at P1-I |
| [10-gic.md](reference/10-gic.md) | GIC v3 driver: arch/arm64/gic.{h,c} — DTB v2/v3 autodetect, distributor + redistributor + sysreg CPU interface, IRQ enable/disable/ack/eoi, handler dispatch table | medium | **P1-G landed**; v2 path + ITS + SMP redist walk deferred |
| [11-timer.md](reference/11-timer.md) | ARM generic timer: arch/arm64/timer.{h,c} — EL1 non-secure phys at 1000 Hz on PPI 14 (INTID 30), CNTP_TVAL_EL0 reload pattern, WFI-based busy wait | small | **P1-G landed**; oneshot + per-CPU SMP at Phase 2 |
| [12-hardening.md](reference/12-hardening.md) | Hardening enablement: toolchain flags, kernel/canary.c (`__stack_chk_guard` + `__stack_chk_fail`), arch/arm64/start.S PAC keys + SCTLR enable, arch/arm64/hwfeat.{h,c} ID-register inspection, PTE_GP for BTI on kernel text | medium | **P1-H landed**; CFI / MTE / `_FORTIFY_SOURCE` deferred with explicit rationale |
| [13-verification.md](reference/13-verification.md) | Phase 1 verification infrastructure: 10000-iter alloc/free leak checks, boot-time measurement, multi-boot KASLR variability, UBSan trapping build, deliberate-fault matrix (canary/W^X/BTI) | medium | **P1-I landed**; PAC deliberate-fault + KASAN deferred with rationale |
| [14-process-model.md](reference/14-process-model.md) | Process model bootstrap: struct Proc + struct Thread + struct Context + cpu_switch_context (asm) + thread_trampoline + thread_switch wrapper. TPIDR_EL1 = current_thread per CPU. SLUB caches for proc + thread. specs/scheduler.tla sketch (TLC-clean). | medium | **P2-A landed**; EEVDF math + wait/wake at P2-Bb/c; work-stealing at P2-C |
| [15-scheduler.md](reference/15-scheduler.md) | Scheduler dispatch: sched() + ready() + 3-band run tree (sorted by vd_t doubly-linked list) + per-CPU current via TPIDR_EL1 + struct Thread EEVDF fields. Simplified EEVDF (monotonic vd_t advance); full math at P2-Bc. | medium | **P2-Ba landed**; sched() refactored at P2-Bb to respect prev->state; preemption + EEVDF math at P2-Bc |
| [16-rendez.md](reference/16-rendez.md) | Plan 9 wait/wake (sleep + wakeup over Rendez): kernel/include/thylacine/rendez.h + sleep/wakeup impl in kernel/sched.c. spin_lock_irqsave for IRQ-mask discipline; single-waiter convention; cond-check + state-transition atomic under r->lock. Maps scheduler.tla WaitOnCond/WakeAll. | medium | **P2-Bb landed**; multi-waiter wait queues at Phase 5 (poll/futex) |
| [17-smp-bringup.md](reference/17-smp-bringup.md) | SMP secondary bring-up via PSCI + per-CPU exception stacks: lib/dtb.c cpu/psci enumeration + arch/arm64/psci.{h,c} HVC/SMC + arch/arm64/start.S secondary_entry (P2-Ca minimal; P2-Cb PAC+MMU+per_cpu_main; P2-Cc SPSel=0 + SP_EL0/SP_EL1 split + HIGH VA re-anchor) + kernel/smp.{h,c} smp_init/per_cpu_main/smp_cpu_idx_self + g_pac_keys/g_secondary_boot_stacks/g_exception_stacks/g_cpu_online/g_cpu_alive. | medium | **P2-Ca + P2-Cb + P2-Cc landed**; per-CPU run trees + idle threads + IPIs at P2-Cd |
| [18-territory.md](reference/18-territory.md) | Plan 9 territory primitives: Territory + bind/unbind + mount/unmount + cycle-detection + mount-refcount discipline. Maps to `specs/territory.tla` actions (Bind/Unbind/Mount/Unmount/ForkClone). | medium | **P5-attach-mount landed** |
| [19-handles.md](reference/19-handles.md) | Kernel handle table: kobj_kind enum (8 types) + rights bitmask + per-Proc HandleTable + handle_alloc/close/dup/get. Maps to `specs/handles.tla` actions. | medium | **P2-Fc landed** |
| [20-burrow.md](reference/20-burrow.md) | BURROW manager + dual-refcount lifecycle (handle_count + mapping_count). Eager allocation via alloc_pages. handle.c integration via per-kind acquire/release hooks. Maps to `specs/burrow.tla` actions. | medium | **P2-Fd landed** |
| [21-elf.md](reference/21-elf.md) | ELF64 ARM64 loader: parse + validate (5-stage pipeline) + W^X rejection per ARCH §28 I-12. Static ET_EXEC binaries at v1.0; mapping deferred to Phase 3. | medium | **P2-Ga landed** |
| [22-asid.md](reference/22-asid.md) | ASID allocator: arch/arm64/asid.{h,c} — 8-bit ASID space (1..255 user; 0 kernel-reserved); LIFO free-list with TLB-flush-on-free; monotonic fallback; extincts on exhaustion at v1.0 (255 ASIDs). | medium | **P3-Ba landed** |
| [23-direct-map.md](reference/23-direct-map.md) | Kernel direct map: 0xFFFF_0000_* linear PA→KVA (1 GiB blocks) + vmalloc range 0xFFFF_8000_* page-grain MMIO via mmu_map_mmio. W^X enforced at alias level (kernel image GiB demoted to L2→L3 with PXN+UXN forced; closes direct-map-write-to-.text hazard). pa_to_kva / kva_to_pa inlines. | medium | **P3-Bb landed** (incl. hardening + R6-B audit close) |
| [24-per-proc-pgtable.md](reference/24-per-proc-pgtable.md) | Per-Proc page-table allocator: arch/arm64/mmu.c proc_pgtable_create/destroy + struct Proc.pgtable_root + struct Proc.asid wired in proc_alloc/proc_free. P3-Bcb scope is lifecycle plumbing only; TTBR0 swap at P3-Bd; sub-table walk in destroy at P3-D. | small | **P3-Bcb + P3-Bdb landed** (TTBR0 swap section added) |
| [25-fault-dispatcher.md](reference/25-fault-dispatcher.md) | Page-fault dispatcher: arch/arm64/fault.{h,c} — fault_info_decode + arch_fault_handle. Refactored exception.c to use the dispatcher. v1.0 P3-C resolve path is empty; P3-D wires user-mode VMA dispatch + demand paging. | medium | **P3-C landed** |
| [26-vma.md](reference/26-vma.md) | Per-Proc VMA list: kernel/include/thylacine/vma.h + kernel/vma.c — struct Vma + alloc/free/insert/remove/lookup/drain + struct Proc.vmas integration. Sorted doubly-linked list at v1.0 (O(N)); RB-tree post-v1.0. | medium | **P3-Da landed** |
| [27-exec.md](reference/27-exec.md) | Kernel-internal exec: kernel/include/thylacine/exec.h + kernel/exec.c — exec_setup(p, blob, size, *entry, *sp). Per-segment burrow_create + bytes copy via direct map + burrow_map; user stack BURROW + map. Caller-held BURROW handles dropped after burrow_map (mapping_count keeps alive). P3-Eb scope: address-space population only; ERET-to-EL0 + SVC + ELF fixture land at P3-Ec/Ed. | medium | **P3-Eb landed** |
| [28-syscall.md](reference/28-syscall.md) | Userspace syscall dispatch: kernel/include/thylacine/syscall.h + kernel/syscall.c — syscall_dispatch(ctx). AArch64 ABI: x8=nr, x0..x5=args, x0=return. SYS_EXITS(status) maps to kernel exits("ok"/"fail"); SYS_PUTS(buf, len) writes to UART. exception_sync_lower_el routes EC_SVC_AARCH64 here. v1.0 minimum surface; full Thylacine syscall set lands Phase 5+. | small | **P3-Ec landed** |
| [29-init.md](reference/29-joey.md) | /init bringup: kernel/include/thylacine/joey.h + kernel/joey.c — joey_run() builds embedded 9-instr AArch64 program (SYS_PUTS("hello\n") + SYS_EXITS(0)), rforks child, exec_setup's blob, userland_enter's to EL0, wait_pid's, asserts exit_status==0. Boot-path placement after tests + fault_test + before "Thylacine boot OK" banner. Retires `userspace.exec_exits_ok` test in favor of the production /init regression guard. v1.0 embedded blob; Phase 4 ramfs / Phase 5 cross-toolchain replace with real binary. | medium | **P3-F landed** |
| [30-dev-spoor.md](reference/30-dev-spoor.md) | Dev vtable + Spoor lifecycle + bestiary registry + devnone stub: kernel/include/thylacine/{dev,spoor}.h + kernel/{dev,spoor,devnone}.c. ARCH §9.2 verbatim 16-op vtable with C99 const-correct typing. Spoor refcount + per-Spoor lock + magic discipline mirroring burrow.c. devnone (dc='-') anchors unconfigured Spoors and serves as audit guard. Boot wire-up `dev_init()` after sched_init. | medium | **P4-A landed** |
| [31-trivial-devs.md](reference/31-trivial-devs.md) | Kernel-internal trivial Devs: cons (dc='c'; UART forward) + null (dc='0'; bit bucket) + zero (dc='z'; zero source) + random (dc='r'; ARM64 RNDR-backed CSPRNG). dev_simple_{attach,open,close} helpers in kernel/dev.c factor the common leaf-file Dev plumbing. ROADMAP §6.2 exit criteria closed for Spoor 10K-cycles-on-/dev/null and /dev/random non-zero bytes. chacha20 stir for /dev/random held to a future hardening sub-chunk. | medium | **P4-B landed** |
| [32-devproc.md](reference/32-devproc.md) | dev/proc: synthetic /proc/<pid>/ Dev (dc='p'). Walks `/proc/<pid>/{status, cmdline, ctl, ns}`; status reports pid+state+threads+exit; cmdline placeholder; ns reports bind count; ctl writes consume command verbs (verb-set + dispatch held to Phase 5+ specs/notes.tla landing). Adds proc_find_by_pid + proc_for_each + walkqid_alloc/free helpers. First directory-typed Dev with multi-step walk support. | medium | **P4-C landed** |
| [33-devctl.md](reference/33-devctl.md) | dev/ctl: kernel admin Dev (dc='C'). Single-level /ctl/{procs, memory, devices, kernel-base, sched} text-format introspection. Reuses devproc walk pattern + tiny formatters (duplicated; future shared <thylacine/fmt.h>). Writes rejected at v1.0 (admin commands held to Phase 5+ syscall surface). | medium | **P4-D landed** |
| [34-devramfs.md](reference/34-devramfs.md) | dev/ramfs: cpio-loaded in-memory FS (dc='m'). Full pipeline: tools/mkcpio.py builds cpio newc archive; tools/build.sh::build_ramfs chained off build_kernel; tools/run-vm.sh -initrd; kernel/cpio.{h,c} parser; dtb_get_chosen_initrd probes /chosen/linux,initrd-{start,end}; mm/phys.c reserves initrd PA range; kernel/devramfs.c populates a static file table at boot. Read-only at v1.0; freed once Stratum mounts persistent FS at Phase 5+. | medium | **P4-E landed** |
| [35-virtio.md](reference/35-virtio.md) | VirtIO core: MMIO transport + split virtqueue per VIRTIO 1.2 spec. virtio_init probes DTB virtio,mmio nodes + maps each slot's MMIO + reads transport identity. Status state machine + feature negotiation + virtqueue alloc (3 page-aligned rings per modern-transport spec). dtb_for_each_compat_reg multi-match enumeration helper. tools/run-vm.sh attaches virtio-rng-device for tests. IRQ forwarding (P4-G) and userspace driver bindings (P4-I+) are downstream. | medium | **P4-F landed** |
| [36-irqfwd.md](reference/36-irqfwd.md) | IRQ forwarding to KObj_IRQ blocker. KObj_IRQ struct + Rendez + edge-triggered pending counter. kobj_irq_create registers GIC handler + enables IRQ; kobj_irq_wait blocks until pending_count > 0; kobj_irq_dispatch increments + wakes. Wait/wake atomicity per scheduler.tla NoMissedWakeup (I-9). Single-waiter per KObj_IRQ; multi-waiter held to Phase 5+ poll/futex. | medium | **P4-G landed** |
| [37-virtio_pci.md](reference/37-virtio_pci.md) | VirtIO PCI transport — ECAM enumeration. DTB-driven probe of `pci-host-ecam-generic`; bus 0 config space mapped via mmu_map_mmio; (dev, fn) walk identifies VirtIO devices by vendor 0x1AF4 + device-ID range. struct virtio_pci_dev carries BDF + IDs + virtio_device_id derivation. BAR mapping + capability walking + INTx/MSI deferred to first PCI-using driver chunk. | medium | **P4-H landed** |
| [38-userspace.md](reference/38-userspace.md) | Userspace tree + libt runtime. usr/ as sibling CMake project; libt provides _start + stack-canary stubs + header-only SVC wrappers (`<thyla/syscall.h>`). Linker script enforces W^X (two PT_LOAD segments). `tools/build.sh userspace` implemented; `build_ramfs` copies curated binaries into the cpio. New `devramfs_lookup` helper for blob-by-name access. `tools/build.sh sysroot` clarified to be the Linux-compat musl tree (Phase 6); usr/ is Thylacine native. | medium | **P4-Ia1 + Ia2 landed** |
| [39-hw-handles.md](reference/39-hw-handles.md) | Hardware handles (KObj_MMIO + KObj_IRQ + KObj_DMA) + capability gating + syscall surface. New `<thylacine/caps.h>` with CAP_HW_CREATE; per-Proc `caps_t caps` field. New `<thylacine/mmio_handle.h>` + g_mmio_claims PA-range exclusivity. INTID claim layer in irqfwd. handle_dup rejects hw kinds (NoHwDup). Six syscalls (MMIO_CREATE, MMIO_MAP, IRQ_CREATE, IRQ_WAIT, DMA_CREATE, DMA_MAP) with cap + arg + rollback discipline. Three new TLC-verified spec invariants. **P4-Ic5b2**: `gic_set_spi_edge_triggered` for the GIC ICFGR transition; full composed userspace driver (`/virtio-blk-probe`) demonstrating MMIO + IRQ + DMA end-to-end with QEMU virtio-blk-device. | medium | **P4-Ib + Ic5b1b + Ic5b2 landed** |
| [40-uaccess.md](reference/40-uaccess.md) | Kernel-mode user-VA fault-recoverable accessor primitives: `arch/arm64/uaccess.{S,c,h}` + `.uaccess_fixup` linker section + `exception_sync_curr_el` integration. Asm primitive `uaccess_load_u8(va, *out)` + PC-relative s32 fixup-table encoding (Linux `__ex_table`-style; PIE-clean). Replaces the per-binary `pretouch_rodata_pages()` discipline; SYS_PUTS now demand-pages user VAs from kernel mode. Vector slots 0x000 + 0x200 (Sync at current EL) switched from `b _torpor` to `b .Lexception_return` so kernel-mode sync exceptions can return recoverably. | medium | **R12-uaccess landed** |
| [41-irq-bench.md](reference/41-irq-bench.md) | IRQ-to-userspace latency benchmark — closes ROADMAP §6.2 exit criterion (infrastructure side). New `usr/irq-bench/` + `kernel/test/test_irq_latency_bench.c` measure end-to-end IRQ delivery latency via shared `KObj_DMA` region + `CNTPCT_EL0` on both sides. New `timer_enable_el0_counter_access()` enables `CNTKCTL_EL1.EL0PCTEN` per-CPU. Bare-metal target `p99 < 5 µs` per VISION §4.5; CI sanity budget `p99 < 50 ms` for QEMU TCG. Bare-metal verification deferred to Phase 5+. | medium | **P4-Ic-latency landed** |
| [42-virtio-input.md](reference/42-virtio-input.md) | Third composed userspace driver (`/virtio-input`). Probe-only at v1.0; proves the composed hw-handle SVC substrate generalizes to VIRTIO INPUT (DeviceID=18). Introduces selector-based device-specific config-space (VIRTIO 1.2 §5.8.4) — first device class to exercise the select+subsel indirection. Configures eventq (queue 0, RX-only) with 16 pre-published WRITE descriptors. Reads INPUT_CFG_ID_NAME + INPUT_CFG_EV_BITS to classify the device (QEMU keyboard: name="QEMU Virtio Keyboard", key_bits=29, rel_bits=0). Reaches DRIVER_OK + exits clean; does NOT consume events (QEMU virt lacks host-side injection without QMP send-key wiring; event consumption is a future P4-K-events). `tools/run-vm.sh` wires `-device virtio-keyboard-device`. Bonus: `-z max-page-size=4096` in `usr/.cargo/config.toml` shrinks every userspace Rust ELF by ~60 KiB (closes rust-lld's 64-KiB MAXPAGESIZE-driven file gap). | small | **P4-K landed** |
| [43-virtio-gpu.md](reference/43-virtio-gpu.md) | Fourth composed userspace driver (`/virtio-gpu`). Probe-only at v1.0; proves the composed hw-handle SVC substrate generalizes to VIRTIO GPU (DeviceID=16). First composed driver to configure two virtqueues (controlq idx 0 + cursorq idx 1; spec §5.7.2); first to issue a command/response chain on controlq (24-byte `virtio_gpu_ctrl_hdr` OUT descriptor + 408-byte `virtio_gpu_resp_display_info` IN descriptor). Sends `VIRTIO_GPU_CMD_GET_DISPLAY_INFO`, waits IRQ, verifies `resp.hdr.type == OK_DISPLAY_INFO` (= 0x1101). Logs `num_scanouts` + `pmodes[0]` dimensions + `enabled` flag (informational). `tools/run-vm.sh` wires `-device virtio-gpu-device`. Halcyon (Phase 8) substrate gate: scanout setup (CREATE_2D / ATTACH / TRANSFER / SET_SCANOUT / FLUSH) is future P4-L-scanout work. | small | **P4-L landed** |
| [44-9p-wire.md](reference/44-9p-wire.md) | 9P2000.L wire codec. Pure byte-level marshal/unmarshal; no kernel state, no allocation, no I/O. Covers handshake (Tversion/Tattach) + navigation (Twalk) + clunk + Rlerror; IO family (Tlopen/Tlcreate/Tread/Twrite); metadata family (Tgetattr/Tsetattr/Treaddir/Tstatfs/Tfsync). Strict body-length equality at every parser; R111 caller-cap-bound on Rwalk/Rread/Rreaddir server-supplied counts. 29 unit tests + 8 + 10 `_Static_assert` lines pin opcode pairs. | medium | **P5-wire + P5-wire-io + P5-wire-meta landed** |
| [45-9p-session.md](reference/45-9p-session.md) | 9P client state machine. Composes the wire codec into per-session tag pool (bitmap over outstanding[64]) + fid table (linear, swap-with-last) + outstanding-request bookkeeping. State machine: INIT → VERSIONED → OPEN → CLOSED. Pins specs/9p_client.tla I-10 (tag uniqueness), I-11 (fid stability), OutOfOrderCorrectness, FlowControl by structural mechanisms. Send/Receive APIs for all currently-landed codec ops; dispatcher applies state mutation per kind. 33 unit tests directly cover the spec's 4 buggy-cfg shapes. | medium | **P5-session + P5-wire-io + P5-wire-meta landed** |
| [46-9p-transport.md](reference/46-9p-transport.md) | 9P transport layer: vtable-driven frame-aware byte pipe + in-memory loopback backend + session composition helper. Sits between the session state machine (45-9p-session) and the actual byte pipe (initially loopback for tests; eventually Spoor wrapping a Unix socket to stratumd). Partial-read aggregation, state machine (INIT/OPEN/CLOSED/ERROR), session composition via `p9_transport_exchange`. No spec extension needed (transport invariants are mechanical; session-level invariants compose unchanged). | medium | **P5-transport landed** |
| [49-9p-attach.md](reference/49-9p-attach.md) | Mount creation primitive. `struct p9_attached` heap-allocates and owns one p9_client + recv_buf; create runs Tversion + Tattach; destroy clunks root_fid + tears down. The kernel-internal body of the future attach_9p syscall (the user-visible SVC is deferred until fd-syscall infrastructure exists). Used by tests + the future P5-stratumd boot path. No spec extension (pure composition over spec'd layers). | medium | **P5-attach-create landed** |
| [50-9p-spoor-transport.md](reference/50-9p-spoor-transport.md) | Adapter wrapping a Spoor pair into `struct p9_transport_ops`. Send/recv routes to the Spoor's Dev vtable's write/read. tx and rx may be the same Spoor (duplex) or distinct (pipe model). `owns_spoors` controls clunking on close. The 9P client's byte pipe becomes whatever Spoor backend the caller picks (future kernel pipe / Unix socket / virtio-vsock). No spec extension. | small | **P5-spoor-transport landed** |
| [51-pipe.md](reference/51-pipe.md) | Plan 9 `pipe(fd[2])` primitive at the kernel layer. Connected pair of Spoors over a shared 4 KiB ring buffer. Blocking semantics (read sleeps on empty, write sleeps on full; close wakes the other side with EOF / EPIPE). Per-pipe spin lock + two rendez wait queues. `specs/pipe.tla` pins NoStuckReader / NoStuckWriter (specializations of I-9). dc='\|'. Production backend for P5-spoor-transport. | medium | **P5-pipe-blocking landed** |
| [52-sys-pipe.md](reference/52-sys-pipe.md) | SYS_PIPE = 8 SVC handler. No args; returns rd fd in x0, wr fd in x1. Calls `pipe_create()` + installs both Spoors as KOBJ_SPOOR handles. Also wires `handle_release_obj` + `handle_acquire_obj` to `spoor_clunk` / `spoor_ref` so handle_close + proc_free release Spoors end-to-end (first kobj kind beyond BURROW + hw kinds to be ref-counted on the handle table). | small | **P5-fd-pipe landed** |
| [53-sys-rw.md](reference/53-sys-rw.md) | SYS_READ = 9 / SYS_WRITE = 10. fd (x0), buf_va (x1), len (x2). Routes through dev->read/write after rights check + user-VA validation. Per-call cap = 4096; userspace loops for larger transfers. Lands `uaccess_store_u8` (the symmetric primitive to uaccess_load_u8). 4-byte FIFO round-trip test composes pipe + SYS_PIPE + SYS_READ + SYS_WRITE end-to-end. | small | **P5-fd-rw landed** |
| [54-sys-fd-syscalls.md](reference/54-sys-fd-syscalls.md) | SYS_CLOSE = 11 + SYS_DUP = 12. Thin wrappers over handle_close / handle_dup. KOBJ_SPOOR release/acquire paths (P5-fd-pipe) fire on close/dup. Userspace libt stubs (t_pipe/t_read/t_write/t_close/t_dup). First userspace test binary `/pipe-probe` exercises pipe → write → read → dup → reject-elevation → close × 3 → read-on-closed → double-close → exit 0. The canonical end-to-end regression test of the byte I/O composition. | small | **P5-fd-syscalls landed** |
| [55-sys-attach-9p.md](reference/55-sys-attach-9p.md) | SYS_ATTACH_9P = 13. Takes tx_fd + rx_fd KOBJ_SPOOR pair + aname; drives Tversion + Tattach handshake; returns a KOBJ_SPOOR fd for the 9P tree's root. dev9p_priv gains `attached_owner` + `adapter_to_free` so close-on-root-fd tears down the entire session. Last deferred SVC handler from the P5-attach plan; closes user-visible §9.6.1. | small | **P5-attach-syscall landed** |
| [56-sys-mount.md](reference/56-sys-mount.md) | SYS_MOUNT = 14 + SYS_UNMOUNT = 15. Thin wrappers over `kernel/territory.c::mount` / `::unmount`. Closes the user-visible body of ARCH §9.6.1's mount-table operations. Includes the companion Plan-9 cclose fix to `spoor_clunk` — dev->close now runs only on the last drop so mount-after-close lifecycle (per ARCH §9.6.6) works. 9 new tests including the end-to-end mount → close → close → drop-Territory lifecycle. | small | **P5-mount-syscall landed** |
| [57-attach-probe.md](reference/57-attach-probe.md) | `/attach-probe` userspace integration test of SYS_ATTACH_9P + SYS_MOUNT + SYS_UNMOUNT. ELF probe (~14.9 KiB) + kernel-thread 9P responder + test framework. First Phase 5 test driving the full SVC dispatch + wire codec + dev9p + spoor-transport + pipe stack through a real EL0 Proc (no loopback shortcuts). Hardcoded fd 0 (tx) + fd 1 (rx) pre-installed by the kernel test framework — RFFDG-based two-Proc design deferred until rfork supports shared fd tables. Documents the load-bearing ref discipline (transfer-not-bump) + the Tclunk-on-root-fid session-layer rejection. | small | **P5-attach-probe landed** |
| [58-corvus-syscalls.md](reference/58-corvus-syscalls.md) | 5 v1.0 hardening syscalls (SYS_MLOCKALL = 16, SYS_SET_DUMPABLE = 17, SYS_SET_TRACEABLE = 18, SYS_EXPLICIT_BZERO = 19, SYS_GETRANDOM = 20) + 2 new caps (CAP_LOCK_PAGES, CAP_CSPRNG_READ) + 3 per-Proc flags (PROC_FLAG_NODUMP/NOTRACE/MLOCKED) + public kernel CSPRNG API (`kern_random_bytes`, `kern_random_seeded`). Foundational scaffold for corvus + per-user stratumd startup per CORVUS-DESIGN §4.1.1. v1.0 forward-compat: flags don't gate yet (swap/core-dumps/debug-Spoors are v1.x); enforcement lands when those subsystems do. | small | **P5-corvus-syscalls landed** |
| [59-joey-from-ramfs.md](reference/59-joey-from-ramfs.md) | `/joey` is now a real userspace binary in `usr/joey/`, shipped in the initrd cpio; replaces the prior 9-instruction kernel-embedded blob. Same orchestration (rfork → exec_setup → userland_enter → wait_pid); the source of the ELF moved. The 8-aligned static-buffer copy is mandatory: cpio newc data is 4-aligned; `elf_load` rejects misaligned Ehdr per R5-G F61. Unblocks the rest of the P5-stratumd-bringup arc — `/joey` can now do arbitrary userspace work. | small | **P5-joey-from-ramfs landed** |
| [60-sys-spawn-wait.md](reference/60-sys-spawn-wait.md) | SYS_SPAWN = 21 (combined `rfork(RFPROC)` + `exec` against a devramfs-named binary; child inherits no caps; ≤ 32 KiB blob) + SYS_WAIT_PID = 22 (reaps one ZOMBIE child; status optional). Plan 9 `rfork(RFPROC\|RFEXEC)` idiom — a real `SYS_RFORK` with COW is deferred until a v1.x workload requires it. /joey now orchestrates /hello end-to-end at boot. 7 kernel-internal tests. | small | **P5-spawn-wait landed** |
| [61-stratumd-stub.md](reference/61-stratumd-stub.md) | First Phase 5 test running TWO userspace Procs cooperating over pipes. `/stratumd-stub` is a minimal userspace 9P responder; `/attach-probe` is the client. Kernel test framework wires them via two pipe pairs. Production shape for real `stratumd-system`/`stratumd-michael` — a userspace process can be the 9P server. P5-stratumd-stub-bringup-a in a multi-sub-chunk arc; RFFDG / SYS_SPAWN_WITH_FDS / pivot lands in subsequent chunks. | medium | **P5-stratumd-stub-bringup-a landed** |
| [62-sys-spawn-with-fds.md](reference/62-sys-spawn-with-fds.md) | `SYS_SPAWN_WITH_FDS` = 23 — extends SYS_SPAWN with explicit fd inheritance (KOBJ_SPOOR-only at v1.0; ≤ 16 fds per call; child fd 0..fd_count-1). Refcount discipline: parent's bumped refs transferred to child's handle table via handle_alloc; failure paths drop all bumps symmetrically. New `/stub-driver` userspace binary drives the full production-shape orchestration at EL0 (pipe × 2 + spawn-with-fds + attach + mount + unmount + close + wait). 7 new tests. P5-stratumd-stub-bringup-b. | medium | **P5-stratumd-stub-bringup-b landed** |
| [63-sys-spawn-with-caps.md](reference/63-sys-spawn-with-caps.md) | `SYS_SPAWN_WITH_CAPS` = 24 — spawn variant granting cap-subset to child. Wraps existing `rfork_with_caps`; child's caps = parent's & cap_mask. Structural enforcement of ARCH I-2 / I-6 (caps monotonically reduce; extra bits in cap_mask silently dropped at AND). Tests use `proc_find_by_pid` to verify cap arithmetic directly. 5 new tests. Needed at P5-corvus-bringup. | small | **P5-spawn-caps landed** |
| [69-corvus-unwrap.md](reference/69-corvus-unwrap.md) | corvus WRAP/UNWRAP + the ML-KEM-768 + X25519 hybrid keypair (P5-corvus-bringup-d) — the real hybrid keypair, the hybrid-PKE DEK envelope, and the C-7-gated WRAP (verb 10) + UNWRAP (verb 4) verbs. |
| [68-corvus-crypto.md](reference/68-corvus-crypto.md) | corvus real crypto (P5-corvus-bringup-c) — Argon2id passphrase verification + AEGIS-256 AEAD + the `CRVS` state-file format + the USER_CREATE verb. Pure-Rust `argon2` / `aegis` (`pure-rust` feature) / `linked_list_allocator` crates; a 24 MiB static-BSS `#[global_allocator]` heap. AUTH now verifies the passphrase via an Argon2id-derived KEK + AEGIS-256-unwrap of a wrapped keypair (placeholder; ML-KEM-768+X25519 hybrid deferred). State records in-memory at this sub-chunk. Argon2id `m_cost=16 MiB` (capped below the design's 64 MiB default for the contiguous heap). | medium | **P5-corvus-bringup-c landed** |
| [67-el1h-kernel.md](reference/67-el1h-kernel.md) | Uniform-EL1h kernel execution model (P5-el1h-kernel) — the kernel runs entirely at EL1h (`SPSel=1`, `sp=SP_EL1`=the running thread's own kernel stack; `SP_EL0` exclusively the userspace stack — invariant I-21). Records the conversion that fixed a pre-existing SMP soundness bug: the former EL1t/EL1h dual-mode kernel let a work-stolen thread resume `KERNEL_EXIT` at the wrong mode, where `msr SP_EL0` traps `EC=0` and silently killed a secondary CPU on every boot. Exception frames now build on the per-thread kernel stack (migration-safe). `start.S`/`vectors.S`/`userland.S` converted; `cpu_switch_context` unchanged. Pinned by `specs/sched_ctxsw.tla`. | medium | **P5-el1h-kernel landed** |
| [66-corvus-server.md](reference/66-corvus-server.md) | `/sbin/corvus` server loop + binary frame wire codec (P5-corvus-bringup-b) — second impl chunk of the corvus daemon. Extends -a's skeleton to a single-peer Spoor server reading framed verb requests on fd 0, writing responses on fd 1, exits 0 on rx EOF. Wire format per CORVUS-DESIGN.md §6.4: req=verb_id+payload_len+payload, resp=status+payload_len+payload. Two verbs land: **AUTH** (parses user+passphrase; passphrase NOT verified at this skeleton — Argon2id at -c; mints 33-byte session token = `'s'` + 32 hex chars of CSPRNG entropy) + **SESSION_CLOSE** (clears session by token). Single-slot session table; `Session::user` is set once at install + never mutated until clear (structural C-3 enforcement). Joey extended to drive AUTH + SESSION_CLOSE round-trip via `t_spawn_full` with pipe-pair + caps. Spec actions `AuthSuccess` + `SessionClose` wired through. libthyla-rs gained `t_pipe` / `t_read` / `t_write` / `t_close` Rust wrappers. | medium | **P5-corvus-bringup-b landed** |
| [65-corvus-skeleton.md](reference/65-corvus-skeleton.md) | `/sbin/corvus` skeleton (P5-corvus-bringup-a) — first impl chunk of the Thylacine-native key-agent daemon. `usr/corvus/` Rust crate runs the 5 v1.0 hardening syscalls (mlockall + set_dumpable + set_traceable + getrandom-seeded probe + explicit_bzero) and exits 0. Production-shape orchestration: joey spawns /sbin/corvus via `SYS_SPAWN_WITH_CAPS` with `CAP_LOCK_PAGES \| CAP_CSPRNG_READ`. Joey's own caps bumped to `CAP_ALL` via `rfork_with_caps` at boot so it can delegate. Pins CORVUS-DESIGN.md C-2 + C-5 (RAM-wipe pattern) + C-15 (CSPRNG-seeded probe) at the runtime layer; C-3 / C-7 / C-11 (session machine + verb dispatch) deferred to P5-corvus-bringup-b onward. Boot path is the regression — `corvus: ready` + joey reaps status=0 = test passes. | small | **P5-corvus-bringup-a landed** |
| [64-sys-spawn-full.md](reference/64-sys-spawn-full.md) | `SYS_SPAWN_FULL` = 25 — fourth spawn variant combining fd inheritance + cap-subset in one call. Unions SYS_SPAWN_WITH_FDS + SYS_SPAWN_WITH_CAPS; reuses existing thunk; cap arithmetic via rfork_with_caps. Needed at P5-corvus-bringup. 5 new tests; cap inspection race-free, fd inheritance verified indirectly via clean-exit + existing /stub-driver coverage. | small | **P5-spawn-full landed** |
| [48-dev9p.md](reference/48-dev9p.md) | Dev vtable proxying to the kernel 9P client. The foundation of ARCH §9.6 "filesystem-as-Spoor" — makes a p9_client-backed Spoor look identical to a kernel-internal Dev above the Spoor walk algorithm. Per-Spoor state: `struct dev9p_priv { magic, client *, fid, fid_owned }`. Root Spoors don't own root_fid (higher layer manages); walk-derived Spoors do (close clunks). Public constructor: `dev9p_attach_client(client, root_fid) → Spoor *`. No spec extension (pure composition over spec'd lower layers). | medium | **P5-attach-dev landed** |
| [47-9p-client.md](reference/47-9p-client.md) | 9P client high-level API: 25 op wrappers (lifecycle, handshake, walk, clunk, IO, metadata, mutation) consolidating wire + session + transport into one callable surface. Every op: `session.send → transport.exchange → result extraction → error mapping`. Signed-errno convention (0 / -EINVAL / -EBUSY / -EIO / -ecode). Caller-allocated `struct p9_client` (~12 KiB, embeds session + transport + outbound buffer). Internal field-by-field copy helpers replace implicit memcpy struct assignments (kernel doesn't link libc). No spec extension needed (pure composition). | medium | **P5-client landed** |
| 12-9p.md (planned) | 9P client (pipelined) + Stratum extensions | medium | Phase 4 |
| 13-syscall.md (planned) | Syscall surface + Linux shim | medium | Phase 5 |
| 14-poll-futex.md (planned) | poll, futex, rendezvous | small | Phase 5 |
| 15-pty.md (planned) | PTY infrastructure + termios | small | Phase 5 |
| 16-network.md (planned) | smoltcp / Plan 9 IP / VirtIO-net userspace | medium | Phase 6 |
| 17-container.md (planned) | thylacine-run + territory construction | small | Phase 6 |
| 18-halcyon.md (planned) | Scroll buffer + framebuffer + image + video | medium | Phase 8 |

---

## Document maintenance

When a chunk lands (bug fix, refactor, new module), the author is responsible for:

1. Updating the relevant `reference/NN-*.md` section(s).
2. Refreshing the [Snapshot](#snapshot) figures.
3. If the chunk introduces or refutes an invariant, updating the spec catalog entry.
4. If a new term or acronym enters the lexicon, updating the glossary.

Reference sections are PR-first like any code change; the audit policy in `CLAUDE.md` extends here: a change to a documented invariant updates the spec FIRST, then the reference, then the code. If the three disagree, the spec wins.

---

## Glossary

See `ARCHITECTURE.md §29` for the definitive glossary. As implementation lands and new terms enter the lexicon, they're appended to that glossary; this reference points to it rather than duplicating.

---

## Revision history

| Date | Change | Reason |
|---|---|---|
| 2026-05-04 | Scaffolded (Phase 0 complete). | Template for the as-built reference. Per-subsystem files (`reference/01-...md`, etc.) appear as subsystems land during Phase 1 onward. |
| 2026-05-04 | P1-G landed: 10-gic.md + 11-timer.md added; snapshot bumped (6/6 tests, 3415 LOC, 190 KB ELF). 08-exception.md updated for live IRQ slot + `.Lexception_return` trampoline. | GIC v3 + ARM generic timer chunk closed; the kernel now receives 1000 Hz ticks. |
| 2026-05-04 | P1-H landed: 12-hardening.md added; snapshot bumped (7/7 tests, 3685 LOC, 206 KB ELF). 03-mmu.md (PTE_GP) + 05-kaslr.md (canary_init slot) + 01-boot.md (PAC + canary entry sequence) updated. | v1.0 hardening commitment closed: canaries + PAC + BTI + LSE + stack-clash + NX stack. CFI / MTE / FORTIFY deferred with rationale. |
| 2026-05-04 | **P1-I landed: Phase 1 CLOSED.** 13-verification.md added; snapshot bumped (9/9 tests, ~3965 LOC, ~216 KB ELF). All ROADMAP §4.2 exit criteria met (boot < 500 ms; 10000-iter leak; KASLR 10/10 distinct; UBSan clean; canary/W^X/BTI fire under attack). Audit round 3 closed 9 findings + 2 deferred. | Foundation phase complete. Phase 2 (process model + scheduler + handles + BURROW + first formal specs) opens. |
| 2026-05-05 | **P2-A landed.** 14-process-model.md added; snapshot bumped (11/11 tests, ~4330 LOC, ~227 KB ELF). struct Proc + struct Thread + struct Context + cpu_switch_context + thread_trampoline + thread_switch + context.create_destroy / context.round_trip tests + specs/scheduler.tla sketch (TLC-clean). | Phase 2 opens with the first multitasking primitives. Pre-EEVDF — direct switches only. P2-B layers the scheduler. |
| 2026-05-05 | **P2-A R4 audit close**: 1 P1 + 3 P2 + 5 P3 fixed; 1 P3 deferred (U-30). KP_ZERO on proc/thread alloc; magic guards (struct Proc 16→24 B, struct Thread 160→168 B); BTI BTYPE comments corrected; THREAD_STATE_INVALID guards; TPIDR_EL0/EL1 init in start.S; _Static_asserts pinned. Plus scheduler.tla extended with wait/wake atomicity (NoMissedWakeup invariant; scheduler_buggy.cfg counterexample). | Defensive measures + spec refinement land before P2-B impl. |
| 2026-05-05 | **P2-Ba landed.** 15-scheduler.md added; snapshot bumped (13/13 tests, ~4790 LOC, ~242 KB ELF). struct Thread extended (168→200 B) with vd_t/weight/band/runnable_{next,prev}. sched.h + sched.c + 2 new tests. sched_init wired in main.c. | Plan 9 sched() + ready() idiom layer over a simplified EEVDF (monotonic vd_t). 3 priority bands. Full EEVDF math at P2-Bc. |
| 2026-05-05 | **P2-Bb landed.** 16-rendez.md added; snapshot bumped (16/16 tests, ~5200 LOC, ~253 KB ELF). New rendez.h + sleep/wakeup impl in sched.c + test_rendez.c (3 tests). struct Thread extended (200→208 B) with rendez_blocked_on. sched() refactored to respect pre-set prev->state (RUNNING → yield-insert; SLEEPING/EXITING → don't insert; defends against SMP race with prev==next check). | Plan 9 wait/wake protocol implemented against the scheduler.tla `WaitOnCond` + `WakeAll` actions. Single-waiter Rendez convention. spin_lock_irqsave for IRQ-mask discipline. Multi-waiter wait queues (poll, futex) at Phase 5. |
| 2026-05-05 | **P2-Bc landed.** 15-scheduler.md extended with preemption section; snapshot bumped (17/17 tests, ~5400 LOC, ~255 KB ELF). New `sched_tick()` + `preempt_check_irq()` in sched.c + scheduler.preemption_smoke test. struct Thread extended (208→216 B) with slice_remaining (s64). vectors.S IRQ slot inserts `bl preempt_check_irq` after exception_irq_curr_el (28 of 32 instructions). thread_trampoline `msr daifclr, #2` to unmask IRQs at first thread entry. `sched()` and `ready()` use `spin_lock_irqsave(NULL)` for IRQ-mask discipline (closes P2-A trip-hazard #3). Spec docs updated (no model changes); SPEC-TO-CODE.md maps preempt path to existing Yield action. | Timer-IRQ-driven preemption live: CPU-bound threads share the CPU under round-robin slice rotation. Full EEVDF math + LatencyBound liveness deferred — current impl is a valid instantiation for weight=1 (which is v1.0). |
| 2026-05-05 | **P2-Ca landed.** 17-smp-bringup.md added; snapshot bumped (18/18 tests, ~6000 LOC, ~280 KB ELF). DTB cpu/psci enumeration (`dtb_cpu_count`/`dtb_cpu_mpidr`/`dtb_psci_method`); arch/arm64/psci.{h,c} for HVC/SMC SMCCC; secondary_entry asm trampoline in start.S; kernel/smp.{h,c} with smp_init. mmu_enable refactored into mmu_program_this_cpu + mmu_enable for future per-CPU re-program at P2-Cb. New banner `smp:` line. New test smp.bringup_smoke. Boot now reports 4/4 cpus online via PSCI HVC on QEMU virt. | First SMP step. Secondaries are alive but inert — they flip online flag + WFI. No MMU/PAC/vectors per-CPU yet. P2-Cb adds full per-CPU init bringing them into the scheduler. P2-C split into Ca-Cg sub-chunks for tractability (Ca landed; Cb..Cg open). |
| 2026-05-05 | **P2-Cb landed.** 17-smp-bringup.md extended with per-CPU init section; snapshot bumped (18/18 tests, ~6200 LOC, ~281 KB ELF). PAC keys refactored: `pac_derive_keys` + `pac_apply_this_cpu` asm functions in start.S; `g_pac_keys[8]` BSS. `g_secondary_boot_stacks[7][16384]` per-CPU 16 KiB stacks. Extended secondary_entry trampoline: SP + PAC + MMU + long-branch via kaslr_high_va_addr to high VA per_cpu_main. New `per_cpu_main` C function: VBAR_EL1 + TPIDR_EL1 + g_cpu_alive flag + idle WFI. smp_init wait now uses g_cpu_alive (stricter signal than g_cpu_online). | Secondaries now reach C runtime at high VA. PAC/MMU/VBAR/TPIDR all set per-CPU. Cross-CPU PAC consistency (shared g_pac_keys) ready for P2-Ce thread migration. Per-CPU run trees + idle thread integration deferred to P2-Cd. |
| 2026-05-06 | **P2-Ga landed.** ELF64 ARM64 loader (parse + W^X validation). New `kernel/include/thylacine/elf.h` (~210 LOC) defines `Elf64_Ehdr` (64 B; pinned via `_Static_assert`) + `Elf64_Phdr` (56 B; pinned) + ELF magic/class/data/version/OSABI/machine/type constants + program header types (PT_LOAD/PT_INTERP/PT_GNU_STACK/etc.) + flag bits (PF_R/W/X) + `elf_load_result` error enum (21 distinct codes) + `elf_image` output struct. New `kernel/elf.c` (~150 LOC) implements `elf_load(blob, size, *out)` as a 5-stage validation pipeline: (1) e_ident, (2) e_type/e_machine/e_version, (3) phdr table layout with overflow-safe phoff + phnum*phentsize, (4) per-segment validation with W^X rejection at the loader layer (mask off PF_MASKOS/PF_MASKPROC bits; check architectural PF_W & PF_X for both-set; reject), (5) entry-in-segment validation. Each additive arithmetic uses overflow-safe helpers (`u64_add_overflow`, widening u32*u32). Per-segment flags stripped to architectural PF_R/W/X bits before storing in elf_load_segment. Phase tag P2-Fd → P2-Ga. New reference doc `docs/reference/21-elf.md` covers full API + impl + test mapping + caveats. New 6 in-kernel tests in `kernel/test/test_elf.c`: parse_minimal_ok, parse_multi_segment_ok (text RX + rodata R + data RW), header_rejection (exhaustive matrix: magic/class/data/ident-version/OSABI/type ET_REL+ET_DYN/machine non-AArch64/file-version/phentsize), rwx_rejected (PF_R+PF_W+PF_X and PF_W+PF_X both rejected; RX/RW + OS-specific bits accepted), bounds_rejection (TOO_SMALL/NULL/no-phdrs/too-many-phdrs/phtab-OOB/filesz>memsz/seg-OOB), policy_rejection (PT_INTERP/PT_GNU_STACK-with-PF_X/entry==0/entry-outside-segment + final positive control). Skipped TLA+ spec per CLAUDE.md "ELF parsing is config parsing" guidance. ARCH §28 I-12 enforced at the third (loader) layer; PTE bit + mprotect are the other two. Static ET_EXEC at v1.0 (PT_INTERP/ET_DYN/PIE/PT_DYNAMIC/PT_TLS deferred to Phase 5+ dynamic-linker support). Mapping/exec deferred to Phase 3+. | **47/47 tests** (6 new); ~249 ms boot (production); ~306 ms UBSan; UBSan-clean; 4/4 fault matrix; KASLR 5/5 distinct; 4 specs all TLC-clean. |
| 2026-05-06 | **P2-Fd landed.** Kernel BURROW impl. New `kernel/include/thylacine/burrow.h` defines `enum burrow_type` (BURROW_TYPE_ANON at v1.0; PHYS at Phase 3; FILE post-v1.0); `struct Burrow` (~50 B; magic at offset 0 for SLUB freelist clobber UAF defense; size + page_count + handle_count + mapping_count + pages chunk + order). New `kernel/burrow.c` (~190 LOC) implements `burrow_init` (SLUB cache), `burrow_create_anon(size)` (eager allocation via alloc_pages(order, KP_ZERO) where order = ceil_log2(page_count); rounds up wastes some pages but simple), `burrow_ref` / `burrow_unref` (handle_count++/--), `burrow_map` / `burrow_unmap` (mapping_count++/--; refcount-only at v1.0 — Phase 3 wires actual VMA), `burrow_get_size` / `burrow_handle_count` / `burrow_mapping_count` (read-only accessors), cumulative diagnostic counters. The dual-check `if (handle_count == 0 && mapping_count == 0) burrow_free_internal(v)` at the END of both `burrow_unref` AND `burrow_unmap` is the runtime enforcement of `specs/burrow.tla::NoUseAfterFree` — neither premature free (counts > 0 ∧ pages dead) nor delayed free (counts = 0 ∧ pages alive) is reachable. `kernel/handle.c` extended with `handle_release_obj(kind, obj)` + `handle_acquire_obj(kind, obj)` per-kind dispatchers; KOBJ_BURROW calls burrow_ref / burrow_unref. `handle_close`, `handle_dup`, and `handle_table_free` all call through to maintain refcounts correctly. `handle_dup` rolls back the acquire on alloc failure. `kernel/main.c` bootstrap order: territory_init → handle_init → **burrow_init** → proc_init → thread_init. New reference doc `docs/reference/20-burrow.md` covers full API + impl + spec mapping + caveats. New 6 in-kernel tests in `kernel/test/test_burrow.c`: `create_close_round_trip` (basic + counters), `refcount_lifecycle` (multi-handle ref/unref), `map_unmap_lifecycle` (handle_count → 0 with mapping_count > 0 must NOT free), `handles_x_mappings_matrix` (6 cross-product cases verifying NoUseAfterFree at every boundary), `via_handle_table` (end-to-end through handle_alloc/dup/close), `handle_table_orphan_cleanup` (proc_free with open BURROW handles releases refs correctly). Phase tag P2-Fc → P2-Fd. | **39/39 tests** (6 new); **4/4 fault matrix**; ~245 ms boot (production); ~309 ms UBSan; KASLR 5/5 distinct; 4 specs all TLC-clean (scheduler.tla 10188, territory.tla 625, handles.tla 6.05M, burrow.tla 100). |
| 2026-05-06 | **P2-Fc landed.** Kernel handle table impl. New `kernel/include/thylacine/handle.h` defines `enum kobj_kind` (8 kinds: Process/Thread/BURROW/Spoor transferable + MMIO/IRQ/DMA/Interrupt hardware) with `_Static_assert(KOBJ_KIND_COUNT == 9)` pinning the count; `KOBJ_KIND_TRANSFERABLE_MASK`/`KOBJ_KIND_HW_MASK` with disjoint `_Static_assert`; `rights_t` bitmask (READ/WRITE/MAP/TRANSFER/DMA/SIGNAL); `struct Handle` (24 B; magic at offset 0); `struct HandleTable` (PROC_HANDLE_MAX=64 slots; 1536 B). New `kernel/handle.c` (~140 LOC) implements `handle_init` (SLUB cache), `handle_table_alloc`/`free`/`count`, `handle_alloc`/`close`/`dup`/`get`, `kobj_kind_is_transferable`/`is_hw` classifiers, cumulative diagnostic counters. `handle_dup`'s `(new_rights & parent->rights) == new_rights` subset check is the runtime enforcement of the spec's `RightsCeiling` invariant — rights elevation rejected with -1. struct Proc grew 88→96 bytes with `struct HandleTable *handles`. proc_alloc allocates a fresh table per Proc; proc_free closes any open handles + releases. `main.c` bootstrap: slub_init → territory_init → **handle_init** → proc_init → thread_init → sched_init. New reference doc `docs/reference/19-handles.md` covers full API + impl + spec mapping + caveats. New 5 in-kernel tests in `kernel/test/test_handle.c`: `alloc_close_smoke` (round-trip + counters + invalid-arg rejection), `rights_monotonic` (subset succeeds / elevated rejected), `dup_lifecycle` (independent close ordering), `full_table_oom` (PROC_HANDLE_MAX overflow + slot reuse), `kind_classifiers` (truth table for transferable/hw partitions). Phase tag P2-Fb → P2-Fc. | **33/33 tests** (5 new); **4/4 fault matrix**; ~242 ms boot (production); ~298 ms UBSan; KASLR 5/5 distinct; scheduler.tla TLC-clean (10188 states); territory.tla TLC-clean (625 states); handles.tla TLC-clean (6.05M states); burrow.tla TLC-clean (100 states). |
| 2026-05-06 | **P2-Fb landed.** burrow.tla spec — ARCH §28 I-7 (BURROW pages live until last handle closed AND last mapping unmapped). Models dual-refcount lifecycle: `handle_count` + `mapping_count` per BURROW; pages alive iff at least one count > 0. Iff-form `NoUseAfterFree` invariant catches both premature free AND delayed free. 3 buggy configs (free-on-close / free-on-unmap / never-free) each produce a counterexample at depth 6. TLC-clean at 100 distinct states / depth 9 / <1s. Models extend handles.tla's invariants — handles.tla covers per-handle policy; burrow.tla covers refcount mechanics. specs/SPEC-TO-CODE.md mapping points at P2-Fd impl targets. | 28/28 tests (unchanged — spec-only); ~270 ms boot; UBSan-clean; fault 4/4; KASLR 5/5; burrow.tla TLC-clean. |
| 2026-05-06 | **P2-Fa landed.** handles.tla spec — ARCH §28 I-2 (capability monotonic), I-4 (transfer only via 9P), I-5 (hw non-transferable), I-6 (rights monotonic on transfer). Kobj universe partitioned into TxKObjs (Process/Thread/BURROW/Spoor) ∪ HwKObjs (MMIO/IRQ/DMA/Interrupt). Per-handle `via` provenance tag in {orig, dup, 9p, direct}; OnlyTransferVia9P forbids "direct". Per-kobj `origin_rights` ceiling pinned at HandleAlloc; RightsCeiling: handle.rights ⊆ origin_rights. Per-Proc `proc_caps` ceiling: ProcRoot starts at full Caps; others at {}; CapsCeiling: caps ⊆ initial. 4 buggy configs (BUGGY_ELEVATE / HW_TRANSFER / DIRECT_TRANSFER / CAPS_ELEVATE) each produce a counterexample. TLC-clean at 6,055,072 distinct states / depth 26 / ~4 min on 8 cores. specs/SPEC-TO-CODE.md mapping points at P2-Fc impl targets. | 28/28 tests (unchanged — spec-only); ~270 ms boot; UBSan-clean; fault 4/4; KASLR 5/5; **handles.tla** TLC-clean. |
| 2026-05-06 | **P2-Eb landed.** Kernel territory impl. New `kernel/include/thylacine/territory.h` defines `struct Territory { u64 magic; int ref; int nbinds; struct PgrpBind binds[PGRP_MAX_BINDS=8] }` (80 bytes; magic at offset 0 for double-free defense). New `kernel/territory.c` (~150 LOC) implements `territory_init` (boot-time SLUB cache + kpgrp allocation), `territory_alloc` / `territory_clone` / `territory_ref` / `territory_unref`, `bind` / `unmount`. `bind`'s `would_create_cycle` precondition is a fixed-point reachability iteration over the existing edges; rejects with -1 if adding `dst -> src` would close a cycle (matches spec's `WouldCreateCycle` exactly). `struct Proc` grew 80→88 bytes with the `struct Territory *territory` field; `_Static_assert` updated. `proc_init` now requires `territory_init` to have run (kproc gets kpgrp at allocation). `rfork(RFPROC)` calls `territory_clone(parent->territory)` for the child's territory; OOM rolls back the proc allocation. `proc_free` calls `territory_unref(p->territory)` before slab release. `main.c` bootstrap order: slub_init → territory_init → proc_init → thread_init. PGRP_MAX_BINDS = 8 chosen to keep the Territory struct under 80 bytes — SLUB's KP_ZERO byte-loop on a larger struct (e.g., 32 entries = 272 bytes) inflated the proc.rfork_stress_1000 boot time past the 500 ms VISION §4 budget under QEMU emulation. New reference doc `docs/reference/18-territory.md` covers API + impl + spec mapping + caveats. New 3 in-kernel tests in `kernel/test/test_territory.c`: `bind_smoke` (round-trip + idempotency + unmount), `cycle_rejected` (spec NoCycle invariant verification: 3-node chain rejects cycle-closing bind + self-bind), `fork_isolated` (territory_clone deep-copy verification: parent and child evolve independently). Phase tag P2-Ea → P2-Eb. | **28/28 tests** (3 new); **4/4 fault matrix**; ~270 ms boot (production); ~320 ms UBSan; KASLR 5/5 distinct; scheduler.tla TLC-clean (10188 states); territory.tla TLC-clean (625 states). |
| 2026-05-06 | **P2-Ea landed.** New spec `specs/territory.tla` (~165 LOC TLA+) + `territory.cfg` + `namespace_buggy.cfg`. Models Plan 9 `bind` as a directed graph: edge `dst → src` means walking `dst` produces `src` (per ARCH §9.1). Cycle-freedom (ARCH §28 I-3) is the primary state invariant: `NoCycle == ∀ p ∈ Procs, x ∈ Paths : x ∉ Reachable(p, bindings[p][x])`. The `Bind` action's precondition is `~WouldCreateCycle(p, src, dst)` — adding the edge `dst → src` is rejected if `dst` is already reachable from `src` (which would close the loop `src → ... → dst → src`). `BuggyBind` skips the cycle check; counterexample at depth 4 / 95 states. Isolation (ARCH §28 I-1) is structural in the data model: bindings[p] and bindings[q] for p#q are independent function values; no action updates two procs simultaneously. RFNAMEG sharing deliberately deferred until Phase 5+ syscall surface lands (impl extincts on non-RFPROC at v1.0). `ForkClone(parent, child)` action models rfork(RFPROC)'s territory copy; `Unbind` mirrors `unmount`. `specs/SPEC-TO-CODE.md` updated with the territory.tla mapping pointing at P2-Eb impl targets (`kernel/territory.c::bind`, etc.). Phase tag P2-Dc → P2-Ea. P2-Eb (kernel/territory.c impl) is the next sub-chunk. | 25/25 tests; 4/4 fault matrix; ~265 ms boot (production); ~312 ms UBSan; KASLR 5/5 distinct; **scheduler.tla TLC-clean (10188 states); territory.tla TLC-clean (625 states); namespace_buggy.cfg counterexample at depth 4 / 95 states**. |
| 2026-05-06 | **P2-Dc landed.** Per-thread kstack guard pages. Closes P2-A trip-hazard #1 (kstack overflow detection). thread_create now allocates 8 pages (32 KiB at order=3) instead of 4; upper 4 pages = 16 KiB usable kstack, lower 4 pages = 16 KiB no-access guard. New `THREAD_KSTACK_TOTAL_SIZE` / `THREAD_KSTACK_TOTAL_ORDER` / `THREAD_KSTACK_GUARD_SIZE` / `THREAD_KSTACK_GUARD_PAGES` constants in `<thylacine/thread.h>`. New MMU API in `arch/arm64/mmu.{h,c}`: `mmu_set_no_access(pa)` + `mmu_restore_normal(pa)` (single-page) and `mmu_set_no_access_range(pa, n)` + `mmu_restore_normal_range(pa, n)` (batched, single TLB flush — substantially faster at thread_create / thread_free volume). The L2 block descriptor covering the kstack PA is demoted to an L3 table on first guard insertion: a fresh page from buddy is initialized with 512 × PTE_KERN_RW page descriptors reproducing the original block's mapping, then the L2 entry is break-before-make replaced with a table pointer. Subsequent guards in the same 2 MiB block reuse the existing L3. The exception handler's `addr_is_stack_guard()` now also checks the current thread's kstack guard region (in addition to the boot-stack guard from start.S) — overflow into a guard page produces "EXTINCTION: kernel stack overflow" with FAR pointing into the guard. New `THYLACINE_FAULT_TEST=kstack_overflow` variant in `kernel/fault_test.c`: spawns a thread that recursively allocates 1 KiB stack frames; ~16 frames overflow the 16 KiB usable region into the guard. `tools/test-fault.sh kstack_overflow` PASSes on observing the extinction. `addr_is_stack_guard()` exception path now `#include`s `<thylacine/thread.h>` for `current_thread()` access. SMP-safety note: thread_create/thread_free are single-CPU at v1.0; concurrent demote at Phase 5+ requires a global mmu_lock. test_context.c bumped to expect THREAD_KSTACK_TOTAL_SIZE (32 KiB). Phase tag P2-Db → P2-Dc. | 25/25 tests; 4/4 fault matrix (canary, W^X, BTI, **kstack_overflow** new); ~236 ms boot (production); ~285 ms (UBSan); KASLR 5/5 distinct; scheduler.tla TLC-clean (10188 states). |
| 2026-05-06 | **P2-Db landed.** Lifecycle stress upgraded to 1000 iterations in batches of 8 (`proc.rfork_stress_100` → `proc.rfork_stress_1000`). Each batch fans out 8 children before reaping any — exercises the run tree's depth + the wait_pid Rendez under sustained load. Per-CPU run counters track which CPU executed each child; sum-must-equal-ITERS check verifies no double-reap or missed run. Cross-CPU placement assertion lifted out: at this phase secondaries don't run their own timer IRQs (per-CPU timer init lands later) and rfork doesn't yet send IPI_RESCHED to peers, so children placed by ready() in the local run tree are typically picked by the same CPU before secondaries can steal — a dedicated cross-CPU test lands once either secondary timers OR rfork-driven IPI_RESCHED is wired (P2-Dd or post-P2-Db). Per-thread guard pages (kstack underflow detection — closes P2-A trip-hazard #1) deferred to P2-Dc: requires L2-block → L3-table demotion in `arch/arm64/mmu.c` because kstacks live in 2 MiB-block-mapped RAM, and per-page no-access protection requires 4 KiB granularity. Phase tag P2-Da → P2-Db. | 25/25 tests; ~232 ms boot (production, +36 ms vs P2-Da); ~270 ms (UBSan); fault matrix 3/3; KASLR 5/5 distinct; scheduler.tla TLC-clean (10188 states). |
| 2026-05-05 | **P2-Da landed.** rfork(RFPROC, entry, arg) + exits(msg) + wait_pid(*status). struct Proc grew 24→80 bytes with state + parent/children/sibling + exit_status + exit_msg + child_done Rendez. exits() transitions Proc to ZOMBIE, marks calling Thread EXITING, wakes parent's child_done; sched() leaves EXITING out of run tree. wait_pid() blocks on parent's child_done Rendez until any child is ZOMBIE; reaps via thread_free + proc_free. Orphan re-parenting: exit-with-children re-parents to kproc (PID 0; init at Phase 5+). thread_create_with_arg passes arg in x0 via `mov x0, x20` in thread_trampoline (extending the existing `blr x21` pattern). 3 new tests: `proc.rfork_basic_smoke` (lifecycle smoke), `proc.rfork_exits_status` (exits("err") → non-zero status), `proc.rfork_stress_100` (100-iter rfork+exits+wait_pid; verifies created/destroyed counters track ITERS — no leak in proc_cache or thread_cache or kstack pages). proc_free now requires ZOMBIE state (lifecycle violation extincts on ALIVE/INVALID free). thread_create_internal helper consolidates the no-arg + with-arg paths. P2-Da is single-thread Procs only; multi-thread Procs require IPI-based exits coordination (Phase 5+). | 25/25 tests; ~196 ms boot (production), ~226 ms (UBSan); UBSan-clean; fault matrix 3/3; KASLR 5/5 distinct; scheduler.tla TLC-clean (10188 states; spec untouched). |
| 2026-05-05 | **P2-Cg landed.** specs/scheduler.tla extended (~389 → ~620 LOC TLA+); 3 new buggy configs (`scheduler_buggy_steal.cfg`, `scheduler_buggy_ipi.cfg` + existing `scheduler_buggy.cfg` updated with new constants); SPEC-TO-CODE.md refreshed for P2-Cg. New actions: `Steal(stealer, victim)` (atomic cross-CPU transfer, maps to `kernel/sched.c::try_steal`), `IPI_Send(src, dst)` + `IPI_Deliver(src, dst)` (per-(src,dst) FIFO queue with monotonic seq numbers; map to `arch/arm64/gic.c::gic_send_ipi` + `kernel/smp.c::ipi_resched_handler`). New buggy actions: `BuggySteal` (forgotten unlink → double-enqueue), `BuggyIPI_Deliver` (out-of-order delivery). New invariants: `NoDoubleEnqueue` (thread in ≤1 runq), `IPIOrdering` (head of queue = next-expected delivery seq). TLC posture: scheduler.cfg explores 10188 distinct states (depth 16, <1 s); scheduler_buggy_steal.cfg counterexample depth 4 / 38 states; scheduler_buggy_ipi.cfg counterexample depth 6 / 470 states; scheduler_buggy.cfg unchanged behavior (245 states / depth 6). Kernel impl untouched — P2-Cg is spec-only. Closes the spec-first deviation introduced in P2-Ce/Cf. | SMP scheduler section's spec side closed. ARCH §28 I-18 (FIFO IPI ordering) and §8.4 (work-stealing safety) both pinned in TLC-checkable form. P2-D (rfork + exits + wait) opens the multi-process model on a fully-realized + fully-specified SMP scheduler. |
| 2026-05-05 | **P2-Cf landed.** snapshot bumped (22/22 tests, ~6950 LOC, ~308 KB ELF). New `volatile bool on_cpu` in struct Thread (size 216 → 224 B). Set true when picked as next; cleared on switch-out via cs->prev_to_clear_on_cpu handoff (mirrors P2-Ce's pending_release_lock pattern). wakeup() spins on `__atomic_load_n(&t->on_cpu)` before state transition. Closes P2-Bb trip-hazard #16: a peer's wakeup can no longer insert a still-running thread into a runqueue mid-cpu_switch_context-save. New public helpers sched_arm_clear_on_cpu + sched_finish_task_switch let thread_switch (legacy direct-switch) participate in the on_cpu protocol. | SMP wait/wake race fully closed at the impl level. P2-Cg lifts the discipline to scheduler.tla (Steal action + IPI ordering invariant I-18). |
| 2026-05-05 | **P2-Ce landed.** snapshot bumped (22/22 tests, ~6900 LOC, ~307 KB ELF). Real spinlocks via `__atomic_exchange_n` (acquire/release; LSE `swpa` or LL/SC fallback). spin_trylock for cross-CPU access. Per-CPU run-tree lock added to `struct CpuSched`. New `try_steal(cs)` scans peers via spin_trylock, takes one runnable thread (highest non-empty band), rebases vd_t to caller's clock. Called in sched() before "yield-no-peer" fallback. New `cs->pending_release_lock` finish_task_switch handoff: prev sets it before cpu_switch_context; resume path (in sched OR thread_trampoline via `sched_finish_task_switch`) reads from destination CPU's slot, releases, clears. Closes the migration race where a stolen thread's resume frame thinks it's on a different CPU than it is. NULL-guard in sched_finish_task_switch lets thread_switch (legacy direct-switch from P2-A context tests) coexist. spin_lock_irqsave/irqrestore now thread NULL through gracefully. New test smp.work_stealing_smoke. | Cross-CPU thread placement via work-stealing complete. The basic SMP scheduler is end-to-end functional: ready() → tree → preempt/yield → cross-CPU steal → migrate → resume on new CPU. P2-Cf refines the wait-wake race close (P2-Bb #16); P2-Cg adds scheduler.tla SMP refinement (Steal action + IPI ordering I-18). |
| 2026-05-05 | **P2-Cdc landed.** snapshot bumped (21/21 tests, ~6700 LOC, ~292 KB ELF). New `gic_init_secondary(cpu_idx)` brings up per-CPU GIC redistributor (refactored `redist_init_cpu0` → `redist_init_cpu(idx)` walking g_redist_base + idx*0x20000) + CPU interface system regs. New `gic_send_ipi(target, sgi)` writes ICC_SGI1R_EL1 with TargetList encoding. New `IPI_RESCHED = SGI 0` + per-CPU `g_ipi_resched_count` BSS + `ipi_resched_handler`. New `smp_cpu_ipi_init(cpu_idx)` called from per_cpu_main: per-CPU GIC bring-up + handler attach + SGI enable + msr daifclr #2. per_cpu_main idle loop changed from pure WFI to `for(;;){sched();wfi;}` — IPI_RESCHED wakes WFI. **Bug fix**: gic_enable_irq for SGI/PPI was hardcoded to CPU 0's redistributor; refactored to use cpu_redist_base(smp_cpu_idx_self()). New test smp.ipi_resched_smoke. | Cross-CPU IPI infrastructure complete. Secondaries can now receive work delivery; IPI_RESCHED is the first reserved SGI. P2-Ce work-stealing introduces cross-CPU thread placement; P2-Cg adds scheduler.tla SMP refinement with IPI ordering invariant I-18. |
| 2026-05-05 | **P2-Cd landed (Cda+Cdb; Cdc deferred).** 15-scheduler.md extended with per-CPU dispatch section; snapshot bumped (20/20 tests, ~6500 LOC, ~288 KB ELF). Per-CPU CpuSched struct (run tree + vd_counter + initialized + idle), `g_cpu_sched[DTB_MAX_CPUS]` BSS. sched()/ready()/sched_tick/preempt_check_irq refactored to index by smp_cpu_idx_self(). g_need_resched is now per-CPU array. sched_init takes cpu_idx. New `thread_init_per_cpu_idle(cpu_idx)` allocates idle Thread descriptor (no kstack — uses per-CPU boot stack from secondary_entry). per_cpu_main on secondaries: VBAR + idle thread + TPIDR_EL1 + sched_init + g_cpu_alive + WFI loop. New `sched_idle_thread()` accessor. New test smp.per_cpu_idle_smoke. **GIC SGI + IPIs deferred to P2-Cdc** — secondaries currently park in pure WFI without IRQ wake source; the sched()-then-WFI idle loop body lands at P2-Cdc when IPIs deliver work. | Per-CPU scheduler infrastructure complete on the data-structure side. Boot CPU continues to run all threads; secondaries hold idle Thread descriptors + initialized run trees but receive no work yet. P2-Cdc's IPI_RESCHED unlocks cross-CPU thread placement. |
| 2026-05-05 | **P2-Cc landed.** 17-smp-bringup.md extended with per-CPU exception stack section; snapshot bumped (19/19 tests, ~6320 LOC, ~282 KB ELF). New `g_exception_stacks[DTB_MAX_CPUS][4096]` (32 KiB BSS) + `EXCEPTION_STACK_SIZE` macro + `smp_cpu_idx_self()` accessor. start.S `_real_start` step 4.6 (primary) + `secondary_entry` (secondaries) split SP into SP_EL0 = boot stack + SP_EL1 = per-CPU exception stack via the SPSel-dance. step 8.5 + post-mmu_program_this_cpu re-anchor SP_EL1 to HIGH VA after MMU. vectors.S keeps both Current-EL/SP_EL0 (0x000+0x080) AND Current-EL/SP_ELx (0x200+0x280) Sync+IRQ slots LIVE — kernel transits through SPSel=1 transiently after sched-from-IRQ. `timer_irq_handler` instrumented to capture `&local` once per CPU into `g_exception_stack_observed[]`. New test `smp.exception_stack_smoke`. New banner `exception:` line. | Closes vectors.S P1-F "KNOWN LIMITATION" — kernel stack overflow no longer recursively faults inside KERNEL_ENTRY (SP_EL1 = exception stack with full headroom regardless of SP_EL0 state). True per-CPU exception isolation. Stack-overflow detection now reachable. P2-Cd lands per-CPU run trees + GIC SGIs + IPIs + idle threads. |
