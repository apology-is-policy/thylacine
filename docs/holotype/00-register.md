# HOLOTYPE register

One row per finding, appended per RW (`docs/HOLOTYPE.md` §5). IDs are
`HT<NN>.F<n>` where NN = the RW number. Severity: soundness P0–P3;
non-soundness (T/P/X/G lenses) H1–H4. Every actionable row is also enqueued
as a tracked task at confirmation time — this register is the RW-13 triage
input, not the queue. Dispositions: FIXED (in-arc, with regression),
ACCEPTED (closed-with-justification + doc caveat), TRACKED (#N), REGISTERED
(non-soundness; triaged at RW-13).

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT00.F1 | ut session shell (LS-5) | S | P1 | `usr/utopia` (ut main) | Fresh-prompt Ctrl-C logged the session out: the session shell is console owner (LS-5a) but opened its note queue LAZILY (first keystroke), so an uncaught `interrupt` before any input took the LS-5b default-terminate; login treats the shell's exit as logout | FIXED `@f145ce8` (eager open gated on `io::stdout_is_live()`; the bare-spawn boot check deliberately stays lazy — an eager open there would mint the note fd as fd 0). Regression: `tools/interactive/ls-5.exp` case A |
| HT00.F2 | kernel notes | S | P3 | `kernel/notes.c` (`notes_post`) | A console Ctrl-C is a synthetic `interrupt` post; a queue already holding NOTE_QUEUE_DEPTH (16) non-interrupt notes gives the coalesce pass nothing to overwrite → -EAGAIN → the Ctrl-C is dropped AND the LS-5c terminate latch never arms (the arm rides a landed post) | ACCEPTED at RW-0 close + `notes.h` known-caveat. Unreachable for a typical foreground coreutil (precondition: 16 queued unconsumed notes); revisit with any queue-pressure poster (e.g. reserve the head slot for interrupt/kill) |
| HT00.F3 | kernel notes | S | P3 | `kernel/notes.c` (`thread_die_pending`) | The lock-free latch leg had no kproc guard; correctness rested entirely on the single arm site (`notes_arm_intr_terminate_locked`) refusing kproc — a future arm path that forgot would put every kernel kthread sleep into perpetual `*_INTR` unwind (kproc threads never EL0-return, so the latch could never be consumed) | FIXED at RW-0 close (kproc skip on the latch leg; the wake `proc_interrupt_terminate_wake` already carried its own belt) + a forced-flag leg in `notes.die_pending_predicate` |
| HT00.F4 | kernel notes | S | P3 | `kernel/notes.c` + the latch consumers | Multi-thread disposition race: thread B registering a handler (SYS_NOTIFY) or opening the notes fd (self-managing) between an interrupt post and blocked thread A's resume → A takes one spurious `*_INTR` (e.g. a 9P RPC surfaces -P9_E_IO). The EL0 tail re-validates, so never a wrong termination | ACCEPTED at RW-0 close + `notes.h` known-caveat. v1.0-unreachable: multi-thread Procs are stratumd-class, never console-owner/foreground; POSIX parallel = EINTR-with-handler |

## RW-1 — Memory (FULL). Reviewers: 3× Fable-max `holotype-reviewer` (all self-reported `claude-fable-5`) over A=allocators / B=mmu+W^X / C=burrow+vma+fault, + an Opus seam self-review. First pass (default-effort) then an authoritative max pass. Full reports: `/tmp/holotype-rw1-{A,B,C}.md` + `-{A,C}-max.md` (B-max inline). Status: most fixed; the ASID rollover sub-chunk is mid-flight (scripture `83bd74e`; spec next).

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT01.A-F1 | slub | S | P2 | `mm/slub.c:354` kmalloc large path | `(n + PAGE_SIZE-1) >> PAGE_SHIFT` wraps for n near SIZE_MAX → returns ONE page (heap-overflow primitive); also defeats kcalloc's n*size guard; `07-slub.md:266` certified the broken path as NULL | FIXED `@baea64e` (reject `n > SIZE_MAX-(PAGE_SIZE-1)`). Regression `slub.kmalloc_overflow_guard`. Verified SOUND by A-max |
| HT01.A-F2 | slub | S | P2 | `mm/slub.c` `g_cache_list_head` | The kmem_cache list had NO lock — create/destroy/walk unsynchronized | FIXED `@baea64e` (`g_cache_list_lock`, leaf). Verified SOUND + lock-order-prosecuted by A-max (no ABBA) |
| HT01.A-F-S1 | slub | S | P2 | `mm/slub.c` `kmem_cache_destroy` | Destroy drained partial slabs unconditionally — a partial slab with a live object had its page recycled (silent UAF); the old guard only checked `nr_full` | FIXED `@5b68210` (gate on `alloc_count-free_count != 0`). Regression `slub.cache_destroy_guards` + `kmem_cache_live_count` |
| HT01.A-F-S2 | slub | S | P3 | destroy quiescence | Destroy's count-read + partial-drain run without c->lock (quiescence assumed, undocumented) | FIXED `@5b68210` (doc the contract, like magazines_drain_all) |
| HT01.A-F-S3 | slub | S | P3 | `kmem_cache_create` | `align > PAGE_SIZE/2` → `objects_per_slab==0` → NULL-deref on first alloc | FIXED `@5b68210` (create-time sizing pre-check → NULL) |
| HT01.B-F1 | asid | S | **P1** | `arch/arm64/asid.c:98` | ASID-space exhaustion `extinction()`s the WHOLE kernel at the 256th concurrent Proc (8-bit, no rollover) — unprivileged whole-system DoS | **CLOSED** — generation-rollover landed: fail-soft `@ef29456` → scripture `@83bd74e` (ARCH §6.2.1 + I-31) → spec `@4fe50f7` (`specs/asid.tla`, model-first) → impl `@d742ffa` → focused Fable audit close `@d40dbbb` (0 P0 / 1 P1 / 1 P2 / 2 P3, all fixed; the P1 was a SPEC-fidelity defect — the model reclaimed reservations ownerless, the impl's full-cid `check_update_reserved` does not — fixed with the `rproc` ownership model + cfg bounds raised to ≥4 Procs). 806/806; SMP gate 0 corruption. Dispositions: `audit_holotype_rw1_closed_list.md` |
| HT01.B-F4 | mmu | S | P3 | `mmu.c` `mmu_map_mmio` | Missing the `size+PAGE_SIZE-1` overflow guard its sibling `burrow_create_anon` has (dormant; boot-only DTB sizes) | FIXED `@0e9a4c3` |
| HT01.B-F5 | mmu | S | P3 | `mmu.c:258` `pte_violates_wxe` | Checked only EL1 (PXN), not EL0 (UXN) — a writable+user-exec PTE returned false (dormant; 0 callers) | FIXED `@0e9a4c3` (also flag UXN; contract → "either EL") |
| HT01.B-F2 | mmu | C | P3 | `mmu.h` + `mmu.c:990` | Stale "Phase 5+ adds a global mmu_lock" comments — #808 discharged it (the kstack-guard path leaf-flips, SMP-safe by construction) | FIXED `@0e9a4c3` (comments → #808 reality) |
| HT01.B-F3 | mmu | C | P3/H2 | `docs/reference/03-mmu.md` | Public API/Status omit the entire runtime MMU surface; perf table 28→44 KiB; documents `mmu_map_device` (real `mmu_map_mmio`) | **FIXED `@cb99514`** (mmu_map_mmio corrected; runtime PTE surface + rolling-ASID note added; 44 KiB) |
| HT01.B-F6 | mmu | P | P3/H3 | `mmu.c:1718` `mmu_uninstall_user_range` | Per-page `dsb ish` loop under `vma_lock` — up to ~tens of ms stall on a 256 MiB detach (the code notes batching) | REGISTERED for RW-11 (perf inventory; TLBIRANGE batching) |
| HT01.C-F1 | proc/fault | S | **P1** | `kernel/proc.c:1442` `proc_fault_terminate` | An EL0 fault in any multi-thread Proc (stratumd) `extinction()`s the whole kernel — defeats the snare:* per-Proc design; #810, but its blocker (SYS_EXIT_GROUP) landed | FIXED `@2891bf2` (retire the stale branch → fall through to `exits()`, which already routes multi-thread via the #811 cascade). Regression `/thread-fault-probe` |
| HT01.C-F2 | burrow | S | P2 | `burrow.h:258-300` | `burrow_map`/`burrow_unmap` header contracts omit the load-bearing `p->vma_lock` precondition (a future Tier-2 caller reintroduces #713) | **FIXED `@cb99514`** (precondition + lock order documented on both) |
| HT01.C-F3 | vma | S | P3 | `kernel/vma.c:63` `vma_alloc` | Accepts write-only prot (no READ); AArch64 has no W-only AP, so it maps RW (readable) — the rights/PTE mismatch the MMIO/DMA syscalls guard | **FIXED `@cb99514`** (reject `WRITE && !READ`) |
| HT01.C-F4 | burrow | S | P3 | `burrow.c:311` `burrow_acquire_mapping` | Lacks `burrow_ref`'s both-counts-zero resurrection guard + reads liveness outside `v->lock` (latent; sole caller passes a live-handle Burrow) | **FIXED `@cb99514`** (zero-zero guard + liveness read + count++ all under `v->lock`; mirrors `burrow_ref`) |
| HT01.C-F5 | syscall | P | P3 | `syscall.c:2836` `SYS_BURROW_ATTACH` | Holds `vma_lock` across an up-to-256 MiB zeroing; "single-threaded so uncontended" is stale | REGISTERED for RW-11 (perf; the documented F2 narrowing — create outside the lock) |
| HT01.C-F6 | burrow | C | P3/H3 | `docs/reference/20-burrow.md` | Misdescribes `burrow_unmap` ("does NOT touch PTEs" — it does, the p6-F1 fix) + the #847 lock ("Single-CPU / not synchronized"); stale struct + `BURROW_TYPE_PHYS` | **FIXED `@cb99514`** (PTE-teardown + #847 per-Burrow lock + P4 MMIO/DMA split corrected) |
| HT01.C-F9 | exec | S | P3 | `kernel/exec.c:111` | I-cache maintenance covers only `[0,filesz)`; an X-segment's BSS tail can retain stale I-cache from a recycled page (low-confidence; reachable only by jumping into a BSS tail) | TRACKED (low-confidence; register for the hardening pass) |
| HT01.A-H/T | slub | T | H2/H3 | `mm/slub.c` | No freelist hardening (XOR-canary/randomization — SLAB_FREELIST_HARDENED absent); no per-CPU active-slab fast path (global c->lock); no double-free/poison debug mode (ARCH §6.4 marks it COMMITTED) | REGISTERED for RW-11 (perf/hardening) |
| HT01.C-F7/F8 | vma/burrow | T | H3/H4 | `proc.h:270` + `vma.c` | Single per-Proc `vma_lock` + O(N) sorted-list VMAs (vs Linux per-VMA locks + maple tree); Fuchsia-VMO feature delta (partial unmap, COW clones, slices) | REGISTERED for RW-11/RW-12 |

**RW-1 verified SOUND (do not re-prosecute without new code):** the I-7 dual-refcount lifecycle under every interleaving (incl. the #926 inverted close-at-exit/drain-at-reap, Loom-pin-vs-munmap, group-terminate-vs-sibling-fault); `vma_lock` coverage on every non-exempt mutator+reader (exec_setup/vma_drain single-threaded exemption still holds — `wait_pid`'s on_cpu-spin); BBM discipline + the #808 pagemap invariant; TLBI-before-page-reuse on every path; W^X (I-12) at every alias (canonical RO+X, direct-map RO+XN, patch alias transient); user/kernel isolation (UXN/PXN); TTBR0 ASID-switch single-copy atomicity; walk index/bit math + the 2^47/2^40 bounds; `proc_free` teardown ordering (vma_drain → asid_free → pgtable_destroy); W1.5 patcher unchanged. **0 P0 across the whole surface.**

## RW-2 — Sched/SMP/threads/death/wait-wake (FULL death-half, DELTA sched). Reviewers: 3× Fable-max `holotype-reviewer` (2A sched-core, 2B death-paths, 2C wait-wake; all `claude-fable-5`) + Opus self-audit; then a 2× Fable dirty-close round-2 on the FIXES (both clean). Reports: `docs/holotype/02-sched-death-waitwake.md`. Closed list: `audit_holotype_rw2_closed_list.md`. Status: CLOSED `@5033b94`. Headline: the death cascade is SOUND under composition; the 4 P1s are all the multi-thread-Proc lift P6 landed but the wait/poll machinery never caught up to.

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT02.2C-F1 | poll | S | **P1** | `poll.c` poll_scan_one | Poll stack-waiter outlived the obj ref (handle_put before tsleep; sibling closes the polled fd mid-sleep → freed poll_list → UAF at unregister). poll.h precondition deferred to "when multi-thread lands" = P6 | **FIXED** `@e504e8b` (retain ref across sleep, release post-sweep; poll.h rewritten) |
| HT02.2A-F1 | sched | S | **P1** | `sched.c` ready_on | Wake never rebased vd_t → cross-CPU wake onto a busy CPU starves (I-17 + ARCH §8.2 fidelity hole) | **FIXED** `@e504e8b` (clamp to target vd_counter). Regression `scheduler.ready_on_clamps_stale_vd` |
| HT02.2B-F1 | proc | S | **P1** | `proc.c` wait_pid_for + `sched.c:1331` | Two peer Threads in wait_pid trip child_done's single-waiter assert → kernel extinction (DoS) | **FIXED** `@e504e8b` (per-Proc `wait_active` serialization). Regression `proc.wait_pid_concurrent_waiter_refused` |
| HT02.2B-F2 | proc | S | **P1** | `proc.c` wait_pid_cond | Lockless p->children walk races a peer's reap+free → UAF. Same root as F1 | **FIXED** `@e504e8b` (same wait_active flag serializes the reaper) |
| HT02.2B-F3 | proc | S | **P2** | `proc.c:533` proc_reparent_children | Orphans → g_kproc (never reaps) → unbounded zombie+kstack leak; diverges from ARCH §7.9 ("PID 1") | **FIXED** `@0ce638b` — reparent-to-init (`g_init_proc`, joey publishes; kproc fallback pre-init) + joey WNOHANG adoptee sweep + by-pid bringup waits + the adopted-already-zombie `child_done` wake. Fable subagent impl + Opus eval (added the adopted-zombie regression) + a formal Fable holotype-reviewer round: 0 P0/P1/P2, 4 P3 (3 fixed + F4 within-session-accum tracked). Closed list: `audit_2bf3_init_reaping_closed_list.md` |
| HT02.2A-F2 | sched | S | P3 | `sched.c:992` sched() | prev==next branch re-inserted a RUNNING on_cpu thread (latent RunqOnCpuSafe violation) | **FIXED** `@e504e8b` (extinction — fail loud) |
| HT02.2A-F3 | sched | S | P3 | `sched.c:1107` resume | resume spin_unlock(lk) lacked the NULL guard its twin has (test-only reachable → store to VA 0) | **FIXED** `@e504e8b` (mirror `if (lk)`) |
| HT02.2A-F4 | smp | S | P3 | `smp.c` per_cpu_main | No context_id == MPIDR-index cross-check → sparse-MPIDR silently aliases a peer CpuSched | **FIXED** `@e504e8b` (equality assert; advances #7) |
| HT02.2A-F5 | sched/proc | C | P3 | thread.h/sched.h/context.S/ARCH | 4 stale comments (thread.h lied about the #811 rendez_blocked_on protocol; sched.h bootcpu-idle; context.S ASID 1..255; ARCH balance steal-freq) | **FIXED** `@e504e8b` |
| HT02.2A-F6 | sched | C | H2 | `sched.c` + ARCH §8.2/8.6/8.7/28 | Shipped "EEVDF" is a per-CPU yield counter (no ve_t/W_total/weights); tickless+IPI-taxonomy unimplemented | **RECONCILED `@fb5e776`** (with #60: ARCH 8.2 "As-built" note documents the yield-counter approximation + the wake-preemption mechanism; I-17 stays a deferred design target). The full weighted-EEVDF lift (tickless / weights / red-black tree) remains the v1.x EEVDF chunk |
| HT02.2A-F7 | sched | S | P3 | `sched.c` Place | Atomic-in-model Place is 2-lock 2-step in code (a window thread_free could pass) | **ACCEPTED** — 2B confirmed the reap-lifecycle excludes the live UAF (thread_free only at reap on EXITING). Model-fidelity note |
| HT02.2C-F2 | notes | C | H3 | `notes.c`/`devnotes.c` | An inherited (not self-opened) notes-fd reader is default-terminated by `interrupt` instead of receiving it | **REGISTERED** — v1.0-unreachable; on record before any notes-fd-across-spawn |
| HT02.SA-1 | sched/death | C/T | H | (verification method) | The death-wake cascade (8 historical bugs) has NO machine-checked model; sched_alpha models only the benign Place. The project re-enabled spec-first for sched_alpha + ASID on the same bar | **SURFACED** RW-13 (task #9) — weigh `specs/death_wake.tla`; spec re-enablement is a user vote |
| HT02.SA-2 | death | C | H | (test coverage) | No deterministic multi-CPU death-cascade test; the 3-way interleave rides only the SMP gate + a racy E2E | **TRACKED** (#10) — the owed concurrent harness (shared w/ Loom/9p). Empirical twin of SA-1 |
| HT02.SA-3 | proc | S/C | P3 | `proc.c:1505`/`:1661` | #926 fd-close asymmetry × LS-5: a killed/interrupt-terminated single-thread Proc defers fd-close to reap → the #926 drain-hang latently re-introduced | **TRACKED** (#11) — v1.0-unreachable on a fragile argument; robust fix = v1.x close-on-kill lift. #926 F2 re-recorded |
| HT02.R2-poll-F1 | poll | C | P3 | `poll.h` | The "keeps a SrvConn alive directly" claim is false — the KObj_Srv listener retain is INERT (no-op acquire/release); safe only via the immortal boot registry | **FIXED** (comment corrected). A-5b mortal-registry listener-poll seam tracked (#18) |
| HT02.R2-sched-F1 | sched test | T | P3 | `test_sched.c` | The vd_t no-op (t2) half is a host-timing flake (key-0 head on the live CPU, preemption armed) | **FIXED** (mask IRQs across place→capture→remove) |
| HT02.R2-sched-F2 | proc test | T | P3 | `test_thread_spawn.c` | The wait_pid test pre-seeded the expected pid → tautological-in-isolation assert | **FIXED** (separate expect_pid; reaped_pid is a witnessed sentinel) |

**RW-2 verified SOUND (do not re-prosecute without new code):** the #809/#811/LS-5/#926/wait_pid-v2 death machinery under the 5-layer composition (per-Proc-lock cascade + I-9 death-wake both interleavings; Option-A stack-pin; lock-order `g_proc_table_lock→wait_lock→g_timerwait→r->lock` acyclic; exactly-once ZOMBIE; on_cpu-spin-before-free; group_exit_msg set-once; #926 at-exit close vs concurrent cascade; proc_fault_terminate fall-through; the 10 *_INTR arms; the LS-5 latch); the on_cpu/migration core vs `sched_alpha.tla` (DoSwitch/FinishSwitch clear-before-unlock, RunqOnCpuSafe, steal/pick ASSERTs, pinned-idle, idle_in_wfi, #801/F1 steal-claim); poll register-then-observe across N fds + two-fds-one-ring release; the fix internals (wait_active cleanup-on-all-paths + the clean `_pad_srv` repurpose + the exclusion property; poll ref balance + double-put freedom + no-sleep-at-death-tail). **0 P0 across the whole surface; round-2 clean on the fixes.**

## RW-3 — Exception entry + syscall surface (FULL). Reviewers: 4× Fable-max `holotype-reviewer` (R1 arch-entry, R2 syscall-proc-half + dispatch core, R3 syscall-fs-half, R4 ABI; all `claude-fable-5`) + Opus self-audit + a focused round-2 on the 2 chokepoint fixes (CLEAN). Report: `docs/holotype/03-exception-syscall.md`. Closed list: `audit_holotype_rw3_closed_list.md`. Status: CLOSED `@01e1b40`. Headline: the privilege chokepoint is SOUND; the 4 P2s were all latent/masked, the sharpest a masking-bug stack (OEXEC) hiding behind a dev9p quirk.

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT03.R1-F1 | exception entry | S | **P2** | `arch/arm64/userland.S` | `userland_enter` (exec→EL0 trampoline) lacks `el0_return_die_check` (I-24); legate-scope teardown group-terminates a child mid-`exec_setup` on a peer CPU → it erets to EL0 + runs its ELF entry (dispatches one syscall) before self-healing | **FIXED** `@01e1b40` (`bl el0_return_die_check` before the daifset window, x19/x20-parked; mirrors thread_user_trampoline; #713 window byte-identical, x30 swept). Opus self-audit L1 flagged the gap; R1 proved reachability |
| HT03.R2-F1 | syscall | S | **P2** | `kernel/syscall.c` explicit_bzero | Validated full `len`, then capped to SYS_RW_MAX + returned 0 (success) → silent secret retention in a scrub primitive; violated the libthyla-rs -1-on-oversize contract | **FIXED** `@01e1b40` (reject `len > SYS_RW_MAX`). Dormant (call sites <4096) |
| HT03.R3-F1 | perm | S | **P2** | `kernel/perm.c` | OEXEC validated `PERM_X` but minted a `RIGHT_READ` handle (grant > check, violating perm.h:47) — a masking-bug stack; masked only by dev9p forwarding omode&3=3 (server-rejected); the obvious "fix" would unmask read-any-`--x` | **FIXED** `@01e1b40` (`perm_want_for_omode(OEXEC)=PERM_R\|PERM_X`). Regression `perm.oexec_no_read_leak` (non-vacuous) |
| HT03.R4-F1 | ABI/userspace | S | **P2** | `usr/lib/libthyla-rs/src/err.rs` | `from_syscall_return(-1)` → `NotPermitted` → every flat-error native failure decoded as EPERM (the native mirror of the T_E_PERM=1 trap) | **FIXED** `@01e1b40` (`-1` → `Error::Io`, pouch-consistent). Systemic fix = R4-F7 re-vote |
| HT03.R2-F2 | syscall | S | P3 | `kernel/syscall.c:2469` | `sys_thread_exit` `__builtin_unreachable` where the dispatch case has no trailing return → UB fall-through to SYS_NOTE_OPEN | **FIXED** `@01e1b40` (extinction backstop, matching EXITS/EXIT_GROUP) |
| HT03.R1-F3 / R4-F2 | ERRORS.md | C | P3 | `docs/ERRORS.md` | Multi-thread-fault carve-out described DEAD behavior (claimed kernel extincts; RW-1 C-F1 @2891bf2 retired it); missing T_E_CANCELED row; snare:bus no-emitter; asserts→rejects | **FIXED** `@01e1b40` (both reviewers caught the carve-out independently) |
| HT03.R1-F2 | exception entry | C | H3 | `arch/arm64/exception.c` | `snare:fpe` never posted — EL0 FP traps fall to default → snare:ill (benign, wrong tag) | **REGISTERED** v1.x (the ERRORS.md F6 gap) |
| HT03.R4-F7 | errno ABI | T | H3 | kernel-wide | The flat-`-1` rollout now has TWO degrading consumers (pouch EIO + the R4-F1 native alias); "no retrospective `-T_E_*` sweep" deserves a re-vote | **REGISTERED** — user decision (the systemic fix behind R4-F1) |
| HT03.R4-F3 | ABI asserts | C | H4 | `lib.rs`/`metadata.rs` | Identity-bearing adjacent-u32 pairs (TSpawnArgs principal/gid; TSrvPeerInfo) have size-only asserts → a silent transposition is privilege-relevant (latent) | **REGISTERED** RW-8/RW-10 (add offset asserts) |
| HT03.R4-F4/F5 / R2-F3 / OBS-1 / OBS-2 | doc | C | H4 | various | Mirror hygiene (libt enum gap, t_poll annotation, lib.rs mask comment); patch-0006 prose; stale burrow comment; userland_enter noreturn decl; rand.rs BadAddress doc | **REGISTERED** (doc follow-up) |
| HT03.R1-H4 / R2-H4 | SOTA | T/P | H4 | entry + syscall | word-granular `copy_*_user`; sorted+bsearch fixup; spawn-entry convergence; per-Proc rlimits | **REGISTERED** RW-11/RW-12 |

**RW-3 verified SOUND (do not re-prosecute without new code):** the #713 eret-window (3 EL0-eret sites, all DAIF-masked before `msr elr_el1`; KERNEL_EXIT always IRQ-masked; census complete); `el0_return_die_check` on every return-to-EL0 path (sync FAULT_HANDLED + syscall tails, IRQ-from-EL0 tail, now userland_enter first-entry); the uaccess fixup (PC-keyed + conservative) + the disproven alignment-DoS (SCTLR.A=0 system-wide; handlers validate align anyway); I-13 GPR/SP/TPIDR/SPSR/x30 leak sweeps; #806 re-entrancy guard; demand_page_locked under vma_lock (W^X at fault time); cpu_switch_context full save/restore + ASID pre-hook ordering; SPSel-never-cleared (0x000/0x080 unreachable); I-2 elevation-only strip (A-1a hole stays closed); I-5/I-6 spawn-fd KOBJ_SPOOR-only + DUP subset; #844 handle ref balance on every traced exit; SYS_NOTED kernel-captured restore + in_handler/N-3 guards; Loom syscall boundary + I-30 pin; TORPOR_WAIT register-then-observe; I-28 containment (`..`-clamp + per-component X-search); the two-axis rights+perm gate (no bypass except R3-F1, fixed); I-19 snare-prefix reject; the four-surface ABI numbering map (68 live, zero collisions, no `-T_E_PERM` returned, 17 errno pins POSIX-correct, t_stat 80B everywhere). **0 P0 across the whole surface; round-2 clean on the fixes.**

## RW-4 — Namespace/FS + 9P + Loom (DELTA) (STANDARD +DELTA Loom). Reviewers: 4× Fable-max `holotype-reviewer` (R1 namespace/resolver, R2 FS dev drivers, R3 9P stack, R4 Loom-DELTA; all `claude-fable-5`) + Opus self-audit + a **dirty-close round-2** (R-A on the ns_lock surgery, R-B on the 3 wait/wake fixes). Report: `docs/holotype/04-namespace-fs-9p-loom.md`. Closed list: `audit_holotype_rw4_closed_list.md`. Status: CLOSED `@ee30f55`+`@6cf5933`. Headline: SA-F1 — the per-Territory mount table was unlocked (a multi-thread-Proc UAF); the #848 race, promoted P3-dormant → P1 by the P6 lift. The systemic twin of RW-2.

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT04.SA-F1 | namespace | S | **P1** | `kernel/territory.c` + `.h`, `stalk.c`, `syscall.c` (6 sites) | Per-Territory `mounts[]`/`nmounts`/`binds[]`/`root_spoor` had NO lock (only `dot_lock` for the cwd string). Peer Threads share the Territory (RFNAMEG too); `SYS_MOUNT/CHROOT/PIVOT_ROOT` have no cap gate → an unprivileged pthread program racing `open(FROM_ROOT)` vs `chroot/pivot/unmount` = `root_spoor`/mount-source UAF + torn `mounts[]` RMW. The `#844` ref sat INSIDE the read-then-ref window | **FIXED** `@6cf5933` (`spin_lock_t ns_lock`; `mount_lookup` borrow→ref; `territory_root_ref`; never held across stalk/clunk). Opus self-audit; **R1 rated it "dormant", OVERRULED to P1** (R2-F1 is the same class @P1; sound-vs-any-EL0) |
| HT04.R2-F1 | srvconn | S | **P1** | `kernel/srvconn.c` + `.h` | Byte-mode `/srv` blocking-recv single-waiter `Rendez` extincts on a 2nd concurrent reader — the 9P-mode `p9_client.lock` serializer is absent on the P6-pouch-sockets byte-mode userspace `read()` path; peer Threads share the fd | **FIXED** `@ee30f55` (per-`chan` `reading` busy-guard, the devcons pattern; 2nd reader → -1). Empirically latent (SMP gate green) |
| HT04.R3-F1 | 9P client | S | **P2** | `kernel/9p_client.c` | An owned reply that passes framing but fails the type/parse check makes `dispatch_rmsg` return <0 BEFORE `clear_outstanding` → `outstanding[tag]` leak (≤64 → session wedge) + a later reply on the tag mutates fid state ownerlessly; not fail-closed (asymmetric w/ #841-F5) | **FIXED** `@ee30f55` (`mark_dead` on `drc<0`, both sync + async paths). **Round-2 R-B-F1 caught it OVER-BROAD → re-fixed (below)** |
| HT04.R4-F1 | Loom | S | **P2** | `kernel/loom.c` | `loom_wait_for_completions` drove rearm/admit/pump even on an SQPOLL ring → wrote `l->chain`/`cq_tail` under `l->lock` while the parked kthread reads them lock-free → data race; falsified the Loom-5 F4 "sole admitter" disposition | **FIXED** `@ee30f55` (early `if (l->sqpoll)` sample-and-wait branch; non-SQPOLL byte-identical) |
| HT04.R-B-F1 | 9P session | S | **P2** | `kernel/9p_session.c` | **The dirty-close catch**: R3-F1's "drc<0 == protocol violation" is FALSE for the TWALK `fid_bind`-full leg (a LOCAL 256-fid exhaustion) → `mark_dead` takes the whole shared root-FS session down on the 257th concurrent fid — worse than the round-1 leak it closed | **FIXED** (this commit): `send_walk` fid-capacity pre-check + the dispatch `fid_bind`-full surfaces as a synthetic per-op error (`is_error`+EIO, reusing the Rlerror path; drc=0 → no latch). Regression `9p_session.walk_fid_full_no_latch` |
| HT04.R1-F1 | resolver | S | P3 | `kernel/stalk.c` | `clone_walk_zero` accepted a 0-walk without asserting `nqid==0` (parity gap vs the main loop's `nqid!=1`) | **FIXED** `@ee30f55` (`\|\| w->nqid != 0`) |
| HT04.R2-F2 | dev/lseek | S/C | P3 | `dev.h`, `syscall.c`, devramfs/dev9p | `#957`/A-4b gave devsrv/devproc a `.stat_native` for fstat → the `stat_native!=NULL` lseek heuristic regressed lseek to succeed on an ignored offset | **FIXED** `@ee30f55` (explicit `dev->seekable` flag; devramfs+dev9p only) |
| HT04.R-A-F1 | docs | C | **P2** | `18-territory.md`, `104-stalk.md` | The binding tech reference taught the PRE-fix contract (borrowed `mount_lookup` + "No per-Territory mount-table lock... Phase-5+") → a future caller leaks a ref OR adds a lockless reader (tech-ref outranks code) | **FIXED** (this commit) — ns_lock discipline + ref-held contract documented |
| HT04.R-A-F2 | comment | S | P3 | `syscall.c:1411` | "src is NOT spoor_ref'd here" now contradicts both ref-held arms → an editor deletes a "spurious" clunk → extinction | **FIXED** (this commit) |
| HT04.R-A-F3 | hygiene | S | P3 | `joey.c:222` | The tree's only bare `root_spoor` read (kproc boot, single-threaded → safe, but outside the discipline) | **FIXED** (this commit, → `territory_root_ref`) |
| HT04.R3-F2 | 9P client | S/T | H3 | `9p_client.c` | The monotonic fid allocator never reclaims clunked fids → ~1 day to 2^32 burn on the durable Stratum mount (fail-safe -EIO) | **REGISTERED** (#23) — v1.x devmnt fid free-list + doc-fix |
| HT04.R3-F3 | 9P session | C | H3 | `9p_session.c` + ARCH §21.5 | ARCH commits "block until a slot frees"; as-built `alloc_tag` returns -1 → -EIO (clean-fail). v1.0 never hits 64 in-flight; A-5b gives the 65th a spurious -EIO | **REGISTERED** (#23) — reconcile scripture-vs-impl (user/scripture) |
| HT04.R3-F4 | 9P | C/T | H4 | `9p_client.c` | Partial walks → -EIO instead of Plan 9 last-bound-qid (documented caveat #3; benign) | **REGISTERED** (documented) |
| HT04.R4-F2 | Loom | C | P3 | `loom.c` | SQPOLL back-pressure: a multishot/chain op in pure busy-poll reap (no ENTER) strands until next submit (a CQ reap doesn't wake the parked kthread) | **REGISTERED** (#23) — Known-caveats doc + v1.x reap-helper-wake |
| HT04.R4-F3 | Loom | C | P3 | `loom.c` | A multi-client Loom ring pumps only the first non-terminal op's client → a slow first client starves later clients | **REGISTERED** (#23) — v1.x round-robin |
| HT04.R4-F4 | Loom | T | **H2** | `loom.c` | io_uring SOTA: an enforced `LOOM_SETUP_SINGLE_ISSUER` + a CQ overflow list retire the over-admit residual + R4-F1 BY CONSTRUCTION | **REGISTERED** (#23) — SURFACED, pre-rc |

## RW-5 — Authority (handles/caps/legate/perm/identity) (STANDARD). Reviewers: 4× Fable-max `holotype-reviewer` (R1 handle table #844-DELTA, R2 caps/legate/identity, R3 the `cap` device, R4 perm.c + /proc kill; all `claude-fable-5`, MODEL start==end on all four) + Opus self-audit. Report: `docs/holotype/05-authority.md`. Closed list: `audit_holotype_rw5_closed_list.md`. Status: CLOSED. Headline: the recurrent multi-thread-lift theme again — A-4a atomized one `p->caps` writer (`proc_become_legate`) and missed the other writer + the readers. NOT a dirty close (0 P0 / 0 P1 / 1 P2 / 8 P3; fixes local).

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT05.F1 | devcap caps | S | **P2** | `kernel/devcap.c:327` | Hostowner redeem `writer->caps \|= to_or` is a non-atomic RMW racing `proc_become_legate`'s `__atomic_fetch_or`. Runs AFTER the one-shot consume frees the grant slot + AFTER the lock release → corvus re-registers a CLEARANCE grant (same stripes), a SIBLING thread redeems it (atomic OR), the resumed plain STORE clobbers it → lost cap. Fail-safe (cap lost, never spuriously set → P2 not P1, contrast a4c2 F1's I-27 break). Triple-converged (R2-F1 = R3-F1 = self-SA-1) | **FIXED** — `__atomic_fetch_or(ACQ_REL)`, matching the clearance path |
| HT05.F2 | caps readers | S | P3 | `perm.c:27/84/88`, `devproc.c:488`, `proc.c:1086` | Plain `p->caps` reads on the cap gates race the legate atomic OR (C11-UB / TSan; benign on aligned ARM64 — single-copy-atomic, fail-safe stale=deny). R3 rated readers P2; ADJUDICATED to P3 (R2) — the readers cannot lose anything, only the F1 writer can | **FIXED** — `__atomic_load_n(ACQUIRE)` at all 5 sites |
| HT05.F3 / R4-F1 | perm.c | S | P3 | `kernel/perm.c:39` | `perm_check(p,st,0)` returns 0 (ALLOW): `(bits & 0)==0` vacuously true, before the cap-bypass. Unreachable (8 sites pass non-zero want) but fail-OPEN polarity | **FIXED** — deny `want==0` before the DAC-override. Regression `perm.check_want_zero_denied` |
| HT05.F4 / R4-F2 | perm.c | S/C | P3 | `kernel/perm.c:79` | `perm_wstat_check` gates only {MODE,UID,GID}; a future `T_WSTAT_*` bit passes ungated | **FIXED** — reject `valid & ~T_WSTAT_VALID`. Regression `perm.wstat_rejects_unknown_valid_bit` |
| HT05.F5 / SA-2 | caps invariant | C | P3 | `caps.h`/`devcap.h` | No `_Static_assert` pinned `CAP_GRANTABLE_CLEARANCE ⊆ CAP_ELEVATION_ONLY` (nor CAP_GRANTABLE) — the load-bearing I-25 "members stay unelevated" guarantee; R2 verified the value by hand. A future fork-grantable clearance cap breaks I-25 silently | **FIXED** — both asserts added in `devcap.h` |
| HT05.F6 / R1-F1 | handle.c | S | P3 | `kernel/handle.c:256` | KOBJ_SRV `handle_acquire_obj` comment "never reached" is false since #844 (`handle_get` reaches it). No-op balanced only because post-stalk-3c KObj_Srv is SERVICE-only; a SrvConn-backed one would be an acquire-noop/release-unref UAF | **FIXED** — comment corrected to name the latent asymmetry |
| HT05.F7 / R1-F2 | handle.c | C | P3 | `kernel/handle.c:390` | `handle_alloc` guards the consumed-ref convention for KOBJ_BURROW but not KOBJ_LOOM (defense-in-depth gap; dormant) | **FIXED** — KOBJ_LOOM refcount>0 guard arm added |
| HT05.F8 / R3-F3 | dev.c | C | P3 | `kernel/dev.c:124` | devcap registration comment says `dc='C'`; it is `'k'` | **FIXED** — comment corrected |
| HT05.F9 / R3-F4 | syscall.c | C | P3 | `kernel/syscall.c:209/251` | "no cross-thread writer of p->caps" is now false (`proc_become_legate`) — the rationale that left F2's readers looking safe | **FIXED** — comment corrected |
| HT05.R2-F3 | legate | S | P3 | `proc.c` proc_become_legate | A 2nd clearance redeem rewrites `valid_until`/`scope_id` as an uncoordinated scalar pair (deeply benign) | **REGISTERED** — folds under the deferred a4a-F2 double-redeem tidiness |
| HT05.T-1 | caps | T | H3 | kernel-wide | A `proc_caps_load()/proc_caps_or()` accessor sweep would close the mixed-atomicity class structurally (mirrors RW-4's mounts[]/root_spoor sweep) | **REGISTERED** (#23-class) — the F1/F2 fixes close the known sites; the sweep makes it structural |
| HT05.SA-h | caps test | C/T | H | (test coverage) | No deterministic two-thread-same-Proc redeem-vs-`perm_check` TSan harness (the F1/F2 witness); shared with the #844/#841 multi-in-flight debt | **TRACKED** — lands with the owed multi-thread harness |
| HT05.H4 | devproc | C | H3 | `devproc.c` | a4b deferrals (SYSTEM-owns-SYSTEM owner-axis breadth, PID-1 unkillability, stat_native lockless window) re-checked + still correctly scoped to the future /proc-mount chunk | **REGISTERED** (documented) — live only once /proc is bound into a territory |

**RW-5 verified SOUND (do not re-prosecute without new code):** the handle-table #844 set + the 3 deltas (KOBJ_LOOM get/put balance, NoSrvSpoorDup, the #926 at-exit lockless-teardown premise — thread_count==1 stable, no foreign-ALIVE-Proc table access, no `/proc/<pid>/fd` yet); every under-lock acquire non-blocking (loom_ref bare atomic, burrow_ref the one acyclic lock-under-lock), releases outside the lock; the I-2 elevation-only strip unconditional on every fork/spawn; cap-mask bounding airtight + `CAP_GRANTABLE_CLEARANCE ⊆ CAP_ELEVATION_ONLY` (now compile-pinned) so scope members stay unelevated; legate teardown on every death path + valid_until expiry, both triggers acyclic; no EL0-reachable ALIVE Proc has principal_id 0 (rfork copies principal before publication, both under g_proc_table_lock; apply_identity extinctions on the sentinels; kproc stamped SYSTEM) → owner-axis (perm_check + devproc kill) sound; spawn_identity_value_ok rejects INVALID+SYSTEM on principal/primary_gid/every supp_gid; peer_snapshot leaks only scalar values under the lock; the devcap grant/redeem single-locked-lookup TOCTOU-free, one-shot consume, cross-stripes reject, self_restriction subset-only, console gate on hostowner, valid_until saturating add, cap_proc_exit_notify complete, leaf-ref aux lifecycle; perm.c owner-first POSIX + the DAC-override (HOSTOWNER|DAC_OVERRIDE) + perm_wstat (chmod owner|HOSTOWNER, chown HOSTOWNER|CHOWN no-give-away, chgrp owner&member|chown-any) + proc_in_group OOB-safe + perm_want/rights coherence (OEXEC fix held); devproc_kill_authorized two-axis DIRECT (DAC_OVERRIDE not a kill axis; I-26), kproc double-guarded, walk under g_proc_table_lock, parse_ctl_verb bounded; every proc_flags RMW atomic (A-4c2 F1 held). **0 P0 across the whole surface; 1 P2 + 8 P3 all fixed; NOT a dirty close.**

## RW-6 — Services: corvus (FULL) + joey/login (STANDARD). Reviewers: 4× Fable-max `holotype-reviewer` (R1 crypto core `corvus-crypto`, R2 corvus protocol/session/auth, R3 corvus identity/clearance DB + legate bridge, R4 joey/login; all `claude-fable-5`, MODEL start==end on all four — no fallback) + Opus self-audit (crypto core + session/auth, double-covered). Report: `docs/holotype/06-services.md`. Closed list: `audit_holotype_rw6_closed_list.md`. Status: CLOSED. Headline: a mature, thrice-audited surface (a5c/828/a1b) under a fresh FULL pass — no P0/P1; the secret-handling + auth spine is sound. The 5 P2 are all localized hardening (parser cost-envelope, a live-requery gate, a missing-guard mirror, secret-scrub-on-error-leg). NOT a dirty close (P1+P2=5<6; no invasive restructure). 1 P2 (AUTH rate-limit) deferred to #876 (v1.0-unreachable).

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT06.F1 / R1-F1+SA-3 | crypto core | S | **P2** | `corvus-crypto/lib.rs` `crvs_v1_unpack` | On-disk Argon2 cost is unbounded (only the m64→u32 fit is checked). A tampered/bit-rotted header (`t_cost=u32::MAX`) wedges the single-threaded daemon on a ~4e9-pass KDF, or `m_cost`>heap OOM-aborts it — downgrading a graceful per-user BadAuth into a whole-daemon DoS (all users + console + system identity). Converged R1=self; self had P3, R1's all-users + OOM impact escalated to P2 | **FIXED** — envelope ceilings (`ARGON2_MAX_*`) in the shared parser, fail-closed. Regression `crvs_unpack_rejects_out_of_envelope_cost` |
| HT06.F2 / R2-F2 | session/auth | S | **P2** | `corvus/main.rs` `handle_admin_elevate` + `handle_recover_system` | The two privileged console-gated verbs gate on the CACHED `conn.peer.console`, not a per-request `SYS_SRV_PEER` (C-22). The "console is immutable" comment is FALSE — `proc_revoke_console_attached` flips it on a live Proc; every sibling verb re-queries live. ADMIN_ELEVATE is kernel-backstopped at `/cap/use`; RECOVER(system)'s only barrier is then the phrase → P2 | **FIXED** — `peer_live_info` helper; both re-query live, fail-closed; the false comment corrected. E2E-validated (ADMIN_ELEVATE + RECOVER(system) ok in the boot log) |
| HT06.F3 / R3-F1 | identity DB | S/C | **P2** | `corvus/main.rs` `handle_user_create` | a1b-F1 SIBLING: `groups_count()` counts standalone groups AND per-user UPGs; a1b capped `handle_group_create` but the UPG side-writer was unguarded → 512 standalone groups + 1 user persists group_count 513 > parse cap 512 → next-boot FATAL boot-brick (CAP_HOSTOWNER-gated → availability) | **FIXED** — mirror the `groups_count() >= 2*MAX_USERS` guard before the UPG push |
| HT06.F4 / R4-F1 | login | S | **P2** | `login/main.rs:887` + `:496` | #828 A-F1 extension: the cleartext passphrase (connect-fail leg) and the session token (provision-dek open-fail leg) drop unscrubbed — the `bzero`s sit after the early returns. Low blast radius (single-thread, immediate exit, KP_ZERO) but the exact A-F1 C-5 invariant, passphrase on the most-reachable leg | **FIXED** — scrub `pass` + `pl` before both early returns |
| HT06.F5 / R2-F1 | session/auth | S | **P2** | `corvus/main.rs` `handle_auth` | AUTH runs the 16 MiB argon2id KDF with NO rate-limit (C-16 unenforced on AUTH; only RECOVER has the in-RAM counter) → argon2id-flood DoS | **DEFERRED → #876** — v1.0-UNREACHABLE (`session_active()` blocks AUTH while a session is live; the only no-session windows hold only trusted Procs under the single-session model; activates with the multi-peer surface). The usable fix needs the time-decay that IS #876's scope; a naive lock-until-restart counter would regress login UX. #876 entry strengthened |
| HT06.F6 / R1-F2+SA-4 | crypto core | S | P3 | `corvus-crypto/lib.rs:220` | `generate_hybrid_keypair`'s length-guard early-return drops the ML-KEM dk byte copy unwiped (unreachable; sizes type-fixed) | **FIXED** — wipe dk/ek before `return None` |
| HT06.F7 / R1-F4 | crypto core | S | P3 | `corvus-crypto/lib.rs:179` | `aegis_wrap`'s `debug_assert_eq!` length-guard → release-mode `copy_from_slice` panic; pub-fn contract foot-gun | **FIXED** — hard `assert_eq!` (release-enforced contract) |
| HT06.F8 / self-SA-2 | crypto core | S | P3 | `corvus-crypto/lib.rs` `aegis_unwrap` | `buf` dropped unwiped on the tag-failure path; a correct-key/corrupt-tag case (tag-byte bit-rot/tamper) leaves real plaintext in it. R1 dismissed as benign (wrong-key→garbage); the self-audit kept the narrow correct-key case | **FIXED** — `wipe(&mut buf)` on the `Err` arm |
| HT06.F9 / R1-F3 | crypto core | S | P3 | `argon2id_kek` / `sha256_kek` | KEK residue in the SHA-256 finalize state + the Argon2 scratch matrix (sha2/argon2 lack zeroize-on-drop) | **DOC** — upstream-bounded (no in-tree fix without swapping crates); mlock+DONTDUMP (C-2) closes the disk/coredump leak. Documented in the fn headers |
| HT06.F10 / R2-F3+SA-5 | session/auth | S | P3 | `corvus/main.rs` drain/reset + `in_buf.drain` | `pending_response.clear()` (token/DEK residue) + the consumed `in_buf` prefix (passphrase frame) leave secrets in Vec capacity. Mitigated by C-2 mlock+DONTDUMP | **FIXED** — `wipe` before each clear + zero the consumed `in_buf` prefix |
| HT06.F11 / R2-F5+R3-F2 | session/auth | S | P3 | `corvus/main.rs` `recover_fail_inc` | RECOVER_FAILS cap = MAX_USERS, but the subject universe is MAX_USERS users + the fixed system subject → the system subject can be dropped → RECOVER(system) un-rate-limited. Converged R2=R3 | **FIXED** — cap MAX_USERS+1 |
| HT06.F12 / R3-F4 | identity DB | C | P3 | `corvus/main.rs:1283` | clearance.db header lacks the identity.db-style layout-drift `_Static_assert` | **FIXED** — `assert!(CLEARANCE_DB_HEADER_LEN == 12)` |
| HT06.F13 / R3-F5 | identity DB | S/C | P3 | `corvus/main.rs` `identity_db_parse` | parser accepts any `backend` byte the writer never emits (inert today; forward hazard if AUTH dispatches on backend) | **FIXED** — reject `backend != BACKEND_ID_PASSPHRASE` (writer==parser domain) |
| HT06.F14 / R4-F2 | joey getty | S | P3 | `joey.c` `session_getty_loop` | Zero-backoff respawn → a wedged `/dev/cons` or down-corvus busy-spins init; R4 refuted the "needs a sleep primitive (v1.x)" deferral (`t_torpor_wait` is already a pacer here) | **FIXED** — 250 ms `t_torpor_wait` backoff between respawns |
| HT06.F15 / R4-F3 | joey init | S | P3 | `joey.c:3317` | Post-BOOT_COMPLETE `return 1` on console-relinquish failure KILLS init (unreachable today; wrong failure mode) | **FIXED** — log + persist-reap (the getty would break I-27 on that leg, so not the getty) |
| HT06.F16 / R1-note | crypto core | C | P3 | `corvus-crypto/lib.rs` wordlist | wordlist len is compile-asserted but not sorted/unique (binary_search correctness) | **FIXED** — const `bip39_wordlist_strictly_sorted()` compile assert |
| HT06.F17 / R2-F4 | session/auth | C | P3 | `corvus/main.rs` `try_dispatch_verb` | One-frame-per-Twrite mis-pairs responses if a client writes 2 frames in 1 Twrite (self-inflicted; NO security desync — every frame fully gated, R2 verified) | **TRACKED** — behavior-changing (drain-all / strict request-response); v1.x hardening |
| HT06.F18 / R3-F3 | identity DB | S | P3 | `corvus/main.rs` `handle_user_create` bootstrap | The unprivileged first-USER_CREATE bootstrap (count==0) is re-enterable via absent identity.db (delete) or all-wraps-dropped; a1b's fail-closed is narrower than claimed. Storage-trust-gated (I-14/A-1.7) + confers NO hostowner (ADMIN_ELEVATE needs the system passphrase) | **DOC+TRACK** — robust fix (provisioned marker / users-subtree check) is a deliberate design choice; documented + surfaced to user |
| HT06.F19 / R1-note | crypto core | C | P3 | `build_*_ad` | AD does not bind `CORVUS_STATE_VERSION` (not a v1.0 break — version rejected at parse, only v1 exists) | **TRACKED** — fold the version into the AD before any v2 wrap format ships (v1.x forward-compat) |
| HT06.F20 / R4-F1-sib | login | S | P3 | `login/main.rs` `token` | The stack `token: [u8;33]` is scrubbed only on success/logout; ~7 error legs leave it on the stack (kernel-zeroed at proc teardown → ~nil risk) | **DOC (accepted)** — C-5 consistency; the heap legs (F4) are the higher-value ones, fixed |

## RW-7 — Drivers/HW + Halls of Extinction (STANDARD, DELTA-eligible where W2/W3/HX-2/A-4c were already audited). Reviewers: round-1 4× Fable-max `holotype-reviewer` (R1 gic/timer/irqfwd, R2 uart/cons, R3 virtio/mmio/dma, R4 chacha/random/halls; all `claude-fable-5`, MODEL start==end, NO fallback even on R4 crypto) + Opus self-audit double-covering R3+R4+R1-irqfwd; round-2 (dirty close) 2× Fable (A irqfwd+virtio, B console) + self; round-3 1× Fable (virtio F1/F2 fixes) + self. Report: `docs/holotype/07-drivers-hw.md`. Closed list: `audit_holotype_rw7_closed_list.md`. Status: CLOSED. Headline: the recurrent P6-multi-thread-lift theme a FOURTH time — the P6 lift, LS-5, and Lazarus each layered onto the driver/HW substrate without re-examining the death paths / multi-thread wait-wake / the cons→notes→SAK seam, and ALL FOUR P1s lived exactly there (none in the crypto or GICv2/vtimer mechanics; R4 = 0 P0/P1/P2). Dirty close (round-1 3 P1 + 3 P2) → round-2 found a 4th P1 (the R3-F1 quiesce was incomplete: the handle-only walk missed map-then-close-fd MMIO devices) + a P2 (the in-range reset raced the kernel RNG slot) → round-3 on those fixes converged clean.

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT07.R1-F1 | irqfwd | S | **P1** | `kernel/irqfwd.c` `kobj_irq_wait` | The single-waiter `Rendez` extincts the kernel on a 2nd concurrent waiter; a `KObj_IRQ` is shared across a multi-thread Proc's peer Threads (stratumd drives virtio-blk IRQs via SYS_IRQ_WAIT) → two Threads on one irq fd → `sleep()`'s single-waiter `extinction` = CAP_HW_CREATE-gated unprivileged DoS. Converged w/ self-audit SA-R1-1 | **FIXED** `@0c0e484` — per-KObj_IRQ `waiting` busy-guard under `k->rendez.lock`; 2nd waiter → `KOBJ_IRQ_WAIT_BUSY` → -1. Regression `irqfwd.second_waiter_refused` |
| HT07.R1-F2 | irqfwd | S | **P2** | `kernel/irqfwd.c` `kobj_irq_free_internal` | The GIC slot holds a raw non-refcounted `arg=k`; `free_internal` (after gic_disable) races an in-flight `kobj_irq_dispatch` on another CPU that touches `*k` after the lock release (wakeup's re-lock) → UAF. Converged w/ self-audit SA-R1-2 | **FIXED** `@0c0e484` — `dying` + `in_dispatch` flags; free sets `dying` then spins until `!in_dispatch` (dispatch's last k-touch, under the same lock) before kfree |
| HT07.R3-F1 | virtio | S | **P1** | `kernel/virtio.c` + `kernel/proc.c` `proc_quiesce_owned_devices` | DMA-into-freed-buddy-pages on ABNORMAL driver death (_Exit): a CAP_HW_CREATE driver (stratumd virtio-blk) dies armed → reaper frees KObj_DMA pages → in-flight used-ring completion DMAs into recycled memory = silent cross-Proc corruption (I-7 device-stop unmet on death). Self-audit MISSED the death path; R3 caught it (the two-prosecutor value) | **FIXED** `@3ec134e` + the round-2 F1 VMA-walk completion — proc_quiesce resets every probed virtio transport in the dying Proc's KObj_MMIO ranges (handle table AND VMA mappings) before the pages free, via the kernel `.base` map. Regressions `virtio.proc_death_quiesces_device` + `..._vma_only_device`. Structural close = per-device KObj_VIRTIO_DEV (R3-F8, P5+) |
| HT07.R3-F2 | virtio | S/C | **P2** | `kernel/virtio.c` `virtio_virtqueue_destroy` | QUEUE_READY=0 with no readback + an over-claiming comment ("device doesn't continue posting"). Converged w/ self-audit SA-R3-2 | **FIXED** `@3ec134e` — QUEUE_READY readback (VIRTIO 1.2 §4.2.3.2 sync point) + honest comment (only a full reset drains an in-flight DMA; the two page-freeing callers reset first) |
| HT07.R3-F6 | virtio | S/C | P3 | `kernel/virtio.c` `virtio_virtqueue_create` | No pow2 validation of QueueNumMax; the split-ring index masks with `size`, undefined for non-pow2 | **FIXED** `@3ec134e` — `virtio_vq_size_for` fails closed on zero/non-pow2 + 3 `_Static_assert`s pin VIRTIO_VQ_NUM_DEFAULT (pow2, desc 16*N + used 6+8*N each ≤ one page). Regression `virtio.vq_size_for` |
| HT07.R2-F1 | console SAK | S | **P1** | `kernel/proc.c` `proc_console_sak` | SAK set `g_console_owner = trusted`, conflating the OWNER (Ctrl-C target) and ATTACH (elevation authority) roles LS-5 made distinct → a Ctrl-C AFTER a SAK posted `interrupt` to corvus (non-self-managing) → its terminate latch armed → the trusted login authority DIED until reboot (unprivileged Ctrl-C kills the trusted path) | **FIXED** `@2608c88` — SAK grants corvus only the ATTACH, leaves the OWNER NULL (re-established when login spawns the session shell, SPAWN_PERM_CONSOLE_OWNER). Regression `cons.sak_does_not_terminate_trusted` (+ positive latch control). Round-2 B verified all 14 soundness properties hold |
| HT07.R2-F2 | console SAK | S | **P2** | `kernel/proc.c` `proc_console_sak` | The SAK's `interrupt` courtesy-post to the OLD owner now TERMINATES a non-self-managing owner (joey during bringup → init dies) since LS-5 made `interrupt` load-bearing | **FIXED** `@2608c88` — drop the courtesy post (no consumer); the attach-bit revoke is the SAK's observable effect. Dedicated `hangup`/`console-revoked` note = R2-F3 v1.x SEAM |
| HT07.R4-F1 | halls | C | P3 | `arch/arm64/halls.c` + `halls.h` + `exception.c` | The per-CPU frame-slot comment "handlers do not migrate CPUs mid-execution" is FALSE for a blocking EL0 syscall (resumes on another CPU → leave runs there, stranding/clobbering the slot). STILL SAFE (HX-I4 gates every read → capture-current) but the false comment could lead a maintainer to delete the HX-I4 gate (the stale-load-bearing-comment class) | **FIXED** (RW-7 close) — corrected all 3 sites crediting HX-I4 for BOTH the noreturn-skip AND the migration-stranding cases |
| HT07.RND2-A-F1 | virtio quiesce | S | **P1** | `kernel/proc.c` `proc_quiesce_owned_devices` | The round-1 R3-F1 fix walked ONLY p->handles; SYS_MMIO_MAP transfers the KObj_MMIO ref to a BURROW_TYPE_MMIO VMA and a driver can CLOSE the fd (idiomatic mmap-then-close) → device reachable ONLY via the VMA → handle-only walk MISSES it → corruption REOPENED. The round-1 regression kept the fd open (false confidence) | **FIXED** (RW-7 close) — walk BOTH the handle table AND p->vmas (BURROW_TYPE_MMIO → reset b->kobj_mmio->pa/size), before vma_drain; the VMA walk is no longer gated on p->handles. Regression `virtio.proc_death_quiesces_vma_only_device` (map-then-close-fd). Round-3 prosecuted |
| HT07.RND2-A-F2 | virtio | S | **P2** | `kernel/virtio.c` `virtio_mmio_reset_in_range` | The in-range reset hits the kernel-driven virtio-rng slot (co-paged, deliberately un-reserved; a blk driver in the rng's page legitimately claims it = the LIVE config) WITHOUT g_rng_dev_lock → races random.c's in-flight pull. Round-2 A escalated the Opus self-audit SA-R2-1 from P3 (converged) | **FIXED** (RW-7 close) — `device_id == VIRTIO_DEVICE_ID_RNG` → skip (the rng is kernel-owned, never a dying driver's device). Round-3 prosecuted |
| HT07.RND2-A-F3 | irqfwd | C | P3 | `kernel/irqfwd.c` | `KOBJ_IRQ_WAIT_BUSY` (0xFFFFFFFF) collides with a legitimate maximal `pending_count` (≈4e9 un-drained fires; fail-safe + documented, cosmetic) | **FIXED** (RW-7 close) — saturate `pending_count` at 0xFFFFFFFE so the sentinel value-space is disjoint |
| HT07.RND2-B-F1 | console docs | C | P3 | `proc.h:818-826` + 6 sibling sites (`joey.c`, `cons.c`, `proc.c`, `test_cons.c`) | Stale binding contracts still describe the PRE-fix SAK (post a note, owner=trusted, idempotent-if-trusted-owns) — on the tree's most re-broken lineage a maintainer could "restore" the R2-F1 bug | **FIXED** (RW-7 close) — doc-sweep all 7 sites to the as-built semantics (attach-only, owner NULL, no note) |
| HT07.RND2-B-F2 | console_mgr | S | P3 | `kernel/cons.c:142-154` | The fixed intr-then-sak batch order re-synthesizes the init-death R2-F2 removed: a coalesced BREAK+Ctrl-C posts `interrupt` to the PRE-SAK owner (joey → init dies). Pre-existing, operator-only, boot-window | **FIXED** (RW-7 close) — a SAK SUPERSEDES a coalesced Ctrl-C (`if do_sak sak; else if do_intr intr`); post-SAK owner is NULL so an after-BREAK Ctrl-C is correctly dropped |
| HT07.RND2-B-F3 | bringup | S | P3 | `joey.c:114` + `proc.c:906` + `notes.c:227` | A bare Ctrl-C during the bringup window terminates init (joey is owner + handler-less + non-self-managing; LS-5 made `interrupt` lethal and the window was never re-examined). PRE-EXISTING; the SAK-synthesized + coalesced instances are closed (R2-F2 + RND2-B-F2), this DIRECT instance remains | **TRACKED → #35** — decide+document: "Ctrl-C during bringup = operator boot-abort (intended)" OR exempt pre-relinquish init (notes-fd-open / g_init_proc skip pre-BOOT_COMPLETE) |
| HT07.RND2-B-F4 | console test | C/T | P3 | `kernel/test/test_cons.c` | The production-typical first-SAK state (owner==NULL via relinquish, trusted live + UNATTACHED) — the one state the new guard could wrongly no-op — was untested; a future guard simplification dropping the attach conjunct would stay green | **FIXED** (RW-7 close) — `cons.sak_attaches_from_relinquished_state` |
| HT07.RND2-B-H1 | srv_peer | C | H3 | `kernel/syscall.c:4923` `SYS_SRV_PEER` | `out->console` is the SrvConn mint-time snapshot; RW-6 F2's "re-query live (console)" holds only as fresh as the connection (a conn minted by a then-attached Proc reports console=1 after revoke). Not unprivileged-reachable (attacker conn mints console=0; gates additionally crypto-gated) | **REGISTERED** — precision caveat on the RW-6 F2 close; the kernel `console` field is connect-time by design |
| HT07.RND2-B-H2 | devcap | C | H4 | `kernel/devcap.c:310` | SAK-revoke vs an in-flight hostowner redeem is eventually-consistent (instruction-scale window; needs a corvus-minted stripe-targeted grant already pending). Pre-existing A-4c-2 property, unchanged by the fix | **REGISTERED** — impact ~nil (the grant is stripe-targeted); v1.x linearize if it matters |
| HT07.R3-F8 | virtio | T/X | **H2** | `kernel/virtio.c` + the handle layer | The per-device KObj_VIRTIO_DEV device-session model (Genode/Fuchsia SOTA) resets a device on owner-death/last-close BY CONSTRUCTION — the structural close for R3-F1 (retires the proc-exit quiesce walk) | **REGISTERED** — Phase 5+ |
| HT07.R2-F3 | notes | T | H2 | `kernel/notes.c` (the closed name table) | A dedicated `hangup`/`console-revoked` note name (no terminate disposition) lets a foreground app distinguish SAK-revoke from Ctrl-C — the structural close for R2-F2 | **REGISTERED** — v1.x notes SEAM |
| HT07.R1-F3 | gic | T/C | H3 | `arch/arm64/gic.c:580` | GICv3 redistributor stride hardcoded 0x20000 with no GICR_TYPER.Last walk (I-15 portability gap; QEMU virt = 0x20000 so dormant) + a stale comment saying 0x40000 | **REGISTERED** (#23-class) + the stale comment fixed at the RW-7 close |
| HT07.R3-F4 | virtio | C/T | H3 | `kernel/virtio.c` `virtio_negotiate_features` | VIRTIO_F_VERSION_1 (bit 32) never acked (bank-1 negotiated as 0); fail-closed if a modern device rejects — completeness, not soundness | **REGISTERED** — the transport is structurally legacy-feature-limited; ack VERSION_1 when a driver needs >32 feature bits |
| HT07.R3-F5 | mmio | T | H3 | `kernel/mmio_handle.c` | The kernel-MMIO reservation rests on a hand-maintained DTB-compat list (board-drift: a new GIC/UART compat lands unreserved) | **REGISTERED** — a W4 board-bringup seam; a boot self-check is the cheap interim |
| HT07.R3-F7 | virtio | C | H4 | `kernel/virtio.c` `virtio_mmio_probe_cb` | Leaks an `mmu_map_mmio` on a magic mismatch (boot-bounded; a stale/odd DTB virtio slot) | **REGISTERED** — boot-bounded; free on the mismatch return when the unmap path is wired |
| HT07.R4-F2 | chacha | C/T | H3 | `kernel/chacha20.c` (test) | The partial-tail keystream branch (non-multiple-of-64 request) is prod-unexercised + untested (random.c only requests 1024=16*64) | **REGISTERED** — add a partial-size KAT (test completeness) |
| HT07.R4-F3 | chacha | C/T | H4 | `kernel/chacha20.c` (test) | The continuity test asserts inequality not a block-1 KAT + a "RFC 8439" citation conflates the DJB counter/nonce layout | **REGISTERED** — pin block-1 KAT + reword (test completeness) |
| HT07.7 | smp | T | H | `kernel/smp.c` `smp_cpu_idx_self` | The dense-Aff0 MPIDR→logical-index assumption (R1 re-noted) | **TRACKED → #7** (DTB MPIDR map) |
| HT08.R1-F1 | io (libthyla-rs) | S | **P1** | `usr/lib/libthyla-rs/src/io.rs` `BufReader::fill_buf` | `set_len(cap)` then `read()?` skipped the truncation on the inner-read Err leg → len==cap with an uninit tail, pos unchanged → the next `fill_buf` saw pos<len, skipped the refill, returned `&buf[pos..]` over uninit heap (UB + a process-local info-leak of a recycled alloc). Triple-converged R1+R2+self. LATENT (no BufReader consumer; R1's shell/login-drive-it claim WRONG — login has its own raw `read_line`) | **FIXED** (`cc79551`) — Err leg `set_len(0)`+pos=0; Ok leg `n.min(cap)`. Regression `u-test::bufreader_error_leg` |
| HT08.R5-F1 | notes (kernel) | S | **P2** | `kernel/syscall.c` `sys_noted_handler` NDFLT | a multi-thread Proc REFUSED the default-terminate (`-1`, predating #809/#811); pouch's always-installed handler bypasses the LS-5 kernel default-terminate → bootstrap NCONT-resumes → a multi-thread pouch daemon un-terminatable through the POSIX signal surface | **FIXED** (`cc79551`) — drop the live-peers refusal; `notes_noted_default` → `exits()` → the #811 cascade. Owed regression RND2-F3 (→ #46) |
| HT08.R5-F2 | build seam | S | **P2** | `tools/build.sh` | the structural anti-misroute seam check had drifted to miss 13 live retargets (0008/0009/0010 never extended it); `mmio_map(5)`/`dma_map(7)` share an arg shape → a re-vendor swap would misroute silently | **FIXED** (`cc79551`) — +13 entries + the fstat/lseek `#undef` gate |
| HT08.R5-F3 | poll (pouch) | C | **P2** | `usr/lib/pouch/patches/0015` | a POSIX-ignored negative fd (negate-to-disable / unused-slot=-1) returned POLLNVAL-ready → the kernel returned immediately, defeating the timeout → busy-spin / fatal-error read | **FIXED** (`cc79551`) — compact fd<0 OUT of the kernel array via `map[]`; invalid TAGGED still POLLNVAL. (Round-2 RND2-F1 closed the nk==0 boundary) |
| HT08.R4-F1 | Stratum bdev | S | **P2** | `(thylacine-pouch-arm)` `bdev_thylacine.c` `do_request` | avail.idx published to the device, `d->avail_idx` advanced only on full success → any error desynced the ring → retry re-published the same idx → FS server HANG; or consumed the prior request's completion → silent stale-read / mis-acked write (STM_ECORRUPT) | **FIXED** (Stratum `bf0cde0`) — latch `d->failed` (`goto io_fail`) → Stratum re-opens (re-init RESETs the device ring) |
| HT08.R4-F2 | virtio-input | S | **P2** | `usr/virtio-input/src/main.rs` | `(desc_id as u16) >= QUEUE_SIZE` truncated before the bound but indexed with the full u32 → 0x10000 → 512 KiB OOB read | **FIXED** (`cc79551`) — full-width compare |
| HT08.R4-F3 | Stratum bdev | S | **P2** | `bdev_thylacine.c` `op_fsync` + `tools/run-vm.sh` | op_fsync was a no-op + F_FLUSH unnegotiated, but the launch attached drives cache=writeback → fsync persisted nothing → host-crash lost committed writes; the "write-through" comment was false | **FIXED** (`cc79551` run-vm.sh + Stratum `bf0cde0`) — cache=writethrough makes the no-op fsync correct; F_FLUSH negotiation is the registered v1.x perf version |
| HT08.RND2-F1 | poll (pouch) | C | **P2** | `usr/lib/pouch/patches/0015` | **the dirty-close round-2 catch**: the R5-F3 compaction → nk==0 on the all-negative-fds boundary → `syscall_cp(SYS_poll, kfds, 0, timeout)` hits the kernel `nfds==0` reject (-1 → EPERM); POSIX wants ignore-all + return 0 | **FIXED** (`9db9300`) — n==0 AND nk==0 → return 0 (timeout==0) / ENOSYS (else, matching 0005 select()) |
| HT08.RND2-F2 | signals (pouch) | C | P3 | `usr/lib/pouch/patches/0007` | the bootstrap still documented the RETIRED kernel NDFLT refusal (cc79551 fixed the kernel comment + 78-pouch.md but not 0007) — the stale-cross-layer-contract class that let R5-F1 lie dormant | **FIXED** (`9db9300`) |
| HT08.RND2-F3 | notes (kernel) | C/T | P3 | `kernel/syscall.c` NDFLT (test) | the R5-F1 NDFLT fix landed without a regression (a multi-thread uncaught-terminate E2E needs a new multi-thread-blocking pouch binary) | **REGISTERED → #46** — fix verified sound by the round-2 deep prosecution |
| HT08.R5-F4 | sockets/stat (pouch) | S | P3 | `0006` + `0010` mirrors | pouch_srv_peer_info + t_stat pin size not field offsets — the A-3 identity-channel drift defense (the Loom-6c/6d offsetof standard) | **REGISTERED → #46** |
| HT08.R5-F5 | openat (pouch) | C | P3 | `0009` | per-component final-omode breaks multi-component O_WRONLY; `open("/")`→ENOENT; relative→ENOTSUP (overtaken by SYS_OPEN + LS-4) | **REGISTERED → #46** — delegate to SYS_OPEN |
| HT08.R4-F7 | Stratum bdev | C | P3 | `bdev_thylacine.c` `mmio_*` | hand-rolled non-ISV-safe MMIO — VERIFIED DORMANT (objdump: current codegen is ISV-safe); a compiler upgrade could reintroduce #890 | **REGISTERED → #46** — structural Stratum hardening |
| HT08.cluster | pouch / libthyla-rs | C/S | P3/H | various | R5-F6 (stdio bypasses socket tag / fdopen-on-socket leak), R5-F8 (getpid/getuid -ENOSYS garbage), R5-F9 (siglongjmp wedges note delivery), R5-F3b (0005 select POLLNVAL→EBADF), R3-F3 (Dma Drop leaks VMA on churn), R2-F3/SA-1 (kernel nonblocking note-read for try_read), SA-3 (print! swallow), SA-4 (4 MiB fixed heap) | **REGISTERED → #46** (the pouch POSIX-coherence + drift-defense revisit) |

**RW-8 verified SOUND (do not re-prosecute without new code):** *syscall routing* — the native `T_SYS_*` mirror AND the composed pouch table both ZERO-drift vs the kernel header; retired numbers 26/30/43 absent; the 0xFFFF sentinel guarded at both `__syscallN` + `__syscall_cp` chokepoints + the static-assert pin. *The A-3 identity channel* — the SO_PEERCRED principal marshal is field-correct (`ucred.uid = principal_id`, `.gid = primary_gid`), no zero/stale path on any return; client-side → ENOTSOCK by design. *MMIO ISV-safety* — structural (the 8 hardware primitives are single base-only `ldr/str`, #890 by construction); all 7 Rust drivers delegate; bdev's hand-rolled accessors are codegen-ISV-safe today (objdump-confirmed, R4-F7 dormant); the Drop-closes-fd-keeps-VMA shape composes with the landed RW-7 `proc_quiesce_owned_devices` p->vmas walk. *Loom DELTA* — the only change since the 6d close is the `offset_of!` asserts (verified vs loom.h); the SPSC happens-before re-derived sound. *libthyla-rs* — the allocator thread-safety (LockedHeap spin::Mutex) + the lazy-init CAS; process.rs fd-passing leak/double-close-free + the A-5a supp_gids lifetime; err.rs -1/-errno + overflow-safe saturation; the panic↔exits composition cascades cleanly post-#811. *pouch* — SP-align + clear_child_tid + __unmapself; the bdev write/RMW/barrier + all 7 drivers' VirtIO init state machines; ninep parsers bounds-checked; cap.rs scalars-only. **The two structurally-invasive fixes (the kernel death-path NDFLT arm; the bdev error-protocol latch) prosecuted SOUND in round-2 (the cascade composes to exactly-once ZOMBIE / I-24 / no-lost-wake; the latch is complete / one-way / lock-clean). 0 P0 / 0 P1 across the whole surface; the dirty close converged over round-2 (which caught RND2-F1, the poll nk==0 boundary the compaction introduced). default 823/823 + bufreader regression + login E2E (FS via bdev) + 0 EXTINCTION; SMP gate PASS — 0 corruption across default+UBSan × smp4/smp8.**

**RW-7 verified SOUND (do not re-prosecute without new code):** *irqfwd* — the R1-F2 dispatch/free `in_dispatch` drain (set-true-before-unlock under the same lock free re-acquires; free spins holding no lock dispatch needs → terminates; the dying-gate + gic_attach(NULL) stop new dispatches; the ≤1 in-flight one drained; the SYS_IRQ_WAIT obj-ref excludes a live waiter at free); the R1-F1 busy-guard (set/clear under k->rendez.lock, cleared on every sleep return incl. SLEEP_INTR, no lost wake in the set..sleep window since sleep re-checks cond); the F3 saturation (caps 0xFFFFFFFE < the sentinel, costs no wakeup). *virtio quiesce* — the device-death walk over BOTH handles + VMAs (round-2 F1), the mapping→burrow→kobj pin chain alive until vma_drain (which runs after both quiesce sites), the lockless walk quiesced at both sites (thread_count==1 / all-reaped), double-reset idempotent, the RNG skip complete for the v1.0 kernel-driver inventory (random.c the sole kernel virtio driver), 5 candidate findings withdrawn (double-quiesce-on-freed-state, stale kobj_mmio, guard-VMA NULL, cross-Proc reset, half-built-proc rollback); the destroy QUEUE_READY readback; vq_size_for pow2 fail-closed + the single-page `_Static_assert`s. *console SAK* — the R2-F1/F2 owner/attach role split verified across all 14 properties (the repurposed guard ≡ the body in every firing state; fail-CLOSED on a dead trusted Proc; single-attach; I-9 no stranded sleeper; I-19 fewer-notes-no-reorder; I-27 preserved; lock order strictly simplified; the full state walk). *crypto + HX (R4)* — ChaCha20 RFC-8439 block vector + forward-secrecy + the fail-closed seeded gate + all-zero guard + the virtio-rng pull memory-safety; HX-I1/I2/I3/I4 + the reloc-free HX-2 symtab. *GICv2/vtimer (W2 DELTA)* — EOI-token no-nesting, I-18 SGI order, GICC I-5 reservation, CNTV timebase. **0 P0 across the whole surface; the dirty close converged over round-2 (which caught the R3-F1 quiesce incompleteness — a 4th P1) + round-3 (clean on the fixes). 823/823; login E2E green; 0 EXTINCTION.**

**RW-6 verified SOUND (do not re-prosecute without new code):** *crypto core* — tag-before-plaintext (AEGIS, the FIPS-203 implicit-rejection integrity gate); constant-time tag compare (crate); fresh salt+nonce per (re)wrap (C-10), no (key,nonce) reuse across keyslots or DEK envelopes; the hybrid-KEM combiner binds both shared secrets + both ciphertexts (X-Wing-shaped, low-order ss_cl cannot forge — ss_pq co-derives); AD domain separation (passphrase vs recovery prefixes diverge at byte 17, injective over no-NUL subjects; b"system" reserved at the USER_CREATE charset chokepoint; the `aead_ad_domain_separation` test); envelope/blob bounds before every slice; BIP-39 decode checksum-gated + bounded (>24 words rejected before the bits write); typed secret keys zeroize on drop (ml-kem + x25519-dalek `zeroize` features on); ThylaRng fail-STOPS (t_exits) not zero-fills. *session/auth* — token unguessable (128-bit CSPRNG, 's'||hex) + constant-time compare; C-7 (UNWRAP/WRAP) ownership check fires before any crypto; the #829 conn-ownership gating intact (`session_clear` only when `conn_id == owner`; conn_id skips 0); malformed-payload safety (every verb len-validates before slicing; MAX_PAYLOAD_LEN gate); C-15 seeded gate (startup probe + per-mint rc checks); 9P framing no partial-write desync (length-prefixed, saturating_sub on every count, oversize tear-down); single-threaded static-mut sound (no thread spawn); GROUP_CREATE/CLEARANCE_GRANT/REVOKE live `peer_live_caps` re-query. *identity/clearance* — clearance.db symmetry (single capped writer, no UPG-style asymmetry); both parsers panic-free; ID allocator C-25 (monotonic, never-reused, reserved sentinels rejected on every id field + parse; a1b-F2 held); CLEARANCE_ACTIVATE→legate `effective_caps` narrows-only (both built-in levels ⊆ CAP_GRANTABLE_CLEARANCE; kernel double-checks; ==0 rejected; non-RE_AUTH refused; dead-stripes fail-closed); cross-user isolation I-1 (DATASET_OWNERS derived per-owner; WRAP/UNWRAP owner==session_user); RESOLVE ungated (C-24); NEXT_LEGATE_SESSION nonzero-saturating. *joey/login* — bringup fail-closed (every assert/step returns 1 before boot_complete); stratumd-readiness FATAL; pivot-survives-/srv; BOOT_COMPLETE one-shot+console-gated; orphan reaper 2B-F3 (non-blocking WNOHANG, top-of-loop only, by-pid session wait → no steal); login never console-attached (I-27) / never holds raw DEK (33-byte token only) / CAP_SET_IDENTITY fidelity (C-8); #856 by-pid Child::wait; bind/unbind teardown. **0 P0 / 0 P1 across the whole surface; 4 P2 fixed + 1 P2 deferred (#876) + P3s; NOT a dirty close. 816/816; ADMIN_ELEVATE + RECOVER(user/system) + CLEARANCE→legate + login all E2E-green; 0 EXTINCTION.**

**RW-4 verified SOUND (do not re-prosecute without new code):** the resolver I-28 (`..`-clamp via trail-pop, per-component X-search on the CROSSED root, base+quarry crossed once, full trail/quarry refcount balance, STALK_MAX_DEPTH, amode fail-closed, cwd-join is convenience); the ns_lock fix (round-2 R-A path-by-path: lock-order leaf, dot_lock/ns_lock never nest, no sleep under lock, deferred-clunk total, mount_lookup migration complete [2 callers clunk], all 6 FROM_ROOT sites balance every exit, rc set every goto, semantics byte-preserved, 2-thread interleavings replayed, `territory_root_ref(NULL)→NULL`, `p->territory` set-once); I-3 cycle ENFORCED at mount/bind; mount-key `(dc,devno,qid.path)`; spoor.c atomic refcounts + magic-clobber; dev9p create/dir/rename fid lifecycle + `attached_owner` (R15-F236 closed); readdir #955 (u64 offset); devsrv registry-ref + open_connect refcount dance (lock-free deadline-bounded handshake) + death-interrupt arms + byte_mode publication; the 9P stack — I-9 per-rpc rendez, elected-reader #841 F6/F7 + #845 Tflush (re-prosecuted from a 2-in-flight+mid-RPC-death interleave), done_reply_buf deferred-free, wire malformed-frame rejection (strict off!=len, caller-cap bounds, ecode [1,4095]), all-or-nothing send, death-interruptible recv, p9_attached dual-destroy; the round-2 wait/wake fixes (R2-F1 `reading` cleared on every recv exit + no elected-reader self-refuse + death-path ring consistency; R4-F1 SQPOLL branch isolation + register-then-observe lost-wake-freedom + no give-up-abandons-stream + no CQ-full deadlock); the Loom DELTA core since 6d (borrow-guard, #898 quiesce, 3 I-30 pins balance, ring TOCTOU both queues, count/payload clamps, registered-buffer slice+W^X, multishot async_inflight↔rearm_pending, chain memory-safety, SQPOLL EXITING handshake+join, CQ wait-list register-then-observe, KObj_Loom lifecycle). **0 P0 across the whole surface; the dirty-close round-2 caught R3-F1 over-broad (R-B-F1) → re-fixed non-invasively; converged clean.**

## RW-9 — The Utopia stack (ut + libutopia + coreutils sample) (STANDARD). Reviewers: 4× Fable-max `holotype-reviewer` (R1 lexer+parser, R2 evaluator+jobs+notes+glob+env+builtins, R3 line-editor+REPL+main, R4 coreutils sample; all `claude-fable-5`, MODEL start==end, no fallback) + Opus seam self-audit. Report: `docs/holotype/09-utopia-stack.md`. Closed list: `audit_holotype_rw9_closed_list.md`. **Status: CLOSED (dirty close, CONVERGED CLEAN over 3 rounds).** Headline: by far the most findings of any RW round (1 P1 + 11 P2 + ~22 P3) — the recurrent substrate-moved theme a 6th time (LS-4/LS-5/#811/#926 moved under the eval/notes/coreutils boundaries without a per-layer revisit). The notes/death/job seam CORE is structurally sound. Round-1 fixed the P1 + 10 P2 (`07a27c9`/`c1aea9d`/`dcc4d08`/`cd7eae2`) + REGISTERED R3-F2 (multi-line render). The **mandatory dirty-close round-2 caught that the round-1 recursion fix was INCOMPLETE** (bounded brackets, not the operator chains `!!!`/`**` -> still a reachable shell-death crash; RND2-F1 P2) + a tr P3 + a self-audit P3 — all FIXED `@aae24db` with non-vacuous regressions. **Round-3 re-prosecuted the F1 fix by exhaustive call-graph enumeration: CLEAN 0/0/0/0.** LESSON: a "complete" recursion fix can miss a whole vector class the regression test can't see; the dirty-close round-2 is what caught it.

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT09.R2-F1 | eval/command-sub | S | **P1** | `eval/stmt.rs::capture_external_pipeline` | A command-sub whose NON-FINAL element fails to spawn orphans the capture write end in the parent → `read_to_end` blocks forever → un-interruptible shell hang | **FIXED** `@07a27c9` (drop un-consumed pipe-end Vecs before the drain; regression /u-subst-test #10) |
| HT09.R1-F1/SA-1/R2-F2 | parser+eval | S | **P2** | parse.rs + expr.rs::parse_subscript; stmt.rs::eval_block + run_command_substitution_script | Unbounded recursion (CONVERGED 3-way): no depth cap in the recursive-descent parser NOR the evaluator → deep nesting / `fn f{f}` overflows the 256 KiB EL0 stack → snare:segv → shell death | **FIXED** `@07a27c9` (parser: token-nesting pre-pass [64] + parse_subscript re-lex counter [32]; eval: Env eval-depth Cell at the 2 chokepoints [64]; new RecursionLimit kinds; regressions #11/#12) |
| HT09.R4-F4 | coreutils | S | **P2** | `coreutils/src/bin/{ls,mkdir,pwd}.rs` | LS-4-stale (6th-time theme): pwd hardcoded "/", ls no-operand listed "/" not cwd, mkdir -p relative silently failed | **FIXED** `@c1aea9d` (env::current_dir(); deep interactive verify owed) |
| HT09.R4-F3 | coreutils/seq | S | **P2** | `coreutils/src/bin/seq.rs` | seq's error-swallowing println! → `seq 1 N\|head -1` CPU runaway | **FIXED** `@c1aea9d` (write_all + break-on-error) |
| HT09.R2-F3 | eval/loops | S | **P2** | `eval/stmt.rs::eval_while`/`eval_for` | A runaway loop is un-interruptible (no note drain between iterations + eager-open makes interrupt deliver-disposition) | **FIXED** `@cd7eae2` (strided note poll [stride 128]; `Env::interrupt_pending` latched + propagated like `pending_exit` through eval_block; `$status 130`; scripture 10.2 pinned; regression ls-5.exp case D) |
| HT09.R3-F1 | REPL/notes | S | **P2** | `repl.rs:143-167` + `ls-5.exp:36-47` | Stale idle-prompt Ctrl-C kills the NEXT command (run_line before deliver_notes); **ls-5.exp papers it over with a load-bearing `\r` — a VERIFY-AROUND** | **FIXED** `@cd7eae2` (`deliver_notes()` at the TOP of the Accept arm; the `ls-5.exp` `\r` verify-around DELETED, case A asserts the real behavior; ls-5 E2E all 4 cases PASS) |
| HT09.R3-F2 | line editor | S | **P2** | `line_editor.rs:691-755` render | Multi-line render block-crawl (stateless renderer + stale rows; the unlanded prev_render_lines/`\x1b[J` promise) | **REGISTERED** (#52) — cosmetic ("not a panic") + invasive (stateful renderer + reset-points through repl.rs, on the audited-clean editor) + NO screen-state verification harness → an unverifiable rewrite on a verified-sound surface is the wrong trade; pre-existing documented caveat |
| HT09.R4-F1 | coreutils/cp | S | **P2** | `coreutils/src/bin/cp.rs` | No same-file/into-itself guard: `cp f f`/`cp f .` truncates source to 0 (silent data loss); `cp -r src src/sub` runaway | **FIXED** `@dcc4d08` (lexical-canonical same-file guard [realpath idiom; identity-sound given no symlinks G11] + -r into-itself guard; writable-fs E2E owed) |
| HT09.R4-F2 | coreutils | S | **P2** | tail/sort/cut/grep/uniq/wc/cmp | 7 utils slurp whole inputs → silent OOM-abort on real /sbin files; cmp OOM exit-1 collides with "differ" | **FIXED** `@dcc4d08` (`io::slurp` capped at SLURP_CAP=2 MiB [chunked] → graceful NoMemory; all 7 fixed at the shared helper) |
| HT09.R4-F5 | coreutils | S | **P2** | 9+ utils' stdout legs | Payload via error-swallowing io::out → silent truncation + exit 0 on a 9P write error | **FIXED** `@dcc4d08` (new `io::OutSink` latch [cat/head discipline]; 9 utils + tee's stdout leg; the exact-output smoke checks pin them byte-for-byte) |
| HT09.R4-F6 | coreutils/tr | S | **P2** | `coreutils/src/bin/tr.rs::expand` | `[:class:]` treated as literal bytes → `tr -d '[:space:]'` silently corrupts | **FIXED** `@dcc4d08` (`expand()` rejects `[:`/`[=`; regression coreutil-smoke `tr class reject`: pre-fix ` b\n` exit 0 → post-fix empty exit 1) |
| HT09.P3-cluster | all | S/C/T | P3 | (see report) | ~22 P3: R1-F2 subshell-`)` mis-parse, R1-F3/F4 scripture-vs-impl, R2-F4..F7 (fg/wait interruptible, poll-error spin, kill-to-reaped seam, builtin completeness incl. ABSENT section-9.2 namespace builtins), R3-F3..F9, R4-F7..F17; + chmod/chown coreutils MISSING while the kernel surface is live | **REGISTERED** (#54; fix-cheap-or-register) |
| HT09.RND2-F1 | parser/expr | S | **P2** | `parser/expr.rs` parse_unary/parse_pow | Round-2 catch: the `07a27c9` recursion fix bounded BRACKET nesting but NOT expr-operator recursion -- `!!!...`/`~~~...`/`2**2**...` (non-bracket tokens) still overflowed the EL0 stack -> snare:segv -> shell death. The #12 regression (pure brackets) gave false confidence | **FIXED** `@aae24db` (shared `ExprParser.depth` counter + inner-fn split at parse_pow + parse_unary [the only unbounded-with-bounded-brackets vectors]; EXPR_MAX_DEPTH=256; regressions /u-subst-test #13 [2000 `!`] + #14 [1000 `**`]) |
| HT09.RND2-F2 | coreutils/tr | S | P3 | `tr.rs` | Round-2: tr exited 0 on a genuine non-pipe stdout write error -- inconsistent with its OutSink filter siblings | **FIXED** `@aae24db` (tr is a filter -> OutSink + report + exit 1). Follow-up #54: the kernel returns `-1` not `-T_E_PIPE` for a native pipe write, so NO filter can be silent-on-EPIPE-only (a shared errno-ABI seam) |
| HT09.SA-1 | eval/cmd-sub | S | P3 | `stmt.rs::eval_command` | Opus self-audit: eval_command checked exit_requested but not interrupt_pending after evaluate_argv -> a Ctrl-C'd `$(...)` arg let the outer command run once on the partial capture | **FIXED** `@aae24db` (the interrupt_pending bail; mirrors the exit_requested checks) |

**RW-9 verified SOUND (do not re-prosecute without new code):** the notes/death/job seam CORE (`wait_pids_interruptible` no-pid-reuse [monotonic allocator + sole-reaper] + no-underflow + no-lost-interrupt + queue-bounded drain; by-pid reap pid-precise; command-sub drain-before-reap success path deadlock-free; pipeline interrupt-forward reaped-gated); pipeline/redirection fd lifecycle (the ONE hole = R2-F1, fixed); parser panic-freedom + UTF-8 char-boundary safety + no parser-math overflow + graceful hostile-input handling (the recursion hole = R1-F1 [brackets] + RND2-F1 [operator chains], BOTH fixed -- round-2 caught that the first fix missed the operator vector); line-editor CSI/UTF-8 robustness + all 13 cursor sites char-boundary-safe + HT00.F1 intact (the render BEHAVIOR bug R3-F2 is on-screen, not a panic); glob OOB-free; eval arithmetic checked_*; coreutils no-arg/input-panic + binary-safe byte streams + checked FS-mutator syscalls. **0 P0 across the whole surface.**

## RW-10 — Cross-cut: Consistency (rubric + invariant ledger + spec/doc drift). Reviewers: 4× Fable-max `holotype-reviewer` (A = ledger I-1..I-12 / B = I-13..I-21+I-31 / C = I-22..I-30 + SPEC-TO-CODE drift / D = the rubric sweep; all `claude-fable-5`, no fallback) + the Fable main-loop self-audit. Full reports: `/tmp/holotype-rw10-{A,B,C,D}.md`; the invariant ledger + rubric verdicts: `docs/holotype/10-consistency.md`. Status: CLOSED. **0 soundness findings — the entire §28 enforcement layer is present, located, and tested; the drift lives in the scripture describing it.**

| ID | Area | Lens | Sev | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT10.F1 | ARCH §28 | X | H2 | `ARCHITECTURE.md` §28 (12 cells) | Row-cell drift cluster: I-2 elevation-only set + console-gate overclaim; I-8/I-17 EEVDF math absent-as-claimed; I-9 phantom `futex.tla` + uncited `tsleep`/`death_wake`; I-10 phantom "monotonic generation"; I-12 "mprotect rejection"; I-15 `<platform>/`; I-16 ctl path; I-20 UNBUILT-read-as-enforced; I-24 `death_wake.tla` uncited; I-27 pre-`@2608c88`; I-31 1-of-5 cfgs | **FIXED** (the §28 cell pass; rows now state as-built + honest deferral markers) |
| HT10.F2 | CLAUDE.md | X | H2 | the invariant mirror + both audit tables | Mirror at 21/31 rows with phantom spec names (`namespace`/`vmo`/`futex`/`notes`/`pty.tla`); the W^X audit rows citing phantom `mm/vm.c`+`mm/wxe.c` (also ARCH:488/:3438/§25.4); the nine-spec gate table | **FIXED** (condensed-31 re-sync; "verbatim" header retired for "condensed + keep row set in sync"; real paths in both tables; the 17-module inventory) |
| HT10.F3 | specs tooling | X | H2 | `Makefile:59` + SPEC-TO-CODE:3 + ARCH §25.3 | `make specs` could not fail (pipe-swallowed exits), errored on `sched_oncpu` (per-option cfgs only), runs zero of the 71 buggy cfgs; the docs claim the CI gate in present tense | **FIXED** (Makefile: skip-no-cfg/TTrace + log-file + propagate; 4-leg smoke) + doc claims honest; **the tiered clean+buggy runner REGISTERED → #56** |
| HT10.F4 | SPEC-TO-CODE | X | H2 | `specs/SPEC-TO-CODE.md` | NO sections for `sched_alpha`/`sched_oncpu`/`asid`/`pipe`; the scheduler section affirmed the RETIRED `g_bootcpu_idle`; territory/handles/9p_client/corvus sections pre-date stalk-2/#844+LOOM/#841+#845/stalk-3c; Loom + HostownerGrant status lag | **FIXED** (4 new mapping sections + 5 dated currency notes + the honest header + 2 status lines) |
| HT10.F5 | scripture | X | H3 | ARCH §25.2 + `specs/README.md` + CLAUDE.md + ROADMAP:985/:1214 + ARCH §23.4/§23.5/:105 | "Nine specs" fossils citing never-written futex/notes/pty and omitting the 11 added modules (specs/README still said "no specs written yet") | **FIXED** (all sites → the 17-module inventory / honest deferral wording) |
| HT10.F6 | I-27 scripture | X | H2 | ARCH §28 I-27 + IDENTITY-DESIGN §9.8:1831 | The RW-7 console role-split (`@2608c88`) missed both sites: the SAK transition still taught notify-note + owner=corvus (both retired) | **FIXED** (rewritten to attach-only re-grant + owner→NULL; verified against `proc.c:995-1020`) |
| HT10.F7 | STALK-DESIGN | X | H3 | `STALK-DESIGN.md:76` | Present-tense survey row ("never consults mounts") falsified by #957, unannotated | **FIXED** (supersession annotation) |
| HT10.F8 | namespace layout | X | **H2** | ARCH §9.4 vs `joey.c:213-248` | The "v1.0 target" /dev + /proc + /ctl layout is unbound (only /srv mounted); the /proc seam's recorded blocker ("no resolver") EXPIRED at stalk; /dev + /ctl seams never recorded | **#57a LANDED** (`cbaef79` impl + `e893623` close): devproc @ /proc + devctl @ /ctl bound into the boot namespace (the /srv idiom + post-pivot re-graft); a reuse-`nc` walk fix made them stalk-reachable. Audit 0 P0 / 2 P1 / 1 P2 / 1 P3 ALL FIXED (F1 `/ctl/kernel-base` KASLR-leak gate, F2 cross-Proc devproc-read UAF, F3 /ctl/procs walk-bound, F4 comments); CONVERGED CLEAN over 2 Fable rounds. **`/dev` = #57b** (the kernel char devices + the I-27 cons/consctl question -- the USER scope call HT10.F8 reserved for RW-13). |
| HT10.F9 | spawn/exec | X | H3 | `syscall.c:3611,3797,3867,3968,4400` | All five spawn variants resolve binaries via the flat global `devramfs_lookup` — bypassing territory/stalk/X-search; documented as-built, never ARGUED, no exec-from-namespace seam recorded (no live violation: initrd binaries system-owned world-x) | **#58 LANDED** (scripture d8fd0ce + impl 0dd30be + audit close): the 5 spawn bodies route through `exec_load_from_namespace` -> `stalk(STALK_OPEN, OEXEC)` (per-component X-search); the seam is now ARGUED in ARCH 9.6.8. Audit 0/0/1P2/4P3. |
| HT10.F10 | reference docs | X | H2 | 9 sites + 1 sibling | Doc-per-chunk violations from the RW-3..RW-9 FIX commits: 99-fs-permission OEXEC; 83-pouch-signals retired NDFLT + kill→EIO; 108-utopia-repl pre-`cd7eae2` Accept + "lazily"; 93/94-utopia missing the RW-9 recursion/interrupt machinery; syscall.h pre-A-3b rights envelope + expired "no t_open" premise; irqfwd.c phantom doc-section cite; 76-admin-elevate cached-peer + byte-compare; (+found in verification) 36-irqfwd "extincts on 2nd waiter" | **FIXED** (all 10; each verified against as-built code first; 36-irqfwd gained the promised "Stale-fire safety" section) |
| HT10.F11 | kernel notes | X | H3 | `notes.c:266` | The `snare:` forge-reject — an ERRORS.md ABI commitment — had no regression test | **FIXED** (`notes.snare_forge_rejected`: 3 reject legs + non-vacuous control; pins the BEHAVIOR, valid if snare:* ever joins the supported set) |
| HT10.F12 | syscall.c | X | H4 | `sys_thread_spawn_handler` | Raw `-22`/`-12` errno literals vs the named `-T_E_*` convention | **FIXED** (13 literals → `-T_E_INVAL`/`-T_E_NOMEM`; `errno.h` included; values identical — behavior-inert) |
| HT10.F13 | ELF loader | X | H4 | `elf.h` | Zero ABI `_Static_assert`s (scripture demands the e_machine/ABI pins) | **FIXED** (6 pins: ELFCLASS64/ELFDATA2LSB/EV_CURRENT/ELFOSABI_NONE/ET_EXEC/EM_AARCH64) |
| HT10.F14 | 9P client | X | H4 | `9p_client.c:99` | The hostile-Rlerror ecode bound (a UBSan-trappable kernel-halt class: `-(int)0x80000000`) was untested | **FIXED** (`9p_client.rlerror_hostile_ecode_bounded`: 0 / 2^31 / 4096 → -EIO; 4095 passthrough control) |
| HT10.F15 | naming | X | H4 | `territory.h:40-74` + LS-K plan | The Territory-vs-Pgrp internal naming split; LS-K's planned `id`/`whoami`/`date` syscall-shaped homes | **REGISTERED** → RW-13 (carried in #57's context) |

**Withdrawn after verification:** the `specs/` TTrace/states hygiene candidate (already gitignored — confirmed independently by self-audit + reviewer C); C-F13's "LOOM.md §7 vs §10" leg (the spec-first statement IS in §7, `LOOM.md:248`).

**RW-10 verified SOUND / checked clean:** 31/31 §28 enforcement mechanisms located + tested (0 ABSENT; the I-17/I-20 rows were honesty defects, now marked); the authority spine + death-wake + stalk-containment + Loom rows CURRENT with cfg sets verbatim; no ambient authority post-A-4; the `-T_E_PERM` trap holds tree-wide; `specs/` hygiene clean. Verification: 825/825 (+2 new tests) + boot OK + 0 EXTINCTION; the new `make specs` smoke-verified on 4 legs; both invariant tables count 31 post-edit.

## RW-11 — Cross-cut: Performance (inventory + comparison; NO rework landed in-arc). Reviewers: 4× Fable-max `holotype-reviewer` (R1 = memory hot paths / R2 = sched-locks-tick / R3 = IPC-9P-FS / R4 = boot-entry-uaccess; all `claude-fable-5`, no fallback) + the Fable main-loop empirical leg (3 timestamped HVF boots + 1 TCG) + self-audit. Full reports: `/tmp/holotype-rw11-R{1,2,3,4}.md`; the budget×measured table + lock-granularity inventory: `docs/holotype/11-performance.md` (§1 + §3). Status: CLOSED. **0 soundness findings across ~31 perf findings; 4/7 measurable budget rows MET on HVF (syscall 83 ns, 9P loopback ~24 µs floor, banner 89 ms non-test, IRQ-path p99 reasoned); the audited mechanisms are cheap or SOTA-matching.**

| ID | Area | Lens | Tier | Where | Finding | Disposition |
|---|---|---|---|---|---|---|
| HT11.SA-1 / R2-F1 | sched wake | P | **V1.0-RISK** | `sched.c:667-681` + `sched.h:48` | **The no-wake-preemption slice cliff** (converged independently: empirical + R2). Same-CPU wakes (the ONLY v1.0 placement) set no need_resched; under saturation every rendez wake (pipe/9P-reply/poll/IRQ-driver) waits up to the FULL 6 ms slice — blows the kernel-handled + IRQ-driver p99/p99.9 cells ~300x. EMPIRICALLY PINNED: irq-bench = 6.0224-6.0236 ms across 3 HVF boots, sigma~10 us = exactly SLICE_TICKS×tick. The fix (check-preempt-on-wake + self need_resched) is additive, weakens no invariant | **LANDED `@fb5e776`** (#60, the D4 pre-rc emerge; user-voted option C = realize the INTERACTIVE band per ARCH 8.3). Same-CPU wake-preempt + the syscall-return preempt point + INTERACTIVE for CAP-gated IRQ-waiters + the trusted console session; folds the 2A-F6 reconcile. Audit 0 P0/0 P1/1 P2 (F1 over-broad console gate -> narrowed to owner/attached)/1 P3 (->#61); SMP gate 0-corruption |
| HT11.SA-1a / G4 | irq-bench | X | H2 | `test_irq_latency_bench.c` + `main.c:654/:734` | The bench has only EVER measured the test-mode slice-wait path (runs before notify-enable + secondary-timer arming; idle peers unwakeable) — every documented number incl. 41-irq-bench.md's was a slice-quantum reading; the production wake paths are UNMEASURED by anything | **DOC-FIXED in-arc** (41-irq-bench.md "What this bench actually measures" + VISION gating note); bench variants → #62 |
| HT11.R4-F1 | boot shape | P | **V1.0-RISK** | `kernel/CMakeLists.txt` + `main.c:654` | No production boot configuration exists: every test_*.c unconditionally compiled + run (extinct-on-fail); the kernel's own `boot-time:` self-measure = 1249-1265 ms HVF vs the 500 ms target (non-test fraction ~0.1 s — fits 5x) | **LANDED `@2500dc9`** (#61a). `option(KERNEL_TESTS ON)`: test/*.c split into KERNEL_TEST_SRCS + gated; main.c skips test_run_all() + boot_stack_report(); sched.c gates the 2 test accessors (#60 P3 fold-in). Kernel flat binary 929 -> 282 KiB. `build.sh --production` + `make production` flip it. Build-config, collides with nothing in §28 |
| HT11.R4-F2 | boot shape | P | **V1.0-RISK** | `usr/joey/joey.c` ladder | joey's 21-stage probe ladder (torture/stress/bench probes + USER_CREATE/RECOVER-E2E/login-E2E = >=7 argon2id derivations ~0.5-2 s) is ungated production-init. NO KDF scripture carve-out needed: per-login argon2 runs AFTER credential entry (never in the boot-to-prompt window); corvus's self-test is argon2-free | **LANDED `@fe1452c`** (#61b). `option(THYLA_BOOT_PROBES ON)`: joey.c gates the whole probe ladder (pre-pivot + post-pivot FS/loom/stalk E2Es + do_corvus_bringup's argon2 ladder [the corvus SPAWN kept] + legate-prover + login/recover E2Es) + the dead probe-only helpers; --production flips both gates. **OFF boot-time 21.5 ms vs 525 ms ON (TCG) -- the V1.0-RISK resolved.** ON = tests 827/827 + all probes intact. Honest scope: --production creates NO bootstrap users (dev fixtures) -> a first-login provisioning SEAM (LS/login line), not regressed. Focused Fable audit 0 P0/0 P1/0 P2/3 P3 all FIXED (F1 probe-statics gated; F2 the 2 test transports -> KERNEL_TEST_SRCS; F3 an OFF-only corvus-readiness barrier so boot-OK still attests corvus reachable); F2-export-sweep residue -> #71. NOT a dirty close |
| HT11.R3-F5 / R4-F3 | uaccess | P | V1.X #1 | `uaccess.S` + `syscall.c:811/839` | Per-BYTE uaccess: `uaccess_load/store_u8` are the ONLY primitives — a function call per byte (fixup keyed per-instruction) on EVERY user↔kernel copy; ~10-40 us per 4 KiB; full-buffer pipe ops blow the <1 us p50 on this alone. Deepens HT03.R1-H4 (whose bsearch half is MOOT — the fixup table has 4 entries) | **REGISTERED → #62** (block copy_*_user with ONE fixup entry — collides with nothing; the top-leverage item) |
| HT11.R3-F9 | stalk/dev9p | P | V1.X #2 | `stalk.c:124-282` + `dev9p.c` | ~2N+2 RPCs per N-component dev9p open EVERY time (per-hop Tgetattr X-search + one-name Twalk; wire supports 16 names; no dcache) — ~10 RPCs ≈ 0.5-2 ms per deep open | **REGISTERED → #62** (qid/perm cache — I-28-compatible; the X-search CHECK stays per-component, the cache feeds it) |
| HT11.R3-F2/F4 / SA / R4-F11 | 9P sizing | P | V1.X #3 | `syscall.c:1033` + `srvconn.h:68/:79` | msize pinned 4096 (Stratum offers 8 MiB; negotiated min) + 2-frame ring: 257 RTTs/MiB, ~19-82 MB/s per-session ceiling, ~55 RPCs per 221 KiB spawn binary | **REGISTERED → #62** (coordinated msize+ring+scratch+pool lift; audit-bearing on the #841/#845 back-pressure model) |
| HT11.R3-F3 | 9P client | P | V1.X | `9p_client.c:413` | Per-RPC `kmalloc(4096, KP_ZERO)` reply buffer through the buddy on every sync op; poolable against the 64-slot tag table, zero protocol change; the KP_ZERO is redundant (parser bounds reads) | **REGISTERED → #62** |
| HT11.R1-F1/F2/F3 | memory eager | P | V1.X | `mmu.c:1711-1740` + `syscall.c:2839` + `burrow.c:88-114` | The eager-memory cluster: per-page tlbi+dsb-ish unmap loop under vma_lock (256 MiB ≈ 13-65 ms; the trailing-single-dsb half is invariant-neutral + named pull-forward); attach zeroing under vma_lock (16-43 ms at max; lock-narrowing already named in-tree); eager-commit + pow2 rounding (129 MiB→256 MiB commit; ~2x RAM worst case; the contiguity tax is self-imposed) — lazy commit is the real lift, preserves the KP_ZERO security property | **REGISTERED → #62** (deepens HT01.B-F6 + C-F5) |
| HT11.R1-F4 | demand-page | P | V1.X (audit-bearing) | `mmu.c:1626` | Broadcast tlbi on EVERY invalid→valid first-touch fault (Linux issues none — architecture contract); ~2-10 ms per 10k-page warm-up. The R7 F129 conservative choice is the CORRECT v1.0 default — **must not be optimized casually** (ARM-ARM review + FEAT-gating) | **REGISTERED → #62** (explicitly audit-gated) |
| HT11.R3-F7 | handle table | P | V1.X | `handle.c:453-485` | Per-fd-op toll: ONE per-Proc table spinlock + 2 atomics + 32 B snapshot (the #844 discipline); stratumd's N I/O threads convoy on one lock. RCU/seqlock fdtable must be re-audited against the #844 closed list; the snapshot itself is intrinsic + cheap | **REGISTERED → #62** |
| HT11.R2-F2/F3/F6 | global locks | P | V1.X | `torpor.c:70` + `proc.c:201` + `spinlock.h:42` | The coarse-global-lock set: torpor_lock (ALL futex traffic, one lock, held across the on_cpu spin; per-bucket split does NOT collide with I-9); g_proc_table_lock (held across the group-terminate wake-storm + 64-bucket sweep + broadcast IPI; narrowing is audit-bearing vs I-24/RW-2-F75); unfair TTAS spinlock (no FIFO; O(N) handoff bounce on the contended globals; qspinlock-class) | **REGISTERED → #62** |
| HT11.R1-F5/F6/F7 | allocators | P/T | V1.X | `buddy.c:28` + `magazines.c` + `slub.c:232` | Global zone lock with magazines fronting only orders {0,9}; refill takes the lock PER PAGE (8x the intended Bonwick amortization — header contract fixed in-arc); SLUB has NO per-CPU fast path (every kmalloc takes the per-cache global lock — ARCH §6.4's committed mechanism, absent; scripture honesty-marked in-arc) | **REGISTERED → #62** (buddy_alloc_bulk + magazine widening + kmem_cache_cpu; deepens HT01.A-H/T) |
| HT11.R2-F4/F5/F7/F8 | sched costs | P | V1.X | `timer.c` + `context.S:98-183` + `sched.c:449` + `smp.c:175` | 1 kHz always-on tick (8k global g_timerwait.lock acquisitions/s at 8 CPUs; NO_HZ_IDLE-class fix); eager FP/SIMD 1 KiB + unconditional FPCR isb per switch incl. FP-less kthreads (lazy-FP is the documented seam; the conditional-isb half is trivial); O(N) run-queue insert on every yield (rbtree folds into the EEVDF lift); broadcast group-terminate IPI (targeted kick needs a per-Thread cpu stamp; composes with I-18) | **REGISTERED → #62** |
| HT11.R4-F4 | spawn/exec | P | V1.X | `syscall.c:3628` + `exec.c:85-150` | Eager-everything spawn: TWO full byte-copies of the binary (blob→kmalloc; KP_ZERO-then-overwrite per segment) + eager 256 KiB stack zero; ut (388 KiB) ≈ 400-520 us ≈ 2-2.5x the 200 us p50 — the session-start binaries miss. Blob-backed RX Burrows (I-12-compatible) delete both copies; the F5 block-copy is the 8-16x stopgap. Subsumes R1-F10 (zero-then-overwrite) | **REGISTERED → #62** |
| HT11.R4-F6 | notes tail | P | WATCH | `notes.c:689` | The notes peek takes `q->lock` on EVERY sync EL0 return (no pending-flag short-circuit) — a shared-cacheline RMW across all threads of a Proc per syscall; a relaxed `q->count` pre-check preserves I-19 (the authoritative peek stays locked) | **REGISTERED → #62** (WATCH cluster) |
| HT11.WATCH-cluster | various | P | WATCH | (see report §4) | R1-F8 SLUB KP_ZERO byte-loop + per-iter bound reload (codegen-confirmed; trivial hoist); R1-F9 O(N) VMA list + single vma_lock at musl-daemon N≈50-100 (the per-VMA-lock half is audit-bearing vs #713); R3-F1 the 9P ladder (2 switches + 5 copies + 1 alloc — small ops fit); R3-F6 pipe 4-copies/pair (the structure is sound; F5 is the fix); R4-F8 IRQ→EL0 ladder 0.6-1.4 us reasoned (p99 fits; GICv2's 4 dsb/delivery is the audited HVF price); R4-F9 per-char polled UART TX (~3.5 s pure TX for the test boot's ~40 KB at real 115200; NO post-boot hot-path prints — verified); R4-F11 the projected 9P spawn leg (~1-3 ms; pairs with #58) | **REGISTERED → #62** (breaking scales named in the report) |

**RW-11 verified IN-BUDGET (do not re-derive):** the syscall entry/exit floor (reasoned 100-160 ns; MEASURED 83 ns/op HVF loom-NOP ×3 boots) — the <200 ns row MET, Linux-class for a full-save uniform-EL1h zero-leak kernel; the audited soundness mechanisms are performance-free or SOTA-matching (#713 windows + die-check ≈1 instr; #844 snapshot cheap; #841 elected reader == Plan 9 mountio / Linux v9fs; SQPOLL parks — zero idle burn, beats io_uring's spin-then-park; ASID fast path 1 lock-free CAS; NO per-switch TLBI); pick_next O(1) + per-CPU rqs + trylock steal; alloc_pages KP_ZERO outside the lock + stp-widened; boot mechanics cheap (BSS sub-ms; 1982 relocs ≈ us; struct-page init 10-25 ms); devramfs O(98) negligible; 9P small-op RTT 24-61 us measured (the <50 us p50 plausibly MET); argon2id correctly placed AFTER credential entry. **0 SOUNDNESS findings — the audited protocols held under the perf-lens trace from all five prosecutors.** In-arc edits: 6 trivially-mechanical honesty fixes only (3 kernel comments + ARCH §6.4 + VISION §4.5 gating notes + 41-irq-bench.md), per report §6.1.

## RW-12 — Cross-cut: Gaps (workload-driven W1-W6; inventory + registration, ZERO in-arc edits). Reviewers: 4× Fable `holotype-reviewer` (R1 W1-fileservice + W2-buildstorm / R2 W3-interactive + W6-editor / R3 W4-network / R4 W5-containers; all `claude-fable-5` start==end, no fallback) + main-loop self-audit (SA-1..SA-7) + the aux `DOC-GAP-REPORT.md` (G01-G18). Catalog: `docs/holotype/12-gaps.md`. Status: CLOSED. **60 findings, 0 H1 / 19 H2 / 29 H3 / 12 H4.** Two dominant themes: (1) present-tense scripture over deferred surface + pervasive phase-record incoherence (net named 5 ways, ARCH §23.8 Phase-7 over-claims vs LS Phase-8, I-20 mispointer) — the RW-13 reconciliation input; (2) two keystone capabilities unbuilt with no/weak re-entry (exec-from-namespace blocks the container ✓; the Resource/DoS floor leaves bombs unbounded). Lens key: SCR=scripture-gap, IMPL=implementation-gap, SEAM-R=seam-recorded, SEAM-U=seam-unrecorded.

| ID | Workload | Kind | H | Where | Gap | Re-entry / disposition |
|---|---|---|---|---|---|---|
| HT12.W5-F1 | W5/W2 | SCR | **H2** | `syscall.c:3612/3798/3868/3969/4401` | **Exec-from-namespace**: all 5 spawn variants `devramfs_lookup` from the boot cpio; a container/mounted-FS binary is categorically unexecutable + a confined Proc can spawn any cpio binary (reverse-visibility leak) | **#58 LANDED** (d8fd0ce + 0dd30be + close): stalk-resolved spawn; the `/bin` bind (option B, user-voted 2026-06-12); reverse-leak CLOSED + mounted-FS exec enabled (F1 dev9p OEXEC->O_RDONLY). Audit 0/0/1P2/4P3. F2 owed dev9p-exec test -> #70. |
| HT12.W5-F2 | W5 | IMPL | **H2** | `IDENTITY-DESIGN:769` + tree | **Resource/DoS floor**: scripture-committed under BUILD ("bomb bounded, not box-extincting"); unbuilt — no per-Proc page/thread/child cap (only fds=64); bombs unbounded; the v1.x quota seam depends on its counters | **#65 LANDED** — scripture `c0e73a6` (ARCH §28 **I-32** + §25.4 + §6.8 `/ctl/mm` reconcile; IDENTITY-DESIGN §3.8) + impl `ffe89c1` (per-Proc `PROC_PAGE_MAX`/`THREAD_MAX`/`CHILD_MAX` caps; unforgeable `PRINCIPAL_SYSTEM` exemption; SEAM counters in `/proc`+`/ctl`; `docs/reference/110-resource.md`) + audit close. Fable holotype-reviewer **0 P0 / 0 P1 / 1 P2 / 5 P3** (MODEL start==end, no fallback), **NOT dirty** — F1 [P2] the Loom-ring page-cap bypass (`SYS_LOOM_SETUP` uncharged anon; close-without-detach leaks the ring VMA) FIXED (charge the ring at setup); F2/F4 doc, F3 `/ctl` header, F5 stale comment, F6 `thread_count`→atomic, all FIXED. Self-audit SA-1 converged with F1 (I under-rated it P3; the reviewer correctly escalated). 834/834 (default+UBSan) + 0 EXTINCTION + SMP gate 0 corruption. Closed list: `audit_65_closed_list.md` |
| HT12.W5-F3 | W5/W1 | IMPL+SCR | **H2** | `devproc.c::format_ns` + `spoor.h:80-108` | **ns is a `binds:N` stub + struct Spoor retains NO name** → no accessor present-or-future can render the namespace without a mount-name-retention data-model change; the Plan 9 signature tool can't be built | **NEW #66** (introspection substrate) — design @ RW-13; ↔#57 |
| HT12.W1-F1 | W1 | IMPL | H2 | `ARCH:2171` vs `:2999`-area | per-Proc 9P connection claimed at v1.0; tree ships the v2.x shared-multiplexed client (corvus+joey share the root client via `territory_clone`) — self-contradiction | #64 RW-13 reconcile (descope to v1.x or schedule) |
| HT12.W1-F2 / W5-F7 | W1/W5 | IMPL / SEAM-U | H2/H3 | `territory.h:284` + `territory.rs:182` | Union mounts: COMPARISON ✓ + NOVEL #1 scope-in; tree = MREPL-only, `bind_before/after` return success + no-op | #64/#70: build or flip ✓→○; fail-loud the no-op API |
| HT12.W1-F3 | W1/W3 | SEAM-R + SEAM-U | H2/H3 | `builtin.rs` + `territory.rs` | no shell `bind`/`mount` builtins (U-8, recorded, phase OPEN); the ns-READ surface has no LS/task anchor | U-8 (builtins) + #66 (ns-read) |
| HT12.W1-F4 | W1 | SEAM-R | H3 | `9p_client.c` exports | Tlock/Tgetlock + xattr + Tbind/Tunbind + Treflink wire families unimplemented; VISION §328/333/397 present-tense them | ROADMAP:463 + LOOM #916; #64 VISION honesty |
| HT12.W1-F5 | W1 | IMPL | H3 | `9p_session.h:94` + `devctl.c` | NOVEL #3: limit compile-time 64 (not configurable), fail-not-block, no `/ctl/9p/<session>/*` observability | #64 implement or amend NOVEL #3 |
| HT12.W1-F6 | W1 | SEAM-R | H3 | `9p_session.h:97` + `handle.h:157` | 256 fids/session (system-wide via F1) + 64 handles/Proc (<64 clients per server) — the W1 capacity envelope | size the Phase-9 stress plan against these |
| HT12.W1-F7 | W1 | SEAM-R | H4 | `srvconn.h:68,79` | msize 4096 / 8 KiB ring throughput ceiling | **overlaps RW-11 #62** |
| HT12.W1-F8 | W1 | SEAM-R(weak) | H3 | `ninep.rs` header | the "userspace 9P server library" (NOVEL #1) is a codec only — no dispatch loop/fid table/walk | name a `lib9p-server` Phase-8 deliverable |
| HT12.W2-F1 | W2 | IMPL | **H2** | `usr/` listing + ROADMAP | **No on-system toolchain** — gcc/clang/make/git in NO phase; VISION §397 "No subset" + Phase-9 "parallel make" presuppose it | **NEW #67** — phase home or descope |
| HT12.W2-F2 | W2 | SEAM-R | H3 | `ARCH:1922-1928` | no fork/exec for ported code (clone→THREAD_SPAWN only); a hard Phase-8 design item | Phase-8 shim; flag for early design |
| HT12.W2-F3 | W2/W1 | SEAM-U | **H2** | `syscall.h:1561` | **argv ceiling 16/4096B**; shell passes operands through unchunked → `grep pat src/*.rs` fails first hour; never recorded | **NEW #69**: raise + chunk; LS-9-adjacent |
| HT12.W2-F4 | W2 | SEAM-R | H3 | `time.rs` + `dev9p.c:281` | no wall clock (mtime READ works; `t_stat` "v1.0=0" stale for dev9p); LS-K on the Phase-7 critical path | LS-K #951 |
| HT12.W2-F5 | W2 | SEAM-R | H3 | `syscall.h:1512` | no atime/mtime setter (Tsetattr+LOOM carry it); `cp -p`/`make` blocked | LS-9 (G12); cheap pull-forward |
| HT12.W2-F6 | W2 | SEAM-R | H3 | `9p_client.c:1055/1120/1147` | link/symlink/readlink: wire+Loom built; sync syscalls + `ln`/`readlink` missing | LS-9 (G11) |
| HT12.W2-F7 | W2 | SEAM-R(weak) | H3 | `usr/coreutils/src/bin/` | sed/awk/find absent; ROADMAP §8.1 names them; LS never absorbed them | #67/#69: LS-9 / new LS-3d |
| HT12.W2-F8 | W2 | SEAM-R | H4 | `syscall.h:1647` | no env (envp reserved+rejected); PATH/CC/CFLAGS blocked | LS-9 (G15) |
| HT12.W2-F9 | W2/W1 | SEAM-R + SEAM-U | H4 | `loom.h` + `9p_client.c:1015` | O_TMPFILE/fallocate/reflink absent; **statfs Loom-only** (no SYS_STATFS) | ROADMAP:463; #69 record statfs asymmetry |
| HT12.W3-F1 | W3 | SEAM-R | H2 | `login/main.rs:108` + `cons.c` | login type-blind: no username echo, accidental password masking | LS-6 #949 (real ECHO @ LS-8b) |
| HT12.W3-F2 | W3 | SEAM-R + SEAM-U | H2 | syscall enum (70) | no self-identity (getpid/uid/gid) → id/whoami; **sub-gap: principal→NAME map unnamed** | LS-K #951 (+ record name-map) |
| HT12.W3-F3 | W3/W2 | SEAM-R + SEAM-U | H2 | enum + no PL031 | no wall clock; **no RTC epoch source in tree** (LS-K's "wall-clock base" has no provider) | LS-K #951 (+ record epoch source) |
| HT12.W3-F4 | W3 | SEAM-R | H3 | `eval/env.rs` + `builtin.rs:64` | no env/envp/PATH (shell var store local; export meaningless) | LS-9 (v1.x) |
| HT12.W3-F5 | W3 | SEAM-R(contradictory) | H3 | `builtin.rs:48` + `cons.c` | no Ctrl-Z/stopped jobs; phase record contradicts (ROADMAP §8.2 P7 vs LS P8) | #64 Phase-8; see W6-F2 |
| HT12.W3-F6 | W3 | SEAM-R(orphaned) | **H2** | `stmt.rs:82` | **child stdin unwired** — `cat > f` silently empty; record says LS-5/LS-8 but LS-5 closed past it + no LS-8 sub-chunk owns it; **LS-7's editor depends on it** | **#69: pin to a named owner** |
| HT12.W3-F7 | W3 | SEAM-R + SEAM-U | H3/H4 | `cons.c:39` + `joey.c` | no concurrent sessions (one console, single-reader); **positive v1.0 "one session" posture only a code comment** | CORVUS §11 v1.x; #64 record posture |
| HT12.W3-F8 | W3 | SEAM-R | H2 | `phase7-status` U-8 | no shell bind/mount/cap/note builtins (U-8, OPEN) | U-8; ns-read → #66 |
| HT12.W3-F9 | W3 | SEAM-U | H4 | `libthyla-rs::cap` | no general self-caps read; only Imperium flag subset (#959) | fold #66 `/proc/self` cluster |
| HT12.W3-F10 | W3/W6 | housekeeping | H4 | aux report | aux staleness: G03/G05/G07 CLOSED; G06/G16 partial; G13/14→LS-K, G15→LS-9, G17→#66, G18→W3-F9 | — |
| HT12.W4-F1 | W4 | SCR | H2 | docs (zero hits) | **packet filter designed nowhere any horizon** (the charter's own example) | **#68** net-phase decision bullet |
| HT12.W4-F2 | W4 | SCR + contradiction | H2 | `ROADMAP:951` vs `ARCH:1922-1937` | **socket-shim CLAIMED but ARCH §11.5 top-50 has ZERO socket syscalls** + "§16 TBD"; curl/redis/ssh exit criteria unmeetable | **#68** reconcile ARCH §11.5/§16 vs ROADMAP §9.1 |
| HT12.W4-F3 | W4 | SCR | H3 | `NOVEL:95`/`ARCH:1744` | DNS/`/net/cs`: file named, mechanism designed by nothing | #68 net-phase bullet |
| HT12.W4-F4 | W4 | SCR | H3 | `ROADMAP:966` | TLS/root-cert-bundle exists only inside an exit criterion | #68 net-phase bullet |
| HT12.W4-F5 | W4 | SCR | H3 | docs (no `/ctl/net`) | network observability charter-named, undesigned (devctl + NOVEL §243 precedent) | #68 net-phase bullet |
| HT12.W4-F6 | W4 | SCR | H3 | `dev9p.c` no `.poll` + `ARCH:1455` | poll-readiness over 9P net fds undesigned; NULL-slot = "always ready" degenerates | #68 net-phase bullet |
| HT12.W4-F7 | W4 | SEAM-R | H3 | net design paras | /net schema + fid-state-machine pre-design; §9.3 spec-waiver mis-scoped (smoltcp ≠ netd fid machine) | #68 re-scope §9.3 |
| HT12.W4-F8 | W4 | SCR(acceptance) | H3 | `ROADMAP:960-969` | exit criteria 100% client-side — the listen/accept/soak shape that DEFINES W4 never proven | #68 add server-side + soak criterion |
| HT12.W4-F9 | W4 | SEAM-pointer | H3 | (6 cites) | **net phase named 5 ways** (P6/7/8); VISION:315→ARCH §13 dangling (now MMIO/VirtIO); ROADMAP:1450 "§16 TBD" | **#64** RW-13 emerge: canonical phase + pointers |
| HT12.W4-F10 | W4 | SCR | H3 | `ROADMAP:950` | IP config (DHCP/static/ipconfig) undesigned; v1.1 board can't hardcode slirp | #68 net-phase bullet |
| HT12.W4-F11 | W4 | SEAM-R + SCR | H4 | `LIFE-SUPPORT:381` | wall-clock recorded (LS-K); **NTP/clock-sync designed nowhere** | LS-K + #68 NTP line |
| HT12.W4-F12 | W4 | SEAM-R + SEAM-U | H3 | `ARCH:1477` | supervision recorded; `/ctl/proc-events/exit` hook task-less since P4-M | #68: task # or decide wait_pid_for suffices |
| HT12.W4-F13 | W4 | SCR(weak) | H4 | corvus precedent | general daemon-logging convention undesigned | one manual para when netd lands |
| HT12.W4-F14 | W4 | SEAM-R(named) | H4 | `ARCH:1744`+`ROADMAP:952` | import/exportfs over authenticated 9P named-only (cpu(1); sshd=Pouch, needs PTY+AF_INET) | acceptable depth |
| HT12.W4-F15 | W4 | IMPL | H3 | `usr/virtio-net-*` + `ROADMAP:375` | virtio-net probes built; scripted `/dev/ether0` never landed; its blocker (no FS namespace) dissolved at stalk | #68 (netd owns NIC; amend ROADMAP:375) |
| HT12.W5-F4 | W5 | mixed | H3 | `devproc.c:425/~190/~225` | /proc: no readdir/enum, stale `<unnamed>` cmdline (argv landed @16b-α), no `/proc/self`, undesigned pid-visibility scoping | readdir+cmdline ride #57; pid-scope → #66 |
| HT12.W5-F5 | W5 | SEAM-R + SCR | H3 | `dev.c:113` + `territory.h:56` | restricted /dev unassemblable: no userspace Dev-attach primitive, QTFILE roots, PGRP_MAX_MOUNTS=8 < §9.4 layout | fold mechanism+sizing into #57 |
| HT12.W5-F6 | W5 | SEAM-R(stale) | H3 | `ROADMAP:940`+`ARCH:2526` | thylacine-run/OCI recorded but anchored at the closed "Phase 6"; format/env/ro-root/tmpfs undesigned beneath | **#70** re-anchor the container phase |
| HT12.W5-F8 | W5 | SEAM-R(incoherent) | H3 | `ARCH:556`+`IDENTITY:782`+`ROADMAP:1283` | quota seam recorded 4× but depends on the unbuilt F2 floor + cites a nonexistent `/ctl/mm/` | #65/#70: reconcile; datasets-as-disk-quota |
| HT12.W5-F9 | W5 | SEAM-R | H4 | (W4 dependency) | container /net isolation blocked on the unbuilt net stack; free with netd | one design line |
| HT12.W5-F10 | W5 | SEAM-R(non-goal) | H4 | `ARCH:2351`+`ROADMAP:945` | no seccomp — recorded non-goal, defensible (≤70 syscalls, cap-gated, no ambient VFS); coherent once F1 closes | none needed |
| HT12.W6-F1 | W6 | IMPL + contradiction | H2 | `usr/` (no editor) | **no editor in tree** vs ROADMAP §8.2/ARCH §23.8 (Helix=Phase-7-exit); newer scripture LS-7/Phase-8 — disagree | **#64/#69** re-scope §8.2/§23.8 to LS-7 |
| HT12.W6-F2 | W6 | SEAM-R(contradictory) | H2 | `ARCH:3282/3660` + `LS:400` | **PTY: 3 contradictory phase records**; **I-20 "lands with LS-8" but LS-8 excludes master/slave**; kernel-vs-userspace home undecided | **#64** pick Phase 8; repoint I-20 |
| HT12.W6-F3 | W6 | SEAM-R + SEAM-U | H2 | `cons.c:9` + `0001-patch:401` | no termios/ISIG/echo; **ported code can't `isatty()`** (ioctl→ENOSYS); **no pouch-termios patch named** | LS-8b #952 (+ #69 name the patch) |
| HT12.W6-F4 | W6 | SEAM-R | H2 | `cons.c:303` no `.poll` | cons not pollable → no editor event loop, no async job-done at idle | LS-8a/8c #952 |
| HT12.W6-F5 | W6 | SEAM-R + SEAM-U | H3 | no TIOCGWINSZ | no winsize query (editor needs cols/rows on entry); SIGWINCH recorded, the static query is not | #69 add to LS-8b consctl |
| HT12.W6-F6 | W6 | note | H4 | `line_editor.rs` | TUI baseline: built (CSI/history/ANSI-passthrough/raw-default) + the exact remainder | — |
| HT12.W6-F7 | W6 | SCR(deliberate) | H4 | no mouse/OSC52 | terminal mouse/clipboard absent; in-band-expressible; Halcyon owns it | none |
| HT12.W6-F8 | W6 | IMPL(no re-entry) | H3 | `ARCH:3285` | `/tmp`,`/run` tmpfs: ARCH §23.8 Phase-7 "must have"; nothing builds it, no anchor; `/home` is the v1.0 workaround | **#64/#70** demote or queue a tmpfs Dev |

**RW-12 X-lens overflow (RW-10 category, not a gap):** the nine-specs fossil survives in `COMPARISON.md:259/271/318` + `NOVEL.md` Angle #8 + `estimate.md:41` ("nine TLA+ specs ... futex, notes, pty" — real inventory 17; those 3 dropped 2026-05-23). RW-10 swept the spec-tooling/CLAUDE.md surfaces, not these positioning docs. NOT trivially-mechanical (a multi-location count+framing change) → registered for the #64 RW-13 honesty pass, not fixed in-arc.

**RW-12 verified PRESENT (do not re-derive as gaps):** the FS substrate (create/fsync/readdir/rename/unlink + fstat/lseek/wstat + atomic rename + dev9p mtime); the audited multi-in-flight 9P client (#841) + the complete Loom arc; spawn-full-argv + wait_pid_for + group-terminate + handle-close-at-exit + pipes/pipelines; the namespace *primitive* (mount/unmount/chroot/pivot_root, I-28 containment, I-1 isolation, rbind-for-free); A-5 single-session login→home→shell→logout; the line editor + job control + Ctrl-C-as-note; virtio-net/blk/gpu/input + the QEMU harness; AF_UNIX SOCK_STREAM + SO_PEERCRED; the closed aux items env::args / io::stdio / cwd / static uname. **The substrate is broad and sound (RW-1..11); the gaps are the userland surface a workload needs + the scripture reconciliation debt.**

## RW-13 — Consolidation + emerge (the arc capstone). The full triage + the re-planned roadmap + the four voted scope decisions: `docs/holotype/13-consolidation.md`. Status: CLOSED — the arc EMERGES here.

The ~250 register rows above are overwhelmingly already dispositioned
(FIXED in-arc / ACCEPTED-with-caveat / v1.x-TRACKED). RW-13 places the open
set; the four v1.0 scope forks were voted 2026-06-11:

| Triage bucket | Rows | Disposition |
|---|---|---|
| **v1.0-pull, scope-independent (D4, first)** | HT11.SA-1/#60, HT11.R4-F1/F2/#61, HT12.W5-F2/#65 | the pre-rc hardening sub-arc |
| **v1.0-pull, container keystone (D2)** | HT10.F9/HT12.W5-F1/#58, HT12.W5-F3/#66, HT10.F8/#57 | pulled forward (pre-LS) |
| **v1.0-pull, reconciliation (#64, first emerge chunk)** | HT12.W4-F9, W1-F1..F4, W6-F1/F2, the nine-specs fossil | the doc-hygiene pass (section 2.1 authoritative) |
| **LS line (Phase-7 completion)** | HT12.W2-F4/W3-F2/F3 -> LS-K; W6-F2/F3/F4/F5 -> LS-8; W6-F1 -> LS-7; W3-F1 -> LS-6; #69 sub-seams | re-sequenced, section 3.2 |
| **Phase 8 (D1 net + D2 containers + D3 toolchain)** | HT12.W4-* -> #68; HT12.W5-F4/F5/F6/#70; HT12.W2-F1/#67 (NEW v1.0 scope) | section 3.3 |
| **Phase 9 (hardening -> rc)** | HT11 #62 budget-gating items | section 3.4 |
| **v1.x (recorded re-entry)** | #23/#26/#28/#46/#54, union mounts, LS-9 cluster, #20 (errno, owed vote) | section 3.5 |
| **rejected-with-rationale (H4)** | HT12.W5-F10 (seccomp non-goal), W6-F7 (mouse/OSC52 -> Halcyon), W4-F13 (daemon-logging) | record-only |

The four voted decisions (D1 networking IN / D2 containers BUILD-core / D3
toolchain IN / D4 pre-rc-hardening FIRST) are recorded in
`13-consolidation.md` section 2. The single systemic finding of the whole arc
— the P6 multi-thread-Proc lift outrunning the wait/wake machinery, where
almost every P1 lived (RW-2/4/5/7/8/9) — is `13-consolidation.md` section 1.2
+ DEBUGGING-PLAYBOOK 6.15. **The HOLOTYPE arc is COMPLETE.**
