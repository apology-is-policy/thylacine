# Graphics arc status — G-0..G-9 (Phases 9/10; TAPESTRY.md §18.9)

The per-chunk status doc for the Tapestry/Halcyon build arc, chartered by the
user 2026-07-19 ("continue with the G items; ABI automatically signed off").
The binding stage table is `docs/TAPESTRY.md` §18.9; the design closed over two
Fable holotype rounds (§18.11/§18.12) with the spec landed model-first
(`specs/tapestry_present.tla`, V3).

## TL;DR

G-3 (tapestryd stage 0 + the R2-F3 kernel reaper) is LANDED — **I-40 is
COMPLETE** (share half G-2, present half G-3): the compositor owns scanout
from day 0, `tapestry-demo` presents the -v pattern + live plasma through
the FULL protocol (the per-boot gate now proves the whole G-2/G-3 path,
with a new liveness leg), and the warden's `gather` mode carries both
graphics-path PCI functions in one I-34 allowance. Earlier: G-2 (the
kernel share delta) landed first per the 2026-07-19 directive; G-0 (the
screendump harness) + G-1 (the resident gpud, now absorbed/retired) built
the substrate. G-3 + G-4 CLOSED (the fbcon live + ls-gfx PASS; holotype 0/0/1/2 NOT dirty);
smp8 re-gate PASS (20/20, 0 corruption). The user then drove the fbcon LIVE
and surfaced five real bugs across three rounds, all fixed same-day: #31
(controlq live-display desync) + #30 (cora's home / the full SrvRegistry)
+ #35 (the blend lane bug) + #36 (bare-LF session output) + #37 (DECAWM +
CPR — nora fullscreen). Interactive graphical mode is UP:
`THYLACINE_DISPLAY=cocoa tools/run-vm.sh`. **G-5 (the graphics audit
round) is CLOSED**: Fable-5-max holotype over the whole live-fix wave —
0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty, all fixed (the F1 wall-clock
dead-latch rework + F2/F5 + doc F3/F4 + self-audit SA-2), plus the
deferred `-c` blend-integrity gate hardening and the #34 LS-CI evidence
retention landed alongside. **G-6 (the compositor) is IN PROGRESS: G-6a
(the pane tree + multi-surface composition) + G-6b (the resize protocol
+ pane close) + G-6c (the interaction layer) are LANDED** — the i3
container model, the Direct/Composed scanout mode machine (the stage-0
zero-copy fullscreen path byte-identical; a compositor-owned
weave-subtype screen buffer for splits), the §18.3 CONFIGURE emission
half, the full reweave protocol (per-generation weaves + the
`resize W H <serial>` ack as THE GENERATION FENCE) + TEV_CLOSE, and now
the interaction layer: the Super chord plane intercepted ABOVE the
event stream (§18.4 — bound chords act, the whole plane reserved, a
swallowed press swallows its release), TEV_FOCUS gained/lost from the
single reconcile tail, the D7 glyph-free tab/stack indicator strips,
spatial focus + directional move (D6 re-parenting incl. root wrap) +
tmux-style zoom, multi-rect presents (rects inline in the payload —
the as-built D4 compositor case), and the §18.6 determinism mode
behind the `test-mode` cargo feature (frozen FRAME clock + `tick` +
TPRESENT_HOLD/`release`). The `ls-gfx-panes` leg grows nine G-6c legs
(focus-event, multirect, strip colors, tab-cycle, zoom, move, chord
via QMP `-k`, test-mode, hold — per-stage pixel dumps). **G-6d (the
focused audit + the arc CLOSE) is DONE**: the Fable-5-max holotype over
the whole 6a+6b+6c delta returned **0 P0 / 0 P1 / 1 P2 / 4 P3, NOT
dirty** (MODEL(end)=Fable 5, no fallback). The one P2 (F1) is the
deliberately-global pane/layout control surface — the WM-control model
(control is 9P), whose v1.0 trust boundary is the per-territory `/srv`
(I-1/I-28: no user session's namespace reaches `/srv/tapestry`); the
per-client layout ACL + renderer-role protection is the tracked
Halcyon-era seam (task #42). F2–F5 tracked P3s (#43/#44) + the F4 24-bit
pane-qid pin. **The #41 LS-CI bridge cluster is ERADICATED**: the
long-fragile `nc -U` serial bridge (SIGPIPE=141 under the boot-output
burst — guest exonerated every time, VM `RN+`) is replaced by the
purpose-built, SIGPIPE-immune `serial-bridge.py` (the #66-comment fix
finally built), proven byte-perfect on a 200 KiB ANSI burst; ls-gfx-panes
went from 3/3 deterministic FAIL → clean first-attempt PASS. The
pipelined-controlq drain obligation still carries (the synchronous engine
was deliberately KEPT — quiesce-by-construction stands). **THE G-6 ARC IS
COMPLETE.**

## Landed chunks

| Commit | Chunk | What | Tests |
|---|---|---|---|
| 3b8e5580 | #51 pacing | **The FRAME-paced present (Track A 2.3; task #51).** `UpdateWindowFramebuffer` waits for the compositor's next FRAME tick before presenting — a 60 Hz compositor can only SHOW 60 fps; beyond that is un-composed overwrites + a spinning vCPU (the uncapped timedemo's ~600 fps / 122–600 HVF variance). The PUMP thread bumps `frame_seq` + signals `frame_cv` per TEV_FRAME read off the fid (driver-private fields — the F2 rule; FRAME no longer rides the ring, and the bump CANNOT live at translation: the present's main-thread wait would starve PumpEvents into timeout-only pacing); the present does ONE `pthread_cond_timedwait` bounded 50 ms WALL-CLOCK (the G-5 F1 lesson — never a wake-count bound). Bounded degradations: frozen/degraded clock or a HIDDEN pane (visible-only FRAME) → ~20 fps timeout pacing = free background throttling; teardown ≤50 ms; spurious wake = one early present. Default ON; `SDL_THYLACINE_NOPACE=1` opts a benchmark out. Kernel byte-unchanged. | **MEASURED: 969 frames 15.5 s 62.5 fps** (stable ≈ clock_hz; the honest close of the fps-variance note) — ls-gfx-quake PASS attempt 1 + ls-gfx-play 8/8 PASS attempt 1 on the paced build; docs 142 (#51 section) + the gate comment |
| 58161c41 | G-7c close | **The G-7c focused audit + gates.** Fable-5-max holotype (MODEL(end)=Fable 5) + concurrent self-audit over `3bac7060`+`7e293176`: **0 P0 / 0 P1 / 0 P2 / 5 P3, NOT dirty — all five FIXED in-commit.** **F1** duplicate `VIRTIO_DEVICE_ID_INPUT` define (pre-existed; deduped). **F2** the "pump-thread `relative_mode` read is benignly racy" comment was FALSE (the G-7d-F1 class): translation runs ONLY on the SDL main thread inside `PumpEvents` — no race exists; comment + the 142 doc now say main-thread-only-by-construction, do NOT move translation onto the pump thread (this also dissolved the self-audit's opposite wrong belief — the two prosecutors converged from opposite errors). **F3** the E2E's "click transitively proves MOVE" was STRUCTURALLY false — the (0,0) pointer default sits inside the ZOOMED pane's full-display rect, so a dead MOVE path still passed; FIXED with the position-discriminating leg (unzoom → aim into Quake's half of the split → click; a dead MOVE leaves the pointer at origin → aurora's pane → timeout). **F4** libt/lib.rs raw-wrapper ABI docs gained the `id \| nth<<32` pack. **F5** a probe failure on input nth 0 no longer suppresses the tablet (`continue` semantics + honest messaging). Verified-sound set + withdrawn candidates: `memory/audit_g7_closed_list.md` (the G-7c round). | Post-fix **ls-gfx-play 8/8 legs PASS attempt 1** (incl. the NEW MOVE-discriminating leg); interactive sweep **5/5** (both blips = the registered #78 bridge signature, guest exonerated ×2 today); `allowance.tla` clean-completes + buggy-violates explicitly re-verified; **SMP gate 40/40, 0 corruption / 0 timing** (default+UBSan × smp4/smp8 N=10); suite 1190/1190 |
| 7e293176 | G-7c | **Mouse-look — the tablet device + the compositor pointer path** (AURORA-CONFIG.md 2.2; task #47). KERNEL: `SYS_PCI_CLAIM` packs `id \| nth<<32` (the 0-based instance selector — pre-G-7c a second same-id function was structurally unreachable; keyboard + tablet are both virtio-input 18); a bare id = nth 0, byte-identical; I-34 resolve/claim coherence by the boot-immutable device table; regression `pci.claim_nth`. TAPESTRYD: `InputDev` dual-probe + the EV_BITS role classify (`supports_abs()` = tablet, order-independent); the tablet drain (EV_ABS accumulate → one coalesced MOVE per EV_SYN, ABS_INFO-scaled; BTN/wheel immediate, pending-MOVE committed first); `Comp::ptr_move/btn/scroll` route to the surface UNDER the pointer (`Layout::surface_at`), translate by the content-origin subtract (the `blit_composed_pixels` inverse; out-of-surface clamps to the far edge), emit the §18.4 wire kinds (MOVE coalescible — pre-wired R2-F4 class; BTN/SCROLL non-droppable). No click-to-focus (chord-driven; Track-B policy). SDL: the pump PTR/BTN/SCROLL arms; relative mode computes deltas driver-side (a `SetRelativeMouseMode` accept-hook keeps SDL core off its warp emulation). HARNESS: `virtio-tablet-pci` in run-vm.sh (the gather manifest collects it, zero manifest change); `qmp-sendtext.sh -p` (abs/btn/wheel); the ls-gfx-play mouse legs. ARCH §25.4 + CLAUDE.md row addenda; docs 139/142 (+ the 142 G-7d-F1 stale-paragraph retro-fix). | suite **1190/1190** in-boot (+`pci.claim_nth`, two-instance leg live) + boot OK; **ls-gfx-play 7/7 legs PASS attempt 1** (tablet click = the BTN gate; wheel notch; `input[0] keyboard / input[1] tablet` classify lines; the MOVE gate landed at the close -- the audit F3 catch); audit + SMP gate = the G-7c close row above |
| 3bac7060 | G-7 play E2E | **The interactive keyboard-play E2E** (AURORA-CONFIG.md 2.1) — the one input leg the timedemo gate never exercised, all asserts deterministic on the serial tee: new-pane focus (no chord needed — `split()` focuses the new leaf) + Super+F zoom; Shift+ESC console (hardcoded toggle) + `version` → "Version TyrQuake-" (un-fakeable by ut — an echo sentinel could false-pass via ut's own builtin); `bind uparrow version` (Key_Bind_f accepts argc 2/3 ONLY — the run-1 lesson) + ONE injected arrow → a fresh banner = the full QMP→virtio→tapestryd→SDL→Key_Event chain + map-signed-on; zoomed dump color floor; `quit` + `whoami`→1000 teardown. Run-2 died to the registered #78 bridge-death (VM alive, guest exonerated — 2 instances today, noted for the #78 owner). | ls-gfx-play PASS (all legs, attempt 1); kernel/userspace byte-unchanged |
| c332dcb9 | G-7d | **The G-7 close — focused audit + SMP gate + docs.** Fable-5-max holotype (MODEL start==end, no fallback) over the G-7 delta (kernel stack bump + proc.c fault-pc, the `0023-pouch-fopen` boundary-line, the `SDL_thylacine` driver) + a concurrent self-audit: **0 P0 / 0 P1 / 1 P2 / 5 P3, NOT dirty.** **F1 [P2, FIXED]** the SDL pump-shutdown "cancel-at-close" comment described a mechanism that DOESN'T EXIST — closing `event_fd` doesn't cancel the pump's parked `t_read` (#844: the pump holds its own ref-held Spoor across the read, so the close hook never runs); the parked read wakes only on a tapestryd event (periodic FRAME), so the shipped 60 Hz teardown was ≤16 ms but rode a runtime-degradable frame clock. FIXED: `StopEventPump` RETIRES the surface (ctl `destroy`) → the event fid reads EMPTY → the parked read returns 0 → the pump exits (bounded, frame-clock-independent); this also eliminates **F5** (the `event_fd` data race). **F4 [P3, FIXED]** `UpdateWindowFramebuffer` additive signed-overflow on a pathological rect → subtract-form clamp. **F6 [P3, FIXED]** exec.h/exec.c stale "256 KiB" docs. **F2 [P3, reworded + #50]** the fopen patch only fixes the READ arm (create-mode fopen/tmpfile still hit the openat O_CREAT gate — fail-closed, no regression; interactive Quake config won't persist). **F3 [P3, #49]** the 1 MiB eager stack's I-32/contiguity cost — graceful; the lazy stack is v-next. Kernel functionally UNCHANGED (F6 comments only) → the G-7b SMP 40/40 carries. Reference docs `142-sdl-port.md` + `143-tyrquake.md` (@126082dd). `memory/audit_g7_closed_list.md`. | suite 1188/1188 + boot OK + 0 EXTINCTION; **ls-gfx-quake PASS** (clean retire-then-join teardown, no hang; fps 122–600 = HVF vCPU variance on an uncapped timedemo, the 969 presents are the deterministic proof); SMP 40/40 (G-7b, kernel unchanged) |
| 908d8bc6 | G-7b | **The Quake gate — software Quake on Thylacine via SDL/tapestry (the §17 acceptance).** TyrQuake 0.71 (GPLv2) vendored pruned-pristine at `third_party/tyrquake` + `usr/ports/tyrquake/patches/0001` (two guards to a build-dir copy: S_ClearOverflow's missing `!snd_initialized` guard — a genuine upstream NULL-deref of an un-allocated `known_sfx` with `-nosound`; + `setvbuf(_IONBF)` so Con_Printf streams past the NQ no-fflush block-buffer stall). `0023-pouch-fopen`: the musl stdio openers (`fopen`/`__fopen_rb_ca`/`tmpfile`) called the RAW `sys_open` macro (`SYS_openat`, unwired 0xFFFF), bypassing the 0009/0021 boundary-line `openat()` — latent until TyrQuake's `fopen(pak0.pak)` found no pak; rerouted through the patched `open()` (SYS_open=65 stalk). `build_tyrquake()` (copy→patch→curated 83-TU glob-compile against `libSDL2.a` → `tyr-quake` 1.8 MB static ET_EXEC; the id shareware `pak0.pak` fetched at build via `bsdtar` over the Deice/LHA resource — no lha dep, sha256-pinned, NEVER committed — staged to `/quake`). **KERNEL (audit-bearing → G-7d): `EXEC_USER_STACK_SIZE` 256 KiB → 1 MiB** (TyrQuake's model loader overflowed 256 KiB into the guard page — correctly caught; 1 MiB eager, the 1 GiB gap above STACK_TOP leaves headroom; the Linux-model lazy demand-grown stack is the tracked v-next lift) + **`proc.c`** `proc_fault_terminate` prints the faulting EL0 pc+lr (from the #88 `debug_trapframe`) — a static non-PIE pouch pc symbolizes directly against its ELF (how the three Quake faults were root-caused; a diagnostic read, no new concurrency). **Live proof (HVF): `+timedemo demo1` → `======= Quake Initialized =======` → 969 frames / 1.7 s / ~600 fps → the textured 3D world on the scanout tiled beside aurora (20 color buckets).** The frame-advance delta is LOGGED not gated (post-timedemo idle timing); the fps line is the deterministic present proof (969 presents can't complete otherwise). | `tools/build.sh all` green; suite 1188/1188 + boot OK + 0 EXTINCTION; **ls-gfx-quake gate PASS** (969 frames @ ~600 fps, `Quake Initialized`, 20/20 color buckets). Full SMP gate + focused kernel-delta audit → G-7d |
| 632961ed | G-7a | **The SDL2 port substrate — the first SDL program on Thylacine (branch gfx-3, off the post-merge main).** Vendored SDL2 2.32.10 pruned-pristine at `third_party/SDL2` (5.4 MB; every kept file byte-identical to the upstream tarball, sha256-recorded; `PRUNE-MANIFEST.md` is the reproducibility contract) + `usr/ports/sdl2/`: the hand `SDL_config.h` (the libsodium no-autotools precedent — musl HAVE_* set + dummy audio/joystick/haptic/sensor/loadso/locale/misc/filesystem + pthread threads + unix timer), the 3-hunk `0001` patch (bootstrap extern + array entry + the SANCTIONED `SDL_dynapi.h` `__thylacine__` off-arm — SDL #errors on a config-forced `SDL_DYNAMIC_API`, so the static-only switch is an in-file platform arm like PS2/Vita; `-D__thylacine__=1` becomes the port-wide platform macro), and the **`SDL_thylacine` driver**: `thyla_tap.{c,h}` (the C tapestry client mirroring `libtapestry::Surface::open_on` 1:1 over plain blocking file ops — the native Loom path is WIRE-IDENTICAL to synchronous read/write, so no C Loom ring; single-slot discipline = tear-free by construction under the stage-0 synchronous present) + `SDL_thylacinevideo.c` (CreateWindowFramebuffer = weave slot 0 zero-copy; UpdateWindowFramebuffer = one blocking tpresent, ≤63 inline damage rects) + `SDL_thylacineevents.c` (a pthread pump parks on the event fid — PumpEvents drains a bounded mutex ring; evdev→SDL via the stock `linux_scancode_table`; rune→SDL_TEXTINPUT; CONFIGURE acks + reweaves on the SDL thread then reports RESIZED). **`0022-pouch-nanosleep`** (musl `__clock_nanosleep` → chunked SYS_TORPOR_WAIT on a never-woken stack word; pre-patch every pouch sleep was ENOSYS and SDL_Delay busy-returned). `t_weft_map` + `T_SYS_WEFT_MAP=82` appended to header-only libt (so pouch C reaches the weave map with zero link boundary). `build_sdl2()` in build.sh (copy→patch→config→glob-compile 130 TUs→`libSDL2.a` 1.5 MB + headers→`include/SDL2/`; zero warnings) + `/sdl-probe` (963 KB static ET_EXEC; quadrants + sweep bar + argv hold). **Live proof, first run**: session `sdl-probe 12` → the compositor TILED the probe beside aurora + CONFIGURE-resized it (the reweave/generation path exercised live: `surface 1 old generation retired`), 42 presents accounted (30 sweep + 12 hold), `PASS driver=thylacine`, clean retire, session continues; the screendump pixel-count asserts all four quadrant colors on the scanout (~124-127K px each). Kernel byte-unchanged. | `tools/build.sh all` green (sysroot REBUILT with 0022); suite 1188/1188 + boot OK + 0 EXTINCTION; the live sdl-probe drive (HVF): SHOWING → mid-hold screendump quadrant-assert PASS → `sdl-probe: PASS driver=thylacine 640x400 frames=30` → surface retired presents=42 |
| 267a6945 | G-6d | **The focused audit + the arc CLOSE + the #41 bridge eradication.** Fable-5-max holotype (`holotype-reviewer`, MODEL start==end==Fable 5, no fallback) over the WHOLE 6a+6b+6c delta with `audit_g3/g4/g5_closed_list.md` as the do-not-re-report preamble + a concurrent self-audit: **0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty.** **F1 [P2]** the pane/layout tree is conn-GLOBAL — a session peer could `close`/focus-steal another client's pane (the console renderer aurora exits on `TEV_CLOSE` → the graphical console darkens). This is the intended WM-control model (control is 9P / layout-as-9P; the i3-IPC/tmux shape), NOT a per-surface ACL — a partial owner-scope would break it (the no-overfit trap). CLOSED-with-justification + tracked (task #42): the v1.0 trust boundary is the per-territory `/srv` (I-1/I-28 — `/srv/tapestry` is in the driver's own territory, unreachable from a user session's namespace, so only the trusted boot chain connects; no untrusted tapestry client exists at v1.0); the per-client layout-control capability + renderer-role protection + the D5 read ACL are the Halcyon-era multi-untrusted-client fix. **F2 [P3]** focus-change plain-key half-pair leak (only swallowed chords track press-state → a held key's release routes to the newly-focused surface; benign for aurora, a raw-evdev client stuck-key; task #43). **F3 [P3]** global `clock-rate` is same-session-trust (doc'd). **F4 [P3]** the 24-bit pane-qid vs 32-bit id divergence past 2^24 allocs → a PIN comment at `make_pane` (unreachable; no crash/alias). **F5 [P3]** single-point pixel asserts + the 64-MiB triple-buffered-weave cap on 4K displays (graceful E_NOMEM; task #44). **The #41 LS-CI bridge cluster ERADICATED**: the instrumented `bridge exit=141` (SIGPIPE) with the VM still `RN+` proved the guest exonerated (the nc bridge died under the boot-output burst, not the guest); the fix is the purpose-built **`serial-bridge.py`** (the #66-comment relay finally built — SIGPIPE-immune via Python-default-ignore + a bounded-park level-triggered drain + blocking-stdout back-pressure), replacing the fragile `nc -U`; proven byte-perfect on a 200 KiB ANSI burst; ls-gfx-panes 3/3 deterministic FAIL → clean first-attempt PASS. Scripture: TAPESTRY.md §18 (F1 weighing) + the ARCH §25.4 G-6 addendum + the CLAUDE.md mirror + 139-tapestryd.md "Control scoping". Kernel byte-unchanged (comment + harness + docs) → the standing SMP records carry. `memory/audit_g6_closed_list.md`. | suite 1188/1188 + 0 EXTINCTION + boot OK; **ls-gfx-panes clean first-attempt PASS** (all 22 legs) + ls-gfx PASS + ls-gfx-live PASS (retry-tolerated #31 VNC leg; the bridge now fails CLEAN exit=0, guest RN+/SN+ exonerated) + ls-ci PASS (the shared-bridge non-gfx validator); spec `tapestry_present` clean **5413 UNPERTURBED** + liveness + 4 buggy cfgs; both cargo feature variants compile; kernel byte-unchanged → standing SMP 40/40 (G-5) carries |
| 1e259a36 | G-6c | **The interaction layer: chords, focus events, tab/stack visuals, move/zoom, multi-rect, determinism mode** (TAPESTRY 18.9 G-6, third sub-chunk; §14/§18.4/§18.6 + D4/D6/D7). **The Super chord plane** intercepted in the input drain ABOVE the event stream (`Comp::chord_key` + a 256-bit swallow set: while Super is held EVERY non-modifier key is compositor input — bound chords act, unbound drop, none reaches a surface; a swallowed press swallows its release/repeats even past Super-up; a pre-Super press keeps flowing; modifiers always flow). Baked i3-flavored table: Super+arrows spatial focus / +Shift move / h,v split / f zoom / t,s tabbed,stacked / e split-toggle / Tab cycle / Shift+q close. **TEV_FOCUS** (value 1/0, F5 never-drop) emits from the single `focus_sync` at the reconcile tail (`last_focus` dedup; retire clears a stale slot ref so a re-minted surface still gets gained). **Tab/stack strips** (D7 glyph-free): `TAB_STRIP_H=5` rows carved in `layout_pane` (tabbed = one segment row w/ 1px gaps; stacked = a row per child); `paint_strips` colors active-on-focus-path FOCUS / active-off-path ACTIVE / inactive BORDER; chrome-only (focus-only epochs repaint them; client memory never touched — tearing-freedom intact). **Spatial focus** (`focus_dir`: nearest visible focusable leaf in the direction, orthogonal-overlap gated). **Directional move** (`move_dir`, D6): sibling swap in the matching-axis ancestor (Tabbed=h, Stacked=v — moves walk tab order) / edge-escalate / nested pull-out beside the subtree (dissolution-safe index bookkeeping) / root-wrap on a pure cross-axis move / screen-edge no-op. **Zoom** (`zoomed_id` by public id, tmux-shaped): the leaf alone fills the display through the ordinary mode machine (a display-sized surface goes DIRECT); structural verbs + focus-elsewhere auto-unzoom; stale target self-clears. **Multi-rect presents**: rect_count k>=2 rides rects 1..k INLINE after the header (payload 32+16*(k-1), cap 64, every rect validated up front) — the as-built D4 compositor case superseding the provisional `buf_idx_or_off` sketch (under D3 the payload already rides the registered buffer); `libtapestry::present_rects` (EV_OFF 64→1024, client cap 63). **Determinism mode** (§18.6+F13+F15) behind the default-on `test-mode` cargo feature (production strips → E_OPNOTSUPP, #880): `test-mode on` freezes the FRAME clock (wall ticks skip; queued FRAMEs drain = F15 for a synchronous engine; anchor re-seats on unfreeze), `tick` steps time, `TPRESENT_HOLD` runs its pixel work INSIDE the dispatch (tearing-freedom holds) deferring only the device push into `Surface.held`, released by ctl `release [<n>]` (F2 ownership-gated; holds union; non-HOLD present / `test-mode off` release implicitly; hold during a pending direct switch → E_AGAIN; a mode-staled hold drops). Layout grammar grows `move <id> <dir>` / `zoom <id>` / id-less `focusdir <dir>` / `tab next\|prev`; pane ctl mirrors. `qmp-sendtext.sh -k` = QMP chord injection; lib.exp gains the #41 bridge-exit instrument (nc stderr+rc captured; `bridge-at-fail` logged). Kernel byte-unchanged. Self-audit caught the move/hold sample-point bug pre-formal (pane taller than the surface → center below content; fixed to covered-region sampling) + the split-serial-line regex hazard (all coordinate regexes line-anchored). | suite 1188/1188 + boot OK + both E2E legs + the `-c` per-boot gate; **`ls-gfx-panes.exp` grows nine G-6c legs** (TEV_FOCUS-at-create; multirect green+yellow quarter pixels; tab-strip segment colors EXACT [58,58,68 / 122,158,204]; tab-cycle; zoom corner pixel + exact geometry restore; move order + new-center pixels; QMP `meta_l+left` chord → focus moved + no arrow leaked to B; test-mode frozen-tick asserts; hold BLUE-then-MAGENTA bracket dumps) + PASS; ls-gfx + ls-gfx-live PASS (fullscreen direct byte-identical); spec `tapestry_present` clean **5413 UNPERTURBED** + liveness + 4 buggy cfgs firing (multi-rect/HOLD below the Submit→Complete abstraction — the SPEC-TO-CODE G-6c note); userspace-only delta → the standing SMP records carry |
| e2161294 | G-6b | **The resize protocol + pane close: weave generations** (TAPESTRY 18.9 G-6, second sub-chunk; §18.3). A surface's `weave`/`resource_id` name its CURRENT generation; a resize acks onto a NEW one. GPU resource ids are PER-GENERATION (`Comp::next_res_id`, minted above SCREEN_RES — a fresh resource never aliases the old, closing the #317 stale-content class); `Comp::alloc_weave` (the shared body of `create` + `resize_ack`) allocates a full generation, `Comp::release_gen` tears one down in the R2-F5 order (unshare → detach → unref → burrow_detach → close). A size-changing CONFIGURE offer records `offered = (serial, w, h)` (`emit_configure_to`; only the latest is ackable — coalesce-by-replacement); the structural composed repaint fans every visible hosted surface its pane's EXACT content size. **THE GENERATION FENCE**: `resize W H <serial>` on the surface ctl → `Comp::resize_ack` mints the new generation FIRST (a failure leaves the current one untouched, the offer standing), swaps the surface onto it + marks `res_stale`, and ONLY THEN does `h_write` send the Rwrite (reply-after-alloc, R2-F5) — the conn stream is FIFO, so every post-ack present validates + blits against the new geometry. Bounded to <=2 generations (`old_weave.is_some()` → E_AGAIN); a stale serial (`< cfg_serial`) → E_AGAIN, an unknown/mismatched echo → E_INVAL — none consume the offer. The displaced generation drains PASSIVELY (its last content stays displayed, never read again — tearing-freedom holds) and retires at the FIRST post-fence present (`Comp::present` tail: the display shows g2 content — a composed blit COPIES into the screen, a direct arm targets g2's resource — so quiesce holds by construction; the spec's `RetireDisplaced` + `ServerRelease`); a surface retire with g1 draining releases it in `Comp::retire`'s tail. Client: `libtapestry::Surface::handle_configure` → `reweave` (write the ack → open a FRESH weave fid → re-map → clunk the old fid; map-new-before-clunk-old keeps the client mapped throughout — #847 keeps g1's pages until the clunk; `cur_slot` restarts at 0). Aurora deliberately does NOT ack a size change (fixed cell grid → crop/ignore posture) + its per-CONFIGURE diagnostic DROPPED (it shares `/dev/cons` with what it renders → chatter interleaves byte-for-byte with a concurrent writer, `t_putstr` not cross-Proc atomic; the run measured the mangling). Pane close → `Comp::send_close` delivers a queued `TEV_CLOSE` exit REQUEST (distinct from a retired surface's stream-end EOF) — close is a request, never a forced retire (the client may need to save). `weft.tla`/`tapestry_present.tla` UNCHANGED (the reweave is already modeled with `ALLOW_REWEAVE`; the SPEC-TO-CODE reweave map records the action↔site mapping). Recorded seam: `weave_va_next` is a monotonic bump (no free-list; bounded — a display weave ~12 MiB, the 47-bit VA holds millions of reweaves — a v1.x free-list closes it). Self-audit clean (no P0/P1/P2; two documented seams — the VA bump + a client error-after-ack blank-frame-until-recovery). Kernel byte-unchanged. | suite 1188/1188 + boot OK + both E2E legs + the `-c` per-boot gate; **`ls-gfx-panes.exp` grows the G-6b legs** (resize negative probes [stale serial→E_AGAIN, unknown→E_INVAL] + the exact-fit reweave [B reweaves onto its pane; the pixel asserts sample the exact-fit result] + the TEV_CLOSE leg) + PASS (one #41 bridge-EOF retry, guest exonerated — attempt 1 `R+` running); **ls-gfx + ls-gfx-live PASS** (fullscreen direct byte-identical); spec `tapestry_present` clean **5413 UNPERTURBED** + liveness + 4 buggy cfgs firing; userspace-only delta (5 files) → the standing SMP records carry |
| fd5a3646 | G-6a | **The compositor stage 1: the pane tree + multi-surface composition** (TAPESTRY 18.9 G-6, first sub-chunk). `pane.rs` = the i3 container model (one primitive, mode splith/splitv/tabbed/stacked, nestable; flatten-same-mode/nest-different-mode splits; split focuses the NEW empty leaf = the auto-host target; collapse; root persists; monotonic never-reused pane ids). Surfaces host at CREATE (focused-empty-leaf else split-focused by aspect). Scanout = a mode machine (Boot/Off/Direct/Composed): single-visible-display-sized keeps the stage-0 DIRECT zero-copy path byte-identical (aurora boot unchanged); everything else composes into a compositor-owned SCREEN buffer (WEAVE-subtype DMA — the G-2 type discipline: scanout backings are the weave class; run 1 MEASURED the plain-DMA 1 MiB `KOBJ_DMA_MAX_SIZE` reject; never registered → no share_id to claim). Every switch onto a client resource rides a present-COMPLETE (F16 uniformly, `pending_direct` + the `res_stale` full-transfer). THE TEARING-FREEDOM INVARIANT: client weave bytes are read ONLY inside the present dispatch for the just-presented slot; chrome never touches client memory; a geometry-signature split keeps focus-ring moves from blanking idle content. **The §18.3 CONFIGURE emission half PULLED FORWARD** (chunk-completeness; run 2 measured: aurora is an ACCUMULATOR client — slots are patchwork, static rows never healed): same-size CONFIGURE = the redraw request (structural composed repaints → all visible; pending-direct entry → the target; coalesce-by-replacement; non-droppable wedge class) + aurora's minimal response (same-size → all rows dirty; size-change logged+ignored → G-6b). 9P: `layout` (tree text read; `split/close/focus/mode` writes) + `pane/<id>/{ctl,mode,role,tag,surface,geometry}`; pane ids bit-41 qids, never reused; pane files GLOBAL (the environment-role mutation gate + the D5 layout-observability caveat = the recorded Halcyon-era seam). Key routing → the focused leaf; FRAME → visible surfaces only; retire gains step 0 (the hosting leaf closes) with the scanout force-away arms explicit. Tools: screendump `-P` PPM capture + `tools/ppm-sample.py` + lib.exp `vm-at-fail` stat evidence (task #41: one run's 3-attempt bridge-layer EOF cluster, guest exonerated by the direct-serial differential). Deferred-recorded: the pipelined-controlq drain (synchronous engine KEPT — quiesce-by-construction stands); resize-ack/reweave + TEV_CLOSE (6b); chords/tabs-visuals/move/zoom/HOLD/multi-rect (6c); aurora reweave (crop/letterbox until then); equal-ratio splits only. | suite 1188/1188 + boot OK + both E2E legs + the `-c` per-boot gate (direct-serial differential, same bits); **`ls-gfx-panes.exp` NEW + PASS** (in-guest structure asserts [layout text vs pane geometry files, disjoint rects] + exact center pixels via `-P`/`ppm-sample` [A red, B blue] + QMP focus legs [A via the layout file, B via its pane ctl] + the collapse coda [`-c` passes again]); **ls-gfx PASS + ls-gfx-live PASS** on the delta (HVF); the offline edge regression OK; spec `tapestry_present` clean **5413 UNPERTURBED** + liveness + 4 buggy cfgs firing (no model change — the pane layer sits above the spec's abstraction); userspace-only delta → the standing SMP records carry |
| 83242c35 + 1bed1867 + d28cf1a0 | G-5 | **The graphics audit round** over the five never-audited post-G-4 commits (#31/#30/harness/#35+#36/#37), Fable-5-max holotype (MODEL start==end, no fallback) + concurrent self-audit: **0 P0 / 0 P1 / 1 P2 / 4 P3, NOT dirty; all findings + one self-audit item FIXED, none deferred**. F1 [P2] the 16-stale-wake dead-latch was a false-dead landmine (a window-resize config-IRQ train during one slow-but-healthy present could exhaust the count → permanent console loss, the exact #31 symptom) → the wedge bound is now WALL CLOCK (`SUBMIT_DEADLINE_MS` 500 ms, lazily anchored at the first stale wake — trips only on event-ful non-progress, 5+ orders over the µs-scale retire). F2 [P3] the spin-break path left the completion's INTx level pending (one systematic stale wake per completes-during-spin present) → post-loop read-to-clear. F3 [P3] the dead paths narrow the I-40 quiesce basis to timeout≫latency + #847 (no reachable UAF; the retire ordering unconditional — unshare/free are syscalls, not controlq commands) → recorded in 139-tapestryd.md; the controlq drain/reset stays the G-6 obligation. F4 [P3] the tombstone-rebind test pins the capacity-INDEPENDENT asymmetry, not the raise (the cora boot leg is the capacity guard) → test header says so. F5 [P3] DECAWM missing from the DECSC/DECRC saved state (DEC STD-070/xterm save autowrap with the cursor; 1049 = implicit DECSC/DECRC) → `saved_wrap` on ESC 7/8 + alt enter/leave; CSI s/u stays position-only (kaua saves via `[s`/`[u` → its ?7l never disturbed) + a dormant regression. SA-2 (self-audit; the prosecutor lacked it) the CPR reply write silently discarded → logged. **Deliverables alongside**: @83242c35 the `-c` blend-integrity edge pass (the deferred #35 gate blindness: exact-fg-adjacent edges must sit in the [bg,fg] envelope, >5% out fails; junctions ~2% vs the buggy formula ~14-15%; `-F` offline mode + `tools/test-screendump-edge.sh` non-vacuity regression; live frame 424 edges/0 out); @1bed1867 #34 LS-CI failed-attempt evidence preserved. `memory/audit_g5_closed_list.md` = the G-6 preamble; task #39 enqueued (the aurora host-test harness — four dormant vt/render regressions wait on it). | suite 1188/1188 + boot OK + both E2E legs + the hardened `-c` gate (HVF); ls-gfx + **ls-gfx-live** PASS on the FIXED wait loop; the offline edge regression OK (bad 14% out → FAIL on the blend arm); spec tapestry clean 5413 UNPERTURBED + liveness + 4 buggy firing; userspace-only delta → the standing SMP records carry (30-boot amplifier + 40/40 + 20/20) |
| 9b2b6f74 | #35+#36 | **The user's second live-driving pair** (cocoa vs Ghostty). **#35 aurora blend lane-bug**: `render.rs::blend` divided the packed R\|B word by 255 — division doesn't distribute over lanes (65536 ≡ 1 mod 255) → every antialiased EDGE pixel's B absorbed R's lane (interiors exact via the a=0/255 short-circuits — exactly why the `-c` near-grey-interior gate was blind); thin glyphs (`⊢`) read wholesale violet, warm colors fringed ("something with the oranges/yellows"). Diagnosed by pixel-sampling the user's screenshot (Ghostty exact 224,120,64; QEMU 197,106,223) + a live screendump (framebuffer-side → cocoa exonerated) + numeric closure (the buggy form reproduces the exact sampled wild pixels (120,116,6)@a=127 + (174,168,239)@a=191). FIX: the lane-safe `na=256-a`,`>>8` form (ideal ±1) + a dormant cfg(test) regression; post-fix screendump edges hue-consistent ((170,92,50) = 0.76×spec on ALL channels). **#36 bare-LF session output**: the `ls` staircase was NOT aurora (vt.rs CR-correct) and NOT the drain (the tee is byte-exact) — the raw serial capture proves the GUEST emitted bare LF for all session output (ut PROMPT_MODE + login MODE_DEFAULT carried `-onlcr`), masked for the project's whole lifetime by QEMU's `mon:stdio` mux re-inserting CRs for terminal viewers; the fbcon is the first honest raw renderer. FIX: PROMPT_MODE/MODE_DEFAULT → `+onlcr` (output cooking ⊥ raw input; the editor's explicit `\r`+CSI is ONLCR-proof); RAW_MODE (TUIs) + CHILD_MODE unchanged; 5 drift asserts + docs updated; the kernel BOOT default deliberately untouched (compat-pinned LS-8 surface; POSIX-parity recorded for a future pass). Gate hardening (saturated-color check) → G-5. Kernel byte-unchanged. | post-fix screendump: aligned listing + orange prompt, pixel-verified; test.sh PASS (1181/1181 + both E2E legs + `-c`); ls-gfx + ls-7 PASS |
| 4c112c6b | #30 | **cora's unprovisionable home** (USER-REPORTED: `listen on /srv/home-cora failed: Permission denied`, rc=-207, while michael's home bound fine). Root: the ONE shared boot `SrvRegistry` (every getty login `territory_clone`s joey's territory → the same devsrv root Spoor; the "per-session registry" design note was never built) + tombstones-never-free + `SRV_MAX_SERVICES = 8` = EXACTLY full at the login prompt (6 permanent posts [stratum-fs, stratum-ctl, corvus, net, ptyfs, tapestry — the ptyfs+tapestry arcs took slots 7→8's neighbors] + the `pouch-sock-demo` probe tombstone + the login-E2E's `home-michael` tombstone). michael forever REBINDS his tombstone by name (no free slot needed); any OTHER user's fresh `/srv/home-<user>` → `srv_reserve_in` no-slot → `devsrv_create` −1 → the pouch bind's documented EACCES. Ground-truthed live: pre-fix boot reproduced the exact user failure on cora's FIRST interactive attempt while michael succeeded in the same boot. FIX: `SRV_MAX_SERVICES` 8→16 (the ledger + raise discipline in the header comment: each unit costs `srv_registry_drain` kernel stack) + the boot login-E2E parameterized to run a SECOND user (cora AFTER michael — a fresh-name post past the accumulated tombstones, so a capacity regression fails the BOOT) + `devsrv.registry_full_tombstone_rebinds` (the at-capacity asymmetry pinned as tested behavior). The real fix — entry-free-at-last-handle-ref (or the per-session registries the A-5b-body note intended, which also closes the cross-session /srv name visibility) — is the recorded v1.x seam (task #33). Also surfaced: devsrv has no `.readdir` (`ls /srv` → I/O error; task #32). | 1181/1181 (+1: the asymmetry test) + boot OK with BOTH E2E legs (`login E2E OK for michael` + `for cora`); post-fix in-guest E2E: cora authed + home bound + `pwd`→`/home/cora` + logout + michael bound in the SAME boot; the 16→8 revert-probe fails the boot LOUDLY (with the cora leg's tombstone in the ledger, an 8-slot registry fills before ptyfs's own post — `post /srv/ptyfs FAILED` → boot-fatal; a capacity regression cannot slip through as a silent user-facing denial again); the reduced SMP amplifier: default-smp8 10/10 + ubsan-smp8 9/10-then-10/10 = **0 CORRUPTION in 30 boots** — the single first-run ubsan-smp8 OTHER was a post-banner harness-verdict artifact on a PROVEN-fully-healthy guest (1181/1181 + boot OK + 0 EXT + aurora up in its own log; nonrepro on a visible manual run AND on the full N=10 re-run), undiagnosable-in-detail ONLY because smp-multiboot discarded test.sh's stdout — the harness now tees it to `test-harness.log` + keeps it per failed boot (landed with this row), so any recurrence names the exact step (the gpu-gate `-c`/liveness compare is the identified candidate); retention of prior-run FAILDIR evidence = task #34 |
| b1d16c0e | #31 | **The live-display controlq fix** (USER-REPORTED under `-display cocoa`: ~2s of console then scanout death). Root: `gpu.rs::submit_and_wait` broke its wait on the ISR HINT then asserted a SINGLE `used.idx` read — a mis-timed wake (stale latched INTx edge / mid-propagation store, routine under a live display backend) read `used.idx` behind → hard-fail → `seq` desync → every later command re-published a consumed avail idx + read its own zeroed response as `resp_type=0x0` (the whole observed cascade, mechanically confirmed). FIX: the used ring is the completion authority — re-read behind `virtio_rmb` per wake + a bounded spin (`USED_SPIN_PER_WAKE`), bounded by `MAX_STALE_WAKES_PER_SUBMIT`; any submit failure LATCHES `Controlq.dead` (fail-fast, no garbage cascade). Aurora: a failed present = a DROPPED FRAME (dirty rows kept → re-render+retry; decay-logged; 240-consecutive backstop; event-stream-EOF still exits; first present stays fatal). The launcher grows first-class display modes: `THYLACINE_DISPLAY=none\|cocoa\|vnc:N` (subsumes the user's local cocoa edit; vnc drops `gpu-mmio0` so gpu0 binds QemuConsole 0) + `tools/rfb-refresh.py` + **`ls-gfx-live.exp`** (the live-display CI leg the headless gate lacked: RFB attach asserted on the gpu0 geometry + session churn under 45s of real display traffic + zero-desync sweep + console-survives). HONEST LIMIT: three escalating headless repro attempts (screendump hammer @66/s = 2x cocoa cadence; a REAL live VNC backend on the right console at 25MB of dirty-rect traffic) never fired the pre-fix bug — the trigger needs the cocoa backend's thread topology; the leg is standing coverage, the cocoa acceptance is the user's window. Kernel byte-unchanged. | test.sh PASS (boot OK + the `-c` gate; exit 0) + ls-gfx PASS + **ls-gfx-live PASS** + a post-fix manual VNC stress run clean (3.3k update-reqs, 45 bursts, zero desync strings) |
| 2961256e | G-3d | The audit close (Fable-5-max holotype, MODEL start==end, + concurrent self-audit): **0 P0 / 0 P1 / 1 P2 / 3 P3, NOT dirty, all addressed** -- F1 [P2] the reaper's per-page TLBI unmap ran IRQs-OFF under gptl (proc_for_each is irqsave) -> FIXED via the find-and-lock callback (vma_lock taken under the walk, HELD PAST it; the unmap runs IRQs-on under vma_lock alone) enabled by `vma_drain` now TAKING `p->vma_lock` (exemption retired; pgtable_destroy is after vma_drain in proc_free, so pgtable_root stays valid); F2/F4 contract rewording (the budget DOES charge the orphan; liveness = "a client whose session OBSERVED the death"); F3 global FRAME coalesce; + self-audit SA-12 (exact gather guard) + SA-15 (the libtapestry `closed` latch). `memory/audit_g3_closed_list.md` = the G-4/G-5 preamble | 1173/1173 + boot OK + the evolved gate GREEN on the fixed code; the reduced amplifier (default-smp4 + ubsan-smp4 N=10) 20/20 PASS 0 corruption on the close delta (the FULL 40/40 ran on the pre-fix commits) |
| 7f0a1b1d | G-4a | The `/dev/cons` drain/feed renderer backend: the `cons_emit` mirror TAP (output + echo; the tee -- serial byte-identical) into a bounded drop-oldest ring + the feed into the UNCHANGED LS-8 discipline (`is_break` hardwired false -- no SAK forgery) + `SPAWN_PERM_CONSOLE_RENDERER` (the I-27 THIRD role: console-attach-only + single-holder CAS) + the devdev `consdrain`/`consfeed` leaves (open + per-I/O + poll gates) + the LS-8a deferred poll-wake second instance | 7 new kernel tests; suite 1180/1180 |
| d3c51ee0 | G-4b | The Cornucopia bake: `tools/bake-cornucopia.py` (outline flatten -> nonzero scanline fill at 4x supersample -> 8-bit alpha) -> the COMMITTED atlas (`usr/lib/cornucopia`, 207 glyphs, cell 10x22 baseline 18 from the font's own metrics); box drawing deliberately procedural | host-side; atlas visually verified (ASCII-art render) |
| 9a9b6d29 | G-4c | `usr/aurora` -- the renderer MVP: VT subset (truecolor SGR, real alt-screen, DECSTBM-ignored seam) + Bonfire palette + atlas blit + procedural U+2500-259F + blinking cursor; the loop BLOCKS on the Loom CQ (the non-SQPOLL pump -- a poll-only loop starves its own completions, measured); joey spawns it perm-granted as the resident boot presenter (demo stays baked, no longer spawns); run-vm keeps kbd-pci0 (un-targeted QMP injection reaches it under -nographic) | first integrated boot: "Thylacine login:" in Cornucopia at 128x36 + blink + QMP-typed input echoed on BOTH sinks |
| 464a24ac | G-4d | The fbcon gates: `screendump.sh -c` (the Aurora CONSOLE signature -- exact Bonfire bg dominant + exact-fg text; content-independent, deterministic) + `test.sh`'s per-boot gate -> `-c` + a bounded retry-COMPARE liveness (cursor blink / prompt arrival); `qmp-sendtext.sh` (un-targeted QMP typing); `ls-gfx.exp` (the E2E); docs (140-aurora.md new + TAPESTRY 18.7 + ARCH 25.4 + I-27 3-role + CLAUDE mirror + TOOLING) | **ls-gfx PASS under HVF** (login + ls rendered [-c + differing dumps] + QMP-typed pwd -> /home/michael on the serial tee) |
| 176d982a | G-4e | Self-audit hardening: proc_set_console_renderer magic/ALIVE asserts + the cons_drain_open reader_busy epoch soundness comment (the #844 pin + #811 death-unwind ordering excludes a prior-epoch reader) + the CLAUDE.md audit-trigger mirror row | suite 1180/1180 + boot OK (HVF) |
| 5cc97785 | G-4f | Audit CLOSE: Fable-5-max holotype 0 P0 / 0 P1 / 1 P2 / 2 P3, NOT dirty (kernel drain/feed/gate/role surface CLEAN). F1[P2] aurora VT erase(1) OOB (deferred-wrap cx==cols -> [..=len] panic, reachable from any console writer) -> clamp + vt.rs tests (dormant host-harness seam); F2[P3] cornucopia verify() extents/geometry; F3[P3] drain single-open footguns -> DEFERRED task #29. ALSO SMP-hardened the PRE-EXISTING srvconn.client_send_blocking_poll_edge (smp8 exact-sched over-specification; task #28) | 1180/1180 + boot OK + spec GREEN; pre-fix SMP 0-corruption/40; smp8 re-gate 20/20 PASS (ff35eab7) |
| 88547181 | G-3a | tapestryd stage 0: NEW `usr/tapestryd` (gather-bound persistent compositor -- the absorbed gpud GPU half generalized [per-surface resources, whole-weave backing, offset TRANSFER, the retire pair]; poll-mode virtio-keyboard-PCI eventq; the /dev/tapestry 9P server with the I-40 present engine [synchronous presents => the quiesce set empty at every retire BY CONSTRUCTION; UNSHARE-before-free retire ordering; scanout at first-present-COMPLETE], F2 per-conn scoping + generation gates, F9 caps, R2-F4 WEDGE; the US-QWERTY keymap + FRAME clock) + NEW `usr/lib/libtapestry` + `usr/tapestry-demo` (the POC model cashed onto libthyla_rs::loom; quadrants + live plasma -- the G-2-F2 Tweft round-trip E2E discharged) + the libdriver/warden `gather = all` mode (resolve_gathered + pci_extra + the pcix codec key; ONE grant over both PCI functions) + the devdev tapestry mount stub + the joey mount/probe/spawn + virtio-keyboard-pci in run-vm.sh + the EVOLVED pattern gate (liveness double-dump) + gpud retired | Suite 1173/1173 + boot OK at 1280x800; the gate GREEN (quadrants through the full path + the dumps differ); 86/86 libdriver host tests incl. 4 gather legs; spec gate GREEN (tapestry 5413 unperturbed + liveness + 4 buggy; weft + readiness families) |
| 6140cef2 | G-3b | The R2-F3 orphaned-weave reaper (the ServerDeath leg's kernel half): WEAVE bindings register at the SYS_WEFT_MAP CAS-win / unregister at dev9p_close; a kproc kthread (parks empty; 1 s cadence) force-reclaims a binding whose serving session is dead past the 2 s grace -- cross-Proc unmap under gptl+vma_lock with the G-2-F1 identity guard re-checked, budget uncharged, the registration pin dropped (the chunk frees AT reclaim); `g_weft_reap_lock` serializes reaper-vs-close (burrow NULLed vs unregister-before-read) | The 3 sweep-driven regressions `weft.reap_{orphan_reclaimed,live_session_untouched,close_unregisters}` (in the 1173) |
| b2c8a6f5 | G-1 | The resident GPU driver: NEW `usr/gpud` (libdriver persistent; the netd precedent) — probe = `PciDev::claim(16)` + the common-cfg handshake + the 7-command 2D bring-up (the P4-L pattern); serve = READY then the 2 Hz render loop (8×8 center heartbeat, compile-asserted clear of the `-v` sample points; partial-rect TRANSFER+FLUSH, IRQ-completed). **The transport pivot, measured not theorized**: a persistent claimant on the shared virtio-mmio slot page (`0xa003000`, all six devices, page-exclusive claims) starved netdev-driver AND stratumd's disk (rc=-207 boot-fatal) → **virtio-gpu-pci** (`virtio-pci:16`, own BARs — the netd/#140 move); the MMIO device stays as `gpu-mmio0` for the one-shot kernel-test probe. crash-probe re-homed to the warden-SYNTHETIC `restart-test` node (F15: resource-less DeviceNode; grant mmio=0 irq=0 dma=0). The **pattern-persists gate** in `tools/test.sh` (screendump `-v` post-banner, bounded retry, `gpu-gate` FAIL arm; skips under NO_GPU/NO_QMP/GPU_GATE=0) — runs in every ci-smp-gate boot. TAPESTRY §18.9 as-built note (G-3's tapestryd binds `virtio-pci:16`); `docs/reference/138-gpud.md`. Kernel byte-unchanged. | Suite 1170/1170 + boot OK; the pattern gate GREEN inside test.sh; ecosystem restored (netdev ARP 24/24 + stratumd serving + crash-probe synthetic ladder); liveness pixel-proven (block(64,64) yellow↔black across a tick, neighbors stable); reduced SMP amplifier |
| 5317ea7d | G-0 | The "agentic eyes" capture step: `tools/screendump.sh` — QMP `screendump` to PNG over the standing `build/qmp.sock` (P4-K wiring), targeting the `gpu0` qdev id; `-v` asserts the P4-L 4-quadrant pattern via a PPM sibling dump (quadrant-center sampling, tol ≤8). TOOLING.md §3.1 addendum. **Headless capture EMPIRICALLY PROVEN**: `VERIFY OK — 128x128 shows the P4-L 4-quadrant pattern` captured under the standing `-nographic` boot (no display backend, no VNC — a VNC-listener differential confirmed listener-independence; HVF and TCG both). **The investigation's load-bearing finding**: a post-reap capture reads a blank surface NOT because headless capture fails but because the **RW-7 proc-death quiesce** (`kernel/virtio.c::virtio_mmio_reset_in_range`) resets the dying driver's virtio devices (DMA soundness: no in-flight device write into freed KObj_DMA pages) — a virtio-gpu reset destroys host-side resources + disables the scanout. Ground-truthed by a held-alive instrumented probe run: guest read-back = pattern, QEMU `xp` at `fb_pa` = pattern, screendump = pattern while alive; black only after reap; kernel/test/QEMU all exonerated as bugs — the behavior is correct + audited. So **a scanout lives exactly as long as its driving Proc** — the persistent capture target is G-1's resident driver (its "pattern persists" gate re-runs `-v` for keeps), and a compositor crash blanks the display until warden restarts it (the TAPESTRY crash contract's visible half). The one-shot probe gained only a comment recording the caveat; instrumentation reverted. | The captured artifact `build/screendump-g0-held.png` (VERIFY OK, all four quadrants exact); harness exercised against live/absent/reaped-scanout VMs; suite + boot unchanged (kernel byte-identical; probe binary byte-identical minus comment) |
| 6599519d + the close commit | G-2 | The DMA-weave share admission: `SYS_DMA_CREATE_WEAVE` (99; the kernel-minted create-immutable `KObj_DMA.weave` subtype — the §18.12 R2-F1 structural close; ABI user-signed-off 2026-07-19) + the `burrow_share_into`/`SYS_WEFT_SHARE` admission gates (ANON or weave ONLY; device-command DMA as unshareable as MMIO) + `weft_claimed_kind` (type-authoritative kind + server-declaration cross-check) + the WEAVE binding kind (no ring view; the kind-gated `validate_rw` closes all three Tweftio fast paths — §18.11 F10) + `SYS_WEFT_UNSHARE` (100; the F3/R2-F5 disarm: removal-before-free + fail-closed late claim = the `Map`-guard NoStaleMap kernel half; closes #289) + the per-client shared-in budget (`Proc.shared_map_pages` @348, 128 MiB, the I-32 fifth axis — R2-F3) + the weave-fid clunk-unmap (`ClunkMap`, F1-identity-guarded via `weft_binding_clunk_unmap`) + `libthyla_rs::{t_dma_create_weave, t_weft_unshare}`. ARCH §28 **I-40** enumerated (share half ENFORCED; present half at G-3); CLAUDE.md + ARCH §25.4 Weft-row addenda; `docs/reference/125-weft.md` "The weave share (G-2)"; SPEC-TO-CODE `tapestry_present.tla` section. **AUDIT CLOSED CLEAN: Fable-5-max holotype (MODEL start==end) + concurrent self-audit — 0 P0 / 0 P1 / 0 P2 / 3 P3, NOT dirty** (F1 stale-VA clunk-unmap → FIXED + revert-probed regression; F2 integration coverage → the clunk-unmap half discharged, the Tweft-round-trip E2E G-3-owed; F3 bare-pid → closed-as-documented). `memory/audit_g2_closed_list.md`. | 1170/1170 (+5: the 4 gate/budget regressions + `weft.weave_clunk_unmap_guard`; BOTH probe families revert-probed — the gate weakenings → 1167/1169, the F1 guard drop → 1169/1170, each failing exactly its targets) + boot OK; spec gate GREEN (tapestry 5413 + liveness + 4 buggy firing; weft 1412 + readiness re-ran); FULL SMP gate 40/40 PASS 0 corruption + the reduced amplifier on the F1 delta |

## Remaining work

| # | Chunk | Scope | Gate |
|---|---|---|---|
| G-5 | Graphics audit round | The reserved rows enforced; the I-40 present half + tapestryd/cons-backend focused audit (the #31 controlq wait-loop rework joins the scope); SMP gate | clean close |
| G-6..G-9 | Phase 10 | Compositor (pane/layout) → SDL+Quake → Halcyon core → integration (§18.9) | per row |

## Trip hazards

- **The RW-7 proc-death quiesce blanks a dead driver's display** (G-0
  finding): `virtio_mmio_reset_in_range` resets every virtio device in a
  dying Proc's MMIO window — correct + load-bearing for DMA soundness, do
  NOT weaken it for display persistence. Consequences the arc must build
  around: (a) the scanout lives exactly as long as its driving Proc — G-1's
  driver must be persistent; (b) a compositor crash → quiesce-blank →
  warden restart → re-init repaints (the TAPESTRY crash contract's visible
  half); (c) `tools/screendump.sh -v` against a one-shot probe races its
  reap — only assert `-v` against a resident scanout owner.
- **A persistent driver CANNOT claim a shared virtio-mmio page** (G-1,
  measured): all six populated QEMU-virt MMIO slots share ONE page-exclusive
  4-KiB page whose lifetime belongs temporally to the transient probes and
  then permanently to stratumd. Any future resident MMIO-transport driver on
  that bank re-creates the G-1 boot-fatal starvation — resident drivers go
  PCI (per-function BARs). G-3's tapestryd binds **`virtio-pci:16`**.
- **A post-warden gpud crash has no restarter** (the boot-probe warden
  exits after its bind loop; shared posture with netd): the display stays
  quiesce-blank until reboot. The resident-warden lift is the v1.x seam.
- **The G-2 admission gates are kernel-minted-subtype-only.** Any widening to
  "any DMA" or a creator-asserted flag re-opens §18.12 R2-F1 — the revert-probe
  proved the regressions fire on exactly that weakening.
- **The charge/uncharge pairing rides `VMA_FLAG_SHARED_IN`.** Every future
  removal path of a flagged VMA must uncharge (today: `burrow_unmap` +
  `vma_drain`); a new removal path that skips it leaks budget.
- **`weft_binding_validate_rw`'s kind gate is the single chokepoint** keeping
  Tweftio off weave fids — do not add a fast-path consumer that bypasses it.
- **tapestryd's retire ordering is LANDED and binding**: `t_weft_unshare`
  runs BEFORE any backing free (`Comp::retire`); a reordering reopens the
  spec's `MapStale`. And the stage-0 quiesce property is STRUCTURAL
  (synchronous presents) — **a pipelined/async controlq (G-6+) must land a
  real in-flight drain before touching retire** (the SPEC-TO-CODE line is
  the obligation).
- **The R2-F3 reaper is LANDED**: reaper-vs-close serializes on
  `g_weft_reap_lock` (the reaper NULLs `wb->burrow`; the close unregisters
  before reading the binding) — any new close/teardown path for a WEAVE
  binding must keep the unregister-first order. Lock order:
  `g_weft_reap_lock -> g_proc_table_lock(irqsave) -> vma_lock -> v->lock
  -> buddy`; register/unregister stay lock-free call sites.
- **The gather grant**: `resolve_gathered` re-checks every extra node's
  bind membership (I-34 never-fabricated, per axis); a new axis added to
  the fold must preserve that re-check.
- G-2 landed on branch `gfx-1` based off `pty-followups` @c1205ce2 (main had
  diverged with Go 8d at branch time); the eventual merge to main is the
  user's, a normal merge.

## Build + verify

```bash
tools/build.sh kernel && tools/test.sh        # 1170/1170 + boot OK + the G-1 pattern gate
cd specs && for c in tapestry_present tapestry_present_liveness \
  tapestry_present_buggy_{premature_reuse,retire_during_transfer,reweave_without_quiesce,map_after_retire}; do
  java -cp /tmp/tla2tools.jar tlc2.TLC -workers auto -deadlock -config $c.cfg tapestry_present.tla; done
tools/ci-smp-gate.sh                          # the full matrix on kernel chunks
```

## References

- `docs/TAPESTRY.md` §18 (the binding concretization; §18.9 the stage table)
- `docs/reference/125-weft.md` "The weave share (G-2)" (as-built)
- `specs/tapestry_present.tla` + `specs/SPEC-TO-CODE.md::tapestry_present.tla`
- ARCH §28 I-40 + §25.4 (the Weft-row G-2 addendum); CLAUDE.md (same)
- `memory/project_tapestry_design_pass.md` + `memory/audit_tapestry_design_closed_list.md`
