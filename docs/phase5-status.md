# Phase 5 — status and pickup guide

Authoritative pickup guide for **Phase 5: 9P client + Stratum integration** (per `ROADMAP.md §7`; header label "Phase 4" is stale by one — see ROADMAP §2.1 for the reconciliation table).

## TL;DR

**Phase 5 is OPEN.** Entry unblocked at Phase 4 close (tip `4f90e62`, P4-M hash fixup). Stratum v2 is feature-complete and shipping; integration binds to its stable 9P2000.L wire + libstratum-9p ABIs per `stratum/v2/docs/OS-INTEGRATION.md`. No external delivery dependency remains.

Phase 5 lifts the kernel from "drivers expose hardware as composed kobjects" (Phase 4 close) to **"the filesystem is the OS"** — userspace processes interact with persistent storage, the admin surface, and each other entirely through 9P. Major deliverables: the kernel 9P client (`kernel/9p_*`), `specs/9p_client.tla` (the gating spec, written before impl), Unix-socket transport on Spoor, mount path integration, per-Proc 9P connection lifecycle, `.key` sidecar handling via janus, `stratumd` lifecycle inside Thylacine's userspace-driver-as-9P-server pattern, `/srv/stratum-ctl/` consumption, ramfs → Stratum boot pivot. The two §6.2 boxes re-scoped from Phase 4 close as natural side effects: virtio-net `/dev/ether0` 9P surface (P4-Id) + virtio-input `/dev/cons` 9P surface — both depend on the same "userspace driver as 9P server" primitive Phase 5 lands.

Per CLAUDE.md spec-first policy: **`specs/9p_client.tla` is mandatory and lands first.** Invariants in scope: I-10 (per-session tag uniqueness), I-11 (per-session fid identity stable across open lifetime), out-of-order completion correctness, flow control / back-pressure. At least 4 buggy `.cfg` variants demonstrating each invariant's violation under buggy assumptions.

The phase culminates with Thylacine booting from a real Stratum pool: `stratumd` starts in the initramfs, the `.key` sidecar is unwrapped via janus, the kernel mounts the FS socket at `/sysroot`, init pivots root, the system runs entirely on Stratum-backed storage with the `/srv/stratum-ctl/` admin surface live for snapshot / scrub / properties / metrics.

---

## Phase entry pickup (read order)

1. `CLAUDE.md` (root) — operational framework. Refreshed at Phase 5 entry: the "Stratum coordination" section now reflects Stratum v2 reality.
2. `docs/VISION.md §11` — Relationship to Stratum (refreshed for v2).
3. `docs/ARCHITECTURE.md §10.2` + `§14` — 9P dialect + Stratum integration (refreshed for v2).
4. `docs/ROADMAP.md §7` — this phase's binding plan.
5. **`stratum/v2/docs/OS-INTEGRATION.md`** — Stratum's canonical OS-integration manual. Required reading for anyone implementing the 9P client or the boot path. Read §3 (Choosing an integration mode), §4 (Boot lifecycle), §5 (Encryption + key delivery), §6 (POSIX surface inventory), §7 (`/ctl/` admin), §13 (ABI stability), §18 (common pitfalls), §19 (Testing).
6. `stratum/v2/docs/reference/20-9p.md` — as-built 9P wire semantics.
7. `stratum/v2/docs/reference/22-ctl.md` — `/ctl/` admin surface trust boundary.
8. `docs/phase4-status.md` + `docs/handoffs/038-p4-close.md` — what Phase 4 delivered + what was re-scoped to Phase 5.
9. `memory/project_phase4_open_boxes.md` — the 2 re-scoped §6.2 boxes with explicit pickup conditions.

---

## Landed chunks

| Commit SHA | What | Tests |
|---|---|---|
| *(pending)* | **P5-tsleep: hash fixup** | (doc-only) |
| `33bfb05` | **P5-tsleep: the deadline-bounded `Rendez` sleep — `tsleep` + the timer-wait machinery + spec + audit close.** The deadline-bearing kernel wait primitive (`ARCH §8.8`): `tsleep(r, cond, arg, deadline_ns)` is `sleep` plus an absolute deadline — the wait also ends when monotonic time (`timer_now_ns`) reaches `deadline_ns`, the return value (`TSLEEP_AWOKEN` / `TSLEEP_TIMEDOUT`) saying which. The prerequisite that keeps a hung corvus from wedging its `/srv` clients (CORVUS-DESIGN §6.2), and the timeout primitive the Phase-5 `poll` / `futex` work will build on. **Mechanism**: no per-deadline hardware timer — the deadline rides the existing 1 kHz scheduler tick, in the manner of Plan 9's own `tsleep`. A global timer-wait list (`g_timerwait` in `kernel/sched.c`) holds every thread inside a deadlined `tsleep`; `sched_tick()` → `timerwait_tick()` scans it on each fire and times out expired sleepers. Lock order `g_timerwait.lock → Rendez.lock → CpuSched.lock`; `wakeup()` reworked to take `g_timerwait.lock` for the eager timer-wait unlink (then release it — the wake runs under `Rendez.lock` alone); `wake_rendez_waiter` factored as the shared wake transition. New `struct Thread` fields (`timerwait_next` / `timerwait_prev` / `sleep_deadline` / `sleep_timedout`; 784 → 816 B, `_Static_assert` bumped); new `timer_now_ns()` + `timer_ns_to_counter()`. **Spec-first**: `specs/tsleep.tla` — a focused `scheduler.tla` sibling (the `sched_ctxsw.tla` precedent) modelling the three-way cond ↔ wakeup ↔ timeout race; clean GREEN (`tsleep.cfg` 52 distinct / depth 10, `tsleep_nodeadline` 18, `tsleep_liveness` 37 — the last proving `TsleepTerminates`, that a hung producer cannot wedge the waiter) + 4 buggy cfgs counterexampling `NoStaleTimerEntry` (depth 3) / `NoDoubleWake` (5) / `TimeoutSound` (4) / `TsleepTerminates` (stutter). **Audit** (opus prosecutor, scheduler / wait-wake surface — audit-bearing): 0 P0, 1 P1, 4 P2, 4 P3, 2 withdrawn — no safety violation (I-9 and every `tsleep.tla` invariant held under prosecution). F4 (P1, `wakeup`'s `g_timerwait.lock` hold widened across `ready()` / IPI / `on_cpu`-spin) + F7 (P2, `on_cpu` spin in IRQ context under the global lock) fixed in-chunk by narrowing `g_timerwait.lock` to the unlink only and skipping `on_cpu` threads in the scan; F5 + F2 (P2) closed by documenting the deadline contract (overflow / 1 ms granularity) in `rendez.h` + `timer.h`; F8/F9/F10/F11 (P3) closed. **F6 (P2, `timerwait_tick` thundering-herd lock hold) deferred** to a timer-wait-list scalability follow-up before/with P5-corvus-srv-impl-a — a contention hazard, zero invariants broken, the safe fix (a generation-counted batch or per-CPU sharding) is a concurrency-structure change that wants its own spec pass, and no `tsleep` caller exists until P5-corvus-srv-impl-a. **Reference**: `docs/reference/16-rendez.md` (new `tsleep` section), `11-timer.md`. **Next**: P5-corvus-srv-impl-a. | 445 → 450 (+5 `test_tsleep.c` tests) × default + UBSan, no UBSan runtime errors. `tsleep.tla` clean GREEN + 4 buggy cfgs; the 10 other specs byte-unchanged. |
| *(pending)* | **P5-corvus-srv-design: hash fixup** | (doc/spec-only) |
| `f45f1a7` | **P5-corvus-srv-design: the r5 `/srv/corvus` transport design into scripture + the two TLA+ specs (no code).** First sub-chunk of the P5-corvus-srv macro-chunk (CORVUS-DESIGN §10). corvus-bringup left corvus on a joey-driven pipe harness; **r5** — converged across four opus adversarial design-audit rounds — makes corvus a full 9P2000.L server reached **per-connection**, every connection carrying a kernel-stamped, unforgeable peer identity. **Scripture**: CORVUS-DESIGN §6 fully rewritten to r5 — the `devsrv` kernel Dev + `/srv`; `SYS_POST_SERVICE` / `SYS_SRV_ACCEPT` / `SYS_SRV_PEER`; per-connection synchronous `p9_client` sessions (sidestepping the shared-`9p_client.c`-lock wedge prosecutor-3 found); the `stripes` u64 per-Proc identity tag; `tsleep`-bounded blocking ops so a hung corvus cannot wedge clients; teardown ordering — plus new invariants **C-22** (kernel-stamped per-connection identity, never client-sourced, never fid-cached) + **C-23** (kernel is corvus's sole 9P client, transport kernel-owned); §10 arc re-decomposed (P5-corvus-srv → -design / P5-tsleep / -impl-a / -impl-b); §4.2 `peer/` demoted to optional introspection; afid text removed. ARCHITECTURE §11.2c (the three service-registry syscalls), §18.2 (`KObj_Srv` — ninth kobj kind, non-transferable) + §18.3/§18.8, §8.8 (`tsleep` in the Plan 9 idiom layer), §9.4 (`/srv` + `devsrv`), §28 footer (C-1..C-23). ROADMAP §7.2a decomposition. **Specs** (spec-first, before any impl): `corvus.tla` gains a connection layer — vars `service_posted`/`connections`/`corvus_accepted`/`proc_alive`/`completed_conn_ops`, actions `PostService`/`SrvBind`/`SrvAccept`/`SrvPeerOp`/`ProcExit` + bug classes `BuggyIdentityCachedOnFid`/`BuggyDeadProcStale`, invariants `ConnOpIdentityIsKernelTruth`/`ConnOpPeerWasLive` (C-22) + `ConnTransportKernelOwned` (C-23); session and connection layers advance orthogonally (`corevars`/`connvars`). `handles.tla` gains the `SrvKObjs` non-transferable partition, the `WalkDerive` action (a `/srv`-own-Dev walk produces a structurally-`KObj_Srv` child Spoor) + bug class `BuggySrvWalkTx`, invariants `WalkChildIsSrv` + `SrvHandlesAtOrigin`. **Verified**: `corvus.cfg` clean GREEN — 3,027,456 distinct / depth 27 / 1min20s — + all 7 buggy cfgs counterexample (the new `identity_cached_on_fid` → `ConnOpIdentityIsKernelTruth` depth 8, `dead_proc_stale` → `ConnOpPeerWasLive` depth 7); `handles.cfg` clean GREEN (`SrvKObjs={}` — 51.7M distinct / depth 28, unchanged from P5-hostowner) + new focused clean cfg `handles_srv.cfg` GREEN (26,784 distinct / depth 12) + all 10 buggy cfgs counterexample (the new `srv_walk_tx` → `WalkChildIsSrv` depth 3). `SPEC-TO-CODE.md` mapping rows. **Not audit-triggering** — docs + specs only; the `devsrv` Dev + the three syscalls + corvus's 9P server land as audit-bearing code in P5-corvus-srv-impl-a/-b. **Next**: P5-tsleep, then P5-corvus-srv-impl-a. | No code-logic change; 445/445 stands. `corvus.tla` clean GREEN (3.03M distinct) + 7 buggy; `handles.cfg` clean GREEN (51.7M) + `handles_srv.cfg` GREEN + 10 buggy. |
| *(pending)* | **P5-hostowner-design: hash fixup** | (doc/spec-only) |
| `856f542` | **P5-hostowner-design: the `cap`-device elevation mechanism — scripture + spec landing (no implementation code).** Resolves the cap-grant scripture gap flagged by P5-hostowner-a, via a user-signed-off design pass. **The mechanism** (CORVUS-DESIGN §5.5 rewrite + new §5.5.1): hostowner elevation is a two-phase, file-mediated grant through a kernel `cap` device, modelled on Plan 9's `cap(3)` — corvus writes a pending grant to `cap`/`grant` (kernel-gated on the new `CAP_GRANT_HOSTOWNER`); the console Proc redeems it via `cap`/`use` (kernel-gated on `PROC_FLAG_CONSOLE_ATTACHED`), and the kernel ORs `CAP_HOSTOWNER` into `current->caps`. Two independent gates — corvus's passphrase check + the kernel's console check — make it robust to a compromised corvus; the target redeems on its own behalf so there is no cross-Proc capability write. The Thylacine-original adaptation of Plan 9's device: it grants a capability *bit* (no UID 0; D5), binds the grant to the unforgeable per-Proc identity tag, and drops Plan 9's HMAC (the `grant` write is itself capability-gated). **Scripture**: CORVUS-DESIGN §5.5/§5.5.1 + invariant C-21 + §10 re-decomposition (new chunk **P5-corvus-srv** for the `/srv/corvus/` transport prerequisite; P5-hostowner split into landed -a / -b elevation / -c RECOVER) + §15 naming; ARCHITECTURE §15.3 (two-axis capability set), §7.7 + §28 I-2 (elevation-only is the sole sanctioned cap growth); CLAUDE.md §28 I-2 mirror synced; ROADMAP §7.2a (P5-corvus-srv added, P5-hostowner bullet updated). **caps.h**: `CAP_GRANT_HOSTOWNER` (bit 4, fork-grantable, added to `CAP_ALL`) + `CAP_ELEVATION_ONLY` mask — foundation defines, unused until P5-hostowner-b; `_Static_assert` bumped; kernel build clean. **Spec** (spec-first, before the P5-hostowner-b impl): `handles.tla` gains the elevation-only capability axis (`ElevationOnly`), the `RforkWithCaps` strip (`granted \ ElevationOnly` — child caps + ceiling both stripped), the `HostownerGrant` action (raises caps + ceiling together — the sole admitter of `CapHostowner`), the `BuggyRforkNoStrip` bug class + `handles_buggy_rfork_hostowner.cfg`; `InitialCapsOf(ProcRoot)` becomes `Caps \ ElevationOnly` (kproc never holds the elevation-only cap). `corvus.tla`: `AdminElevate` note that the console gate is enforced redundantly in the kernel — what makes `HostownerRequiresConsole` robust to a buggy corvus. `SPEC-TO-CODE.md` mapping rows. **Verified**: `handles.tla` clean cfg GREEN — *Model checking completed. No error has been found.* — 51,744,096 distinct states / depth 28 / 56 min (the `HostownerGrant` action ~4.4×'d the state space vs. the prior 11.7M baseline); all 11 buggy cfgs counterexample (the new `rfork_hostowner` → `CapsCeiling`, catching an rfork that fails to strip the elevation-only cap from a forked child). `corvus.tla` clean green + all 5 buggy cfgs violate; the 7 other specs are byte-unchanged. Kernel build exit 0. No kernel-logic change (caps.h adds only unused defines) → test count unchanged at 445/445. **Not audit-triggering** — docs + specs + 2 unused `caps.h` defines; the `cap` device + the `rfork` strip + ADMIN_ELEVATE land as audit-bearing code in P5-hostowner-b with its own audit. **Next**: P5-corvus-srv (the `/srv/corvus/` transport — kernel-stamped peer identity; prerequisite for login + hostowner), then P5-hostowner-b. | No code-logic change; 445/445 stands. `handles.tla` clean green (51.7M states) + 11 buggy cfgs; `corvus.tla` clean green + 5 buggy. |
| *(pending)* | **P5-hostowner-a: hash fixup** | (doc-only) |
| `20fa615` | **P5-hostowner-a: kernel CAP_HOSTOWNER + console-attachment foundation.** First sub-chunk of the P5-hostowner macro-chunk (CORVUS-DESIGN §10). **`CAP_HOSTOWNER`** (`caps.h`, bit 3): an *elevation-only* capability — deliberately excluded from `CAP_ALL`, so no Proc holds it at creation and `rfork`'s mask-AND can never confer it (double-bounded: absent from `CAP_ALL`, and bit-AND inheritance is monotone-decreasing from a kproc that never holds it). The grant path is corvus ADMIN_ELEVATE (P5-hostowner-b). **`PROC_FLAG_CONSOLE_ATTACHED`** (`proc.h`, `proc_flags` bit 3): the kernel-stamped local-console trust anchor. `proc_mark_console_attached` (one-way set, ALIVE-gated, idempotent — materializes `specs/corvus.tla` `MarkConsoleAttached`) + the fail-closed `proc_is_console_attached`. `joey_thunk` stamps joey at boot (race-free — joey's own context, before exec — with a self-check). The console bit is NOT propagated by `rfork` — `proc_alloc` zeroes `proc_flags`, `rfork_internal` doesn't copy it — so only an explicit mark confers it; a future remote-login (sshd) chain cannot inherit the trust anchor. **No spec change** — `corvus.tla` already models `console_attached`/`MarkConsoleAttached`/`HostownerRequiresConsole`; `SPEC-TO-CODE.md`'s MarkConsoleAttached row updated to the as-built impl. **Tests**: `proc.console_attached_smoke` — NULL fail-closed, fresh-Proc-unattached, mark sets it + idempotence, and rfork non-propagation BOTH from kproc and from a self-marked console-attached intermediate parent (the latter catches a `proc_flags`-copy regression). 444 → 445. **Audit** (opus prosecutor, capability surface): 0 P0, 0 P1, 0 P2, 4 P3 — F1 (ALIVE-state guard on the mark) + F3 (stale `caps.h` rfork-inheritance comment) + F4 (test non-propagation-from-attached-parent gap) fixed in-chunk; F2 (non-atomic `proc_flags |=`) closed with justification — matches the 3 existing hardening-syscall writers, sound under v1.0 single-thread-per-Proc; atomic conversion deferred to the multi-threaded-Proc lift. **Verified**: 445/445 PASS × default + UBSan (UBSan: `BOOT_TIMEOUT=300` — the corvus-crypto suite outgrew 180 s), 0 `EXTINCTION`. `docs/reference/14-process-model.md` updated. **Scripture gap flagged for P5-hostowner-b**: the cap-grant mechanism (corvus → kernel sets `CAP_HOSTOWNER` on a peer Proc) is under-specified in ARCH/CORVUS-DESIGN — needs design + signoff before -b. | 444→445 PASS × default + UBSan. |
| *(pending)* | **P5-secondary-stack-guard: hash fixup** | (doc-only) |
| `78dbc36` | **P5-secondary-stack-guard: secondary boot-stack guard pages + user-stack guard VMA.** Closes two deferred audit findings — both pre-existing gaps where a stack lacked a guard page so an overflow corrupted adjacent memory silently. **el1h audit F1** (P2): `g_secondary_boot_stacks` (the per-secondary boot stacks; also the permanent stack of each CPU's kstack-less idle thread) had no guard, unlike the boot CPU's `_boot_stack_guard`. Restructured `char[7][16384]` → `struct secondary_stack[7]`, each slot `{ char guard[4096]; char usable[16384] }`, `aligned(4096)`; `build_page_tables` (mmu.c) zeroes each slot's leading guard-page L3 PTE in both `l3_kernel` and `l3_directmap_kernel`; `secondary_entry` (start.S) sets SP to the slot top (stride `SECONDARY_STACK_SLOT_SIZE` = 20480); `addr_is_stack_guard` (fault.c) recognizes the secondary guard ranges (PA + high-VA) → `extinction("kernel stack overflow")`. **corvus-bringup-d audit F7** (P3): the 256 KiB user-stack VMA had nothing reserved below it. New `vma_alloc_guard` (vma.c) creates a `prot==0` / `burrow==NULL` reserved guard VMA — no BURROW, outside the dual-refcount lifecycle; `vma_free`/`vma_drain` are already NULL-burrow-safe. `exec_map_user_stack` installs a one-page guard at `[EXEC_USER_STACK_GUARD_BASE, EXEC_USER_STACK_BASE)`: an overflow faults (`userland_demand_page` rejects `prot==0` before the NULL BURROW is dereferenced) and `vma_insert` overlap-rejection reserves the page against a future mapping allocator (Phase 5+ mmap / heap). Guard size 1 page for both, matching `_boot_stack_guard`. **No spec** — guard placement is page-table layout + a reserved VMA range, not a §28 concurrency/ordering invariant; `corvus.tla` + the 7 other specs unchanged. **Tests**: new `secondary_stack_guard` fault-test variant (`tools/test-fault.sh` — writes a guard page, asserts `EXTINCTION: kernel stack overflow`; pre-fix the write lands silently → FAIL); `smp.secondary_stack_guard_layout` (slot-layout pin); `exec.user_stack_guard` (guard VMA present + `prot==0`/`burrow==NULL` + overlap rejection). **Audit** (opus prosecutor — start.S/mmu.c/fault.c/smp.c+h/vma.c/exec.c): 0 P0, 0 P1, 0 P2, 3 P3 — F2 fixed in-chunk (`_Static_assert(DTB_MAX_CPUS==8)` pinning start.S's `#8` idx-bound literal, the sibling of the `#20480` literal this chunk already pinned); F1 (guard PTE index unbounded at runtime) + F3 (direct-map-KVA FAR form unrecognized) closed no-change — both consistent with the pre-existing `_boot_stack_guard` pattern. **Verified**: 444/444 PASS × default + UBSan, 0 `EXTINCTION` lines, all 5 fault-test variants PASS. Reference docs `03-mmu`/`17-smp-bringup`/`25-fault-dispatcher`/`26-vma`/`27-exec` updated; F1 marked closed in `67-el1h-kernel.md`, F7 in `69-corvus-unwrap.md`. **Next**: the remaining corvus verbs + `CRVS` FS persistence. | 442→444 PASS × default + UBSan. |
| *(pending)* | **P5-corvus-bringup-d: hash fixup** | (doc-only) |
| `cb0f849` | **P5-corvus-bringup-d: ML-KEM-768 + X25519 hybrid keypair + the hybrid-PKE DEK envelope + the WRAP and UNWRAP verbs.** Fourth corvus sub-chunk per CORVUS-DESIGN.md §6.4/§6.5. P5-corvus-bringup-c wrapped a 64-byte CSPRNG placeholder and discarded it after AUTH; -d makes the keypair a real **ML-KEM-768 (FIPS 203) + X25519 hybrid** — a 3648-byte plaintext (x25519 sk/pk + ml-kem ek/dk) generated at USER_CREATE, AEGIS-256-wrapped into the `CorvusUserState` ciphertext (`KEYPAIR_LEN` 64→3648; `CRVS` version unchanged — no on-disk file exists and the ciphertext length is implied by file size, so the resize breaks no format). AUTH retains the unwrapped keypair in a new `keypair` field of the mlock'd session slab; `session_clear()` volatile-wipes it. **New crates** on `aarch64-unknown-none`, all pure-Rust + explicit-RNG: `ml-kem` 0.2 (RustCrypto FIPS 203), `x25519-dalek` 2 (`static_secrets`+`zeroize`), `sha2` 0.10, `kem` 0.3.0-pre.0 (the `Encapsulate`/`Decapsulate` traits — ml-kem does not re-export them, so a direct dep), `rand_core` 0.6; `ThylaRng` adapts the RNG trait over `t_getrandom` (no `getrandom` crate — it has no bare-metal backend). **DEK envelope** (CORVUS-DESIGN §6.5 — corvus-owned format, 1217 B): a KEM-DEM hybrid — ML-KEM-768 encapsulate + X25519 ephemeral→static ECDH → a `SHA-256` KEK combiner (`"thylacine-corvus-dek-kdf-v1"` ‖ ss_pq ‖ ss_cl ‖ eph_pk ‖ mlkem_ct, transcript-bound) → AEGIS-256 over the 32-byte DEK, AD = `"thylacine-corvus-dek-v1"` ‖ dataset. Secure if either KEM holds; ML-KEM implicit-rejection means the AEGIS tag is the sole integrity gate. **WRAP (verb_id=10)** — a new verb added to CORVUS-DESIGN §6.4 (binding scripture; landed with user signoff): wraps a DEK under the session user's hybrid public key, returns the envelope. **UNWRAP (verb_id=4)**: unwraps with the secret key; the C-7 ownership gate (`specs/corvus.tla` `UnwrapOwnerOnly`) fires *before any crypto*. Both gate on an in-memory dataset-ownership table populated at USER_CREATE (`users/<name>`→`<name>`): unknown dataset → `NotFound`, non-owner → `PermissionDenied`. **Kernel**: `EXEC_USER_STACK_SIZE` (`exec.h`) 16 KiB → 256 KiB — ML-KEM-768's FO-transform decapsulate uses tens of KiB of stack and the prior 16 KiB user-stack VMA overflowed; a strict headroom increase for every userspace Proc. **Spec**: `corvus.tla` header note — WRAP shares Unwrap's C-7 gate, modeled as no new action (comment-only change); corvus.tla re-verified — clean cfg green (141109 states / 23652 distinct) + all 5 buggy counterexample cfgs violate as expected; the 7 other specs are unchanged by this chunk. **Joey**: USER_CREATE michael + susan → AUTH(wrong)→`BadAuth` → AUTH(ok)→token → WRAP(users/michael) → UNWRAP(users/michael)→32-byte DEK byte-matches → UNWRAP(users/susan)→`PermissionDenied` (the spec-pinned C-7 test) → UNWRAP(users/ghost)→`NotFound` → SESSION_CLOSE. corvus binary 198 KiB (< `SYS_SPAWN_BLOB_MAX` 256 KiB); joey 40 KiB (< `JOEY_BLOB_MAX` 64 KiB). `docs/reference/69-corvus-unwrap.md` (new). **Verified**: 442/442 PASS × default + UBSan, 0 `EXTINCTION` lines, full hybrid-PKE round-trip + C-7 enforcement. **Next**: the remaining corvus verbs + FS persistence; or the deferred P5-secondary-stack-guard. | 442/442 PASS × default + UBSan. |
| *(pending)* | **P5-corvus-bringup-c: hash fixup** | (doc-only) |
| `24ddb91` | **P5-corvus-bringup-c: real crypto — Argon2id passphrase verification + AEGIS-256 AEAD + state file format (`CRVS`) + USER_CREATE verb.** Third corvus sub-chunk per CORVUS-DESIGN.md §4.3/§5.1; rebased on P5-el1h-kernel. P5-corvus-bringup-b's AUTH accepted any non-empty passphrase — this makes AUTH verify it through an Argon2id-derived KEK + AEGIS-256 AEAD-unwrap. **Pure-Rust crypto** pulled into the `aarch64-unknown-none` `usr/corvus` build: `argon2` (Argon2id KDF), `aegis` with the `pure-rust` feature (AEGIS-256 — the default build wraps a C `libaegis` needing libc headers absent on bare-metal; `pure-rust` short-circuits `build.rs` to a software-AES backend), `linked_list_allocator`. **Heap**: corvus gains a `#[global_allocator]` `LockedHeap` over a 24 MiB static BSS buffer (Argon2id's working-memory matrix), initialized at `rs_main` entry. **State file format** `CorvusUserState` per §4.3 — `magic 'CRVS'` + version + Argon2id params + 16-byte salt + 32-byte AEGIS nonce + ciphertext + 32-byte tag; byte-precise serializer + compile-time layout asserts; in-memory `Vec` at this sub-chunk (FS persistence lands when `/var/lib/corvus/` is mounted). **USER_CREATE (verb_id=5)**: fresh salt+nonce via `t_getrandom` → `Argon2id(passphrase, salt)` → 32-byte KEK → 64-byte CSPRNG placeholder keypair (ML-KEM-768+X25519 hybrid deferred) → AEGIS-256-wrap with AD = `"thylacine-corvus-v1" || user || backend` → store record; re-creating an existing user → `PermissionDenied`. **AUTH** now real-crypto-gated: lookup user → re-derive KEK → AEGIS-256-unwrap; tag mismatch → `BadAuth`; success → token. KEK + keypair wiped (`write_volatile`) on every path. **Argon2id preset**: `t_cost=2, m_cost=16 MiB, p=1` (the §4.3 default is 64 MiB — capped at 16 MiB so the heap fits a single contiguous buddy allocation; `m_cost_kib` is a per-record stored field so a future bump is format-compatible). **Kernel**: `SYS_SPAWN_BLOB_MAX` 32 KiB → 256 KiB + `JOEY_BLOB_MAX` 32 KiB → 64 KiB (corvus binary grew to ~109 KiB with embedded crypto; joey's inline orchestration grew with the USER_CREATE + AUTH(wrong) + AUTH(ok) + SESSION_CLOSE wire codec). **Joey** drives the full round-trip: `USER_CREATE("michael")` → `AUTH(wrong pass)` (asserts `BadAuth` — exercises the AEGIS tag-mismatch branch) → `AUTH(correct pass)` (asserts OK + token) → `SESSION_CLOSE`. No new audit-trigger surfaces (corvus is userspace; the kernel `*_BLOB_MAX` bumps are constants). `docs/reference/68-corvus-crypto.md` (new). **Verified**: 442/442 PASS × default + UBSan, 0 `EXTINCTION` lines, full crypto round-trip on the EL1h kernel. **Next**: P5-corvus-bringup-d (UNWRAP verb + ML-KEM-768 hybrid keypair). | 442/442 PASS × default + UBSan. |
| `0e5fd1f` | **P5-el1h-kernel: hash fixup** | (doc-only) |
| `e2784c2` | **P5-el1h-kernel: convert the kernel to run uniformly at EL1h — fixes a pre-existing SMP soundness bug (a secondary CPU silently died on every boot).** Root cause: the kernel ran normal code at EL1t (`SPSel=0`) and exception handlers at EL1h (`SPSel=1`); `cpu_switch_context` carries `SP` but not `SPSel`, so under SMP work-stealing a thread could resume the `KERNEL_EXIT` trampoline in the wrong mode, where `msr SP_EL0` against the selected SP is CONSTRAINED UNPREDICTABLE (`EC=0` Undefined trap → `extinction()` parks the faulting CPU). A per-CPU exception stack was also incompatible with migrating a mid-exception thread. Discovered during P5-corvus-bringup-c testing; the dual-mode design had already been band-aided three times (P4-Fix157, R7-F135, the P4-Ic6 `g_bootcpu_idle`-off-run-tree workaround). **Fix (Model 1)**: run the kernel uniformly at EL1h — one stack bank (`SP_EL1` = the running thread's own kernel stack), `SP_EL0` exclusively the userspace stack, exception frames built on the per-thread kernel stack (→ migration-safe). `cpu_switch_context` + `KERNEL_ENTRY`/`KERNEL_EXIT` need no change (bank-agnostic). **Scripture**: `ARCHITECTURE.md §12.1`/§12.2 made explicit + new invariant **I-21**; `CLAUDE.md` §28 mirror. **Code**: `start.S` (primary + secondary boot to EL1h — assert `SPSel=1` before every SP write, single kernel stack, one high-VA re-anchor); `vectors.S` (EL1t vector group `0x000`/`0x080` → `VEC_UNEXPECTED`); `userland.S` (`userland_enter` installs the user SP via `msr sp_el0` at EL1h, P4-Fix157 dance removed); `sched.c` (`try_steal` skips `kstack_base==NULL` bootstrap threads — audit F2); `main.c`/`smp.c`/`smp.h`/`timer.c`/`test_smp.c`/`thread.c` (banner, `g_exception_stacks` repurposed as reserved, `g_exception_stack_observed` + the timer hook removed, `test_smp` asserts `SPSel==1`, comment fixes). **Spec**: new `specs/sched_ctxsw.tla` (+2 cfgs) pins I-21 — `DUAL_MODE=FALSE` (Model 1) clean, `DUAL_MODE=TRUE` (Model 2) the executable counterexample. **Audit**: adversarial round on exception-entry + boot + scheduler — no P0; F2 (P1) + F3 (P2) fixed in-chunk; F1 (P2, secondary-stack guard pages) deferred (see Trip hazards). **Verified**: 442/442 PASS × default + UBSan, **0 `EXTINCTION` lines**, 4/4 CPUs, the full joey→corvus crypto round-trip with work-stealing enabled. `docs/reference/67-el1h-kernel.md` (new). | 442/442 PASS × default + UBSan. |
| `6d9994f` | **P5-corvus-bringup-b: hash fixup** | (doc-only) |
| `9cf92c6` | **P5-corvus-bringup-b: `/sbin/corvus` server loop + binary frame wire codec + AUTH + SESSION_CLOSE verbs.** Second impl chunk of the corvus daemon arc per CORVUS-DESIGN.md §10. Replaces -a's hardening-then-exit skeleton with a single-peer Spoor server. **Wire format** per CORVUS-DESIGN.md §6.4: request = `verb_id u8 + payload_len u16 LE + payload`; response = `status u8 + payload_len u16 LE + payload`. Two verbs implemented: AUTH (verb_id=1) parses `user_len u8 + user + pass_len u16 LE + passphrase`; passphrase NOT verified at this sub-chunk — Argon2id at -c; presence + valid framing = success; mints a 33-byte session token (`'s'` + 32 hex chars encoding 16 bytes of CSPRNG entropy via t_getrandom). SESSION_CLOSE (verb_id=3) parses 33-byte token + clears the session if it matches via constant-time compare. Every other verb_id returns BadFormat (5). **Session table**: single-slot `static mut SESSION` at this skeleton; accessor wrappers use `core::ptr::addr_of!` / `addr_of_mut!` to avoid Rust 1.77+'s `static_mut_refs` lint. **Structural C-3 enforcement**: there is no setter that mutates `bound_user` in place — install() writes the field once + only clear() (which zeroes the entire record) ever touches it again. Spec's `BuggyAuthBindingMutate` shape is structurally unreachable. **One-session-per-peer**: `handle_auth` short-circuits on `session_active()` → PermissionDenied; pins spec's AuthSuccess precondition. **Defence-in-depth**: payload buffer wiped via t_explicit_bzero between iterations (forward-compat for verbs carrying secrets). Server loop exits 0 on rx EOF. **libthyla-rs extension**: +4 Rust SVC wrappers (`t_pipe` returning (i64,i64) tuple, `t_read`, `t_write`, `t_close`) + 4 syscall consts (T_SYS_PIPE=8 .. T_SYS_CLOSE=11) + 1 forward-compat const (T_SYS_SPAWN_FULL=25, declared for parity). Mirrors the C-side libt stubs from P5-fd-* (`5fd72f6` / `54d900b` / `0f66f5c`). **Joey orchestration extension**: creates 2 pipes via `t_pipe` → spawns /sbin/corvus via `t_spawn_full(name, len, [c2s_rd, s2c_wr], 2, T_CAP_LOCK_PAGES \| T_CAP_CSPRNG_READ)` → closes joey's copies of corvus-side fds → drives AUTH ("michael" + skeleton passphrase; verifies status=0 + 33-byte token starting `'s'`) → drives SESSION_CLOSE with the same token (verifies status=0 + 0-byte payload) → closes c2s_wr (triggers corvus rx EOF) → wait_pid corvus + assert status=0 → close s2c_rd. **Spec correspondence** (`specs/corvus.tla` at `c00de63`/`e5ce30a`): the AUTH handler is the `AuthSuccess(p, u)` action's runtime body; SESSION_CLOSE handler is `SessionClose(p)`. `SessionUserImmutable` (C-3) pinned structurally. `SessionTransfer` / `AdminElevate` / `Unwrap` / `AdminVerb` + invariants C-7 / C-11 / HostownerRequiresConsole require future sub-chunks. **No new audit-trigger surfaces** — corvus is userspace consuming existing audited syscalls (pipe + read + write + close + spawn-full are at the audited syscall surface). **Audit posture**: no static_mut races; single-Proc daemon; constant-time token compare; CSPRNG-sourced entropy wiped from raw buffer post-hex. **Testing**: boot path is the regression. AUTH + SESSION_CLOSE round-trip runs on every boot; failure at any step prints a tagged diagnostic + exits non-zero, breaking the boot test. Boot log on success: `corvus: ready (hardening applied; serving /srv/corvus/ over fd 0/1)` → `joey: AUTH ok (token=s3e02730...)` → `joey: SESSION_CLOSE ok` → `corvus: server_loop returned EOF; shutting down clean` → `joey: /sbin/corvus reaped status=0; AUTH + SESSION_CLOSE round-trip verified`. **Test timing**: default-build boot ≈ 15s; UBSan ≈ 25s. If a default-build CI run flakes, raise to `BOOT_TIMEOUT=60` (already standard for UBSan). No new kernel-internal tests — P5-fd-* + P5-spawn-full + P5-corvus-syscalls already cover the syscalls in isolation; this chunk composes them on the production cap-grant chain. **Files**: `usr/corvus/src/main.rs` (heavy extension; +280 LOC: wire constants + session table + verb handlers + server loop, replaces -a's post-hardening exit); `usr/lib/libthyla-rs/src/lib.rs` (+4 wrappers + 5 syscall consts; ~110 LOC); `usr/joey/joey.c` (+pipe-pair + spawn_full + AUTH/SESSION_CLOSE round-trip + close + wait; ~150 LOC); `docs/reference/66-corvus-server.md` (new ~250 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 66); `docs/phase5-status.md` (this row + P5-corvus-bringup-a row marked LANDED); memory updates. Test count 442/442 unchanged × default + UBSan. **Stage**: structural wire surface live; AUTH + SESSION_CLOSE state-machine actions materialized. The corvus binary grew 5528B → 13304B. **Next**: P5-corvus-bringup-c (Argon2id passphrase verification + state file format `CRVS` + hybrid keypair storage; opens the door to UNWRAP verb at -d). | 442/442 PASS × default + UBSan. |
| `a4bb28c` | **P5-corvus-bringup-a: hash fixup** | (doc-only) |
| `7487054` | **P5-corvus-bringup-a: `/sbin/corvus` skeleton — first impl chunk of the corvus daemon arc per CORVUS-DESIGN.md §10.** New Rust crate `usr/corvus/` (~140 LOC main + Cargo manifest) running the 5 v1.0 hardening syscalls at startup: `t_mlockall(0)` → `t_set_dumpable(0)` → `t_set_traceable(0)` → `t_getrandom(probe, 32, 0)` (CSPRNG-seeded probe) → `t_explicit_bzero(probe, 32)` (RAM-wipe rehearsal). Step-level failure tags (`corvus: STEP=<n> FAIL rc=<low-hex>`) make a regression immediately attributable. On success: `corvus: ready (hardening applied; CSPRNG seeded; no verbs yet)` + exit 0. **Production-shape orchestration**: joey extended to spawn /sbin/corvus via `t_spawn_with_caps("corvus", 6, T_CAP_LOCK_PAGES \| T_CAP_CSPRNG_READ)` after the /hello hop; `t_wait_pid` reaps + asserts status=0. **Joey caps bump** (load-bearing): `kernel/joey.c` changed from plain `rfork(RFPROC, joey_thunk, &args)` (which gives child CAP_NONE per `rfork_internal(... CAP_NONE)`) to `rfork_with_caps(RFPROC, joey_thunk, &args, CAP_ALL)`. Joey now holds the full v1.0 cap ceiling so it can grant any subset to children via spawn-with-caps. ARCH §28 I-2 (caps monotonically reduce) preserved structurally — `rfork_with_caps`'s AND can only reduce, never elevate. Production-init pattern: joey is the trusted policy-decision point. **Rust runtime extension** (`usr/lib/libthyla-rs/src/lib.rs`): +5 SVC wrappers (`t_mlockall` / `t_set_dumpable` / `t_set_traceable` / `t_explicit_bzero` / `t_getrandom`) + 5 syscall consts (T_SYS_MLOCKALL=16 .. T_SYS_GETRANDOM=20) + 3 cap consts (T_CAP_HW_CREATE / T_CAP_LOCK_PAGES / T_CAP_CSPRNG_READ). Mirrors the C-side libt stubs that already exist from P5-corvus-syscalls (`0db0dcf`). **C-side libt extension** (`usr/lib/libt/include/thyla/syscall.h`): +3 `T_CAP_*` macros so joey can express `T_CAP_LOCK_PAGES \| T_CAP_CSPRNG_READ` symbolically. **Workspace + build**: `usr/Cargo.toml` members += "corvus"; `tools/build.sh` usr_rs_bins += "corvus" — corvus binary lands in the initrd cpio alongside hello / joey / pipe-probe / etc. **Spec correspondence**: skeleton's runtime contribution covers CORVUS-DESIGN.md C-2 (mlock + DONTDUMP via mlockall + set_dumpable) + C-5 (RAM-wipe pattern rehearsed via explicit_bzero on probe scratch) + C-15 (refuse to generate randomness until CSPRNG-seeded; getrandom-seeded probe). Session-state-machine invariants (C-3 / C-7 / C-11) spec'd at `specs/corvus.tla` (P5-corvus-spec at `c00de63`/`e5ce30a`) but require wire codec + verb dispatch (deferred to P5-corvus-bringup-b onward). **Testing**: boot path IS the regression. `joey: /sbin/corvus reaped status=0; hardening verified` is the test signal; test framework's `Thylacine boot OK` is gated on joey's clean exit which is gated on corvus's clean exit. The kernel-internal `test_sys_corvus.c` (P5-corvus-syscalls) already covers each syscall in isolation; this chunk's contribution is end-to-end composition on a real EL0 Proc with production cap-grant chain. **Audit posture**: no new audit-trigger surfaces — `usr/corvus/` is userspace consuming existing audited syscalls; `kernel/joey.c`'s rfork→rfork_with_caps switch keeps the existing `rfork_with_caps` audit-trigger surface (covered at R11 P4-Ic3 audit close — F162-F164 all closed). **Files**: `usr/corvus/{Cargo.toml, src/main.rs}` (new); `usr/lib/libthyla-rs/src/lib.rs` (+5 wrappers + 5 syscall consts + 3 cap consts; ~120 LOC); `usr/lib/libt/include/thyla/syscall.h` (+3 T_CAP_* macros); `usr/Cargo.toml` (members +corvus); `tools/build.sh` (usr_rs_bins +corvus); `usr/joey/joey.c` (+spawn-with-caps + wait_pid for corvus; ~40 LOC); `kernel/joey.c` (rfork → rfork_with_caps(CAP_ALL)); `docs/reference/65-corvus-skeleton.md` (new ~180 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 65); `docs/phase5-status.md` (this row + P5-corvus-spec row marked LANDED); memory updates. Test count 442/442 unchanged × default + UBSan (BOOT_TIMEOUT=60 for UBSan; only the existing benign EL0 sync-exception line in the boot log, no test failure). **Stage**: corvus skeleton boots end-to-end. The runtime-hardening floor is in place; the next sub-chunk is the spec-gated session-state-machine impl. **Next**: P5-corvus-bringup-b (`/srv/corvus/` Spoor server bring-up + binary frame wire codec + AUTH/SESSION_CLOSE verb skeletons; pins SessionUserImmutable + the runtime body of the session-identity model). | 442/442 PASS × default + UBSan. |
| `e5ce30a` | **P5-corvus-spec: hash fixup** | (doc-only) |
| `c00de63` | **P5-corvus-spec: land `specs/corvus.tla` + 5 buggy cfgs — the gating spec for corvus daemon (CORVUS-DESIGN.md §9 invariants C-3 + C-7 + C-11 + HostownerRequiresConsole).** Per CLAUDE.md spec-first policy: corvus touches load-bearing session-binding + capability-arithmetic + authorization invariants; the TLA+ model comes BEFORE the impl. Models the daemon's session state machine + capability arithmetic + authorization surface. **Identity model**: sessions identified by (creation_proc, bound_user) — the IMMUTABLE pair set at AuthSuccess; owner_proc moves on SessionTransfer; bound_user MUST be preserved. The separation lets SessionUserImmutable catch BOTH in-place mutation (BuggyAuthBindingMutate) AND mutation-during-transfer (BuggyTransferRebind) by checking the immutable pair has a matching origin record, regardless of which Proc currently owns the session Spoor. **Dataset modeling**: Datasets identified by their owner user (`Datasets == Users`; `DatasetOwner(d) == d`) — sidesteps TLC's config-file limitation on record/function constants; matches the live-system convention that user datasets are named after their owner (`thylacine/users/michael`). **Console-attachment**: monotonic SUBSET Procs grown by MarkConsoleAttached (joey's console-login spawn); never propagatable across rfork — pinned by HostownerRequiresConsole invariant. **Spec actions**: MarkConsoleAttached, AuthSuccess, SessionClose, SessionTransfer, AdminElevate, Unwrap, AdminVerb. **Bug classes**: BuggyUnwrapCrossUser (skips C-7 owner check), BuggyAuthBindingMutate (mutates bound_user in place), BuggyAdminWithoutProcCap (skips CapHostowner gate), BuggyElevateWithoutConsole (skips console check), BuggyTransferRebind (mutates bound_user during transfer). **Invariants**: TypeOk + SessionUserImmutable (C-3) + UnwrapOwnerOnly (C-7) + AdminRequiresProcCap (C-11 Proc-cap side) + HostownerRequiresConsole (§5.5). **Out of scope**: in-RAM secret discipline (C-1/C-2/C-5; runtime), audit-log encryption (C-9/C-19; file format), Argon2id discipline (C-10/C-12; backend), rate-limit + session timeouts (C-16/C-17; operational), per-user-stratumd ownership (C-18; cross-component, pinned by composition), recovery phrase (C-20; provisioning). **Verification**: clean cfg verified at 141109 states / 23652 distinct / depth 18 / <1s. All 5 buggy cfgs find counterexamples at depth 4 in <1s each. **SPEC-TO-CODE.md** extended with full corvus.tla section (state universe, config table, action↔impl-target mapping, invariant↔impl-enforcement mapping). **Files**: `specs/corvus.tla` (new ~530 LOC); `specs/corvus.cfg` (clean cfg); `specs/corvus_buggy_unwrap_cross_user.cfg`; `specs/corvus_buggy_auth_binding_mutate.cfg`; `specs/corvus_buggy_admin_without_proc_cap.cfg`; `specs/corvus_buggy_elevate_without_console.cfg`; `specs/corvus_buggy_transfer_rebind.cfg` (new buggy cfgs); `specs/SPEC-TO-CODE.md` (+corvus.tla section); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (this row + R15-d row marked LANDED); memory updates. Test count 442/442 unchanged × default + UBSan (spec-only commit). **Stage**: spec-gated; impl ready to start. **Next**: P5-corvus-bringup-a (`usr/corvus/` skeleton crate; startup hardening; idle loop). | 442/442 PASS × default + UBSan. |
| `c819169` | **P5-audit-r15-d: hash fixup** | (doc-only) |
| `f306529` | **P5-audit-r15-d: close F237 (P3) + F238 (P3) + F239 (P3) + F240 (P3) — R15 polish bundle.** **F237** SYS_GETRANDOM partial-fault entropy scrubbing — on `uaccess_store_u8` failure mid-stream, zero the partial range (best-effort; uaccess to zero may itself fail — caller sees -1 and must not trust the buffer) + zero scratch. Pre-fix: partial CSPRNG bytes lingered in user buffer while syscall returned -1, exposing partial entropy that the caller might misuse. **F238** `dev9p_attach_client` error path now uses `spoor_clunk(c)` instead of `spoor_unref(c)` for uniformity with all other Spoor-release sites in dev9p.c. Both are correct (Spoor was just allocated with ref=1 and c->aux=NULL; dev9p_close handles NULL aux safely via priv_of's magic check); the canonical Plan-9-shape teardown uses clunk. **F239** `sys_attach_9p_handler` n_uname u64→u32 truncation — added explicit `if (n_uname > 0xFFFFFFFFu) return -1` guard. 9P2000.L's n_uname is u32 on the wire; pre-fix the syscall did `(u32)n_uname` blindly, masking high bits silently. **F240** `sys_wait_pid_handler` status_out partial-write — on per-byte `uaccess_store_u8` failure after the child has been reaped, best-effort zero the partial range so caller doesn't read torn status. Pre-fix: partial bytes [0..i) of the kernel int were visible to userspace while the syscall returned -1, mixing "old user data" with "partial reaped status." No new tests — all four are degenerate-path patches in already-tested handlers. F237 partial-fault path can only fire on actual uaccess failure (unmapped page) which our test harness doesn't easily simulate; F239 truncation is a guard that fires before any wire activity; F240 partial-write same as F237; F238 is purely structural. The fixes are correctness-by-construction. **R15 audit progress**: 9 of 11 findings closed (F231 + F233 + F234 + F230 + F232 + F237 + F238 + F239 + F240); 2 deferred to enabling chunks (F235 RFFDG, F236 SYS_WALK). The R15 audit is now FUNCTIONALLY CLOSED — only the latent-on-future-features findings remain, and those are tracked as preconditions for the chunks that introduce the enabling code. **Files**: `kernel/syscall.c` (sys_getrandom_handler F237 scrub + sys_attach_9p_handler F239 guard + sys_wait_pid_handler F240 scrub); `kernel/dev9p.c` (F238 spoor_unref → spoor_clunk); `memory/audit_r15_closed_list.md` (extended with F237-F240 close notes); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (this row + P5-audit-r15-c section marked LANDED). Test count 442/442 unchanged × default + UBSan. **Stage**: R15 audit functionally closed. **Next**: P5-corvus-bringup (the next major architectural chunk — implements /sbin/corvus Thylacine-native daemon using libstratum-crypto via library link). | 442/442 PASS × default + UBSan. |
| `0288d98` | **P5-audit-r15-c: hash fixup** | (doc-only) |
| `9641281` | **P5-audit-r15-c: close F230 (P1) + F232 (P2) — per-client spin_lock_t around every public 9P client op + handles.tla spec extension `RforkWithFds` + `SpawnFdsRightsMonotonic` invariant.** F230: cross-Proc dev9p Spoor sharing (post-spawn-with-fds inheritance) made `struct p9_client`'s session bookkeeping (next_fid, outstanding[], bound_fids[], out_buf, total_ops/total_errors counters) racy on SMP. **Fix**: added `spin_lock_t lock` to `struct p9_client`; every public `p9_client_*` op acquires it at entry, releases at exit. `CLIENT_UNLOCK_RET(c, rc)` helper macro handles early-return paths cleanly without converting the function body to do-while-0 + break. `p9_client_alloc_fid` also acquires (uniformity over atomic). Diagnostic readers (`is_open`, `inflight`) take the lock briefly for non-torn reads. The macro is preferred over the `_locked` helper-function pattern because it preserves the existing function structure (less churn; ~20 functions touched, each with 2-4 LOC of insertions). **Why client-level not session-level**: client owns the session by value, owns out_buf, owns next_fid. One lock covers everything the client could mutate. Direct callers of `p9_session_*` below the client are typically test code; they self-serialize. Per-session callers via the client see proper atomicity. F232: handles.tla extended with `RforkWithFds(parent, child, h, new_rights)` action modeling the fd-inheritance pattern introduced by SYS_SPAWN_WITH_FDS / SYS_SPAWN_FULL + `BuggySpawnFdsElevate` bug class that skips the rights-subset guard + new `SpawnFdsRightsMonotonic` invariant cross-checking child handles against the `spawn_inherits` ghost ledger. Captures R15-a's F231 close at the spec level: child's spawn-inherited rights MUST be ⊆ parent's rights AT THE TIME OF INHERITANCE. Distinct from `RightsCeiling` which bounds by origin_rights (catches super-origin fabrication); `SpawnFdsRightsMonotonic` bounds by parent's CURRENT rights (catches the F231 shape: parent had {R}, child got {R,W} where origin had {R,W,T}). Buggy cfg `handles_buggy_spawn_fds_elevate.cfg` produces counterexample at depth 4 in <1s. 9p_client.tla extended with a Concurrency / Lock Discipline section documenting how the existing buggy variants (TagCollision, OOOMatch, FidAfterClunk, Unbounded) cover the spec-level consequences of F230's race conditions — the atomic-action style already pins the lock discipline (mirrors pipe.tla's modeling pattern). State constraint added to handles.cfg + all buggy cfgs (Cardinality(handles[p]) ≤ 2 + Cardinality(spawn_inherits) ≤ 2) to keep the clean run tractable post-extension; clean cfg verified at full state-space (574M states; 26M distinct; depth 26; 39 min). **Regression test**: new `9p_client.lock_released_between_ops` — verifies the lock value is unlocked between every public op (init/handshake/walk/clunk/alloc_fid/is_open/inflight), exercising the acquire/release plumbing on UP. Two consecutive `alloc_fid` calls assert monotonicity. **Single-CPU posture**: on UP the spin part is a no-op; the IRQ-mask discipline isn't required here (9P client paths are syscall/kthread context, never IRQ). SMP race detection awaits a future P5-tsan-enable chunk. **R15 audit progress**: 5 findings now closed (F231 + F233 + F234 + F230 + F232); 4 still pending (F237-F240 at R15-d; F235/F236 latent on RFFDG / SYS_WALK). **Files**: `kernel/include/thylacine/9p_client.h` (+spin_lock_t field; +include; +concurrency doc); `kernel/include/thylacine/9p_session.h` (updated concurrency note); `kernel/9p_client.c` (+CLIENT_UNLOCK_RET macro; spin_lock_init in p9_client_init; spin_lock/spin_unlock around every public op); `kernel/test/test_9p_client.c` (+test_9p_client_lock_released_between_ops); `kernel/test/test.c` (+1 forward decl + 1 registry); `specs/handles.tla` (+RforkWithFds + BuggySpawnFdsElevate actions + spawn_inherits VARIABLE + SpawnFdsRightsMonotonic invariant + StateConstraint; +"spawn" via case); `specs/handles.cfg` + 8 existing buggy cfgs (+BUGGY_SPAWN_FDS_ELEVATE = FALSE + CONSTRAINT clause); `specs/handles_buggy_spawn_fds_elevate.cfg` (new); `specs/9p_client.tla` (Concurrency / Lock Discipline header section ~50 LOC); `memory/audit_r15_closed_list.md` (extended with F230 + F232 close notes); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (this row + P5-audit-r15-b section marked LANDED). Test count 441 → 442; **442/442 PASS × default + UBSan**. **Stage**: 9P client SMP-safe; precondition for P5-corvus-bringup met. **Next**: P5-audit-r15-d (P3 polish F237-F240) OR P5-corvus-bringup (gated on R15 audit close; can start now or after R15-d). | 442/442 PASS × default + UBSan. |
| `f8f7f22` | **P5-audit-r15-b: hash fixup** | (doc-only) |
| `1025026` | **P5-audit-r15-b: close F233 (P1) + F234 (P2) — atomic refcounts for Spoor + pipe_ring; SMP correctness for cross-Proc inheritance.** F233: Spoor->ref's six access sites in `kernel/spoor.c` converted to `__atomic_*` ops (RELAXED init store at alloc; `fetch_add(ACQ_REL)` in spoor_ref; `fetch_sub(ACQ_REL)` in spoor_unref + spoor_clunk; ACQUIRE-load in diagnostics). Pre-value capture on fetch_sub: pre == 1 → was last drop → run free / dev->close; pre <= 0 → extinct (use-after-free guard). F234: same idiom for `kernel/pipe.c` pipe_ring->ref (init=2; concurrent endpoint close races eliminated). **Standard atomic-refcount idiom**: ACQ_REL ordering ensures all CPUs see prior writes to the resource's fields BEFORE the last-drop free; pre-value capture ensures exactly one CPU sees pre==1 even under concurrent decrement from N CPUs. **Semantic shift in spoor_clunk**: dev->close runs AFTER the atomic decrement now (ref=0, storage intact, no concurrent observers possible). Pre-fix: dev->close ran while ref was still 1. Post-fix: dev->close hooks still safely access c->aux / c->dev / c->qid — storage is valid until spoor_free_internal. **No new tests** — atomic discipline is correctness-by-construction; existing test suite is the regression. SMP race detection awaits a future P5-tsan-enable chunk. **No spec extension** — atomic ops are implementation refinement; abstract spec (refcount drops to 0 → free) unchanged. **Audit posture**: kernel/spoor.c + kernel/pipe.c are existing audit-trigger surfaces. No new attack surfaces. **R15 audit progress**: 3 findings now closed (F231 at R15-a + F233 + F234 at this); 4 still active (F230, F232 at R15-c; F237-F240 at R15-d); 2 deferred to enabling chunks (F235 RFFDG, F236 SYS_WALK). **Files**: `kernel/spoor.c` (~25 LOC diff; 6 ref sites converted); `kernel/pipe.c` (~15 LOC diff; 2 ref sites converted); `memory/audit_r15_closed_list.md` (extended with F233 + F234 close notes); `docs/REFERENCE.md` (Tip); `docs/phase5-status.md` (this row + P5-audit-r15-a section marked LANDED); memory updates. Test count 441/441 unchanged × default + UBSan. **Stage**: SMP refcount races closed (the most reachable Phase 5 race). **Next**: P5-audit-r15-c (9P session per-session lock + atomic next_fid + per-call out_buf; F230 close; precondition for P5-corvus-bringup — dev9p Spoor inheritance) OR P5-audit-r15-d (P3 polish bundle). | 441/441 PASS × default + UBSan. |
| `b70192e` | **P5-audit-r15-a: hash fixup** | (doc-only) |
| `2e7afae` | **P5-audit-r15-a: close F231 (rights elevation in spawn-with-fds + spawn-full) — Opus-4.7-max-effort adversarial audit found that the child's `handle_alloc` hardcoded `RIGHT_READ\|WRITE\|TRANSFER`, ignoring parent's slot rights → I-6 violation.** ACTIVE bug (a parent with `RIGHT_READ`-only fd spawns a child that gets full READ|WRITE|TRANSFER on the same Spoor — privilege elevation across the spawn boundary). **Fix**: `struct spawn_with_fds_args` gains `rights[SYS_SPAWN_MAX_FDS]`; new `sys_bump_inherit_fds` helper captures `slot->rights` alongside `spoor_ref` bump; child thunk's `handle_alloc` uses captured rights instead of hardcoded mask. Both SYS_SPAWN_WITH_FDS and SYS_SPAWN_FULL handlers updated. **Regression test** `sys_spawn_with_fds.child_rights_subset_of_parent`: parent allocates a Spoor with `RIGHT_READ` only; spawns /hello with that fd; polls (bounded; up to 1024 sched yields) for the child's slot to appear in its handle table; asserts `child_slot->rights == RIGHT_READ`. Pre-fix this test would fail (child would have full rights). **Why polling**: thunk's handle_alloc runs asynchronously on the child's CPU. Caps are race-free (set inside rfork_with_caps before thread ready'd); rights/handle inheritance is set IN the thunk after the thread starts. Bounded poll-with-sched-yield matches the design constraint without adding a race-aware syscall surface. **R15 audit context**: cumulative Phase 5 spawn + mount + lifecycle audit since R14 P4-Z; 11 findings (3 P1 + 4 P2 + 4 P3); F231 is the only ACTIVE P1; F230 + F233 + F234 + F232 are reachable-or-latent SMP/spec items held for R15-b/c; F235 + F236 are latent (gated on future RFFDG / SYS_WALK); F237-F240 are P3 polish for R15-d. Closed-list: `memory/audit_r15_closed_list.md`. **Files**: `kernel/syscall.c` (struct spawn_with_fds_args + rights[] field; sys_bump_inherit_fds helper extracted; sys_spawn_with_fds_thunk uses captured rights; sys_spawn_with_fds_for_proc + sys_spawn_full_for_proc populate sa->rights[]); `kernel/test/test_sys_spawn_with_fds.c` (+ child_rights_subset_of_parent test ~50 LOC + sched.h include); `kernel/test/test.c` (+1 forward decl + 1 registry entry); `memory/audit_r15_closed_list.md` (new; tracks all 11 findings + closing arcs); `docs/REFERENCE.md` (Tip + audit context); `docs/phase5-status.md` (this row + P5-spawn-full section marked LANDED). Test count 440 → 441; **441/441 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: F231 (active P1) closed; 5 findings deferred to R15-b/c/d sub-chunks; 2 findings deferred to future enabling chunks. **Next**: P5-audit-r15-b (close F233 + F234 — atomic refcounts for Spoor + pipe_ring; SMP correctness; reachable race) OR P5-audit-r15-c (close F230 — 9P session per-session lock + atomic next_fid + per-call out_buf; precondition for P5-corvus-bringup which inherits dev9p Spoors). | 441/441 PASS × default + UBSan. |
| `a91a6e2` | **P5-spawn-full: hash fixup** | (doc-only) |
| `d1b0d64` | **P5-spawn-full: `SYS_SPAWN_FULL` (= 25) — combines fd inheritance + cap-subset in one syscall; the fourth and most general spawn variant.** ABI: `SYS_SPAWN_FULL(name_va, name_len, fd_list_va, fd_count, cap_mask) → child_pid/-1`. Unions SYS_SPAWN_WITH_FDS + SYS_SPAWN_WITH_CAPS. Needed at P5-corvus-bringup where joey spawns /sbin/corvus with both a pipe pair (login comm) AND CAP_LOCK_PAGES | CAP_CSPRNG_READ. **Reuses** sys_spawn_with_fds_thunk + spawn_with_fds_args; only difference vs SYS_SPAWN_WITH_FDS is rfork_with_caps instead of rfork. Cap arithmetic happens inside rfork_with_caps BEFORE thunk runs (caps set synchronously); fd installation happens IN the thunk (asynchronously). **Four spawn variants** now coexist; each call site reads more clearly with the variant matching its needs. **Mid-chunk discovery**: first draft tested fd inheritance by directly reading child->handles[0] from the parent — failed with race (thunk's handle_alloc runs async on child's CPU). Resolution: rely on indirect coverage — caps inspected directly (race-free; set inside rfork_with_caps before thread ready'd); fds verified indirectly via clean-exit assertion + the existing /stub-driver_round_trip end-to-end test (which exercises the same thunk + struct). **5 new kernel-internal tests** in `kernel/test/test_sys_spawn_full.c`: happy_path_fds_and_caps (verifies child has exactly CAP_LOCK_PAGES + /hello exits 0) + zero_count_zero_mask_succeeds (≡ SYS_SPAWN) + rejects_oversize_fd_count + rejects_bad_fd + rejects_missing_binary. libt stub `t_spawn_full(name, name_len, fds, fd_count, cap_mask) → pid` (5-arg inline-asm using x0..x4 + x8). **No spec extension** — pure union of existing primitives. **Audit posture**: extends kernel/syscall.c (existing). No new attack surfaces — both paths reuse existing audited primitives. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_FULL=25 + doc-comment); `kernel/syscall.c` (+~115 LOC); `kernel/test/test_sys_spawn_full.c` (new; 5 tests; ~145 LOC); `kernel/test/test.c` (+5 forward decls + 5 registry); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_FULL=25 + t_spawn_full stub); `docs/reference/64-sys-spawn-full.md` (new; ~170 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 64); `docs/phase5-status.md` (this row + P5-spawn-caps section marked LANDED). Test count 435 → 440; **440/440 PASS × default + UBSan**. **Stage**: spawn surface complete for P5-corvus-bringup. **Next**: P5-stratumd-stub-c (long-running stub) OR P5-stratumd-stub-d (pivot/chroot) OR P5-close audit (R15-class adversarial review). | 440/440 PASS × default + UBSan. |
| `a5313f1` | **P5-spawn-caps: hash fixup** | (doc-only) |
| `e3d34e3` | **P5-spawn-caps: `SYS_SPAWN_WITH_CAPS` (= 24) — grants cap-subset to spawned child via existing `rfork_with_caps` (P4-Ic3).** ABI: `SYS_SPAWN_WITH_CAPS(name_va, name_len, cap_mask) → child_pid/-1`. Child's caps = `parent->caps & cap_mask`. **Structural enforcement of ARCH I-2 / I-6**: extra bits in cap_mask silently dropped at AND; no rejection needed. Needed at P5-corvus-bringup so joey can spawn /sbin/corvus with `CAP_LOCK_PAGES | CAP_CSPRNG_READ` (subset of joey's CAP_ALL). **Reuses** sys_spawn_thunk + spawn_args from SYS_SPAWN; only difference is `rfork_with_caps` vs `rfork`. **Why a third spawn variant**: explicit intent at call site; future SYS_SPAWN_FULL combines fds + caps when needed. **Tests use proc_find_by_pid** to read child->caps BEFORE wait_pid (caps stable for Proc lifetime; race-free even when child runs concurrently on another CPU). **5 new kernel-internal tests** in `kernel/test/test_sys_spawn_with_caps.c`: happy_path_zero_mask (CAP_NONE → child caps == 0) + happy_path_subset_of_parent (CAP_LOCK_PAGES → child has exactly CAP_LOCK_PAGES, kproc has CAP_ALL) + clamps_to_parent (mask=0xFFF...FF → child clamped to CAP_ALL; no spurious bits) + rejects_missing_binary + rejects_oversize_name. **libt stub** `t_spawn_with_caps(name, name_len, cap_mask) → pid`. **No spec extension** — cap arithmetic already covered by `specs/handles.tla::RforkWithCaps` from P4-Ic3. **Audit posture**: extends kernel/syscall.c (existing). No new attack surfaces — name uaccess reuses existing primitives; cap arithmetic delegated to audited rfork_with_caps. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_WITH_CAPS=24); `kernel/syscall.c` (+~75 LOC); `kernel/test/test_sys_spawn_with_caps.c` (new; 5 tests; ~130 LOC); `kernel/test/test.c` (+5 forward decls + 5 registry); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_WITH_CAPS=24 + t_spawn_with_caps stub); `docs/reference/63-sys-spawn-with-caps.md` (new; ~165 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 63); `docs/phase5-status.md` (this row + P5-stratumd-stub-bringup-b section marked LANDED). Test count 430 → 435; **435/435 PASS × default + UBSan**. **Stage**: corvus startup precondition syscalls all present (CAP_LOCK_PAGES + CAP_CSPRNG_READ grantable via SYS_SPAWN_WITH_CAPS). **Next**: P5-stratumd-stub-c (long-running stub) OR P5-spawn-full (combined fds + caps) OR P5-stratumd-stub-d (pivot/chroot). | 435/435 PASS × default + UBSan. |
| `904553c` | **P5-stratumd-stub-bringup-b: hash fixup** | (doc-only) |
| `73784b4` | **P5-stratumd-stub-bringup-b: `SYS_SPAWN_WITH_FDS` (= 23) + new `/stub-driver` userspace binary; production-shape orchestration at EL0.** Extends `SYS_SPAWN` with explicit fd inheritance: caller names KOBJ_SPOOR fds (≤ SYS_SPAWN_MAX_FDS = 16) that get installed in child's handle table at slots 0..fd_count-1 BEFORE exec_setup. **Refcount discipline (load-bearing)**: handler bumps spoor_ref for each listed fd; parent retains own holds (this is "give child own ref," not "transfer"); child's handle_alloc consumes the bumped ref. Failure paths drop all bumps symmetrically. **Why separate syscall**: extending SYS_SPAWN's 2-arg ABI would break the libt stub (existing x2/x3 garbage suddenly meaningful). **Why KOBJ_SPOOR-only**: ARCH I-5 prohibits MMIO/IRQ/DMA cross-Proc transfer; Burrow held until v1.x. **Why explicit-list, not Plan 9 RFFDG**: cleaner for "give child these specific fds"; real SYS_RFORK with COW lands when a v1.x workload needs the general primitive. **/stub-driver** (~110 LOC userspace binary): t_pipe × 2 → t_spawn_with_fds("stratumd-stub", [c2s_rd, s2c_wr], 2) → t_close driver-side copies → t_attach_9p → t_mount(99) → t_unmount(99) → t_close(attach_fd) + t_close transport fds → t_wait_pid(stub) → t_exits(0). The full production-shape orchestration with no kernel-supervised setup. **Test framework** (`test_stub_driver.c`, ~85 LOC): just load + rfork-exec /stub-driver + wait_pid + assert status=0. Driver does all real work. **Rejection tests** (`test_sys_spawn_with_fds.c`, ~120 LOC, 5 tests): rejects_oversize_fd_count + rejects_bad_fd + rejects_non_spoor_fd (Burrow handle) + rejects_missing_binary + zero_count_succeeds (fd_count=0 ≡ SYS_SPAWN). **No spec extension** — pure composition over spawn + handle + pipe; fd-inheritance is local refcount bookkeeping. **Audit posture**: extends kernel/syscall.c (existing). No new attack surfaces — name uaccess + fd-list uaccess use existing uaccess_load_u8 + UACCESS_USER_VA_TOP bound + fault-fixup; handle_get/handle_alloc/spoor_ref/spoor_unref are existing audited primitives. **Sub-chunk arc**: a (userspace responder + 2-Proc kernel-supervised demo) + **b (this — SYS_SPAWN_WITH_FDS + /stub-driver production-shape)** → c (long-running stub serving multiple clients) → d (pivot/chroot mechanism) → e (full joey-in-production). **Boot log on success**: `/stub-driver size=17280 → exec; driver orchestrates stratumd-stub itself` + `stratumd-stub: serving on fd 0 (rx) + fd 1 (tx)` + `stratumd-stub: EOF on rx; exit 0` + `stub-driver: PASS` + `/stub-driver reaped pid=N status=0 — SYS_SPAWN_WITH_FDS production-shape orchestration verified end-to-end`. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN_WITH_FDS=23 + SYS_SPAWN_MAX_FDS=16); `kernel/syscall.c` (+~170 LOC: spawn_with_fds_args + sys_spawn_with_fds_thunk + sys_spawn_with_fds_for_proc + sys_spawn_with_fds_handler + 1 dispatcher case); `kernel/test/test_sys_spawn_with_fds.c` (new; 5 tests); `kernel/test/test_stub_driver.c` (new; 1 test); `kernel/test/test.c` (+7 forward decls + 7 registry entries); `kernel/CMakeLists.txt` (+2 test sources); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN_WITH_FDS=23 + T_SPAWN_MAX_FDS + t_spawn_with_fds stub); `usr/stub-driver/{stub-driver.c, CMakeLists.txt}` (new); `usr/CMakeLists.txt` (+add_subdirectory); `tools/build.sh` (+"stub-driver" in usr_bins); `docs/reference/62-sys-spawn-with-fds.md` (new; ~250 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 62); `docs/phase5-status.md` (this row + P5-stratumd-stub-bringup-a section marked LANDED). Test count 424 → 430; **430/430 PASS × default + UBSan**. **Stage**: production-shape userspace orchestration unblocked. **Next**: P5-stratumd-stub-c (long-running stub) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS; gated on P5-corvus-bringup) OR P5-stratumd-stub-d (pivot/chroot). | 430/430 PASS × default + UBSan. |
| `5d14d7f` | **P5-stratumd-stub-bringup-a: hash fixup** | (doc-only) |
| `479d997` | **P5-stratumd-stub-bringup-a: userspace 9P responder `/stratumd-stub` + kernel-supervised 2-Proc demo (stub + `/attach-probe` wired via 2 pipe pairs).** First Phase 5 test running TWO userspace Procs cooperating over a real wire — until this chunk, every userspace 9P test used a kernel-thread responder. Proves a USERSPACE process can be the 9P server, which is the production shape for real `stratumd-system` / `stratumd-michael` / etc. **`usr/stratumd-stub/stratumd-stub.c`** (~170 LOC userspace responder; reads framed 9P Tmsgs from fd 0; builds Rversion / Rattach / Rclunk / fallback Rlerror; writes to fd 1; loops until EOF on rx → exit 0; wire encoding inlined — no libc available). **`kernel/test/test_stratumd_stub.c`** (~210 LOC kernel-supervised framework; pipe_create × 2 → 4 Spoors; rfork stub Proc with c2s_rd@fd0 + s2c_wr@fd1; rfork client Proc with c2s_wr@fd0 + s2c_rd@fd1; wait_pid both; collects PIDs in any order; asserts both status=0). **Refcount discipline (load-bearing)**: each pipe_create Spoor's ref=1 TRANSFERRED (not bumped) to its owner's handle table; boot retains 0 refs after rfork thunks. **Why kernel-supervised, not joey-driven**: production joey orchestrates from userspace, but that needs RFFDG (Plan 9 flag — copy fd table at fork) or SYS_SPAWN_WITH_FDS — held as P5-stratumd-stub-bringup-b. This chunk demonstrates the wiring works end-to-end at EL0 ↔ EL0 via the kernel's 9P client + dev9p + spoor-transport + pipe stack. **Sub-chunk arc**: a=this (userspace responder + 2-Proc kernel-supervised demo) → b=RFFDG / SYS_SPAWN_WITH_FDS (joey-driven from userspace) → c=long-running stub serving multiple clients → d=pivot/chroot kernel mechanism → e=full joey-in-production orchestration. **No spec extension** — pure composition over already-spec'd 9P client + pipe + handle layers. **Audit posture**: kernel/test/test_stratumd_stub.c is test-only (not on audit-trigger surface). The stratumd-stub binary at EL0 uses existing syscalls (t_read/t_write/t_putstr/t_exits) with valid arg shapes; no new attack surface. **Boot log shows live**: `stratumd-stub: serving on fd 0 (rx) + fd 1 (tx)` + `attach-probe: PASS` + `stratumd-stub: EOF on rx; exit 0` + `stratumd-stub pid=N1 status=0 + attach-probe pid=N2 status=0 — userspace 9P server end-to-end verified`. **Files**: `usr/stratumd-stub/stratumd-stub.c` (new; ~170 LOC); `usr/stratumd-stub/CMakeLists.txt` (new); `usr/CMakeLists.txt` (+add_subdirectory); `tools/build.sh` (+"stratumd-stub" in usr_bins); `kernel/test/test_stratumd_stub.c` (new; ~210 LOC); `kernel/test/test.c` (+1 forward decl + 1 registry entry `userspace.stratumd_stub_round_trip`); `kernel/CMakeLists.txt` (+1 test source); `docs/reference/61-stratumd-stub.md` (new; ~190 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 61); `docs/phase5-status.md` (this row + P5-stratum-api-spec section marked LANDED). Test count 423 → 424; **424/424 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: P5-stratumd-stub-bringup arc has its foundation. **Next chunks**: P5-stratumd-stub-b (RFFDG or SYS_SPAWN_WITH_FDS — joey-driven orchestration from userspace; smallest natural follow-on) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for cap-subset; needed at P5-corvus-bringup) OR Stratum-side parallel work per STRATUM-API-V1.md (Michal-owned). | 424/424 PASS × default + UBSan. |
| `4d6b3d3` | **P5-stratum-api-spec: hash fixup** | (doc-only) |
| `bd19cf7` | **P5-stratum-api-spec: `docs/STRATUM-API-V1.md` (~710 LOC) — complete Thylacine-side spec for the 5 Stratum API additions Thylacine needs at v1.0 + 1 v1.x deferral.** Detailed version of `CORVUS-DESIGN.md §13` with concrete CLI shapes, wire frame layouts, error codes, behavioural contracts, test matrices, open questions, audit-trigger surfaces. **6 asks**: A1 `--bind-pool-serial` + `pool_serial` superblock field (smallest; STM_ESERIAL on mismatch; closes C-14 evil-maid defence; unblocks P5-stratumd-bringup-real) + A2 multi-stratumd-per-pool with `--datasets-allowed` patterns + STM_EDATASETNOTALLOWED + pool-level coordination + crash recovery (unblocks P5-login per-user stratumd) + A3 corvus UNWRAP wire (Stratum learns corvus's binary frame format from `CORVUS-DESIGN §6.4`; session-token-file CLI; status code mapping; protocol version byte; unblocks P5-corvus-bringup) + A4 corvus notify Spoor / DEK eviction (SESSION_CLOSED frame consumer; strict vs tolerant mode with 30s timeout; unblocks P5-login logout flow) + A5 snapshot rollback compromise marking (corvus-driven admin verb `mark-rollback-compromised`; marker survives export/import; force-rollback override; closes C-13) + A6 per-dataset key rotation (deferred to v1.x). **Priority order**: A1 → A4 → A2 → A3 → A5 → A6. Stratum coordination protocol enumerated (§11). **Bilateral contract** (§10): Thylacine-side commitments — pool-serial discipline, multi-stratumd discipline, wire discipline, eviction discipline, rollback marker discipline, audit cooperation. **Cross-cutting concerns**: format-version handling for pool superblock additions (feature-flag with all-zero-default semantics), audit posture per-ask, integration test matrices, doc discipline both sides. **Hard-dep tier**: A1 alone unblocks the boot-into-Stratum-mounted-root milestone; A2+A3+A4 needed for the post-pivot stack. **Cross-references**: CORVUS-DESIGN.md §13 now points to this doc as the detailed version; CORVUS-DESIGN.md §14 cross-references list includes STRATUM-API-V1.md. **No code; scripture only.** Tests unchanged at 423/423 × default + UBSan. **Audit posture**: scripture-only commit; the audit-bearing implementations are on the Stratum side. Thylacine-side audit-trigger surfaces don't change. **Stage**: Stratum-side parallel-trackable; Michal can chunk Stratum-side work in parallel with Thylacine's P5-stratumd-stub-bringup + P5-spawn-caps. **Files**: `docs/STRATUM-API-V1.md` (new; ~710 LOC); `docs/CORVUS-DESIGN.md` (§13 + §14 cross-refs); `docs/REFERENCE.md` (Tip line); `docs/phase5-status.md` (this row + P5-spawn-wait section marked LANDED); memory updates. **Next Thylacine-side chunks**: P5-stratumd-stub-bringup (architectural moment — kernel-internal stratumd-stub + joey forks it + kernel-mounts-as-root mechanism; no Stratum dependency) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for cap-subset; gated on a use case). | 423/423 PASS unchanged (scripture-only chunk). |
| `3915559` | **P5-spawn-wait: hash fixup** | (doc-only) |
| `335d87d` | **P5-spawn-wait: `SYS_SPAWN` (= 21) + `SYS_WAIT_PID` (= 22) — the smallest userspace orchestration primitives; `/joey` now orchestrates `/hello` end-to-end at boot.** ABI: `SYS_SPAWN(name_va, name_len) → child_pid/-1` (combined `rfork(RFPROC)` + `exec` against a devramfs-named binary; name ≤ SYS_SPAWN_NAME_MAX = 64; blob ≤ SYS_SPAWN_BLOB_MAX = 32 KiB; child inherits no caps); `SYS_WAIT_PID(status_out_va) → reaped_pid/-1` (mirrors `kernel/proc.c::wait_pid`; status optional via NULL `status_out_va`; per-byte `uaccess_store_u8` × 4 for the int write). **Plan 9 `rfork(RFPROC\|RFEXEC)` idiom** sidesteps the COW + child-context restoration that a real `SYS_RFORK` needs — child gets a fresh address space from the named binary, not a copy of the parent. A separate `SYS_RFORK` with COW lands when a v1.x workload requires it. **Lifetime discipline**: `sys_spawn_thunk` inherits ownership of two kmalloc'd buffers (8-aligned blob copy + args struct); thunk frees both after `exec_setup`. On rfork failure, parent frees them. The 8-aligned copy is mandatory (cpio newc is 4-aligned; `elf_load` requires 8 per R5-G F61). **Factoring**: `int sys_spawn_for_proc(struct Proc *p, const char *name, size_t name_len)` extracted as non-static inner so kernel-internal tests drive it with kernel-resident names; SVC wrapper does user-VA validation + per-byte name copy into a 65-byte kernel-stack scratch then calls the inner. `sys_wait_pid_handler` not extracted — its only non-trivial step is per-byte uaccess store; tests call kernel-internal `wait_pid()` directly. **Joey demonstration**: `usr/joey/joey.c` now spawns /hello, waits for it, verifies status=0, prints "orchestration verified", exits 0. Boot log shows four interleaved lines: joey banner + joey spawn message + hello's own banner + joey reap message. **Userspace libt stubs**: `t_spawn(name, name_len) → pid/-1`, `t_wait_pid(int *status) → pid/-1`, `T_SPAWN_NAME_MAX = 64u`. **7 new kernel-internal tests** in `kernel/test/test_sys_spawn.c`: sys_spawn.happy_path (spawn "hello" + wait_pid + status=0) + sys_spawn.rejects_null_name + sys_spawn.rejects_zero_len + sys_spawn.rejects_oversize_name + sys_spawn.rejects_missing_binary + sys_spawn.rejects_embedded_nul + sys_wait_pid.no_children_returns_neg1. Each test calls `drain_zombies()` first for deterministic ordering. **No spec extension** — orchestration shape is pure composition over `specs/scheduler.tla` (rfork + ready + wait_pid wakeup) + `specs/handles.tla` (fresh address space, no fd-table sharing). **Audit posture**: extends `kernel/syscall.c` (existing audit-trigger surface). No new attack surfaces — uaccess_load_u8 + UACCESS_USER_VA_TOP bound + fault-fixup reused; the rfork machinery is the existing audited primitive (joined trigger list at P2-D). The kmalloc-blob-copy + free pattern is symmetric and covered by the 7 tests + the live boot. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_SPAWN=21 + SYS_WAIT_PID=22 + SYS_SPAWN_NAME_MAX + SYS_SPAWN_BLOB_MAX); `kernel/syscall.c` (+devramfs.h + elf.h + exec.h includes; ~110 LOC for sys_spawn_thunk + sys_spawn_for_proc + sys_spawn_handler + sys_wait_pid_handler + 2 dispatcher cases); `kernel/test/test_sys_spawn.c` (new; 7 tests; ~110 LOC); `kernel/test/test.c` (+7 forward decls + 7 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_SPAWN=21 + T_SYS_WAIT_PID=22 + T_SPAWN_NAME_MAX macro + t_spawn + t_wait_pid stubs); `usr/joey/joey.c` (orchestration body + tiny itoa_dec helper); `docs/reference/60-sys-spawn-wait.md` (new; ~230 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 60); `docs/phase5-status.md` (this row + P5-joey-from-ramfs section marked LANDED). Test count 416 → 423; **423/423 PASS × default + UBSan**. **Stage**: P5-stratumd-stub-bringup is the natural next chunk — joey can spawn a stratumd-stub binary the same way it spawns /hello today. **Next chunks**: P5-stratumd-stub-bringup (the architectural moment — userspace stratumd-stub binary serving a tiny 9P tree; joey forks it, attaches, mounts as root, verifies) OR P5-spawn-caps (SYS_SPAWN_WITH_CAPS for granting a cap-subset; needed for corvus startup at P5-corvus-bringup) OR P5-stratumd-multi (Stratum-side, Michal-owned, parallel-trackable). | 423/423 PASS × default + UBSan. |
| `c401ccf` | **P5-joey-from-ramfs: hash fixup** | (doc-only) |
| `2abe83f` | **P5-joey-from-ramfs: `/joey` becomes a real userspace binary built from `usr/joey/` and shipped in the initrd cpio, replacing the prior 9-instruction kernel-embedded blob in `kernel/joey.c`.** This is the structural prerequisite for the P5-stratumd-bringup arc: `/joey` can now do arbitrary userspace work (fork stratumd, drive SYS_ATTACH_9P + SYS_MOUNT, eventually pivot root), which the 36-byte hand-encoded predecessor could not. Orchestration shape unchanged (rfork → exec_setup → userland_enter → wait_pid); the source of the ELF moved from compile-time-embedded BSS to a `devramfs_lookup("joey", ...)` call. **Mandatory 8-aligned static-buffer copy** (`g_joey_elf_blob`, 32 KiB): cpio newc data is only 4-byte-aligned, but `kernel/elf.c::elf_load` rejects misaligned Ehdr (R5-G F61). Same pattern as `kernel/test/test_userspace_ramfs.c`. **Boot diagnostic disambiguator**: `joey: rforking child for /joey (<N> byte ELF from initrd)` (prior boots said `9-instr hello blob`). **Userspace banner**: `joey: hello from /joey (real userspace binary, loaded from ramfs)\n` (prior was bare `hello\n`). **Failure paths** all extinct with descriptive prefixes: missing from cpio / zero size / exceeds JOEY_BLOB_MAX / exec_setup failed (rc printed) / EL0 fault. **No new tests**: the boot path (boot_main → joey_run → /joey → SYS_EXITS(0) → "Thylacine boot OK") IS the regression — if /joey breaks, joey_run extincts and the banner never prints. **No spec extension** — the orchestration shape is unchanged; only the byte source moved. **Audit posture**: kernel/joey.c is not on the audit-trigger surface (init-time orchestration helper). The cpio → 8-aligned-copy pattern reuses the test-side approach already in-tree. **Files**: `usr/joey/joey.c` (new; ~20 LOC); `usr/joey/CMakeLists.txt` (new); `usr/CMakeLists.txt` (+add_subdirectory(joey)); `tools/build.sh` (+"joey" in usr_bins); `kernel/joey.c` (build_init_elf + g_joey_program[] removed; devramfs.h + elf.h includes added; JOEY_RAMFS_NAME macro; 8-aligned g_joey_elf_blob; joey_run calls devramfs_lookup + copies into the static buffer; joey_thunk gained an "exec_setup failed rc=N" diagnostic on the failure path); `docs/reference/59-joey-from-ramfs.md` (new; ~140 LOC); `docs/REFERENCE.md` (Tip + doc-table entry 59); `docs/phase5-status.md` (this row + section + P5-corvus-syscalls section marked LANDED); `memory/MEMORY.md` (entry); `memory/project_active.md` (description); `memory/project_next_session.md` (rewritten). **Stage**: P5-stratumd-bringup arc unblocked. /joey can now grow into the long-running supervisor (next chunk: P5-joey-orchestrator) without further infrastructure work. 416/416 PASS × default + UBSan (BOOT_TIMEOUT=60 for UBSan). **Next chunks**: P5-joey-orchestrator (loop + fork-children + wait_pid pattern) OR P5-stratumd-stub-bringup (userspace stratumd-stub + joey forks it + kernel-mounts-as-root mechanism — the architectural moment) OR P5-stratumd-multi (Stratum-side, Michal-owned, parallel-trackable). | 416/416 PASS × default + UBSan. |
| `d10d4ee` | **P5-corvus-syscalls: hash fixup** | (doc-only) |
| `0db0dcf` | **P5-corvus-syscalls: 5 v1.0 hardening syscalls (SYS_MLOCKALL = 16, SYS_SET_DUMPABLE = 17, SYS_SET_TRACEABLE = 18, SYS_EXPLICIT_BZERO = 19, SYS_GETRANDOM = 20) + 2 new caps (CAP_LOCK_PAGES, CAP_CSPRNG_READ) + per-Proc flags (PROC_FLAG_NODUMP/NOTRACE/MLOCKED) + public kernel CSPRNG API (`kern_random_bytes`, `kern_random_seeded`); foundational for P5-corvus-bringup per CORVUS-DESIGN.md §4.1.1.** **ABI**: mlockall sets PROC_FLAG_MLOCKED with CAP_LOCK_PAGES check; set_dumpable/set_traceable are one-way to 0 (re-enable refused once flag set); explicit_bzero is per-byte uaccess_store_u8 (kernel/user boundary IS the compiler barrier); getrandom backed by `kern_random_bytes` via ARM RNDR (FEAT_RNG), CAP_CSPRNG_READ-gated, ≤4 KiB per call. **struct Proc grew 128→136 bytes** (added u32 proc_flags + u32 _pad_flags; static_assert updated). **Forward-compat scaffolding**: at v1.0 the flags don't gate anything because swap / core dumps / debug Spoors don't exist yet — corvus calls the syscalls at startup so its startup is forward-compatible; flag enforcement lands when those subsystems do (v1.x). **One-way discipline**: SYS_SET_DUMPABLE(1) on a Proc that already has PROC_FLAG_NODUMP returns -1 (prevents elevated-then-weakened escalation pattern). Same for traceable. **kernel/random.c refactor**: extracted public `kern_random_bytes(buf, len)` + `kern_random_seeded()` from previously-static `devrandom_read`; devrandom_read is now a thin wrapper. New `kernel/include/thylacine/random.h` declares the public API. **caps.h discipline**: CAP_ALL extended; static_assert catches drift on future additions. CAP_HOSTOWNER is reserved but unimplemented (lands at P5-hostowner). **libt stubs**: `t_mlockall` + `t_set_dumpable` + `t_set_traceable` + `t_explicit_bzero` + `t_getrandom` (inline-asm); `T_GRND_NONBLOCK` macro. **6 new kernel-internal tests** in `kernel/test/test_sys_corvus.c`: sys_mlockall.cap_gate (refuse without cap; succeed with; idempotent) + sys_set_dumpable.one_way_to_zero (default dumpable; 0 sets NODUMP; 1 after refused; bad args rejected) + sys_set_traceable.one_way_to_zero (same shape) + sys_corvus_caps.kproc_has_new_caps (kproc has all three caps via CAP_ALL) + kern_random.seeded_returns_true_on_qemu + kern_random.bytes_produces_nonzero (32-byte read has ≥1 non-zero + two reads differ + zero-length no-op + NULL/negative rejected). **No spec extension** — these are forward-compat hardening syscalls; their semantic invariants become spec-pinned when their enforcing subsystems land in v1.x. **Audit posture**: extends kernel/syscall.c (existing trigger surface) + adds kernel/random.c public surface. No new attack surfaces — SYS_GETRANDOM's user-VA bounce uses existing uaccess_store_u8 + UACCESS_USER_VA_TOP bound + fault-fixup; SYS_EXPLICIT_BZERO same. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_MLOCKALL=16 through SYS_GETRANDOM=20 + GRND_NONBLOCK macro; ~80 LOC of doc-comments + 5 enum entries); `kernel/include/thylacine/caps.h` (+CAP_LOCK_PAGES + CAP_CSPRNG_READ + CAP_ALL bump + static_assert update); `kernel/include/thylacine/proc.h` (+proc_flags + pad fields + PROC_FLAG_* macros + size pinning bumped 128→136); `kernel/include/thylacine/random.h` (new; ~30 LOC); `kernel/random.c` (+kern_random_bytes public extraction + kern_random_seeded; devrandom_read wrapper); `kernel/syscall.c` (+random.h include; +~140 LOC: 5 handlers + 3 for_proc inners + 5 dispatcher cases); `kernel/test/test_sys_corvus.c` (new; 6 tests; ~190 LOC); `kernel/test/test.c` (+6 forward decls + 6 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+5 T_SYS_* enum entries + T_GRND_NONBLOCK macro + 5 inline-asm stubs); `docs/reference/58-corvus-syscalls.md` (new; ~280 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + P5-corvus-design section marked LANDED). Test count 410 → 416; **416/416 PASS × default + UBSan** (BOOT_TIMEOUT=60 for UBSan). **Stage**: P5-corvus-bringup unblocked — all syscalls corvus's startup needs are landed. **Next chunks**: P5-stratumd-bringup (joey forks stratumd-system; kernel mounts /sysroot; pivot root — first big architectural integration) OR P5-corvus-bringup (implement /sbin/corvus from scratch using these syscalls — needs P5-stratumd-bringup for state-dir on Stratum-mounted /var/lib/corvus) OR P5-stratumd-multi (Stratum-side, Michal-owned, can parallel-track). | 416/416 PASS × default + UBSan. |
| `cf45eca` | **P5-corvus-design: hash fixup** | (doc-only) |
| `fc9cf47` | **P5-corvus-design: binding scripture for the post-pivot stack — `docs/CORVUS-DESIGN.md` (~930 LOC) + cross-references from ARCH §5 + §11.2b + §14.3a + §15.2/§15.3/§15.4 + §28 + §29 + ROADMAP §7.2a. Captures D1-D6 (six design decisions): classic ESP, integrity-only system pool with boot-key device binding, always-encrypted user datasets, Order-C boot with per-user stratumd, no-UID-0 hostowner + recovery phrase, v1.0 explicit acceptance of no-boot-chain-integrity (Secure Boot deferred to v1.1).** **Architectural reframing**: corvus is a Thylacine-native daemon, NOT a port of Stratum's janus — draws design influence from factotum, janus, FileVault, ZFS native encryption, fscrypt. State file magic `CRVS` (distinct from janus's `JPAS`). Binary frame wire (no parsing ambiguity; passphrases never on text wire). 20 invariants C-1..C-20 published with explicit enforcement mechanisms. **Hardened against an Opus-4.7-max-effort adversarial security audit** before commit (21 findings; all 3 P0s + 7 P1s + 8 P2s + 3 P3s resolved inline or explicitly deferred). Key audit resolutions: F1 (per-user stratumd Option A — multi-stratumd-per-pool requires Stratum-side support; corvus's UNWRAP verb carries session token → owner check enforced); F2 (boot key HKDF-bound to pool serial; stratumd-system refuses on mismatch); F3 (explicit v1.0 acceptance of no-boot-chain-integrity; P5-secure-boot in v1.x); F5 (5 v1.0 hardening syscalls enumerated for ARCH §11); F6 (24-word BIP-39 recovery phrase displayed once at install); F7 (fresh salt + fresh AEAD nonce + re-evaluated Argon2id params on every rotation); F8 (binary frame wire format with mlock'd buffers); F10 (orthogonal session-cap vs Proc-cap rule, explicit); F14 (drop "forward-secret" misnomer; reword to "RAM-wiped-on-logout"). **Implementation arc**: 7 chunks (this design + P5-stratumd-multi [Stratum-side; Michal] + P5-corvus-syscalls + P5-stratumd-bringup + P5-corvus-bringup + P5-login + P5-hostowner + P5-kernel-update). **v1.x deferrals**: Secure Boot + corvid-boot bootloader + encrypted system pool + TPM + FIDO2/smartcard backends + Shamir k-of-n multi-hostowner + per-dataset rekeying (true forward secrecy) + sshd. **Stratum API additions enumerated in CORVUS-DESIGN §13** (Michal-owned): multi-stratumd-per-pool support; corvus-native UNWRAP wire; session-close notify+evict; snapshot rollback-compromise marking; boot-key device binding via `--bind-pool-serial`; per-dataset key rotation hooks. **No code; scripture only**; tests unchanged at 410/410 PASS × default + UBSan. **Audit posture**: this chunk modifies binding scripture only (CORVUS-DESIGN.md is new; ARCH cross-refs are pointers, no semantic change). No new attack surface introduced; future implementation chunks land the audit-bearing code with their own audits. **Files**: `docs/CORVUS-DESIGN.md` (new); `docs/ARCHITECTURE.md` (§5.1 boot chain + §11.2b corvus syscalls + §14.3a system-pool integrity-only note + §15.2 no-UID-0 + §15.3 caps + §15.4 corvus-vs-factotum-v2.0 note + §28 C-invariants cross-ref + §29 glossary); `docs/ROADMAP.md` (§7.2a corvus arc); `docs/phase5-status.md` (this row + section + P5-attach-probe section marked LANDED); `memory/MEMORY.md` (entry); `memory/project_active.md` (description); `memory/project_mycelium.md` (separate; queued design topic). **Stage**: Phase 5's scripture commitment is complete; implementation arc unblocked. **Next chunks**: P5-corvus-syscalls (smallest, foundational; 5 kernel syscalls + libt stubs + tests; critical-path for P5-corvus-bringup) OR P5-stratumd-bringup (joey forks stratumd-system; pivot root into Stratum-mounted /; first big architectural integration) OR P5-stratumd-multi (Stratum-side; Michal-owned). | 410/410 PASS unchanged (scripture-only chunk). |
| `106edeb` | **P5-attach-probe: hash fixup** | (doc-only) |
| `029016b` | **P5-attach-probe: userspace integration test of SYS_ATTACH_9P + SYS_MOUNT + SYS_UNMOUNT; first Phase 5 test driving the full SVC dispatch + wire codec + dev9p + spoor-transport + pipe stack through a real EL0 Proc (no loopback shortcuts).** New `/attach-probe` ELF binary (`usr/attach-probe/attach-probe.c`, ~14.9 KiB) + kernel-thread 9P responder + test framework (`kernel/test/test_attach_probe.c`, ~280 LOC). The probe expects fd 0 (tx) + fd 1 (rx) pre-installed in its handle table by the kernel test framework; calls t_attach_9p("/", 1, 0) → t_mount(99, 0) → t_unmount(99) → t_unmount(99 — expect fail) → t_close(attach_fd) → t_close(0) → t_close(1) → t_putstr("attach-probe: PASS\n") → t_exits(0). **Kernel responder** is a kproc kthread that owns the OTHER ends of the two pipe pairs (c2s_rd + s2c_wr); reads framed Tmsgs from rx → builds responses via inline switch (Tversion → Rversion msize=4096+"9P2000.L"; Tattach → Rattach qid; Tclunk → Rclunk; * → Rlerror EIO) → writes to tx. Exits cleanly on rx EOF (when probe closes tx_fd → write_eof on c2s ring → wakeup → next read returns 0). **Why kernel-thread, not two userspace Procs**: v1.0 rfork supports RFPROC only; RFFDG (shared fd table) is deferred. Cross-Proc Spoor transfer is gated on RIGHT_TRANSFER through a 9P session (ARCH I-4) — circular dependency. Kernel-thread responder sidesteps both. When RFFDG lands later, the test can graduate to a two-proc design. **Refcount discipline (LOAD-BEARING)**: each pipe_create Spoor's ref=1 is TRANSFERRED (not bumped) to its owner — responder gets c2s_rd + s2c_wr; probe's handle table gets c2s_wr (fd 0) + s2c_rd (fd 1) via handle_alloc's ref-consumption convention. Boot retains ZERO refs after setup. If boot held a residual ref on c2s_wr, the probe's t_close(tx_fd) would drop to 1 (not 0), devpipe_close wouldn't fire, write_eof wouldn't be set, responder would block forever in dev_read — surfaced by an earlier draft of the test; documented as caveat #1 in `57-attach-probe.md`. **Mid-chunk discovery**: Tclunk on root_fid is rejected at the session layer (`kernel/9p_session.c::p9_session_send_clunk` line 303: `if (fid == s->root_fid) return -1;` — root fids are session-scoped, cleaned up implicitly at session close, not via wire Tclunk). p9_attached_destroy already tolerates this via `(void)p9_client_clunk(...)`. The probe's t_close(attach_fd) → dev9p_close → p9_attached_destroy → attempted Tclunk silently rejected; the responder sees exactly 2 messages (Tversion + Tattach), not 3. Test asserts `>= 2` and documents the design intent at the assertion site. **Build wiring**: `usr/attach-probe/` (new module: 1 C source + 1 CMakeLists) + `usr/CMakeLists.txt` (add_subdirectory) + `tools/build.sh` (usr_bins array += "attach-probe") + `kernel/CMakeLists.txt` (test_attach_probe.c in KERNEL_SRCS) + `kernel/test/test.c` (extern + registry entry `userspace.attach_probe_round_trip`). **No spec extension** — pure composition over already-spec'd 9P client + pipe + handle layers; Tclunk-on-root rejection is a documented session-layer policy not a new invariant. **Audit posture**: kernel-source change is `kernel/test/test_attach_probe.c` (test-only, not on the trigger list). Probe binary at EL0 has no new attack surface — uses existing syscalls with valid arg shapes. **Test count** 409 → 410; **410/410 PASS × default + UBSan** (with `BOOT_TIMEOUT=60` for UBSan due to ~3× per-test overhead). **Stage**: Phase 5's userspace-visible mount/attach surface is now empirically validated end-to-end at EL0. **Files**: `usr/attach-probe/attach-probe.c` (new; ~100 LOC); `usr/attach-probe/CMakeLists.txt` (new); `usr/CMakeLists.txt` (+add_subdirectory); `tools/build.sh` (+"attach-probe" in usr_bins); `kernel/test/test_attach_probe.c` (new; ~280 LOC); `kernel/CMakeLists.txt` (+1 source); `kernel/test/test.c` (+1 extern + 1 registry entry); `docs/reference/57-attach-probe.md` (new; ~270 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-mount-syscall section marked LANDED). **Next chunks**: P5-stratumd (boot ordering + .key sidecar design pass — now-feasible end-to-end; needs architectural design discussion) OR P5-fd-syscalls-RFFDG (graduate to a two-userspace-Proc design when rfork gains RFFDG) OR codec extensions P5-wire-lock/-xattr/-stratum-ext (mechanical; low-priority). | 410/410 PASS × default + UBSan. |
| `fb4c2cd` | **P5-mount-syscall: hash fixup** | (doc-only) |
| `bb778e6` | **P5-mount-syscall: `SYS_MOUNT = 14` + `SYS_UNMOUNT = 15` SVC handlers + companion `spoor_clunk` Plan-9 cclose fix; the last deferred SVC handler from Phase 5's plan. With this chunk every user-visible mount/attach syscall from ARCH §11.2 is live.** Thin wrappers over `kernel/territory.c::mount` / `::unmount`. **ABI**: `SYS_MOUNT(source_spoor_fd, target_path_id, flags) → 0/-1`; `SYS_UNMOUNT(target_path_id) → 0/-1`. **target_path_id** is a u32 abstract path token at v1.0 (matches the existing `path_id_t` used by bind/unbind/mount/unmount C-API + `struct PgrpMount`); string-path resolution waits on the future walk subsystem. **flags** mirror Plan 9 (`MREPL` / `MBEFORE` / `MAFTER` / `MCREATE`); only `MREPL` has distinguished semantics at v1.0 (displaces an existing entry at the same path, `spoor_clunk`s the prior source). **Rights gate**: source handle requires `RIGHT_READ` (mount holders consume the source's tree; WRITE-only is structurally useless). **Handler factoring**: `sys_mount_for_proc(p, source_fd, target, flags)` + `sys_unmount_for_proc(p, target)` non-static inners for kernel-internal testing; static SVC wrappers (`sys_mount_handler` / `sys_unmount_handler`) validate u32 bounds on the raw u64 args + pull current Proc + call the inners. **Companion fix — `kernel/spoor.c::spoor_clunk` Plan-9 cclose semantics**: switched from "run dev->close on every clunk" to "run dev->close ONLY when ref hits 0." Prior behavior tore down per-Spoor Dev state (pipe endpoints, 9P sessions) on the user's first close even while a mount-table entry still held a reference — ARCH §9.6.6's mount-after-close lifecycle ("Caller can close the attach_9p fd after `mount` — the mount table holds the ref") required the fix. P5-mount-syscall is the first feature where the asymmetry becomes wrong; prior chunks all had ref=1 holders so the eager-close behavior was indistinguishable. **Site updates**: `kernel/territory.c` switched 3 `spoor_unref` → `spoor_clunk` (in `mount()` MREPL displacement, `unmount()`, and `territory_unref()`'s final-release loop). `kernel/dev9p.c::dev9p_close` switched 2 `spoor_unref` → `spoor_clunk` (the transport-Spoor cleanup paths in the attach-teardown branch). Sites kept on `spoor_unref`: `dev9p_attach_client` constructor failure (no aux installed; close would no-op anyway) and `sys_attach_9p_handler` rollback paths (each `spoor_unref` balances a paired earlier `spoor_ref`; not a last-drop). **Userspace libt stubs**: `t_mount` + `t_unmount` in `<thyla/syscall.h>`, matching the kernel ABI. Flag macros `T_MREPL` / `T_MBEFORE` / `T_MAFTER` / `T_MCREATE` mirror the kernel side. **No spec extension** — `specs/territory.tla::MountRefcountConsistency` is preserved structurally by territory.c primitives unchanged; the spoor_clunk semantics fix is an implementation refinement orthogonal to the spec's abstract refcount drop. **9 kernel-internal tests** in `kernel/test/test_sys_mount.c`: happy_path_grafts_pipe_spoor (mount a pipe-Spoor at path_id=42; nmounts 0→1) + idempotent_on_duplicate (same source+target twice; no-op success) + rejects_bad_fd (out-of-range / negative / closed → -1) + rejects_missing_right_read (dup to WRITE-only → mount returns -1) + rejects_invalid_flags (bits outside MREPL\|MBEFORE\|MAFTER\|MCREATE → -1; MREPL accepted) + rejects_null_territory (Proc without Territory → -1) + unmount.removes_entry_and_drops_ref (mount → close both pipe ends → unmount → ring freed at unmount: proves mount-table held the LAST live ref) + unmount.rejects_nonexistent_target (-1 on missing) + **caller_close_keeps_mount_alive** (the end-to-end lifecycle test: mount → close source fd → close other pipe end → ring STILL alive while mount-table holds ref → Territory drop frees the ring → validates the Plan 9 cclose semantics fix). **Audit posture**: extends kernel/syscall.c (existing; on trigger list since P4-Ib) + kernel/territory.c (existing; joined trigger list at P5-attach-mount) + kernel/dev9p.c (existing; joined at P5-attach-dev) + kernel/spoor.c (existing; Spoor lifecycle). No new attack surfaces — handlers thin-wrap already-audited primitives. **Mid-chunk discovery**: the initial sys_mount tests revealed that closing a user fd on a mounted Spoor tore down the pipe ring even with the mount-table holding a ref. Root cause: spoor_clunk running dev->close on every drop, not just the last. Fix scope grew to include the Plan 9 cclose refactor + the 5 site-changes in territory.c and dev9p.c — bundled here because the same chunk that introduces the multi-holder pattern (mount + handle simultaneously holding) must also fix the eager-close behavior or land broken. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_MOUNT=14 + SYS_UNMOUNT=15 + SYS_MOUNT_VALID_FLAGS comment block); `kernel/syscall.c` (+~95 LOC: territory.h include + sys_mount_for_proc + sys_unmount_for_proc + 2 SVC wrappers + 2 dispatcher cases); `kernel/spoor.c` (spoor_clunk + spoor_unref doc-comments + Plan-9 cclose refactor in spoor_clunk); `kernel/territory.c` (3 spoor_unref → spoor_clunk site-changes + updated rationale comments); `kernel/dev9p.c` (2 spoor_unref → spoor_clunk site-changes); `kernel/test/test_sys_mount.c` (new; 9 tests; ~240 LOC); `kernel/test/test.c` (+9 forward decls + 9 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+T_SYS_MOUNT=14 + T_SYS_UNMOUNT=15 + T_MREPL/T_MBEFORE/T_MAFTER/T_MCREATE macros + t_mount + t_unmount inline-asm stubs); `docs/reference/56-sys-mount.md` (new; ~280 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-attach-syscall section marked LANDED). Test count 400 → 409; **409/409 PASS × default + UBSan**. **Stage**: Phase 5's user-visible mount/attach surface is COMPLETE. **Next chunks**: P5-attach-probe (userspace integration test with 9P-responder companion) OR P5-stratumd (now-feasible end-to-end — needs design pass on boot ordering + .key sidecar) OR codec extensions P5-wire-lock/-xattr/-stratum-ext (mechanical; low-priority). | 409/409 PASS × default + UBSan. |
| `88cf9c3` | **P5-attach-syscall: hash fixup** | (doc-only) |
| `ae0746e` | **P5-attach-syscall: `SYS_ATTACH_9P = 13` SVC handler — user-visible body of ARCH §9.6.1's `attach_9p`. The last deferred SVC handler from the P5-attach plan.** ABI: tx_fd (x0), rx_fd (x1), aname_va (x2), aname_len (x3 ≤ 256), n_uname (x4); returns KOBJ_SPOOR fd for the 9P tree's root in x0, -1 on failure with full rollback. Half-duplex pipes (from SYS_PIPE) pass two distinct fds; duplex transports (future Unix socket / vsock) pass the same fd for both. **dev9p_priv extension**: new `attached_owner` + `adapter_to_free` fields on root Spoors created by SYS_ATTACH_9P. dev9p_close on such a Spoor tears down the entire attach session — `p9_attached_destroy` (clunks root_fid + p9_client_destroy + frees recv_buf) + spoor_unref the transport Spoors + kfree the adapter. Walk-derived Spoors AND kernel-internal callers (tests + future stratumd boot path) leave both fields NULL — they use the existing `fid_owned` path. **Handler discipline**: validates aname length + user-VA bound; takes independent `spoor_ref`s on tx/rx (so userspace closing the original transport_fd doesn't free Spoors out from under the attach — original fd keeps its own handle-table ref; the attach holds ours); copies aname via per-byte uaccess_load_u8 into a 256-byte stack scratch; kmallocs adapter with `owns_spoors=false`; `p9_spoor_transport_init` then `p9_attached_create` drives Tversion+Tattach over the byte pipe; patches root Spoor's `dev9p_priv.attached_owner` + `adapter_to_free`; `handle_alloc` as KOBJ_SPOOR with RIGHT_READ|WRITE|TRANSFER. **Full rollback on any failure** (5 distinct unwind points: aname uaccess fault → unref spoors; adapter kmalloc OOM → unref spoors; spoor_transport_init failure → unref spoors + kfree adapter; p9_attached_create handshake failure → unref spoors + kfree adapter; handle_alloc table-full → spoor_clunk(root) which fires dev9p_close's attach-teardown branch). **Userspace libt stub** `t_attach_9p(tx_fd, rx_fd, aname, aname_len, n_uname) → fd / -1` added to `<thyla/syscall.h>` matching the kernel ABI shape. **No spec extension** — pure composition over `specs/9p_client.tla` (session correctness) + `specs/handles.tla` (rights gates) + `specs/pipe.tla` (transport wait/wake invariants). No new invariants at this layer; the lifecycle discipline is structurally enforced (only the SVC handler sets attached_owner; only dev9p_close reads it). **Audit posture**: extends kernel/syscall.c + kernel/dev9p.c (both already on the trigger list — syscall.c since P4-Ib; dev9p since the kernel/9p_* group joined at P5-session). No new attack surfaces — user-VA aname copy uses the existing uaccess_load_u8 primitive with the existing UACCESS_USER_VA_TOP bound and fault-fixup discipline (matches SYS_PUTS / SYS_WRITE shape). **Kernel-internal happy-path test deferred**: requires a 9P-responder companion to drive Tversion+Tattach responses. The existing `test_9p_attach.create_destroy` + `test_9p_spoor_transport.end_to_end_handshake` cover p9_attached_create + the spoor-transport adapter against synthetic responders; what's new at SYS_ATTACH_9P is the SVC dispatch shape + dev9p_priv attached_owner wiring + rollback discipline, both testable structurally without a server. The integration test is P5-attach-probe (next). **Kernel-internal rejection-paths test** (`sys_attach_9p.rejection_paths`) exercises the structural pre-checks: out-of-range fd / closed fd → handle_get returns NULL → handler returns -1 before any allocation. **Hardcoded v1.0 defaults**: `msize = 4096` (matches PIPE_BUF_SIZE + SYS_RW_MAX); `root_fid = 1`. Future iterations may expose them as additional SVC args. **Files**: `kernel/include/thylacine/syscall.h` (+ SYS_ATTACH_9P = 13 enum entry + SYS_ATTACH_ANAME_MAX = 256 macro); `kernel/syscall.c` (+ ~115 LOC: sys_attach_9p_handler with full rollback + 9p_attach.h + 9p_spoor_transport.h + dev9p.h includes; +1 dispatcher case; slub.h include for kmalloc/kfree); `kernel/include/thylacine/dev9p.h` (+ 2 forward decls for p9_attached + p9_spoor_transport; + 2 fields in dev9p_priv); `kernel/dev9p.c` (+ 9p_attach.h + 9p_spoor_transport.h includes; + attach-teardown branch in dev9p_close); `kernel/test/test_sys_pipe.c` (+ 1 rejection-paths test); `kernel/test/test.c` (+ 1 forward decl + 1 registry entry); `usr/lib/libt/include/thyla/syscall.h` (+ T_SYS_ATTACH_9P = 13 enum entry; + t_attach_9p inline-asm stub); `docs/reference/55-sys-attach-9p.md` (new; ~200 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-fd-syscalls section marked LANDED). Test count 399 → 400; **400/400 PASS × default + UBSan**. **Note**: test.sh's "no boot marker" detection is occasionally flaky due to the pre-existing joey-side extinction interleaving with the boot-OK marker line — when fmt collides the regex misses. Tests themselves are deterministic and pass. **Stage gate**: SYS_MOUNT + SYS_UNMOUNT now have a clean path — consume KOBJ_SPOOR source_fd (from SYS_ATTACH_9P at this chunk OR SYS_PIPE) + graft at target_path. **Next chunks**: P5-mount-syscall (final deferred SVC handler — closes the user-visible mount surface ARCH §9.6 mapped to) + P5-attach-probe (userspace test binary with 9P-responder companion exercising the full attach + mount round trip) OR P5-stratumd (now-feasible end-to-end — needs design pass on boot ordering + .key sidecar). | 400/400 PASS × default + UBSan. |
| `3948bd4` | **P5-fd-syscalls: hash fixup** | (doc-only) |
| `5fd72f6` | **P5-fd-syscalls: `SYS_CLOSE` + `SYS_DUP` SVC handlers + `/pipe-probe` userspace binary; the byte I/O composition is now empirically live in the boot log.** Kernel: `SYS_CLOSE` = 11 thin wrapper over `handle_close` (KOBJ_SPOOR release path from P5-fd-pipe runs spoor_clunk); `SYS_DUP` = 12 wraps `handle_dup` with rights-bit validation (RightsCeiling rejects elevation; KOBJ_SPOOR acquire path bumps spoor_ref). **Userspace**: libt's `<thyla/syscall.h>` extended with `t_pipe` / `t_read` / `t_write` / `t_close` / `t_dup` inline-asm stubs (matches the existing always_inline pattern). **First userspace test binary** `/pipe-probe` (16 KiB ELF in `usr/pipe-probe/`): exercises pipe→write 22 bytes→read 22 bytes→verify FIFO order→dup with reduced rights→reject rights-elevation→close dup→close rd→close wr→read on closed fd→double-close, then `t_putstr("pipe-probe: PASS\n")` + `t_exits(0)`. Spawned by `kernel/test/test_pipe_probe.c` via `devramfs_lookup` + `rfork` + `exec_setup` + `userland_enter`; `wait_pid` checks status=0. **Boot log marker**: `pipe-probe: PASS` + `/pipe-probe reaped pid=N status=0 — full byte-I/O syscall surface verified end-to-end`. **8 distinct syscall surfaces** exercised in one invocation: SYS_PIPE (1×), SYS_WRITE (1×), SYS_READ (1×), SYS_DUP (1× success + 1× rejection), SYS_CLOSE (3×), SYS_READ on closed fd (1× rejection), SYS_CLOSE on closed fd (1× rejection), SYS_PUTS (1×), SYS_EXITS (1×). **Kernel-internal test** `sys_pipe.dup_spoor_handle_acquires_ref` verifies the KOBJ_SPOOR acquire path end-to-end: handle_dup of a SPOOR bumps the underlying Spoor's refcount; rights-elevation rejected (RightsCeiling); close of dup drops refcount back. **No spec extension** — pure composition over specs/handles.tla (HandleClose + HandleDup actions; RightsCeiling enforcement) + specs/pipe.tla (the underlying primitive's invariants). **Audit posture**: extends kernel/syscall.c surface (already on trigger list); no new attack surfaces — both handlers are thin wrappers; rights enforcement delegated to handle_dup's existing RightsCeiling check. **Stage gate**: SYS_ATTACH_9P + SYS_MOUNT SVC handlers are now **unblocked** — the fd surface is sufficient for userspace KOBJ_SPOOR usage. **Mid-chunk fix**: `/pipe-probe` is 16552 bytes (slightly over 16 KiB; inline syscall stubs inflate). Bumped `PIPE_PROBE_BLOB_MAX` to 32 KiB. **Files**: `kernel/include/thylacine/syscall.h` (+SYS_CLOSE = 11 + SYS_DUP = 12 enum entries); `kernel/syscall.c` (+~35 LOC: sys_close_handler + sys_dup_handler + 2 dispatcher cases); `kernel/test/test_sys_pipe.c` (+1 test: dup_spoor_handle_acquires_ref); `kernel/test/test_pipe_probe.c` (new; ~95 LOC); `kernel/test/test.c` (+2 forward decls + 2 registry entries); `kernel/CMakeLists.txt` (+1 test source); `usr/lib/libt/include/thyla/syscall.h` (+5 inline-asm stubs); `usr/pipe-probe/pipe-probe.c` (new; ~110 LOC); `usr/pipe-probe/CMakeLists.txt` (new); `usr/CMakeLists.txt` (+1 add_subdirectory); `tools/build.sh` (+"pipe-probe" to usr_bins); `docs/reference/54-sys-fd-syscalls.md` (new; ~180 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-fd-rw section marked LANDED). Test count 397 → 399; **399/399 PASS × default + UBSan**. **Next chunks**: **P5-attach-syscall + P5-mount-syscall** (user-visible mount surface; both deferred since P5-attach-create + P5-attach-mount waiting for fd surface — now unblocked) OR P5-stratumd (boot ordering + .key sidecar design pass; the kernel-side primitives are all in place) OR P5-wire-lock/-xattr/-stratum-ext (deferred codec extensions). | 399/399 PASS × default + UBSan. |
| `a134782` | **P5-fd-rw: hash fixup** | (doc-only) |
| `54d900b` | **P5-fd-rw: `SYS_READ` + `SYS_WRITE` SVC handlers + `uaccess_store_u8` primitive; userspace can finally exchange bytes through fds.** ABI: `SYS_READ`(=9) / `SYS_WRITE`(=10) take fd in x0, buf_va in x1, len in x2; return bytes transferred (>=0; 0 EOF for read) in x0, -1 on error. Per-call cap = `SYS_RW_MAX = 4096` bytes (matches PIPE_BUF_SIZE; userspace loops for larger). **Rights gates**: SYS_READ requires RIGHT_READ; SYS_WRITE requires RIGHT_WRITE; SYS_PIPE's READ\|WRITE\|TRANSFER grant passes both gates. **User-VA validation**: same `UACCESS_USER_VA_TOP = 1<<47` bound as SYS_PUTS; NULL / overflow rejected; zero-length still validates the fd (POSIX `EBADF`-shape discipline). Handlers factored into non-static `sys_{read,write}_for_proc(p, h, kbuf, len)` inners + static SVC wrappers. Inners call `handle_get` → KOBJ_SPOOR check → rights check → `dev->{read,write}(c, kbuf, len, c->offset)` → advance `c->offset` → return byte count. SVC wrapper does user-VA range check + 4 KiB stack scratch + per-byte uaccess copy in/out. **`uaccess_store_u8` primitive** (new at this chunk; symmetric to existing `uaccess_load_u8`): single-byte STRB at the fault point + fixup-table entry mapping to a fault-handler label that returns -1. C decl in `arch/arm64/uaccess.h`. The existing fault dispatcher (arch/arm64/exception.c) already handles both read AND write user-VA faults from kernel mode via `userland_demand_page` (which itself checks VMA prot vs `is_write`); no dispatcher change needed. **Audit-trigger surfaces extended**: kernel/syscall.c (existing) gains read/write handlers; arch/arm64/uaccess.S (existing) gains store_u8 + fixup entry — same shape + invariants as load_u8. **4 new kernel-internal tests** in `kernel/test/test_sys_pipe.c` (appended to the existing test_sys_pipe scaffold since they share the test_proc + sys_pipe_for_proc fixture): write_then_read_round_trip (7-byte payload writes via sys_write_for_proc; reads back via sys_read_for_proc; FIFO order preserved — **canonical end-to-end test of the byte I/O path through pipe**) + rights_check (out-of-range fd → -1 from both handlers; rights-reduction can't be tested without sys_handle_reduce — deferred) + zero_length_validates_fd (0-len read/write on valid fd → 0; on bad fd → -1) + read_after_close_returns_eof (close wr; read empty pipe → 0 EOF — **composes SYS_READ with the blocking-pipe ReadEof spec action from specs/pipe.tla**). **Partial-fault data-loss caveat** (documented in 53-sys-rw.md): SYS_READ on uaccess_store_u8 mid-copy fault returns -1, but bytes drained from dev->read beyond the fault are LOST. Phase 5+ can pre-touch user pages to mitigate. **Stack scratch size**: 4 KiB on the kernel test thread's 16 KiB stack; 12 KiB headroom suffices for v1.0 syscall depths. **Performance**: per-byte uaccess loops — not the fastest possible; matches SYS_PUTS pattern. A future P5-fd-rw-uaccess-fast chunk can add u64-stride copy_from_user/copy_to_user once a benchmark justifies the additional fixup-table entries. **No spec extension** — pure composition over `specs/handles.tla` (HandleAlloc + RIGHT_READ/RIGHT_WRITE gates) + `specs/pipe.tla` (ReadEof / WriteEpipe actions); the byte I/O path itself is a thin shim from syscall ABI to Dev vtable. Files: `arch/arm64/uaccess.S` (+~25 LOC: uaccess_store_u8 + fixup table entry); `arch/arm64/uaccess.h` (+1 extern decl); `kernel/include/thylacine/syscall.h` (+SYS_READ = 9 + SYS_WRITE = 10 enum entries + SYS_RW_MAX = 4096 macro); `kernel/syscall.c` (+~140 LOC: sys_lookup_spoor + sys_validate_user_buf helpers; sys_{read,write}_for_proc inners; sys_{read,write}_handler wrappers; 2 dispatcher cases; dev.h include added); `kernel/test/test_sys_pipe.c` (+4 tests + 3 extern decls); `kernel/test/test.c` (+4 forward decls + 4 registry entries); `docs/reference/53-sys-rw.md` (new; ~210 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-fd-pipe section marked LANDED). Test count 393 → 397; **397/397 PASS × default + UBSan**. **Next chunks**: P5-fd-close (SYS_CLOSE wrapper over handle_close — thin) + P5-fd-dup (SYS_DUP wraps handle_dup; uses the KOBJ_SPOOR acquire path wired at P5-fd-pipe) + **P5-fd-pipe-probe** (userspace C probe binary exercising SYS_PIPE + SYS_READ + SYS_WRITE + SYS_CLOSE end-to-end via libt; first userspace test of the byte I/O path). Once these land, the deferred SYS_ATTACH_9P + SYS_MOUNT SVC handlers can be wired since KOBJ_SPOOR is now usable from userspace. | 397/397 PASS × default + UBSan. |
| `65293f5` | **P5-fd-pipe: hash fixup** | (doc-only) |
| `0f66f5c` | **P5-fd-pipe: `SYS_PIPE` SVC handler + KOBJ_SPOOR release/acquire wiring; first userspace consumer of the kernel pipe primitive.** SYS_PIPE = 8: no args; calls `pipe_create()`; installs both Spoors as KOBJ_SPOOR handles in the caller's HandleTable with RIGHT_READ\|WRITE\|TRANSFER. Returns rd fd in x0, wr fd in x1; -1 in x0 on failure with full rollback of partial state. **Wires KOBJ_SPOOR into handle.c** for the first time: `handle_release_obj` calls `spoor_clunk` so handle_close (and proc_free's handle_table_free) tear down the underlying Spoor end-to-end. `handle_acquire_obj` calls `spoor_ref` so handle_dup gives the second handle its own reference. KOBJ_SPOOR is now the third kobj kind (after BURROW + hw kinds: MMIO/IRQ/DMA) to be reference-counted on the handle table. **Handler structure**: extracted `sys_pipe_for_proc(p, *rd, *wr)` as a non-static inner used by tests; the static SVC handler looks up `current_thread()->proc` + writes ctx->regs[0/1]. **No user-VA fd[2] pointer** — uses x0 + x1 return registers per ARCH §11.6 ABI (which reserves x1 for the second return value, e.g., `pipe`). Sidesteps adding `uaccess_store_u32` (only `uaccess_load_u8` exists today). Userspace libc shim (Phase 6+) translates to POSIX pipe(2)'s buffer-style API. **Rights granted**: RIGHT_READ \| RIGHT_WRITE \| RIGHT_TRANSFER on both handles. Actual read/write gating happens inside `devpipe_read`/`devpipe_write` via the `is_read_end` flag (wrong-end → -1). Future revisions may pre-narrow handle rights per end. **3 kernel-internal integration tests** in `kernel/test/test_sys_pipe.c`: allocates_two_distinct_spoor_handles (sys_pipe_for_proc returns 0; two distinct fds; both KOBJ_SPOOR; rights match; spoor_total_allocated += 2); proc_free_releases_handles (drop_test_proc → handle_table_free → KOBJ_SPOOR release → spoor_clunk → ring freed; pipe_total_freed += 1, spoor_total_freed += 2 — **end-to-end test of the new KOBJ_SPOOR release path**); handle_close_releases_one_end (close one fd drops ring's per-end ref but ring stays alive; second close frees ring). **No spec extension** — pure composition over `specs/handles.tla` (HandleAlloc + HandleClose actions; KOBJ_SPOOR fits the existing transferable-kind mask) + `specs/pipe.tla` (the underlying primitive's invariants propagate). **Pre-existing extinction** at boot (joey-side trap; observed identical on baseline pre-commit via git stash) is NOT a regression. **Audit posture**: extends kernel/syscall.c surface + kernel/handle.c surface (both already on the trigger list — handle.c since P2-Fc; syscall.c since P4-Ib). No new attack surfaces — KOBJ_SPOOR release wiring is mechanical; the SVC handler's only data-dependent path is the partial-failure rollback (covered by test cases). Files: `kernel/include/thylacine/syscall.h` (+SYS_PIPE = 8 enum); `kernel/syscall.c` (+sys_pipe_for_proc + sys_pipe_handler + dispatcher case; ~60 LOC); `kernel/handle.c` (+spoor.h include; KOBJ_SPOOR cases in handle_release_obj + handle_acquire_obj); `kernel/test/test_sys_pipe.c` (new; ~135 LOC; 3 tests); `kernel/test/test.c` (+3 forward decls + 3 registry entries); `kernel/CMakeLists.txt` (+1 test source); `docs/reference/52-sys-pipe.md` (new; ~160 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-pipe-blocking section marked LANDED). Test count 390 → 393; **393/393 PASS × default + UBSan**. **Next chunks**: P5-fd-rw (SYS_READ + SYS_WRITE; user-VA buffer copy; routes through dev->read/write — when this lands, userspace can actually exchange bytes through pipes and through 9P-mounted filesystems) + P5-fd-close + P5-fd-dup + P5-fd-pipe-probe (userspace test binary) + eventual SYS_MOUNT / SYS_ATTACH_9P (gated on the fd handle being usable from userspace). | 393/393 PASS × default + UBSan. |
| `92be8b6` | **P5-pipe-blocking: hash fixup** | (doc-only) |
| `431db3a` | **P5-pipe-blocking: `specs/pipe.tla` + extended `kernel/pipe.c` — blocking read/write with rendez integration; ARCH §28 I-9 (missed-wakeup-freedom) is now spec-pinned for the pipe.** Per CLAUDE.md spec-first policy: this is the chunk that opens the wait/wake invariant scope, so the spec lands BEFORE the impl. **Spec** (`specs/pipe.tla`, ~225 LOC): 8 clean actions + 4 buggy variants over state vars `ringCount` / `readEof` / `writeEof` / `threadState[Threads]`. Clean actions atomically mutate state + perform the wake-if-applicable (matching the impl's pattern: mutate under r->lock; drop r->lock; call wakeup which takes rendez->lock); buggy variants elide the wake. 5 invariants — TypeOk + SingleWaiter + EofMonotonic + NoStuckReader + NoStuckWriter — the last two specialize I-9 to the pipe's two-direction state machine. **TLC verdicts**: pipe.cfg clean (model checking completed; no error); 4 buggy cfgs (write_no_wake_reader / read_no_wake_writer / close_write_no_wake_reader / close_read_no_wake_writer) each produce an Invariant-violated counterexample. **Composition**: `specs/scheduler.tla::NoMissedWakeup` proves the atomic cond-check + sleep transition at the rendez API surface; `specs/pipe.tla::NoStuckReader/Writer` proves the pipe-side discipline of "every mutation that could enable a waiter MUST issue a wakeup." Together they close the missed-wakeup hazard end-to-end. **C-side**: `struct pipe_ring` gains `spin_lock_t lock` + `bool read_eof` + `bool write_eof` + 2 `struct Rendez` wait queues. Header 32→72 B; total 4128→4168 B. devpipe_read becomes a `for(;;) { take r->lock; if count>0 → drain + drop + wakeup(write_rendez) + return; if write_eof + empty → drop + return 0 (EOF); else → drop + sleep(read_rendez, cond_can_read, r); }` loop. devpipe_write symmetric (returns -1 EPIPE when read_eof; sleeps on write_rendez when full + !read_eof). devpipe_close sets the appropriate EOF flag under r->lock, drops the lock, **calls wakeup on the OTHER side's rendez** (so any sleeper exits with EOF / EPIPE), then handles refcount + free. **Sleep cond functions**: `cond_can_read = (count > 0 \|\| write_eof)`; `cond_can_write = (count < PIPE_BUF_SIZE \|\| read_eof)`. These mirror the spec's `CanRead` / `CanWrite` predicates. Sleep's atomic cond-check + sleep protocol (via rendez API) handles the race between caller-side check + producer-side mutation; pipe's discipline guarantees the wake actually fires. **4 new multi-thread unit tests** in `kernel/test/test_pipe_blocking.c` mirroring the `test_rendez_basic_handoff` pattern: spawn consumer thread → ready + sched (yield to consumer; consumer reaches blocking op + sleeps); boot does the wake-triggering action; sched (consumer resumes + completes op); boot verifies result. Tests cover: write_wakes_sleeping_reader, read_wakes_sleeping_writer, close_write_end_wakes_reader_with_eof, close_read_end_wakes_writer_with_epipe. Each test parks the consumer thread at sched() at the end (leak-on-purpose; per the rendez-test idiom). **Repurposed 2 existing tests**: read_on_empty_returns_zero (now closes write end FIRST → tests EOF path) + write_to_full_returns_zero (closes read end FIRST → tests EPIPE path). Both still test the non-sleeping paths but adapted for blocking semantics. **Mid-chunk bug**: _Static_assert miscounted ring header (80 B vs actual 72 B; walked alignment manually: 4+4+8+8+8+1+1+pad2+4+pad4+16+16=72). Fixed. **kernel/pipe.c joins the audit-trigger surface** (alongside the wait/wake hazard category in CLAUDE.md/ARCH §25.4). The bug class "forgot to wake on a state-enabling mutation" is exactly what `specs/pipe.tla` catches. Files: `specs/pipe.tla` (new; ~225 LOC); `specs/pipe.cfg` (new) + 4 buggy cfgs (new); `kernel/pipe.c` (extended; +~100 LOC: 2 cond fns + sleep/wakeup loops + close-side EOF + wake); `kernel/test/test_pipe.c` (2 tests repurposed); `kernel/test/test_pipe_blocking.c` (new; ~200 LOC; 4 multi-thread tests); `kernel/test/test.c` (+4 forward decls + 4 registry entries); `kernel/CMakeLists.txt` (+1 test source); `docs/reference/51-pipe.md` (semantic section rewritten for blocking; Tests + Status + Spec posture sections refreshed); `docs/REFERENCE.md` (Tip + doc-table entry + spec snapshot for pipe.tla); `docs/phase5-status.md` (this row + section + P5-pipe section marked LANDED). Test count 386 → 390; **390/390 PASS × default + UBSan** (~1.7 s boot). **Next chunks**: P5-fd-syscalls (open/close/read/write/dup → KOBJ_SPOOR population → unblocks deferred SVC handlers + general userspace 9P + userspace pipe(2)) OR P5-wire-lock/-xattr/-stratum-ext (deferred codec extensions). | 390/390 PASS × default + UBSan. |
| `7f95cd0` | **P5-pipe: hash fixup** | (doc-only) |
| `23acfbf` | **P5-pipe: `kernel/pipe.{c,h}` — Plan 9 pipe(fd[2]) primitive; connected Spoor pair over a shared 4 KiB ring buffer.** Production byte-pipe Spoor backend; replaces the test-only `test_pipe_dev` scaffold from P5-spoor-transport. **v1.0 semantics (non-blocking)**: read returns 0 on empty, write returns 0 on full; no rendez wait. Wrong-end calls (read on write-end / write on read-end) return -1. **Lifecycle**: `struct pipe_ring` (32 B header + 4096 B buf; ref=2 at create) shared across two Spoors; each Spoor's `dev->close` drops the ring ref; ring freed at 0. `struct pipe_endpoint` (16 B) per-Spoor priv holds `is_read_end` flag. **Dev vtable** dispatches read/write on `is_read_end`. **dc='\|'** matches Plan 9 9front devpipe. **Spec posture**: no extension — ring correctness is local + structural (tested directly). The blocking variant (P5-pipe-blocking) adds rendez integration + spec extension for the missed-wakeup hazard (ARCH §28 I-9). **10 new unit tests**: smoke + empty + full + short + wraparound (write 3000 / read 2500 / write 3000 / read 3500 → bytes in order across wrap) + wrong-end-rejected (×2) + close-one-end-keeps-other-alive + close-both-frees-ring + **compose_with_spoor_transport** (canonical e2e: two pipe pairs wired into a `p9_spoor_transport` adapter run Tversion + Tattach handshake through real pipes; proves the entire 9P stack composes against the production primitive). **Mid-chunk bugs**: 1. _Static_assert miscounted header (24 vs actual 32 with size_t fields on 8-byte alignment); fixed to `32 + PIPE_BUF_SIZE`. 2. `kmem_cache_create` rejected the 4128 B ring (above SLUB max-object threshold); fix: kmalloc for ring (routes through alloc_pages, same as p9_client), SLUB cache only for the tiny endpoint. **Bootstrap**: `pipe_init()` called from kernel/main.c after `dev9p_init()`; both run post-`dev_init` which has already done `spoor_init`. **No new attack surfaces** — kernel-internal primitive exercised via tests; user-visible pipe(2) syscall deferred to P5-fd-syscalls. Files: `kernel/include/thylacine/pipe.h` (new; ~70 LOC); `kernel/pipe.c` (new; ~330 LOC); `kernel/test/test_pipe.c` (new; ~310 LOC); `kernel/test/test.c` (+10 forward decls + 10 registry entries); `kernel/CMakeLists.txt` (+1 kernel + 1 test source); `kernel/main.c` (+1 include + 1 init call); `docs/reference/51-pipe.md` (new; ~165 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-spoor-transport section marked LANDED). Test count 376 → 386; **386/386 PASS × default + UBSan** (~1.7 s boot). **Next chunks**: P5-pipe-blocking (rendez integration + spec extension — opens the wait/wake invariant scope; needed for actual cross-Proc IPC) OR P5-fd-syscalls (open/close/read/write/dup → KOBJ_SPOOR population from userspace → unblocks deferred SVC handlers + general userspace 9P) OR P5-wire-lock/-xattr/-stratum-ext (deferred low-priority codec extensions). | 386/386 PASS × default + UBSan. |
| `28b5cbb` | **P5-spoor-transport: hash fixup** | (doc-only) |
| `0f45fc6` | **P5-spoor-transport: `kernel/9p_spoor_transport.{c,h}` — adapter wrapping a Spoor pair into `struct p9_transport_ops`.** The Spoor-side counterpart to dev9p — dev9p sits ABOVE the 9P client (Spoors back themselves with 9P round trips); spoor-transport sits BELOW the 9P client (a Spoor pair IS the transport's byte pipe). Together they realize ARCH §9.6's "every filesystem entity is a Spoor" from both directions. **Adapter shape**: `struct p9_spoor_transport { magic, tx_spoor *, rx_spoor *, owns_spoors }`. send routes through `tx_spoor->dev->write` (looped on short writes); recv routes through `rx_spoor->dev->read` (single call per recv — transport core aggregates partial frames); close clunks both Spoors iff `owns_spoors`. tx and rx may be the same Spoor (duplex socket model — Unix socket, vsock) or distinct (Plan 9 pipe model). **No spec extension** — pure plumbing over already-spec'd layers. `specs/9p_client.tla` and `specs/territory.tla` invariants propagate unchanged. **9 new unit tests** in `kernel/test/test_9p_spoor_transport.c`: init_destroy (lifecycle + is_open) + init_null_rejected (NULL guards) + send_routes_to_tx_dev_write (bytes flow tx→buffer) + recv_routes_to_rx_dev_read (pre-loaded buffer→adapter.recv) + recv_empty_returns_zero (EOF) + close_clunks_when_owned (owns=true → both Spoors clunked; spoor_total_freed += 2; second close idempotent) + close_preserves_when_unowned (owns=false → no clunk) + transport_core_round_trip (wraps adapter in struct p9_transport; sends raw Tversion frame; inline synthesizes Rversion in rx; transport recv aggregates correctly) + end_to_end_handshake (full p9_session + p9_transport + spoor-adapter composition: Tversion → INIT→VERSIONED → Tattach → VERSIONED→OPEN; root fid bound). **Test fixture**: static `test_pipe_dev` Dev (NOT registered in bestiary; read/write slots only — other slots NULL, safe because spoor_clunk's dev->close call is guarded) backing per-Spoor 8 KiB FIFO buffers in `aux`. Buffers in static storage (matches P5-transport's lesson: large structs go in static or caller-allocated, never local-stack). **Audit posture**: extends the kernel/9p_* trigger surface that joined CLAUDE.md list at P5-session. No new attack surfaces — adapter is a thin delegation to Dev vtable. **Performance**: identical to underlying Dev — adapter adds only `if (magic && tx_spoor && tx_spoor->dev->write)` checks per call. Files: `kernel/include/thylacine/9p_spoor_transport.h` (new; ~70 LOC); `kernel/9p_spoor_transport.c` (new; ~135 LOC); `kernel/test/test_9p_spoor_transport.c` (new; ~340 LOC); `kernel/test/test.c` (+9 forward decls + 9 registry entries); `kernel/CMakeLists.txt` (+1 kernel source + 1 test source); `docs/reference/50-9p-spoor-transport.md` (new; ~150 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (this row + section + P5-attach-mount section marked LANDED). Test count 367 → 376; **376/376 PASS × default + UBSan** (~1.7 s boot). **Next chunks**: P5-pipe (kernel-internal Plan 9 pipe primitive — pair of Spoors over a shared ring buffer; the production backing for this adapter when stratumd integration lands) OR P5-wire-lock/-xattr/-stratum-ext (codec extensions; deferred low-priority) OR P5-fd-syscalls (the unblocker for the deferred attach_9p / mount / unmount SVC handlers + general userspace 9P consumption). | 376/376 PASS × default + UBSan. |
| `178866f` | **P5-attach-mount: hash fixup** | (doc-only) |
| `c21d002` | **P5-attach-mount: Territory mount table + `mount`/`unmount` kernel primitives + `specs/territory.tla` extension for mount-lifecycle invariants.** Per ARCH §9.6 + §9.6.6: every filesystem entity is a Spoor; `mount(territory, source_spoor, target_path, flags)` grafts a Spoor at a path in a Territory holding one refcount per entry; `unmount(territory, target_path)` removes one entry and drops the ref. **Spec extension**: `specs/territory.tla` grows from 1 clean + 1 buggy cfg to 1 clean + 4 buggy cfgs. New actions Mount / Unmount / ForkClone-extended (mounts cloning) / BuggyMountNoRefbump / BuggyUnmountNoRefdrop / BuggyDestroyLeak over a new state layer `mounts: [Procs -> SUBSET (Paths X Spoors)]` + `refcount: [Spoors -> Nat]`. New invariants MountRefcountConsistency (the per-Spoor counter must equal cardinality of mount entries referencing it) + MountRefcountNonNegative. TLC verdicts: clean 2,560,000 distinct states / no error; cycle buggy 106 states / NoCycle violated; mount-no-refbump 172 states / MRC violated at depth 2; unmount-no-refdrop 884 states / MRC violated at depth 3; destroy-leak 855 states / MRC violated at depth 3. **C-side**: `struct PgrpMount { source *, target, flags }` (16 B; pointer-first for compact layout); `struct Territory` extended with `nmounts` + `mounts[PGRP_MAX_MOUNTS=8]`; total size 80 B → 216 B (24 header + 64 binds + 128 mounts). **Naming rename**: `unmount(territory, src, dst)` → `unbind(territory, src, dst)` (the inverse of bind; reflects Plan 9's bind/unbind/mount/unmount four-way split). Two existing test calls updated; one comment in proc.c updated. **Public API** (kernel/include/thylacine/territory.h): `mount(p, source, target, flags) → 0/-1/-2` (success / source NULL / table full; idempotent on (target, source) equality; MREPL replaces-old-take-new); `unmount(p, target_path) → 0/-1` (success / no entry; removes ONE entry per call, FIRST found); `territory_nmounts` accessor. **Lifecycle integration**: `territory_clone` deep-copies parent's mount table AND `spoor_ref`'s each entry (each clone contributes one new reference per entry, matching the spec's ForkClone refcount update); `territory_unref` final-release loops `mounts[]` and `spoor_unref`'s each before `kmem_cache_free` (mirrors the spec's "Unmount every entry then destroy" decomposition; the BUGGY_DESTROY_LEAK class skips this loop). **Bootstrap discipline**: `territory_init` runs BEFORE `spoor_init` (which is inside `dev_init`); safe because territory_init only creates empty Territories — the dependency `nmounts > 0 → needs Spoors` is satisfied automatically by call ordering. **Mount flags**: MREPL has distinguished semantics at v1.0 (replace existing entry at same target); MBEFORE/MAFTER/MCREATE are stored in the entry's `flags` field for future union-walk support. **`unmount` removes ONE entry** per call (the first found at target_path); Plan 9 union mounts at same target are unmounted by repeated calls. **7 new unit tests** in `kernel/test/test_territory_mount.c`: smoke (mount + nmounts + ref bump + unmount + ref drop) + idempotent_same_source (re-mount no-op success; no second ref bump) + mrepl_replaces (old ref dropped, new ref taken, nmounts unchanged) + unmount_missing_returns_error (-1) + table_full (-2; overflow source ref NOT bumped) + clone_bumps_refs (parent + child each contribute a ref; per-Territory destroy drops one ref) + destroy_drops_all_refs (final release drops all entries' refs in one shot). Existing 3 territory tests (bind_smoke + cycle_rejected + fork_isolated) renamed `unmount` → `unbind` calls (smoke test's last two assertions). **`kernel/territory.c` joins the audit-trigger surface table** (CLAUDE.md / ARCH §25.4) — the mount/unmount paths are now load-bearing for I-7 (VMO refcount / mapping lifecycle generalized to Spoor refcount discipline) and the MountRefcountConsistency invariant. No formal audit round triggered for this chunk (the change is local + spec-proven; the next chunk's surfaces are larger and warrant an audit round). **Audit posture**: extends the kernel/territory.c surface (in the trigger list since P2-Eb); the mount/unmount + spoor_ref discipline is the new attack surface. Bugs in this discipline either leak Spoors (refcount never drops) or use-after-free Spoors (refcount drops below entry count). Both classes pinned by `MountRefcountConsistency`; impl-side discipline mirrors the spec's clean actions. Files: `specs/territory.tla` (extended; ~330 LOC); `specs/territory.cfg` (extended with new constants + flags); `specs/territory_buggy.cfg` (extended with new flags); 3 new buggy cfgs (`territory_buggy_mount_no_refbump.cfg` + `territory_buggy_unmount_no_refdrop.cfg` + `territory_buggy_destroy_leak.cfg`); `kernel/include/thylacine/territory.h` (extended; +60 LOC); `kernel/territory.c` (extended; ~290 LOC; +140 LOC over P2-Eb baseline); `kernel/test/test_territory.c` (rename unmount → unbind in 2 call sites + 2 messages); `kernel/test/test_territory_mount.c` (new; ~210 LOC); `kernel/test/test.c` (+7 forward decls + 7 registry entries); `kernel/CMakeLists.txt` (+1 test source); `kernel/proc.c` (rfork comment refresh referencing the new mount-clone semantics); `docs/reference/18-territory.md` (full rewrite covering both tables); `docs/REFERENCE.md` (Tip + doc-table entry + spec snapshot for territory.tla); `docs/phase5-status.md` (this row + section). Test count 360 → 367; **367/367 PASS × default + UBSan** (~1.7 s boot). **Next chunk**: P5-spoor-transport (Spoor-over-byte-pipe transport backend — the first step toward real stratumd integration; binds the transport vtable's recv/send to a Spoor's read/write) OR P5-wire-lock/-xattr/-stratum-ext (finish the codec). | 367/367 PASS × default + UBSan. |
| `0e1a339` | **P5-attach-create: hash fixup** | (doc-only) |
| `fa31e51` | **P5-attach-create: new `kernel/9p_attach.{c,h}` — kernel-internal mount creation primitive; the body of the future `attach_9p` syscall.** Per ARCH §9.6 + ROADMAP §7.2 refined this chunk: the user-visible `SYS_ATTACH_9P` SVC handler is **deferred until fd-syscall infrastructure exists** (v1.0 has no open/close/read/write syscalls and no userspace path to populate KOBJ_SPOOR handles with byte-pipe Spoors). The substantive work — wrapping a transport in a `p9_client`, driving the handshake, producing a dev9p Spoor for callers — lives in this chunk and is ready for the SVC handler to glue around when fd syscalls land. **`struct p9_attached`**: heap-resident wrapper holding (client *, recv_buf *, recv_cap, root_fid, msize, handshake_ok). Magic field for UAF defense (clobbered on destroy). **Lifecycle**: `p9_attached_create(transport_ops, recv_cap, root_fid, msize, uname, aname, n_uname)` heap-allocates the wrapper + client (~12 KiB; kmalloc routes through alloc_pages for large objects) + recv_buf; runs `p9_client_init` + `p9_client_handshake`; returns the wrapper or NULL on failure (with full cleanup of partial state). `p9_attached_root_spoor(a)` returns a dev9p-backed Spoor wrapping (a->client, a->root_fid) with `fid_owned=false`. `p9_attached_destroy(a)` clunks root_fid + closes the transport + destroys the client + frees all heap allocations. **Lifecycle discipline**: caller must destroy all dev9p Spoors derived from a->client BEFORE calling destroy — at v1.0 enforced by convention; future spec extension (P5-attach-mount's namespace.tla extension per ARCH §9.6.6) will formalize. **Kernel-internal callers at v1.0**: tests (this chunk; loopback transport) + the future P5-stratumd boot path (init creates the transport Spoor + calls p9_attached_create directly, no SVC involved). **4 new unit tests** in `kernel/test/test_9p_attach.c`: create_destroy (lifecycle + is_open) + handshake_failure_returns_null (server Rlerror on Tattach → NULL + full cleanup of partial state; tests the no-leak path) + root_spoor_walk_read (root_spoor → walk → open → read end-to-end through the wrapper) + query_helpers (is_open NULL-safety). Test fixture uses the same canonical-responder pattern as test_dev9p.c. **No spec extension** — p9_attach is pure composition over already-spec'd layers. **Audit posture**: extends the kernel/9p_* trigger surface that joined CLAUDE.md list at P5-session. No new attack surfaces — kernel-internal entry, exercised via test machinery; the future SVC handler will add the user-boundary attack surface (KOBJ_SPOOR fd lookup, user-VA aname pointer validation) which gets formal audit at that chunk. Files: `kernel/include/thylacine/9p_attach.h` (new; ~105 LOC); `kernel/9p_attach.c` (new; ~110 LOC); `kernel/test/test_9p_attach.c` (new; ~210 LOC); `kernel/test/test.c` (+4 forward decls + 4 registry entries); `kernel/CMakeLists.txt` (+1 source + 1 test); `docs/reference/49-9p-attach.md` (new; ~150 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (P5-attach-create row + section + P5-attach-dev section marked LANDED); `docs/ROADMAP.md` (§7.2 P5-attach plan refined: P5-attach-create lands now; P5-attach-syscall deferred until fd syscalls exist; the audit-trigger surface table reflects the split); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-attach-create ✅; P5-attach-syscall remains ⬜ with the deferred-pending-fd-syscalls note). Test count 356 → 360; **360/360 PASS × default + UBSan** (~9 s boot unchanged). **Next chunk**: P5-attach-mount (the architecturally substantive piece — Territory mount table + `specs/namespace.tla` extension for mount-lifecycle invariants; can land WITHOUT the user-visible attach_9p syscall, since the kernel-internal p9_attached_root_spoor is a sufficient source-Spoor for the mount syscall) OR P5-spoor-transport (Spoor-over-Unix-socket backend for the transport vtable; first step toward a real stratumd integration). | 360/360 PASS × default + UBSan. |
| `7786bf0` | **P5-attach-dev: hash fixup** | (doc-only) |
| `f7b2d6c` | **P5-attach-dev: new `kernel/dev9p.{c,h}` — Dev vtable proxying to the kernel 9P client; the foundation of ARCH §9.6 filesystem-as-Spoor.** Implements the bridge that makes a `p9_client`-backed Spoor look identical to a kernel-internal Dev above the vtable. **Dev vtable**: 16 slots; walk → `p9_client_alloc_fid` + `p9_client_walk`; open → `p9_client_lopen` (Plan 9 omode → Linux O_* flag translation); read → `p9_client_read`; write → `p9_client_write`; close → `p9_client_clunk` (conditional on `fid_owned`); stat / create / remove / wstat stubbed at v1.0 (deferred to P5-attach-syscall / -mount where the syscall layer carries mode/perm context). **Per-Spoor state**: `struct dev9p_priv { magic, client *, fid, fid_owned }` allocated via `kmalloc(KP_ZERO)` and stashed in `Spoor->aux`. Magic clobber on free (UAF defense mirroring R9 F148). **Lifecycle ownership**: dev9p does NOT own the `p9_client`; the higher layer (attach_9p syscall) manages client lifecycle. Root Spoors (from `dev9p_attach_client`) have `fid_owned=false` — close does NOT clunk root_fid. Walk-derived Spoors have `fid_owned=true` — close clunks. **Public API surface**: `dev9p_init()` (registers in bestiary; called from `kernel/main.c` after `dev_init()`); `dev9p_attach_client(client, root_fid) → Spoor *` (constructor for the root Spoor of a 9P tree). The standard Dev `attach(spec)` slot returns NULL — spec strings can't carry a (client_ptr, root_fid) pair. **Fid allocator**: new `p9_client_alloc_fid()` helper on the client API; monotonic `next_fid` counter starting at `root_fid + 1`; no reclaim at v1.0 (32-bit space; plenty). **9 new unit tests** in `kernel/test/test_dev9p.c`: registered (bestiary lookup) + attach_client_root_spoor + walk_one_component (Twalk routes correctly, nc gets fresh priv) + walk_clone (nwname=0 shape) + open_lopens_fid (qid updated, COPEN set) + read_routes_through_client (loopback payload echoes) + write_routes_through_client (accepted-count round-trip) + close_clunks_owned_fid (verified via session.fid_bound) + close_does_not_clunk_root_fid (root fid stays bound after closing root Spoor). Test fixture composes loopback transport + p9_client + dev9p root Spoor; canonical responder handles every op dev9p might issue. **Mid-chunk fix**: dev9p initially had `.init = NULL` which broke `dev.vtable_slot_coverage`'s "every slot non-NULL" check. Fix: `.init = dev9p_init_noop` (the actual registration happens in the standalone `dev9p_init()` called from boot, not via the bestiary walk). **No spec extension**: dev9p is pure composition over the spec'd lower layers; `specs/9p_client.tla` invariants propagate unchanged. Mount-lifecycle invariants land at P5-attach-mount. **Audit posture**: extends the kernel/9p_* trigger surface that joined the CLAUDE.md list at P5-session. No new attack surfaces — each vtable op is a thin proxy over already-audited client ops. The `fid_owned` flag is the load-bearing discipline; getting it wrong causes either double-clunk or leaked fids. Documented as caveat #1+#2 in `48-dev9p.md`. Files: `kernel/include/thylacine/dev9p.h` (new; ~100 LOC); `kernel/dev9p.c` (new; ~240 LOC); `kernel/test/test_dev9p.c` (new; ~280 LOC); `kernel/test/test.c` (+9 forward decls + 9 registry entries); `kernel/CMakeLists.txt` (+1 source + 1 test); `kernel/main.c` (+1 include + 1 init call); `kernel/include/thylacine/9p_client.h` (+next_fid field + `p9_client_alloc_fid` decl); `kernel/9p_client.c` (+next_fid init + allocator impl); `docs/reference/48-dev9p.md` (new; ~165 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (P5-attach-dev row + section); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-attach-dev ✅). Test count 347 → 356; **356/356 PASS × default + UBSan** (~9 s boot unchanged). **Next chunk: P5-attach-syscall** — the `attach_9p` syscall that wraps a transport Spoor in a p9_client + drives handshake + returns a dev9p Spoor fd. | 356/356 PASS × default + UBSan. |
| `e1648ef` | **P5-client: hash fixup** | (doc-only) |
| `fc4f2ce` | **P5-client: new `kernel/9p_client.{c,h}` — high-level API consolidating wire + session + transport into 25 op wrappers.** Each wrapper is mechanically identical: `session.send_* → transport.exchange → result extraction → error mapping`. **Lifecycle**: `p9_client_init` (configures embedded session + transport), `p9_client_destroy` (clobbers magic + tears down inner layers), `p9_client_close` (graceful — closes both layers). **Handshake**: one call drives Tversion + Tattach; on return, session state is OPEN and root_fid is bound. **25 op wrappers**: 1 handshake + 3 path (walk / walk_one / clunk) + 4 IO (lopen / lcreate / read / write) + 5 metadata (getattr / setattr / readdir / statfs / fsync) + 8 mutation (symlink / mknod / rename / readlink / link / mkdir / renameat / unlinkat). **Errno convention**: 0 on success, `-errno` on failure. Mapping: `-EINVAL` (bad args / magic mismatch / partial walk request), `-EBUSY` (session state != OPEN), `-EIO` (lower-layer failure — send/recv error, frame malformed, tag pool full, fid bookkeeping conflict), `-<server's ecode>` (Rlerror propagated unchanged from server). **Buffer ownership**: `struct p9_client` embeds session + transport + 8 KiB outbound buffer inline; ~12 KiB total. Caller-provided recv buffer (transport's recv_cap). Read + readdir use **copy semantics** (out of the transport's zero-copy recv_buf pointer) so callers don't need to track recv_buf lifetime. **Internal `copy_qid` / `copy_attr` / `copy_statfs` helpers** replace implicit struct assignments (`*dst = *src`) that the compiler would otherwise lower to memcpy — kernel doesn't link libc, so memcpy is undefined at link time. Mirrors P5-session's explicit-init discipline. **13 new unit tests** in `kernel/test/test_9p_client.c`: lifecycle (init/destroy + invalid args) + handshake (state machine reaches OPEN; total_ops == 2) + walk_and_clunk (clone walk + walk_one + clunk) + lopen_read (qid + iounit + payload extraction) + write (accepted count round-trip) + getattr (Linux statx-shape fields round-trip) + readdir (empty dirent stream) + statfs (bsize round-trip) + mkdir (created_qid surfaced) + unlinkat + readlink (target string surfaced with caller-cap bounded copy) + **rlerror_propagates_to_negative_errno** (server Rlerror{ecode=2} → client returns -2) + **op_before_handshake_returns_ebusy** (op before handshake → -EBUSY). The test fixture uses a single "canonical responder" loopback responder that handles every standard 9P2000.L op with sensible canned responses — one fixture serves all 11 op-coverage tests; only the 2 error-path tests use specialized responders. **No spec extension needed** — pure composition over already-spec'd layers. **Mid-chunk bug + fix**: initial struct assignments (`*out_attr = r.attr;` etc.) compiled to memcpy calls; ld.lld error: `undefined symbol: memcpy`. Fix: 3 field-by-field copy helpers + sed-rewrote 7 call sites in 9p_client.c. The lesson "kernel doesn't link libc → explicit init/copy everywhere" continues to surface across chunks; documented as caveat #1 in 47-9p-client.md (alongside P5-session's "no memset" + P5-transport's "no 16-KiB-on-stack" lessons). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. No new attack surfaces — every op is a thin wrapper over already-audited lower layers. Files: `kernel/include/thylacine/9p_client.h` (new; ~170 LOC); `kernel/9p_client.c` (new; ~520 LOC); `kernel/test/test_9p_client.c` (new; ~430 LOC); `kernel/test/test.c` (+13 forward decls + 13 registry entries); `kernel/CMakeLists.txt` (+1 kernel source + 1 test source); `docs/reference/47-9p-client.md` (new; ~180 LOC); `docs/REFERENCE.md` (Tip + doc-table entry); `docs/phase5-status.md` (P5-client row + section + P5-wire-mutation section marked LANDED); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-client ✅). Test count 334 → 347; **347/347 PASS × default + UBSan** (~9 s boot unchanged). **Codec + session + transport + client stack is now feature-complete for the standard 9P2000.L surface.** The API is ready for the future mount/Dev/syscall integration: a Proc gets a `struct p9_client`, calls `p9_client_init` + `p9_client_handshake`, then issues op wrappers as needed; cleanup is `p9_client_destroy`. The remaining Phase 5 work is integration (P5-attach: mount syscall + per-Proc client lifecycle; P5-spoor-transport: real byte pipe to stratumd) and finishing the codec (lock + xattr + Stratum extensions — deferred low-priority). **Next chunks**: P5-attach (mount syscall + per-Proc lifecycle; architecturally interesting — needs design discussion on syscall ABI shape) OR P5-spoor-transport (Spoor-over-Unix-socket backend binding the transport vtable). | 347/347 PASS × default + UBSan. |
| `97cc73f` | **P5-wire-mutation: hash fixup** | (doc-only) |
| `c7daae7` | **P5-wire-mutation: extend `kernel/9p_wire.c` + `kernel/9p_session.c` with the directory-mutation message family (Tsymlink / Tmknod / Trename / Treadlink / Tlink / Tmkdir / Trenameat / Tunlinkat).** Completes the codec's coverage of the standard 9P2000.L filesystem surface. **Wire codec**: 8 builders + 8 parsers + 16 new `_Static_assert` lines pinning each TX/RX opcode pair. Three response shapes — empty body (Runlinkat / Rrename / Rrenameat / Rlink), single qid (Rsymlink / Rmknod / Rmkdir), string (Rreadlink). Two internal helpers `parse_empty_body` + `parse_qid_body` share the three shapes' boilerplate. `P9_UNLINK_AT_REMOVEDIR = 0x200` exposed for rmdir-equivalent. **Session module**: 8 send APIs — `p9_session_send_symlink` / `_mknod` / `_rename` / `_readlink` / `_link` / `_mkdir` / `_renameat` / `_unlinkat`. **Fid-exclusivity rules**: `send_rename` requires no other in-flight op on `fid` (server-side identity mutation; mirrors setattr discipline from P5-wire-meta); the remaining 7 mutation ops permit concurrent ops on same fid/dfid (server serializes per directory entry internally). **Dispatcher extension**: 8 new branches. None mutate the client-side fid table — even Trename, which mutates server-side path binding, keeps the client's fid bound to the same inode. **`struct p9_dispatch_result` extended** with `created_qid` (Rsymlink / Rmknod / Rmkdir surface; kept distinct from `open_qid` to avoid semantic confusion between "opened existing entry" and "newly created entry") and `readlink_target` + `readlink_target_len` (zero-copy pointer into rmsg buffer). **No spec extension needed** — `specs/9p_client.tla` SendIO action already models all mutation ops at the `io` op-kind level; the spec's invariants (I-10, I-11, FlowControl, OutOfOrderCorrectness) hold by construction across the new ops. **19 new unit tests** — 8 wire (round-trips for each of the 8 ops, with the bonus byte-level encoding check on Tunlinkat's `P9_UNLINK_AT_REMOVEDIR` flag) + 11 session (8 end-to-end round-trips with full Rmsg synthesis + rename-with-inflight-rejected + unlinkat-permits-concurrent + mutation-on-unbound-fid sweep across all 8 ops). Files: `kernel/include/thylacine/9p_wire.h` (+155 LOC); `kernel/9p_wire.c` (+230 LOC); `kernel/include/thylacine/9p_session.h` (+85 LOC; struct p9_dispatch_result + 8 send APIs); `kernel/9p_session.c` (+200 LOC; 8 send fns + 8 dispatcher branches + zero_result extensions); `kernel/test/test_9p_wire.c` (+250 LOC); `kernel/test/test_9p_session.c` (+325 LOC); `kernel/test/test.c` (+19 forward decls + 19 registry entries); `docs/reference/44-9p-wire.md` (+ mutation API + per-message bodies + 8 test entries); `docs/reference/45-9p-session.md` (+ 8 send APIs + 11 test entries + 3 SendIO row split for mutation). Test count 315 → 334; **334/334 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. The mutation family's body shapes are simpler than IO/metadata (no per-op structs; reuse of `parse_empty_body` + `parse_qid_body` keeps the parser surface tight); no R111 cap-bound is needed (every response is either header-only or bounded by a fixed-size qid or a frame-bounded string). **Codec surface now feature-complete for non-Stratum-extension 9P2000.L** — all standard ops (handshake + nav + clunk + IO + metadata + mutation) covered. Remaining: lock family (Tlock / Tgetlock), xattr family (Txattrwalk / Txattrcreate), Stratum extensions (Tsync / Treflink / Tbind / Tunbind / Tfallocate / Tfadvise). **Next chunks**: P5-attach (mount syscall integration + per-Proc session+transport lifecycle) OR P5-wire-lock / P5-wire-xattr / P5-wire-stratum-ext (finish the codec). | 334/334 PASS × default + UBSan. |
| `6a81bb7` | **P5-transport: hash fixup** | (doc-only) |
| `3f7c0e2` | **P5-transport: new `kernel/9p_transport.c` + `kernel/9p_transport_loopback.c` — frame-aware byte pipe + in-memory test loopback backend + session composition helper.** Sits between the session state machine and any underlying byte pipe. **Vtable-driven**: `struct p9_transport_ops { send, recv, close, ctx }`. Backends provide raw read(2)/write(2) semantics; the transport core does framing + partial-read aggregation. **State machine**: INIT → OPEN → CLOSED + ERROR sink (state stays in ERROR once a backend fails — no automatic recovery; caller is expected to destroy + reinit + reconnect at a higher layer). **Send path**: validates outbound `header.size == len` (defends against caller-side framing bugs); defensively loops on short writes (real backends shouldn't but the discipline costs nothing). **Recv path**: reads the 7-byte header first, peeks `header.size`, then loops on `ops->recv` until `header.size` bytes are aggregated. Rejects `size < P9_HDR_LEN`, `size > recv_cap`, and EOF mid-frame. Caller provides the recv buffer (no internal allocation); buffer sized to negotiated msize. **Loopback backend** (test scaffold): in-memory pipe where each `ops->send` call invokes a caller-supplied responder fn that synthesizes the response into a caller-provided staging buffer. Configurable `chunk_size` knob (default 0 = no limit) forces partial-read tests through the transport's recv loop. Loopback's `ops->send` refuses if the prior response hasn't been fully drained (catches test-discipline errors that would silently lose data on real backends). **Session composition**: `p9_transport_exchange(t, s, request_msg, request_len, *out)` = `transport.send` + `transport.recv` + `session.dispatch_rmsg` in one call; lets tests + future client wrapper drive the full stack with a single function call. **9 new unit tests** (`kernel/test/test_9p_transport.c`): lifecycle (init/destroy + magic clobber + invalid arg refusal) + round-trip via echo-flip responder + frame-size mismatch on send (rejected before backend invoked) + frame-too-large on recv (rejected before body bytes read; ERROR transition) + partial-read aggregation (chunk_size=3 forces ~10 ops.recv calls on a 28-byte Tattach; verifies `lb.recvs > 1`) + backend-error → ERROR transition (rejector responder; verifies subsequent I/O refused) + close idempotency + end-to-end handshake (Tversion → Rversion → Tattach → Rattach with state transitions verified at each step) + end-to-end walk (full handshake + Twalk → Rwalk with new_fid binding observed in fid table + qid extracted from result struct). **No spec extension needed**: transport invariants (FramingIntegrity = partial-read aggregation makes frame boundaries structural; RequestResponseOrdering = synchronous request-response is trivially ordered at v1.0; NoMessageLoss = backend handles delivery) are mechanical at this layer. The session-level invariants (`specs/9p_client.tla` I-10, I-11, FlowControl, OutOfOrderCorrectness) compose unchanged — once the transport delivers a complete frame, the session's tag-indexed dispatch handles correctness. When the first async backend lands (Phase 5+), the spec may be extended with connection states + multi-message-in-flight ordering. **Mid-chunk bug**: initial loopback design had a 16 KiB inline staging buffer (`u8 response[P9_LOOPBACK_BUF_MAX]` with `P9_LOOPBACK_BUF_MAX = 16*1024`). When declared as `struct p9_loopback lb;` in the first test function, the 16 KiB inline buffer overflowed the kernel's 16 KiB test-thread stack on the very first transport test (`EXTINCTION: kernel stack overflow 0x...`). **Fix**: refactored loopback to caller-provided staging buffer (matches the transport's caller-provided recv_buf pattern). Loopback struct shrank from ~16 KiB to ~80 bytes. Tests now declare a `static u8 g_loopback_resp_buf[4096]` at file scope. The lesson — "structs > 1 KiB go in `static` or caller-allocated, never local-stack" — applies project-wide and is now documented in `46-9p-transport.md` caveat #1. Files: `kernel/include/thylacine/9p_transport.h` (new; ~150 LOC); `kernel/9p_transport.c` (new; ~210 LOC); `kernel/include/thylacine/9p_transport_loopback.h` (new; ~75 LOC); `kernel/9p_transport_loopback.c` (new; ~115 LOC); `kernel/test/test_9p_transport.c` (new; ~340 LOC after removing unused `rclunk_with_request_tag_responder`); `kernel/test/test.c` (+9 forward decls + 9 registry entries); `kernel/CMakeLists.txt` (2 new kernel sources + 1 new test source); `docs/reference/46-9p-transport.md` (new; ~180 LOC); `docs/phase5-status.md` (P5-transport row + section + P5-wire-meta section marked LANDED with hashes); `docs/REFERENCE.md` (Tip refresh + new doc-table entries for 44/45/46); `specs/SPEC-TO-CODE.md` (Phase 5 progress P5-transport ✅; no spec module changes). Test count 306 → 315; **315/315 PASS × default + UBSan** (~9 s boot unchanged; new tests are pure-byte work). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. The transport's only attack surfaces are framing (defended structurally by strict size-equality + partial-read aggregation) and backend-failure recovery (transport sinks into ERROR; caller responsible for destroy + reinit). **Next chunks**: P5-attach (`kernel/9p_attach.c` — mount syscall integration + per-Proc session+transport lifecycle); OR P5-wire-mutation (Tunlinkat / Trename / Trenameat / Tsymlink / Tmknod / Tlink / Treadlink / Tmkdir — directory mutation surface to complete the codec). | 315/315 PASS × default + UBSan. |
| `c44970d` | **P5-wire-meta: hash fixup** | (doc-only) |
| `c95b194` | **P5-wire-meta: extend `kernel/9p_wire.c` + `kernel/9p_session.c` with the metadata message family (Tgetattr / Tsetattr / Treaddir / Tstatfs / Tfsync).** **Wire codec**: 5 builders + 5 parsers + 10 new `_Static_assert` lines pinning every TX/RX opcode pair. New `struct p9_attr` (Linux statx-shaped attribute record — 20 fields including qid + mode + uid/gid + nlink + rdev + size + blksize + blocks + 4 timestamp pairs + gen + data_version; 153-byte wire body), `struct p9_setattr` (the 60-byte mutation payload — caller's valid-mask + mode + uid + gid + size + atime/mtime sec+nsec pairs), `struct p9_statfs` (60-byte fs-stats record). New `p9_unpack_dirent` helper consumes one record from a Rreaddir data stream (zero-copy name pointer aliasing input buffer). Rgetattr request-mask + setattr valid-mask constants exposed (`P9_GETATTR_BASIC / _ALL`, `P9_SETATTR_MODE / _UID / _GID / _SIZE / _ATIME / _MTIME / _CTIME / _ATIME_SET / _MTIME_SET`). **Discipline**: strict body-length equality at every parser end + R111 caller-cap-bound at `p9_parse_rreaddir` (identical to Rread — server-supplied dirent-stream count bounded against caller's `data_cap` BEFORE the data pointer is exposed). **Session module**: 5 send APIs — `p9_session_send_getattr` / `_setattr` / `_readdir` / `_statfs` / `_fsync`. **Fid-exclusivity rules**: `send_getattr` / `send_statfs` / `send_readdir` / `send_fsync` permit concurrent ops on same fid (read-shaped at the wire layer — no server-side fid state mutation on canonical identity, and Treaddir/Tread share explicit-offset semantics); `send_setattr` requires no other in-flight op on fid (mutation-shaped — server-side attribute write is exclusive; concurrent setattr is undefined behavior per 9P2000.L). **Dispatcher extension**: 5 new branches for `op->kind ∈ {TGETATTR, TSETATTR, TREADDIR, TSTATFS, TFSYNC}`. None mutate the fid table. Treaddir's `data_cap` derived from `s->negotiated_msize - 11` (same as Tread; the dirent stream is bounded by msize). **`struct p9_dispatch_result` extended** with `attr` (Rgetattr surface), `statfs` (Rstatfs surface), `readdir_count` + `readdir_data` (zero-copy dirent stream; consumer parses entries via `p9_unpack_dirent`). **No spec extension needed**: `specs/9p_client.tla`'s SendIO action already models all metadata ops at the `io` op-kind level; the spec's invariants (I-10 tag uniqueness, I-11 fid stability, FlowControl, OutOfOrderCorrectness) hold by construction across the new ops. **15 new unit tests** — 7 wire (tgetattr / tsetattr / treaddir / tstatfs / tfsync round-trips + dirent_unpack zero-copy traversal + rreaddir_data_cap_enforced (R111)) + 8 session (getattr / setattr / readdir / statfs / fsync end-to-end with full Rmsg synthesis + setattr_with_inflight_on_fid_refused + getattr_permits_concurrent + meta_from_unbound_fid_refused). **Mid-chunk bug found**: initial body-length arithmetic for Rgetattr had `8 * 13` (= 104 bytes for 13 u64s); actual u64 count after qid is 15 (5 mid + 8 times + 2 trailing). Fix: `8 * 15` (= 120 bytes); test caught it before formal audit. Files: `kernel/include/thylacine/9p_wire.h` (+155 LOC); `kernel/9p_wire.c` (+200 LOC); `kernel/include/thylacine/9p_session.h` (+60 LOC; struct p9_dispatch_result extended); `kernel/9p_session.c` (+135 LOC including 5 send fns + 5 dispatcher branches + zero_result extensions); `kernel/test/test_9p_wire.c` (+280 LOC; 7 new tests); `kernel/test/test_9p_session.c` (+270 LOC; 8 new tests + 5 synth_* helpers); `kernel/test/test.c` (+15 forward decls + 15 registry entries); `docs/reference/44-9p-wire.md` (+ metadata family API + bodies + caller-cap-bound for Rreaddir + dirent record shape + 7 test entries); `docs/reference/45-9p-session.md` (+ metadata send APIs + dispatcher behavior + struct p9_dispatch_result attr/statfs/readdir fields + 8 test entries + 3 SendIO row split). Test count 291 → 306; **306/306 PASS × default + UBSan** (~9 s boot unchanged). **Audit posture**: extends the kernel/9p_* surface that joined the CLAUDE.md trigger list at P5-session. The Rgetattr body-length bug is a perfect illustration of why pinned struct sizing matters — caught at the test layer in seconds; the kernel never shipped the wrong frame. **Next chunks**: P5-transport (Spoor-over-Unix-socket transport layer wrapping the session); OR P5-wire-mutation (Tunlinkat / Trename / Trenameat / Tsymlink / Tmknod / Tlink / Treadlink / Tmkdir — directory mutation surface). | 306/306 PASS × default + UBSan. |
| `f06fe12` | **P5-wire-io: hash fixup** | (doc-only) |
| `3aa508c` | **P5-wire-io: extend `kernel/9p_wire.c` + `kernel/9p_session.c` with the IO message family.** **Wire codec** (`kernel/9p_wire.{c,h}`): adds 4 builders + 4 parsers — `p9_build_tlopen` / `p9_parse_rlopen` (open existing fid; body shape: `[fid: u32][flags: u32]` → `[qid: 13][iounit: u32]`); `p9_build_tlcreate` / `p9_parse_rlcreate` (create + rebind; body shape: `[fid][name: str][flags][mode][gid]` → `[qid: 13][iounit: u32]`); `p9_build_tread` / `p9_parse_rread` (zero-copy data pointer; R111 caller-cap-bound applied; body shape: `[fid][offset: u64][count]` → `[count][data * count]`); `p9_build_twrite` / `p9_parse_rwrite` (payload-copy at build; body shape: `[fid][offset][count][data * count]` → `[count]`). Internal `parse_open_create_body` helper shared between Rlopen + Rlcreate parsers (identical body shape; type-strict at the header validator). **8 new `_Static_assert` lines** pin TLOPEN/RLOPEN/TLCREATE/RLCREATE/TREAD/RREAD/TWRITE/RWRITE opcode pairs (every `RX = TX + 1`). **Session module** (`kernel/9p_session.{c,h}`): adds 4 send APIs — `p9_session_send_lopen`, `p9_session_send_lcreate`, `p9_session_send_read`, `p9_session_send_write`. **Fid-exclusivity rules**: `send_lopen` + `send_lcreate` require no other in-flight op on fid (server-side fid state mutation is exclusive at the wire layer); `send_read` + `send_write` permit concurrent ops on same fid (offset is explicit on the wire; server is stateless wrt position). **Dispatcher extension**: `p9_session_dispatch_rmsg` gains 4 new branches for `op->kind ∈ {TLOPEN, TLCREATE, TREAD, TWRITE}`; none mutate the fid table (Tlopen + Tlcreate change server-side meaning; Tread + Twrite mutate only file content). **`struct p9_dispatch_result` extended** with `open_qid` + `open_iounit` (Tlopen/Tlcreate result); `read_count` + `read_data` (zero-copy data pointer for Tread); `write_count` (Tread/Twrite accepted byte count). Tread's `data_cap` derived from the session's `negotiated_msize`: `negotiated_msize - 11` bounds the server-supplied count BEFORE the data pointer is exposed (R111 doctrine at the session boundary). **15 new unit tests** — 7 in `test_9p_wire.c` (Tlopen/Tlcreate/Tread/Twrite round-trips + R111 caller-cap-bound on Rread + strict-equality on Rread + Rlopen-vs-Rlcreate type-strict) + 8 in `test_9p_session.c` (lopen/lcreate/read/write end-to-end + concurrent-Tlopen refused + concurrent-Tread permitted + IO-from-unbound-fid refused + IO-before-OPEN refused). **No spec extension needed**: `specs/9p_client.tla` already models `SendIO` generically (4 kinds collapse to `io`); the wire-level dispatch composes within the existing spec envelope. **Audit posture**: extends the `kernel/9p_*` surface that joined the CLAUDE.md trigger list at P5-session. Bugs here (mis-decoded count, type-confusion between Rlopen and Rlcreate, OOO mispair on shared-fid concurrent reads) all surface as test failures + would break I-10 (tag) / I-11 (fid) / FlowControl (window) properties at the spec level. Files: `kernel/include/thylacine/9p_wire.h` (+90 LOC); `kernel/9p_wire.c` (+140 LOC); `kernel/include/thylacine/9p_session.h` (+50 LOC; `struct p9_dispatch_result` extended); `kernel/9p_session.c` (+115 LOC); `kernel/test/test_9p_wire.c` (+225 LOC; 7 new tests); `kernel/test/test_9p_session.c` (+225 LOC; 8 new tests + 3 helpers); `kernel/test/test.c` (forward decls + 15 registry entries); `docs/reference/44-9p-wire.md` (+IO family API + bodies + caller-cap-bound section + 7 test entries); `docs/reference/45-9p-session.md` (+IO send APIs + IO dispatcher behavior + 8 test entries). Test count 276 → 291; **291/291 PASS × default + UBSan** (~9 s boot, unchanged). **Next chunks**: P5-transport (Spoor-over-Unix-socket transport layer wrapping the session); OR P5-wire-meta (Tgetattr/Tsetattr/Treaddir/Tstatfs/Tfsync — adds the metadata surface for ls/stat/mkdir-equivalent operations). | 291/291 PASS × default + UBSan. |
| `a8eddd0` | **P5-session: hash fixup** | (doc-only) |
| `16d6c80` | **P5-session: `kernel/9p_session.c` state machine.** Composes the P5-wire codec into the per-session tag pool + fid table + outstanding-request bookkeeping that `specs/9p_client.tla` describes. **State machine**: INIT → VERSIONED → OPEN → CLOSED with explicit transitions. **Tag pool**: bitmap-style allocator over `outstanding[64]`; first-free-slot return; refuses if no free slot (flow-control trip). **Fid table**: linear array, swap-with-last on unbind. **Send API** (each writes a Tmsg into the caller's buffer + inserts into outstanding; returns total bytes or -1): `p9_session_send_version` (uses NOTAG; out-of-band), `p9_session_send_attach` (binds root on Rattach), `p9_session_send_walk` (Send-time precondition: src bound + new not bound + new ≠ root + no other in-flight op on new), `p9_session_send_clunk` (Send-time-unbinds fid + precondition: fid bound + fid ≠ root + no other in-flight op on fid). **Receive API**: single `p9_session_dispatch_rmsg` peeks the header type, special-cases Rversion (NOTAG, out-of-band), looks up `outstanding[tag]` for every other Rmsg, validates the type matches the stored kind (or is Rlerror), applies the state mutation (walk binds new_fid; clunk no-op; Rlerror surfaces ecode), clears outstanding[tag]. **Spec invariants pinned** by the impl: **I-10 TagUniqueness** (alloc_tag refuses an active slot; structural prevention of overwriting), **I-11 FidStability** (every Send checks `fid_bound`; SendClunk Send-time-unbinds), **OutOfOrderCorrectness** (dispatch_rmsg uses tag-indexed lookup; op_id stored at Send pairs with Receive), **FlowControl** (alloc_tag returns -1 when window full; back-pressure surfaces as Send-side -1 not silent overflow). **17 new unit tests** in `kernel/test/test_9p_session.c`: lifecycle (init/destroy/magic clobber/invalid arg) + 2-step handshake (version → attach → OPEN) + walk/clunk round trips + Send-time-unbind verification + Rlerror dispatch + 5 Send-time precondition rejection paths (walk-to-root / walk-to-bound / walk-from-unbound / clunk-root / clunk-with-inflight) + fid-after-clunk refusal (the spec's BuggyFidAfterClunkSend shape) + 2 dispatch rejection paths (wrong-tag / wrong-type) + close-with-inflight refusal + state-gate test (send_walk before OPEN). **Compile-time invariants**: 3 `_Static_assert` lines pin `MAX_OUTSTANDING >= 1` + `MAX_OUTSTANDING <= 0xFFFE` (leaves room for NOTAG) + `MAX_FIDS >= 1`. **Sizing**: P9_SESSION_MAX_OUTSTANDING = 64; P9_SESSION_MAX_FIDS = 256; P9_SESSION_MAGIC = "P9SE" (0x50395345). **No memset / memcpy** in the impl — all field initialization is explicit. (Test code's `struct p9_dispatch_result r;` is uninitialized at declaration; dispatcher's internal `zero_result` is the load-bearing init.) Files: `kernel/include/thylacine/9p_session.h` (~165 LOC); `kernel/9p_session.c` (~410 LOC); `kernel/test/test_9p_session.c` (~395 LOC); `kernel/test/test.c` (forward decls + 17 registry entries); `kernel/CMakeLists.txt` (kernel + test source lists); `specs/SPEC-TO-CODE.md` (9p_client section: state-machine table updated to point at actual impl symbols + Phase 5 progress table); `docs/reference/45-9p-session.md` (~200 LOC; new). Test count 259 → 276; 276/276 PASS × default + UBSan (~9 s boot). **Audit posture**: the surface joins CLAUDE.md trigger list as the spec's state machine realized in kernel C. The unit-test matrix directly covers the spec's 4 buggy cfg shapes — any impl regression that introduces a tag-collision, fid-after-clunk, OOO-match, or unbounded-outstanding bug breaks at least one test. **Predecessor**: P5-wire (`1aa4826`/`42e87ed`). **Next chunk**: P5-transport (`kernel/9p_transport.c`) — wraps this session module + a Spoor into the actual transport layer; OR P5-wire-io (extends the codec with Tread/Twrite/Tlopen/Tlcreate, then the session adds matching dispatch handlers). | 276/276 PASS × default + UBSan. |
| `42e87ed` | **P5-wire: hash fixup** | (doc-only) |
| `1aa4826` | **P5-wire: `kernel/9p_wire.c` — 9P2000.L wire codec (bring-up subset).** The lowest layer of the kernel 9P client stack; pure byte-level marshal/unmarshal with no kernel state, no allocation, no I/O. **Subset covered**: header peek; pack/unpack primitives (u8 / u16 / u32 / u64 / 9P-string / qid); Tversion/Rversion (handshake); Tattach/Rattach (root fid bind); Twalk/Rwalk (fid navigation + clone); Tclunk/Rclunk (fid release); Rlerror parse (server error response). **Out of scope for this chunk** (deferred to P5-wire-io / P5-wire-meta / P5-wire-mutation / P5-wire-lock / P5-wire-xattr / P5-wire-stratum-ext): Tlopen / Tread / Twrite / Tlcreate; Tgetattr / Tsetattr / Treaddir / Tstatfs / Tfsync; mutation family (Tunlinkat / Trename / Trenameat / Tsymlink / Tmknod / Tlink / Treadlink / Tmkdir); Tlock / Tgetlock; xattr family; Stratum extensions (Tsync / Treflink / Tbind / Tunbind / Tfallocate / Tfadvise). **Discipline**: every parser enforces `header.size == frame_length` AND `body offset == frame_length` at the end (strict body-length equality per Stratum's R111 P3 F-10 doctrine); R111 caller-cap-bound applied at `p9_parse_rwalk` (`nwqid > qid_cap` rejected BEFORE any qid is unpacked into the caller buffer). **Compile-time invariants**: 8 `_Static_assert` lines pin opcode constants + header/qid lengths + sentinel values + struct-shape minimum size. **Error convention**: pack/unpack/build return byte count or negative; parse returns 0 or negative. Coarse-grained errors (buffer-too-small / malformed / wrong-type); no errno-style channel. **Audit posture**: foundation layer; the `kernel/9p_*` surface joins CLAUDE.md trigger list when P5-session lands. Bugs here can break I-10 (tag mis-decoded) and I-11 (newfid corrupted) — guarded by the unit-test matrix. **15 new unit tests** in `kernel/test/test_9p_wire.c`: 7 round-trips (primitives × 4, string, qid, header, each Tmsg+Rmsg pair) + 8 rejection paths (overflow, underflow, size-mismatch, wrong-type, oversize-string, caller-cap-bound violation). Files: `kernel/include/thylacine/9p_wire.h` (~225 LOC), `kernel/9p_wire.c` (~370 LOC), `kernel/test/test_9p_wire.c` (~325 LOC), `kernel/test/test.c` (forward decls + 15 registry entries), `kernel/CMakeLists.txt` (kernel-source list + test-source list), `docs/reference/44-9p-wire.md` (new; ~200 LOC). Test count 244 → 259. 259/259 PASS × default + UBSan. Non-audit-bearing at this layer (pure codec, no state); audit-bearing when composed with P5-session in the next chunk. Predecessor: P5-spec. | 259/259 PASS × default + UBSan. |
| `f1aadfc` | **P5-spec: hash fixup** | (doc-only) |
| `ce4fa31` | **P5-spec: `specs/9p_client.tla` — Phase 5 entry, spec-first per CLAUDE.md.** Pins ARCH §28 I-10 (per-session tag uniqueness) + I-11 (fid identity stable across open lifetime) + two composition-layer properties from ROADMAP §7 / `stratum/v2/docs/OS-INTEGRATION.md` §3: OutOfOrderCorrectness (Rmessages match Tmessages by tag, not by arrival order) + FlowControl (outstanding-request count bounded; back-pressure surfaces as Send-side blocking, never as silent drop). **4 invariants** (`TypeOk`, `RootFidImmutable`, `TagAndOpAccounting`, `FidStability`, `BoundedOutstanding`). **4 buggy cfg variants** exercise each invariant's violation surface: `tag_collision` (alloc_tag returns in-use tag), `fid_after_clunk` (IO on unbound fid), `ooo_match` (Rmsg paired with wrong tag's op), `unbounded` (Send past MaxWindow). **TLC posture**: correct cfg clean (462 generated / 197 distinct / depth 9); each buggy cfg produces expected counterexample in ≤ 6 steps (50-68 generated / 42-52 distinct / depth 5-6). **Modeling decisions**: one session modeled (cross-session is Stratum's `fid.tla` + `namespace.tla` server-side concern); op kinds collapsed to `{walk, clunk, io}` (full 9P2000.L baseline + Stratum extensions all collapse into `io` for spec purposes); `SendClunk` requires no other in-flight op on the same fid (canonical client discipline); per-op `op_id` (monotonic) lets invariants distinguish ops across tag-slot reuse. Bounded model: `TagIds = {t1, t2, t3}`, `FidIds = {root, f1}`, `MaxWindow = 2`, `MaxOps = 3` — small state space, every bug class reaches violation in ≤ 6 steps. **State machine cross-references** in `specs/SPEC-TO-CODE.md` map each spec action to its planned Phase 5 impl symbol (`kernel/9p_attach.c::session_attach`, `kernel/9p_session.c::session_send_io` / `_walk` / `_clunk` / `dispatch_rmsg`). **No impl yet** — Phase 5 entry chunk, gates P5-wire (codec) + P5-session (state machine) + P5-transport (Spoor-over-Unix-socket) + P5-attach (mount syscall) per CLAUDE.md spec-first policy. Files: `specs/9p_client.tla` (~395 LOC), `specs/9p_client.cfg` + 4 buggy cfgs (~15 LOC each), `specs/SPEC-TO-CODE.md` (new 9p_client section ~85 LOC; replaces the prior "Phase 4 (planned)" stub). 244/244 PASS × default + UBSan unchanged (spec-only commit). Audit posture: spec-bearing per CLAUDE.md §"Spec-first policy"; the 9p_client surface joins the audit-trigger surface list once P5-wire/P5-session impl lands. | 244/244 PASS × default + UBSan (spec-only). 5 TLC verdicts: 1 clean (correct) + 4 expected-violation (buggy). |

---

## Planned chunk sequence (proposed)

This is a working plan, refined as chunks land. The spec-first chunks come first; everything else builds on them.

### P5-spec — `specs/9p_client.tla` (mandatory; spec-first per CLAUDE.md)

State model: per-session tag pool, fid table, outstanding-request set, transport queue. Actions: `Tversion` / `Rversion`, `Tattach` / `Rattach`, `Twalk` / `Rwalk`, `Tlopen` / `Rlopen`, `Tread` / `Rread` (with offset + count), `Tclunk` / `Rclunk`, plus the Stratum extensions per ARCH §10.2. Invariants:

- **I-10 TagUniqueness**: at all times, no two outstanding `Tmessage`s on the same session share a tag.
- **I-11 FidStability**: a fid's identity (qid binding) is stable from `Tattach` / `Twalk` until `Tclunk`.
- **OutOfOrderCorrectness**: an `Rmessage` arriving after a later-issued `Rmessage` (out-of-order completion) is matched by tag, not by arrival order. The pipelined client must not assume FIFO.
- **FlowControl**: outstanding-request count is bounded by the negotiated `msize` / per-session window; back-pressure surfaces as `Tsubmit` blocking, not as silent drops.

At least 4 buggy `.cfg` variants: `tag_collision`, `fid_after_clunk`, `out_of_order_wrong_match`, `unbounded_outstanding`.

### P5-wire — `kernel/9p_wire.c` (codec) [LANDED `1aa4826`/`42e87ed`]

9P2000.L baseline message framing + marshal/unmarshal. Each message gets a `_Static_assert` on layout. Round-trip tests against a Stratum reference message corpus (Stratum's own test suite includes message fixtures we can consume).

### P5-wire-io — codec IO extension [LANDED `3aa508c`/`f06fe12`]

Extension chunk: adds Tlopen/Rlopen, Tlcreate/Rlcreate, Tread/Rread, Twrite/Rwrite to the codec + matching `p9_session_send_lopen/lcreate/read/write` send APIs + dispatcher branches for the 4 R-types. Fid-exclusivity discipline: lopen + lcreate require fid not currently in flight (server-side state mutation is exclusive); read + write permit concurrent ops on same fid (explicit-offset wire). R111 caller-cap-bound applied at `p9_parse_rread`. Test count 276 → 291.

### P5-wire-meta — codec metadata extension [LANDED `c95b194`/`c44970d`]

Extension chunk: adds Tgetattr/Rgetattr, Tsetattr/Rsetattr, Treaddir/Rreaddir, Tstatfs/Rstatfs, Tfsync/Rfsync to the codec + 5 session send APIs + dispatcher branches. New structs: `p9_attr` (statx-shape), `p9_setattr` (mutation payload), `p9_statfs` (fs-stats). New `p9_unpack_dirent` for consumer-side dirent-stream traversal. Fid-exclusivity: getattr/statfs/readdir/fsync concurrent-permitted; setattr fid-exclusive. R111 caller-cap-bound at `p9_parse_rreaddir`. Test count 291 → 306.

### P5-transport — `kernel/9p_transport.c` + loopback backend [LANDED `3f7c0e2`/`6a81bb7`]

Frame-aware byte pipe + in-memory loopback backend + session composition helper. Vtable-driven (`struct p9_transport_ops { send, recv, close, ctx }`); backends provide raw byte-level read/write semantics; transport core does framing + partial-read aggregation. State machine: INIT → OPEN → CLOSED + ERROR sink. Session composition (`p9_transport_exchange`) chains session.send + transport.send + transport.recv + session.dispatch in one call. Loopback backend (test scaffold) lets tests drive full session+transport composition without an external server; `chunk_size` knob forces partial-read paths. No spec extension needed (transport invariants are mechanical; session invariants compose unchanged). Test count 306 → 315.

### P5-wire-mutation — codec mutation extension [LANDED `c7daae7`/`97cc73f`]

Extension chunk: adds Tsymlink/Tmknod/Trename/Treadlink/Tlink/Tmkdir/Trenameat/Tunlinkat to the codec + 8 session send APIs + dispatcher branches. Three response shapes (empty body / qid-only / string). Internal `parse_empty_body` + `parse_qid_body` helpers shared across responses. Trename is fid-exclusive (server-side identity mutation); other 7 ops permit concurrent. `P9_UNLINK_AT_REMOVEDIR = 0x200` exposed. struct p9_dispatch_result gains `created_qid` (symlink/mknod/mkdir) + `readlink_target` + `readlink_target_len`. **Codec now feature-complete for standard 9P2000.L** — only lock, xattr, and Stratum extensions remain. 19 new unit tests; test count 315 → 334.

### P5-client — high-level client API [LANDED — see top of Landed chunks table]

Consolidates the wire + session + transport stack into one callable surface. `struct p9_client` embeds session + transport + 8 KiB outbound buffer (~12 KiB total — must be statically allocated, not on stack). 25 op wrappers covering every codec-supported op: lifecycle, handshake (Tversion + Tattach in one call), path (walk / walk_one / clunk), IO (lopen / lcreate / read / write), metadata (getattr / setattr / readdir / statfs / fsync), mutation (symlink / mknod / rename / readlink / link / mkdir / renameat / unlinkat). Each op: `session.send_* → transport.exchange → result extraction → error mapping`. Signed-errno return convention (0 / -EINVAL / -EBUSY / -EIO / -ecode). Internal `copy_qid` / `copy_attr` / `copy_statfs` helpers replace implicit memcpy-via-struct-assignment (kernel doesn't link libc). 13 new unit tests; test count 334 → 347. **Stack is now feature-complete for non-Stratum-extension 9P2000.L** — the API surface ready for mount/Dev/syscall integration.

### P5-session — `kernel/9p_session.c` (state machine)

Tag pool (per-session, monotonic generation per CLAUDE.md handle-discipline pattern). Fid table (per-session, with magic clobber on clunk mirroring KObj_* discipline). Outstanding-request table. Send / receive loops. Spec cross-references at each state transition.

### P5-spoor-transport — Spoor-over-Unix-socket backend (deferred)

The actual byte-pipe binding that connects the transport layer (above) to `stratumd` over a Unix-socket Spoor inside the guest VM. Deferred until P5-stratumd lands the daemon-side. The transport layer at P5-transport is backend-agnostic; this is a mechanical binding chunk when its dependencies are ready. Per Stratum's `OS-INTEGRATION.md` §14: 9P2000.L `msize` negotiation with the standard 8-MiB ceiling.

### P5-attach — `kernel/9p_attach.c` (mount syscall integration)

`mount(fd, path, "subvol_name")` → `Tattach` with `subvol_name` as `aname`. Per-Proc connection setup at `rfork`. Cleanup at `exits`.

### P5-key — janus integration

`.key` sidecar handling in libthyla-rs (or a kernel-side janus client; design pass needed). `mlock(2)` + `MADV_DONTDUMP` + `explicit_bzero` discipline. Argon2id is the v1.0 default; TPM-sealed at v1.x.

### P5-stratumd — `stratumd` lifecycle in initramfs

Initramfs holds `stratumd`, `janus`, `init`, and the wrapped `.key`. init prompts → janus unwraps → init forks `stratumd` → init waits for FS socket bind → init mounts `/sysroot` over 9P → init pivots root. Post-pivot: `/srv/stratum-ctl/` mounted from the `/ctl/` socket.

### P5-ctl — `/srv/stratum-ctl/` consumption

Userspace tools dial the `/ctl/` socket. The first integration is `cat /srv/stratum-ctl/version` (sanity); the second is `echo start > /srv/stratum-ctl/pools/<uuid>/scrub-trigger` + monitoring via `/srv/stratum-ctl/pools/<uuid>/scrub`.

### P5-id — virtio-net `/dev/ether0` 9P surface (re-scoped from P4-Id)

Once the "userspace driver as 9P server" primitive is in place (likely as a side effect of P5-attach + P5-transport), virtio-net-loop's RX-drain feeds into a 9P file-tree backing `/dev/ether0`. Other processes `open` / `read` / `write` it for raw Ethernet frames.

### P5-cons — virtio-input `/dev/cons` 9P surface (re-scoped from P4-K-events tail)

Same shape as P5-id but for the keyboard event stream. virtio-input's eventq drain feeds into a 9P file at `/dev/cons` that other processes (a shell, a textual editor) `read` to consume input events.

### P5-snapshot-upgrade — atomic-snapshot upgrade smoke test

Per OS-INTEGRATION.md §8: snapshot root dataset → break userspace → reboot → rollback verb → reboot succeeds. This is the strongest exit signal for Phase 5: a real atomic-system-upgrade demonstration.

### P5-audit — cumulative 9P client audit (R-series prosecutor)

The 9P client is an audit-bearing surface per CLAUDE.md trigger surfaces. After all P5-* chunks land, spawn the prosecutor scoped to `kernel/9p_*` + `kernel/9p_session.c`'s tag / fid management + boot-pivot ordering. Adversarial categories: wire-protocol malformations, tag collision under retry, fid lifetime UAF, out-of-order completion races, msize boundary, flow-control deadlocks, key-handling lifetime.

---

## §7.3 Exit criteria status

(All boxes ticked from the ROADMAP §7.3 list at Phase 5 close. Each gets a citation when ticked.)

- [ ] `specs/9p_client.tla` clean under TLC. ≥ 4 buggy `.cfg` variants exist.
- [ ] `stratumd` boots from initramfs against a real Stratum pool on virtio-blk.
- [ ] Kernel mounts the FS socket at `/`. Basic FS ops (`ls`, `cat`, `mkdir`, `echo > / cat <`) all work.
- [ ] janus unwraps a passphrase-protected pool key.
- [ ] **Reboot test**: data written before reboot persists; Stratum's three-phase sync verifies.
- [ ] **Pull-the-plug test**: SIGKILL stratumd mid-write; next mount succeeds + state consistent.
- [ ] 9P session: 10,000 open/read/write/close cycles without leak.
- [ ] Pipelined throughput: 32 concurrent reads achieve ≥ 90% session bandwidth.
- [ ] 9P round-trip latency (loopback Stratum): p99 < 500µs (VISION §4.5).
- [ ] 100 procs × 1 connection each works without leaks.
- [ ] Stratum extensions verified: `Tbind`, `Tsync`, `Treflink`, xattr round-trip.
- [ ] No P0/P1 audit findings on the 9P client.
- [ ] `/srv/stratum-ctl/` mounted; `version` + `scrub-trigger` + `events` all functional.
- [ ] Atomic-snapshot upgrade smoke test passes.
- [ ] Prometheus exposition reachable; `stratum_pool_*` metrics readable.
- [ ] POSIX surface from Stratum v2 (per OS-INTEGRATION.md §6) exercises correctly through Thylacine's 9P client.

---

## Trip hazards carried from Phase 4

(per `docs/handoffs/038-p4-close.md`):

1. F225 (QueueReady-before-populate ordering) — composition-layer cosmetic; ride alongside any P5 chunk that touches `usr/virtio-*` if convenient.
2. R12-bss-2mib (kernel.ld assert tightening) — 792 KiB headroom today; not urgent.
3. F202 + F203 (GIC ICFGR SMP-RMW + stale-ICPENDR on re-claim) — both v1.0-not-reachable; revisit when SMP-driver-restart paths land.
4. F149 + F150 (per-CPU SGI/PPI semantics + ReduceCaps drop-precondition) — both forward-looking; revisit when SMP-aware drivers + cap-drop syscall land.

---

## Build + verify commands

Once `kernel/9p_*` lands, the test runner needs a Stratum pool to test against. Recommended test fixture: a tiny pre-formatted Stratum pool image (`build/stratum-test-pool.img`) generated by `tools/mkstratumpool.sh` calling Stratum's `mkfs` CLI. `tools/run-vm.sh` attaches it as a second virtio-blk device.

```bash
# Build everything
tools/build.sh all

# Generate the Stratum test pool (one-time per Stratum version)
tools/mkstratumpool.sh

# Run tests (default)
tools/test.sh

# Run tests with the Stratum pool attached + boot pivot active
THYLACINE_STRATUM_POOL=build/stratum-test-pool.img tools/test.sh

# All specs (including the new 9p_client.tla)
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"
cd specs && for s in *.tla; do
  java -cp /tmp/tla2tools.jar tlc2.TLC -workers auto -deadlock \
    -config "${s%.tla}.cfg" "$s" 2>&1 | tail -3
done
```

---

## References

- `CLAUDE.md` — operational framework + Stratum coordination
- `docs/VISION.md §11` — Relationship to Stratum
- `docs/ARCHITECTURE.md §10.2 + §14` — 9P dialect + integration
- `docs/ROADMAP.md §7` — binding plan
- `docs/handoffs/038-p4-close.md` — Phase 4 close handoff
- `memory/project_phase4_open_boxes.md` — re-scoped §6.2 boxes
- `stratum/v2/docs/OS-INTEGRATION.md` — Stratum's OS-integration manual
- `stratum/v2/docs/reference/20-9p.md` — 9P wire semantics
- `stratum/v2/docs/reference/22-ctl.md` — admin surface
