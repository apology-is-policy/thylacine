# 123 — usr/lib/tls: native TLS for libthyla-rs clients [ABSORBED INTO THE VAULT]

Absorbed at the docs/reference retirement (`chg-2026-09-06-tls-doc-absorb`).
`usr/lib/tls` — the native-userspace TLS (Transport Layer Security) substrate
(NET-DESIGN §9): a thin adapter wrapping **rustls** (pure-Rust TLS) with the
**RustCrypto** `CryptoProvider`, driving the unbuffered rustls state machine over
any blocking stream. Its content lives, code-verified and current, in:

- the **whole TLS adapter** — `TlsStream<S>` / `TlsConn<C>` (the role-generic
  unbuffered connection), `TlsStream::connect(sock, host, cfg)` (the client
  handshake), `client_config` / `load_roots_pem`, and the grow-not-guess staging
  buffer for the no-std unbuffered encode/decode:

      vault/system/userspace/runtime/sub-tls.md   (owns usr/lib/tls)

- the **net clients that consume it** (curl and the other native HTTPS clients):

      vault/system/userspace/tools/sub-net-clients.md

**What this file got WRONG or MISSED by the time it was absorbed:**

- **Nothing load-bearing — a clean zero-fold.** `sub-tls` is the exhaustive home:
  the crate is explicitly a thin adapter, not a cryptographic implementation, and
  the dossier carries the unbuffered-connection contract, the handshake, and the
  root-store loading.
