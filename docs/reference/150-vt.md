# 150 — vt: the shared VT/xterm escape-sequence parser [ABSORBED INTO THE VAULT]

Absorbed at the docs/reference retirement (`chg-2026-09-06-vt-doc-absorb`).
`usr/lib/vt` — the terminal escape-sequence interpreter (bytes in, a `cols x rows`
cell grid out), the SCREEN side of the terminal protocol. A `#![no_std]` + alloc
pure-logic crate shared by aurora (one `Vt` per surface), halcyond (one per raw-VT
pane), and kaua-term (one per tile). Its content lives, code-verified and current,
in:

- the **whole parser** — the escape-sequence coverage (C0, CSI cursor/edit/scroll,
  DECSTBM scroll regions, DECOM origin, SU/SD, DSR/CPR, private modes, the SGR set
  incl. italic/dim/blink/strike + the 22-clears-both rule, OSC), the **TOTALITY**
  contract enforced by construction (CSI params `saturating_mul`/`add`, every grid
  write proven `< cells.len()` by the cursor-containment invariant, clamped repeat
  counts, U+FFFD on malformed UTF-8 — never a panic/OOB/wedge), the **span serial**
  (the monotonic `span_serial` an OSC-1936 frame advances, copied into `Cell.span`
  so a cell recovers its markup without a second Beacon parser), the **OSC 7770**
  in-band settings channel allowlisted twice (control bytes rejected in key and
  value — the cfg-3 F1 laundering guard), and the **wide-glyph model** (`char_width`,
  `ATTR_WIDE` on the left half + a pen-carrying blank right, combining marks zero-
  column, the `cols < 2` single-width degrade):

      vault/system/userspace/shell-tui/sub-lib-vt.md   (audit: light)

- the **untrusted-ingest boundary** (the format-fuzz audit class is here, at the
  consumer that ingests untrusted frames, not the pure-logic parser):

      vault/system/userspace/shell-tui/sub-kaua-term.md   (audit: hard)
      vault/system/userspace/shell-tui/sub-halcyond.md

**What this file got WRONG or MISSED by the time it was absorbed:**

- **Nothing load-bearing — a clean zero-fold onto a current dossier.** sub-lib-vt
  (updated the day before this absorption) carries every atom, including the two
  the audit hardened: the OSC 7770 double-allowlist (control-byte rejection) and
  the `cols < 2` wide-glyph degrade (both pinned as "must stay" caveats there). The
  doc's "governing invariant is TOTALITY / format-fuzz audit class" framing is
  preserved with the right split: totality is the parser's by-construction property
  (sub-lib-vt), while the format-fuzz *audit rounds* run at the untrusted-ingest
  boundary (kaua-term / halcyond, audit: hard). No fold owed.
