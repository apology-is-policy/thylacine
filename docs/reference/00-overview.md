# 00 — Overview [ABSORBED INTO THE VAULT]

Absorbed at the docs/reference retirement (`chg-2026-09-07-overview-doc-absorb`).
This was the bird's-eye index of the `docs/reference/` tree — a Phase-0 scaffold
(2026-05-04) whose per-concern sections ("filled in when X lands") were never
filled and whose figures froze at Phase-1-in-progress (it still read "Test suite
count: 0", "Phase 2 planned"). The as-built bird's-eye view of Thylacine is now
the vault, which is generated from the code rather than hand-maintained:

- **the dashboard** — the live top-level status (coverage, audit tiers, churn):

      vault/dashboard.md

- **the area maps** — one MOC per layer, each linking its dossiers:

      vault/system/kernel/moc-kernel.md
      vault/system/userspace/moc-userspace.md
      vault/system/substrate/moc-substrate.md
      vault/system/boundary/moc-boundary.md
      vault/system/stratum/moc-stratum.md

- **the cross-cutting views** — the concerns this doc scaffolded (the invariant
  registry, code coverage, the roadmap, the open seams), regenerated by `quaestor
  render`:

      vault/views/view-invariants.md        (W^X / I-12, the lock order, KASLR, ...)
      vault/views/view-code-coverage.md
      vault/views/view-roadmap.md
      vault/views/view-seams.md

The per-file retirement status of the old `docs/reference/` tree is tracked in
`docs/REFERENCE.md` (which carries an `ABSORBED INTO THE VAULT` row per file).

**What this file got WRONG or MISSED by the time it was absorbed:**

- **Clean redirect — a stale scaffold, no code, nothing to fold.** Every figure in
  it was frozen at Phase 0 and years out of date; the cross-cutting sections it
  reserved (subordination, W^X, lock order, KASLR, 9P lifecycle) are now
  code-verified in the vault's invariant notes and views. The layer cake it
  sketched as a "target end state" is the shipped system the MOCs describe. Nothing
  was owed a fold; the vault is the source of truth.
