# 145 — The JIT capability (I-42) [ABSORBED INTO THE VAULT]

Absorbed at the docs/reference retirement (`chg-2026-09-07-jit-doc-absorb`).
The W^X-preserving JIT primitive (CL-7k): **dual mapping** — the same physical
pages reachable at an RW writer alias and an RX exec alias, so no single PTE is
ever writable-and-executable and I-12 holds at page granularity *unchanged*.
Making code executable is a deliberate, `CAP_JIT`-gated, region-confined act. Its
content lives, code-verified and current, in:

- **the code Burrow + the dual refcount** — `BURROW_TYPE_CODE` (backing
  byte-identical to ANON; the type is a different *admissibility* the kernel mints
  at create under `CAP_JIT`, not a flag a caller asserts — the G-2 WEAVE discipline
  on the W^X boundary), the one-syscall-both-aliases create, destroy-by-writer-VA,
  and the W^X-clean PTE table:

      vault/system/kernel/memory/sub-kernel-burrow.md   (audit: hard)
      vault/invariants/inv-i12.md                       (W^X the invariant)

- **the I-cache contract** — **now folded**: the CL-7k-3 F1 create-time
  invalidation (a recycled code page carries stale I-cache lines → runs bytes never
  emitted; `sys_jit_create_region` invalidates before any RX PTE names the pages)
  AND the publish-time cross-PE ISB contract (`IC IVAU` broadcasts but the trailing
  `ISB` is calling-PE-only; a peer must take a context-sync event; the direct-map
  maintenance is fault-safe + PIPT-exact):

      vault/system/kernel/memory/sub-kernel-mmu.md   (owns arch_icache_sync_range — folded here)

- **the capability** — `CAP_JIT` elevation-only + non-heritable (I-42 requires
  it), acquirable only through a corvus-mediated scope/time-bounded clearance (no
  spawn-time grant), and the scripture-wording note that "the `CAP_HW_CREATE`
  class" read literally is *wrong* (that cap is fork-grantable):

      vault/system/boundary/registries/abi-caps.md
      vault/system/kernel/security/sub-kernel-caps.md

- **the clearance walk + the client** — the corvus `jit` clearance level, the
  SDL-GL-backend-walks-it-on-the-program's-behalf case (bearer vs SELF form), and
  `libthyla_rs::jit::CodeRegion` (`!Send`/`!Sync`, `publish()` required, BTI-C
  landing pad):

      vault/system/userspace/services/sub-corvus.md
      vault/system/userspace/runtime/sub-libthyla-rs.md

- **the fault-path CODE arm** — the CODE arm of `userland_demand_page` is the ANON
  arm (a new gate, not a new lifetime):

      vault/system/kernel/memory/sub-kernel-fault.md

**What this file got WRONG or MISSED by the time it was absorbed:**

- **One genuine gap, now folded — the I-cache contract.** The dual-map W^X and the
  code-Burrow admissibility were in sub-kernel-burrow, and the scripture-wording
  note in abi-caps + sub-kernel-caps, but the two I-cache-correctness atoms (the F1
  create-time invalidation and the cross-PE `ISB` publish contract) appeared
  *nowhere* — sub-kernel-mmu owned the `arch_icache_sync_range` primitive and only
  named I-42 in passing. Now a proper I-42 entry in sub-kernel-mmu's Invariants
  (`chg-2026-09-07-jit-doc-absorb`).
- **The §8.1 ORC mapper defects are external (the Mesa/LLVM fork).** The
  `writerFor()` assert-compiled-out + `upper_bound()-1` containment miss and the
  `initialize()` phantom-reservation insert (the "changing the container to ordered
  changed what a spurious key costs" lesson) live in the vendored LLVM/Mesa fork,
  not the Thylacine tree — as-built history worth keeping with that fork, not a
  vault surface. `kernel/test/test_jit.c` remains UNOWNED (a pre-existing gap; the
  JIT mechanism is distributed across burrow/mmu/fault/caps with no single primary
  owner to claim the suite — a light follow-up). Zero code change.
