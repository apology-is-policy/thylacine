---
id: adt-348-r1
type: adt
title: "#348 focused round (the blocking s2c server send)"
date: 2026-06-24
scope: [sub-kernel-srvconn, sub-kernel-devsrv]
reviewer: opus
model-start: "claude-opus-4-8"
model-end: "claude-opus-4-8"
verdict: clean
counts: {p0: 0, p1: 0, p2: 1, p3: 3}
findings: [fnd-348-r1-f1, fnd-348-r1-f2, fnd-348-r1-f3, fnd-348-r1-f4]
round-of: chg-2026-06-24-348-s2c-blocking
created: 2026-07-31
---
## Scope

The new `srvconn_server_send_blocking` + `chan_cond_writable` + the
`client_recv` drain-wake + teardown's wrendez wakes + `devsrv_write`'s
server arm + the regression test. Opus-4.8-max holotype-reviewer
(MODEL start==end, no fallback) + a concurrent self-audit.

## Convergence

Both prosecutors independently converged on the I-9 soundness of the
drain-wake (mutate-under-lock then wake-under-rendez-lock; the window
closed both ways) and on the separate-wrendez design making the
single-waiter extinction structurally unreachable (the whole point of
rejecting the shared-rendez EMPTY-xor-FULL sketch). Deadlock-freedom was
verified END-TO-END including the Stratum tree (stratumd's strictly
synchronous serve loop; the kernel elected reader as the unconditional
s2c drainer; #349's self-pump). The one P2 is the second-writer −1
latent — the self-audit flagged the adjacent P3 (non-blocking send
ignores the guard), the formal round correctly elevated the
−1→write_full→mount-close chain to P2; dispositioned as the documented
v1.0 single-writer precondition + task #354 rather than an unverified
wait/wake mechanism on the most-bug-prone lineage ("complexity only
where verified"). In-VM proof at close: the go build's snare:bus GONE;
the build progressed into package compilation, unmasking the #351/#352/
#353 FS-path blockers. SMP gate 40/40, 0 corruption.
