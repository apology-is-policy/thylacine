---
id: adt-957-r1
type: adt
title: "#957 (single-hop mount crossing + devsrv root nav) focused round"
date: 2026-06-09
scope: [sub-kernel-stalk, sub-kernel-devsrv]
reviewer: opus
model-start: "claude-opus-4-8"
model-end: "claude-opus-4-8"
verdict: clean
counts: {p0: 0, p1: 1, p2: 0, p3: 2}
findings: [fnd-957-r1-f1, fnd-957-r1-f2]
round-of: chg-2026-06-09-957-single-hop-cross
created: 2026-08-01
---
## Scope

`stalk_cross_mounts` made public + the two new crosses in
`sys_walk_open_handler` (source + result) + the devsrv
registry-root-as-dir open + `stat_native` (commit `8301e8d7`). One Opus
prosecutor (grounded read-only, every `src`/`nc` exit traced) + a
concurrent self-audit.

## Convergence

The two new crosses VERIFIED lifetime-clean (the
`if (crossed) { clunk(old); X = crossed; }` idiom drops one ref +
adopts one on every path; the crossed clone owns its fid or registry
ref) with X-search/perm ordering mirroring stalk (source-cross before
the src X-search; result-cross before the R/W check + open). The P1
(F1) was PRE-EXISTING, exposed by the new reachability: the handler
treated `Dev.open`'s return as a bool, and devsrv is the only
user-reachable Dev whose open returns a DIFFERENT Spoor — a single-hop
`File::open("/srv/<name>")` leaked the connection endpoint + installed
the spent service-ref. Fixed by adopting open()'s return exactly like
stalk's audited arm. F3 [P3] — a stale `nc->dev == src->dev` comment
after the result-cross (code already correct) — fixed in place, kept in
this body per the doc-only bar. NOT dirty (one localized fix mirroring
an audited pattern). Owed and still open: a deterministic
single-hop-open-of-a-/srv-leaf handler test (the shared user-VA harness
gap; the fix is covered by `stalk.open_replace` parity +
`devsrv.open_connect_byte`).
