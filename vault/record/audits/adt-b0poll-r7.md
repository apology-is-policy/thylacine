---
id: adt-b0poll-r7
type: adt
title: "Poll B-0 round 7 (the preemption point): the spec property could not fail, the tests could not witness, and the bound is attacker-inflatable"
date: 2026-09-22
scope: [sub-kernel-poll, sub-kernel-sched, spec-poll, spec-poll-cpu]
reviewer: opus
model-start: "claude-opus-5"
model-end: "claude-opus-5"
verdict: clean
counts: {p0: 0, p1: 0, p2: 3, p3: 4}
findings: [fnd-b0poll-r7-f1, fnd-b0poll-r7-f2, fnd-b0poll-r7-f3, fnd-b0poll-r7-s4]
round-of: chg-2026-09-22-poll-preemption-point
prior-round: adt-b0poll-r6
created: 2026-09-22
---
## Scope

Branch `browser-b0`: the scripture, spec, code and test commits for the
preemption point (a538df28, 9d8319b5, fbd96fa0, 70cf9ba6, ccc52c5e). FALLBACK
tier. The round reproduced TLC 12/12, DISASSEMBLED `sched.c` to measure
compiler ordering (the `preempt_count` store lands before the window; the
decrement is a true reload), and measured stack depth with `-fstack-usage`
(~5.9 KiB peak of 16 KiB). 34 items verified sound, including `NoSpuriousZero`
after the `sleep_dl` conjunct was removed, and the SAK chain end to end.

## Convergence

Clean by the dirty-close criteria, and the three P2s are all failures of
VERIFICATION rather than of the mechanism: a property satisfied by the thing it
was written to exclude ([[fnd-b0poll-r7-f2]]), tests that could not witness the
window they were named for ([[fnd-b0poll-r7-f3]]), and a latency claim resting
on an unbounded walk ([[fnd-b0poll-r7-f1]], qualified in ARCH and tracked). The
four P3s: the point's `need_resched_clear` could clear a foreign CPU's flag and
swallow the #866-F1 placement kick (dropped; `sched()`'s own entry clear does
it under a mask); the count RMW bypassed the #360 chokepoint (routed through
`spin_preempt_inc`/`dec`, with the contract half -- that the guard cannot see a
mask-only region -- documented rather than fixed); six stale sentences,
including a `vault/specs/spec-poll.md` never regenerated by the three commits,
a real doc-update-per-PR miss; and the TIMEDOUT pass crossing the point, which
could defer a return past its own deadline and also diverged from the model's
`FinalSample` path.

A fourth item arrived AFTER the round, from running the sabotages the F3 fix
asked for: [[fnd-b0poll-r7-s4]].
