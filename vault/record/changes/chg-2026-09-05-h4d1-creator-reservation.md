---
id: chg-2026-09-05-h4d1-creator-reservation
type: chg
title: "H-4d-1: a peer-split empty leaf is reserved to its creator conn until the conn goes; the session compositor hosts a tagged leaf as a terminal tile; the restore tool defers to a present compositor; print!/println! write one statement per syscall"
date: 2026-09-05
arc: arc-tapestry
commits: ["c96f5173"]
touched: [sub-tapestryd, sub-libthyla-rs]
established: []
closed: []
opened: []
mirrors-checked: []
depth: skeletal
no-dossier-change: "sub-tapestryd's delta is one Pane field (creator_conn) + the E_AGAIN arm on the claim mint + the release fan at retire_conn + the menu role/verbs admitting the declared session; the vault peer folds it from this record and docs/reference/139-tapestryd.md (the creator-reservation paragraph). libthyla-rs's delta is the Stdout/Stderr write_fmt override + the println!/eprintln! one-write form. halcyond/halcyon are unowned (150/151 carry the prose)."
created: 2026-09-05
---
The H-4 arc's fourth chunk, first sub-chunk (HALCYON.md 13.7 AS-BUILT: "who hosts a tagged leaf under the session compositor"). The claim race H-4c left owed -- the restore tool's splits vs the same-principal compositor filling every empty it owns, last mint wins -- cannot be closed by any mark made after the split (a tag, a claim: the compositor's reconcile runs per TEV_LAYOUT, per split). The split itself marks the leaf: `Pane.creator_conn` (stamped on both empties by a ctl split; a chord split stamps none); the claim mint answers E_AGAIN to every other conn of the principal while the creator lives; `retire_conn` releases and fans ONE TEV_LAYOUT to the declared session (no geometry changes at a release). rio's rule: a window a program creates is that program's, not the menu's. The second decision the race forced: under a session the compositor is the user's rio, so an empty leaf's tag is the tile's command line (`kaua-term cols rows <argv>`, `libhalcyon::tag` shared with the tool); the tool, seeing the compositor's /env mark, tags and exits. Two placement rules give the welcome its shape without touching the bootstrap: a saved root whose last child is the one env leaf puts the built part before the anchor (`move` per step, dump-checked); `active` on that leaf focuses the anchor. Gates: panes, restore, test.sh, session (the rc leg's reservation + release witnesses; first attempt after the gate's own arm-order and torn-line fixes -- libthyla-rs's print!/println! wrote one syscall per format fragment, and the console's writer role is per write). Unaudited by the double-the-distance rule.
