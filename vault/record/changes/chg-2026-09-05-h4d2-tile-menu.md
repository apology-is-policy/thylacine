---
id: chg-2026-09-05-h4d2-tile-menu
type: chg
title: "H-4d-2: the obj verb menu inside a session tile -- the Normal-mode port + the cell-span model it needed (vt::Cell.span, Osc1936Raw { serial, frame }, SpanMap, the obj copied into the landing block, the live grid as the virtual trailing block) + the kaua-term Text down-record"
date: 2026-09-05
arc: arc-tapestry
commits: ["946ac379"]
touched: [sub-halcyond, sub-kaua-term, sub-lib-vt, sub-substrate-build]
established: []
closed: []
opened: []
mirrors-checked: []
depth: skeletal
no-dossier-change: "sub-halcyond's delta: `SessionTile` gains Mode / flat + Sel / scroll_up / ptr, `normal_input` (the console's Normal keys minus yank/paste; Esc enters Normal only on the VT's normal screen), `click`, the summon at display coordinates through the H-3c-2 MenuSet on the session's ring, a choice typed into the tile it was opened over as ONE Input::Text `^E^U<cmd>\\n`; `Tile.frame` (the last render's block placement, `Tile::hit`) + `Mark` + `render(.., &mut scroll_up, Option<Mark>)` (the view follows the cursor; the band + the ember underline); `layout::laid_line_for` shared with the console bin. sub-kaua-term: `Input::Text(Vec<u8>)` (tag 2) written to the master verbatim under the key lock. The vault peer folds these from this record + docs/reference/150-halcyond.md ('The menu in a session tile') + 152-kaua-term.md (the Text record) + docs/KAUA-TERM.md. ALSO (the cell-span model): sub-lib-vt: `Cell.span: u32` + `Vt.span/span_serial` (advanced on a 1936 OSC in capture mode; a numeric selector, no body read) + `Boundary::Osc { serial, body }`. sub-kaua-term: `Control::Osc1936Raw { serial, frame }`, the 17-byte wire cell, `Input::Text` (H-4d-2). sub-halcyond: `SpanMap` (8192-ring serial -> `SpanTag {block, obj, em, hdr}`, noted after each fed frame), `push_scrolled_rows(rows, &spans)` + `local_obj` (the obj copied into the landing block), `Transcript::{span_tag, block_by_id, obj_in_block}`, `Tile::{grid_runs, grid_run, grid_run_obj, grid_hit}` + the GRID_KEY render arms, `select::{GRID_BLOCK, flatten_with_grid}`, `menu::step_run_with`, the session's grid-aware Normal mode. sub-substrate-build: the corvus-mint host build now runs from the repo root (cargo's config discovery is cwd-based; from usr/ the vendor replacement applies and its aegis 0.9.8 cannot satisfy the tool's 0.9.12 lock -- a bake launched from usr/ failed there). The vault peer folds these from this record + docs/HALCYON.md 14.11.4/14.11.5 AS-BUILT, docs/KAUA-TERM.md (the wire cell), docs/reference/150-vt.md (the span serial), 152-kaua-term.md (the cell span), 150-halcyond.md ('The grid as the virtual trailing block')."
created: 2026-09-05
---
The console renderer's Helix-modal transcript + obj verb menu (H-3c) ported into the per-user session compositor's tiles (HALCYON 14.12: the compositor is the user's rio and reuses the menus; H-4d-1 admitted the declared seat). The only new wire is the Text record: a chosen verb's command line rides the bounded down-queue whole or not at all, never half a command; the `^E^U` prefix (SA-8) moves a half-typed draft to ut's kill buffer instead of running into it. The modal gate is the VT's screen mode (a full-screen app on the alt screen owns Esc); a cooked-mode non-shell program in the tile does receive the typed bytes, bounded by the user having chosen a verb over their own tile. Host test added for the frame / hit / mark mechanics the draft had left unwitnessed. Gate: ls-gfx-session's menu leg (`ls /lib/halcyon` -> objects; Esc, b, Enter -> the menu placed by the SESSION compositor; Enter -> `menu ran: ls -l -- '<path>'`). Unaudited by the double-the-distance rule (the H-arc round; AUDIT-TRIGGERS row 142 carries the prosecution notes).

The H-4d-2 port (the console renderer's Normal mode + obj menu into session tiles) landed HOLLOW on its first instrumented gate: Esc entered Normal mode with zero rows. A tile's content is cells -- the live grid, and scrolled-off rows interned with no obj -- so the console's row/run machinery saw nothing, and the `ls` output was still on the 36-row grid anyway. HALCYON 14.11.4/14.11.5 had specified both missing halves (the frames drive the same span state; the grid is a virtual trailing block). The association is made where the cells are WRITTEN: halcyond cannot stamp its grid (the producer's post-scroll diff rewrites nearly every cell and would wipe the stamp), so the vt stamps every cell with the serial of the last Beacon frame it forwarded, the record carries the serial explicitly (never counted at both ends -- a dropped frame must not shift later cells onto the wrong span, the anti-clickjack class), and halcyond maps a serial to the span state after feeding that frame, copying an obj into the landing block when the grid straddled a zone cut so every block stays self-contained. Genera/CLIM-shaped: presentations are recorded on the output stream at output time. Host tests: vt 46 (+1), kaua-term 30, halcyond 106 (+1). Unaudited by the double-the-distance rule (the H-arc round; AUDIT-TRIGGERS row 142's 2b addendum).
