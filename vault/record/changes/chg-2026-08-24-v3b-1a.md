---
id: chg-2026-08-24-v3b-1a
type: chg
title: "V-3b-1a HOST3D+MAP_BLOB substrate: sub-tapestryd mechanism section + the venus-ctx finding"
date: 2026-08-24
arc: arc-tapestry
commits: ["ace487f7", "95160d9b"]
touched: [sub-tapestryd, seam-warp-prove-unowned]
established: []
closed: []
opened: []
mirrors-checked: []
depth: skeletal
created: 2026-08-24
---
The V-3b-1a HOST3D + MAP_BLOB substrate -- tapestryd's `create_host3d_blob` /
`map_blob` / `unmap_blob` in `gpu.rs` plus the `host3d_probe` 2-arm init
self-test -- landed on main (`ace487f7`, hash-fixup `95160d9b`) with a CLEAN
holotype (Opus 4.8 fallback, Fable out on 4x 529; 0 P0 / 0 P1 / 0 P2 / 2 P3,
both P3s fixed). This pass rings it into the vault. [[sub-tapestryd]] gains a
"HOST3D map-blob substrate" mechanism section: the three wire shapes (create
HDR+32 `nr_entries=0`, map HDR+16 -> `hostmem_base+offset` + `RESP_OK_MAP_INFO`
0x1106, unmap HDR+8), the residue guard, the empirical venus-context
requirement ([[inv-i45]]: a HOST3D `blob_id=0` `USE_MAPPABLE` blob is the vkr
shm path, served ONLY under a capset-4 context -- a virgl-ctx / device-global
create refused; proven on GL, thyla-pi KVM/V3D), the 2-arm probe (venus-ctx
POSITIVE / device-global NEGATIVE control), and the thyla-pi
`virgl_render_server` provisioning note (Debian ships none; without it get_blob
EINVALs silently). The edge worth pinning: the venus gate tools
(`tools/warp-host.sh` venus-verdict + `tools/test-venus-verdict.sh`) are
`quaestor owner`-UNOWNED test-infra, the same class as `usr/warp-prove` -- so
[[seam-warp-prove-unowned]] is broadened to cover them (same routing decision:
test-infra documents itself where it runs, no dossier) rather than minting a
sibling seam. `gpu.rs`'s edit is the vault-owned surface; the tools stay
self-documenting.
