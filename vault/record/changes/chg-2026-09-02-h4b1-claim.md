---
id: chg-2026-09-02-h4b1-claim
type: chg
title: "H-4b-1: the one-shot placement claim (pane/<id>/claim + create claim=) lands in tapestryd"
date: 2026-09-02
arc: arc-tapestry
commits: ["4bb6048c"]
touched: [sub-tapestryd, sub-warden, sub-stratum-boot, seam-tapestry-battery-unowned]
established: []
closed: []
opened: [seam-tapestry-battery-unowned]
mirrors-checked: []
depth: skeletal
created: 2026-09-02
---
H-4b-1 lands the placement primitive HALCYON.md 13.7 names: `pane/<id>/claim`
mints a one-shot 128-bit token on an EMPTY leaf (the offset-0 read, pinned to
the fid; last mint wins) and `create W H claim=<32hex>` hosts the new surface
into that leaf via `Layout::host_into` (never splitting, never moving focus),
FALLING BACK to focus placement when the token names no live empty leaf -- the
child's opaque cookie must never fail its create. NOT audit-closed here: the
H-4b arc batches its holotype at the arc close. [[sub-tapestryd]] gains the
mechanism section, a Seams bullet (the mint is any peer's until
`owner_principal` at split narrows it, H-4b-2) and the Tests pointer (the 23rd
ls-gfx-panes leg). Three edges worth pinning. (1) The first cut failed LOUD
(E_INVAL) on a stale token; the pre-audit self-read caught it as a scripture
deviation (13.7 says fall back) before landing. (2) The first E2E run failed
on the new leg -- `first mint unreadable` -- and the hunt proved by code that
tapestryd cannot call `SYS_GETRANDOM`: the warden confers only
`CAP_HW_CREATE`, and `CAP_CSPRNG_READ` gates the syscall. The fix
(operator-chosen over reading the world-rw `/dev/random` or a DRBG seeded from
`AT_RANDOM`) is a manifest cap grant: `caps = ["csprng"]` on tapestryd's
manifest (libdriver's closed `Cap` vocabulary, fail-closed on unknown +
duplicate), the warden maps it to the bit at spawn ([[sub-warden]]), and joey
adds the bit to the warden's own mask ([[sub-stratum-boot]]; I-2: a monotone
joey -> warden -> driver chain). The mint's E_IO path now `say!`s. (3) Found
in passing, NOT fixed: `/dev/random` is world-rw while the syscall is
cap-gated -- two gates of different strength on the CSPRNG axis; carried as a
session memory item for the A-4 sweep. The battery + expect scenario are
UNOWNED test-infra (the prover's class): [[seam-tapestry-battery-unowned]]
records the routing.
