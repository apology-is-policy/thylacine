---
id: chg-2026-06-02-stalk2
type: chg
title: "stalk-2: the mount-table re-key + Plan 9 domount crossing"
date: 2026-06-02
arc: arc-identity-detour
commits: ["e291b74d", "c185186c"]
touched:
  - sub-kernel-stalk
established: []
closed: []
opened: []
mirrors-checked: []
depth: skeletal
---
The mount table re-keyed from `path_id_t` to the full Plan 9 mount-point
identity `(dc, devno, qid.path)` — the `devno` axis is load-bearing
(every dev9p session shares `dc='9'` + root `qid.path 0`, so two
concurrent sessions' mount points collide without it; `Spoor.devno`
minted per attach by `spoor_next_devno`). `cross_mounts` (Plan 9
`domount`): cross-on-descent + the quarry cross + the `STALK_MOUNT`
no-cross-final amode so MREPL re-keys the same point; path-keyed
`SYS_MOUNT`/`SYS_UNMOUNT`; devramfs synthetic `/srv`+`/proc` mount
points. The close commit landed the audit's F1: I-3 cycle-freedom
ENFORCED at `mount()` (`would_create_mount_cycle`) — the "by
construction" claim had been false ([[fnd-stalk2-r1-f1]]).
[[adt-stalk2-r1]] 0/0/1/2.
