---
id: fnd-294-r1-f3
type: fnd
title: "Claimed: netd must cancel a parked readiness Tread on the ready-fd clunk (already handled)"
round: adt-294-r1
severity: P3
status: withdrawn
surface: [sub-netd-server]
threatens: []
created: 2026-07-31
---
## Prosecution

Flagged out-of-scope during the #294 cancel-at-close round: when the
kernel delivers the ready-fd's Tclunk (the #294 deterministic
slot-free), a readiness Tread still parked on that fid would be
stranded server-side unless netd cancels it at clunk.

## Disposition

WITHDRAWN — verified already handled at the round, and re-verified at
this sweep against current code: `fid_clunk` retains-out every
pending keyed to the clunked fid (`pending_ready` alongside
`pending_reads`/`pending_weftio`/`pending_connects` + the accept and
query cancels), so no parked probe survives its fid. The residual
neighbor that IS real — a cancelled pending's held tag is dropped
without a terminal reply on the clunk/Tversion paths — is the separate
[[seam-56-netd-cancelled-tag]]. (This note discharges the batch-1
promise recorded in [[adt-294-r1]]'s body.)
