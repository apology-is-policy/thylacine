---
id: view-closed-sub-kernel-larder
type: view
title: "Do-not-re-report preamble — sub-kernel-larder"
query: closed:sub-kernel-larder
---
# Do-not-re-report preamble — sub-kernel-larder

Generated from `fnd-*` notes (`quaestor render`; also emitted
on-demand by `quaestor closed sub-kernel-larder`). Paste or
transclude into a prosecutor prompt as the closed-findings preamble.

<!-- generated:begin -->
8 closed findings on [[sub-kernel-larder]] — do NOT re-report
these in a future round (open/deferred findings are NOT listed
here; see the seam inbox):

- [[fnd-25-r1-f3]] [P3] larder_page_invalidate is O(page_cap) — a full-array scan per own-write (fixed) — Dispositioned deferred at the round; CLOSED before the keeper committed
- [[fnd-29-r1-f2]] [P3] The dentry whole-parent invalidate scan is O(dentry_cap) — the same class just fixed for pages (fixed) — Dispositioned at the round as fixed-by-justification (a measured-
- [[fnd-29-r1-f3]] [P3] The multifile regression proved page_hash relink but never qhash relink after slot reuse (fixed) — Fixed in-round: the test gained a second `larder_page_invalidate` after
- [[fnd-29-r1-f4]] [P3] Stale test-file comment described the retired 256-entry inline shape (fixed) — Fixed in-round: the comment updated to the heap-lazy 4096/4096/32768
- [[fnd-b1-r1-f1]] [P2] A third-actor stale-fid repopulate re-satisfies snapshot coverage at the OLD cvers — a torn cached-open view (fixed) — Fixed: `larder_pages_snapshot` takes the caller's pre-decision gen
- [[fnd-d44-r1-f2]] [P3] The attr-served EOF extends close-to-open to the EOF determination (external-writer window) (documented) — Documented — within-premise sound, and EXACTLY the same close-to-open
- [[fnd-l1f-r1-f1]] [P1] create at a reused ino drops the child's ATTR but not its PAGES — a stale prior-occupant page can serve (fixed) — Fixed in the arc-close commit: `larder_page_invalidate` of the child
- [[fnd-term2-r1-f3]] [P3] The uncommitted DIAG23 working tree re-added the dead whole-parent scan + a stale header comment (fixed) — Fixed by stripping the instrument to clean HEAD before the close — the
<!-- generated:end -->
