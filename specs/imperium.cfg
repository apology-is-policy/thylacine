\* Imperium: the fork-PROPAGATING legate scope (I-25 STRENGTHENED) -- the CLEAN
\* model. Spec-first RE-ENABLED for this surface (user-directed 2026-09-07).
\* TLC proves: an alive elevated Proc always has a live root in the scope it
\* first joined (incl. the fork-vs-teardown race, the killed-root window and
\* the expiry sweep); caps flow only under a propagating scope and never wider
\* than the set the Proc joined with; a Proc's scope, propagating property and
\* flowing set are set once; a forked child is never a root; a scope agrees on
\* whether it propagates; a member never widens what flows; and once a root is
\* dead every member eventually dies. Expected: TLC-green.
CONSTANTS
    Procs     = {p1, p2, p3}
    Caps      = {c1, c2}
    MaxScopes = 3
    BUGGY_STRAGGLER         = FALSE
    BUGGY_RETAG             = FALSE
    BUGGY_FLOW_WITHOUT_FLAG = FALSE
    BUGGY_NEST_ADMITTED     = FALSE
SPECIFICATION Spec
INVARIANT Safety
PROPERTY ScopeEventuallyEmpty
