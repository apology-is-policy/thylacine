\* sched_rebalance_buggy_nokick.cfg -- BUGGY: no busy-side overload kick (the
\* TI-3 regression in model form). With Overload disabled, a non-yielding
\* producer's surplus queued work strands forever while a peer sits idle --
\* exactly what removing the 1 kHz pull-re-poll did. Expect: Eventually
\* Parallelized VIOLATED (surplus[producer] holds forever; an idle peer never
\* takes it).
CONSTANTS
    CPUs = {c1, c2}
    BUGGY_NO_KICK = TRUE
    BUGGY_KICK_NO_LIFT = FALSE
SPECIFICATION Spec_Live
PROPERTIES
    EventuallyParallelized
