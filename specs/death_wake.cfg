\* HOLOTYPE RW-2 SA-1 -- the death-wake cascade, CORRECT register-then-observe.
\* No death-wake is lost (I-9 generalized); the Proc reaps exactly once and no
\* Thread runs at EL0 after ZOMBIE (I-24); and once group termination starts the
\* Proc eventually reaps (the liveness witness that the hang cannot occur).
\* Expected: TLC-green.
CONSTANTS
    Threads = {t1, t2, t3}
    BUGGY_OBSERVE_BEFORE_REGISTER = FALSE
SPECIFICATION Spec
INVARIANT Safety
PROPERTY EventuallyReaps
