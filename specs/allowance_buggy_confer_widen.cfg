\* Counterexample: an intersection bug grants a superset. The warden's
\* node-INTERSECT-manifest computation is wrong and confers an allowance
\* NOT a subset of the bound node's resources -- a grant past the device.
\* Expected: TLC reports ConferredWithinNode violated
\* (BuggyConferWiden with A not a subset of N, e.g. N = {} and A = {r1}).
CONSTANTS
    Drivers   = {d1}
    Resources = {r1, r2}
    BUGGY_COMMIT_NO_RECHECK     = FALSE
    BUGGY_REVOKE_LEAVES_HANDLES = FALSE
    BUGGY_CONFER_WIDEN          = TRUE
    BUGGY_SELF_WIDEN            = FALSE
SPECIFICATION Spec
INVARIANT Safety
