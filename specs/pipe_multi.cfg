\* Multi-waiter clean run: THREE threads, so two can sleep on one side of
\* the pipe at once (the fork/dup/thread-shared endpoint shape). All buggy
\* flags FALSE; wake-all keeps NoStuckReader / NoStuckWriter under
\* re-sleeping.

SPECIFICATION Spec

CONSTANTS
    Threads                              = {t1, t2, t3}
    CAP                                  = 2
    BUGGY_WRITE_NO_WAKE_READER           = FALSE
    BUGGY_READ_NO_WAKE_WRITER            = FALSE
    BUGGY_CLOSE_WRITE_NO_WAKE_READER     = FALSE
    BUGGY_CLOSE_READ_NO_WAKE_WRITER      = FALSE
    BUGGY_WAKE_ONE_READER                = FALSE

INVARIANTS
    Invariants
