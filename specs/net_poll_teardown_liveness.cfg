\* Clean (the FIX): the netd slot is eventually freed WITHOUT any kthread
\* fairness -- the user's fd-close delivers the `ready`-fd Tclunk deterministically.
SPECIFICATION Spec

CONSTANTS
    Fix = TRUE

PROPERTIES
    Liveness
