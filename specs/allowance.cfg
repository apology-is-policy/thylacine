\* Menagerie hardware allowance (I-34) -- the CLEAN model. Spec-first
\* RE-ENABLED for this surface (user-voted 2026-06-15). TLC proves the four
\* legs of I-34 (handles within the live allowance incl. the revoke-vs-create
\* race; allowance never widened past the grant; grant within the bound node;
\* fully revoked on teardown) AND the liveness witness that the re-check gate
\* cannot wedge an in-flight SYS_*_CREATE. Expected: TLC-green.
CONSTANTS
    Drivers   = {d1, d2}
    Resources = {r1, r2}
    BUGGY_COMMIT_NO_RECHECK     = FALSE
    BUGGY_REVOKE_LEAVES_HANDLES = FALSE
    BUGGY_CONFER_WIDEN          = FALSE
    BUGGY_SELF_WIDEN            = FALSE
SPECIFICATION Spec
INVARIANT Safety
PROPERTY EventuallyResolves
