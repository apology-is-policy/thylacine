\* BUGGY (the CURRENT deferred-pin design): safety holds (no UAF, no double
\* slot_unref) but Liveness FAILS -- a stranded op whose kthread GC never fires
\* (no WF on KthreadGc) leaves the `ready`-fd Tclunk undelivered forever: the
\* permanent netd slot LEAK (#294). TLC reports the Liveness counterexample.
SPECIFICATION Spec

CONSTANTS
    Fix = FALSE

INVARIANTS
    SafetyInvariants

PROPERTIES
    Liveness
