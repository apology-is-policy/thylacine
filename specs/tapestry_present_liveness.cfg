\* Tapestry (T-1) -- the LIVENESS witness. Spec_Live (weak fairness on the
\* drain actions: Complete, ClunkMap, Free). EventuallyRetired: a destroy
\* always drains to full teardown -- every generation reaches gone/none; no
\* weave is stranded retiring behind an in-flight present or a held mapping.
\* The temporal proof that "retire only after quiesce" does not deadlock the
\* teardown. Expected: TLC-green.
SPECIFICATION Spec_Live

CONSTANTS
    ALLOW_DESTROY            = TRUE
    ALLOW_REWEAVE            = TRUE
    BUGGY_EARLY_FREE         = FALSE
    BUGGY_RETIRE_NO_QUIESCE  = FALSE
    BUGGY_REWEAVE_NO_QUIESCE = FALSE
    BUGGY_STALE_MAP          = FALSE

INVARIANTS
    Invariants

PROPERTIES
    EventuallyRetired
