\* PTY stop (round-1 F6) -- the LIVENESS witness. DeathWinsOverJobStop: once a
\* group-terminate is requested, the group always eventually dies, EVEN from a
\* stop (the death-wake reaches a stopped thread; #811). WF(GroupDie). Green.
SPECIFICATION Spec_Live

CONSTANTS
    BUGGY_DOUBLE_STOP   = FALSE
    BUGGY_DEATH_BLOCKED = FALSE

INVARIANTS
    Invariants

PROPERTIES
    DeathWinsOverJobStop
