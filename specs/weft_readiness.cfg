\* Weft readiness ring (Weft-4) -- the CLEAN model. The Shenango single-cache-
\* line readiness poke; the no-lost-wake is the STORE-BUFFER register-then-
\* observe (I-9 across the shared-memory poke). Spec-first RE-ENABLED for Weft
\* (NET-THROUGHPUT.md section 5.5). TLC proves: a consumer is never parked with
\* an unprocessed readiness edge (the correct register-then-observe makes the
\* producer's edge wake any armed consumer). MaxSeq = 2 exercises park -> wake
\* -> park -> wake. Expected: TLC-green.
SPECIFICATION Spec

CONSTANTS
    MaxSeq                    = 2
    BUGGY_OBSERVE_BEFORE_ARM  = FALSE

INVARIANTS
    Invariants
