\* PTY stop composition (I-20 stop leg; round-1 F6) -- the CLEAN model. Proves
\* StopCompatI39: a debug stop stays until its OWN resume (a job resume never
\* clears it), composed with the cook->stop trigger + death. Expected: green.
SPECIFICATION Spec

CONSTANTS
    INPUT               = 1
    BUGGY_DOUBLE_STOP   = FALSE
    BUGGY_DEATH_BLOCKED = FALSE

INVARIANTS
    Invariants
