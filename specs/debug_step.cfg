\* debug_step.cfg -- CLEAN: die-check-FIRST tail, the SS EC returns to the tail
\* after exactly one instruction, and a step re-entry runs the die-check. A step
\* executes exactly one EL0 instruction (StepExactlyOne); death always completes
\* even against a step in flight (EventuallyAllDead); a non-racing step re-parks
\* (StepEventuallyReparks). Expected: TLC-green.
CONSTANTS
    Threads = {t1, t2}
    BUGGY_STEP_RUNS_FREE = FALSE
    BUGGY_STEP_DEATH_LOST = FALSE
SPECIFICATION Spec
INVARIANT Safety
PROPERTIES
    EventuallyAllDead
    StepEventuallyReparks
