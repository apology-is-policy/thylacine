\* Counterexample: the live allowance grows past the conferred grant (a
\* kernel bug where the per-Proc allowance set is mutable after the warden's
\* confer, or a stale/widened copy is consulted at the gate). A resource the
\* warden never conferred becomes grantable -- the "never widened" leg fails.
\* Expected: TLC reports AllowanceWithinConferred violated
\* (Confer -> BuggySelfWiden adds a resource not in conferred).
CONSTANTS
    Drivers   = {d1}
    Resources = {r1, r2}
    BUGGY_COMMIT_NO_RECHECK     = FALSE
    BUGGY_REVOKE_LEAVES_HANDLES = FALSE
    BUGGY_CONFER_WIDEN          = FALSE
    BUGGY_SELF_WIDEN            = TRUE
SPECIFICATION Spec
INVARIANT Safety
