\* debug_step_buggy_runs_free.cfg -- the SS EC never re-traps to the tail, so a
\* stepped Thread keeps executing at EL0 (win grows past 1). Violates
\* StepExactlyOne (a `step` runs the target free) -- the SPSR.SS/MDSCR.SS
\* stuck/missing-re-trap family. Expected: TLC finds a StepExactlyOne violation.
CONSTANTS
    Threads = {t1, t2}
    BUGGY_STEP_RUNS_FREE = TRUE
    BUGGY_STEP_DEATH_LOST = FALSE
SPECIFICATION Spec
INVARIANT Safety
