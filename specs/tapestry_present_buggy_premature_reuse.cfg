\* The D1 recycle-gate counterexample: SubmitEarlyFree recycles the slot at
\* submit-ack instead of at the present's terminal CQE. The client's next
\* Draw scribbles a slot the host is still TRANSFER_TO_HOST_2D-reading --
\* the torn frame (content torn; the lifetime analog is
\* retire_during_transfer).
\* Expected: TLC reports RecycleGate violated
\* (WeaveFirst -> Map -> Draw -> SubmitEarlyFree -> Draw: the slot is
\* "drawn" with a transfer still in flight on it).
SPECIFICATION Spec

CONSTANTS
    ALLOW_DESTROY            = FALSE
    ALLOW_REWEAVE            = FALSE
    BUGGY_EARLY_FREE         = TRUE
    BUGGY_RETIRE_NO_QUIESCE  = FALSE
    BUGGY_REWEAVE_NO_QUIESCE = FALSE
    BUGGY_STALE_MAP          = FALSE

INVARIANTS
    RecycleGate
