\* PTY master/slave atomicity (I-20) -- the CLEAN model. Spec-first RE-ENABLED
\* for this surface (docs/PTY-DESIGN.md P4 -- the 7th instance of re-enabling
\* point (a), after Tapestry's T-1). TLC proves the four legs of I-20:
\* (1) no byte lost/torn/duped across the cook + at teardown (RingConserved +
\* SignalXorByte); (2) a cooked signal char raises exactly one signal to
\* exactly the fg pgrp, not-also-a-byte (SignalToFgOnly + SignalXorByte);
\* (3) no lost wake per endpoint (NoStuck*Reader -- the pipe.tla shape);
\* (4) teardown drains-then-EOF + SIGHUP-exactly-once (RingConserved +
\* HupAtMostOnce); plus the P3/I-39 stop composition (StopCompatI39).
\* Expected: TLC-green.
SPECIFICATION Spec

CONSTANTS
    CAP                       = 2
    INPUT                     = 2
    ECHO                      = TRUE
    BUGGY_SIGNAL_ALSO_BYTE    = FALSE
    BUGGY_LOST_TEARDOWN_BYTE  = FALSE
    BUGGY_DOUBLE_STOP         = FALSE
    BUGGY_SIGNAL_WRONG_PGRP   = FALSE

INVARIANTS
    Invariants
