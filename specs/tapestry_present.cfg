\* Tapestry present/recycle/reweave (T-1) -- the CLEAN model. Spec-first
\* RE-ENABLED for this surface (TAPESTRY.md section 18.8, V3 -- the sixth
\* instance of re-enabling point (a)). TLC proves: (1) T-1 -- a weave's pages
\* stay backed while any present is in flight on it and while scanout
\* composition references it; (2) the D1 recycle gate -- no draw into a slot
\* the host still reads; (3) the #847 dual-refcount -- the client mapping
\* keeps pages alive; GONE requires both sides done; (4) the reweave path
\* retires the old weave only after the displayed switch + quiesce; (5) the
\* consume-once claim never resolves stale. Expected: TLC-green.
SPECIFICATION Spec

CONSTANTS
    ALLOW_DESTROY            = TRUE
    ALLOW_REWEAVE            = TRUE
    BUGGY_EARLY_FREE         = FALSE
    BUGGY_RETIRE_NO_QUIESCE  = FALSE
    BUGGY_REWEAVE_NO_QUIESCE = FALSE
    BUGGY_STALE_MAP          = FALSE

INVARIANTS
    Invariants
