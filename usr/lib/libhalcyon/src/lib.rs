// libhalcyon -- the Halcyon environment library (HALCYON.md section 13).
//
// H-3a: the `theme` module. Everything else in the family (chrome helpers, the
// verbs engine, layout save/restore) arrives in later H-3/H-4 chunks.

#![no_std]

extern crate alloc;

pub mod layout;
pub mod theme;
